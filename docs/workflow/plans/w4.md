# w4 — plan file

Working state for the item whose contract is `docs/workflow/w4.md` (split shape,
`docs/workflow/README.md`); deleted at delivery, deletion sha recorded in the
contract's `## Delivery`.

## Decisions

| ID | Stage | Decision | Why (one sentence) |
|---|---|---|---|
| w4-D-1 | req | The KG + retrieval prototype runs over a **self-contained seeded sample**, not the live records-service or production PHI tables (owner ask, 2026-08-16) | the client scoped "keep the KG to a seeded sample"; a prototype that never binds to real PHI stays out of the `docs/landmines.md` §1 PHI-column zone and can carry the ownership check without an approval-gated auth change |
| w4-D-2 | req | "Multi-agent" retrieval collapses to a **single agent** unless assembly genuinely needs more (owner ask, 2026-08-16) | the client stated `'multi-agent' ~= single agent if single agent is sufficient`; requirements bind the assembled-view outcome and the boundary check, not the agent count — an architecture call deferred to spec/plan |
| w4-D-3 | req | The search-corpus bound (`records.search` LIKE-escape + LIMIT) is **not** w4's — it is e6's (e6-D-3) | e6 already carries E6-REQ-1/2/8 for the unbounded read; w4 closes only the session→patient bind so the two items do not fight over one fix surface |
| w4-D-4 | req | w4 stays **analysis-plus-prototype**: the live D11 session→patient bind on the production route is deferred, not landed this week (owner 2026-08-16, w4-Q1) | the client named the security finding as the headline, and the live fix is approval-gated §1 auth blocked on TODO-20 (no patient principal) — deferring keeps the planted defect visible per `CLAUDE.md` §0 rather than silently closing an exercise |
| w4-D-5 | req | N+1 is a **note plus a fast prototype**; the production records-service route is not refactored (owner 2026-08-16, w4-Q2) | "make it feel fast" is satisfied inside the seeded substrate (w4-REQ-7); the live-route N+1 is a design change on a PHI read path that e6 also holds out-of-scope |
| w4-D-6 | req | **No rendered patient-view UI** this week; the "one view" is the prototype's assembled result (owner 2026-08-16, w4-Q3) | a patient-facing surface is blocked by TODO-20 (patient auth, a `config/roles.yaml` principal, and origin separation per `adr/0015` §4) — none in this week's deliverable |
| w4-D-7 | req | Never-expiring session is a **writeup finding only**; no session-expiry code change (owner 2026-08-16, w4-Q4) | session TTL is approval-gated §1 auth and `auth.yaml` is declarative-only (the behaviour is hardcoded in `security.py`), so the fix is a separate scheduled auth item, not this deliverable |
| w4-D-8 | spec | Each writeup/finding requirement carries a `gate:` adequacy row **and** a separate `cmd:` presence row (SPEC-17/18/19) (owner 2026-08-16) | prose adequacy — right enablers named, exposure sized — is a human read, but artifact presence and required-citation are mechanically checkable; one mechanism per row means the presence check is its own row |
| w4-D-9 | spec | The prototype's caller→patient binding is a **seeded/simulated principal**; no live gateway-session integration (owner 2026-08-16) | no patient principal exists in `config/roles.yaml` (TODO-20), so reusing gateway sessions is impossible without a §1 auth change w4 is scoped out of (w4-D-4); the simulated binding carries the boundary check without it |
| w4-D-10 | spec | The seeded sample pins a **≥50-encounter patient** as the bounded-at-scale fixture (SPEC-16); 50 is fixed, not plan detail (owner 2026-08-16) | the requirement itself fixed the number — the owner's ask names "a real chart with 50+ encounters" — so the fixing decision is recorded here per the spec-stage number rule |
| w4-D-11 | plan | The prototype lands at **`eval/kg/`**, stdlib-only, corpus built in memory — no service import, no DB driver, no new dependency (owner 2026-08-16) | mirrors the `eval/rag/` retrieval-prototype precedent (stdlib graded core); keeps SPEC-2 self-containment structural, leaves the `make eval` drift gate untouched (it reads only `eval/rag/` + seed sources), and preserves e6's recorded "nothing under `eval/` calls the records route" |
| w4-D-12 | plan | Assembly is a **single plain retrieval pipeline** — boundary check, then three batch reads (encounters-for-patient, records-for-encounters, providers-by-id), then merge; no multi-agent framework, no LLM call (owner 2026-08-16) | applies the w4-D-2 collapse: three batch retrievals satisfy SPEC-15/16 directly, so nothing in assembly needs a second agent |
| w4-D-13 | plan | The three findings land as **one document, `docs/research/w4-findings.md`** — sections: IDOR (headline) · N+1 note · session finding (owner 2026-08-16) | SPEC-11's exposure-set sizing cross-references the sibling findings in place, the three SPEC-17/18/19 presence checks target one path, and `docs/workflow/w4.md` stays inside the 400-line cap |
| w4-D-14 | req | Source-note correction (gate r1 finding 1): HAR ids `1042`/`1043` **are** seeded patient rows — the seed runs 255 patients, ids 1042–1851 (owner 2026-08-16) | the prior note asserted the opposite from memory, not the tree; the corrected fact strengthens the SPEC-9 repro — the HAR requests hit live seeded charts |
| w4-D-15 | spec | Dead-cite correction (gate r1 finding 2): "column-level PHI encryption (D3)" → cite the real registry, `docs/debt-log.md` cross-cutting table / TODO-12, in both Out-of-scope and Spec Exclusions (owner 2026-08-16) | `docs/debt-log.md:6-8` records D3 does not exist in this repo; citation-level fix only, no scope change |
| w4-D-16 | plan | Verification 2 re-pinned 1300+N/5/1 → 1317+N/19/1 and the plan's findings-doc bullet sizes the search vector as closed — e6 merged e53cd81 2026-08-17 (after the 08-16 gate), adding 14 integration tests (deselected lane, `make test-migrations`) and closing the D11 corpus-read vector (e6-SPEC-1/2/5) (owner 2026-08-17) | count moved for a recorded reason per `CLAUDE.md` §6 — reported here, not silently updated |
| w4-D-17 | impl | `docs/debt-log.md` is edited on the branch (D11 exposure-set row extended with `GET /patients/{patient_id}`) though absent from the plan's Changes list — the edit stands, recorded here (impl-gate r1 finding 1) | the residuals rule (implementation stage, registry contract at `docs/todo.md:8-11`) files a discovered risk in the registry that owns it *at landing*; w4-SPEC-11's sizing found a surface on no list, and an unfiled surface never gets sized — the registry write is the rule working, this row makes it stage-traceable |
| w4-D-18 | impl | Round-3 owner calls (2026-08-17, round-3 rule): finding 1 **fix** — file the `GET /appointments` surface and its cancel twin into D11 + the findings-doc exposure table · finding 2 **fix** — raise a typed error at the assemble site instead of an unreachable `KeyError` · finding 3 **compress** the r1/r2 gate records rather than raise the 400-line cap | all three are cheap and closing; finding 1 is the second instance of exactly the miss SPEC-11 exists to prevent, so leaving it unfiled would ship an exposure set the gate has twice read as incomplete, and finding 2 gets the same treatment as its r2 twin so the class is closed consistently rather than half-accepted |
| w4-D-19 | impl | Round-4 owner call (2026-08-17): finding 1 **back to stage 3** — the exposure-set miss recurred a third time (r1 `GET /patients/{id}`, r3 `GET /appointments`, r4 `review-queue`), so the defect is the plan, not the instance: `gate:`-only exposure sizing (SPEC-11) is replaced by a plan step that produces the set from a systematic sweep of every gateway `@app.` route. Findings 2/4/5 **carried** into the re-plan cycle (findings doc + debt-log reworked there); finding 3 **fix** by compressing the superseded r3 record (cap held at 400). The impl-gate loop stops; the plan returns to DRAFT for revision + re-gate, spec unchanged | instance-patching an analysis miss three rounds running is the loop paying the same cost repeatedly for a plan that never made the exposure set complete-by-construction; a sweep-driven plan closes the class, and the round-3 rule's "back to stage 3" branch is exactly this call — a plan-design defect, not a residual to accept or an item to overrule |
| w4-D-20 | plan | Re-plan mechanism for w4-D-19: the SPEC-11 exposure set is produced by a systematic sweep of every gateway `@app.` route (23), rendered as a full classification table in `docs/research/w4-findings.md`, and pinned by `tests/test_w4_exposure_sweep.py` — it loads `gateway.app`, partitions every declared `APIRoute` into in-test `EXPOSURE_SET` ∪ `EXCLUDED`, and reddens on any route in neither (mirroring the `test_gateway_proxy_error_contract.py` route-set closure, CLAUDE.md §4), with a second assertion pinning every `EXPOSURE_SET` path into the findings table. SPEC-11's spec check cell stays `gate:` (spec frozen); the sweep makes the set the gate reads complete-by-construction | a machine-checked route-set-closure invariant closes the class the three impl-gate misses proved open — a route added or missed later reddens the suite instead of waiting for a fourth manual catch; an example list can never be complete-by-construction, a partition of the declared route set is |
| w4-D-21 | plan | ~~Size cap raised **400 → 500** for this item only (owner, 2026-08-17), per the `docs/workflow/README.md` size-budget mechanism~~ — **withdrawn 2026-08-18 (gate r5 f4, superseded by w4-D-33)**: the overrun was a defect in the single-budget mechanism, not a property of this item, and under the split budget w4 needs no per-item raise (authored 365/400, loop record 157/200) | the raise was re-priced twice in two days (399/400 → 500, then 494/500) because a single cap charges gate-loop residue — round records and disposition-only register rows — against a budget sized for authored deltas; a third raise would be the instance patch w4-D-19 rejected for the exposure set, so the fix went to the mechanism instead (w4-D-33) |
| w4-D-22 | plan | ~~Gate r3 f1: `EXPOSURE_SET`'s in-set test is a **disjunction over the three D11 sub-classes**, not the single narrow "caller-influenced selector + no bind + PHI" predicate — (a) a walkable patient selector with no bind returning PHI, **or** (b) cross-patient PHI/disclosure reach by construction with no per-patient selector (`GET /patients`, `GET /records/search?q=`, `GET /roi/requests`), **or** (c) a filed write-side twin mutating a patient-scoped resource on a walkable id with no bind, PHI-returning or not (`POST /appointments/{appointment_id}/cancel`)~~ — **superseded 2026-08-18 by w4-D-30** (gate r5 f1: clause (c) keyed membership on prior filing) | the r3 predicate applied literally would drop four surfaces `docs/debt-log.md` D11 and the findings table already carry, so the sweep built to complete the set would shrink it; the D11-sizing question §1 poses is "every route the fix must be sized against", which is the union of the three ways a route lands cross-patient, not the walk vector alone |
| w4-D-23 | plan | Gate r3 f2: the `docs/landmines.md` §1 D11 bullet's **enumerated-set clause** is updated post-sweep to point at the swept 23-route table as the sized set (the three named routes stay as the by-construction examples), so §1 stops being the last stale copy of the exposure claim after the sweep lands. **Owner-approved §1 edit** (2026-08-17), scoped to that clause only — the ⚠ zone's substance (no bind, walkable ids, fix deferred to TODO-20) is unchanged | §1 is the third registry every impl-gate miss named (r1/r3/r4); once the sweep table is the maintained set, an un-updated §1 bullet is a falsified claim the sweep itself creates — the falsified-claims sweep requires a change row or an out-of-scope entry, and leaving the authoritative do-not-touch register stale is the worse of the two |
| w4-D-24 | plan | Gate r3 f3: the sweep buys **classification-completeness, not verdict-correctness** — route-set closure forces every one of the 23 routes to be *classified*, and reddens on an unclassified or stale-set route, but a route parked in `EXCLUDED` with a wrong reason still passes green. The Landmines block names the two residuals: (i) per-route verdict correctness stays the `gate:` human read, (ii) the suite pins the D11 claim, so the TODO-20 bind fix must update `test_w4_exposure_sweep` | "complete-by-construction" / "machine-verified-complete set" overstated what a partition assertion checks; the honest claim is that the *set of routes considered* is complete and drift-guarded, while whether each verdict is right stays human — naming that keeps the gate's adequacy read from resting on a guarantee the test does not give |
| w4-D-25 | plan | Gate r3 f4 (repeat of impl-gate r1 f1's class — landed change untraceable to a plan row): the `Changes:` list is closed against the branch it continues from — the `eval/kg/assemble.py` bullet names the w4-D-18 landings (`IncompleteCorpus`, `_resolve_provider`, the `records_for_encounters` dedupe) and the `tests/test_kg_prototype.py` bullet enumerates all **9** functions incl. `test_records_for_encounters_dedupes` + `test_assemble_unmodelled_provider_raises_typed_error` (13 total across both test files, not 11). **Class closure, not instance:** the disposition records a full `git diff --name-status main` bullet-by-bullet sweep of the branch tree against the list and its scope, per the drift-gate class-recurrence rule | instance-patching the two named files would leave the class — "the Changes list drifts from what the branch landed" — open for the next round; the sweep over the whole 9-file diff is what closes it, matching how impl-gate should have closed r1 f1 the first time |
| w4-D-26 | plan | Gate r3 f5: `tests/test_w4_exposure_sweep.py` loading `gateway.app` **pins and restores `sys.modules`** around the load, exactly as `tests/test_gateway_proxy_error_contract.py:32-46` does (save the bare siblings `config`/`logging_config`/`db`/`models`/`security`/`authz`, load the gateway copies, restore after) — `CLAUDE.md` §6. The bullet's "Static" description is corrected: it imports the gateway package, it does not merely read route templates as text | an unpinned load collides with every other service's bare-named `config`/`db` in the same session; the mirror test already solved this and the sweep test must copy the idiom rather than reintroduce the collision |
| w4-D-27 | plan | Gate r3 f6: the doc-parity assertion is **bidirectional** — every `EXPOSURE_SET` path appears in the findings table **and** every `EXCLUDED` route + its one-line reason appears in the table's excluded rows, so neither partition can drift out of `docs/research/w4-findings.md` silently. Verification 12's "covers all 23" claim is restated to name both directions | the r3 plan pinned only `EXPOSURE_SET` paths, so the ~16 excluded rows and their reasons could vanish from the "full 23-route sweep table" with the suite green — a one-directional pin cannot back the "code, test and findings table cannot drift" claim |
| w4-D-28 | plan | Gate r4 f1: verification 15 is rewritten to a per-token `|| echo "MISSING: $s"` loop over five tokens — the three by-construction routes plus `docs/research/w4-findings.md` and `swept 23-route`, the two only the w4-D-23 §1 edit adds — expected **no output**, with the pre-edit tree as its own negative control (run this session against un-edited `docs/landmines.md` it prints MISSING for exactly those two). **Class closure, not instance:** all 15 verifications swept for shell-semantics defects; the one other hit, verification 5's `&&` short-circuit, is tightened to `;` so both counts always print | `for … done; echo $?` reports only the last iteration (demonstrated: a loop whose first iteration fails still exits `0`), and the three route greps already match the un-edited file, so the old expected `0` held whether or not the §1 edit landed — an expected output that did not follow from the property checked; per-token MISSING lines both discriminate and localise which token is absent |
| w4-D-29 | plan | Gate r4 f2: w4-D-21's cite to the README superseded-round compression rule is **withdrawn** (struck in place per the register's revised-entry rule); the 400→500 raise now rests on committed basis only — the `docs/workflow/README.md:122-130` raise-by-decision mechanism, plus this item's own already-applied compression of superseded rounds (`63e5896`, `b26b385`). The raise itself is unchanged | the compression rule exists only as an uncommitted working-tree edit, so the cite dangles if that edit never lands and would rest this item's cap decision on tree state outside its own delivery; committed mechanism plus applied practice makes the decision self-standing |
| w4-D-30 | plan | Gate r5 f1 (**repeat of gate r3 f1's class**, so closed at the predicate not the route): w4-D-22's three-way disjunction is replaced by a **two-clause property predicate that reads only the route**, never a registry — a route is in `EXPOSURE_SET` iff **(a)** its subject is chosen by caller-supplied input (path param, query param, **or request-body field**) naming a patient or a patient-scoped resource on a walkable id, with no session→patient bind, **regardless of method and regardless of whether the response carries PHI**, **or (b)** it reaches data spanning patients by construction, with no per-patient narrowing at all. Class sweep run over all 23 routes: (a) now decides every write r4 f1 and r5 f1 named — `POST /review-queue/{pair_id}/disposition` (`app.py:284`), `POST /appointments/{appointment_id}/cancel` (`:379`), `POST /roi/requests/{request_id}/fulfill` (`:406`), `GET /appointments?patient_id=` (`:365`), and the body-selector pair `POST /appointments` (`:374`) / `POST /roi/requests` (`:401`) — and (b) keeps the three §1 by-construction routes (**enumeration corrected 2026-08-18 by w4-D-35** — (b) has a fourth member, `GET /review-queue`; predicate unchanged) | keying membership on "filed" made the sweep read the registry it was built to complete, which is the same circularity r3 f1 rejected one clause earlier; a predicate evaluated against the route's own signature (who picks the subject, is the pick bounded, is there a bind) decides every declared route including ones no registry has ever named, which is what "derivable verdict for all 23" requires |
| w4-D-31 | plan | Gate r5 f2: doc parity matches on **exact `(method, path)` table cells**, parsed from the findings table's method and path columns — not substring containment of the route string anywhere in the file | `verbatim`-substring parity cannot redden for a path that prefixes another declared route, so `GET /patients` (one of the three §1 routes), `POST /appointments` and `POST /roi/requests` were pinned only by their own longer siblings' rows — the exact routes the bidirectional pin (w4-D-27) most needed to cover were the ones it could not fail on |
| w4-D-32 | plan | Gate r5 f3: verification 2's expected count is **pinned absolutely, not as `1317+N`** — `tests/test_w4_exposure_sweep.py` carries **3** functions (route-set closure · `EXPOSURE_SET` doc parity · `EXCLUDED` doc parity), so w4 adds 9 + 4 + 3 = **16** and verification 2 expects `1333 passed, 19 deselected, 1 xfailed` | `CLAUDE.md` §6 makes the baseline load-bearing, and an expected output of "1317 plus however many tests we wrote" is satisfied by any number ≥ 1317 — including a run that silently lost a test; the count is only a check once every added function is enumerated in the Changes list |
| w4-D-33 | plan | Gate r5 f4 (owner, 2026-08-18): the cap overrun is **fixed at the process layer, not raised again** — ~~`docs/workflow/README.md` splits the size budget into **authored sections (Requirements + Spec + Plan + Delivery) 400** and **loop record (Decisions + Findings) 200**, never traded against each other~~ (**mechanism text superseded 2026-08-18 by w4-D-34** — the landed fix is the split-file shape, not the dual budget); w4-D-21's per-item raise stays withdrawn as unnecessary under the landed shape. The README change landed committed **before** the re-gate (PR #86 @3585cc5), so no w4 artifact cites uncommitted tree state (w4-D-29 rule) | w4's authored sections are 362 lines — inside the original single cap it was said to have outgrown — while nine gate and impl-gate rounds drove the file to 494: the budget was charging review pressure to the authoring rules, which is why cap housekeeping became a finding in three separate rounds and the raise's basis was spent twice; pricing each kind of growth against its own bound removes the raise decision from every future hard-reviewed item |
| w4-D-34 | plan | The w4-D-33 process fix **landed as the split-file shape, not the dual budget** (owner, 2026-08-18): PR #85 (authored-400 / loop-record-200 dual budget) closed unmerged; PR #86 (merged @3585cc5) rewrote `docs/workflow/README.md` to the two-file shape — contract file `docs/workflow/<item>.md` (400-line cap, CI-enforced `workflow-doc-cap`) + uncapped plan file `docs/workflow/plans/<item>.md`, deleted at delivery. w4-D-33's mechanism text is struck as superseded; its verdict — process-layer fix, w4-D-21 withdrawn, no per-item raise — stands. w4 is split accordingly (contract: header + Requirements + Spec + Delivery, 158 lines · this plan file: Decisions + Plan + Findings). Notes w4-D-29's cite: `docs/workflow/README.md:122-130` resolved against the pre-#86 README — the raise-by-decision mechanism is gone from the rewritten file and those lines now carry plan-file shape rules — so the cite reads only against history; moot, since the raise it grounded is withdrawn (w4-D-21) | the finding-4 verdict was right but its mechanism cell was written before the owner chose between the two candidate process fixes (PR #85 vs #86); the register must record which one landed so the re-gate reads this artifact against the committed README, not a never-merged branch — and the one README line cite the rewrite moved is flagged here rather than left reading as if it pointed at current tree lines |
| w4-D-35 | plan | Gate r6 f1 (owner, 2026-08-18; **repeat of gate r3 f1 / r5 f1's class**, closed over the whole enumeration): w4-D-30's **recorded enumeration is corrected — the predicate is unchanged**. Clause (b) has **four** members, not three: the three §1 by-construction routes plus `GET /review-queue` (`services/gateway/app.py:279` — proxies intake `/review-queue` wholesale under `patients.write`, cross-patient candidate pairs with name+dob, no per-patient narrowing; no caller-supplied subject, so only (b) can decide it). Class sweep re-run over all 23 declared `@app.` routes this session under the two-clause predicate: `GET /review-queue` is the **only** route the r5 enumerations left unaccounted. Corrected at every recorded site — w4-D-30's cell annotated in place, the findings-doc bullet's in-set list now carries the route (the plan's `docs/debt-log.md` change row already did); §1's example clause untouched (w4-D-23 keeps the three named routes as examples — the sized set is the swept table) | the recorded partition excluded a route the same plan files into D11 because the sweep *result* was written down as "the six (a) writes + the three §1 routes" — a count carried forward from §1's example list rather than re-derived from the predicate; correcting the enumeration at every recorded site, with the re-sweep's scope recorded, closes the class rather than the instance |
| w4-D-36 | plan | Gate r6 f2 (owner, 2026-08-18): the branch's rebase onto `main` @3585cc5 (2026-08-18, done so the w4-D-33/w4-D-34 README shape was committed before the re-gate) is recorded with the **full old→new commit map** (subject-identical rebased twins, all old shas reflog-only): base `7cdc812`→`3585cc5` · `5027fc8`→`7961085` · `8ac8c47`→`7496247` · `ac6fa8d`→`6150270` · `c7bb6be`→`6a7afe8` · `990761a`→`c897e22` · `b6f26dd`→`59e82cc` · `db8cff4`→`f82834d` · `7b74b77`→`c37658e` · `92490bc`→`8d5edd2` · `374eb0f`→`1373693` · `63e5896`→`9d1922c` · `b26b385`→`9db62f7`. The three live claims move to the rebased tree (contract Baseline line, contract Delivery commit list, verification 2's cite); round-log disposition cells keep their old shas verbatim as historical record, derivable through this map. The rebase window `7cdc812..3585cc5` is docs/tooling only (`.claude/` skills + agents, `docs/workflow/README.md`, `docs/review-loop-metrics.md`), so the 1317/19/1 baseline measured at 7cdc812 carries unchanged | rewriting the history cells would falsify what each round actually recorded, while leaving the map unrecorded breaks "item state derivable from the two files alone" — one mapping row in the register keeps both, and pinning the docs-only window is what lets verification 2 keep citing the pre-rebase baseline measurement |

## Plan

Status: GATED 2026-08-18 (gate round 7 clean) — re-planned 2026-08-17 (w4-D-19/w4-D-20; prior GATED 2026-08-16, returned to
stage 3 at impl-gate r4 finding-1), then revised for Gate round-3 findings f1–f6 (w4-D-22..27,
2026-08-17), then for Gate round-4 findings f1–f2 (w4-D-28/w4-D-29, 2026-08-17), then for Gate round-5
findings f1–f4 (w4-D-30..33, 2026-08-18). Re-plan closes the SPEC-11 exposure-set miss (recurred r1/r3/r4) by construction:
the set is the output of a systematic sweep of every gateway `@app.` route, pinned by a
route-set-closure test that reddens on any unclassified route; the r3 revision widens the
in-set predicate (w4-D-22), makes doc parity bidirectional
(w4-D-27), corrects the completeness claim to classification-complete (w4-D-24), closes the
Changes list against the branch (w4-D-25), pins `sys.modules` (w4-D-26), and adds the
owner-approved §1 doc edit (w4-D-23). The r5 revision replaces that predicate with a
two-clause property predicate that reads only the route (w4-D-30, superseding w4-D-22),
makes doc parity an exact `(method, path)` cell match (w4-D-31), and pins verification 2's
count absolutely (w4-D-32); the size-cap overrun is fixed in `docs/workflow/README.md`
rather than raised again (w4-D-33, withdrawing w4-D-21 — landed as the split-file shape,
w4-D-34, under which this artifact is split). The r6 revision corrects the recorded clause-(b)
enumeration to four routes — `GET /review-queue` joins the three §1 by-construction routes
(w4-D-35, predicate unchanged) — and records the 2026-08-18 rebase onto `main` @3585cc5 with
the full old→new commit map (w4-D-36). Spec unchanged; drift gate re-runs.

Changes (file level):
- `eval/kg/schema.py` — KG node dataclasses (Patient, Encounter, Provider, Record) and the
  edges patient→encounter, encounter→provider, encounter→record (w4-SPEC-1 / w4-D-11)
- `eval/kg/corpus.py` — deterministic in-code seeded sample, built in memory with no I/O:
  ≥3 patients including the fixture patient with 53 encounters (w4-SPEC-2, w4-SPEC-16 /
  w4-D-10, w4-D-11)
- `eval/kg/assemble.py` — `GraphStore` exposing exactly three batch accessors
  (encounters-for-patient, `records_for_encounters` deduping on the way through,
  providers-by-id) with a retrieval counter; `Principal` as the seeded caller→patient
  binding; `assemble_patient_view` evaluating the ownership check first and raising a
  refusal before any store read, and resolving each encounter's provider through
  `_resolve_provider`, which raises the typed `IncompleteCorpus(provider_id)` rather than a
  bare `KeyError` on an unmodelled provider (the last two landed w4-D-18; listed here per
  w4-D-25) (w4-SPEC-3..8, w4-SPEC-15/16 / w4-D-9, w4-D-12, w4-D-18)
- `eval/kg/run.py` — demo entry: assembles the 53-encounter fixture, prints the view
  summary and retrieval count, then demonstrates one cross-patient refusal (live-run
  evidence for Delivery)
- `tests/test_kg_prototype.py` — 9 functions (w4-D-25): kg-schema-shape ·
  kg-self-contained (static import scan of `eval/kg/*.py`, stdlib + intra-package only) ·
  kg-self-contained-positive-control (reddens on an injected forbidden import) ·
  assemble-full-view · assemble-complete · records-for-encounters-dedupes (w4-D-18) ·
  assemble-unmodelled-provider-raises-typed-error (w4-D-18) · bounded-retrieval-count (count
  equal for a 2-encounter and the 53-encounter patient) · bounded-at-scale
  (w4-SPEC-1..4, w4-SPEC-15/16)
- `tests/test_kg_boundary.py` — ⚠ boundary-authorized-served ·
  boundary-cross-patient-refused (adversarial: sibling-id request) ·
  boundary-before-traversal (refusal with store read-count still zero) ·
  boundary-simulated-principal (w4-SPEC-5..8; `docs/landmines.md` §3 rows)
- `tests/test_w4_exposure_sweep.py` — NEW (w4-D-20): loads `gateway.app`, **pinning and
  restoring `sys.modules` around the load** exactly as `test_gateway_proxy_error_contract.py:32-46`
  does — save the bare siblings, load the gateway copies, restore after (CLAUDE.md §6,
  w4-D-26); it imports the gateway package, it does not merely read route text. **3 functions**
  (w4-D-32): route-set closure · `EXPOSURE_SET` doc parity · `EXCLUDED` doc parity. Closure
  enumerates every `APIRoute` `(method, path)` and asserts it partitions exactly into two
  in-test sets — `EXPOSURE_SET`, whose in-set test is the **two-clause property predicate**
  (caller-supplied subject in path/query/**body** with no bind, any method, PHI-bearing or
  not · **or** cross-patient reach by construction with no per-patient narrowing; w4-D-30,
  superseding w4-D-22) — and `EXCLUDED` (each keyed to a one-line reason). A route in
  neither, or a stale set entry, reddens (mirrors the
  `test_gateway_proxy_error_contract.py:482,502` route-set closure, CLAUDE.md §4). Doc parity
  is pinned **both directions** (w4-D-27) and matches on **exact `(method, path)` cells parsed
  from the findings table**, never substring containment (w4-D-31): every `EXPOSURE_SET` path
  **and** every `EXCLUDED` route + reason has its own row, so a path that prefixes another
  declared route (`GET /patients` ⊂ `GET /patients/{patient_id}/records`) is pinned by its own
  row rather than by a sibling's. No service, no DB, no PHI (w4-SPEC-11 completeness enforcer /
  w4-D-20)
- `docs/research/w4-findings.md` — one document, three sections (w4-D-13): IDOR finding —
  HAR repro from `docs/handover/portal.har` (ids 1042/1043 are live seeded charts,
  w4-D-14), both enablers, and the §1 exposure set rendered as the **full 23-route sweep
  table** — every gateway `@app.` route with its verdict (in-set / excluded + reason),
  replacing the prior example list; the table is a markdown table with **method and path in
  their own fixed columns**, since `test_w4_exposure_sweep` parses those cells for the
  exact-match parity (w4-D-31) — a restructured table reddens the suite rather than passing
  silently; the table's header claims a **classification-complete**
  sweep (every route classified, drift-guarded by `test_w4_exposure_sweep`), not
  verdict-correctness — which stays the `gate:` human read (w4-D-24) — and `EXPOSURE_SET`
  membership follows the two-clause property predicate (w4-D-30), so the **four** clause-(b)
  by-construction routes (`GET /patients`, `GET /records/search?q=`, `GET /roi/requests`, and
  `GET /review-queue` — the fourth (b) member, w4-D-35; §1 keeps the first three as its named
  examples per w4-D-23) and every
  caller-selected write — `POST /review-queue/{pair_id}/disposition`,
  `POST /appointments/{appointment_id}/cancel`, `POST /roi/requests/{request_id}/fulfill`,
  and the body-selector pair `POST /appointments` / `POST /roi/requests` — stay in-set (`/patients/{id}/relevant-records` and the search path stay listed; search
  closed by e6 2026-08-17, sized as closed) (w4-SPEC-9..11, 17; w4-D-16, w4-D-20, w4-D-30, w4-D-24) · N+1 note — production pattern at
  `services/records-service/app.py:103-104,128-129`, p95=3.8s, the 50+-encounter argument,
  with the count derivation corrected to the source (37 per-encounter queries → 37 encounters,
  w4-D-19 r4 finding 2) (w4-SPEC-12/13, 18) · session finding — `auth.yaml:2` declarative-only
  vs hardcoded `services/gateway/security.py:275-279`, no code change (w4-SPEC-14, 19)
- `docs/debt-log.md` — D11 exposure-set account reworked to the swept set (adds
  `GET /review-queue` + `POST /review-queue/{pair_id}/disposition` and the swept remainder)
  and the entry's chronology put right — the "grew 2026-08-17" clause currently precedes the
  "grew 2026-08-08 (W2)" clause (w4-D-19 r4 finding 5); registry row only, no code (w4-D-17
  rule — an unlisted D11 surface is filed to be sized)
- `docs/landmines.md` — ⚠ **§1 zone, owner-approved (w4-D-23)**: the D11 bullet's
  enumerated-set clause is updated to point at the swept 23-route table as the sized set,
  keeping `GET /patients` / `GET /records/search?q=` / `GET /roi/requests` as the
  by-construction examples; the bullet's substance (no bind, walkable ids, fix deferred to
  TODO-20, e6 having closed the metacharacter vector) is unchanged. The clause names the
  table by the literal phrase **`swept 23-route`** and cites it by path
  (`docs/research/w4-findings.md`) — the two tokens verification 15 discriminates on, so the
  change row and its check agree (w4-D-28). Scoped strictly to the set-enumeration clause so
  §1 stops being the last stale copy of the claim after the sweep
  (w4-SPEC-11 / w4-D-23)
- `docs/workflow/w4.md` + `docs/workflow/plans/w4.md` — the split artifact (w4-D-34):
  contract file + this plan file; Delivery filled at impl per the shape rules

Nothing changes under `services/`, `db/`, `config/`, `frontend/`, or `contracts/` — the
spec Exclusions hold structurally (verification 8).

Landmine approvals (verbatim, never compressed):
- **IDOR on chart reads (§1)** — entered analysis-only. The w4-SPEC-9..11 writeup and
  its w4-SPEC-17 presence/citation check (`cmd: idor-writeup-present`) document D11,
  reproduce the cross-patient read from `docs/handover/portal.har`, and size the
  exposure set per §1 as a complete sweep of all 23 gateway `@app.` routes — pinned by
  `tests/test_w4_exposure_sweep.py` (w4-D-20), including `/patients/{id}/relevant-records`
  and the e6-owned search path. No code changes on `GET /patients/{id}/records` or any
  production PHI route; the planted defect stays, and its integration xfail
  (`tests/integration/test_records_flow.py:52`) stays visible and deselected. Approved by
  w4-D-4 (owner, 2026-08-16): w4 is analysis-plus-prototype, the live session→patient
  bind is deferred to the scheduled D11 item (TODO-20). **One §1 *doc* edit is made** (not
  code): the `docs/landmines.md` §1 D11 bullet's enumerated-set clause is repointed at the
  swept table as the sized set — owner-approved w4-D-23 (2026-08-17), scoped to that clause,
  the ⚠ zone's substance and the deferred fix unchanged — closing the last stale copy of the
  exposure claim the sweep itself creates.
- **Auth / sessions (§1)** — entered analysis-only. The w4-SPEC-14/19 finding records
  `SESSION_TIMEOUT: never` and the hardcoded behaviour at
  `services/gateway/security.py:275-279`; per §1, `auth.yaml` is not edited (the change
  would be inert) and no session-expiry code changes. Approved by w4-D-7 (owner,
  2026-08-16): finding only, the fix is a separate scheduled auth item.
- **The ⚠ boundary-check rows (w4-SPEC-5..8)** put authorization code only in `eval/kg/`,
  deciding against a seeded/simulated principal — approved by w4-D-9 (owner, 2026-08-16).
  This is not the gateway auth boundary: `require_session` / `require_capability` and
  `config/roles.yaml` are untouched. The §3 negative-test rule is carried by the
  adversarial rows boundary-cross-patient-refused and boundary-before-traversal.
- **Deliberate defects in reach, preserved:** the D11 route; the D8 per-encounter loop
  (`services/records-service/app.py:128`, "do not collapse to a join") — w4-SPEC-12
  documents it, no refactor (w4-D-5); the no-TTL session store; the inert `auth.yaml`.
  The prototype reads only its own synthetic in-memory sample (w4-D-1) — no PHI column,
  no production table, no live service.
- **Accepted residuals:** the live D11 fix, the production-route N+1 fix, and the
  session-TTL fix all remain open (TODO-20, w4-D-5, w4-D-7); the search-corpus bound is
  e6's (w4-D-3). w4-SPEC-11's spec check cell stays `gate:` (spec frozen). The sweep +
  `test_w4_exposure_sweep` route-set closure make the sweep **classification-complete** —
  every one of the 23 routes is classified and the partition is drift-guarded — but not
  verdict-correct: **(i)** whether each route's in-set/excluded verdict is right stays the
  `gate:` human read (a route parked in `EXCLUDED` with a wrong reason still passes green),
  and **(ii)** the suite pins the D11 exposure claim, so the TODO-20 session→patient bind
  fix must update `test_w4_exposure_sweep` when it lands. The gate's adequacy read is over a
  route set that is complete and drift-guarded, not over guaranteed-correct verdicts
  (w4-D-20, w4-D-24). Verification 15 is the same split one level down: it pins the
  **presence** of the §1 repoint's tokens, not that the repointed clause reads correctly —
  the wording adequacy of the w4-D-23 §1 edit stays the `gate:` human read (w4-D-28).

Verification (runnable, expected output stated):
1. `.venv/bin/python -m pytest tests/test_kg_prototype.py tests/test_kg_boundary.py -q`
   → all pass, 0 failed (w4-SPEC-1..8, 15, 16)
2. `make test-docker` → `1333 passed, 19 deselected, 1 xfailed` — the branch baseline 1317
   (`Baseline at branch:`, measured at 7cdc812; carried across the docs/tooling-only rebase
   onto `main` @3585cc5, w4-D-36) plus exactly the **16** functions this plan
   enumerates: 9 in `tests/test_kg_prototype.py` + 4 in `tests/test_kg_boundary.py`
   (w4-D-25) + 3 in `tests/test_w4_exposure_sweep.py` (w4-D-32). Deselected (19 — the
   integration lane, run by `make test-migrations`) and xfailed unchanged; any other total,
   up or down, is a finding to report, not a number to update (`CLAUDE.md` §6 baseline rule;
   re-pinned post-e6, w4-D-16; absolute per w4-D-32)
3. `grep -c "docs/handover/portal.har" docs/research/w4-findings.md` → ≥1 (w4-SPEC-17,
   cmd: idor-writeup-present)
4. `grep -c "services/records-service/app.py" docs/research/w4-findings.md` → ≥1
   (w4-SPEC-18, cmd: n1-note-present)
5. `grep -c "services/gateway/auth.yaml" docs/research/w4-findings.md; grep -c
   "services/gateway/security.py" docs/research/w4-findings.md` → two counts, both ≥1
   (`;` not `&&`, so a zero first count still prints the second — w4-D-28 shell sweep)
   (w4-SPEC-19, cmd: session-finding-present)
6. `grep -rnE "^(import|from) (sqlalchemy|psycopg|httpx|fastapi|redis|requests)|from services" eval/kg/`
   → no output (w4-SPEC-2; the kg-self-contained test asserts the same from inside the
   suite, with its positive control)
7. `.venv/bin/python eval/kg/run.py` → prints the assembled fixture view (53 encounters,
   every encounter present), retrieval count 3, then one refused cross-patient attempt
   (w4-SPEC-16, live-run evidence)
8. `git diff --stat main -- services/ db/ config/ frontend/ contracts/` → empty (spec
   Exclusions)
9. break-then-revert: disable the ownership check in `eval/kg/assemble.py` →
   `tests/test_kg_boundary.py` red (boundary-cross-patient-refused,
   boundary-before-traversal); revert → green (w4-SPEC-6/7, §3)
10. break-then-revert: replace the batch records read with a per-encounter loop →
    bounded-retrieval-count and bounded-at-scale red; revert → green (w4-SPEC-15/16)
11. `make eval` → green, unchanged — `eval/rag/check_drift.py` reads only `eval/rag/` and
    seed sources, so `eval/kg/` cannot move it (gate interaction, w4-D-11)
12. `.venv/bin/python -m pytest tests/test_w4_exposure_sweep.py -q` → `3 passed`; the route
    partition covers all 23 declared gateway `@app.` routes under the two-clause property
    predicate, and doc parity holds **both directions** as an exact `(method, path)` cell
    match — every `EXPOSURE_SET` path and every `EXCLUDED` route + reason has its own row in
    `docs/research/w4-findings.md`, so neither partition drifts from the table (w4-SPEC-11 /
    w4-D-20, w4-D-30, w4-D-27, w4-D-31)
13. break-then-revert: drop one route from `EXPOSURE_SET` in `tests/test_w4_exposure_sweep.py`
    → the partition assertion red (declared route now in neither set); revert → green — proves
    the closure is not vacuous without touching the gateway wall (w4-D-20)
14. break-then-revert: delete one `EXCLUDED` route's row from the sweep table in
    `docs/research/w4-findings.md` → the bidirectional doc-parity assertion red; revert → green
    — proves the excluded half of the table is pinned too, not just `EXPOSURE_SET` (w4-D-27, f6)
15. `for s in "GET /patients" "GET /records/search?q=" "GET /roi/requests"
    "docs/research/w4-findings.md" "swept 23-route"; do grep -qF "$s" docs/landmines.md ||
    echo "MISSING: $s"; done` → **no output**: the §1 D11 bullet still names the three
    by-construction routes *and* points at the swept 23-route table as the sized set, so §1
    is not the stale copy after the sweep lands. Negative control is the pre-edit tree
    itself — run against un-edited `docs/landmines.md` (observed 2026-08-17) it prints
    `MISSING: docs/research/w4-findings.md` and `MISSING: swept 23-route`, the two tokens
    only the w4-D-23 edit adds; a dropped route prints its own line rather than being
    masked by a later iteration's exit status (w4-SPEC-11 / w4-D-23, w4-D-28)
16. break-then-revert (**prefix case**): delete only the `GET /patients` row from the sweep
    table, leaving every `GET /patients/{patient_id}…` row in place → the `EXPOSURE_SET`
    doc-parity assertion red; revert → green — proves parity is an exact cell match, not
    substring containment that a longer sibling row can satisfy (w4-D-31, r5 f2)

## Findings

### Gate — round 1, 2026-08-16

3 findings, no stamp. Plan returns to stage 3.

| # | SPEC | Finding | Disposition (stage 3) |
|---|------|---------|-----------------------|
| 1 | w4-SPEC-9 | The Requirements Source note claims HAR ids 1042/1043 "are not seeded patient rows (the seed runs ids 1–255)" — wrong on both counts: `db/seed/seed.sql` seeds 255 patients with ids **1042–1851**, and 1042 (Maria Gonzalez) and 1043 (James O'Brien) are both seeded rows. The SPEC-9 writeup the plan commissions would reproduce a false fact; the HAR ids being live seeded charts makes the repro stronger, not weaker. | Fixed per w4-D-14 (owner 2026-08-16): Source note corrected; plan's `w4-findings.md` row now notes the seeded-chart fact |
| 2 | — | Out-of-scope and Spec Exclusions both cite "column-level PHI encryption (D3)" — a dead id: `docs/debt-log.md:6-7` records that D3 does not exist in this repo (`D3b` is the Redis entry, a different defect); plaintext PHI at rest is tracked via the cross-cutting table / TODO-12. | Fixed per w4-D-15 (owner 2026-08-16): both cites now point at the cross-cutting table / TODO-12 |
| 3 | w4-SPEC-17 | SPEC-17 is a ⚠ row but no Landmines approval bullet cites it: the IDOR bullet names "the w4-SPEC-9..11 writeup" only, while the session bullet names w4-SPEC-14/19 explicitly — 17 is the one ⚠ row outside any recorded approval's row list (w4-D-4 covers the zone; the row citation is the gap). | Fixed in place: IDOR bullet now cites w4-SPEC-17 alongside 9..11 (plan edit under existing w4-D-4 approval; no new decision) |

checked: w4-SPEC-1..19 (all rows); check map complete — one mechanism per row (test: 1-8,15,16 · gate: 9-14 · cmd: 17-19); ⚠ rows 5-8, 9-11, 14, 17, 19 — approvals w4-D-4/w4-D-7/w4-D-9 cover all except SPEC-17 (finding 3); freeze scope clean (no DEFERRED rows); tree facts verified except the seed-id claim (finding 1) and the D3 cite (finding 2).

### Gate — round 2, 2026-08-16

Clean — plan stamped GATED.

checked: w4-SPEC-1..19 (all rows), both directions (spec→plan coverage and plan→spec traceability, `run.py` via w4-D-11 + verification 7); check map complete — exactly one mechanism per row (test: 1-8,15,16 · gate: 9-14 · cmd: 17-19); ⚠ rows 5-8, 9-11, 14, 17, 19 all covered — w4-D-9 (5-8), w4-D-4 (9-11, 17), w4-D-7 (14, 19); freeze scope clean (no DEFERRED rows); round-1 dispositions verified applied against the tree (w4-D-14: seed runs 255 patients ids 1042–1851, HAR 1042/1043 both 200 · w4-D-15: dead D3 cite gone, both cites at cross-cutting table / TODO-12 · finding 3: IDOR approval bullet now cites SPEC-17); tree facts verified incl. `auth.yaml:2`, `security.py:275-279`, records-service loop `:119-128`, `tests/README.md:54` + integration xfail `:52`, no patient principal in `config/roles.yaml`, TODO-20, e6 cross-cites (e6-D-3 / E6-REQ-1/2/8), `eval/rag/check_drift.py` isolation (verifications 2, 11); residuals honestly named (live D11, production N+1, session TTL, e6 search bound, SPEC-11 adequacy → impl-gate human read).

### Impl gate — round 1, 2026-08-17

2 findings, no stamp; adv review clean. Coverage per r3's `checked:` line; baseline 1328/19/1.

| # | SPEC | Finding | Disposition (stage 4) |
|---|------|---------|-----------------------|
| 1 | — | `docs/debt-log.md` (D11 exposure-set extension for `GET /patients/{patient_id}`) is changed on the branch but absent from the plan's file-level Changes list — untraceable to a planned change (check 6). Disclosed in Delivery (deviation 3, Residuals, commit `c7bb6be`) and is the registry contract working as designed (debt-log owns risk; an unlisted D11 surface must be filed to be sized), but unplanned by the letter with no stage-tagged decision. | Fixed @990761a: w4-D-17 (impl) records the unplanned file under the residuals-at-landing rule; deviation 3 cites it |
| 2 | w4-REQ-5 | Stale line cites in the Requirements REQ-5 note: cites the per-encounter loop at `services/records-service/app.py:119-132` and "the code comment at `:100-103`" — tree has the route at `:103-104`, the D8 comment in the docstring `~:112-127`, the loop at `:128-129`. Delivery deviation 2 discloses only the plan-bullet variant (`:119-128`); the findings doc carries correct lines, so record-keeping not substance. | Fixed @b6f26dd: REQ-5 note cites corrected to tree (`:103-104`, `:112-113`, `:128-129`), Requirements status line annotated, deviation 2 extended to disclose both variants; GATED plan text untouched |

### Impl gate — round 2, 2026-08-17

2 findings, no stamp; adv review clean. Coverage per r3's `checked:` line, which confirms the
r1 dispositions applied; baseline 1328/19/1.

| # | SPEC | Finding | Disposition (stage 4) |
|---|------|---------|-----------------------|
| 1 | — | Dead filter in `eval/kg/assemble.py:125-131`: `records_for_encounters` builds `wanted = set(encounter_ids)` then filters `if enc_id in wanted` while iterating `encounter_ids` itself — the guard is tautologically true (dead code), and iterating the list rather than the set means duplicate ids would duplicate records. No SPEC row violated (callers pass unique ids, all tests green); cosmetic. | A · fixed @db8cff4: the accessor now dedupes on the way through, preserving first-seen order; pinned by `test_records_for_encounters_dedupes` (red before the fix, +1 baseline) so the latent duplicate-id half of the class is closed too, not just the dead guard |
| 2 | — | Delivery §Residuals is headed "registry IDs only" but two of four pointers are item decision IDs, not registry rows: `w4-D-5` (production-route N+1) and `w4-D-7` (session-TTL). The owning registry rows exist and are cited in the findings doc (`docs/debt-log.md` D8; debt-log cross-cutting "Sessions never expire…" row) — record-keeping, not an unfiled risk. | A · fixed @7b74b77: both pointers now name the owning registry row (`docs/debt-log.md` D8; the debt-log cross-cutting "Sessions never expire, single role, no MFA" row), with w4-D-5/w4-D-7 kept only as the scoping cite |

### Impl gate — round 3, 2026-08-17

3 findings, no stamp; adv review clean. Owner calls recorded as w4-D-18 (all three fixes).
Round-3 rule live: the loop stops, each finding is the owner's call, the next run honours it.
Coverage prose superseded by round 4's `checked:` line and E-3/E-4 (compressed here per the
finding-3 disposition, matching the r1/r2 treatment; the commit cites are the evidence).

| # | SPEC | Finding | Disposition (owner) |
|---|------|---------|---------------------|
| 1 | w4-SPEC-11 | Exposure set misses a surface of the same property: `GET /appointments?patient_id=` (gateway `services/gateway/app.py:363-368`, `schedule.read` only → `services/scheduling-service/app.py:71-93`) returns `AppointmentOut` for any walkable sequential `patient_id` with no patient bind — incl. free-text `reason`, `provider`, `location`, `scheduled_for` (`scheduling-service/schemas.py:27-38`). On none of `docs/landmines.md` §1, `docs/debt-log.md` D11, or the findings-doc table, whose header says the list covers "every route with the same property". Same class as the `GET /patients/{id}` row w4 itself found and filed (w4-D-17); write-side twin `POST /appointments/{appointment_id}/cancel` (gateway `:379`, scheduling `:138-139`) takes only an appointment id and no patient bind either. Not red — analysis-only item, fix is the same two doc edits as w4-D-17. | fix (owner, w4-D-18) @92490bc — both surfaces filed: `docs/debt-log.md` D11 exposure row and the findings-doc exposure table now carry `GET /appointments?patient_id=` and the cancel twin, with the consequence the sizing turned up (the bind has to hold at the gateway, not inside records-service); no code, same rule and register row as w4-D-17 |
| 2 | — | Internal contradiction in `eval/kg/assemble.py`: `providers_by_id` (`:133-140`) defensively drops unknown ids (`if pid in self._providers`) but `assemble_patient_view` (`:166`) indexes `providers[enc.provider_id]` unguarded, while the adjacent line uses `records_by_encounter.get(enc.id, ())` — an encounter on an unmodelled provider raises `KeyError` rather than being handled. Unreachable on the current corpus (`build_corpus` assigns `n % len(providers)`), no SPEC row, all tests green; same cosmetic class as r2 finding 1. | fix (owner, w4-D-18) @374eb0f — the merge resolves the provider through `_resolve_provider`, raising a typed `IncompleteCorpus(provider_id)` instead of a bare `KeyError`, so the defensive accessor and the merge now agree on what an unmodelled provider is; pinned by `test_assemble_unmodelled_provider_raises_typed_error` (red on the missing type, +1 baseline) |
| 3 | — | Size budget: `docs/workflow/w4.md` was 397/400 at HEAD, and this round record puts it over the `docs/workflow/README.md:123` cap. The budget is advisory by construction (skill-run, `CLAUDE.md` §11), raised only by a stage-tagged decision in this register saying why. Owner's call: compress the r1/r2 records, or raise the cap with a decision row. | fix (owner, w4-D-18) @63e5896 — r1/r2 records compressed to findings + dispositions, cap left at 400; the dropped prose is superseded coverage, not evidence (this round re-verified it, and each disposition cites its commit) |

### Impl gate — round 4, 2026-08-17

5 findings, no stamp; adv review clean. None is a re-raise (r3 dispositions applied —
`92490bc`, `374eb0f`, `63e5896`). Owner calls recorded as **w4-D-19**: finding 1 → **back to
stage 3** (SPEC-11's `gate:`-only sizing is a plan defect, missed at r1/r3/r4); 2/4/5 carried
into the re-plan cycle; 3 fixed here by compressing the superseded r3 record.

| # | SPEC | Finding | Disposition (owner) |
|---|------|---------|---------------------|
| 1 | w4-SPEC-11 | Exposure set incomplete a **third** time: `GET /review-queue` and `POST /review-queue/{pair_id}/disposition` (gateway `:279,:284` → `intake-service/app.py:497,:548`) are `patients.write` only with no patient bind — the GET returns `ReviewQueuePatient` name + dob for candidate pairs **across all patients** (`intake-service/schemas.py:119-142`), the POST files a two-charts-are-one-person judgment on a sequential `pair_id`. On none of `docs/landmines.md` §1, `docs/debt-log.md` D11, or the findings-doc table (`grep -rn review-queue` over the three registries returns nothing). Same-class tail, lower confidence: `POST /appointments` (gateway `:374`) and `POST /roi/requests` (`:401`) take a body `patient_id` with no bind; `POST /roi/requests/{request_id}/fulfill` (`:406`) discloses on a sequential id (substantively D12's). Not red — analysis-only item, fix is the same two doc edits as w4-D-17/w4-D-18. But `docs/research/w4-findings.md:69` claims the list covers "every route with the same property", and that claim has now failed at r1, r3 and here: either complete it from a systematic sweep of the gateway's 23 `@app.` routes, or narrow the header to what was actually swept. | E · back to stage 3 (owner, w4-D-19) — third recurrence proves the plan defect, not this instance: SPEC-11's `gate:`-only sizing is replaced in the re-plan by a systematic sweep of all gateway `@app.` routes producing the exposure set by construction, so `review-queue`, the POST tail, and the "every route" header are all resolved there under the revised+re-gated plan; not patched in this dead loop |
| 2 | w4-SPEC-12 | The N+1 note inverts the handed-over count. Source (`docs/workflow/w4.md:54`): the per-encounter query "runs 37 times (one per encounter)" → 37 encounters, 38 queries. `docs/research/w4-findings.md:108-109` derives the opposite: "the captured 37 means 36 encounters plus the encounter query". `tests/test_kg_prototype.py:242-244` reads it the source's way, so the branch carries both readings. Immaterial to the argument (linear in `E` either way; the 51+ figure holds), but the note is the SPEC-12 deliverable and this is the same class as gate r1 finding 1 — a repro fact asserted against, not from, the source. | E · carried to stage-3 rework (w4-D-19) — the findings doc is rewritten in re-impl; the count derivation is corrected to the source there (37 per-encounter → 37 encounters), not patched in this dead loop |
| 3 | — | Size budget: `docs/workflow/w4.md` = **400/400** at HEAD — at the cap, zero headroom, so neither this round nor the `delivery IMPLEMENTED` stamp fits. w4-D-18's finding-3 half chose "compress, cap left at 400"; that headroom is now spent. Needs further compression or a stage-tagged cap-raising decision in this register. | fix (owner, w4-D-19) — the superseded r3 intro + `checked:` prose is compressed to findings + dispositions (its coverage is subsumed by this round's `checked:` line and E-3/E-4), cap held at 400; this write fits inside it |
| 4 | — | Delivery commit list does not reach HEAD: `:342-343` reads "rounds + dispositions `990761a^..374eb0f` and this commit", written in `63e5896`, so `de32a7d` (the r3-f3 sha fill) is uncovered. Record-keeping, self-referential by nature. | E · carried to stage-3 rework (w4-D-19) — the Delivery commit list is rewritten when the loop resumes after re-plan; recording a moving HEAD in a dead loop would just re-stale |
| 5 | — | `docs/debt-log.md` D11 cell now runs backwards in time — "Exposure set grew 2026-08-17 (w4 sizing)" was inserted ahead of "Exposure set grew 2026-08-08 (W2)". Cosmetic, in an OPEN registry row three items now append to. | E · carried to stage-3 rework (w4-D-19) — the systematic sweep reworks the D11 exposure account (adds `review-queue` + the swept set); the chronology is put right in that same edit rather than reordered now to reorder again |

checked: w4-SPEC-1..19 both agents; test: 1-8,15,16 filled+passing (13/13 kg tests), pinned-test diff clean (`5027fc8`→HEAD fills only; the sole post-`8ac8c47` edit to a pinned test is a width reflow in `test_boundary_simulated_principal`, no behaviour); cmd: 17-19 re-run → 1 / 2 / 1+1 (all ≥1); gate: 9-14 human-observed clean by this session against the tree — SPEC-9 HAR both requests `Bearer <patient-1042-token>`, 1042→200 and 1043→200, repro table `w4-findings.md:23-26` matches exactly · SPEC-10 both enablers named (`:42-65`), no-bind claim confirmed (only `require_session`/`require_capability` exist) and `db/schema.sql:34` sequential-id comment verbatim · SPEC-11 sized at `:67-90` with "w4 does not re-fix" stated, **but see finding 1** · SPEC-12 pattern documented at `:94-126` with `records-service/app.py` and p95=3.8s cites, **but see finding 2** · SPEC-13 the 50+-chart argument at `:113-121` (51+ round trips, aggregate-median blindness) · SPEC-14 recorded at `:134-171`, `auth.yaml` inertness independently confirmed (no parser in the tree — only docstring mentions at `security.py:7`, `gateway/app.py:10,212`) and no session-expiry code change (verification-8 diff empty); adv-reviewer clean over frozen Spec + diff (all rows, every findings-doc citation resolved against tree, exclusions diff empty, spot-run 53 enc / 3 retrievals, refusal 0); verifications 1, 3-8, 11 re-run this session (`git status --porcelain` unchanged after each), 9-10 break-then-revert accepted from the E-3/E-4 record; r1-r3 dispositions verified applied; planted defects D11/D8/no-TTL/inert-`auth.yaml` intact, integration IDOR xfail still xfail; idiom sweep clean (no gateway route, no logging or `str(e)` added, no `Co-Authored-By` on any of the 16 branch commits); Delivery evidence shape clean (deviations 1-4, test-first disclosure with the non-red-first deviation backed by E-3/E-4, isolation clause, residuals as registry rows) except finding 4; baseline re-measured **1330 passed, 19 deselected, 1 xfailed** (`make test-docker`, 32.14s) = 1317 + exactly the 13 test functions the branch adds, deselected/xfailed unmoved; diff closed both ways over 9 files/1431 ins/1 del, all traceable; size 400/400 (finding 3); 5 findings above, none red.

### Gate — round 3, 2026-08-17

6 findings, no stamp. Plan returns to stage 3. **Round-3 rule live:** the gate loop stops here — each finding is the owner's call (accept as a named residual, overrule, or change the spec at stage 2), recorded in its disposition cell; the next gate run honours the recorded call rather than re-flagging. Five of six are w4-SPEC-11 — the same row whose `gate:`-only sizing returned the item at impl-gate r4 (w4-D-19); the re-plan replaced the mechanism, and these are defects **in the replacement**, so stage 3 closes the class over the whole sweep design, not the six cited sites. Finding 4 is a repeat of impl-gate r1 finding 1's class (landed change traceable to no plan row, dispositioned w4-D-17) — its disposition must record a sweep of the branch diff against the Changes list and the scope that sweep covered, not just the two files named.

| # | SPEC | Finding | Disposition (stage 3) |
|---|------|---------|-----------------------|
| 1 | w4-SPEC-11 | The sweep's in-set predicate is narrower than the set SPEC-11 must size. The plan defines `EXPOSURE_SET` as "a caller-influenced patient selector, no session→patient bind, PHI-bearing" (`docs/workflow/w4.md:206-208`), but `docs/landmines.md` §1's D11 bullet already names `GET /patients`, `GET /records/search?q=` and `GET /roi/requests` — cross-patient **by construction**, no patient selector at all — and the filed write twin `POST /appointments/{appointment_id}/cancel` returns no PHI. Applied literally the predicate drops four surfaces `docs/debt-log.md` D11 and `docs/research/w4-findings.md:73-80` already carry: the sweep built to complete the sized set would shrink it. | fixed per w4-D-22 — `EXPOSURE_SET`'s in-set test is now the three-way D11 disjunction (walk-vector ∪ cross-patient-by-construction ∪ filed write twin), so the four dropped surfaces stay in-set and the sweep can only grow the set, not shrink it; the sweep-test bullet, findings-doc bullet and verification 12 all carry the disjunction |
| 2 | w4-SPEC-11 | Falsified-claims sweep gap: the re-plan adds a `docs/debt-log.md` change row for the reworked D11 account but no row — and no out-of-scope entry — for `docs/landmines.md` §1's D11 bullet, whose enumerated set is `GET /patients` / `GET /records/search?q=` / `GET /roi/requests` and which states the fix "must be sized against this whole set". §1 is the third registry every impl-gate miss named (r1 `GET /patients/{id}`, r3 `GET /appointments`, r4 `review-queue`); after the sweep lands it is the one copy of the exposure claim left stale. | fixed per w4-D-23 (owner-approved §1 edit, 2026-08-17) — a `docs/landmines.md` change row is added repointing the §1 D11 enumerated-set clause at the swept table; the Landmines approval block records the §1 doc edit, and verification 15 checks §1 is not stale after the sweep |
| 3 | w4-SPEC-11 | Residual honesty: route-set closure forces every route to be **classified**, not classified **correctly** — a route parked in `EXCLUDED` with a wrong one-line reason passes green, and the partition passes with `EXPOSURE_SET` empty. "Complete-by-construction" and "the gate's adequacy read is over a machine-verified-complete set" (`:265-267`) overstate what the test buys. The real residuals — verdict correctness stays the `gate:` human read, and the suite now pins the D11 claim so the TODO-20 bind fix must update this test — are not named in the Landmines block. | fixed per w4-D-24 — the claim is restated to **classification-complete, not verdict-correct** in the findings-doc bullet and Accepted-residuals block, which now name both residuals (verdict correctness stays `gate:` human; the suite pins the D11 claim so the TODO-20 fix must update `test_w4_exposure_sweep`) |
| 4 | — (check 6) | Changes list not closed against the branch it continues from: the `eval/kg/assemble.py` bullet omits behaviour w4-D-18 landed in the tree (`IncompleteCorpus` `:44`, `_resolve_provider` `:188-192`, the dedupe in `records_for_encounters` `:135-147`), and the `tests/test_kg_prototype.py` bullet omits `test_records_for_encounters_dedupes` (`:179`) and `test_assemble_unmodelled_provider_raises_typed_error` (`:197`) — the tree carries 13 test functions against the 11 the plan enumerates. Re-implementing against this list reproduces impl-gate r1 finding 1's class. **Repeat class** — see the round intro. | fixed per w4-D-25, **class not instance** — full `git diff --name-status main` sweep this session over all 9 branch files (`docs/debt-log.md` M; `docs/research/w4-findings.md`, `docs/workflow/w4.md`, `eval/kg/{assemble,corpus,run,schema}.py`, `tests/test_kg_{prototype,boundary}.py` A): the assemble bullet now names the three w4-D-18 landings and the prototype-test bullet enumerates all 9 functions (13 total across both files). The remaining bullets already matched the tree; `test_w4_exposure_sweep.py`/`w4-findings.md`/`landmines.md` are net-new here and listed. Scope covered: every path in the branch diff vs the Changes list |
| 5 | w4-SPEC-11 | Self-consistency: `tests/test_w4_exposure_sweep.py` is described as "loads `gateway.app`" **and** "Static — reads route templates and one markdown file" (`:205,212-213`). Loading it imports gateway `app.py` plus its bare-named siblings (`config`, `db`, `models`, `security`, `authz`); `CLAUDE.md` §6 and the mirror test (`tests/test_gateway_proxy_error_contract.py:32-46`) require pinning **and restoring** `sys.modules` around the load. The plan names neither the import nor the pinning, and an unpinned load collides with every other service's `config`/`db` in the same session. | fixed per w4-D-26 — the sweep-test bullet now names the `sys.modules` pin-and-restore around the `gateway.app` load, citing the mirror test `:32-46` and CLAUDE.md §6, and drops the "Static — reads route templates" wording for "imports the gateway package"; the collision the r3 plan invited is closed |
| 6 | w4-SPEC-11 | The doc-parity assertion is one-directional — only `EXPOSURE_SET` paths are pinned into `docs/research/w4-findings.md` (`:210-212`) — so the ~16 `EXCLUDED` rows and their reasons can vanish from the "full 23-route sweep table" with the suite green, while the plan claims "code, test and findings table cannot drift" and verification 12 states the partition "covers all 23". | fixed per w4-D-27 — the doc-parity assertion is now **bidirectional** (every `EXPOSURE_SET` path and every `EXCLUDED` route + reason pinned into the table); the sweep-test bullet and verification 12 state both directions, and verification 14 break-then-reverts an excluded row to prove the excluded half is pinned |

checked: w4-SPEC-1..19 (all rows), both directions — spec→plan coverage and plan→spec traceability (`run.py` via w4-D-11 + verification 7; `tests/test_w4_exposure_sweep.py` via w4-D-20 + verifications 12/13; `docs/debt-log.md` as registry upkeep under w4-D-17 / w4-D-19 r4-f5), closure broken plan→tree at `eval/kg/assemble.py` + `tests/test_kg_prototype.py` (finding 4); check map complete — exactly one mechanism per row (test: 1-8,15,16 · gate: 9-14 · cmd: 17-19); spec unchanged from the 2026-08-16 freeze (no Spec-section edit in the uncommitted re-plan); ⚠ rows 5-8, 9-11, 14, 17, 19 all covered by recorded approvals citing decision IDs — w4-D-9 (5-8), w4-D-4 (9-11, 17), w4-D-7 (14, 19); freeze scope clean (no `DEFERRED → ` rows); cites resolve — w4-D-1..20 all present, every SPEC id the plan cites exists; tree facts spot-verified this session: 23 gateway `@app.<method>` routes (the two other `@app.` lines are `exception_handler`), gateway line cites `:320,:325,:332,:343,:365,:374,:379,:391` exact, route-set-closure mirror `tests/test_gateway_proxy_error_contract.py:482,502` exact, records-service route `:103-104` and per-encounter loop `:128`, `security.py:275-279` no-TTL `create_session`, `auth.yaml:2` `SESSION_TIMEOUT: never`, integration IDOR xfail `tests/integration/test_records_flow.py:52`, debt-log D11 chronology inversion confirmed (r4 f5 still open), branch base `main` @7cdc812 with e6 `e53cd81`; four plan-authoring checks run cold — self-consistency (finding 5), gate interaction (mirror closure named, `make eval` isolation holds), residual honesty (finding 3), falsified-claims sweep (finding 2; `ARCHITECTURE.md:111`, `CLAUDE.md:96`, TODO-20 carry no enumerated exposure set, so not falsified); verification block runnable and SPEC-cited, negatives present (9, 10, 13); size 399/400 after this round — no headroom for a further round, r4 finding 3's class is live again.

### Gate — round 4, 2026-08-17

2 findings, no stamp. Plan returns to stage 3. Round-3 rule stays live (loop stopped at round 3):
each finding is the owner's call — accept as a named residual, overrule, or change the spec —
recorded in its disposition cell; the next gate run honours the recorded call. All six round-3
dispositions (w4-D-22..27) verified **applied** in the final plan text and against the tree
(branch diff = the 9 files w4-D-25 names, all bullets tree-verified); neither finding repeats a
dispositioned class.

| # | SPEC | Finding | Disposition (stage 3 / owner) |
|---|------|---------|-------------------------------|
| 1 | w4-SPEC-11 | Verification 15's command cannot verify the w4-D-23 §1 repoint it exists to prove: the `for r in ...; do grep -qF "$r" ... && grep -qF "swept" ...; done; echo $?` loop reports only the **last** iteration's exit status (earlier failed iterations leave `$?` = 0), and `grep -qF "GET /patients"` / `"GET /records/search"` already match today's un-edited `docs/landmines.md:36-37`, so `swept` is the only discriminating token — expected `0` holds even if the §1 edit drops a route (check 9: expected output must actually follow from the property checked). | fixed per w4-D-28 — verification 15 is rewritten as a per-token `|| echo "MISSING: $s"` loop over five tokens, adding the two the w4-D-23 edit alone introduces (`docs/research/w4-findings.md`, `swept 23-route`); expected output is now **no output**, and the un-edited tree is its own negative control (observed this session: exactly those two print MISSING). Both cited defects are evidenced — the masking form was demonstrated with a failing first iteration still exiting `0`. **Class closure, not instance:** all 15 verifications swept for shell-semantics defects this session (exit-status masking, short-circuit, silent-empty); one other hit — verification 5's `&&` — tightened to `;`. Scope covered: every command in the verification block |
| 2 | — (w4-D-21 cite) | w4-D-21's load-bearing cite — "the README's new superseded-round compression rule (2026-08-17)" — resolves only against an **uncommitted** working-tree edit to `docs/workflow/README.md`; committed HEAD carries the 400-cap raise-by-decision mechanism (which the cap raise itself stands on) but not the compression rule, so the cite dangles if that parallel edit never lands. Traceability, not a wrong fact. | fixed per w4-D-29 — the cite is struck in w4-D-21 (register revised-entry rule, kept visible) and the 400→500 raise now rests only on committed basis: `docs/workflow/README.md:122-130` raise-by-decision plus this item's own applied compression (`63e5896`, `b26b385`). The raise is unchanged; no w4 artifact now cites uncommitted tree state |

checked: w4-SPEC-1..19 (all rows), both directions — spec→plan coverage and plan→spec traceability (`run.py` via w4-D-11 + verification 7, `tests/test_w4_exposure_sweep.py` via w4-SPEC-11/w4-D-20, `docs/debt-log.md` registry upkeep w4-D-17/w4-D-19, `docs/landmines.md` §1 clause via w4-D-23), Changes list closed against the branch (`git diff main HEAD` = the 9 files w4-D-25 names, all bullets tree-verified); check map complete — exactly one mechanism per row (test: 1-8,15,16 · gate: 9-14 · cmd: 17-19); ⚠ rows 5-8, 9-11, 14, 17, 19 all covered by recorded approvals citing decision IDs — w4-D-9 (5-8), w4-D-4 (9-11, 17), w4-D-7 (14, 19), plus the w4-D-23 owner-approved §1 doc edit recorded verbatim in the IDOR bullet; freeze scope clean (no DEFERRED rows); Spec section byte-identical to HEAD/freeze (md5 match, edit hunks confined to Status/Decisions/Plan/Findings/Delivery); cites resolve — w4-D-1..27 present, all cited SPEC ids exist; tree facts verified this session: 23 gateway `@app.<method>` routes (decorator lines :179..:1260 enumerated, review-queue :279/:284, /patients/{id} :320, /appointments :365/:374, cancel :379, /roi :391/:401/:406), mirror closure + pin idiom at `test_gateway_proxy_error_contract.py:31-46,483-499`, records-service `:103-104/:112-113/:128-129`, `auth.yaml:2`, `security.py:275-279`, seed 1042 live, `docs/handover/portal.har` present, `tests/README.md:52-54` + integration xfail `:52`, no patient principal in `config/roles.yaml`, TODO-20, debt-log D11 chronology inversion still present (r4-f5 carried, per plan), findings-doc "every route" header + 36-encounter inversion still present (r4-f2 carried, per plan), `eval/rag/check_drift.py` reads only its own files + seed (verification 11), verification 6 grep clean, README committed cap mechanism at :122-130; four plan-authoring checks run cold — self-consistency clean, gate interaction named (make-eval isolation, proxy-contract mirror), residual honesty clean (w4-D-24), falsified-claims sweep clean (landmines + debt-log rows present; ARCHITECTURE.md §7 / CLAUDE.md / TODO-20 / tests/README.md carry no enumerated exposure set); verifications runnable and SPEC-cited with negatives (9, 10, 13, 14) **except finding 1 (verification 15)**; per-SPEC verdicts: 1-10, 12-19 satisfied, 11 residual-named (classification-complete per w4-D-24; finding 1 is against its verification 15, not the mechanism); size after this round under the w4-D-21 500 cap.

### Gate — round 5, 2026-08-18

4 findings, no stamp. Plan returns to stage 3. Round-3 rule stays live (loop stopped at round 3):
each finding is the owner's call — accept as a named residual, overrule, or change the spec —
recorded in its disposition cell; the next gate run honours the recorded call. Both round-4
dispositions (w4-D-28, w4-D-29) verified applied. **Finding 1 repeats gate r3 finding 1's class**
(the in-set predicate is narrower than the set w4-SPEC-11 must size) — stage 3 closes the class
over the predicate as a whole, and the disposition records the sweep that ran and its scope, not
the one cited route.

| # | SPEC | Finding | Disposition (stage 3 / owner) |
|---|------|---------|-------------------------------|
| 1 | w4-SPEC-11 (**repeat of gate r3 f1's class**) | The in-set predicate still excludes a route the same plan puts in the set: w4-D-22 clause (c) admits only "a **filed** write-side **twin**", so `POST /review-queue/{pair_id}/disposition` — which the plan's `docs/debt-log.md` change row (`:250-254`) adds to the D11 account — matches none of (a) (no patient selector, no chart returned), (b) (it is a write), or (c) (neither filed nor a twin), though it mutates on a walkable sequential `pair_id` under `patients.write` with no bind (`services/gateway/app.py:284-289` → `services/intake-service/app.py:548`). Keying membership on *prior filing* reproduces the property r3 f1 rejected — the sweep meant to complete the set cannot decide membership from what is already registered — and it leaves r4 f1's named tail (`POST /appointments :374`, `POST /roi/requests :401`, `POST /roi/requests/{request_id}/fulfill :406`), which w4-D-19 says the re-plan resolves, with no verdict derivable from the predicate. |  fixed per w4-D-30 — **class closed at the predicate, not the route**: the disjunction is replaced by a two-clause predicate evaluated against the route's own signature (who chooses the subject — path, query or body; is the choice bounded; is there a session→patient bind), so membership never consults a registry. Scope of the sweep run: all 23 declared `@app.` routes re-classified under it this session; clause (a) now yields a derivable verdict for every write r4 f1 and r5 f1 named — `POST /review-queue/{pair_id}/disposition` `:284`, `POST /appointments/{appointment_id}/cancel` `:379`, `POST /roi/requests/{request_id}/fulfill` `:406`, `GET /appointments?patient_id=` `:365`, and the body-selector pair `POST /appointments` `:374` / `POST /roi/requests` `:401` — while clause (b) keeps the three §1 by-construction routes. w4-D-22 struck as superseded |
| 2 | w4-SPEC-11 | Doc parity is specified as "appears **verbatim** in `docs/research/w4-findings.md`" (`:230-234`, w4-D-27) — substring containment — so an `EXPOSURE_SET` entry whose path prefixes another declared route can never redden: `GET /patients` (one of the three §1 by-construction routes) is contained in every `GET /patients/{patient_id}…` row, and likewise `POST /appointments` ⊂ `POST /appointments/{appointment_id}/cancel`, `POST /roi/requests` ⊂ `POST /roi/requests/{request_id}/fulfill` if in-set. Verification 14 break-then-reverts an *excluded* row and so does not cover the prefix case, leaving "neither partition drifts from the table" untrue for exactly the routes §1 names. |  fixed per w4-D-31 — parity is now an exact `(method, path)` cell match parsed from the findings table, so a prefixing path is pinned by its own row and never by a longer sibling's. New verification 16 break-then-reverts exactly the prefix case (delete the `GET /patients` row, leave every `GET /patients/{patient_id}…` row) — the case verification 14 could not reach |
| 3 | — (verification 2, check 9) | Verification 2's expected output is not derivable: `1317+N passed` with "N = the new w4 tests … pinned in `Baseline at branch:` at branch cut" — that line (`:7-8`) pins the pre-change baseline 1317/19/1, not N, and the Changes list enumerates 9+4=13 functions for the two kg test files (w4-D-25) but no function count for `tests/test_w4_exposure_sweep.py`. Any count ≥1317 satisfies the check as written, while `CLAUDE.md` §6 makes the count load-bearing. |  fixed per w4-D-32 — verification 2 pins `1333 passed, 19 deselected, 1 xfailed` absolutely (1317 + 9 + 4 + 3), and the Changes list now names the sweep test's 3 functions so the addend is enumerated rather than asserted |
| 4 | — (w4-D-21) | The cap-raise decision's own basis is spent: w4-D-21 sized 400→500 on "the file sits at 399/400" plus "the resumed cycle still owes a re-gate round, impl-gate rounds, the stamp, and the Delivery rewrite"; the file is **475** lines before this round (gate r3 + r4 records added ~76) and **494** after it, leaving ~6 lines for a re-gate round, the impl-gate rounds, the stamp and the Delivery rewrite — impl-gate r4 f3's class is live again at the new cap. |  fixed per w4-D-33 (owner, 2026-08-18) — **at the process layer, not by a third raise**: `docs/workflow/README.md` splits the budget into authored sections (Requirements + Spec + Plan + Delivery) 400 and loop record (Decisions + Findings) 200, never traded against each other. w4 needs no per-item raise under it — authored 365, loop record 157 — and w4-D-21 is withdrawn (struck in place). The README edit lands committed before the re-gate, so nothing here cites uncommitted tree state (w4-D-29 rule) |

checked: w4-SPEC-1..19 (all rows), both directions — spec→plan coverage and plan→spec traceability (`eval/kg/run.py` via w4-D-11 + verification 7; `tests/test_w4_exposure_sweep.py` via w4-SPEC-11/w4-D-20; `docs/debt-log.md` + `docs/landmines.md` as registry upkeep under w4-D-17/w4-D-19/w4-D-23; the two unpinned kg tests trace to w4-D-18), Changes list closed against the branch (`git diff --name-status main HEAD` = exactly the 9 files w4-D-25 names; the 11-file plan list adds the sweep test + `docs/landmines.md`, both net-new); check map complete — exactly one mechanism per row (test: 1-8,15,16 · gate: 9-14 · cmd: 17-19), all 13 pinned test ids exist in the tree (9 in `tests/test_kg_prototype.py`, 4 in `tests/test_kg_boundary.py`); ⚠ rows 5-8, 9-11, 14, 17, 19 all covered by recorded approvals citing decision IDs — w4-D-9 (5-8), w4-D-4 (9-11, 17), w4-D-7 (14, 19) — plus the owner-approved §1 doc edit (w4-D-23) recorded verbatim in the IDOR bullet; freeze scope clean (no `DEFERRED → ` rows); Spec section byte-identical to HEAD/freeze (md5 `359ea22b…`, all diff hunks in Status/Decisions/Plan/Findings); cites resolve — w4-D-1..29 all present, every cited SPEC id exists, no w4 artifact now cites uncommitted tree state (w4-D-29 strike verified; the class-recurrence rule at committed `.claude/skills/drift-gate/SKILL.md:60-65`, round-3 rule at `.claude/skills/impl-gate/SKILL.md:79`, cap mechanism at committed `docs/workflow/README.md:123-130`); tree facts verified this session: 23 `APIRoute` decorators of 25 `@app.` lines in `services/gateway/app.py` (review-queue `:279/:284`, `/patients/{patient_id}` `:320`, `/records` `:325`, `/relevant-records` `:332`, `/records/search` `:343`, `/appointments` `:365/:374`, cancel `:379`, `/roi` `:391/:401/:406`), mirror pin-and-restore + route-set closure at `tests/test_gateway_proxy_error_contract.py:31-46,482,502`, records-service `:103-104` / D8 docstring `:112-113` / loop `:128-129`, `services/gateway/security.py:275-279` no-TTL `create_session`, `services/gateway/auth.yaml:2`, `docs/landmines.md` §1 D11 clause `:34-40` still naming only the three routes, `docs/debt-log.md:288` D11 chronology inversion still present (r4-f5, carried), `eval/kg` three-accessor / `IncompleteCorpus` / `_resolve_provider` shape as described; verifications re-run read-only this session: 3 → 1, 4 → 2, 5 → 1 and 1, 6 → no output, 8 → empty, 15 → prints exactly `MISSING: docs/research/w4-findings.md` and `MISSING: swept 23-route` (the negative control w4-D-28 claims); four plan-authoring checks run cold — self-consistency (findings 1-3), gate interaction (`make eval` isolation confirmed: `eval/rag/check_drift.py` reads only `eval/rag/*` + `db/seed/*`; `tests/test_compose_topology.py:648-649` repo-wide scan is host-port-only and untouched by the new files; proxy-contract mirror named), residual honesty clean (w4-D-24 split, plus verification 15's presence-vs-wording split), falsified-claims sweep clean beyond the two filed rows (`ARCHITECTURE.md` carries no D11 mention, `docs/todo.md` TODO-20 and `tests/README.md` carry no enumerated exposure set, `CLAUDE.md` §2's `eval/rag/` line is not falsified by `eval/kg/`, `docs/phi-logging-policy.md` / `docs/runbook.md` unaffected); per-SPEC verdicts: 1-10 and 12-19 satisfied (12 satisfied with the r4-f2 findings-doc inversion carried by plan), 11 finding (findings 1-2 are mechanism defects; its verdict-correctness residual is properly named under w4-D-24); size 475/500 before this round, 494/500 after it (finding 4).

### Gate — round 6, 2026-08-18

2 findings, no stamp. Plan returns to stage 3. Round-3 rule stays live (loop stopped at round 3):
each finding is the owner's call — accept as a named residual, overrule, or change the spec —
recorded in its disposition cell; the next gate run honours the recorded call. All four round-5
dispositions (w4-D-30..33) verified applied. **Finding 1 repeats the class of gate r3 finding 1
and r5 finding 1** (the recorded sweep leaves an in-set surface without a derivable verdict) —
stage 3 closes the class over the whole enumeration, and the disposition records the sweep that
ran and its scope, not the one cited route.

| # | SPEC | Finding | Disposition (stage 3 / owner) |
|---|------|---------|-------------------------------|
| 1 | w4-SPEC-11 (**repeat of gate r3 f1 / r5 f1's class**) | `GET /review-queue` (`services/gateway/app.py:279-281` — proxies intake `/review-queue` wholesale under `patients.write`, cross-patient candidate pairs with name+dob, no per-patient narrowing) is unaccounted in every in-set enumeration the r5 revision produced: w4-D-30's recorded sweep says clause (a) decides the six named caller-selected routes and clause (b) keeps the **three** §1 by-construction routes, and the findings-doc Changes bullet's in-set list (three (b) routes + five writes) omits it too — yet the route matches only clause (b) (no caller-supplied subject, so (a) cannot catch it), while the plan's own `docs/debt-log.md` change row adds `GET /review-queue` + `POST /review-queue/{pair_id}/disposition` to the D11 account. Read literally, the recorded partition EXCLUDEs a route the same plan files into D11 (self-consistency), or (b)'s count is wrong — four, not three. r4 f1 named this exact GET first; its POST twin got a clause, the GET did not. | fixed per w4-D-35 (owner, 2026-08-18) — **class closed over the whole enumeration, not the cited route**: the two-clause predicate re-run over all 23 declared `@app.` routes this session; `GET /review-queue` is the only route the r5 enumerations left unaccounted, and it enters as clause (b)'s **fourth** member (no caller-supplied subject, cross-patient by construction). Corrected at every recorded site: w4-D-30's cell annotated in place, the findings-doc bullet's in-set list now carries the route, and the partition now agrees with the plan's own debt-log change row. Predicate unchanged; §1's three named examples unchanged (w4-D-23) |
| 2 | — (check 4 — wrong in-repo facts) | The branch was rebased onto `main` @3585cc5 (PR #86) after every commit cite was written: merge-base is 3585cc5, not 7cdc812, and none of the cited shas is an ancestor of HEAD (verified: `5027fc8`, `92490bc` not on branch — reflog-only, prunable). Live claims now wrong: contract `Baseline at branch: … branch cut at 7cdc812` (`w4.md:7-8`), Delivery's "off `main` @7cdc812, commits @5027fc8 · @8ac8c47 · @ac6fa8d · @c7bb6be · `990761a^..b26b385`" (`w4.md:155-157`), and verification 2's "cut at 7cdc812" cite. The round-log disposition shas (@990761a, @b6f26dd, @db8cff4, @7b74b77, @92490bc, @374eb0f, @63e5896, r4-intro's "r3 dispositions applied") are historical records but no longer verifiable from the branch — the old→new mapping is recorded nowhere, breaking "item state derivable from the two files alone". | fixed per w4-D-36 (owner, 2026-08-18) — the rebase is recorded in the register with the full old→new map (13 shas, subject-identical twins, window `7cdc812..3585cc5` verified docs/tooling-only so the 1317/19/1 baseline carries); the three live claims (contract Baseline line, contract Delivery commit list, verification 2's cite) updated to the rebased tree; round-log disposition cells keep their old shas verbatim as historical record, now derivable from the two files via the map |

checked: w4-SPEC-1..19 (all rows), both directions — spec→plan coverage and plan→spec traceability (`eval/kg/run.py` via w4-D-11 + verification 7; `tests/test_w4_exposure_sweep.py` via w4-SPEC-11/w4-D-20; `docs/debt-log.md` + `docs/landmines.md` registry upkeep under w4-D-17/w4-D-19/w4-D-23; the split pair via w4-D-34), Changes list closed against the branch (`git diff --name-status main...HEAD` = 10 paths, every one under a bullet; sweep test + landmines edit net-new as planned); check map complete — exactly one mechanism per row (test: 1-8,15,16 · gate: 9-14 · cmd: 17-19), all 13 pinned test ids exist (9 in `tests/test_kg_prototype.py`, 4 in `tests/test_kg_boundary.py`); ⚠ rows 5-8, 9-11, 14, 17, 19 all covered by recorded approvals citing decision IDs — w4-D-9 (5-8), w4-D-4 (9-11, 17), w4-D-7 (14, 19), plus the owner-approved §1 doc edit (w4-D-23) verbatim in the IDOR bullet; freeze scope clean (no `DEFERRED →` rows); Spec rows byte-identical through the split commit (`git diff f9f4eee 64de685` — 0 `| w4-SPEC` row lines changed); cites resolve — w4-D-1..34 all present, every cited SPEC id exists, w4-D-29/w4-D-34 uncommitted-cite rule holds (README rewrite committed @3585cc5, `workflow-doc-cap` in CI, contract 158/400); tree facts verified this session: 23 `APIRoute` decorators of 25 `@app.` lines with every gateway line cite exact (`:279/:284/:305/:320/:325/:332/:343/:365/:374/:379/:391/:401/:406`), mirror pin-and-restore + route-set closure at `tests/test_gateway_proxy_error_contract.py:30-46,~483`, records-service `:103-104`/`:112-113`/`:128-129`, `auth.yaml:2`, `security.py:275-279`, landmines §1 D11 clause still naming only the three routes, verification 15 negative control reproduced (`MISSING:` exactly the two w4-D-23 tokens), debt-log D11 chronology inversion + findings-doc "every route" header + 36-encounter inversion still present (carried per plan), integration xfail `tests/integration/test_records_flow.py:52` + `tests/README.md:54`, no patient principal in `config/roles.yaml`, seed 255 patients ids 1042–1851, HAR 1042/1043 both 200 (SPEC-9: ids live seeded rows), verification 8 diff empty, verification 2 math consistent (1317+9+4+3=1333) — **except** merge-base = 3585cc5 not 7cdc812 and cited branch shas off-branch (finding 2); four plan-authoring checks run cold — self-consistency (finding 1), gate interaction clean (`make eval` isolation, workflow-doc-cap, proxy-contract mirror), residual honesty clean (w4-D-24 split + verification 15's presence-vs-wording split), falsified-claims sweep clean beyond the two filed rows (`CLAUDE.md` §2 `eval/rag/` line not falsified by `eval/kg/`, e6's "nothing under eval/ calls the records route" preserved by w4-D-11); verifications 1-16 runnable and SPEC-cited with negatives (9, 10, 13, 14, 16); per-SPEC verdicts: 1-10 and 12-19 satisfied (12 with the r4-f2 inversion carried by plan), 11 finding (finding 1 — mechanism gap in the recorded sweep; its verdict-correctness residual properly named per w4-D-24).
### Gate — round 7, 2026-08-18

Clean — no findings; Plan stamped GATED 2026-08-18. Dry round.

checked: w4-SPEC-1..19 (all rows), both directions — spec→plan coverage and plan→tree traceability (`git diff --name-status main...HEAD` = 10 paths, every one under a Changes bullet; `tests/test_w4_exposure_sweep.py` + `docs/landmines.md` §1 edit net-new-as-planned); check map complete — exactly one mechanism per row (test: 1-8,15,16 · gate: 9-14 · cmd: 17-19), 13 pinned kg test ids exist (9 in `tests/test_kg_prototype.py`, 4 in `tests/test_kg_boundary.py`); ⚠ rows 5-8, 9-11, 14, 17, 19 all covered by Landmines-block approvals citing decision IDs — w4-D-9 (5-8), w4-D-4 (9-11, 17), w4-D-7 (14, 19), plus owner-approved §1 doc edit (w4-D-23) verbatim in the IDOR bullet; freeze scope clean (no `DEFERRED →` rows); cites resolve — w4-D-1..36 all present, every cited SPEC id exists; independent 23-route sweep confirms the clause-(b) four-member enumeration complete (w4-D-35) and no omitted in-set surface — `GET /eligibility`, `POST /ai/intake-instructions`, `POST /ai/visit-chat` all correctly excludable (`/ai/visit-chat` binds via `visit_memory_get(visit_id, owner)`, the counter-example); rebase map (w4-D-36) verified against reflog + docs-only window `7cdc812..3585cc5`, all 12 new twins on-branch, 1317/19/1 baseline carries; line cites exact (records-service `:103-104`/`:112-113`/`:128-129`, `security.py:275-279` no-TTL, `auth.yaml:2`, gateway route lines); carried-by-design stale state confirmed carried per w4-D-19, not findings (landmines §1 three-example clause = verification 15 negative control, debt-log D11 chronology inversion r4-f5, findings-doc "every route" header + 36-encounter inversion r4-f2 — all rewritten at re-impl); four plan-authoring checks run cold clean — self-consistency, gate interaction (`make eval` isolation, `workflow-doc-cap` contract 158/400, proxy-contract mirror), residual honesty (w4-D-24 split + verification 15 presence-vs-wording split), falsified-claims sweep (only landmines §1 + debt-log D11 enumerate the set, both in the change set); verifications 1-16 runnable and SPEC-cited with negatives (9, 10, 13, 14, 16); per-SPEC verdicts: 1-10, 12-19 satisfied (11 residual-named per w4-D-24 — classification-complete, not verdict-correct; 12 with the r4-f2 inversion as disclosed carry). No characterization in the spawning prompt to report.

### Impl gate — round 5, 2026-08-18

1 finding, no stamp; adv review clean. Fresh-context gate on the resumed loop (plan GATED gate
round 7, delivery DRAFT). Round count continues the impl-gate loop across the stage-3 return
(gate rounds continued 3→7 the same way); this is impl-gate round 5. **Round-3 rule live** (loop
past round 3): the finding is the owner's call — accept as a named residual, overrule, or send
back to stage 3 — recorded in the disposition cell; the next run honours it. **Finding 1 repeats
impl-gate r4 finding 4's class** (Delivery commit accounting drifts from the branch), so it closes
at the class — a sweep of every commit/count reference in Delivery against the branch — not the one
line.

| # | SPEC | Finding | Disposition (stage 4) |
|---|------|---------|-----------------------|
| 1 | — (check 10, **repeat of impl-gate r4 f4's class**) | Delivery's commit-list line (`docs/workflow/w4.md:167`) reads "Full branch commit list: `git log main..HEAD` (**23** commits)" but the command returns **24** — the count omits `7961085` "carry gated artifact onto the impl branch", the first branch commit (the rebased twin of `7b74b77`, w4-D-36 map). Record-keeping, off by one. Same class as impl-gate r4 f4 (there the Delivery range `990761a^..374eb0f` did not reach HEAD `de32a7d`; here the count is one short after the rebase/rewrite w4-D-19 said would fix it) — so an instance patch (23→24) is not an available disposition. | |

checked: w4-SPEC-1..19 both agents. Impl-gate-agent: pinned-test diff clean (13 kg `test:` ids filled + present, 9 in `tests/test_kg_prototype.py` + 4 in `tests/test_kg_boundary.py`, 3 sweep functions; sole post-landing edit to a pinned test `6a7afe8` is a width reflow, no owner decision); contract cap 206/400; `cmd:` 17/18/19 re-run → 1 / 2 / 1+1 (all ≥1); diff closed both ways over 12 files, merge-base `3585cc5`, every file under a Changes bullet incl. the two scoped registry edits (w4-D-17 debt-log, w4-D-23 landmines §1); planted defects D11/D8/no-TTL/inert-`auth.yaml` intact (`git diff main -- services/ db/ config/ frontend/ contracts/` empty); r4-carried inversions correctly rewritten at impl (37-encounter count from source, "swept 23-route / classification-complete" header); idiom sweep clean (no gateway route, no `str(e)`/PHI logging on new files, no `Co-Authored-By` on any of the 24 commits); baseline re-measured **1333 passed, 19 deselected, 1 xfailed** (`make test-docker`, 33.61s) = 1317 + exactly the 16 functions (9+4+3), deselected/xfailed unmoved, matches Delivery V2; `make eval` green; `eval/kg/run.py` → 53 enc / 53 summaries / 53 labs / 3 retrievals, cross-patient 1042→1043 refused with retrievals 0. Adv-reviewer clean over frozen Spec + diff only: 16 new tests re-run `16 passed`, every code citation resolved against source (records-service `:104,:108-113,:115,:128-129,:194,:335-370`, schemas `:18-32`, all 23 gateway route line numbers), 23-route partition (13 in-set + 10 excluded) exhaustive and self-consistent vs live `gw.app.routes`, no production route touched, arithmetic spot-checks correct (255/810≈31%, 37 enc→38 queries); no correctness bug, no SPEC-row violation. `gate:` rows 9-14 human-observed clean this session against `docs/research/w4-findings.md`: SPEC-9 HAR repro table `:23-26` (both requests `Bearer <patient-1042-token>`, 1042→200 own, 1043→200 sibling) · SPEC-10 both enablers `:42-65` (no session→patient bind; sequential/guessable ids, `db/schema.sql:34`) · SPEC-11 swept 23-route exposure set `:67-119` incl. `/patients/{id}/relevant-records` and the e6 search path, "w4 does not re-fix" `:126` (verdict-correctness residual per w4-D-24) · SPEC-12 N+1 note `:131-169` cites `services/records-service/app.py:104-140,128-129` and p95=3.8s · SPEC-13 50+-encounter argument `:152-160` · SPEC-14 session finding `:173-210` (`auth.yaml:2` declarative-only vs hardcoded `security.py:275-279`, no code change). 1 finding above, none red.
