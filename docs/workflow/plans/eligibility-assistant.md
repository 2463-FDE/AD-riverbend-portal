# eligibility-assistant — plan file (deleted at delivery; contract: docs/workflow/eligibility-assistant.md)

## Decisions

| ID | Stage | Decision | Why (one sentence) |
|---|---|---|---|
| eligibility-assistant-D-1 | req | ~~Item id `a1` — new prefix for the code-freeze agentic mandate (owner 2026-08-23)~~ **Renamed 2026-08-25:** `eligibility-assistant` — feature-slug naming rule, docs/workflow/README.md | Neither a client weekly ask (`w`) nor an engineering batch (`e`); the prefix keeps the freeze item distinguishable in the registries. |
| eligibility-assistant-D-2 | req | Framework = LangChain v1 agent; the Claude Agent SDK is not chosen (owner 2026-08-23). Spike done — E-1 (2026-08-24). **Amended 2026-08-24 from E-1(a):** LangChain v1 `create_agent` compiles to a langgraph `CompiledStateGraph`; langgraph is v1's internal runtime and ships as a dependency, so "no LangGraph" means **no hand-authored graphs, state machines, or human-review transitions** — not no dependency. **Exception clause:** hand-authored LangGraph is permitted when, and only when, a scope change makes it the best solution (real multi-step state, a human-review transition, or a multi-agent workflow); the change is recorded as a new decision naming the justifying scope change and approved by the owner before it enters the spec or plan | The ask's first-named option; LangSmith-native tracing; no real state machine or human-review transition exists on this path today to justify a hand-authored graph, and the exception keeps the door open without a silent reversal later. |
| eligibility-assistant-D-3 | req | One item, two PRs: PR-1 = agentic retrieval on the request path (REQ-1–6, REQ-12–15, and the deterministic acceptance tests of eligibility-assistant-D-10); PR-2 = end-to-end trace, exact-sha live proof, demo/handoff docs (REQ-7–11). *Amended 2026-08-24: REQ-13–15 and the acceptance harness ride PR-1 because they are request-path behaviour the cases assert.* One spec, one plan, two impl chunks (owner 2026-08-23). *Amended 2026-08-24 (owner): PR-1 lands as three sequential PRs, 1a → 1b → 1c, per eligibility-assistant-D-34; scope and SPEC set unchanged.* | The core pass/fail outcome lands even if the trace review loop runs long; chunking never narrows scope. |
| eligibility-assistant-D-4 | req | w5–w8 parked: each header stamped `PARKED 2026-08-23 (client priority)`; otherwise untouched, still working-tree only (owner 2026-08-23) | The ask makes the agentic design and trace 100% of delivery priority until demonstrated. |
| eligibility-assistant-D-5 | req | Scope is the authenticated front-desk eligibility assistant path only — portal `/assistant` → gateway `/ai/visit-chat` → ai-assistant `/visit-chat`; `/intake-instructions` is unchanged (from the ask, 2026-08-22) | The ask names that path as the brownfield base to extend; widening to intake-instructions is unrelated work the ask tells us not to let displace the core outcome. |
| eligibility-assistant-D-6 | req | ~~The synthetic corpus is authored by us; no client documents exist or are expected (owner 2026-08-23).~~ **Superseded 2026-08-24:** the corpus is the client package `Armaan-Eligibility-Client-Inputs-Only-2026-08-24` — its `approval_status: approved` manifest rows only (87), vendored in-repo as versioned fixtures with each file's content sha256 pinned to `document-manifest.json`, license disposition kept per document (public-domain full text / citation-only bibliographic record + original note / original synthetic). `evaluations/fixtures/FIX-NEG-*` are vendored **only** under `tests/` as negative inputs, never under the corpus path. ~~Which rows to vendor (all 87 vs the cited subset) and the corpus location are plan-stage~~ **Amended 2026-08-24 (req-review r1):** all 87 rows are vendored — the corpus is 131 KB (E-3), a subset saves nothing and breaks the manifest-sha pin as a whole; location stays plan-stage **Note 2026-08-30 (`corpus` PR #93 codex review r1 MINOR-1, E — the finding is answered from this decision):** byte-identical means byte-identical including whitespace. `procedures/front-desk-eligibility-procedure.md:3-4` end in the two-space markdown hard line break — a rendering-significant sequence in the package's own bytes, not lint noise — so `git diff --check` flags the vendored file at every re-vendoring. Stripping it is refused on this decision, and the refusal is mechanical, not stylistic: runtime check on a scratch copy only (`shutil.copytree` of `policy_corpus/` to a tmp dir, `policy_index.load(root=…)`; the tracked tree is untouched and the gate r5 f3 non-publish rule keeps module state untouched) — stripping the two lines takes the file 1,227 → 1,223 bytes and `load` raises `CorpusLoadError: sha256 mismatch (DOC-FRONT-DESK-ELIGIBILITY-PROCEDURE)`; the pinned sha `5663e141…` is the package manifest's own, so updating it to match would break `document-manifest.json`'s byte-identity in turn. Nothing gates on the flag: no CI job runs `git diff --check` (`.github/workflows/ci.yml` — `frontend`, `frontend-boot`, `services`, `tests`, `eval`, `secret-scan`, `workflow-doc-cap`, `migrations`, `docker-build`). Accepted vendored-source exception, standing for every vendored file rather than this one. | The client supplied the corpus the ask assumed we would fabricate; a sha-pinned copy keeps the demo reproducible, keeps the licensing split auditable, and lets the drift gate prove the retriever cannot see a fixture. |
| eligibility-assistant-D-7 | req | Handoff ownership recorded as Armaan Dhanoa, engagement lead (owner 2026-08-23) | REQ-10 requires a named owner in the handoff record. |
| eligibility-assistant-D-8 | req | Live-proof credentials: Bedrock bearer key available (owner-held `.env`); LangSmith API key ~~**not yet provisioned** — recorded as the item's standing blocker, needed before PR-2's trace validation and the REQ-9 live proof~~ (owner 2026-08-23). **RESOLVED 2026-08-26:** key provisioned in the owner's `.env` and smoke-tested in session — `Client().info` and `list_projects` authenticate, one metadata-only run through the repo's `tracing.wrap_create` landed in project `Riverbend` with `inputs: {}` / `outputs: None` (E-7); `LANGSMITH_TRACING` stays `false` at rest | The freeze directive asks for blockers to be reported; trace verification against a real LangSmith project cannot run keyless. |
| eligibility-assistant-D-9 | req | **RESOLVED 2026-08-24 (req-review r1 #7): shape (ii)** — the agent's model binding is a thin LangChain chat-model over the existing `invoke_model` seam in `llm_client` (Anthropic Messages format carries `tools` / `tool_use` natively), so the byte-based budget gate, bearer and pricing fail-closed guards, and typed errors apply to the bytes that actually leave the process. Shape (i) is disqualified: `ChatBedrockConverse` sends a Converse body, so a gate wrapped around it would measure a payload that is not the egress payload. E-1 proved the framework only over Converse; **E-4** (live spike of shape ii with a model-invoked tool) is due before spec freeze. **E-4 done 2026-08-24: shape (ii) proven live, 12/12; one additive seam delta (`_adapt` superset) carried to the spec.** Original text kept for the record: **OPEN — resolved at plan stage.** From E-1(b): the spike's `ChatBedrockConverse` calls the Bedrock **Converse** API, while the repo's `llm_client` calls `invoke_model`; both honor bearer auth. Two candidate shapes: (i) keep `ChatBedrockConverse` and wrap the existing budget preflight / pricing fail-closed / bearer fail-closed / typed-error controls around the Converse client; (ii) rebuild the agent's model binding on the existing `invoke_model` seam in `llm_client` so the controls apply unchanged. Decision criterion: REQ-6 requires the existing `llm_client` controls to stay in force and REQ-1/REQ-6 require the framework to execute the live turn — whichever shape satisfies both with the smaller surface wins; a shape that bypasses any control fails regardless of convenience | The control set is the item's preserved brownfield base (ask: "Preserve those controls"); which API the agent speaks is an implementation seam, so it belongs to the plan, not the requirements. |
| eligibility-assistant-D-10 | req | Acceptance harness = the client's `evaluations/eligibility-assistant-evaluations.jsonl` (32 cases, fields `id/category/scenario/allowed_roles/refused_roles/expected_source_ids/expected_sections/expected_outcome/refusal_or_escalation/pass_criteria`). Each case becomes one deterministic test in the default suite (fixture mode, model output stubbed to the case's expected bounded selection where the model is in the loop); the live demo (REQ-10) replays a named subset against real Bedrock. The jsonl is vendored verbatim and never edited — a case we cannot pass is reported as a known limitation, not rewritten | Client: "demonstrate it against the acceptance evaluations in this package"; deterministic tests give CI a pass/fail line the freeze gate can hold, and verbatim vendoring keeps the harness the client's, not ours. |
| eligibility-assistant-D-11 | req | Cross-patient acceptance case is met by **refusing the turn**: a `/visit-chat` turn whose free text or payload references a member/patient other than the turn's single `insurance_id` gets an explicit refusal outcome before retrieval or eligibility runs, for every role. The session↔patient binding gap (D11, intentional) is not touched; the "same patient" the EVAL case allows is the one `insurance_id` the turn carries (owner 2026-08-24) | Passes the case with no authz change and no reversal of a deliberate teaching artifact; a known-limitation write-up instead would have left an acceptance case red at handoff. |
| eligibility-assistant-D-12 | req | **Harness ↔ corpus contract** (owner 2026-08-24, req-review r1 #1). `expected_source_ids` carries three kinds of source and each asserts differently: a manifest `DOC-*` id = must be retrieved on the turn and rendered as a citation; a `procedures/*.md` path = the manifest row it is vendored as (`DOC-FRONT-DESK-*`, `DOC-COVERAGE-QUESTION-CHEAT-SHEET`, `DOC-ESCALATION-MATRIX`), asserted the same way; a `policies/*.md` path = behaviour governed — the test asserts the expected outcome / action / refusal, never a citation. Three test kinds: turn (30), corpus-gate (EVAL-031), trace-payload (EVAL-019). The jsonl stays verbatim (eligibility-assistant-D-10); a one-paragraph clarification goes to the client before PR-1. **Amended 2026-08-24 (req-review r2 #2):** a `DOC-*` expected on a deterministic turn is satisfied by the reason table's fixed citation (eligibility-assistant-D-19) — fetched by id through the same index, rendered — so "retrieved + rendered" is one rule on both paths; EVAL-014 splits into two turn tests (subverted / resists); the client paragraph carries both and asks subset-vs-exact on `expected_source_ids` | 16/32 cases cite a non-manifest source and 5 cite only such sources, so REQ-2 + REQ-3 + eligibility-assistant-D-10 as written made 10+ cases unpassable; E-2 read the package but never joined harness against manifest. |
| eligibility-assistant-D-13 | req | **Retriever = deterministic in-process metadata lookup over the vendored corpus, no embeddings** (owner 2026-08-24, req-review r1 premise). Model-invoked (REQ-1′ holds); arguments are closed enums only — payer, topic (= manifest `category`), product, state — no free-text argument; returns id/title/section/version/section-text; source tier for REQ-13′ is computed in code from category + license disposition. The enum index over the 87 rows is a curated, tracked artifact (manifest applicability is prose: 81 distinct strings) with a coverage test | 131 KB pre-sectioned corpus with the client's own precedence rule; embeddings would add a second model dependency, nondeterminism across 32 tests, and infra in a seven-working-day window for no retrieval gain; enum-only args make REQ-3′'s "clerk text never enters a query" true by construction instead of by assertion. |
| eligibility-assistant-D-14 | req | **What the model sees** (owner 2026-08-24, req-review r1 #3, #4): closed inputs only — the clerk picks a question type from a portal menu whose rows are the client cheat sheet (REQ-17) and an explicit `emergency: bool` (REQ-14′); clerk free text stays deterministic-only (`_derive_intent`) and never reaches the prompt, as today. The emergency turn is a deterministic short-circuit with no model, no retrieval, no payer call, fixed EMTALA citations, mode `care_first` | Three options: extend the regex classifier to ~15 question kinds (brittle), send redacted free text to Bedrock (D13 shift, no BAA, requirements silent), or a portal selector (deterministic, PHI-safe, demo-visible, gives the model real inputs — payer × question × status — for its retrieval decision). Emergency: model latency must never gate screening, so a model-in-loop emergency turn is the wrong design regardless of prompt. |
| eligibility-assistant-D-15 | req | **Outcome enum separate from action ids; catalog extended by named ids** (owner 2026-08-24, req-review r1 #2). Ten closed outcomes (`active` `inactive` `unknown` `unavailable` `reverify` `conflict` `refuse_definitive` `refuse` `stop` `care_first`); the action catalog gains `refuse`, `escalate`, `state_conflict`, `care_first`, `reverify`, `note_disputed`, `stop` as catalog templates; the 32-case mapping lives in the contract Appendix | REQ-13's note that refuse/escalate "are existing catalog actions" was false (eight ids, none of them); without a written enum the plan widens silently or someone maps `conflict` onto `proceed_per_policy`; ~20 harness outcomes collapse onto `active` + a specific item. |
| eligibility-assistant-D-16 | req | **Correlation id moves to PR-1** (owner 2026-08-24, req-review r1 #8; amends eligibility-assistant-D-3): born in the portal's Next server route, header on every hop portal → gateway → ai-assistant, asserted keyless; the LangSmith tree that hangs off it stays PR-2. LangSmith key (eligibility-assistant-D-8) escalated to the owner 2026-08-24 as a freeze blocker, needed by 2026-08-27 | Zero correlation ids exist in the estate; the header is cheap, correlates even keyless, and unblocks the trace shape. The freeze directive asked for blockers to be reported — a line in a working-tree file is not a report. |
| eligibility-assistant-D-17 | req | **`staff` = known limitation, not a refusal** (owner 2026-08-24, req-review r1 #9): `staff` keeps `ai.use` as the compat default role; ~~a test pins `staff` absent from the assistant's own allowed-role set~~ **amended 2026-08-24 (spec-review r3 new #3):** the assistant has no role set to pin — no role reaches it — so the pin is a config test on the `ai.use` grants in `config/roles.yaml` (= `front_desk`, `admin`, `staff`), and the gateway stays the only role gate; EVAL-030 is met as "reclassify" and the handoff record names the limitation | A new `staff` account is indistinguishable from an old one at the gateway; refusing it is an authz human-gate plus a compat break, not worth the freeze window. |
| eligibility-assistant-D-18 | req | **Turn order = two model calls split around the app-triggered payer call** (owner 2026-08-24, req-review r2 #1): understand → model₁ (retriever arguments) → retriever → payer call (application-triggered on the deterministic intent, keyed on intent not on the tool result; a `before_model` hook on the second iteration runs it and injects status) → model₂ (citations + action from retrieved rows + status) → validate → ground → phrase. Payer call runs **exactly once** per turn under a once-guard shared by the agent step and the fallback path; model₁ returning with no tool call is bounded out as `no_retrieval`; the `awaiting_id` / `ambiguous_id` no-model short-circuit (`app.py:757`) is retained. Which call is agentic about what is stated in REQ-1″. E-4 proves the hook, the guard, and the bound — **proven live 2026-08-24 (E-4 A/B/C)** | The draft order (agent before payer) had model₂ choose `retry_shortly` vs `self_pay_options` blind to status and then fail the status-keyed gate `required ⊆ selection ⊆ allowed` on the happy path — a demo showing `mode=fallback` on active coverage; the split keeps retrieval before the eligibility action (client wording), spends exactly REQ-6″'s bound, and lets the model see `unavailable` before citing `DOC-SYN-DOWNTIME` (EVAL-015). |
| eligibility-assistant-D-19 | req | **Deterministic-turn reason table** (owner 2026-08-24, req-review r2 #2): a closed reason enum — `emergency` `cross_patient` `validation_reject` `no_retrieval` `spend_stop` `model_failure` — each with a fixed citation set from the vendored corpus (Appendix table), fetched **by id through the same bounded index** via an application-side entry the model tool does not have (REQ-3″), rendered as citations, with the reason in response and trace (REQ-8″). REQ-16's "no citations" becomes "the reason's fixed citation"; REQ-1″'s exception list collapses to one class (deterministic turns cite from the table, the agent path cites from the model's retrieved set); role refusals carry no fixed citation | eligibility-assistant-D-12 ("`DOC-*` = retrieved + rendered") and REQ-16 ("fallback = no citations") collided on EVAL-012/014/017/018/024 — every no-model path was un-citable; REQ-14′ had already solved it for emergency by special case, and one table keyed by reason is the same mechanism generalised instead of enumerated. |
| eligibility-assistant-D-20 | spec | **RESOLVED 2026-08-24 (spec-review r3: as stated)** — SPEC-45 orders the emergency short-circuit **before** the understand step's `awaiting_id` / `ambiguous_id` checks: an emergency turn with no member id still gets care-first | REQ-14″: no eligibility state may gate screening; an id prompt on an emergency turn is such a gate. |
| eligibility-assistant-D-21 | spec | **RESOLVED 2026-08-24 (spec-review r3: as stated)** — SPEC-22 reason split: model₁ returning with no tool call = `no_retrieval`; a second tool call or a third model call = `validation_reject` (Appendix row already lists both under that reason) | Keeps the eligibility-assistant-D-19 enum at six values; the overrun is a vocabulary violation of the bound, not a missing retrieval. |
| eligibility-assistant-D-22 | spec | **RESOLVED 2026-08-24 (spec-review r3: as stated)** — SPEC-36 check mechanism: impl-gate `cmd:` (suite under docker `--network none`, empty env) or `gate:` if that run cannot be made reproducible | A `cmd:` is preferred; a `gate:` is honest if the network-denied run is click-ops. |
| eligibility-assistant-D-23 | spec | **RESOLVED 2026-08-24 (spec-review r3: as stated)** — SPEC-27 reading: the spec pins one trace tree with eight named stages; whether portal entry and gateway entry (separate processes) emit their own spans or only mint/carry the correlation id is plan-stage | Portal is Next.js, gateway is FastAPI — span emission there is design; the contract is the tree, not the emitter. |
| eligibility-assistant-D-24 | spec | **RESOLVED 2026-08-24 (spec-review r3: as stated, plus)** — SPEC-17 (EVAL-014 resists) rides the opt-in live suite, so PR-1 lands it skipped by default; SPEC-13 (subverted) is the default-suite proof of the gate; **the handoff record lists which of the 32 cases are default-green and which live-gated** (SPEC-39b after eligibility-assistant-D-47), so "32/32" cannot mean two things | REQ-11″ names the split; the freeze gate needs the owner to accept a skipped-by-default acceptance test as the record for that half. |
| eligibility-assistant-D-25 | spec | **Product and state are clerk inputs; model₁ chooses the topic** (owner 2026-08-24, spec-review r3 #1): the portal adds closed product (commercial · Medicare Advantage · Medicaid MCO · CHIP · Original Medicare · `unconfirmed`) and state (curated from the index at vendoring + `unconfirmed`) menus beside question type and payer; ~~the retriever tool keeps its four enum arguments, payer/product/state must equal the turn's selections (mismatch → `validation_reject`)~~ **amended 2026-08-24 (spec-review r4 #3):** the model-facing tool is `policy_lookup(topic)` only; the application binds payer/product/state at execution — no mismatch path, no enum checks on values the app already holds; E-4's `policy_lookup(payer=…, topic=…)` shape narrows by one argument. Topic — one of the 25 manifest categories — is the argument model₁ decides | Nothing in the request, facts, or 271 projection carries product or state, and the verdict arrives after model₁, so REQ-1″'s "model₁ picks product/state" was a model guessing; the client's own precedence rule makes them clerk-confirmed facts (steps 2–3) with `unconfirmed` as a first-class value (EVAL-023), while 9 question types → 25 categories is a real, non-functional choice. |
| eligibility-assistant-D-26 | spec | **`spend_stop` ends the agent step, not the lookup** (owner 2026-08-24, spec-review r3 #2, **amended r4 #1**): outcome `stop`, no further model egress, `DOC-SYN-SPEND-STOP`, mode `fallback` + reason; ~~no further call of any kind — the payer call is not made if it has not run~~ the payer call runs regardless of the site (model-free, costs nothing) and its verdict persists in facts, not rendered — symmetric at model₁ and model₂. `spend_stop` = `_enforce_budget` (per-request, deterministic for a given payload: a config fault that trips always or never); the estate's spend *event* is ADR 0007's daily ceiling → gateway 429 before any turn (`_reserve_ai_budget`, existing `test_aggregate_ceiling_bounds_total_paid_calls`), the other `stop` surface, named in the handoff record | SPEC-15 (`stop`), SPEC-52 (payer result if obtained) and EVAL-017 (`stop`) said three things; the client's safeguard is "stop … complete eligibility by a human", which is not "render the verdict anyway" — but r3's "no payer call" made a check turn that trips at model₁ trip every time, killing the brownfield lookup for that configuration, the regression REQ-16's own rationale forbids. |
| eligibility-assistant-D-27 | spec | **The agent path is every turn that reaches the model** (owner 2026-08-24, spec-review r3 #3): after the emergency and cross-patient gates, any turn whose status leaves the model freedom (`app.py:757` short-circuit retained) runs model₁ → retriever → [payer] → model₂; the payer call keeps today's `app.py:845` gate — exactly once on `check_eligibility` / `recheck_eligibility` with an id, never on `ask_status` / `other` / `clarify_member_id` — so it is "at most once per turn", and on non-payer intents model₂ receives the remembered verdict as today | "Enters the agent path" was undefined and "payer exactly once per turn regardless" was false on three of five intents; one path for every model-consulting turn keeps the two-call / one-tool bound universal and leaves no retrieval-less model path (which the ask lists as failing). |
| eligibility-assistant-D-28 | spec | **A deterministic turn = no model output reaches the reply** (owner 2026-08-24, spec-review r3 new #1): citations and action come from the eligibility-assistant-D-19 table; zero model calls is asserted only for `emergency` and `cross_patient` (pre-model), the other four reasons fire after model₁ has egressed | REQ-1″'s "no model in the loop" made SPEC-3 unpassable for `validation_reject` / `no_retrieval` / `spend_stop`-at-model₂ / `model_failure`; the property that matters is what reaches the clerk, not how many calls were made. |
| eligibility-assistant-D-29 | spec | **Budget = the existing per-request preflight, per call; one slot per turn** (owner 2026-08-24, spec-review r3 new #2): `_enforce_budget` (`llm_client.py:458`) gates each of the two calls, the turn's ceiling is 2× the per-request caps by the `run_limit=2` bound, no new per-turn reservation; one ADR 0011 §7 admission slot covers both calls and `llm_egress` = "any payload left the process" | "Preflight spans both calls" named no mechanism; a real per-turn reservation would have to estimate model₂'s payload before retrieval returns; E-4 already measured the per-call gate running 2×; without a slot row, ADR 0011 §7's one-call accounting had no stated successor. |
| eligibility-assistant-D-30 | spec | **SPEC-48 drops the assistant-side role set** (owner 2026-08-24, spec-review r3 new #3): the gateway posts `{message, turns, facts}` (`gateway/app.py:1153`) and `VisitChatRequest` is `extra="forbid"`, so no role reaches the assistant; the row becomes a config test on `config/roles.yaml` `ai.use` grants in the gateway suite; adding a role to the body (second authz layer, `⚠ human-gate`) is rejected for the freeze window | A spec row asserting a set the assistant never sees was a pretence; EVAL-029/030 still pass, with `staff` documented as the eligibility-assistant-D-17 limitation. |
| eligibility-assistant-D-31 | spec | **Acceptance tests are parametrised on the bracketed case id** (owner 2026-08-24, spec-review r3 new #4): `[EVAL-001]` … `[EVAL-014-subverted]` / `[EVAL-014-resists]`; SPEC-37's cmd greps `EVAL-0[0-9][0-9]` on `--collect-only` output | `--collect-only -q` prints `path::name`; the draft's own test names (`test_eval_007_020_013_023`, `test_no_result_never_inactive`) yield ~11 ids, so the cmd could never reach 32. |
| eligibility-assistant-D-32 | spec | **`unconfirmed` is non-filtering** (owner 2026-08-24, spec-review r4 #2): a product or state of `unconfirmed` does not filter the index on that axis; applicability mismatch for REQ-13′ / SPEC-42 is computed in code from the rows returned, never inferred from an empty result | The manifest's state axis is "United States" except two California rows and CHIP has no dedicated row, so a filtering `unconfirmed` would empty the result on exactly the turns EVAL-023 needs to reach `refuse_definitive` through the applicability check. |
| eligibility-assistant-D-33 | spec | **Mode `no_lookup` for the no-freedom short-circuit** (owner 2026-08-24, spec-review r4 minor): the mode set is fixed at six values — real / fixture / fallback / care_first / refuse / no_lookup — the last carried by the `awaiting_id` / `ambiguous_id` turn (REQ-8‴, REQ-16‴, SPEC-33, SPEC-50) | SPEC-33 pinned a five-value set while REQ-16″ deferred the short-circuit's mode to plan stage — the set was going to change; `no_lookup` is `visit_templates.NO_LOOKUP_STATUSES`' own name, so it is fixed now. |
| eligibility-assistant-D-34 | plan | **PR-1 split into 1a / 1b / 1c** (owner 2026-08-24; amends eligibility-assistant-D-3). Landing order fixed; 1a and 1b land dark (not wired to the request path) and 1c wires them. **1a corpus:** 87 vendored docs with manifest sha pin, the tracked index, the in-process `topic`-only retriever, the tier-rank function, `FIX-NEG-*` negatives under `tests/` — SPEC-7–11, 38, 43. **1b seam:** `llm_client._adapt` carrying `tool_use` id/name/input + `stop_reason` (E-4 delta i), the thin `BaseChatModel` binding, ~~LangChain v1 pins~~, bearer fail-closed test — SPEC-24. **Amended 2026-08-27 (eligibility-assistant-D-59, corpus gate r1 f6 / llm-seam gate r1 f1): the LangChain v1 pins are a `corpus` change row, not 1b's — `llm-seam` depends on `corpus` for them.** **1c wire:** turn order and gates, agent path, validation / outcome / mode enums, fallback, once-guard, `VisitFacts` provenance, admission slot, gateway authz + correlation forward, portal menus / emergency / citations / correlation mint, the 32-case harness, the `--network none` proof — every other PR-1 row (SPEC-1–6, 12–23, 25–26, 33, 36–37, 39, 41–42, 44–56). Not split further: portal cannot land apart from 1c (`VisitChatRequest` `extra="forbid"` + SPEC-55 reject unknown / missing selections, so a portal ahead of or behind the backend breaks the live path); the harness asserts full behaviour and has nothing to run against before 1c. Each PR gets its own impl gate, codex loop, and deliberately reported baseline move (CLAUDE.md §6). **Amended 2026-08-25 (re-slice):** PR-1a / PR-1b / PR-1c / PR-2 now map to tickets `corpus` / `llm-seam` / `turn` / `trace`, each with its own plan file under `plans/eligibility-assistant/`; landing order, scope and SPEC sets unchanged | Current PR-1 is ~45 SPEC rows across four tiers plus 131 KB of fixtures and an index artifact — the e5-chunk2 shape that closed unmerged after eight rounds; the corpus is mechanical review that would drown a behaviour diff, and the egress seam is the one file a reviewer wants to see alone. Sequential landing saves no wall-clock, only review depth; scope narrows nowhere. |
| eligibility-assistant-D-35 | plan | **Corpus, harness and fixture locations (PR-1a).** The 87 approved documents are vendored at `services/ai-assistant/policy_corpus/` keeping the package's relative paths (`documents/federal/` ×41, `documents/payers/citation-only/` ×14, `documents/synthetic/` ×26 — every reason-table `DOC-SYN-*` lives here — `documents/x12/citation-only/` ×2, `procedures/` ×4; measured from the manifest 2026-08-25, gate r1 f11) beside a verbatim `document-manifest.json`; the corpus ships in the image through the existing Dockerfile `COPY . .` (`.dockerignore` excludes only caches) and ~~is loaded lazily on the first turn~~ **amended 2026-08-26 (SPEC-59 reads "fail at load, before any turn"):** is loaded and sha-verified in a FastAPI lifespan startup hook (`app.py` has none today, `:51`), with a lazy load on first use for direct module callers; ~~CI's `services` import smoke (`python -c "import app"`) runs no startup hook, so it is unchanged~~ **note 2, 2026-08-27 (`corpus` gate r4 f4):** the smoke runs no startup hook, but since the r3 dark `import policy_tool` in `app.py` it **does** load and sha-verify the corpus at import — `policy_tool`'s `topic` enum is built at import through the one loader, `policy_index.load()` — so a corpus fault reddens `import app` (CI `services`) before any hook; the lifespan hook re-runs the same `load()` at boot, and a mismatch now fails the container at boot rather than the first turn. The harness `evaluations/eligibility-assistant-evaluations.jsonl` is vendored verbatim at `tests/fixtures/a1/` and the seven `FIX-NEG-*` files at `tests/fixtures/a1/fix_neg/` — never under the corpus path. The six client policies, `scope/` and `sources/` are not vendored: behaviour sources cited by package name (eligibility-assistant-D-12). **Note 3, 2026-08-27 (`corpus` gate r7 f1; record only, no rule change):** "tests reach them through `conftest.load_module`'s service dir" (rationale) is true but not sufficient — `load_module` loads under a *unique* name, so the corpus tests reach `policy_index` / `policy_tool` / `app` through the one pinned rig `tests/a1_corpus_rig.py` (eligibility-assistant-D-66), not through per-file loads; `turn`'s `tests/a1_rig.py` is a different file and imports it. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)** | An in-process read needs the files beside the service (ADR 0001, no shared lib); tests reach them through `conftest.load_module`'s service dir; a tests-only fixture home makes the SPEC-8 corpus gate a path check rather than a content search. |
| eligibility-assistant-D-36 | plan | **Enum spellings and index shape (PR-1a/1c).** `question_type` = `covered_today · will_it_pay · in_network · referral_needed · prior_auth · who_pays_first · copay · portal_down · emergency` (the nine cheat-sheet rows, `procedures/coverage-question-cheat-sheet.md`); `payer` = `unitedhealthcare · aetna · cigna · humana · anthem_blue · medicare · medicaid` (the client mix in `policies/synthetic-payer-mix-and-applicability.md`; Availity and X12 are paths, not payers); `product` = `commercial · medicare_advantage · medicaid_mco · chip · original_medicare · unconfirmed` (REQ-17′); `state` = `CA · other_us · unconfirmed` (the manifest's state axis is "United States" except two California rows — spec-review r4). `policy_corpus/index.json` is a tracked artifact curated at vendoring, one entry per approved document: `document_id`, `topics` (= the manifest `category`, parsed), `payers` / `products` (subsets of the enums, or `*`), `states` (`*` or `CA`), `needs_product_confirmation` (true on citation-only payer rows — EVAL-023), `sections` (the manifest `section_labels`). Lookup filters topic ∧ payer ∧ product ∧ state, `*` matches any value and `unconfirmed` matches every row on its axis (eligibility-assistant-D-32), returns one row per (document, section) with id/title/section/version/section-text, ordered by tier rank then version date, capped by `A1_RETRIEVAL_MAX_ROWS` (default 12; **6 from 2026-08-27, eligibility-assistant-D-64; 5 from the same day's D-64 amendment — `corpus` gate r4 f1, this note corrected at r5 f2**). The clerk's payer selection is not cross-checked against the member-id prefix catalog — a recorded residual, Landmines block. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)**. **Amended 2026-08-27 (eligibility-assistant-D-61 / eligibility-assistant-D-62, `corpus` gate r2 f1 / f2): one row per document — `section` = the manifest `section_labels` string verbatim, `section-text` = the whole vendored file; ordered by tier rank, then `retrieval_date` desc, then `document_id` asc — "version date" is not a sort key.** | Manifest applicability is prose (81 strings / 87 rows, E-3), so the axes are curated once and pinned by the SPEC-11 coverage test; a national row must serve a California turn, which is what `*` is for. |
| eligibility-assistant-D-37 | plan | **Agent runtime = E-4's measured shape, unchanged (PR-1b/1c).** `services/ai-assistant/agent_binding.py`: `SeamChatModel(BaseChatModel)` whose only egress is `llm_client._call` — tools ride `extra_body["tools"]` (already inside the byte-bound budget gate), messages convert per E-4 including the role-alternation merge of the tool result and the injected status. `services/ai-assistant/agent_turn.py`: `create_agent(model, tools=[policy_lookup], middleware=[TurnMiddleware, ModelCallLimitMiddleware(run_limit=2, exit_behavior="end")])`; tools stay defined on both calls (the shape E-4 proved 12/12 live; `tool_choice: none` and structured output on model₂ are unmeasured combinations and are not used). `after_model`: model₁ with no `tool_use` → `no_retrieval`; model₁ with more than one `tool_use`, any `tool_use` at model₂, or the run-limit exit → `validation_reject` (eligibility-assistant-D-21). `before_model` on the tool-result iteration runs the once-guarded, intent-keyed payer call and injects one user message: the a1 status label, the retrieved row keys, and the instruction to answer with one JSON object `{"action_ids": [...], "citation_ids": [...]}`; model₂'s text is parsed into `AgentDecision` (pydantic, `extra="forbid"`), a parse failure is `validation_reject`. `LLMBudgetExceeded` escaping `agent.invoke` is `spend_stop`; any other `LLMError` is `model_failure`. The existing `tracing.wrap_create` (installed at `llm_client` import) traces both Bedrock calls unchanged. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)** | Every deviation from the measured composition would be an unmeasured surface a week before the freeze; the JSON-text decision is the E-4 `ACTION=/CITE=` line with a validator in front of it. |
| eligibility-assistant-D-38 | plan | **Outcome, required/allowed sets, conflict and applicability are computed in code from closed inputs (PR-1c).** `services/ai-assistant/outcome.py`. Deterministic turns take the reason table: `emergency` → `care_first`, `cross_patient` → `refuse`, `spend_stop` → `stop`; `validation_reject` / `no_retrieval` / `model_failure` → the payer-derived outcome. Payer-derived: verdict `active` → `active`; `inactive` → `inactive`; `pending`, or a degraded verdict (`_degraded`: `payer is None`) → `unavailable` (SPEC-53 — the ADR 0010 vocabulary has no `unavailable`, so it is derived here, never a new payer status); a 2xx non-definitive verdict (`unknown` with a payer) or no verdict → `unknown`. Agent path, in order: applicability check in code — retrieved set empty, or no row of tier ≤ 3, or product/state `unconfirmed` with only `needs_product_confirmation` rows → `refuse_definitive`, required `escalate` (EVAL-006/013/023); model₂ selected `state_conflict` (valid only with ≥ 2 citation ids, else `validation_reject`) → `conflict`, required `state_conflict` + `escalate` (EVAL-007/020); model₂ selected `reverify` / `note_disputed` → `reverify` (EVAL-004/005/025); otherwise payer-derived, with `unknown` / `unavailable` requiring `escalate` (EVAL-003/015/018). Conflict *detection* is model₂'s bounded choice of `state_conflict`; its consequences (cite both, no definitive line, escalate) and the tier order are code (SPEC-42/43). `default_selection(a1_status, question_type)` / `allowed_selection` extend today's `required ⊆ selection ⊆ allowed` gate; question-type boundary requirements on `active`: `will_it_pay` → `no_guarantee`, `prior_auth` → `auth_unknown`, `in_network` → `network_unknown` + `escalate`, `referral_needed` → `referral_required`, `who_pays_first` → `collect_secondary` (EVAL-008/009/022/026/027/028). Tier (SPEC-43) from license class × category: 1 = public-domain regulation (`42CFR` / `EMTALA-42CFR` / `HIPAA-164-*` ids, categories `emergency-care-boundary` / `privacy-minimum-necessary`); 2 = other public-domain federal official sources; 3 = citation-only payer / X12 records; 4 = original synthetic clinic procedures (`front-desk-procedure`, `front-desk-scripts`, the cheat sheet, the escalation matrix, `DOC-SYN-*` procedures); 5 = `payer-training-summary`. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)**. **Amendment note 2026-08-27 (`corpus` gate r1 f3; clarifies, does not change, the tier rule):** tier 4 is the *rule* "every original-synthetic row whose category is not `payer-training-summary`", not the illustrative list in its parenthetical — that list names the clinic-procedure rows the Appendix happens to cite and was read as exhaustive. Consequence for the one case the gate checked: EVAL-006's expected source `DOC-SYN-CITATION-RECENCY` is category `source-recency`, license "Original synthetic training material", so it is **tier 4**, not tier 5; `refuse_definitive` still follows from "no row of tier ≤ 3" and no outcome, required set or test expectation moves. Tier 5 remains category `payer-training-summary` alone (7 rows, all `DOC-SYN-*-TRAINING-SUMMARY` / `-TRAINING`). The tier function is pinned on named ids by `tests/test_a1_conflict.py::test_tier_rank_in_code`. **Amendment note 2 2026-08-27 (`corpus` gate r3 f5, the r1 f3 class closed over all five tiers; clarifies, does not change):** every tier is a *rule* on license class × category, the parentheticals are illustrative. License class is the manifest `license_disposition` prefix — `US Government work; public domain` (41 rows), `Citation-only` (16), `Original synthetic training material` (30) — and the rules are: **1** = public-domain × category ∈ {`emergency-care-boundary`, `privacy-minimum-necessary`} (9 rows: `DOC-FED-EMTALA-CMS`, `-ER-RIGHTS`, `-42CFR-489-24`, `-CRS`, `-OIG`, `DOC-FED-HIPAA-164-502-MIN-NEC`, `-514`, `-506-TPO`, `-508-AUTH` — the id fragments in the tier-1 parenthetical were never the rule); **2** = public-domain × any other category (32); **3** = citation-only (16: 14 payer rows including the two MAC pages, 2 X12); **4** = original synthetic × category ≠ `payer-training-summary` (23, including the two `DOC-SYN-*` rows in the tier-1 categories and the cheat sheet); **5** = original synthetic × `payer-training-summary` (7). Partition 9 / 32 / 16 / 23 / 7 = 87 (package `document-manifest.json` read 2026-08-27, `license_disposition` × `category`); pinned over all 87 rows by `test_tier_rank_in_code`. **Note 3 2026-08-27 (`corpus` gate r5 f1; record only, no rule change):** the id-fragment reading of the tier-1 parenthetical — the counter-rule `corpus` verification 4n breaks with — partitions the 87 rows **7 / 34 / 16 / 23 / 7**, not the 5 / 36 stated at r3 / r4: the bare `42CFR` fragment also matches `DOC-FED-42CFR-435-POINTER` / `-438-POINTER` (tier 2 under the rule, `medicaid-managed-care`), while four tier-1 ids carry no fragment — `DOC-FED-EMTALA-CMS`, `-CRS`, `-ER-RIGHTS`, `-OIG` (9 − 4 + 2 = 7). Recomputed over all 87 rows from the package manifest 2026-08-27 | A coverage answer never rests on model judgement about tiers or applicability; the model's freedom is a bounded selection, exactly as today — the selection gate is generalised, not replaced. |
| eligibility-assistant-D-39 | plan | **Catalog extension and mode spelled (PR-1c).** `visit_templates.CATALOG` gains the seven REQ-4′ ids (`refuse`, `escalate`, `state_conflict`, `care_first`, `reverify`, `note_disputed`, `stop`) and the four boundary templates the Appendix rows name (`no_guarantee`, `auth_unknown`, `network_unknown`, `referral_required`), all reviewable policy copy under the clinical-vocabulary screen. Mode: `real` / `fixture` is `settings.a1_model_fixture` (`A1_MODEL_FIXTURE`, default `false` → `real`; the default suite's rig sets it true beside the scripted seam), so a fixture flag left on in a deployment renders `fixture` on every reply — fail-visible; the response and trace also carry the resolved model id (`settings.bedrock_model_id`) so `real` is tied to a named profile. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)**. **Amended 2026-08-27 (eligibility-assistant-D-71, `turn` gate r1 f3 / r2 f3):** "renders `fixture` on **every** reply" is narrowed to **every model-produced reply** — the mode field carries a path name (`care_first` / `refuse` / `no_lookup` / `fallback`) in preference to `real` / `fixture`, so a flag left on is observable on every agent-path turn that reaches model₂ and on no other. The `real` / `fixture` derivation from `settings.a1_model_fixture` is unchanged, which is why a keyless suite can assert `real`. | REQ-4′ names seven ids and the Appendix names the boundary templates by role; the spellings were left to plan stage (spec Exclusions). |
| eligibility-assistant-D-40 | plan | **Existing visit-chat tests are retained and re-seamed, not deleted (PR-1c).** `tests/test_ai_visit_chat.py` and `tests/test_visit_chat_phi.py` fake `llm_client.complete_structured`; after 1c the agent path's only egress is `llm_client._call` → `client.messages.create`, so both rigs move to a shared `tests/a1_rig.py` fake at `llm_client.client.messages.create` — a scripted, Bedrock-shaped response queue (call 1: one `tool_use` block; call 2: the JSON text) with `_enforce_char_cap` / `_enforce_budget` / `_require_bearer_token` left live and the bearer set to a non-placeholder test value. Assertions that pinned the single-prompt shape (`test_the_prompt_is_exactly_the_deterministic_build`, `test_prompt_carries_only_the_ids_the_status_justifies`) re-pin the two-call payloads. ~~The retained files' test count does not move; every eligibility-assistant addition is a new file.~~ **Amended 2026-08-25 (gate r1 f5):** no retained test is deleted or renamed; the spec pins additive tests in six existing files (SPEC-20 `test_visit_memory.py`, SPEC-24 `test_llm_client.py`, SPEC-26 `test_gateway_ai_proxy.py`, SPEC-47/48 `test_gateway_authz.py`, SPEC-56 `test_ai_visit_chat.py`, plus the `test_every_verdict_line_is_clinical_term_free` parametrise list extended to the six new statuses) — those files' counts move by exactly those additions and every other eligibility-assistant test is a new file. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)**. **Amended 2026-08-27 (eligibility-assistant-D-73, `turn` gate r1 f1 / r2 f1):** one bounded exception to "no retained test is deleted or renamed" — the three `tests/test_ai_visit_chat.py` contradicting-id tests (`:789`, `:862`, `:1350`) are re-pointed with their subject under eligibility-assistant-D-50 and the first is renamed; their safety assertions survive verbatim. No other retained test moves. | Characterization first (`docs/landmines.md` §3): the brownfield invariants — no free text egresses, no LLM fault discards a paid verdict, spend and health reported separately — must stay red-able through the new seam. |
| eligibility-assistant-D-41 | plan | **Trace shape (PR-2; resolves eligibility-assistant-D-23).** The root run *is* the correlation id: a UUIDv4 minted by the portal's `api/ai/visit-chat/route.ts` (`crypto.randomUUID()`), sent as `X-Correlation-Id` with `X-Portal-Sent-At`; the gateway validates the shape (mints its own for a non-portal caller), forwards it to the assistant, and opens the root in `proxy_visit_chat` with a copied `tracing.py` (per-service copy, parity test, same two-layer hide). Gateway spans: `portal_entry` (zero-length, from `X-Portal-Sent-At`; the portal emits no span and takes no langsmith dependency), `gateway_entry`, `persistence` (metadata `visit_memory` only). The assistant attaches its subtree through LangSmith distributed-trace headers forwarded on the `_post_checked` call (`tracing_context(parent=headers)`) and emits `eligibility`, `retrieval`, `bedrock` (the existing `wrap_create`, once per call), `validation`, `final_outcome`. Keyless proof: a capturing fake extending `tests/test_tracing.py::_FakeLangSmith` records names, parents and metadata; the negative scan walks every captured payload. Consequence: the gateway gains a metadata-only LangSmith egress (`langsmith==0.10.5` in `services/gateway/requirements.txt`), and the "ai-assistant is the only vendor-egress service" clauses (CLAUDE.md §2, `docs/debt-log.md` D13) are corrected in PR-2. A missing key is fail-open — no span, turn unaffected (ADR 0006 decision 3). Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)**. **Note 2026-08-27 (`trace` gate r2 f2; record of how "a copied `tracing.py`" is realised — revises `trace`'s gate r1 f4 disposition, not this decision):** the gateway copy is **byte-identical** to the assistant's, one file carrying both helper sets — `wrap_create` / `tracing_context(parent=…)` / `span(name, metadata)` and `turn_root(correlation_id)` / `span(name)` — each service calling only its own. Reason: `tests/conftest.py:16-26`'s `load_module` inserts each service dir at `sys.path[0]`, `llm_client.py:728`'s bare `import tracing` caches the first copy loaded under that name for the whole session, and twelve test files load `services/gateway/app.py` with no `tracing` in their pinned sets (`tests/test_gateway_*.py` ×9, `test_visit_chat_phi.py:71`, `test_w4_exposure_sweep.py:55`, `test_eligibility_budget_alignment.py`; `grep -l` 2026-08-27) — with two different files every one of them would bind the assistant's copy and `proxy_visit_chat`'s `turn_root` would be an `AttributeError`, and a `-k` run loading a gateway file first would hand the assistant the gateway's copy and switch tracing silently off through `llm_client.py:727-732`'s guard. Identical bytes make any bound copy correct; the parity pin becomes a byte equality (`tests/test_tracing.py::test_tracing_copies_are_byte_identical`) beside the hide-layer and output tests, and the keyless chain test installs its capturing fake on both apps' bound `tracing` objects' `_langsmith` seams | Persistence and the id's birth happen outside the assistant, so an assistant-rooted tree cannot carry eight stages; the gateway is where the request's whole lifetime is visible. |
| eligibility-assistant-D-42 | plan | **ADR 0019 records the design** — lands in PR-1a as `Accepted`, extended in PR-2 with the trace section: framework on the `invoke_model` seam, the bounded loop, the deterministic retriever and index, the outcome derivation, the trace shape; `Debt: D13`. ADR 0006 and ADR 0011 get a one-line `Extended by ADR 0019 (eligibility-assistant)` status note, never an in-place rewrite. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)**. **Note 2026-08-27 (`llm-seam` gate r6 f1; record only, no rule change):** under the ticket split (eligibility-assistant-D-55) the ADR is authored by `corpus` — frame, decision list, `Debt: D13`, the retriever / index / tier paragraph — and each later ticket appends the paragraph for the mechanism its own diff lands, as a change row in its own plan file: `llm-seam` (seam binding, `SystemMessage` → `system`, twin guard as the ADR 0004 `:57` justification, never-stream), `turn` (bounded loop, outcome derivation), `trace` (trace shape), `lifecycle`, `retrieval-eval`. The reason is this decision's own: a plan file is deleted at merge, so a record a review round could reopen must not rest on one (eligibility-assistant-D-60) | `adr/_template.md`: a decision a review round could reopen needs a record to point at; eligibility-assistant has five such. |
| eligibility-assistant-D-43 | plan | **Live tests skip, never deselect (PR-1c/2).** `tests/test_a1_live.py` (SPEC-17 *resists*, SPEC-32 live-run record) is `skipif(not os.getenv("A1_LIVE_BEDROCK"))` and reports as `skipped`; no `integration` marker — that would move the load-bearing deselected count and still never run in CI's `migrations` job. The default summary gains a `skipped` term — `1 skipped` from 1c (SPEC-17 only), `2 skipped` from PR-2 (SPEC-32 joins) — stated in each Delivery and in the CLAUDE.md §6 baseline line (corrected 2026-08-25, gate r1 f4). Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)** | SPEC-39a's own wording ("default run reports the live tests as skipped") and CLAUDE.md §6's rule that deselected/xfailed never move. |
| eligibility-assistant-D-44 | plan | **Cross-patient detection mechanism (SPEC-49; PR-1c).** Deterministic and closed: (a) two or more distinct recognised member ids in the message while the visit already holds a confirmed id; (b) a closed phrase list (`another patient`, `other patient`, `different patient`, `someone else`, `also check`, `and also`, `next patient`, `for my other`), matched case-insensitively on the free text. Two ids with no confirmed id keep today's `clarify_member_id`. A second patient named in prose with no id and no phrase is not detectable by pattern — recorded residual (REQ-15″ lists it; EVAL-012 is asserted on the phrase and id forms). Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)** | The free text keeps its deterministic-only role (REQ-17′); a name detector would be a pattern filter the transcript design already rejected (`VisitTurn` docstring). |
| eligibility-assistant-D-45 | plan | **One payload declaration for the turn (PR-1c).** `contracts/visit-chat-turn.json` declares the request (`message`, `visit_id`, `question_type`, `payer`, `product`, `state`, `emergency`), the four enums, the correlation header names, and the response's eligibility-assistant fields (`citations[]` with the four fields, `mode`, `reason`, `outcome`, `correlation_id`, `model`), asserted from `tests/test_a1_turn_contract.py` and `frontend/app/assistant/turn.contract.test.ts` — the `contracts/intake-registration.json` pattern. Proposed 2026-08-24; **confirmed (owner 2026-08-25, plan review r1)** | Portal and backend land together in 1c (eligibility-assistant-D-34) precisely because the payload is closed on both sides; one declaration keeps them from drifting apart afterwards. |
| eligibility-assistant-D-46 | plan | **Correlation id on the assistant hop, keyless (PR-1c; closes gate r1 f7 — SPEC-26's assistant half).** The gateway adds `X-Correlation-Id` to the headers dict it already passes to `_post_checked` beside `X-Internal-Auth` (`services/gateway/app.py:1150-1156`); the assistant reads it from the request headers the way it reads `x-internal-auth` (`app.py:187`, `request.headers.get`) — a header, never a body field, so `VisitChatRequest` stays `extra="forbid"` and the gateway body is unchanged — validates the UUIDv4 shape (else mints its own, so a direct caller still gets a bound turn), and binds it to the turn: echoed as `VisitChatResponse.correlation_id`, carried in `visit_chat_log_metadata`, and (PR-2) set as the root id the assistant subtree attaches under. Proposed and **confirmed (owner 2026-08-25, plan review r1)** | SPEC-26 says the assistant binds trace *and response* to the id in PR-1, keyless; the 1c change list added the response field without saying where the id came from. |
| eligibility-assistant-D-47 | plan | **SPEC-39 split into SPEC-39a / SPEC-39b (owner 2026-08-25, plan review r1; amends a frozen row — gate r1 f10).** 39a = the live-Bedrock check is opt-in via configuration and skipped by default, check `cmd:` (default run reports the live tests as skipped); 39b = the handoff record lists which acceptance cases are default-green and which live-gated (eligibility-assistant-D-24), check `gate:`. Spec row count 56 → 57; no wording changes beyond the split | The check map's rule is one mechanism per row; the row carried a `cmd:` and a `gate:` for two different obligations, and only the owner can amend a frozen row. |
| eligibility-assistant-D-48 | req | **Corpus lifecycle = status field, load-time enforcement, engineering-procedure updates** (owner 2026-08-25, in session; ask verbatim: "Solution by demo should be able to handle updating docs as well"). Each vendored manifest row gains `status` ∈ {`current`, `superseded`, `withdrawn`} and `superseded_by`; lookup and by-id fetch return `current` only; a supersession chain must resolve to a `current` row and every reason-table id must be `current`, else the loader fails at start; superseded/withdrawn files stay on disk, sha-pinned (SPEC-7 unchanged, retention norm); `effective_until` **out** — client `version_effective` is prose (E-3), parsing it is curation with no demo case; the demo update uses a synthetic `DOC-SYN-*` document, never client public-domain text; the procedure is runbook steps (SPEC-34), not tooling — no hot reload. **§1 PHI approval extended** by the owner 2026-08-25 to the lifecycle turn row and the retrieval-record row (REQ-18 / REQ-19 ⚠) | Status, not date, is the mechanism the literature names for the stale-guideline failure (E-6); a replaced document that is never cited and a removed one that fails loudly are what the demo must show; hot reload would defeat the sha pin that keeps the demo reproducible. |
| eligibility-assistant-D-49 | req | **Retrieval quality is measured before any ranker is chosen** (owner 2026-08-25, in session — "in, full"). Metadata-only lookup record (resolved filter + provenance + pre/post counts + truncated/empty flag); ranking isolated as one named unit with today's tier-then-date order; recall@k baseline over the acceptance cases in the default suite, minimum as headline, k = the configured cap; the ranker ladder for a **follow-on item** is BM25 → cross-encoder rerank → build-time embeddings, entered only when the baseline or a corpus-growth threshold (any (topic, payer, product, state) bucket > cap) says so | Every vendor pattern puts a relevance ranker after the metadata filter and ours is a hand-written order with no evidence either way (E-6) — measuring is cheaper than arguing, and the number is what the follow-on cites; reranking beat dense retrieval in the controlled biomedical comparison and build-time vectors are the only PHI-safe form, so embeddings are last, not first. |
| eligibility-assistant-D-50 | plan | **Single contradicting member id → refuse** (owner 2026-08-26, PR #89 codex r1 B2 = gate r2 f1; amends eligibility-assistant-D-44 rule (a)). When the visit holds a confirmed `insurance_id` and the message carries **one** recognised member id that differs from it, the cross-patient gate refuses — outcome `refuse`, reason `cross_patient`, citation `DOC-SYN-PRIVACY-FD` — instead of today's `_derive_intent` "contradicts → ambiguous, ask" (`services/ai-assistant/app.py:565-567`). Two ids with no confirmed id keep `clarify_member_id`; the prose-only second patient stays eligibility-assistant-D-44's residual. The comparison inputs are closed: the held `insurance_id`, recognised member-id patterns, and eligibility-assistant-D-44's phrase list — no patient-name set exists or is introduced | SPEC-49 / REQ-15″ name "a second member id" as refuse; keeping ask-semantics would have left frozen spec and plan disagreeing on the commonest form. Cost accepted: a clerk's mistyped id gets a refusal, not a correction prompt. |
| eligibility-assistant-D-51 | spec | **SPEC-31 check re-keyed** (owner 2026-08-26, PR #89 codex r1 M1; closes gate r2 f3's class). The frozen check cell `grep -c a1 docs/phi-logging-policy.md` ≥ 1 becomes `grep -c "eligibility-assistant" docs/phi-logging-policy.md` ≥ 1 **inside the register table**, the matching row's heading naming the trace path. **Amended (owner, r2 same day): executed in place in #89**, not deferred to the re-freeze — a vacuous check must not land frozen; the trace ticket's verification 21 follows the re-keyed cell | The item rename (eligibility-assistant-D-1) made `a1` a dead item token that survives only in planned code identifiers, so the count measured leftovers, not the register row; only the owner amends a frozen row (eligibility-assistant-D-47 precedent). |
| eligibility-assistant-D-52 | spec | **Frozen text amended in place to match the record** (owner 2026-08-26, PR #89 codex r2 B1/B2/M1a; eligibility-assistant-D-47 precedent). SPEC-27's Notes names the span emitter (gateway for `portal_entry` / `gateway_entry` / `persistence`, eligibility-assistant-D-41; the portal mints the header) and the Exclusions carve-out "whether portal and gateway emit their own spans" is dropped — eligibility-assistant-D-23's reading stands, the choice is now in the frozen document; SPEC-49 is narrowed to the forms eligibility-assistant-D-44 / D-50 close and the prose-only name form moves to an explicit Exclusion (REQ-15″ Notes cites it); SPEC-31 re-key per D-51. Wording only; 57 rows; no `spec-authoring` re-derive | Codex r2 was right that a plan choosing an answer does not remove a contradiction from the frozen artifact: with two frozen lines, a `trace` reviewer could accept or reject the same implementation by which line they read. SPEC-37 stays as eligibility-assistant-D-31 pinned — answered from the record, not amended. |
| eligibility-assistant-D-53 | spec | **SPEC-37 check made subcase-aware** (owner 2026-08-26, PR #89 codex r3 M1b, round-3 rule; amends the cmd eligibility-assistant-D-31 pinned, not its design). `grep -o 'EVAL-0[0-9][0-9]'` becomes `grep -o 'EVAL-0[0-9][0-9]\(-[a-z]*\)*'` and the expected count 32 → **33** (32 case ids + the second EVAL-014 subcase; `--collect-only` lists the opt-in `resists` test as collected, skipped only at run). Per-PR expectations follow: `2` after 1a, `2` after 1b, `32` after 1c (both EVAL-014 subcases land there, eligibility-assistant-D-24), `33` after PR-2 (EVAL-019). Kind and suite placement remain each EVAL row's `test:` pin; the manifest-check script stays declined | Codex r3 showed the pinned cmd's `sort -u` on stripped base ids could not tell `[EVAL-014-subverted]` from `[EVAL-014-resists]` even in principle — new evidence the 2026-08-26 decline had not weighed; a suffix-aware pattern closes it with no new file. |
| eligibility-assistant-D-54 | spec | **Lifecycle and retrieval-eval tests are not acceptance cases** (proposed 2026-08-26 at spec drafting of SPEC-58–66; **confirmed (owner 2026-08-26, spec draft review)** together with two readings: SPEC-62's update procedure is manifest edit + service restart, no hot reload; SPEC-64 is measurement-only, no recall floor). No new `EVAL-*` ids: the client's 32-case harness and SPEC-37's expected 33 stand; SPEC-60 / 61 / 62 exercise a synthetic `DOC-SYN-*` document created for the demo, never a client public-domain text (eligibility-assistant-D-48); SPEC-63–66 assert on the retriever and the suite, not on a case | The harness is the client's acceptance surface, pinned verbatim (eligibility-assistant-D-10); adding cases the client never wrote would move that count and the frozen SPEC-37 check for behaviour the owner asked for, not the client. |
| eligibility-assistant-D-55 | plan | **Ticket ownership propagated after eligibility-assistant-D-47 and the re-slice (closes gate r2 f4; amends eligibility-assistant-D-34).** SPEC-39a (`cmd:` — live tests skipped by default) is owned by `turn`, where `tests/test_a1_live.py` lands; SPEC-39b (`gate:` — the handoff list) by `trace`, where the runbook section lands; SPEC-30 moves from `trace` to `llm-seam` — the binding's `_stream` raise is its mechanism, so `tests/test_a1_trace.py` is created there with `test_model_call_not_streamed` alone and `trace` extends the file; `docs/todo.md`'s PR-2 row cites SPEC-32/39a. Each `Scope:` line and the contract ticket table carry the result. Contract is 339 lines after the re-freeze — 61 under the CI cap for six ticket Deliveries, so each Delivery is table-row + evidence-by-reference, never prose. Proposed 2026-08-26; **confirmed (owner 2026-08-26, in session)** | Every frozen row is owned by exactly one ticket (README): `SPEC-39` no longer exists and SPEC-30's pinned test was landing in a ticket that did not own the row. |
| eligibility-assistant-D-56 | plan | **§1 PHI-handling code-entry approval — decision of record (closes gate r2 f5).** The owner approved code entry on eligibility-assistant-SPEC-7–10, 12, 20, 28–31 as designed in the Landmines block, 2026-08-25 in session at plan review r1 (gate r1 f1), unconditional, standing on the 2026-08-24 freeze of the ⚠ rows, eligibility-assistant-D-14 and eligibility-assistant-D-41; and extended it the same day to the lifecycle turn row and the retrieval-record row — SPEC-60, 61, 63 (eligibility-assistant-D-48). Every ticket's Landmines block cites this ID; a change to a mechanism named there re-opens it. Recorded 2026-08-26; **confirmed (owner 2026-08-26, in session)** | plan-authoring requires the approving act cited by decision ID and date; it existed only as a Findings cell. |
| eligibility-assistant-D-57 | plan | **Lifecycle fields live on the vendored manifest rows; the loader enforces them at startup (`lifecycle` ticket).** `document-manifest.json` = the package manifest plus, per row, `status` ∈ `current` / `superseded` / `withdrawn` and `superseded_by` (a document id, `null` unless `superseded`): `corpus` lands the manifest verbatim (the 87 documents byte-identical, the manifest untouched — the package has no lifecycle key, E-3 re-read 2026-08-26), `lifecycle` adds the two keys to every row as `current`, and from then the loader requires them — a row without `status`, a value outside the set, a `superseded_by` chain that does not end on a `current` row (a cycle included), or a reason-table id (eligibility-assistant-D-19, the set `outcome.py` exports) that is not `current` raises at startup (eligibility-assistant-D-35 as amended). `_filter` and `fetch_by_id` keep `current` rows only, before `rank`. Supersession = vendor the successor under `documents/synthetic/`, add its manifest row (sha) and `index.json` entry, mark the predecessor `superseded` + `superseded_by`, restart; withdrawal = `withdrawn`, restart; both are runbook steps (SPEC-62), no reload. Tests build variants of the real corpus in `tmp_path` through `policy_index.load(root=…)` (copy + row mutation + a successor file with its computed sha); the demo document is a synthetic `DOC-SYN-*` named in the runbook, never a reason-table id — a withdrawn reason-table id is the SPEC-59 refuse-to-start demonstration, shown once and reverted. **Note 2026-08-27 (`lifecycle` gate r1 f1 / f2 / f3 / f9): one amendment and three clarifications.** (a) **Amended by eligibility-assistant-D-75:** "`_filter` and `fetch_by_id` keep `current` rows only, before `rank`" becomes "the loader builds the in-memory index from the approved rows that are `current`, only" — the same guarantee one site earlier, and no lifecycle field on the index row, which is what keeps `corpus`'s `MAX_ROW_BYTES` at 2,789 and its three pins on it intact (`corpus.md:15`, `:25`, `:26`). (b) **"raises at startup" has two load sites, not one:** `import app` → the dark `import policy_tool` → `categories()` → `load()` (this decision's own eligibility-assistant-D-35 note 2; `corpus.md:16`, `:60`) and the FastAPI lifespan hook. Only the hook passes `required_current`, because `policy_index` cannot import `outcome` — `outcome` imports `policy_index` for `fetch_by_id` (`turn.md:15`) — so the closed-set, `superseded_by` and chain checks run at **both** sites while the reason-id check runs at the hook alone; the hook's argument is therefore pinned by its own test (`lifecycle` verification 4b). (c) The `superseded_by` rule is **non-null iff `status == "superseded"`**: a `superseded` row with `null` and a `current` or `withdrawn` row with a non-null id both raise — the two states the "`null` unless `superseded`" wording left legal. (d) The **restart** in "restart; both are runbook steps (SPEC-62)" is an **image rebuild and recreate**: the corpus is baked in by `COPY . .` (`services/ai-assistant/Dockerfile:8`) and the service has no bind mount and no `restart:` policy (`docker-compose.yml:222-247`), so `docker compose restart` re-runs the same image — see eligibility-assistant-D-76 and `lifecycle` gate r1 f1. Proposed 2026-08-26; **confirmed (owner 2026-08-26, in session)** | SPEC-58 says "every manifest row"; a sidecar file would keep lifecycle state apart from the sha pin it guards, and the client artifact stays recoverable as the vendored manifest minus two keys. |
| eligibility-assistant-D-58 | plan | **Retrieval record and eval mechanics (`retrieval-eval` ticket).** `policy_index.lookup` returns `(rows, LookupRecord)`; the record is a frozen dataclass of closed fields — per axis (`topic`, `payer`, `product`, `state`) the resolved value and a provenance label from `clerk_selection` / `model_topic` / `application_default`, plus `pre_filter_rows`, `post_filter_rows`, `returned_rows`, `cap`, `truncated`, `empty`; `policy_tool` labels the bound axes `clerk_selection` and the topic `model_topic`; `fetch_by_id` records `application_default` on every axis. The record is emitted once per lookup as a structured log line from `policy_index` (fields only, class-only idiom for any error) — the `trace` ticket attaches the same record to the `retrieval` span. Ranking is `policy_index.rank(rows)` — tier rank, then version date — called once after `_filter`, the only ordering site, and substitutable. Recall: `tests/fixtures/a1/case_selections.json` (landed by `corpus` beside the vendored harness, so `turn` and `retrieval-eval` consume it without depending on each other) maps each of the **27** harness cases that name a `DOC-*` or `procedures/*` source (E-3 re-join 2026-08-26; the other five are role, corpus-gate and fixture cases) to its question type, payer, product, state and topic, curated from the scenario text; `procedures/*.md` resolve to their manifest ids (eligibility-assistant-D-12); recall@cap = |expected ∩ returned| / |expected| per case through `lookup`, the deterministic-turn cases (EVAL-012/014/016/017/018/024) through `fetch_by_id` of their reason set; cap = `A1_RETRIEVAL_MAX_ROWS`; one test loops the cases and prints the table and the minimum (not parametrised — no new `[EVAL-n]` ids, eligibility-assistant-D-54 and the SPEC-37 per-PR counts hold), and the numbers are copied into the ticket's Delivery with the cap (SPEC-65). No floor. Proposed 2026-08-26; **confirmed (owner 2026-08-26, in session)**. **Amended 2026-08-27 (eligibility-assistant-D-62, `corpus` gate r2 f2): `rank(rows)`' key is tier rank, then `retrieval_date` desc, then `document_id` asc — not "version date"; the recall unit is the document id (eligibility-assistant-D-61).** **Note 2026-08-27 (`retrieval-eval` gate r1 f11; record of the reading, no mechanism change):** SPEC-64's "every acceptance case that names expected sources" is read through eligibility-assistant-D-12's source kinds — a case is measurable when its `expected_source_ids` names a **retrievable** source (a manifest `DOC-*` id, or a `procedures/*.md` path that is a manifest row); a `policies/*.md` path is asserted as the case's outcome / action / refusal and never as a citation, and `evaluations/fixtures/README.md` is the corpus gate, so neither has a recall. All 32 harness cases carry a non-empty `expected_source_ids`; exactly **27** name a retrievable source and exactly five do not — EVAL-010, -011, -029, -030 (`policies/access-control-matrix.md`) and EVAL-031 (`evaluations/fixtures/README.md`) — which is the same 27 this decision confirmed, re-derived from the package `evaluations/eligibility-assistant-evaluations.jsonl` joined against `document-manifest.json` 2026-08-27. The narrowing is what the min-headline number rests on, so it is stated in `retrieval-eval`'s change row and in its residual bullet, not only here. Also amended by **eligibility-assistant-D-69** (the record's axis values are `str \| None`; `application_default` is the label every unlabelled caller records) and **eligibility-assistant-D-68** (the log line is the record's only emitter — the `trace` ticket does not attach it to the `retrieval` span). | REQ-19's three obligations need one seam each — the record at the lookup, the ranker as a function, the baseline over the cases — and the per-case selections are the input the harness never carried. |
| eligibility-assistant-D-59 | plan | **LangChain v1 pins land in `corpus`; `llm-seam` depends on `corpus` for them (supersedes eligibility-assistant-D-34's 1b "LangChain v1 pins" clause, dated note there; closes `corpus` gate r1 f6 and `llm-seam` gate r1 f1 as one class).** `langchain==1.3.16` / `langchain-core==1.6.0` / `langgraph==1.2.11` are one `corpus` change row on `services/ai-assistant/requirements.txt` **and** the root `requirements-dev.txt` — the file CI's `tests` job installs (`.github/workflows/ci.yml:86`; the `services` job installs the per-service file, `:73-76`) — because SPEC-9's `policy_tool` imports `langchain_core.tools`, so `corpus` is the first ticket that needs them; the row is recited **SPEC-21 → SPEC-9** (SPEC-21 is `turn`'s). `llm-seam` carries no requirements row: its `SeamChatModel(BaseChatModel)` and the `tests/test_llm_client.py` case import `langchain_core`, so landed ahead of `corpus` the CI `tests` job reddens on import — its line reads `Depends on: corpus` (pins), and the item ticket table's `depends on` cell agrees. No SPEC row changes ticket, no `Scope:` line moves, and the landing order **corpus → llm-seam → retrieval-eval → turn → {lifecycle, trace}** is unchanged (`corpus` was already first). `Depends on:` swept across all six ticket files 2026-08-27: corpus `none` · llm-seam `corpus` (the one fix) · turn `corpus, llm-seam` · trace `turn` · lifecycle `corpus, turn` · retrieval-eval `corpus`. Proposed 2026-08-27; **CONFIRMED by owner 2026-08-27 (in session)** **Note 2026-08-27 (stage 3, D-59..D-65 propagation sweep; record only, no rule change):** the `Depends on:` graph swept here gains one edge — **turn** is now `corpus, llm-seam, **retrieval-eval**`. The edge is created by eligibility-assistant-D-61, not by this decision: D-61 moves the SPEC-4 section label out of `outcome.py`'s reason table and onto the index row, so the reason path calls `policy_index.fetch_by_id`, whose `(rows, record)` two-tuple return is eligibility-assistant-D-58's and lands with `retrieval-eval`. The landing order is unchanged and already puts `retrieval-eval` ahead of `turn`, so the edge is satisfied by the existing order and no ticket moves. Graph as of this sweep: corpus `none` · llm-seam `corpus` · retrieval-eval `corpus` · turn `corpus, llm-seam, retrieval-eval` · lifecycle `corpus, turn` · trace `turn`. **Note 3 2026-08-27 (`turn` gate r2 f11; record only, no rule change):** the note above edited `turn.md:5` but not the item plan's ticket-table `depends on` cell, which still read `corpus, llm-seam` — the two sites disagreed for one round. Settled **in favour of `corpus, llm-seam, retrieval-eval`**, the direction this note gives with its reason and locator; the ticket-table cell is the losing site and is corrected. The six `Depends on:` lines and the six ticket-table cells were re-read against each other 2026-08-27 and now agree row for row: corpus `none` / `none` · llm-seam `corpus` / `corpus (pins — D-59)` · turn `corpus, llm-seam, retrieval-eval` / same · trace `turn` / `turn` · lifecycle `corpus, turn` / `corpus, turn` · retrieval-eval `corpus` / `corpus`. **Note 2 2026-08-27 (`retrieval-eval` gate r1 f5; record only, no rule change):** this decision's order clause — **corpus → llm-seam → retrieval-eval → turn → {lifecycle, trace}**, owner-confirmed above and stated identically in the item plan's `## Plan` `Status:` line (cited by section per the README stable-ID rule — `plans/eligibility-assistant.md:102` as read 2026-08-27 after the eligibility-assistant-D-80 row; the `:87` this note carried was the line before D-66..D-80 were inserted above it, corrected by `lifecycle` gate r2 f3 / `retrieval-eval` gate r2 f1) — **is** `retrieval-eval`'s landing slot: third, after `llm-seam`, before `turn`. Two values in that ticket's plan rest on it and are now pinned rather than hedged: its collect-count is `2` (`corpus`'s `EVAL-031` + `EVAL-023`, unchanged by `llm-seam` and by this ticket), and its suite line carries **no `skipped` term** (the live tests are `turn`'s, eligibility-assistant-D-43). No ticket moves and no `Depends on:` edge changes. | eligibility-assistant-D-34's 1b clause assigned the pins by PR shape before the re-slice, while the tool that needs them lands in 1a — leaving the decision, `corpus.md` and `llm-seam.md` stating three different things; one decision plus a `Depends on:` edge is the form that cannot drift, and a dependency declared `none` would have been discovered by a red CI job on the second PR. |
| eligibility-assistant-D-60 | plan | **Each ticket's Landmines block is verbatim for that ticket's own diff (closes `corpus` gate r1 f1/f2 and `llm-seam` gate r1 f2 as one class).** A ticket's block names every `docs/landmines.md` §1 zone **its own change list** enters — or states that zone not touched — and never a zone only a sibling enters; it cites the approving owner act by decision ID **and date** (eligibility-assistant-D-56, 2026-08-25 in session at plan review r1, extended the same day to SPEC-60/61/63, registered 2026-08-26); it states that the deliberate defects in reach are preserved; and it writes out **that ticket's** accepted residuals, the Secrets / Migrations entries, the structural-exclusion statement and the residual-honesty bullet in the block itself. No entry, residual or verification may exist in a ticket file as a citation to a sibling ticket file. Consequence, applied 2026-08-27 across all six: the shared five-PHI-path list is split — `corpus` carries the vendored sha-pinned corpus, the `topic`-only tool and the app-side by-id entry (SPEC-7–10); `llm-seam` the egress seam and the never-stream guarantee (SPEC-24/30); `turn` the model payloads, the visit-memory shape, the turn log metadata and the correlation header (SPEC-12/20/26) plus its Auth, IDOR and Inline-eligibility entries (SPEC-18/19/47/48/49); `trace` the trace payloads and the live-run record (SPEC-28/29/31/32); `lifecycle` the lifecycle turn and load-time enforcement (SPEC-58–61); `retrieval-eval` the retrieval record (SPEC-63). Coverage is unchanged — every path is still described once, by the ticket whose diff creates it, so a PR's "Risk & landmines" section drafted from a block names only code in that PR's diff. Recorded 2026-08-27 at the stage-3 revision; **no owner decision is required** — it applies the `.claude/skills/plan-authoring/` Landmines rule ("verbatim, never compressed, summarized, or replaced by a citation") and the README Tickets rule ("each ticket's `## Plan` with its own Landmines block") as written, and changes no mechanism, no SPEC row and no `Scope:` line. **Second pass 2026-08-27 — the citation-cross-reference form, outside the Landmines blocks.** The rule's last sentence was applied to the **Verification, Gate-interactions and Changes sections of all six ticket files** (corpus · llm-seam · turn · trace · lifecycle · retrieval-eval), searched for "full text in `<sibling>.md`", "recorded in `<sibling>.md`" and "see `<sibling>.md`"; Landmines blocks were swept in the first pass above and were **not** rewritten again. Five sites, all in `llm-seam` (3) and `turn` (2), each closed the way `corpus` gate r1 f2 closed the same class: a check that runs on the citing ticket's own PR is **written out** with that ticket's command and expected value — llm-seam 5 (`ok`, with `agent_binding`), 8 (`2`), 9 (baseline + 3 — **+ 4 since the llm-seam gate r2 disposition 2026-08-27; gate r3 f4; + 6 since the llm-seam gate r4 dispositions 2026-08-27 (r4 f1 / f2)**, no `skipped` term yet), 19 (empty diff) plus a Gate-interactions section of its own; turn 8 (`32`), 9 (baseline + N, the `1 skipped` term appears here), 11 (`--network none`, `1 skipped`), 12 (`1 skipped` default / `1 passed` opt-in), 13 (the SPEC-12 half), 19 (empty diff), 20 (SPEC-18, unchanged counts) plus the four item-wide gate interactions (`workflow-doc-cap`, `make eval`, `test_compose_topology.py`, baseline) in turn's own facts — and a check on another ticket's rows is **dropped**: llm-seam 20 (SPEC-18, `turn`'s) and 21 (SPEC-31, `trace`'s), turn 21 (SPEC-31, `trace`'s). `llm-seam`'s three citations pointed at `turn.md`, which lands **after** it in the order corpus → llm-seam → retrieval-eval → turn → {lifecycle, trace}, so an earlier PR's checks rested on a later, still-revisable file — a forward citation, wrong twice. `trace` and `retrieval-eval` carried none of the three forms in those sections; `lifecycle`'s two Gate-interactions bullets reached a sibling's verification number (`corpus.md` 4c, `trace.md` 8) and were re-pointed at its own verifications 2 and 5 | A block that describes a sibling's code is unusable for the PR body it is meant to draft, and the eligibility-assistant-D-56 approval says "as designed in this block" — a block missing its own ticket's design makes that citation vacuous. |
| eligibility-assistant-D-61 | plan | **Index row = one document; the section label and the text come from the manifest and the file, never from a heading split (`corpus`; closes `corpus` gate r2 f1; amends eligibility-assistant-D-36 and eligibility-assistant-D-58).** `policy_index` builds exactly one row per approved manifest row: `id` = `document_id`, `title` = manifest `title`, `section` = the manifest `section_labels` string verbatim (the `\|`-delimited client labels; `index.json` `sections` keeps the parsed list per D-36, informational only), `version` = `version_effective` and `retrieval_date` verbatim, `section-text` = the whole vendored file. No heading split, no `\|` parse on the row, no section offsets: the labels are the client's topical labels, not heading anchors — 142 labels over 87 rows, a label equals a markdown heading in its file for 55/142 and 62/87 documents match none (package re-read 2026-08-27) — and the documents are 131,032 bytes over 87 files (median ~1.4 KB, max 2,448 bytes; E-3), so a whole document is already smaller than the section a split would invent. Consequences: the row cap counts documents (`A1_RETRIEVAL_MAX_ROWS` 12 → at most 12 documents, ≤ 29,376 bytes of section-text, inside `LLM_MAX_INPUT_CHARS` = 4 × `LLM_MAX_INPUT_TOKENS` 20,000 = 80,000, `services/ai-assistant/config.py:64-75` — **amended 2026-08-27 (eligibility-assistant-D-64, `corpus` gate r3 f1): that headroom was measured against the backstop, not the binding byte gate; the default is D-64's — 6, then **5** from D-64's same-day amendment (`corpus` gate r4 f1; this clause corrected at r5 f2) — and the arithmetic is D-64's**); the (document, section) identity SPEC-4 / 12 / 64 cite is the document id with the label string riding it; `turn` renders the SPEC-4 section label from that string (the harness `expected_sections` — `HETS purpose`, `Disclosure Statement` — are these labels where they name one); `retrieval-eval`'s recall unit is the document id. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** **Propagation sweep 2026-08-27 (stage 3; record only, no rule change — closes `turn` gate r1 f4 and the D-61 half of `retrieval-eval` gate r1 f8).** This decision hands obligations to three sibling tickets and none had cited it (`corpus` authored it, 7 cites; every other ticket 0). All six ticket files swept on the terms `section` / `heading`: **corpus** 6 hits, authoring ticket, no change · **llm-seam** 1 hit (`:51` "lands dark — two senses", incidental word), clean · **turn** `:13` `RenderedCitation` — CHANGED, `section` now stated as the manifest `section_labels` string verbatim as the index row carries it, never split or re-derived; `:15` reason table — CHANGED, keyed on `document_id` alone with the label riding the index row through `fetch_by_id`, so no second copy of a manifest field exists and SPEC-6's "through the index" holds on the reason path; `:82` `Citation` = `document_id` + `version` only, already consistent, no change · **trace** `:15` SPEC-28 allowlist — CHANGED, the allowlisted section label named as the verbatim string; `:20`, `:24`, `:42`, `:43`, `:50`, `:69` are the English word "section" (runbook, ADR, policy register), no change · **lifecycle** 1 hit (the runbook subsection, `:19` when this sweep ran, `:28` after `lifecycle`'s gate r1 disposition of 2026-08-27), clean · **retrieval-eval** `:17` — CHANGED, recall unit stated as the document id; `:16`, `:31` incidental, no change. | The plan named a (document, section) row on a premise the package does not hold; a mechanism that fabricates section boundaries from a label that is not a heading would put the assistant's citation on text the client never labelled, and the cap, the identity and the recall baseline downstream each need one determinate row count. |
| eligibility-assistant-D-62 | plan | **Ranking key = (tier rank asc, `retrieval_date` desc, `document_id` asc); no version-date parse (`corpus`; closes `corpus` gate r2 f2; amends eligibility-assistant-D-36 and eligibility-assistant-D-58 "tier rank, then version date").** `version_effective` is prose on 87/87 rows (0 ISO dates, 48 distinct strings; E-3 — and eligibility-assistant-D-48 already rules parsing it out as curation with no demo case), while `retrieval_date` is ISO on every row (`2026-08-24` on all 87 today). `policy_index.rank(rows)` sorts by the eligibility-assistant-D-38 tier, then newest `retrieval_date`, then `document_id` — a total order on closed manifest fields, so the pinned order in `test_tier_rank_in_code`, the cap cut and SPEC-66's ranking unit are determined with no curated date; today the third key decides every within-tier order. Staleness stays D-48's `status` field, not a date. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** **Propagation sweep 2026-08-27 (stage 3; record only, no rule change — closes the D-62 half of `retrieval-eval` gate r1 f8(a)).** Swept on `rank` / `ordering` / `version date` across all six: **corpus** 7 hits, authoring ticket, no change · **llm-seam** 0 · **turn** 0 · **trace** 0 · **lifecycle** 4 hits (`:5` `rank` named in `Depends on:`, `:14` and the Landmines load-time entry — `:45` when this sweep ran, `:61` after `lifecycle`'s gate r1 disposition of 2026-08-27 — "before `rank`", and the approval-of-record prose, then `:46`, now `:62`) — the key is stated nowhere, no change · **retrieval-eval** `:13` — CHANGED, `ranker=` default was "the tier-then-date order" and is now this decision's key verbatim, (tier rank asc, `retrieval_date` desc, `document_id` asc), with the note that it amends D-58's own "tier rank, then version date" and that `document_id` is what makes the order total enough for `test_ranking_isolated_from_filtering` to compare it as a list; `:4`, `:5`, `:25`, `:31`, `:41` name `rank` without stating its key, no change. **Not closed by this sweep:** f8(b), the unnamed bucket behind the ≥ 2-row precondition and the tier-5 break — a mechanism choice for stage 3, not a propagation. | Within-tier order was input order — undefined — and the ranking unit `retrieval-eval` substitutes must be a function of row fields alone. |
| eligibility-assistant-D-63 | plan | **Loader boundary: `.DS_Store` is the one exempt name in the unlisted-file scan, and a manifest row that is not `approved` fails the load (`corpus`; closes `corpus` gate r2 f3 / f5).** The SPEC-7 scan ignores exactly the file name `.DS_Store` at the corpus root and under the roots — macOS Finder writes it into any browsed folder (`.gitignore:6` ignores it; the client package shipped `documents/.DS_Store`), it is not a document, carries no manifest row and has no path into the index — and `services/ai-assistant/.dockerignore` gains `.DS_Store` so the image never carries one. No other name or dotfile is exempt. A manifest row whose `approval_status` is not `approved` raises the loader's own error at load, before any lookup, rather than being skipped: SPEC-7 says the corpus is *exactly* the approved rows, and a silently dropped row is the silent narrowing the sha pin exists to prevent. `lifecycle`'s `status` axis (eligibility-assistant-D-57) is a separate field with its own values and is not read by this check. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)**. **Note 2026-08-27 (`corpus` gate r6 f3; record only, no rule change):** the unlisted-file scan walks the whole `policy_corpus/` tree at every depth — every file is one of the two named root-level files or a manifest-row `path` — not "the corpus root and under the roots"; `.DS_Store` is exempt at any depth. The r2 plan wording was narrower than SPEC-7's frozen clause; the plan now matches the clause **Propagation sweep 2026-08-27 (stage 3; record only, no rule change).** Swept on `approval_status` / `approved` / `.DS_Store` across all six: **corpus** 14 hits, authoring ticket, no change · **llm-seam** 0 · **turn** 0 · **trace** 0 · **retrieval-eval** 0 · **lifecycle** `:14` — CHANGED: this ticket edits the same `load()` that already raises on a non-`approved` row, so the row now states that `status` is a separate closed field, that neither check reads the other's field, and that this ticket adds a validation beside that one without replacing, widening or narrowing it — this decision's own clause written into the ticket that could otherwise collide with it; the residual-honesty bullet (`:58` when this sweep ran, `:74` after `lifecycle`'s gate r1 disposition of 2026-08-27) incidental, no change. | A Finder side-effect must not read as a tampered corpus on the demo machine, and a vendoring mistake on the client's approval gate must fail loudly, not narrow the corpus. |
| eligibility-assistant-D-64 | plan | **Retrieval cap sized against the binding byte gate; the cap clause exercised on a legal call; `*` is never a query value (`corpus`; closes `corpus` gate r3 f1 / f2; amends eligibility-assistant-D-61's headroom clause and eligibility-assistant-D-36's default).** The gate that binds a model call is `_enforce_budget` on `max_input_tokens` — the UTF-8 byte length of system + messages + `extra_body`, plus 8 B base and 8 B per message (`services/ai-assistant/llm_client.py:336-337`, `:368-375`, `:458-461`, `:494-495`) — against `LLM_MAX_INPUT_TOKENS` = 20,000 (`services/ai-assistant/config.py:64`); `LLM_MAX_INPUT_CHARS` = 80,000 (`config.py:75`) is the gross backstop and never the binding one. `A1_RETRIEVAL_MAX_ROWS` default becomes **6** (was 12). Sizing rule, both halves pinned by `tests/test_a1_retriever.py::test_cap_binds_on_a_legal_call`: (i) `cap × max vendored file bytes + PROMPT_RESERVE_BYTES ≤ settings.llm_max_input_tokens` — 6 × 2,448 B (largest file; package `document-manifest.json` paths sized 2026-08-27: 87 files, 131,032 B total, max 2,448) = 14,688 B + 5,000 B reserve = 19,688 ≤ 20,000, where `PROMPT_RESERVE_BYTES = 5000` is a `policy_index` module constant naming what `turn` may spend on system prompt, tool schema and the turn's own messages across both model calls (`turn` proves its prompt fits it in its own verification and, if it does not, lowers the default by its own decision — never raises `LLM_MAX_INPUT_TOKENS`, a PHI-egress bound); (ii) at least one legal argument set returns more than `a1_retrieval_max_rows` candidate rows, so the cap clause has a legal-path exercise — today `topic="coordination-of-benefits"` with `payer="medicare"` (7 rows, every row Medicare or non-payer-specific: `DOC-FED-COB-*` ×4, `DOC-FED-MSP-CRS`, `DOC-FED-MEDIGAP`, `DOC-SYN-COB-FRONT-DESK`; 11,363 B) and `medicaid-managed-care` with `payer="medicaid"` (7 rows, 9,307 B); the largest set any curation of `payer-eligibility-public`'s 14 rows can yield is 6 (≤ 3 under one payer + the 3 non-payer-specific rows `DOC-PAY-AVAILITY-ELIG`, `DOC-MAC-CGS-ELIG`, `DOC-PAY-NOVITAS-HETS`). A re-curation or `lifecycle` change that drops every legal set to ≤ cap reddens (ii) deliberately: the default then moves, by decision. Query values: `lookup` accepts only eligibility-assistant-D-36 enum values on `payer` / `product` / `state` (`unconfirmed` non-filtering per eligibility-assistant-D-32); `*` is an **index-row** value and a query `*` raises `ValueError` — the axis SPEC-9 closes has no wildcard on the query side. Consequences: `retrieval-eval`'s `truncated` flag and recall@cap inherit a real exercising case; `turn`'s `.env.example` value follows the default. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session; round-3 rule, `corpus` gate r3)**. **Amended 2026-08-27 (owner, `corpus` gate r4 f1 adjudication under the round-3 rule):** rule (i) was missing the row-metadata term — the row that egresses (eligibility-assistant-D-61) carries `id`, `title`, `section`, `version` and `retrieval_date` beside the text, 97–341 B per row (package `document-manifest.json`, five fields UTF-8-summed 2026-08-27; max 341 on `DOC-FED-COB-PROVIDER-SERVICES`, whose file is also the 2,448 B maximum, so max(file + metadata) = 2,789 B), and `turn`'s reserve measures "excluding retrieved row text". Rule (i) is now **`cap × policy_index.MAX_ROW_BYTES + PROMPT_RESERVE_BYTES ≤ settings.llm_max_input_tokens`**, where `MAX_ROW_BYTES` is computed at `load()` as the maximum over index rows of the UTF-8 byte total of every field value the row carries (text and metadata) — 2,789 today, pinned — and `turn`'s reserve owns everything else, including the per-row serialisation overhead (field names, punctuation) for `cap` rows, stated in its verification 18. Under the corrected rule 6 × 2,789 + 5,000 = 21,734 > 20,000, so the default becomes **5**: 5 × 2,789 + 5,000 = 18,945 ≤ 20,000 (1,055 B slack). Half (ii) is unchanged (the 7-row COB and Medicaid sets still exceed 5; `payer-eligibility-public`'s maximum 6 now exceeds it too). Bound and figures are executable from the vendored tree by `test_cap_binds_on_a_legal_call`, not hand arithmetic — the cap math was wrong in r2, r3 and r4 as prose. | Twelve documents at the manifest maximum exceed the byte gate before any prompt — a full-cap result was `spend_stop`, not headroom — and no legal call could reach twelve, so the cap was decorative on both sides. |
| eligibility-assistant-D-65 | spec | **SPEC-7 frozen text amended in place to match eligibility-assistant-D-63** (owner 2026-08-27, in session; `corpus` gate r3 f4 under the round-3 rule; D-51 / D-52 precedent). The row's "an unlisted file shall fail the suite" gains "other than the single exempt name `.DS_Store`", dated in the row and on the `## Spec` Status line (`docs/workflow/eligibility-assistant.md:253`, `:265`); no re-freeze, no other row moves. Alternatives declined: overrule (a reviewer reading `## Spec` alone would read a stricter rule than the implementer follows) and dropping the exemption (Finder reddens `test_manifest_sha_pinned` on the demo Mac — D-63's reason) | A plan choosing an answer does not remove a contradiction from the frozen artifact (D-52); the contract must say what the loader does. |
| eligibility-assistant-D-66 | plan | **One pinned bare-name module set for the corpus test files (`corpus`; closes `corpus` gate r7 f1).** `tests/a1_corpus_rig.py` is the single site that loads the ai-assistant modules this item's tests and `app` share. It follows the house idiom (`tests/test_ai_visit_chat.py:41-73`; `CLAUDE.md:138-140` "pin `sys.modules` first"): save and pop the bare names, pin ai-assistant's `config` / `logging_config`, pin `policy_index` and `policy_tool` under their **bare** names through `tests/conftest.py:16-26`'s `load_module` (which loads by path under a *unique* name and puts the service dir on `sys.path`, `:21-22`), load `app` as `a1_corpus_app`, restore. It exports `policy_index`, `policy_tool`, `app_mod` and `settings` (the pinned `config.settings`); `tests/test_a1_corpus.py`, `tests/test_a1_retriever.py` and `tests/test_a1_conflict.py` import it and open **every** test with the identity assertions `policy_tool.policy_index is policy_index` and `policy_index.settings is app_mod.settings` — plus `app_mod.policy_index is policy_index` and `app_mod.policy_tool is policy_tool` in the file that drives the lifespan — the shape `llm-seam` gate r3 f1 landed for the binding (`agent_binding.llm_client is llm_mod`). Because the rig's `app_mod` load imports `policy_tool`, whose `categories()` runs a default-root `load()`, the rig's import is what publishes `_INDEX` / `MAX_ROW_BYTES`, so the eligibility-assistant-D-57 / gate r5 f3 / r6 f1 module-state rules are about one object with a settled pre-test value. Later tickets consume the same set rather than loading their own: `turn`'s `tests/a1_rig.py` imports and re-exports it, `lifecycle`'s `monkeypatch` install of `_INDEX` targets it, `retrieval-eval`'s two new test files import it. `corpus` verification 4p is the break-then-revert (drop either pin → the first assertion reddens before any load, lookup or `TestClient`). Proposed 2026-08-27 (stage 3, `corpus` gate r7 f1 under the round-3 rule; the owner's adjudication of 2026-08-27 accepted the finding and chose *name the pinned rig* as the resolution); **confirmed (owner 2026-08-27, in session)**. **Note 2 2026-08-27 (`turn` gate r3 f1; record only, the rule applied one seam further):** `turn`'s `tests/a1_rig.py` consumes the set by patching the pinned objects — `app_mod.llm_client.client` (the scripted `messages.create` queue; `_call` resolves `client.messages.create` at call time, `llm_client.py:723-724`) and `app_mod.eligibility_client.check_coverage` (`eligibility_client.py:98`; the call at `app.py:850` and `agent_turn`'s `PayerGate` both read it as a module attribute at call time) — because this rig restores the bare names after its `app` load, so `app_mod.<name>` is the only handle on the copy the wired turn calls; the rig exports those two as `llm_client` / `eligibility_client`, and every file seamed onto it opens with `app_mod.agent_binding.llm_client is app_mod.llm_client`, `app_mod.agent_turn.llm_client is app_mod.llm_client` and `app_mod.agent_turn.eligibility_client is app_mod.eligibility_client` (`turn` verification 14's rig break). | Without one pinned object, `load_module`'s unique-name loading hands each test file its own `policy_index`: the lifespan monkeypatch misses the copy `app`'s hook calls, the cross-file `_INDEX` / `MAX_ROW_BYTES` reasoning is about different objects, and the tests that rest on it pass for the wrong reason — the failure `llm-seam` gate r3 f1 found on the binding, one module set later. **Note 3 2026-08-29 (`corpus` impl-gate r1 f3; record only, no rule change) — the shipped rig pins ten bare names, not four.** `tests/a1_corpus_rig.py:24-35` pins **all eight** names `tests/test_ai_visit_chat.py:41-50` pins — `config`, `logging_config`, `schemas`, `llm_client`, `templates`, `visit_templates`, `breaker`, `eligibility_client` — plus `policy_index` and `policy_tool`, where this row above and `corpus.md:25` describe popping eight and pinning four. Four leaves `app.py:30-36`'s other six resolving by `sys.path` order, masked in the default run and exposed by a mixed invocation with another service's dir ahead; that is `corpus` gate r10 f1, accepted by the owner 2026-08-28 as named residual (e) with the implementation taking the eight-pin form, and it is the form that shipped. This note is the site that outlives the ticket: residual (e) and `corpus.md:25` are deleted with the ticket file, and every later ticket that consumes the set (`turn`'s `tests/a1_rig.py`, `lifecycle`'s `_INDEX` install, `retrieval-eval`'s two files) reads the ten-name rig, not the four-name one. |
| eligibility-assistant-D-67 | plan | **SPEC-65's recall numbers live in the contract's `## Delivery`, in three compact lines (`retrieval-eval`; closes `retrieval-eval` gate r1 f4).** SPEC-65's frozen check is `gate:` "the numbers are in the Delivery section", so a by-reference record does not satisfy it and the numbers must be written into the capped contract file. The form is fixed here: **(1)** the cap in use at the merged sha, **(2)** the 27 per-case values as one wrapped `EVAL-nnn <value>` list, **(3)** the `min recall@<cap> = <value> (<case>)` headline — copied from `retrieval-eval` verification 1's `-s` output, no prose, no per-case table. Budget: the contract measured **339 lines** 2026-08-27 (`wc -l docs/workflow/eligibility-assistant.md`) against the blocking 400-line `workflow-doc-cap` job (`.github/workflows/ci.yml:135-155`, terminal-gate `needs:` at `:207`), i.e. the 61 spare lines eligibility-assistant-D-55 accounts for across six ticket Deliveries; three lines is this ticket's share of that and the only ticket record in the item that carries measurements rather than references. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** | A frozen `gate:` row that says "the numbers are in the Delivery section" cannot be met by a citation, and the one file the numbers must go into is the one CI caps — so the shape of the record is a decision, not an authoring choice made at merge under cap pressure. |
| eligibility-assistant-D-68 | plan | **The lookup record is emitted as a log line only; no emitted run payload carries it (`retrieval-eval`; closes `retrieval-eval` gate r1 f7).** SPEC-63's Notes leave the emission site — "log line or retrieval span" — to plan stage (contract Exclusions: "where the lookup record is emitted, log line or span (plan stage)"). This decision spends that choice on the **log line**, for the whole item. Basis: the record's four resolved axis values *are* `lookup`'s query, which SPEC-29 (`docs/workflow/eligibility-assistant.md:287`) forbids in any emitted run payload on any path, and resolved filter values and row counts are not members of SPEC-28's allowlist (`:286`: document ids + section labels, modes, fallback reason, outcome, timings, model/profile identifier, correlation metadata). Attaching the record to the `retrieval` span therefore needs a **stage-2 amendment of SPEC-28 and SPEC-29 by owner decision**, not a plan-stage move; SPEC-63's Notes assert allowlist membership, and that assertion is what the amendment would have to make true. Consequence swept 2026-08-27: `trace`'s `services/ai-assistant/tracing.py` change row (`trace.md:15`) no longer attaches the `LookupRecord` to the `retrieval` span — the span keeps its allowlisted metadata (document ids + section labels, mode, reason, outcome, timings, model id, correlation id) and nothing else. REQ-19 is unaffected: it asks that every lookup **leave a record**, which the log line does, and E-6's basis (a deployed clinical assistant that could not evaluate retrieval because nothing was recorded) is met by a recorded line, not by its destination. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** — the SPEC-28/-29 amendment named above was put to the owner as the alternative in the same act and **not taken**, so it is a recorded option, not a pending call | Two frozen rows say the record cannot go in a run payload and one frozen Notes cell says it can; the plan may not bend either, and the one option that needs no amendment is the one the same Notes cell already offers. |
| eligibility-assistant-D-69 | plan | **`LookupRecord` axis shape and the provenance of a by-id or direct call (`retrieval-eval`; closes `retrieval-eval` gate r1 f6; amends eligibility-assistant-D-58's field description).** The record's axis value is typed **`str \| None`**: on the by-id path (`fetch_by_id`, the eligibility-assistant-D-19 reason table's entry) there are no filter axes, so all four values are `None` at provenance `application_default`, and `None` is the one non-enum value the closed field set admits. `lookup` gains a keyword-only `provenance=` mapping of axis → label; **every axis it omits records `application_default`**, so a direct module call — `corpus`'s `test_in_process_read_only_capped` / `test_unconfirmed_axis_non_filtering[EVAL-023]`, `lifecycle`'s variant tests — leaves a record without knowing the tool exists, and SPEC-63's "every retriever lookup shall leave a record" holds for every caller rather than only the tool's. `policy_tool` passes `{topic: model_topic, payer: clerk_selection, product: clerk_selection, state: clerk_selection}`. Counts on the by-id path: `pre_filter_rows` = the loaded index's row count, `post_filter_rows` = `returned_rows` = rows found for the requested ids, `cap` = the configured cap (**not applied** there), `truncated` = `False`, `empty` = `post_filter_rows == 0`. Fourteen fields total. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** — and because this row changes the closed field set eligibility-assistant-D-56's re-open clause names, the same act **re-closes the eligibility-assistant-D-56 §1 PHI approval on the amended field set** (owner 2026-08-27) | eligibility-assistant-D-58 said "`fetch_by_id` records `application_default` on every axis" without saying what the values are, while the Landmines safety argument rested on "the axis values are enum members" — untrue on a path with no axes. A null and a default label are the two smallest additions that make both sentences true. |
| eligibility-assistant-D-70 | plan | **The named argument sets for SPEC-63's empty leg and SPEC-66's buckets, and the `index.json` curation obligation they rest on (`retrieval-eval`; closes `retrieval-eval` gate r1 f3 and f8(b)).** Every quadruple is `(topic, payer, product, state)` in eligibility-assistant-D-36's enums. **Empty leg:** `("medicaid-managed-care", "aetna", "commercial", "unconfirmed")` — empty on both filtering axes, since the seven `medicaid-managed-care` rows are Medicaid/CHIP publishers with state-Medicaid applicability (package `document-manifest.json`, the seven rows read 2026-08-27). **Truncated leg and ranking bucket 1:** `("coordination-of-benefits", "medicare", "unconfirmed", "unconfirmed")` — 7 rows, the set `corpus`'s `test_in_process_read_only_capped` already pins. **Ranking buckets 2 and 3:** `("medicaid-managed-care", "medicaid", "unconfirmed", "unconfirmed")` (7 rows) and `("payer-eligibility-public", "humana", "unconfirmed", "unconfirmed")` (the three `DOC-PAY-HUMANA-*` rows, four with the multi-payer `DOC-PAY-AVAILITY-ELIG`). **Obligation on `corpus`:** the curated `index.json` must leave the empty quadruple empty and each ranking bucket at ≥ 2 rows; it is carried as a clause on `corpus`'s `index.json` change row and asserted by `retrieval-eval`'s two tests, so a curation that broke either reddens rather than passing silently — the reachability class eligibility-assistant-D-64 settled for the cap, applied to the empty and ordering legs. Tier note: tier 5 is the category `payer-training-summary` alone, and a bucket's topic *is* one category, so no bucket mixes tier 5 with another tier — the round-1 "drop tier-5 rows" break is replaced by "drop the last row of the input". Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** | An "empty result" and an "order that can differ" are facts of a curated artifact that does not exist yet; naming them in the plan and asserting them in the test is the only way the two legs have a legal exercise, and the only way `corpus` learns it owes them. |
| eligibility-assistant-D-71 | plan | **Mode, health and egress are three fields with three predicates (`turn`; closes `turn` gate r1 f2 / f3 and r2 f2 / f3; amends eligibility-assistant-D-39's "every reply" clause).** **(a) `mode`** (SPEC-33, the six-value eligibility-assistant-D-33 set) names *what produced the reply*, and its precedence is: a deterministic-gate turn carries the gate's own value — `care_first` (SPEC-45), `refuse` (SPEC-49), `no_lookup` (the `awaiting_id` / `ambiguous_id` short-circuit) — otherwise a turn whose agent step ended on a eligibility-assistant-D-19 reason carries `fallback`, otherwise the model produced the reply and the turn carries `real` or `fixture` from `settings.a1_model_fixture`. So `real` / `fixture` is not a second axis stacked on the other four: it is the value the field takes exactly when no path name applies. eligibility-assistant-D-39's fail-visibility survives, restated: a fixture flag left on renders `fixture` on **every model-produced reply**, which is every agent-path turn that reaches model₂ — not on the three zero-model gates, where nothing was faked, nor on `fallback`, which already announces itself with its reason. `real` is reachable in the keyless default suite because the field is derived from the setting, not from whether Bedrock answered: `test_mode_explicit`'s `real` leg flips `settings.a1_model_fixture` to false with the scripted seam still installed and asserts the derivation; the *live* proof that a `real` reply came from Bedrock is SPEC-17 / SPEC-32, opt-in. **(b) health.** `VisitChatResponse.assistant` (`services/ai-assistant/app.py:901`, `"degraded" if plan.degraded else "ok"`) keeps today's source and today's predicate: `run_agent_path` gains a fifth member `degraded`, true **iff an `llm_client.LLMError` escaped the agent step** — the same test as today's `except llm_client.LLMError` branch (`app.py:812-816`). Consequences, each pinned by a retained test: `model_failure` and `spend_stop` are `degraded` (`tests/test_ai_visit_chat.py::test_every_llm_failure_is_reported_as_degraded`, `:463`, parametrised over `_LLM_FAILURES` `:384-400`, which includes `LLMBudgetExceeded`; assert `:475`); a bounded-out **selection** — an out-of-vocabulary action id, a second tool call, a third model call — is `ok`, because the model answered and we discarded its choice (`::test_a_rejected_model_selection_is_not_a_degraded_assistant`, `:590`, assert `:600`); `no_retrieval` is `ok` for the same reason (model₁ answered, in shape, without the tool); a successful agent turn is `ok` (`::test_a_successful_model_call_is_charged_and_healthy`, `:478`, asserts `:481-482`); and the three deterministic gates are `ok` — the designed path is not a fault, the rule `app.py:781-783` states for today's no-freedom short-circuit ("health must keep meaning health"). A model₂ body the seam cannot parse raises `LLMResponseError` inside `llm_client` and is therefore `degraded` even though its reason is `validation_reject` — the one place mode and health deliberately disagree. **(c) `llm_egress`** is unchanged (SPEC-56, eligibility-assistant-D-29). The three are independent by construction and stay so: `app.py:712-717` already forbids one boolean carrying spend and health, and this decision adds the third field without collapsing any pair — `spend_stop` at model₁ is `degraded` + no egress + mode `fallback`; a rejected selection is `ok` + egress + mode `fallback`; `no_lookup` is `ok` + no egress + mode `no_lookup`. Pinned by `tests/test_a1_validation.py::test_mode_health_egress_independent`. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** (it amends a eligibility-assistant-D-39 clause the owner confirmed 2026-08-25; the same act carries the amendment) | One six-value field was carrying two axes and one boolean had no carrier at all: the plan's own tests asserted six distinct modes under a fixture flag eligibility-assistant-D-39 says forces one, and `run_agent_path`'s stated tuple dropped the health member six retained tests pin. Deriving health from the reason enum instead would have inverted `test_a_rejected_model_selection_is_not_a_degraded_assistant`, so the predicate that survives is the exception type, exactly as today. |
| eligibility-assistant-D-72 | plan | **The turn's new downstream fields are answer-only: validated, never fatal (`turn`; closes `turn` gate r1 f6 and r2 f5).** `services/gateway/app.py`'s `_VisitChatDownstream` states its own rule — "What is validated is exactly what the gateway acts on" (`:968`), `llm_egress` / `assistant` deliberately absent because "a field that cannot corrupt state and cannot mislead a clerk is not worth a 502" (`:981-986`), and `eligibility` "validated but NOT fatal … An unusable value degrades to null" (`:988-993`, the `field_validator(mode="before")` at `:1005-1015`). This ticket's five new response fields join `eligibility`'s class: **`citations`, `mode`, `reason`, `outcome`, `model` are validated for shape and degrade rather than fail** — `citations` to `[]`, the other four to `null` — through one `mode="before"` validator per field on the `_degrade_unusable_verdict` pattern, with a count-only `log.warning` (never a value, never a key from downstream). They are answer-only: none is written into Redis, none is fed back on a later turn, and the coverage sentence the clerk reads is already server-rendered in `reply`, so failing the turn over any of them would throw away `facts.last_eligibility` — the verdict the payer call this turn already paid for — which is the mistake `:988-993` exists to prevent. **`correlation_id` is not relayed from downstream at all**: the gateway answers with the id it minted or forwarded (eligibility-assistant-D-46), so a downstream echo that differs is a count-only warning and never a value the gateway learns — one fewer field to validate and no path by which a downstream body can change the request's identity. **`facts.last_citations` stays fatal** with the rest of `facts` (`_VisitChatFacts`, `:970` "written into Redis, so it gets the closed shape above") — it is persisted state, the class the round-6 defect was about. Pinned by `tests/test_gateway_ai_proxy.py::test_a1_answer_fields_degrade_and_facts_stay_fatal`. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** **Note 2026-08-28 (`turn` gate r4 f1):** the `citations` degrade can render a non-refused answer with zero citations — the shape SPEC-4 / SPEC-41 forbid — on a gateway/assistant version-skew path only; accepted as `turn` residual (i), not made conditional on `outcome`, which is itself degradable on the same path. | The plan added five fields to a model whose docstring is an argument about which fields may fail a turn, and said nothing about which side they fall on; the default (a required field on a `ValidationError`-fatal model) would have put five renderer-only values on the 502 path that discards a paid verdict. |
| eligibility-assistant-D-73 | plan | **Three retained characterization tests are re-pointed with their subject under eligibility-assistant-D-50 (`turn`; closes `turn` gate r1 f1 and r2 f1; amends eligibility-assistant-D-40's "no retained test is deleted or renamed").** eligibility-assistant-D-50 (owner 2026-08-26) makes a single message id that contradicts the visit's confirmed `insurance_id` a cross-patient **refusal** decided before `_derive_intent` runs, replacing the branch at `services/ai-assistant/app.py:610-611` for that case. Three tests in `tests/test_ai_visit_chat.py` assert the replaced behaviour and must move with it: `::test_an_id_that_contradicts_the_visits_confirmed_id_asks_first` (`:789`; `status == "ambiguous_id"` `:799`, `"Confirm which member ID" in reply` `:803`), `::test_a_genuinely_different_id_still_contradicts_in_lower_case` (`:862`; assert `:870`) and `::test_freshness_does_not_soften_the_contradiction_rule` (`:1350`; asserts `:1357-1360`). **What moves:** the reply-shape assertions become the refusal's — `outcome == "refuse"`, `reason == "cross_patient"`, `mode == "refuse"`, the `DOC-SYN-PRIVACY-FD` citation (contract reason table) — and the first test is **renamed** to `::test_an_id_that_contradicts_the_visits_confirmed_id_is_refused`, because its name states the rule eligibility-assistant-D-50 replaces; the other two names stay (the id still contradicts; only the consequence changed). **What does not move:** every safety assertion each test was written for survives verbatim and stays asserted — `fake_eligibility == []` (no payer call for the second party), `facts["insurance_id"]` unchanged, and the third's "the stored ACTIVE verdict is not restated in the verdict line". The echoed `intent` / `status` a gate turn carries are `VisitIntent.other` (`services/ai-assistant/schemas.py:150`) and the reason name as a pseudo-status — the `awaiting_id` / `ambiguous_id` precedent (`services/ai-assistant/visit_templates.py:86`) — so the gateway's fatal `^[a-z_]{1,32}$` shape on both (`services/gateway/app.py:999-1000`) still holds on a refusal turn. No other retained test moves: this is an exception to eligibility-assistant-D-40 for exactly these three, granted because the frozen row they contradict (SPEC-49 as narrowed 2026-08-26) is the owner's, not because the characterization rule bends (`docs/landmines.md` §3 — capture, then change under an explicit decision, never "fix the tests to hide a gap"). Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** (it is a ⚠ SPEC-49 row and amends an owner-confirmed decision; the same act carries the amendment) | eligibility-assistant-D-40 forbids renaming a retained test and eligibility-assistant-D-50 makes one retained test's *name* a false statement; leaving both standing would have had the implementer discover the contradiction with a red suite and no written answer. |
| eligibility-assistant-D-74 | spec | **Owner call made 2026-08-27 (in session) — amend SPEC-48 at stage 2; its parenthetical evidence is falsified by `turn`'s own diff (`turn` gate r1 f7 / r2 f6; eligibility-assistant-D-52 / D-65 precedent).** SPEC-48 as frozen reads "the assistant shall receive no role (`VisitChatRequest` `extra=\"forbid\"`, gateway body `{message, turns, facts}`)" and REQ-15″ repeats the parenthetical citing `services/gateway/app.py:1153` (verified 2026-08-27: the body literal at `:1153` is exactly `{"message": …, "turns": …, "facts": …}`). `turn` forwards **eight** keys (`message`, `turns`, `facts`, `question_type`, `payer`, `product`, `state`, `emergency` — SPEC-41/54/55), so the requirement survives (no role is among the eight, and `VisitChatRequest` stays `extra=\"forbid\"`) while the stated evidence does not. **Proposed amendment in place, wording only, no re-freeze:** SPEC-48's parenthetical becomes "(`VisitChatRequest` `extra=\"forbid\"`; the gateway body carries no role field — `{message, turns, facts, question_type, payer, product, state, emergency}` after eligibility-assistant-SPEC-55)", with REQ-15″'s Notes cite updated the same way and the `## Spec` Status line dated as eligibility-assistant-D-65 dated it. **Alternative:** decline and record why — the plan then carries the residual that a reviewer reading `## Spec` alone reads a narrower body than the implementer ships, the exact reading eligibility-assistant-D-52 rejected. **Not executed: only the owner amends a frozen row.** Proposed 2026-08-27; **owner call made 2026-08-27 (in session): amend SPEC-48 at stage 2**, the eligibility-assistant-D-65 precedent — the parenthetical is corrected in place and the spec re-frozen at 66 rows; this row is **confirmed** as the record of that call | The plan may not bend a frozen row and may not ship against one either; naming the amendment and its alternative is what stage 3 can do with a stage-2 fact. |
| eligibility-assistant-D-75 | plan | **Lifecycle state stays loader-side: the index is built from `current` rows only and carries no lifecycle field (`lifecycle`; closes `lifecycle` gate r1 f3; amends eligibility-assistant-D-57's "`_filter` and `fetch_by_id` keep `current` rows only, before `rank`").** `load()` verifies each vendored file's sha256 and runs the SPEC-7 whole-tree scan over **every** manifest row whatever its `status` — so a superseded or withdrawn file stays vendored and pinned (SPEC-58's retention clause) — validates the lifecycle fields, and **then builds the in-memory index from the approved rows whose `status` is `current`, only**. Consequences: `lookup`, `_filter`, `fetch_by_id` and `rank` are current-only by construction and before `rank`, with no per-row status to read; the index row keeps exactly eligibility-assistant-D-61's six values (`id`, `title`, `section`, `version`, `retrieval_date`, `section-text`), so `policy_index.MAX_ROW_BYTES` stays **2,789** and `corpus`'s three pins on it are untouched — `test_cap_binds_on_a_legal_call`'s recompute-and-compare (`corpus.md:26`), `test_load_with_root_does_not_publish_module_state`'s "still 2,789" and `test_non_approved_row_fails_load`'s `(…, 2789)` snapshot (`corpus.md:25`) — and no lifecycle value can egress in a model payload, because the row that egresses has no field to carry it. `index.json`, the SPEC-11 **tracked** artifact, is unchanged: it keeps one entry per approved row whatever its status, so `test_index_covers_every_row` is untouched by this ticket (all 87 vendored rows land `current`, package `document-manifest.json` read 2026-08-27). Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** — and because it amends an owner-confirmed decision and fires eligibility-assistant-D-56's re-open clause (whose named mechanism is "the `current`-only filter before `rank`"), **that same act re-closes the eligibility-assistant-D-56 §1 PHI approval on the amended mechanism** (load-time exclusion rather than a filter before `rank`; owner 2026-08-27). **Note 2026-08-27 (`lifecycle` gate r2 f2):** "the approved rows that are `current`, only" is the index's **`rows`**; the same object also carries a manifest-wide `categories` tuple over every approved row whatever its `status`, so the model-facing `topic` enum is not narrowed — eligibility-assistant-D-80 | The alternative that keeps D-57's letter puts `status` on the index row, which moves `MAX_ROW_BYTES` to 2,796 and reddens three `corpus` pins that recompute it from five manifest fields plus the file; keeping the state in a second module-level map instead would change `load()`'s single return value, which `corpus`'s non-publish rule and `lifecycle`'s rig install both rest on. Excluding at load is the one form with no ripple and a strictly stronger guarantee. |
| eligibility-assistant-D-76 | plan | **The corpus-update operator step is an image rebuild and recreate, not `docker compose restart` (`lifecycle`; closes `lifecycle` gate r1 f1; amends eligibility-assistant-D-48's and eligibility-assistant-D-57's "restart").** The corpus is **baked into the image**: `services/ai-assistant/Dockerfile:8` is `COPY . .` under `WORKDIR /app` (`:3`), and the compose service declares `build: ./services/ai-assistant` with **no `volumes:`** and no `restart:` policy (`docker-compose.yml:222-247`; the file's only two bind mounts are postgres `:10` and the migration runner `:267`, and `restart:` has zero hits). `make up` is `docker compose up -d` (`Makefile:34-35`), which starts an existing image and never rebuilds it. So a working-tree manifest edit reaches the container only through `docker compose up -d --build ai-assistant` (or `make build` + `make up`, `Makefile:46-47`), and every runbook step, verification and demo turn that said "restart" says that instead. SPEC-62's frozen "with a service restart and no runtime mutation or reload" is **satisfied, not amended**: the service is stopped and recreated from a freshly built image, the corpus is read once at load, and nothing mutates or reloads at runtime — the row constrains the *mechanism class*, and a rebuild-and-recreate is a restart with the new bytes actually present. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** (it is the reading of a frozen `gate:` row's wording and it changes the demo the owner will watch) | The plan's procedure, its "first time the refusal is observed in a real container" claim and the demo's two turns all rested on a command that restarts the same image; a supersession that changes nothing the service sees would have failed at the impl gate, live, with no test able to catch it. |
| eligibility-assistant-D-77 | plan | **The named lifecycle document is `DOC-SYN-NEVER-STATE`, and the bucket its topic yields is an `index.json` curation obligation on `corpus` (`lifecycle`; closes `lifecycle` gate r1 f5; the reachability class eligibility-assistant-D-64 settled for the cap and eligibility-assistant-D-70 for the record's legs).** The supersession and withdrawal demonstrated in the runbook (SPEC-62) and asserted in `tests/test_a1_lifecycle.py` (SPEC-60/61) use one lineage: `DOC-SYN-NEVER-STATE`, `documents/synthetic/DOC-SYN-NEVER-STATE.md`, category `front-desk-scripts`, successor `DOC-SYN-NEVER-STATE-V2`. Facts it is chosen on, all from the package (`document-manifest.json` and `evaluations/eligibility-assistant-evaluations.jsonl`, read and joined 2026-08-27): it is a synthetic `DOC-SYN-*` row (eligibility-assistant-D-48/D-54 require that, never client public-domain text); it is **not** in `outcome.reason_citation_ids()` (the seven eligibility-assistant-D-19 ids); `front-desk-scripts` is one of the **eight single-row categories** and the only one of the five non-reason-table singles that is the `expected_source_ids` of **zero** acceptance cases (`DOC-SYN-DOWNTIME` EVAL-015, `-EFFECTIVE-TERM-DATES` EVAL-002/032, `-MEMBER-ID-MISMATCH` EVAL-021, `-VISIBILITY-OUTCOMES` EVAL-019 are the other four), so withdrawing it moves no recall figure and no harness case; its applicability is "All visits". **Curation obligation on `corpus`:** `index.json` must give this row `payers` / `products` / `states` = `*`, so `("front-desk-scripts", <any payer>, "unconfirmed", "unconfirmed")` returns exactly this one row and, once it is withdrawn, none — carried as a clause on `corpus`'s `index.json` change row and asserted by both `lifecycle` tests, so a curation that broke it reddens rather than passing silently. **Consequence stated, not hidden:** the row is **tier 4** (original synthetic, category not `payer-training-summary` — eligibility-assistant-D-38 note 2), so by eligibility-assistant-D-38's applicability rule ("no row of tier ≤ 3") a turn on this topic renders `refuse_definitive` + `escalate` **before** any withdrawal. The outcome therefore does not discriminate and the **citation set** does: pre-withdrawal the reply cites the row, post-withdrawal it cites nothing; pre-supersession it cites `DOC-SYN-NEVER-STATE`, post-supersession `DOC-SYN-NEVER-STATE-V2`. No single-row `DOC-SYN-*` category can do better — a tier ≤ 3 row in the same topic is exactly what "sole source" excludes. Proposed 2026-08-27; **confirmed (owner 2026-08-27, in session)** (it fixes the document the owner watches at the demo and hands `corpus` an obligation) **Note 2026-08-28 (`lifecycle` gate r3 f1):** the demo's portal turn is entered as question type `covered today`, payer Aetna (the seeded demo member `AETN1224`), product and state `unconfirmed`; its topic is model₁'s choice (SPEC-1, eligibility-assistant-D-25) and cannot be forced, so the runbook pairs it with a deterministic in-container `policy_index.lookup("front-desk-scripts", "aetna", "unconfirmed", "unconfirmed")` observation, and the dependence is `lifecycle` residual (h). | "A synthetic row that is the sole row of its bucket, chosen at implementation" is an existence claim about a curated artifact that does not exist yet; naming the row and the curation that keeps the bucket sole-sourced is the only way SPEC-61's test and the demo have a legal exercise, and stating the tier-4 consequence is what keeps `test_withdrawn_sole_source_refuses` from passing on the unmodified corpus. |
| eligibility-assistant-D-78 | plan | **The live-run record: file name, two runs, and the commit that carries the merged sha (`trace`; closes `trace` gate r1 f2).** **(a) Name.** The record is `docs/eligibility-assistant-live-run.md`, not `docs/a1-live-run.md`: `a1` is a dead item token (eligibility-assistant-D-1 rename) and eligibility-assistant-D-51 already closed that class for the SPEC-31 check cell — a tracked *doc name* is the same class, while `a1` survives by design in planned code identifiers (`tests/test_a1_live.py`, `A1_LIVE_BEDROCK`, `A1_*` settings), which this decision does not rename. **(b) Two runs, one file, and the writer of each.** SPEC-32's frozen check is `gate:` "record present and reproduced at impl gate (PR-2); observation in Delivery" (`docs/workflow/eligibility-assistant.md:290`) and the impl gate is **pre-push** (`docs/workflow/README.md:46-49`), so the run it reproduces is against the **branch HEAD**: that run lands in the PR diff, its `sha:` field carrying the branch-HEAD commit, and it is what the `gate:` cell checks. A PR cannot carry its own squash sha, so the record is **re-run once post-merge and updated in the `noncode-merge` stamp commit** — the second of the two post-merge commits the README landing rule defines (`docs/workflow/README.md:236-241`: first delete the merged plan file, then stamp `delivery MERGED <merge-sha>` and record the deletion sha in `## Delivery`) — which rewrites the record's `sha:`, `date:` and `trace reference:` to the merged run's. That commit is the only post-merge write the README defines, so it is the only carrier available; the `## Delivery` observation names both runs, and "produced against that exact sha" is true of the second. **(c) The producing command turns tracing on explicitly.** `.env` at rest carries `LANGSMITH_TRACING=false` (E-7; the template says so at `.env.example:224`), so a live leg run "with the owner's `.env` loaded" emits no LangSmith run and can produce no trace reference: the record-producing leg is `A1_LIVE_BEDROCK=1 LANGSMITH_TRACING=true` with the `.env` key loaded from the environment. No `.env*` file is edited — the Secrets §1 zone stays untouched. **(d) Fields, unchanged:** merged sha, trace reference, date, `mode=real`, model id — never a key, never a payload, never a member id. Proposed 2026-08-27 (`trace` gate r1 f2, stage 3); **confirmed (owner 2026-08-27, in session)** — **two runs, one file**: the branch-HEAD run in the PR and the post-merge re-run written by the `noncode-merge` stamp commit, as written **Note 2026-08-28 (`trace` gate r3 f3):** the record-producing run drives the two-stack chain (gateway `TestClient` → assistant `TestClient` with a header-forwarding router, real key, real Bedrock), so the tree it references is the SPEC-27 tree; the trace reference is `<project>/<root run id>`, the root run id being the request's `X-Correlation-Id`. | The frozen row asks for a record produced against a sha that does not exist until the PR has merged, and the plan named neither the writer of that update nor the env the producing run needs — so as written the `gate:` row would be met by a run that emits nothing, recorded against a sha that is not the merged one. |
| eligibility-assistant-D-79 | plan | **TODO-10 closes on this item; TODO-9 does not (`trace`; closes `trace` gate r1 f8).** `docs/todo.md:52` TODO-9 reads "Capture a real Bedrock response from a live `complete()` / `complete_structured()` call and pin it as a fixture, so the mocks match a shape AWS actually returned"; `:53` TODO-10 reads "Gated live-LLM integration test; needs a real bearer key \| blocked_by: TODO-9". **TODO-10 closes here:** `tests/test_a1_live.py` is exactly that test — opt-in on `A1_LIVE_BEDROCK` (eligibility-assistant-D-43), real bearer, real Bedrock, created by `turn` for SPEC-17 and extended here for SPEC-32. **TODO-9 does not close and stays OPEN, out of scope:** it names `complete()` / `complete_structured()`, which the agent path does not call — its only egress is `llm_client._call` → `client.messages.create` (eligibility-assistant-D-40) — and the one endpoint that still calls `complete_structured`, `/ai/intake-instructions`, is out of scope by eligibility-assistant-D-5. Capturing a live response body into the tracked tree is separately refused: this ticket's Landmines block fixes the live record's content at "never a payload", so a payload-derived tracked artifact would re-open the eligibility-assistant-D-56 approval to satisfy a hygiene TODO. Consequence: `trace`'s `docs/todo.md` change row closes **TODO-10 only** and TODO-9 is left verbatim; REQ-9's Notes clause "Closes `docs/todo.md` TODO-9 / TODO-10" (`docs/workflow/eligibility-assistant.md:125`) is **over-broad as written**, and amending that Notes cell is an owner act on an AGREED requirement — put to the owner with this row. No frozen SPEC row cites TODO-9, so the plan is unchanged either way. Proposed 2026-08-27 (`trace` gate r1 f8, stage 3); **owner call made 2026-08-27 (in session): amend REQ-9's Notes at stage 1** to "Closes TODO-10; TODO-9 stays open — it names `complete()` / `complete_structured()`, the `/ai/intake-instructions` path this item excludes (eligibility-assistant-D-5)"; this row is **confirmed** as the record of that call. **Note 2026-08-27 (executed, and one consequence found while executing):** REQ-9's Notes cell is amended and the `## Requirements` Status line dated. `docs/todo.md:53` carries `TODO-10 | ... | blocked_by: TODO-9` — closing TODO-10 while TODO-9 stays open leaves a dangling `blocked_by`, so `trace`'s `docs/todo.md` change row must also clear or re-word that field; raised here rather than left for the next gate. **Closed 2026-08-27 (`trace` gate r2 f3):** `trace`'s row now removes TODO-10's `blocked_by: TODO-9` in the closing edit — a closed row has no precondition, and the live test needs no captured fixture — with TODO-9 left `[ ]` and verbatim | A change row that closes a TODO its own diff does not satisfy is the cheapest kind of false record; and the fixture the TODO asks for would be a live provider payload in the tracked tree — the one thing this ticket's §1 block forbids. |
| eligibility-assistant-D-80 | plan | **`categories()` is manifest-wide: the model-facing `topic` enum is built over every approved manifest row whatever its lifecycle status (`lifecycle`; closes `lifecycle` gate r2 f2; clarifies eligibility-assistant-D-75 and `corpus`'s `categories()` clause).** `policy_index.categories(root=None)` mirrors `load(root=None)`: it runs the one sha-verifying `load(root=root)` (no second manifest reader — SPEC-7's boundary, `corpus` gate r4 f4 / eligibility-assistant-D-35 note 2) and returns `load(root=root).categories`, the sorted tuple of distinct `category` values over the approved rows of that manifest **including `superseded` and `withdrawn` rows**; the object `load()` returns carries `rows` (the current-only approved rows `_filter` / `lookup` / `fetch_by_id` / `rank` read and `MAX_ROW_BYTES` is computed over — D-75) and `categories` from its one parse, so `_INDEX` is still one object, the non-publish rule for a non-default `root` (`corpus` gate r5 f3) is unchanged, and a `lifecycle` variant installed as `_INDEX` carries its manifest-wide categories with it. Consequences: (1) a topic whose every row is withdrawn stays in `PolicyLookupArgs.topic`, model₁ may still choose it, `lookup` returns zero rows and the turn renders `refuse_definitive` + `escalate` with no citation — SPEC-61's contract, and the only reading under which the runbook's withdrawal step on the eligibility-assistant-D-77 lineage (`front-desk-scripts`, a single-row category — package `document-manifest.json` read 2026-08-27) demonstrates SPEC-61/62 on the real tree, since a current-only enum would drop the topic from the tool schema and model₁ could never reach the refusal; (2) `corpus`'s `test_topic_enum_equals_manifest_categories` (`Literal` == manifest categories == `index.json` topics) stays a three-way equality over one manifest-wide set, because `index.json` keeps an entry per approved row whatever its status; (3) the 25-category count moves only when a manifest row in a **new** category is added, never on a supersession or withdrawal within an existing one — stated in the runbook's operator warning. Pinned by `tests/test_a1_lifecycle.py::test_categories_survive_withdrawal` (on the D-77 withdrawn variant, `categories(root=variant)` still contains `front-desk-scripts` while `load(root=variant).rows` holds no row of that topic; `lifecycle` verification 3's second break). Because it fixes what the `topic`-only tool schema is built over — a mechanism the `corpus` Landmines block names — it re-opens eligibility-assistant-D-56 on the `corpus` and `lifecycle` blocks until confirmed. Proposed 2026-08-27 (`lifecycle` gate r2 f2, stage 3); **confirmed (owner 2026-08-27, in session)** — and because it fixes a mechanism both the `corpus` and `lifecycle` Landmines blocks name (what the `topic`-only tool schema is built over), **that same act re-closes the eligibility-assistant-D-56 §1 PHI approval on the manifest-wide `categories()` clause in both blocks** | The plan left the enum's row set unnamed once D-75 split "approved" from "current", and the default suite could not observe the choice (the `Literal` is built from the real tree at rig import while a variant is installed as `_INDEX` alone) — so the demo's withdrawal step would have shown a refusal or a vanished menu topic depending on an implementer's guess; manifest-wide is the reading that keeps SPEC-61 reachable and the three-way enum equality intact, and the attribute on the one returned object is the only form that adds no second reader and no second module-level structure. |
| eligibility-assistant-D-81 | spec | **SPEC-28's allowlist admits the Bedrock call's request id, token counts and cost estimate (owner 2026-08-28, `trace` gate r3 f2 under the round-3 rule; amended in place at stage 2 — the eligibility-assistant-D-65 / D-74 precedent).** `services/ai-assistant/llm_client.py:618-640` `_trace_metadata` attaches `model`, `request_id` and — when the response carries integer usage — `input_tokens`, `output_tokens`, `estimated_cost_usd` to the `bedrock` run: the metadata-only set ADR 0009 (`adr/0009-*.md:125-131`, "model, token counts, cost, latency, request id") and ADR 0006 put there. The 2026-08-24 freeze enumerated the allowlist without reading that function, and E-7's smoke saw `request_id` only because its fake response carried no usage. Chosen over narrowing `_trace_metadata` to the frozen list, which would drop the cost signal the ADRs added it for, move `tests/test_tracing.py`'s pins (`:61-74`, `:211-214`) and falsify ADR 0009's constraint. Consequence for `trace`: the closed key set is **twelve** keys — the eight common keys plus four the `bedrock` run alone may carry (`request_id` a string, `input_tokens` / `output_tokens` non-negative integers, `estimated_cost_usd` a non-negative float, the three usage keys present together or not at all), each still bounded to a scalar domain; nothing free-text, no payload. The widening re-opened eligibility-assistant-D-56 on `trace`'s closed-allowlist mechanism and this call re-closes it the same day. Executed 2026-08-28: SPEC-28's row and the `## Spec` Status line amended, 66 rows, contract 339 lines. | The frozen row described the allowlist the tests would pin, not the scalars the existing wrap already emits — the row was wrong about the tree, not the tree about the row. |
| eligibility-assistant-D-82 | plan | **The contract's per-ticket delivery record is one table row; deviations and live-run evidence live in the ticket file (`corpus` impl-gate r1 f1).** The record shape as first written for `corpus` was 74 prose lines, which at six tickets puts `docs/workflow/eligibility-assistant.md` at ~784 against CI's 400-line `workflow-doc-cap` (`.github/workflows/ci.yml:135-160`, a `needs:` of the terminal gate, so it blocks merge); the non-record content of this contract is already 340 lines, leaving ~60 for all six records — under 10 lines each, which no wrapped prose record fits. Resolution: `## Delivery` gains a **`### Per-ticket delivery records`** table, one row per ticket, carrying only what the `implementation` skill's evidence rule calls references — branch / stamp / series shas, baseline movement, the test-first vs verification-covered split, traceability, residual registry IDs — while **deviations, the stamp-commit scope note and the live-run detail move to a `## Delivery evidence` section in `plans/<item>/<ticket>.md`**, which is uncapped, is what the impl gate and the codex round read alongside the diff, and dies at merge behind the `plan-file deletion sha` already recorded in the ticket table. A table row is one physical line whatever its length, so six filled rows cost the cap nothing beyond the block's fifteen lines (contract measured 355 with `corpus` filled). This does **not** weaken the durability rule: the README's pre-delete sweep still carries every decision ID the contract cites into `## Delivery` before the ticket file is deleted, and nothing that must outlive the item lives only in `## Delivery evidence`. Recorded as a README shape rule in the same commit (`docs/workflow/README.md`, `## Delivery` row and the Size cap bullet, dated in place) because it binds every ticketed item, not this one. Proposed 2026-08-29 (stage 4, `corpus` impl-gate r1 f1); **CONFIRMED by owner 2026-08-29 (in session)** — the owner chose compression over raising the cap, and the row shape over a compact prose block after the prose block was measured at 418 > 400 for six tickets. |

### Evidence

| ID | Date | Measurement |
|---|---|---|
| E-1 | 2026-08-24 | eligibility-assistant-D-2 spike (scratch venv, outside repo): langchain 1.3.16 + langchain-aws 1.7.3 + boto3/botocore 1.43.78 on python 3.12. **Dry leg**: `create_agent` + `ChatBedrockConverse` construct offline under `AWS_BEARER_TOKEN_BEDROCK` env auth; agent compiles to nodes `model`/`tools`. **Live leg (real Bedrock, bearer token from owner `.env`, model `us.anthropic.claude-sonnet-4-6`, temp 0, max 300 tok)**: the model itself invoked the bounded `policy_lookup` tool (args `AETN`), one tool message, final answer cited the fixture doc id `AETN-ELIG-001`, usage metadata (input/output/total tokens) returned. Exit green. Two caveats surfaced and were folded 2026-08-24: (a) `create_agent` returns a langgraph `CompiledStateGraph` → eligibility-assistant-D-2 amendment; (b) `ChatBedrockConverse` (Converse API) vs `llm_client` (`invoke_model`) → eligibility-assistant-D-9. |
| E-2 | 2026-08-24 | Client package read (README, scope, six policies, evaluations READMEs, licensing notes, manifest + jsonl schemas, three sample docs; **not** the 87 documents themselves): 114 files / ~700K; manifest 87 approved docs across 25 categories, every row carries id/title/section_labels/version_effective/retrieval_date/applicability/allowed_roles/license_disposition/content_sha256/path — REQ-2's four citation fields are all present per document. 32 acceptance cases, 23 categories, expected outcomes include `active`, `inactive`, `unknown`, `unavailable`, `reverify`, `conflict`, `refuse-definitive`, `refuse`, `care-first`, `stop`, `disputed`, `visibility-record`, `reject-fixture`. Manifest carries **no source-tier field** (REQ-13 tiering must be derived). Access matrix matches shipped `config/roles.yaml` at `87e0b37`. |
| E-3 | 2026-08-24 | Package **joined** (script, scratch): 87 manifest `path` files present, **131,032 bytes total** (~1.5 KB each); the 696K/113-file figure was the whole package. `category`: 25 values (usable as the `topic` enum); `payer_publisher` and `plan_product_state_applicability` are prose (81 distinct applicability strings / 87 rows — enum index must be curated). `license_disposition`: 37 public-domain full text, 30 original synthetic, 15 citation-only + 5 public-domain pointers/extracts. Harness: 32 cases, 23 categories, 26 distinct `expected_outcome` values; **16 cases cite a non-manifest source, 5 cite only non-manifest sources** — `policies/*.md` ×13 (never in manifest), `procedures/front-desk-scripts.md` ×2 + `procedures/coverage-question-cheat-sheet.md` ×1 (in manifest as `DOC-FRONT-DESK-SCRIPTS` / `DOC-COVERAGE-QUESTION-CHEAT-SHEET`), `evaluations/fixtures/README.md` ×1 (EVAL-031; "Never approve" is its title line, not a section heading). Code: `visit_templates.py` catalog = 8 ids (`ask_member_id` `confirm_which_member_id` `verify_card_details` `retry_shortly` `proceed_per_policy` `self_pay_options` `collect_secondary` `note_coverage_result`), no refuse/escalate/conflict/care-first; `VisitIntent` = 5 values; no `emergency` in ai-assistant or `frontend/app/assistant/page.tsx`; no correlation header in `frontend/app/lib/gateway.ts`, `services/gateway/app.py`, or `services/ai-assistant/app.py`; `VisitFacts` is `extra="forbid"`; `config/roles.yaml` `default_role: staff` with capabilities. |
| E-4 | 2026-08-24 | eligibility-assistant-D-9 shape (ii) + eligibility-assistant-D-18 mechanism spike (scratch venv, outside repo: langchain 1.3.16 / langgraph 1.2.11 / langchain-core 1.6.0 / langsmith 0.11.1 on the repo-pinned boto3 1.40.0 + pydantic 2.10.4, python 3.12). A thin `BaseChatModel` (`_generate` + `bind_tools`, ~40 lines) whose only egress is `llm_client._call`; tools ride `extra_body`, which the byte-bound budget gate already serialises. **Live (real Bedrock, owner bearer key, `claude-sonnet-4-6`, max 300 tok) — 12/12 checks green.** (A) happy turn: model₁ `stop_reason=tool_use`, invoked `policy_lookup(payer=AETN, topic=eligibility_verification)` (enum args); the `before_model` hook ran the payer stand-in once on the tool-result iteration and injected `active`; model₂ `end_turn`, replied `ACTION=proceed_per_policy CITE=DOC-SYN-AETN-ELIG-VERIFY` (the retrieved id); exactly 2 model calls (`ModelCallLimitMiddleware(run_limit=2)`); `_enforce_char_cap` / `_enforce_budget` / `_require_bearer_token` each ran 2×; tokens 695+95 / 979+31, 2.0–2.4 s per call. (B) `no_retrieval`: off-topic question → model₁ `end_turn` with no tool call → `after_model` jumped to `end` with `reason=no_retrieval`, no tool ran, the hook made no payer call; the fallback path then made the single payer call. (C) once-guard: a second payer call in the same turn raises. (D) bearer unset → the agent path raises `LLMConfigError(egressed=False)` with zero model calls. **Seam deltas (carried to the spec):** (i) `llm_client._adapt` keeps only `type`/`text` — shape (ii) needs it to also carry `id`/`name`/`input` on `tool_use` blocks and `stop_reason` (additive superset; `_result_from_response` untouched); (ii) `_call`'s `str(content)` bound already covers list-content (`tool_result`) messages, so no gate change; (iii) the tool-result turn and the injected status must merge into one user message — Anthropic Messages requires role alternation. `tracing.wrap_create` is installed at `llm_client` import, so the agent path inherits it; run keyless with `LANGSMITH_TRACING=false` (eligibility-assistant-D-8). Script + results: session scratchpad `e4/spike_e4.py`, `e4_results.json` (not tracked). |
| E-5 | 2026-08-25 | Gate r1 f12 — the E-4 pins composed with the **repo's** `langsmith==0.10.5` (E-4 itself ran on 0.11.1), scratch venv outside the repo, python 3.12.13: pip resolved `langchain==1.3.16` / `langchain-core==1.6.0` / `langgraph==1.2.11` / `langsmith==0.10.5` / `pydantic==2.10.4` without conflict (langgraph-checkpoint 4.2.0, langgraph-prebuilt 1.1.0 pulled). **Offline dry leg green:** a thin `BaseChatModel` (`_generate` + `bind_tools`, `_stream` raises) with a scripted call-1 `tool_use` / call-2 JSON text under `create_agent(tools=[policy_lookup], middleware=[ModelCallLimitMiddleware(run_limit=2, exit_behavior="end")])` → graph nodes `model` / `tools` / `ModelCallLimitMiddleware.before_model` / `.after_model`, exactly 2 model calls, exactly 1 tool call (`topic=eligibility-verification`), a `ToolMessage` in the transcript, final `AIMessage` = the JSON decision; `langchain_core.tracers.langchain` imports against langsmith 0.10.5 with `LANGSMITH_TRACING=false`. **Not measured:** the live Bedrock leg under 0.10.5 (E-4's live 12/12 was on 0.11.1) — named as a residual in the Landmines block; first exercised live at SPEC-17 / SPEC-32. Script: session scratchpad `e5/dry_e5.py` (not tracked). |
| E-6 | 2026-08-25 | Research sweep (subagent, 25 sources cited; report in session scratchpad, not tracked — headline claims carried here): BM25 70.05 vs MedCPT dense 70.06 on MIRAGE (arxiv 2402.13178); AWS vectorless agentic-search study within 6–12% of Bedrock KB on RAGAS, ahead on FinanceBench (arxiv 2602.23368); Anthropic contextual-retrieval guidance — under ~200K tokens put the corpus in the prompt (ours ≈ 33K); hard metadata filters' silent-fail pattern and its three mitigations — soft bias, pre-retrieval coverage check, log resolved filter + counts (optyxstack.com); soft-deleted embeddings recoverable from HNSW files, 73% gender / 50% surname on synthetic clinical data (arxiv 2606.18497); embeddings of PHI are PHI (arxiv 2511.11347); Bedrock model-invocation logs persist prompts (AWS HIPAA-ready GenAI blog); a deployed clinical RAG logged 2,660 conversations and could not evaluate retrieval, minimum score tracked downvotes where mean hid them (medRxiv 10.64898/2026.01.26.26344757); cross-encoder rerank 0.827 vs dense 0.822, query expansion 0.671 (arxiv 2605.02520); eval-set sizes 47 (insurance case study) / 100–200 production guidance; MedRAG (589★) does not pre-embed its fast-moving corpus. Reddit unreachable to the crawler — practitioner coverage is HN + LangChain forum. |
| E-7 | 2026-08-26 | LangSmith key smoke (session scratch, not tracked; `.env` loaded in-process, never printed): server `0.17.15`, `list_projects` → `Riverbend`; one run through `services/ai-assistant/tracing.py::wrap_create` around a fake `create` carrying a sentinel string in `messages` → run `bedrock.invoke_model` (`llm`, `success`) in project `Riverbend`, `inputs: {}`, `outputs: None`, metadata `request_id=smoke-req-1` only; sentinel absent. `.env` at rest: `LANGSMITH_TRACING=false`, `LANGSMITH_HIDE_INPUTS/OUTPUTS=true`, project `Riverbend`. venv `langsmith 0.10.5` on python 3.12.13. |

## Plan

Status: DRAFT 2026-08-26 — gate r2 (6 findings) dispositioned this session (f1/f3 on 2026-08-26 by eligibility-assistant-D-50/51; f2/f4/f5/f6 below); eligibility-assistant-D-55..58 owner-confirmed in session 2026-08-26; `lifecycle` and `retrieval-eval` ticket files created (eligibility-assistant-D-57/58); the LangSmith blocker closed (eligibility-assistant-D-8, E-7). Six tickets, each with its own gate, `Baseline at branch:` cell, impl gate, codex loop and deliberately reported baseline move. Landing order: **corpus** → **llm-seam** → **retrieval-eval** (dark) → **turn** → {**lifecycle**, **trace**} (independent of each other). Per-ticket gates run per ticket file, `corpus` first.

| ticket | scope (SPEC rows) | depends on | file |
|---|---|---|---|
| corpus | eligibility-assistant-SPEC-7–11, 38, 43 | none | `plans/eligibility-assistant/corpus.md` |
| llm-seam | eligibility-assistant-SPEC-24, 30 | corpus (pins — eligibility-assistant-D-59) | `plans/eligibility-assistant/llm-seam.md` |
| turn | eligibility-assistant-SPEC-1–6, 12–23, 25–26, 33, 36–37, 39a, 41–42, 44–56 | corpus, llm-seam, retrieval-eval (`fetch_by_id`'s `(rows, record)` — eligibility-assistant-D-58 via D-61; D-59 note of 2026-08-27) | `plans/eligibility-assistant/turn.md` |
| trace | eligibility-assistant-SPEC-27–29, 31–32, 34–35, 39b, 40 | turn | `plans/eligibility-assistant/trace.md` |
| lifecycle | eligibility-assistant-SPEC-58–62 | corpus, turn | `plans/eligibility-assistant/lifecycle.md` |
| retrieval-eval | eligibility-assistant-SPEC-63–66 | corpus | `plans/eligibility-assistant/retrieval-eval.md` |

Per-ticket plans live in `plans/eligibility-assistant/`; re-sliced 2026-08-25 from the monolithic plan per README rule B; `lifecycle` / `retrieval-eval` authored 2026-08-26 from the re-frozen SPEC-58–66 (ownership per eligibility-assistant-D-55).

## Findings

### Req-review — round 1, 2026-08-24

Owner review of the requirements DRAFT of 2026-08-24 (with research notes). Folded in the
same session; dispositions are working-tree edits, not commits (stage-1 landing rule).
What held, per the owner: problem scoping, eligibility-assistant-D-6 supersession, E-1 before framework
commitment, eligibility-assistant-D-10 verbatim harness, REQ-2 cite-only-if-retrieved.

| # | anchor | finding | disposition |
|---|---|---|---|
| P | premise / REQ-3 | "Complete RAG pipeline" premise wrong: corpus is 87 files / 131 KB pre-sectioned with metadata in manifest; the client's "How to apply a document" is the retrieval algorithm; embeddings add a second egress, nondeterminism in 32 tests, and infra for no gain. Call: deterministic in-process retriever, enum-only tool args, no `query: str`; close Open (4) as vendor-all-87 | A · folded → eligibility-assistant-D-13, REQ-3′, eligibility-assistant-D-6 amended, Open (4) closed, E-3 |
| 1 | eligibility-assistant-D-10 / REQ-2 / REQ-3 / REQ-11 | Harness ↔ corpus contract broken: 16/32 cases cite non-manifest sources, 5 cite only those; REQ-2 + REQ-3 + D-10 make 10+ cases unpassable as written. E-2 read the package but never joined it. Call: new decision splitting source kinds and test kinds; client clarification before PR-1; say what "32/32" means now | A · folded → eligibility-assistant-D-12, REQ-11′, Appendix, Open (5) |
| 2 | REQ-13 / REQ-4 | REQ-13 note false at source: refuse/escalate are not catalog actions (catalog = 8 ids, none of them); `VisitIntent` 5 values; harness ~26 outcome values. Call: "closed catalog, extended by named ids"; an outcome enum (~10) separate from action ids; the 32→outcome mapping table in requirements | A · folded → eligibility-assistant-D-15, REQ-4′, REQ-13′, Appendix |
| 3 | REQ-3 | What the model sees is undecided by omission — clerk text never reaches the prompt today, the harness needs ~15 question types, `_derive_intent` handles five by regex. Options: extend classifier (brittle), send redacted free text (D13 shift), portal question-type selector (deterministic, PHI-safe, demo-visible). Call: (c); REQ-3 must state prompt contents, not just query contents | A · folded → eligibility-assistant-D-14, REQ-17, REQ-3′ prompt-contents sentence |
| 4 | REQ-14 | Emergency has no input — no flag in request/facts/intent; model in the loop on an emergency turn is the wrong design. Call: explicit `emergency: bool` from portal, deterministic short-circuit, fixed EMTALA citations, mode `care_first`; REQ-1 exception stated; note REQ-14 stricter than EVAL-016 | A · folded → eligibility-assistant-D-14, REQ-14′, REQ-1′ exception clause |
| 5 | REQ-4 / REQ-5 | Turn order and degraded path unpinned: does the payer call still run when Bedrock fails? If not, an outage regresses the working feature. Call: payer call on deterministic intent regardless; fallback = today's reply, no citations, mode `fallback` explicit | A · folded → REQ-16 |
| 6 | REQ-6 | Loop unbounded: agent = ≥2 Bedrock calls/turn, ADR 0011 §7's `llm_egress` slot assumes one. Call: max 1 tool call, max 2 model calls, per-turn budget spans both, overrun → fallback | A · folded → REQ-6′ |
| 7 | eligibility-assistant-D-9 | Give the call now: shape (ii), bound to the `llm_client` seam — `ChatBedrockConverse` sends a Converse body while the byte-based budget gate measures the Messages body `llm_client` builds; wrapping (i) gates a payload that is not the egress payload; disqualifying. `invoke_model` Messages format supports tools natively | A · folded → eligibility-assistant-D-9 RESOLVED, REQ-6′; residual: E-4 live spike of shape (ii) required before spec freeze (E-1 proved Converse only) — Open (6) |
| 8 | REQ-7 / REQ-8 / eligibility-assistant-D-8 | Correlation id → PR-1 (zero ids in estate; header born in the Next route correlates keyless). LangSmith key: escalate today with a date — "Open (1)" in a working-tree file is not the report the trainer asked for. Research: LangSmith `hide_*` streaming redaction bypass → don't stream; REQ-8 negative scan must cover error-path traces | A · folded → eligibility-assistant-D-16 (amends eligibility-assistant-D-3), REQ-7′, REQ-8′, Open (1) escalated needed-by 2026-08-27 |
| 9 | REQ-15 | EVAL-030 `staff`: has `ai.use` compat, "new vs old account" undetectable, REQ-15 hides the choice in "not for new grants". Call: known limitation + test pinning `staff` absent from the assistant's allowed set; demo as "reclassify" | A · folded → eligibility-assistant-D-17, REQ-15′ |
| 10 | REQ-5 / REQ-3 | Minor: `VisitFacts` `extra="forbid"` → provenance fields are a persisted schema change, note Redis TTL; citation-only records already hold only bibliographic facts + note, so REQ-3's "special handling" is just "vendor as-is" | A · folded → REQ-5′, REQ-3′ |
| R | out of scope | Research: 271 answers coverage status only (answer-boundary doc aligned); ReAct audit gap closed by validation gate + 1-tool bound + trace, no LangGraph needed; CA AB 3030 carves out admin use — one limitations line; Bedrock HIPAA-eligible ≠ compliant — limitations line | A · folded → out-of-scope entries (AB 3030, HIPAA-eligible); eligibility-assistant-D-2 unchanged |
| Pt | pattern | Requirement lines asserting a control without naming the mechanism (catalog vocabulary, tool-arg boundary, emergency input, loop bound) — each needs one sentence on what makes it true. Second: harness accepted as the pass/fail line before being joined against the corpus contract | A · applied: every primed row names its mechanism; E-3 is the join; carried as the stage-1 lesson for eligibility-assistant's spec |

### Req-review — round 2, 2026-08-24

Owner review of the r1-folded DRAFT. Folded in the same session; working-tree edits only.
What held, per the owner: all eleven r1 dispositions with mechanism named; E-3's numbers
(81/87 applicability strings, 26 outcomes, 131,032 bytes, `VisitIntent` 5 — the owner's 4
was a grep cut-off); the Appendix's 32-row mapping; the eligibility-assistant-D-12 three-kind split; the
08-27 escalation. Both new findings were verified against code before folding.

| # | anchor | finding | disposition |
|---|---|---|---|
| 1 | REQ-16 | Turn order breaks the existing selection gate: the draft put the agent (model → retriever → model) before the payer call, so the model picks `retry_shortly` vs `self_pay_options` blind to status and is then validated against `required ⊆ selection ⊆ allowed` (`_select_reply_items` / `default_selection` / `allowed_selection`, status-keyed) — reject → fallback on the happy path; demo shows `mode=fallback` on active coverage. Call: model₁ (tool choice) → retriever → app-triggered payer (intent-keyed `before_model` hook on iteration 2) → model₂ (rows + status) → validate; retrieval still precedes the eligibility action; exactly REQ-6′'s bound; also fixes EVAL-015; E-4 must prove the hook | A · folded → eligibility-assistant-D-18, REQ-16′, REQ-6″ ("exactly", `no_retrieval`), REQ-1″ (which call decides what), Open (6). Fold notes from the code read: the payer call now has two possible trigger sites (hook, or the fallback path when model₁ dies first) → once-guard in REQ-16′; model₁ can answer without calling the tool → `no_retrieval` bound, reject not cheaper success; the `app.py:757` no-freedom short-circuit stays so a no-id turn does not buy model₁ |
| 2 | eligibility-assistant-D-12 / REQ-16 / REQ-14′ | Every no-model path un-citable: D-12 says `DOC-*` in `expected_source_ids` = retrieved + rendered, REQ-16 says fallback = no citations; collide on EVAL-012 (`DOC-SYN-PRIVACY-FD`), 014 (`NO-INVENTION`), 017 (`SPEND-STOP`), 018 (`MODEL-FAILURE`), 024 (cheat sheet; REQ-14′ renders EMTALA + SYN-EMERGENCY only). Call: generalise REQ-14′ — one deterministic citation table keyed by non-agent reason, fetched through the same retriever so REQ-2 holds literally; REQ-16 "no citations" → fixed reason citation; REQ-1′ exceptions → one class. EVAL-014 hides two sub-cases (model resists vs model subverted) — test the subverted one, it proves the gate; fold into the D-12 client note | A · folded → eligibility-assistant-D-19, eligibility-assistant-D-12 amended, REQ-1″, REQ-2′, REQ-3″ (no id argument on the model tool — the by-id fetch is app-side on the same index), REQ-8″ (reason in the trace allowlist), REQ-11″ (EVAL-014 ×2), REQ-14″, Appendix reason table + rows, Open (5). Verified against the jsonl: all five rows match. Fold notes: EVAL-024 expects EMTALA + cheat sheet and **not** `DOC-SYN-EMERGENCY`, and the harness README is silent on subset-vs-exact → asked in the client paragraph; `no_retrieval` has no harness case, negative test only; role refusals (010/011/030) expect a behaviour source only, no fixed citation |
| m1 | REQ-3′ | Cites `_render_situation`; the function is `_build_visit_prompt` (`app.py:638`) | A · REQ-3″ |
| m2 | REQ-11′ / Appendix | EVAL-010/011 "gateway 403, no turn" — the test lives in the gateway suite, not the assistant's; count still 30, say where | A · REQ-11″, Appendix rows |

### Spec-review — round 3, 2026-08-24

Owner review of the spec DRAFT of 2026-08-24. Folded in the same session; working-tree
edits only. Call for the round: fold the three headline findings into the requirements
first and re-derive the rows that carry them (SPEC-1, 15, 50, 51, 52, Appendix EVAL-017) —
no row-at-a-time patching. What held: eligibility-assistant-D-20..23 as stated, `_build_visit_prompt` at
`app.py:638` (the earlier 636 was the reviewer's), the eligibility-assistant-D-19 table, SPEC-13/17 split.

| # | anchor | finding | disposition |
|---|---|---|---|
| 1 | REQ-1″ / SPEC-1 | model₁ has no product/state input: nothing in request, facts, or 271 projection carries them and the verdict arrives after model₁, so "model₁ picks product/state" was a guess, not a choice | A · folded → eligibility-assistant-D-25, REQ-1‴ (topic is the choice; payer/product/state pass through), REQ-17′ (product + state menus), SPEC-1, SPEC-12, SPEC-54, SPEC-55 |
| 2 | SPEC-15 vs SPEC-52 vs Appendix EVAL-017 | spend-stop contradiction: `stop` (SPEC-15, EVAL-017) vs "payer result if obtained" (SPEC-52) — under eligibility-assistant-D-18 a stop at model₁ leaves the fallback path calling the payer and rendering `active` with reason `spend_stop` | A · folded → eligibility-assistant-D-26 (stop halts), REQ-16″, Appendix `spend_stop` row + EVAL-017 row, SPEC-15, SPEC-23, SPEC-52 |
| 3 | SPEC-50 / SPEC-51 | agent path gated on wrong intents: "enters the agent path" undefined; "payer exactly once per turn regardless" false on `ask_status` / `other` / `clarify_member_id` (`app.py:845` gates the payer on check/recheck + id) | A · folded → eligibility-assistant-D-27, REQ-16″ (agent path = every model-consulting turn; payer at most once, exactly once on the gated intents), SPEC-50, SPEC-51 |
| N1 | SPEC-3 / REQ-1″ | "no model call ×6 reasons" unpassable for 4 of 6: `validation_reject`, `no_retrieval`, `spend_stop` (model₂), `model_failure` fire after model₁ egressed. Root: "no model in the loop" is the wrong definition | A · folded → eligibility-assistant-D-28, REQ-1‴, SPEC-3 (no model output reaches the reply ×6; zero calls asserted for emergency / cross_patient only) |
| N2 | SPEC-23 / REQ-6″ | no mechanism: `_enforce_budget` is per-request (`llm_max_input_tokens`, `llm_max_cost_per_request_usd`), E-4 ran it 2×. Also no row says what an ADR 0011 §7 slot now buys | A · folded → eligibility-assistant-D-29 (cheap true statement: per-call gate × run_limit=2, no reservation), REQ-6‴, SPEC-23, **new SPEC-56** (one slot per turn, `llm_egress` = any egress) |
| N3 | SPEC-48 / REQ-15′ | asserts a role set the assistant never sees: gateway posts `{message, turns, facts}`, `VisitChatRequest` `extra="forbid"`. Call: drop the pretence, pin `ai.use` grants in `config/roles.yaml` as a config test, gateway = only role gate | A · folded → eligibility-assistant-D-30, eligibility-assistant-D-17 amended, REQ-15″, REQ-11‴ (029/030 in the gateway suite), SPEC-48, Appendix EVAL-029/030 rows |
| N4 | SPEC-37 | cmd cannot reach 32: collect output is `path::name`, the spec's own test names yield ~11 ids | A · folded → eligibility-assistant-D-31, REQ-11‴, SPEC-37 (parametrise on the bracketed case id, grep `EVAL-0[0-9][0-9]`); pinned test ids across the table now carry `[EVAL-n]` |
| m1 | SPEC-24 | `test_ai_llm_client.py` → `tests/test_llm_client.py`; all suites at repo-root `tests/` | A · SPEC-24, spec preamble |
| m2 | SPEC-25 | `gateway.test.ts` does not exist (`frontend/app/lib/` has no tests) | A · SPEC-25 marked new file |
| m3 | SPEC-50 | order omitted the emergency and cross-patient gates and their precedence | A · REQ-16″, SPEC-50 (emergency first per SPEC-45, cross-patient before understand) |
| m4 | eligibility-assistant-D-24 | handoff record must list default-green vs live-gated cases, else "32/32" means two things again | A · eligibility-assistant-D-24, REQ-11‴, SPEC-39 |
| D | eligibility-assistant-D-20..23 | fine as stated | A · RESOLVED |

### Spec-review — round 4, 2026-08-24

Owner review of the r3-folded DRAFT. Folded in the same session; working-tree edits only.
Verified before folding: `test_aggregate_ceiling_bounds_total_paid_calls` exists
(`tests/test_gateway_ai_rate_limit.py:225`); manifest state axis = "United States" except
two California rows, no CHIP-only row; `NO_LOOKUP_STATUSES` (`visit_templates.py:86`).

| # | anchor | finding | disposition |
|---|---|---|---|
| 1 | Appendix EVAL-017 / eligibility-assistant-D-26 / SPEC-23 / SPEC-51 | EVAL-017 mapped onto the wrong mechanism: the client's "spending limit reached mid-question" is ADR 0007's aggregate daily ceiling → gateway 429 before any turn (`_reserve_ai_budget`, existing test); `spend_stop` is `_enforce_budget`, per-request and deterministic for a given payload — a config fault, not a spend event. Consequence: r3's "halt, no payer call" made a check turn that trips at model₁ trip every time — the brownfield lookup dead for that config, the regression REQ-16″ forbids | A · folded → eligibility-assistant-D-26 amended (payer runs regardless of site, verdict persisted not rendered, symmetric), REQ-16‴, SPEC-23 (trip forced by lowering the cap, said so), SPEC-51, SPEC-52, SPEC-15 wording, Appendix `spend_stop` + EVAL-017 rows (429 = the other `stop` surface), SPEC-34 (handoff names both surfaces) |
| 2 | SPEC-9 / SPEC-10 | `unconfirmed` semantics unstated — filters to zero rows or non-filtering decides whether EVAL-023 reaches `refuse_definitive` by emptiness or by SPEC-42's applicability check. Call: non-filtering, mismatch computed in code | A · folded → eligibility-assistant-D-32, REQ-3‴, SPEC-10 (+ `[EVAL-023]` test) |
| 3 | SPEC-1 / SPEC-9 / REQ-3″ | Simplification: tool = `policy_lookup(topic)` only, app binds payer/product/state at execution — removes SPEC-1's mismatch → `validation_reject` path and three enum checks on values the app already holds; still model-invoked | A · folded → eligibility-assistant-D-25 amended, REQ-1⁗, REQ-3‴, REQ-17′ (in place), SPEC-1, SPEC-9, SPEC-55 |
| m | SPEC-33 / REQ-16″ | Mode set pinned at five while the no-freedom short-circuit's mode was deferred to plan — set would change. Call: note it or add the value now | A · value added now → eligibility-assistant-D-33 `no_lookup`, REQ-8‴, REQ-16‴, SPEC-33, SPEC-50, exclusion removed |

### Gate — round 1, 2026-08-25

Fresh-session gate (post-`/clear`; this session authored no part of the plan). Adversarial
read by `drift-gate-agent`, spawning prompt = item name only, no characterization, no branch
named (tree `main`, both artifacts untracked, `Baseline at branch: not yet cut`). Plan
returns to stage 3; spec unchanged. Finding 10 is on a frozen spec row and needs an owner
decision, not a plan edit. Finding 9 is the falsified-claims class (e6 rounds-2–4 lesson):
stage 3 closes it by sweep, and the disposition cell records the sweep's scope.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | Landmines block · SPEC-7/8/9/10/12/20/28/29/30/31 (⚠) | PHI-handling zone entered with no recorded §1 approval: the block itself says "this session is autonomous and could not obtain it — it is the first item at plan review", and the trace-shape entry cites eligibility-assistant-D-41, which is only *proposed*. A zone entered with no recorded approval is a finding, always | A · 2026-08-25 — owner §1 approval of code entry on SPEC-7–10/12/20/28–31, unconditional, given in session at plan review r1 and recorded in the Landmines block's PHI entry; eligibility-assistant-D-41 confirmed the same session (f2), so the trace-shape citation is no longer to a proposal |
| 2 | Plan Status · eligibility-assistant-D-35..45 | Every plan-stage decision is "Proposed … owner sign-off at plan review" — none owner-confirmed/dated as plan-authoring requires, and the Plan Status says sign-off is due *before the gate*; from the two files alone it has not happened | A · 2026-08-25 — eligibility-assistant-D-35..46 confirmed by the owner in session at plan review r1 (each row's marker now reads "confirmed (owner 2026-08-25, plan review r1)"); eligibility-assistant-D-47 added as an owner decision the same session |
| 3 | SPEC-37 · verification 8 · residual-honesty bullet | Counts wrong: after 1a the collect grep yields **2** (`EVAL-031` + `EVAL-023`, which verification 1 itself lists), not 1; after 1c it yields **31**, not 32 — `EVAL-019` (`test_eval_019_payload_allowlist`) lands in PR-2. "reaches 32 only at 1c" is false; 32 is a PR-2 fact | A · 2026-08-25 — verification 8 now reads 2 / 2 / 31 / 32 per PR; residual-honesty bullet restated: 32 is a PR-2 fact, `EVAL-019` named as the PR-2 case |
| 4 | SPEC-36/39/40 · eligibility-assistant-D-43 · verifications 9, 11, 12 | `2 skipped` at 1c is wrong: 1c lands one live test (SPEC-17), SPEC-32's joins in PR-2, so 1c reports `1 skipped` (verification 12's own parenthetical says "1c has 1 test") | A · 2026-08-25 — eligibility-assistant-D-43 corrected (`1 skipped` from 1c, `2 skipped` from PR-2); verifications 9, 11, 12 and the Gate-interactions baseline bullet carry the per-PR term |
| 5 | SPEC-56 · eligibility-assistant-D-40 vs Changes | Self-contradiction: D-40 says "the retained files' test count does not move; every eligibility-assistant addition is a new file", but the change list adds `test_a1_llm_egress_covers_both_calls ×3` to `tests/test_ai_visit_chat.py` (and the spec pins new tests in five other existing files: SPEC-20/24/26/47/48) | A · 2026-08-25 — eligibility-assistant-D-40 amended: no retained test deleted or renamed; the six existing files the spec pins additive tests into are enumerated (SPEC-20/24/26/47/48/56 + the verdict-line parametrise extension found under f8); the 1c change row for `test_ai_visit_chat.py` restated to match |
| 6 | SPEC-47/48 · verification 15 | Break-then-revert cannot redden `test_visit_chat_roles[EVAL-010]`: enforcement is the hard-coded twin `services/gateway/authz.py` (roles.yaml declarative only — `authz.py:4`), so granting `ai.use` to `clinician` in yaml alone reddens only `test_roles_yaml_matches_enforced_map`. The negative check proves the parity test, not SPEC-47's gate | A · 2026-08-25 — verification 15 rewritten: the break is on the enforced twin (`authz.py` `ROLE_CAPABILITIES["clinician"]`, `:49-56`, working tree only, `git checkout --` revert) → `[EVAL-010]` **and** the parity test red; the yaml-only break is kept as the second check with its expected outcome stated honestly (parity red, `[EVAL-010]` green — yaml gates nothing); a SPEC-26 header-drop break added in the same row |
| 7 | SPEC-26 · Changes (1c `services/ai-assistant/app.py`) | Change-list closure gap on the assistant half ("the assistant shall bind the turn's trace and response to it, PR-1 keyless"): the 1c `app.py` row never reads `X-Correlation-Id`, the forwarded body is enumerated without it, `VisitChatRequest` stays `extra="forbid"`, yet `VisitChatResponse.correlation_id` is added — nothing says how the assistant obtains the id before PR-2's distributed-trace headers | A · 2026-08-25 — new **eligibility-assistant-D-46**: header on the hop (gateway adds it to the `_post_checked` headers dict beside `X-Internal-Auth`, `:1150-1156`; assistant reads it as `x-internal-auth` is read, `app.py:187`), UUIDv4-validated or minted, echoed + logged; 1c `app.py` and gateway change rows updated; body and `extra="forbid"` untouched |
| 8 | Gate interactions · SPEC-13/16 · eligibility-assistant-D-39 | Wrong fact: the clinical-vocabulary screen over `visit_templates.CATALOG` is `tests/test_ai_visit_chat.py:721` (borrowing `_CLINICAL_TERMS` from `tests/test_ai_intake_instructions.py`), not `test_ai_intake_instructions.py` itself — `grep visit_templates tests/test_ai_intake_instructions.py` is empty | A · 2026-08-25 — bullet corrected to `test_ai_visit_chat.py::test_catalog_copy_is_clinical_term_free` (`:721`); the read also found `test_every_verdict_line_is_clinical_term_free` (`:727`) is parametrised on a fixed four-status list, so the six new `verdict_line` sentences are only screened once that list is extended — added as a 1c change (under eligibility-assistant-D-40 amended) |
| 9 | Falsified-claims sweep · SPEC-27/31 · eligibility-assistant-D-41 | `docs/phi-logging-policy.md:92` ("The only vendor-egress service") is a second clause PR-2's gateway LangSmith egress falsifies; the sweep names only CLAUDE.md §2 and debt-log D13. Same class as the e6 rounds-2–4 lesson — close by sweep (scope recorded here), not by instance | A · 2026-08-25 — class closed by sweep. **Scope:** `grep -rn -i` for `vendor-egress` / `vendor egress` / `only egress` / `egress path` / `consulted last` / `one span` / `single span` / `exactly one span` / `only vendor` / `only external` / `only outbound` / `wraps one` over `docs/landmines.md`, `docs/debt-log.md`, `docs/phi-logging-policy.md`, `docs/todo.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `docs/runbook.md`, `docs/onboarding-seam-map.md`, `adr/0006`, `adr/0011`, `adr/0016`, `README.md`, `CONTRIBUTING.md`, `tests/README.md`, plus a second pass for one-call / one-trace / `correlation` / `LangSmith span` / `wrap_create` claims, plus in-code markers in `services/gateway/app.py`, `services/ai-assistant/{app,tracing,llm_client,visit_templates,schemas}.py` and `docker-compose.yml`. **Hits (4):** `CLAUDE.md:51`, `docs/debt-log.md:289` (D13), `docs/phi-logging-policy.md:92`, `adr/0011-*.md:83` — the first three get the scoped PR-2 change row (clause replaced, rest verbatim); the ADR line is a decision-as-taken record covered by the eligibility-assistant-D-42 status note. `services/ai-assistant/app.py:385` ("must not reach the vendor while D13") survives eligibility-assistant true and is not touched. `README.md` exempt (TODO-12) |
| 10 | SPEC-39 (check column) | Row carries two mechanisms (`cmd:` … `; gate:` …); the map rule is exactly one. Frozen row — owner decision (split or pick), not a plan edit; reported for the record | A · 2026-08-25 — owner chose **split**: eligibility-assistant-D-47; contract rows SPEC-39a (`cmd:`) / SPEC-39b (`gate:`), Spec Status line carries the amendment; plan cites repointed (eligibility-assistant-D-24/43, runbook change row, residual bullet, verification 12) |
| 11 | SPEC-7/8 · eligibility-assistant-D-35 vs verifications 3–4 | D-35's vendored-path list (`documents/federal/…`, `documents/payers/citation-only/…`, `documents/x12/citation-only/…`, `procedures/…`) omits `documents/synthetic/` — 26 package rows including every reason-table `DOC-SYN-*` — while verifications 3–4 rely on that path. Minor fact gap | A · 2026-08-25 — eligibility-assistant-D-35 path list re-measured from the manifest: federal 41 · payers/citation-only 14 · **synthetic 26** · x12/citation-only 2 · procedures 4 = 87 |
| 12 | SPEC-21/24 · eligibility-assistant-D-37 · 1b requirements | E-4 measured langchain 1.3.16 / langgraph 1.2.11 / langchain-core 1.6.0 **with langsmith 0.11.1**; the plan keeps `langsmith==0.10.5` on a wheel-metadata floor claim — a composition E-4 never ran, against D-37's own "no unmeasured surface" rule. Measure it or name it as a residual. Minor | A · 2026-08-25 — measured: **E-5** (scratch venv, offline) — the four pins + `langsmith==0.10.5` resolve, `create_agent` compiles with the run-limit middleware nodes, 2 model calls / 1 tool call bound holds, tracer module imports; the 1a requirements row cites E-5 instead of the floor claim; the un-run live leg under 0.10.5 is named in the residual-honesty bullet |
| 13 | SPEC-34 · verification 22 | `docker compose exec gateway curl -s` at the loopback records port (literal probe redacted 2026-08-26, #89 CI) is refused inside the gateway container regardless of topology (records-service is `records:8073`, not localhost) — it evidences nothing about "topology unchanged". Minor | A · 2026-08-25 — verification 22 now spot-checks from the **host** (`make ps` shows no host mapping for `records-service`; `localhost:8070/healthz` → 200, `gateway/app.py:179` — probe wording amended 2026-08-26, #89 CI: a literal domain-service host-port probe is itself what `test_compose_topology.py` forbids in docs) and names verification 20's `test_compose_topology.py` as the tracked proof |

Per-SPEC verdicts (round 1): satisfied — SPEC-1, 2, 3, 4, 5, 6, 11, 14, 15, 18, 19, 21, 22,
23, 25, 33, 38, 41, 42, 43, 44, 45, 46, 50, 51, 52, 53, 54, 55 · residual-named — SPEC-17
(live-gated, eligibility-assistant-D-24), SPEC-27 (deterministic turns carry fewer stages), SPEC-32 (gate row,
PR-2 live run), SPEC-35 (gate row, subset named), SPEC-49 (prose-only second patient,
eligibility-assistant-D-44) · FINDING — SPEC-7/8/9/10/12/20/28/29/30/31 (f1; 7/8 also f11), SPEC-13/16 (f8),
SPEC-24 (f12), SPEC-26 (f7), SPEC-34 (f13), SPEC-36 (f4), SPEC-37 (f3), SPEC-39 (f4, f10),
SPEC-40 (f4), SPEC-47/48 (f6), SPEC-56 (f5). Design coverage on the ⚠ rows under f1 is
otherwise sound (mechanisms named, negative tests pinned, both paths covered); the verdict
is FINDING solely because the zone approval is not recorded.

checked: eligibility-assistant-SPEC-1–56 (56 rows) against Decisions eligibility-assistant-D-1..45, Changes (1a/1b/1c/2), Gate
interactions, Landmines block, Verification 1–22 · check map 55/56 single-mechanism (SPEC-39
dual) · ⚠ rows 7, 8, 9, 10, 12, 18, 19, 20, 28, 29, 30, 31, 32, 47, 48, 49 (16): PHI-handling
zone entered on 7–10/12/20/28–31 with §1 approval not recorded; Auth, IDOR, Inline-eligibility
values, Secrets, Domain-internal, Migrations stated not-touched with residuals · freeze scope:
no `DEFERRED` requirements, no SPEC row on a superseded REQ · cites: every SPEC and decision
id the plan cites resolves; test-pinned files all exist · facts verified in tree vs `main`:
`app.py` 14-18/638/757/845/832-835, `llm_client.py` 458 + `_call`/`_adapt`/`extra_body`/
`LLMBudgetExceeded.egressed=False`, gateway 454/1153/`_post_checked(headers=)`,
`visit_templates.py:86`, `test_gateway_ai_rate_limit.py:225`, `test_visit_chat_phi.py:239-240`,
seed users 80-93 + `AETN1224`, `security.py:275-279`, seam-map:14 stale cell, runbook:96
`staff` line, CLAUDE.md:51, debt-log D13 `:239` clause, contract 316 lines, `route.ts`/`proxy()`
(no headers option today), ai-assistant Dockerfile `COPY . .` + `.dockerignore`, compose
context `./services/ai-assistant`, visit memory in the gateway (D-41 consistent),
`Dockerfile.test`/`make test-docker`, CI gitleaks v8.18.4 / `services` import smoke /
400-line cap; SPEC-31 and SPEC-37 cmds return 0 on today's tree (non-vacuous); manifest
(outside repo, read-only): 87 approved / 25 categories, all reason-table ids present · wrong
facts: 2 (f8, f11) + arithmetic (f3, f4) · 13 findings

### Gate — round 2, 2026-08-25

Fresh-session re-gate (post-`/clear`; this session authored no part of the plan). Adversarial
read by `drift-gate-agent`, spawning prompt = item name only, no characterization, no branch
named (tree `main`, both artifacts untracked, `Baseline at branch: not yet cut`). All 13 r1
dispositions hold on the re-read. Plan returns to stage 3; spec unchanged. Finding 1 is a
design gap on a ⚠ row and needs a decision (refuse or record residual), not a cite fix.
Finding 6 is a **repeat of the r1 f9 class** (falsified in-code claims): the r1 sweep scope
listed `services/**` markers but not `tests/`, so stage 3 closes the class by re-sweep over
`tests/` and the frontend, and the disposition cell records that scope — not the two lines.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-49 · eligibility-assistant-D-44 → ticket: turn | Cross-patient rule (a) covers only "two or more ids in the message while the visit holds a confirmed id". The commonest form — visit holds confirmed `AETN1224`, message carries **one** different id — is today's `_derive_intent` "single candidate that CONTRADICTS the confirmed id → ambiguous, ask" (`services/ai-assistant/app.py:565-567`), which the plan retains unchanged: `clarify_member_id` → `ambiguous_id` → `no_lookup`, no reason, no citation. SPEC-49 / REQ-15″ name "a second member id" as `refuse` · `cross_patient` · `DOC-SYN-PRIVACY-FD`. Plan neither refuses that form nor names it as a residual (eligibility-assistant-D-44's residual is the prose-only form). Decide — refuse it, or keep correction semantics and record the residual — and write it down | A · 2026-08-26 — refuse: eligibility-assistant-D-50 (owner, PR #89 codex r1 B2 raised the same call); the `turn` ticket's stage-3 revision carries rule (a) as amended |
| 2 | SPEC-7 · 1a `policy_index.py` / `tests/test_a1_corpus.py::test_manifest_sha_pinned` → ticket: corpus | Self-consistency: the plan's own `policy_corpus/index.json` and `policy_corpus/document-manifest.json` are unlisted files under the corpus path, and both the loader ("an unlisted file raises before any lookup") and the test ("an unlisted file under the corpus path fails") are written to fail on exactly that. Scan scope (manifest path roots `documents/` + `procedures/`, or a named exemption) unstated | A · 2026-08-26 — scope stated in the `corpus` change row and a break-then-revert added (verification 4b): the scan walks the manifest path roots `documents/` and `procedures/` under `policy_corpus/`, where every file must be a manifest row with a matching sha; `document-manifest.json` and `index.json` are the two named root-level non-document files; any other file at the root or under the roots fails (the package's own `documents/.DS_Store` is the vendoring-time example, excluded at copy) |
| 3 | SPEC-31 · 1c `docs/phi-logging-policy.md` change row · verification 21 → ticket: trace | Gate interaction: SPEC-31's check is `grep -c a1 docs/phi-logging-policy.md ≥ 1` (0 today — non-vacuous). The 1c amendment of the "Redis holds `facts.insurance_id`" register row (`:96`, cited to eligibility-assistant-SPEC-20) puts the token `a1` into the register one PR before the trace row exists, so the cmd passes vacuously from 1c on. State that the 1c edit carries no `a1` token, or narrow the observation to the trace-row text | A · 2026-08-26 — class closed by eligibility-assistant-D-51 (check re-keyed to the item token inside the register table at the re-freeze; PR #89 codex r1 M1 raised the rename side of it); the `trace` ticket's verification 21 tracks the amended cell |
| 4 | eligibility-assistant-D-34 · PR-2 `docs/todo.md` change row · Gate interactions (doc-cap bullet) → item | Post-eligibility-assistant-D-47 propagation gaps: eligibility-assistant-D-34 still cites `SPEC-39` (no longer a row) and places it in 1c while 39b (handoff record) lands in PR-2; the `docs/todo.md` row cites "eligibility-assistant-SPEC-32/39"; eligibility-assistant-D-34's 1b scope reads "SPEC-24" while the 1b change list also lands SPEC-30's `test_model_call_not_streamed`; contract file is 317 lines (83 left for four Deliveries), not 316/84 | A · 2026-08-26 — eligibility-assistant-D-55: 39a → `turn`, 39b → `trace`, SPEC-30 → `llm-seam` (test file created there, extended in `trace`); D-34 read through D-55; `docs/todo.md` row repointed to SPEC-32/39a in the `trace` change list; contract measured at 339 lines after the re-freeze (61 left for six Deliveries — table row + evidence by reference each) |
| 5 | Landmines block (PHI entry) · SPEC-7–10/12/20/28–31 → item | §1 approval act is recorded with owner and date but cited to a Findings cell (gate r1 f1) and to eligibility-assistant-D-14/41 as basis — no decision ID records the approval itself. Plan-authoring requires "which owner act approved the entry (cited by decision ID and date)"; one `plan`-tagged register row closes it. Minor — approval exists, register entry does not | A · 2026-08-26 — eligibility-assistant-D-56 records the act (2026-08-25 approval + same-day extension to SPEC-60/61/63 under eligibility-assistant-D-48); every ticket's Landmines block now cites it |
| 6 | eligibility-assistant-D-40 · `tests/test_ai_visit_chat.py:25`, `:392` → ticket: turn | In-code claim the re-seam falsifies: module docstring and a comment say "The LLM is faked at the complete_structured seam"; eligibility-assistant-D-40 moves both rigs to `client.messages.create`. **Repeat of r1 f9 class** (falsified-claims sweep) — r1 scope named `services/**` markers, not `tests/`. Close by sweep over `tests/` + `frontend/` in-code markers on every edited/re-seamed surface; disposition records scope. Minor | A · 2026-08-26 — class closed by sweep. **Scope:** `grep -rn -i` for `complete_structured seam` / `faked at` / `consulted last` / `deterministic build` / `byte-identical` / `single prompt` / `one span` / `single span` / `exactly one span` / `wraps one` / `only egress` / `vendor-egress` / `prompt is exactly` / `no citations` / `the LLM is faked` over `tests/**/*.py`, and `consulted last` / `no citations` / `mode` / `fallback` / `citation` over `frontend/app/**/*.{ts,tsx}`. **Hits falsified by 1c (4):** `tests/test_ai_visit_chat.py:25-26` (module docstring) and `:392` (comment) — rewritten with the re-seam; `tests/test_visit_chat_phi.py:242` — the "byte-identical to what the builder produces" comment is re-pinned with its test under eligibility-assistant-D-40; `tests/test_gateway_ai_rate_limit.py:167` — "before reaching `complete_structured`" reworded to "before reaching the assistant's egress seam" (a `turn` change row). **Survive true:** `tests/test_ai_intake_instructions.py:17` (intake path unchanged, eligibility-assistant-D-5), `tests/test_llm_client.py:647` (the binding still egresses through `invoke_model`), the gateway-seam docstrings (`test_gateway_ai_proxy.py:10`, `test_gateway_ai_chat_controls.py:14`, `test_gateway_proxy_error_contract.py:15`, `test_gateway_intake_proxy.py:12`, `test_gateway_review_queue.py:14`), `test_gateway_ai_chat_controls.py:475` and `test_review_queue.py:282` (unrelated byte-identical claims). **Frontend:** no falsified claim; `frontend/app/assistant/page.test.tsx` "degraded mode" is W3-SPEC-22's `assistant: "ok" \| "degraded" \| "unknown"` health tri-state (`frontend/app/lib/types.ts:102`), a different field from the eligibility-assistant `mode` — the `turn` change row for `types.ts` names both so the collision is deliberate, not accidental |

Per-SPEC verdicts (round 2): satisfied — SPEC-1, 2, 3, 4, 5, 6, 8, 9, 10, 11, 12, 13, 14, 15,
16, 18, 19, 20, 21, 22, 23, 25, 26, 28, 29, 30, 33, 34, 36, 38, 39a, 39b, 40, 41, 42, 43, 44,
45, 46, 47, 48, 50, 51, 52, 53, 54, 55, 56 · residual-named — SPEC-17 (live-gated,
eligibility-assistant-D-24/43), SPEC-24 (langsmith 0.10.5 live leg unmeasured, E-5 residual), SPEC-27
(deterministic turns carry fewer stages), SPEC-32 (gate row, PR-2 live run), SPEC-35 (gate
row, subset named), SPEC-37 (32 is a PR-2 fact, 31 after 1c) · FINDING — SPEC-49 (f1), SPEC-7
(f2), SPEC-31 (f3). f4/f5/f6 are cite/register/marker gaps, not row verdicts.

checked: eligibility-assistant-SPEC-1–56 + 39a/39b (57 rows) against Decisions eligibility-assistant-D-1..47, Evidence E-1..5,
Changes (1a/1b/1c/2), Gate interactions, Landmines block, Verification 1–22 · check map 57/57
single-mechanism · ⚠ rows 7, 8, 9, 10, 12, 18, 19, 20, 28, 29, 30, 31, 32, 47, 48, 49 (16):
PHI-handling zone entered on 7–10/12/20/28–31 with owner approval recorded (2026-08-25, cited
to gate r1 f1 + eligibility-assistant-D-14/41; no register ID — f5); Auth, IDOR, Inline-eligibility values,
Secrets, Domain-internal, Migrations stated not-touched with residuals · freeze scope: no
`DEFERRED` requirements, no SPEC row on a superseded REQ · cites: every decision id resolves;
`SPEC-39` stale in eligibility-assistant-D-34 and the todo row (f4) · change list closes both ways · facts
verified in tree vs `main`: `app.py` 14-18/187/385/555-601/638/757/825-850, `llm_client.py`
192/458/471/566/683/728-730, `tracing.py` `wrap_create` + `_blank`, `config.py`, gateway
179/454/767/881/957/1059/1150-1156/1321, `authz.py:4-8` + clinician frozenset 49-56,
`security.py:275-279`, `visit_templates.py` CATALOG/`:86`/4-status `verdict_line`,
`schemas.py` `extra="forbid"`, `eligibility_client`, `test_ai_visit_chat.py:715` + `:724-725`,
`test_visit_chat_phi.py:240`, `test_gateway_ai_rate_limit.py:225`, `test_gateway_ai_chat_controls.py`,
`test_gateway_authz.py`, `test_tracing.py::_FakeLangSmith`, `conftest.load_module`,
`tests/README.md`, contract pattern (`test_intake_payload_contract.py` + `payload.contract.test.ts`),
`route.ts` → `proxy()` (no headers option), `assistant/page.test.tsx`, seed users 80-93 +
`AETN1224`, ai-assistant Dockerfile + `.dockerignore`, `Dockerfile.test`, CI import smoke /
gitleaks v8.18.4 / 400-line cap, requirements pins, CLAUDE.md:51, debt-log D13 `:289`,
phi-policy `:92` + `:96`, `adr/0011:83`, seam-map:14, runbook:96, TODO-9/10 open, ADR 0019
absent, `.env.example` `LANGSMITH_*` via `git grep` only, no `services/gateway/tracing.py`
today · SPEC-31 and SPEC-37 cmds return 0 on today's tree (non-vacuous) · manifest (outside
repo, read-only): 87/87 approved, 25 categories, federal 41 / synthetic 26 /
payers-citation-only 14 / x12 2 / procedures 4, all reason-table + Appendix ids present, 32
harness ids EVAL-001..032, seven `FIX-NEG-*` · cold falsified-claims sweep (git grep across
registries, ADRs, code, tests): `app.py:834` (covered by plan), `test_ai_visit_chat.py:25/:392`
(f6), no other hit · wrong facts: 1 arithmetic (f4) · 6 findings (1 design gap, 1
self-consistency, 1 gate interaction, 3 minor)
