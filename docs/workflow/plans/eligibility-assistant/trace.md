# eligibility-assistant / trace — ticket plan file (deleted at its merge; contract: docs/workflow/eligibility-assistant.md)

Status: plan GATED 2026-08-28 (**owner override 2026-08-28, in session: stamped in the gate session without a fresh re-run; the drift-gate rule "never stamp with an open finding" set aside by explicit owner decision for this wave; gate r4's two findings accepted under the round-3 rule as named residuals (h) / (i) and dispositioned A in the same act**) (**gate round 3 dispositioned 2026-08-28 under the round-3 rule** — owner adjudicated all four findings 2026-08-28: f1 / f3 / f4 accepted → A, **f2 change-the-spec → eligibility-assistant-D-81 executed at stage 2 the same day** (SPEC-28's allowlist gains the Bedrock call's request id, token counts and cost estimate — the scalars `llm_client._trace_metadata` already attaches; wording only, 66 rows, contract 339 lines) → A: f1 the `traced_stack` fixture re-wraps the rig's scripted `create` after env + fake (wrap-time decision, `tracing.py:136-137`), its chain router forwards `headers`, verification 18 fourth break; f2 the closed key set is twelve keys with four `bedrock`-only scalars and their domains, D-56 re-opened by the widening and re-closed by the owner's call, verification 13 fourth break; f3 the live run drives the two-stack chain (SPEC-27 tree; reference = project/root run id = `X-Correlation-Id`), the no-parent rule stated and pinned by NEW `test_assistant_alone_roots_on_correlation_id`, **+13 → +14**, residual (c) re-worded, dated note on D-78; f4 twelve → **eleven** gateway-loading rigs (`test_eligibility_budget_alignment.py` reads text), the `test_w4_exposure_sweep.py` cite `:55` → `:65`; no owner call open) (**gate round 2 dispositioned 2026-08-27, stage 3** — all five findings **A**: f1 the negative scan's matching rule and the allowlist test's value domains written in (13 of 87 ids contain `CA`; token-exact scan excluding the two manifest-bounded keys); f2 the two `tracing.py` copies made **byte-identical** — both helper sets in one file, revising the r1 f4 choice on the suite-wide bare-name collision (twelve gateway rigs pin no `tracing`; eligibility-assistant-D-41 dated note), parity now three tests, the chain test's fake on both apps' bound copies, baseline +12 → **+13**; f3 TODO-10's `blocked_by` removed in the closing edit (eligibility-assistant-D-79 note closed); f4 known limitations six → seven; f5 the gateway never-stream half stated and pinned; plus one stale sibling cite corrected (`:25`, `corpus.md:27` → `:28`); no new decision, no owner call open) (**gate round 1 dispositioned 2026-08-27, stage 3** — all nine findings, every one **A**; NEW eligibility-assistant-D-78 (the live-run record: name, two runs, the `noncode-merge` stamp commit as the merged-sha carrier, and the env the producing run needs) and **eligibility-assistant-D-79** (TODO-10 closes, TODO-9 does not) — **both confirmed by the owner 2026-08-27 (in session)**: D-78 as written (**two runs, one file**), and D-79 with the owner's call to **amend REQ-9's Notes at stage 1**, **executed 2026-08-27** (Notes narrowed to TODO-10, requirement text untouched, `## Requirements` Status dated; the plan is unchanged either written. eligibility-assistant-D-68 (owner-CONFIRMED 2026-08-27) closes f3 from this side; no new decision was needed for f1, f4, f5, f6, f7 or f9) (**cross-ticket revisions this file has received:** `retrieval-eval` gate r1 f7's disposition, 2026-08-27, rewrote the `services/ai-assistant/tracing.py` change row so the `retrieval` span no longer carries the `LookupRecord` — recorded in `retrieval-eval.md:74`; the **D-59..D-65 propagation sweep 2026-08-27, stage 3** propagated eligibility-assistant-D-61 into the SPEC-28 allowlist row — enumerated site lists in the eligibility-assistant-D-61 / D-62 / D-63 sweep notes in the decision register) (re-sliced 2026-08-25 from the item plan; gate rounds 1–2 on the monolithic plan are in plans/eligibility-assistant.md — carried f3 dispositioned 2026-08-26)
Scope: SPEC rows eligibility-assistant-SPEC-27, -28, -29, -31, -32, -34, -35, -39b, -40 (PR-2 — trace, live proof, handoff; eligibility-assistant-D-3 as amended by D-34, ownership per eligibility-assistant-D-55: SPEC-30 is `llm-seam`'s, 39b is this ticket's)
Depends on: turn

## Plan

Status: GATED 2026-08-28 (owner override, in session — see the header Status line and the closing gate round)

Changes (file level):

- `services/gateway/tracing.py` — NEW, a **byte-identical copy** of `services/ai-assistant/tracing.py` as this ticket leaves it (**gate r2 f2**; eligibility-assistant-D-41 note 2026-08-27 — revises the gate r1 f4 choice of two different files): one module carrying both helper sets — the assistant's `wrap_create` / `tracing_context(parent=headers)` / `span(name, metadata)` and the gateway's `turn_root(correlation_id)` / `span(name)` — each service calling only its own, so whichever copy a bare `import tracing` binds in a test process is correct; `_RUN_NAME`, `_tracing_enabled`, `_langsmith`, `_blank` and the `Client(hide_inputs=_blank, hide_outputs=_blank)` construction (`services/ai-assistant/tracing.py:45`, `:49`, `:56`, `:79`, `:142`) are therefore the same bytes in both, and the module still imports only the standard library at load (`:38-41`), so the keyless CI `services` smoke on the gateway (`ci.yml:58` matrix) is unchanged. Why identical rather than two files with a pin: `tests/conftest.py:16-26`'s `load_module` inserts each service dir at `sys.path[0]`, `llm_client.py:728`'s bare `import tracing` caches the first copy loaded under that name for the whole session and no rig pops it, and **eleven** test files load `services/gateway/app.py` with no `tracing` in their popped-and-pinned sets (`tests/test_gateway_ai_chat_controls.py:25`, `test_gateway_ai_proxy.py:20`, `test_gateway_ai_rate_limit.py:23`, `test_gateway_authz.py:31`, `test_gateway_intake_proxy.py:24`, `test_gateway_login_db_error_phi.py:20`, `test_gateway_proxy_error_contract.py:31`, `test_gateway_redis_auth.py:246`, `test_gateway_review_queue.py:26`, `test_w4_exposure_sweep.py:65`, `test_visit_chat_phi.py:71` — `grep -l "services/gateway/app.py" tests/*.py` matches a twelfth file, `tests/test_eligibility_budget_alignment.py`, but that one reads the source as text (`:405` `read_text()`, `:448`) and loads only `config.py` modules (`:240`, `:296`), binding no `tracing`; **gate r3 f4**, re-derived 2026-08-28) — with two different files every one of them would bind the assistant's copy after any `tests/test_a1_*` file ran, and `proxy_visit_chat`'s `tracing.turn_root` would raise `AttributeError` on every `/ai/visit-chat` case, while a `-k` run that loaded a gateway file first would hand the assistant the gateway's copy and switch its tracing silently off through `llm_client.py:727-732`'s try/except. Identical bytes dissolve the class instead of managing it per rig. **Never-stream, the gateway half (gate r2 f5 — the obligation `llm-seam.md:66` residual (b) hands this ticket):** `turn_root` / `span(name)` are `contextmanager`s that open one `RunTree` with `inputs={}`, attach allowlisted metadata, and end it **once** with `outputs={}` — no `traceable`-decorated generator, no streamed run, no chunked output — so the ADR 0006 hide layer has no streaming bypass on the gateway side either; `services/gateway/app.py` references only `tracing.turn_root` / `tracing.span` (pinned in the `tests/test_a1_trace.py` row below). `services/gateway/requirements.txt` — `langsmith==0.10.5` (today seven pins, `:1-7`, none of them `langsmith`) (eligibility-assistant-SPEC-27 / eligibility-assistant-D-41)
- `services/gateway/app.py` — `proxy_visit_chat` opens the root run, emits `portal_entry` / `gateway_entry` / `persistence`, forwards the distributed-trace headers on `_post_checked` (eligibility-assistant-SPEC-27 / eligibility-assistant-D-41)
- `services/ai-assistant/tracing.py` — `tracing_context(parent=headers)` entry — **with no trace parent in the headers** (a direct caller, or a gateway whose tracing is off) **the assistant opens its own root on the request's correlation id**, so the assistant alone still produces one tree rooted on the correlation id and never five top-level runs (**gate r3 f3**; pinned by `test_assistant_alone_roots_on_correlation_id`, row below) — `span(name, metadata)` helper with the same swallow-on-failure contract, **and — so the gateway copy stays byte-identical (gate r2 f2, eligibility-assistant-D-41 note) — `turn_root(correlation_id)` / `span(name)`, which this service never calls**; `wrap_create`, `_blank`, `_tracing_enabled`, `_langsmith` unchanged; `app.py` / `agent_turn.py` emit `eligibility`, `retrieval`, `validation`, `final_outcome` with allowlisted metadata only (document ids + section labels — the label being the manifest `section_labels` string verbatim as the index row carries it, eligibility-assistant-D-61, not a heading or a parsed fragment — mode, reason, outcome, timings, model id, correlation id); the `retrieval` span carries **only** those allowlisted fields — the `LookupRecord` `retrieval-eval` emits is **not** attached to it, because the record's four resolved axis values are the lookup's query (SPEC-29) and resolved filter values and row counts are not members of SPEC-28's allowlist; the record's emitter is its log line alone (**eligibility-assistant-D-68**, `retrieval-eval` gate r1 f7, which spends SPEC-63's plan-stage "log line or retrieval span" choice; putting it on the span is an owner call on SPEC-28/-29 at stage 2). **The reading the two tests pin (gate r1 f3), stated once here and asserted twice:** `test_eval_019_payload_allowlist[EVAL-019]` is a **key-set and value-domain** assertion, not a substring scan — every captured run is serialised and each metadata key must be a member of the closed set `document_ids` · `section_labels` · `mode` · `reason` · `outcome` · `duration_ms` · `model` · `correlation_id`, plus — **on the `bedrock` run only** — `request_id` · `input_tokens` · `output_tokens` · `estimated_cost_usd` (the SPEC-28 enumeration as amended 2026-08-28, `docs/workflow/eligibility-assistant.md:286`, eligibility-assistant-D-81 — **twelve** keys, one per allowlist member; the four Bedrock-call scalars are exactly what `services/ai-assistant/llm_client.py:618-640` `_trace_metadata` attaches, `wrap_create` being unchanged, and a run of any other name carrying one of them fails the key-set assertion — **gate r3 f2**), **and each value must be in its key's domain (gate r2 f1):** `document_ids` ⊆ the vendored manifest's `document_id` set, `section_labels` == the `section_labels` strings of exactly those ids (both read by the test from `services/ai-assistant/policy_corpus/document-manifest.json`), `mode` ∈ the six eligibility-assistant-D-33 values, `reason` ∈ the six eligibility-assistant-D-19 values or `None`, `outcome` ∈ the ten eligibility-assistant-D-15 values, `duration_ms` a non-negative integer, `model` == `settings.bedrock_model_id` (on the `bedrock` run `_trace_metadata` reads `response.model` and falls back to the setting, `llm_client.py:634`, so the `traced_stack` fixture's scripted responses carry that id or no `model` attribute), `correlation_id` == the request's UUIDv4, and on the `bedrock` run `request_id` a string, `input_tokens` / `output_tokens` non-negative integers and `estimated_cost_usd` a non-negative float, the three usage keys present together or not at all (`_trace_metadata`'s `isinstance(…, int)` guard, `llm_client.py:637-640`; eligibility-assistant-D-81) — with `inputs == {}` and `outputs in (None, {})` from the ADR 0006 two-layer hide; a key outside the set, or a value outside its domain, fails, which is what makes the metadata an allowlist rather than a filter and what bounds the two string-bearing keys to manifest strings. `test_negative_scan_every_path` reads SPEC-29's "query" (`:287`) as **any lookup argument value, enum member or not** — the turn's resolved `topic` / `payer` / `product` / `state` and the clerk's message are planted and asserted absent from every captured payload on all eight paths — so the scan reddens on a resolved filter value even though it is closed vocabulary. **The matching rule (gate r2 f1):** two plant classes. The free-form plants — the clerk message, a patient name / DOB / SSN, a second member id, the held `insurance_id`, the session / user / visit ids, the bearer, a raw payer body, a raw provider error string — are distinctive tokens asserted absent from every manifest id and label as a precondition (so a plant cannot collide) and matched **case-insensitively as substrings over every serialised run in full** (name, metadata, `inputs`, `outputs`, tags, extra). The four resolved axis values cannot be chosen — they are the turn's enum members (`CA`, `humana`, `medicaid_mco`, …) — and the vendored corpus contains them: **13 of the 87 manifest ids contain `CA`** (every `DOC-FED-MEDICAID-*`, `DOC-PAY-ANTHEM-CA-ELIG`, `DOC-ESCALATION-MATRIX`, …), 4 contain `HUMANA`, 3 `AETNA`, 4 `CIGNA`, 6 `MEDICAID`, 4 `MEDICARE`, 1 `CHIP`, and labels carry `Humana Healthcare for Providers`, `Medicare Second…`, `… Eligible for Medicaid?` (package `document-manifest.json`, substring counts 2026-08-27) — so they are matched **token-exact and case-sensitive over every payload string *except* the values under `document_ids` and `section_labels`**, the two keys whose value domain `test_eval_019_payload_allowlist` pins to the manifest's own strings. That split is what lets a correct implementation cite `DOC-PAY-ANTHEM-CA-ELIG` on a `state=CA` turn while a resolved value under any other key, in a run name, in `inputs` or `outputs` still reddens; a resolved value can reach the two excluded keys only inside a manifest string, which the turn's selections never enter, and a fake id or label carrying one fails the value-domain assertion instead (verification 13's third break). Both readings are consistent with eligibility-assistant-D-68 by construction: nothing attaches the four axis values to a span, so neither test has to distinguish a legal one from a leak (eligibility-assistant-SPEC-27/28/29 / eligibility-assistant-D-41, eligibility-assistant-D-68)
- `tests/test_tracing.py` — gateway-copy parity, **three tests** (gate r1 f4, revised at gate r2 f2 — the two files are now byte-identical by design, eligibility-assistant-D-41 note 2026-08-27): `test_tracing_copies_are_byte_identical` reads both files and asserts equal bytes — the strongest form, and a stricter pin than the `test_redaction.py` idiom (whose two copies differ at their docstring: `cmp` 2026-08-27, char 26, line 1), added because here a differing copy is a wrong-copy hazard in every gateway rig, not a drift; plus the two on the shared surface the r1 f4 disposition named, kept because they prove behaviour rather than bytes and load each copy under its own name (`ai_tracing` at `tests/test_tracing.py:27`, and a new `gw_tracing = load_module("services/gateway/tracing.py", "gw_tracing")`) — the `test_redaction.py` idiom compares exported constants and function outputs: `test_copies_have_identical_phi_fields` `tests/test_redaction.py:99`, `test_copies_produce_identical_output` `:153`): `test_tracing_copies_share_the_hide_layer` — the constants idiom — asserts the two modules expose the same `_blank` / `_tracing_enabled` / `_langsmith` names and that **each module's own client-construction site** builds its LangSmith `Client` with `hide_inputs=_blank, hide_outputs=_blank` (captured by injecting a fake through each module's `_langsmith` seam, the rig `tests/test_tracing.py::_FakeLangSmith` `:93` already uses); and `test_tracing_copies_produce_identical_output` — the outputs idiom — asserts `_blank` returns `{}` for both over a table of payload shapes including a PHI-bearing dict, and `_tracing_enabled()` agrees for both over the `LANGSMITH_TRACING` table (unset · `""` · `"false"` · `" TRUE "` · `"true"`; the case/whitespace contract at `services/ai-assistant/tracing.py:49-53`). **What each test pins is stated:** bytes prove the copies cannot drift; the hide-layer and output tests prove what the shared code does; the helpers each service actually calls are proven by this ticket's span tests, not by parity (eligibility-assistant-SPEC-28/29 / eligibility-assistant-D-41)
- `tests/test_a1_trace.py` — extended (created in `llm-seam` with `test_model_call_not_streamed`, eligibility-assistant-D-55). **The module fixture `traced_stack` (gate r3 f1), which every test below takes, in this order:** `monkeypatch.setenv("LANGSMITH_TRACING", "true")`; the capturing fake installed on both apps' bound `tracing._langsmith` (the two `setattr`s described under the first test); then **the rig's scripted queue re-wrapped** — `monkeypatch.setattr(app_mod.llm_client.client.messages, "create", app_mod.tracing.wrap_create(app_mod.llm_client.client.messages.create, app_mod.llm_client._trace_metadata))`, restored at teardown so a later file's rig sees the bare queue — because `wrap_create` decides at **wrap time** (`services/ai-assistant/tracing.py:136-137`, `if not _tracing_enabled(): return create_fn`) and the one import-time wrap (`llm_client.py:727-732`) ran keyless on the real client the rig then replaced (`turn.md:25`), so without the re-wrap no `bedrock` run exists in any test process and the eight-stage test could list at most seven. **Its chain router forwards headers:** the `_fake_post_checked` it installs on the gateway is `ai_client.post(path, json=payload, headers=headers)` — `tests/test_visit_chat_phi.py:209-210`'s router drops `headers`, which would sever the parent link the gateway forwards (`:14`). The tests: `test_single_tree_eight_stages` (capturing fake across the real gateway → assistant chain on the `tests/test_visit_chat_phi.py` two-stack idiom — **the ai stack** is `tests/a1_rig.py`'s module set (the corpus rig's `app_mod`, whose `llm_client` bound the bare `tracing` at its load, `llm_client.py:728`); **the gateway stack** is loaded here with `_GW_PINNED`'s five names (`tests/test_visit_chat_phi.py:71`) popped, pinned and restored, as that file does at `:71-85`; **the fake is installed on both apps' bound `tracing` objects (gate r2 f2)** — `monkeypatch.setattr(app_mod.tracing, "_langsmith", lambda: fake)` and the same on `gw_app.tracing`, the `tests/test_tracing.py::_use_fake` idiom (`:137-139`), one fake object so both processes' runs land in one `sent` list and the tree is asserted as one; the two attributes may be one object or two, and byte-identical copies make either correct (eligibility-assistant-D-41 note), so the test opens with `app_mod.llm_client.tracing is app_mod.tracing` (one object inside the ai stack) and asserts every helper it drives exists on the object each app bound (`turn_root` / `span` on `gw_app.tracing`, `tracing_context` / `span` / `wrap_create` on `app_mod.tracing`); **gateway never-stream leg (gate r2 f5):** each captured gateway run — the root and `portal_entry` / `gateway_entry` / `persistence` — was created once with `inputs == {}` and ended once with `outputs in (None, {})`, no run receives a second end or a streamed chunk, and a string scan of `services/gateway/app.py` finds `tracing.turn_root` / `tracing.span` and no `wrap_create` / `traceable` / `stream` token — the SPEC-30 reference-half idiom (`llm-seam.md:16`) applied to the gateway's caller), `test_eval_019_payload_allowlist[EVAL-019]` (key set **and value domain**, `:15`), `test_negative_scan_every_path` ×8 (success, payer error, provider error, budget stop, validation reject, no retrieval, emergency, cross-patient — PHI planted, every captured payload scanned for the never-list under the matching rule at `:15`), and **`test_assistant_alone_roots_on_correlation_id` (gate r3 f3):** the assistant `TestClient` alone under `traced_stack`, one turn with `X-Correlation-Id` and no trace-parent headers → the captured runs form one tree whose root id is the correlation id, with `eligibility` / `retrieval` / `bedrock` / `validation` / `final_outcome` under it and no run top-level beside it — the `:15` no-parent rule, pinned so a direct caller's tree is never five orphans (eligibility-assistant-SPEC-27/28/29)
- `tests/test_a1_live.py` — `test_live_run_record` (opt-in, `skipif(not A1_LIVE_BEDROCK)` as `turn` created it, eligibility-assistant-D-43): runs one real turn **through the two-stack chain** — the `:17` gateway `TestClient` → assistant `TestClient` idiom with its header-forwarding router, `app_mod.llm_client.client` left real (`turn.md:25`), a real key and real Bedrock — against the **checked-out** sha (`git rev-parse HEAD`), so the tree it produces is the SPEC-27 tree, not an assistant-only subtree (**gate r3 f3**; eligibility-assistant-D-78 note of 2026-08-28); asserts `mode == "real"`; the **trace reference** it prints is `<LangSmith project>/<root run id>`, the root run id being the request's `X-Correlation-Id` (`:42`) — what the operator locates in project `Riverbend`; and **prints** the five record fields — sha, trace reference, date, `mode=real`, model id — to stdout under `-s`; the test never writes a tracked file, the operator copies the printed block into the record, the same hand-copy shape `retrieval-eval` verification 1 uses for SPEC-65 (eligibility-assistant-D-67). The trace reference exists only when the run is traced, so the record-producing leg is `A1_LIVE_BEDROCK=1 LANGSMITH_TRACING=true` — `.env` at rest is `LANGSMITH_TRACING=false` (E-7; template `.env.example:224`), and no `.env*` file is edited (eligibility-assistant-SPEC-32 / eligibility-assistant-D-78)
- `docs/eligibility-assistant-live-run.md` — NEW: the live run record — sha, trace reference, date, `mode=real`, model id. **Renamed from `docs/a1-live-run.md` (gate r1 f2): `a1` is a dead item token in a tracked doc name, the class eligibility-assistant-D-51 closed for the SPEC-31 check cell; `a1` stays only in code identifiers (`tests/test_a1_live.py`, `A1_LIVE_BEDROCK`).** **Two runs, one file (eligibility-assistant-D-78):** the file lands in this PR carrying the **branch-HEAD** run — that is the run SPEC-32's frozen `gate:` cell has the impl gate reproduce, the impl gate being pre-push (`docs/workflow/README.md:46-49`) — and is **re-run once post-merge and updated in the `noncode-merge` stamp commit**, the second of the two post-merge commits the README landing rule defines (`docs/workflow/README.md:236-241`), which rewrites `sha:`, `date:` and `trace reference:` to the merged run's, so "produced against that exact sha" is true of the record on `main`. `## Delivery` names both runs (eligibility-assistant-SPEC-32 / REQ-9 / eligibility-assistant-D-78)
- `docs/runbook.md` — "Eligibility assistant (eligibility-assistant)" section — created here or by `lifecycle`, whichever lands first; this ticket owns its "Demo" subsection: `make up` → `.env` keys → login as `frontdesk` → the four menus + a member id → cited turn → trace location; real/fixture/fallback per component; **known limitations — seven, of which five the contract names** (gate r2 f4: SPEC-34's four + the prose-only second patient are the five contract-named ones; the live-leg pin gap and `turn`'s payer-menu residual make seven): eligibility-assistant-D-17 `staff`, the CA AB 3030 line, HIPAA-eligible ≠ compliant and D13 (SPEC-34's own four, `docs/workflow/eligibility-assistant.md:292`) plus **the prose-only second patient** — a second patient named in prose alone, with no member id and no closed cross-patient phrase, is not detected (eligibility-assistant-D-44; SPEC-49 as narrowed by eligibility-assistant-D-50, contract Exclusions `:326` "recorded residual …, **named in the handoff record**" and REQ-15″ Notes `:139`), which is contract-mandated here and is the one this list omitted (gate r1 f6); and, not contract-mandated but named because this record is the item's single known-limitations home: **the live leg's pin gap** — the LangChain composition has been measured live only on `langsmith==0.11.1` (E-4) while the repo pins `0.10.5` (E-5 "Not measured"), residual (d) below. `turn`'s payer-menu residual (eligibility-assistant-D-36 — the clerk's payer selection is not cross-checked against the member-id prefix catalog) is filed as a `docs/todo.md` row on `turn`'s own PR (`turn.md:47`) and is named in this list too, since the handoff record is where the client reads them; the two `stop` surfaces; the default-green / live-gated case list; handoff owner (eligibility-assistant-D-7); the named live replay subset — `EVAL-001`, `EVAL-007` (conflict), `EVAL-016` (emergency), `EVAL-012` (cross-patient), `EVAL-014-resists`; the "Demo accounts" line "role `staff`" corrected to the seeded per-role accounts (`db/seed/generate_seed.py:80-93`) (eligibility-assistant-SPEC-34/35/39b / eligibility-assistant-D-24, eligibility-assistant-D-47; fix-wrong-claims rule)
- `docs/phi-logging-policy.md` — NEW register row for the trace path (both services, metadata-only, negative-scanned on every path; residual: metadata-only egress to LangSmith cloud from two processes) (eligibility-assistant-SPEC-31)
- `docs/debt-log.md` D13 (`:289` "`/ai/*` is the estate's only vendor-egress path"), `CLAUDE.md` §2 (`:51` "the only vendor-egress path in the estate"), `docs/phi-logging-policy.md` register row `:92` (rationale opener "The only vendor-egress service.") — each clause corrected to: `/ai/*` remains the only *PHI-adjacent model* egress; the gateway now emits metadata-only trace runs; every other sentence in those rows stays verbatim (eligibility-assistant-D-41). `adr/0011-*.md:83` ("ai-assistant is the only egress point") is a decision-as-taken record and is covered by the eligibility-assistant-D-42 status note, not rewritten in place
- `.env.example:214` — the LangSmith block header "`# --- Observability: LangSmith tracing (ai-assistant, see ADR 0006) ---------`" is **corrected in place** to name both services: the gateway container is handed the same shared `.env` (`docker-compose.yml:79-82`, `env_file: .env`), so from this ticket the `LANGSMITH_*` keys below it configure the gateway's root-run client as well as the assistant's. **Comment line only** — no key added, no value changed, no secret (`LANGSMITH_API_KEY=` stays empty, `:229`); the two non-secret `A1_*` template keys in the same file are `turn`'s change row, not this ticket's (falsified-claims sweep, gate r1 f5)
- `tests/test_tracing.py:2` — module docstring "Unit tests for the ai-assistant LangSmith tracing wrapper (tracing.py)" is corrected with the three gateway-copy parity tests that land in the same file (byte identity, hide layer, outputs — gate r2 f2) (falsified-claims sweep, gate r1 f5; the tests are the change row above)
- **Falsified-claims sweep — run 2026-08-27, this session, over every registry the plan-authoring skill names and every in-tree claim about the LangSmith path** (gate r1 f5, class-repeat of item gate r1 f9 / r2 f6 and `turn` gate r1 f5; the class is closed by an enumerated sweep, not a scope phrase). Terms: `langsmith` / `LangSmith` (case-insensitive), `tracing`, `vendor-egress`, `egress point`, `traces LLM`, `TODO|FIXME|XXX|HACK|DEBT` on the edited files. **Falsified, each with its change row above:** `CLAUDE.md:51` · `docs/debt-log.md:289` · `docs/phi-logging-policy.md:92` (the three egress clauses) · `.env.example:214` · `tests/test_tracing.py:2`. **Falsified but not rewritten in place — decisions-as-taken, covered by the eligibility-assistant-D-42 `Extended by ADR 0019 (eligibility-assistant)` status notes `corpus` lands (`corpus.md:28` — `:27` before `corpus` r7 f1's rig-row insert; corrected 2026-08-27 in the `lifecycle` r2 f3 sibling-cite sweep) with ADR 0019's trace section as the current record:** `adr/0011-*.md:83` and `adr/0006-*.md:129-133` decision 4 — of which only the first clause, "`langsmith` is added to the ai-assistant `requirements.txt` **only**", is falsified (by the `services/gateway/requirements.txt` row above); its second clause "No other service traces LLM calls (none other calls an LLM)" stays **true** — the gateway emits spans and calls no LLM — and stays verbatim. **Checked and clean, no hit this diff falsifies:** `docs/landmines.md` (0 hits on any term) · `docs/todo.md` (0 `langsmith` / `tracing` hits; TODO-9 / TODO-10 are their own row and eligibility-assistant-D-79) · `ARCHITECTURE.md` §7 (0) · `docs/runbook.md` (0 — its new section is this ticket's own row) · `README.md` (TODO-12 human-gated exemption) · `tests/README.md:36` "tracing (`test_tracing.py`)" stays true, and this ticket adds **no new test file** to the "AI paths" bullet (`test_a1_trace.py` is `llm-seam`'s, `test_a1_live.py` is `turn`'s, `test_tracing.py` exists), so it carries no `tests/README.md` row where its five siblings do (`llm-seam` gate r3 f3 class, eligibility-assistant-D-60) · `adr/0009-*.md:125-131` (LangSmith tracing "planned … metadata only — model, token counts, cost, latency, request id" for the ai-assistant — still true, mirrored by SPEC-28 as amended 2026-08-28 (eligibility-assistant-D-81), and a decisions-as-taken record) · `services/ai-assistant/tracing.py:1-37` module docstring (describes the assistant copy, which this ticket does not change; the gateway copy carries its own header naming its source). **Records of what happened, verbatim under the doc-archive rule:** `docs/review-loop-metrics.md:376` · `docs/design/w4-multi-agent-assembly.html:247` · `docs/workflow/w1/plan.md:488`, `w1/pr-body.md:68`, `w2/plan.md:37`, `w2/pr-body.md:97`, `w3/plan.md:248`, `w3/pr-body.md:101`, `w3/requirements.md:65`, `w7.md:56`, `w8.md:11`/`:38`/`:42`/`:44`. **In-code debt markers on or near the edited lines:** `grep -rnE "TODO|FIXME|XXX|HACK|DEBT" services/gateway/app.py services/ai-assistant/tracing.py services/ai-assistant/app.py tests/test_tracing.py` → **no match**
- `docs/todo.md` — **TODO-10 closed** (`:53`, "Gated live-LLM integration test; needs a real bearer key") by `tests/test_a1_live.py`, which is exactly that test. **TODO-9 is NOT closed and stays verbatim and OPEN** (`:52`, "Capture a real Bedrock response from a live `complete()` / `complete_structured()` call and pin it as a fixture, so the mocks match a shape AWS actually returned"): the agent path calls neither `complete()` nor `complete_structured()` — its only egress is `llm_client._call` → `client.messages.create` (eligibility-assistant-D-40) — and the endpoint that still calls `complete_structured`, `/ai/intake-instructions`, is out of scope (eligibility-assistant-D-5); capturing a live response body into the tracked tree is refused besides, because this ticket's §1 block fixes the live record at "never a payload" (**eligibility-assistant-D-79**, gate r1 f8; the same decision records that REQ-9's Notes clause "Closes TODO-9 / TODO-10" is over-broad and puts the Notes amendment to the owner). Plus **one new entry** for this ticket's own residual (d): the LangChain composition is measured live only on `langsmith==0.11.1` (E-4) while the repo pins `0.10.5` (E-5 "Not measured"), so the composition's live behaviour under the pinned version rests on two opt-in tests — `src:` `services/ai-assistant/agent_binding.py`, `services/ai-assistant/requirements.txt`. The row follows the file's own line format (`docs/todo.md:18-22`, `- [ ] TODO-n | statement | key: value`) under the registry contract at `:11-16`; the id is allocated **at landing** and re-checked then, never reserved here — the highest id in the register today is **TODO-68** (`grep -o "TODO-[0-9]*" docs/todo.md | sort -u -t- -k2 -n | tail -1`, run 2026-08-27), and `turn` also files rows at its landing (`turn.md:47`), so the collision rule decides the numbers, not this file. **TODO-10's `blocked_by: TODO-9` field is removed in the same closing edit (gate r2 f3; the consequence eligibility-assistant-D-79's executed note raised):** `blocked_by` names a precondition (`docs/todo.md:29`, "a week, a debt id, or another `TODO-n`") and a closed row has none — the gated live test needs no captured fixture, it hits real Bedrock — so the line becomes `- [x] TODO-10 | **CLOSED by \`trace\` (<merge date>) — \`tests/test_a1_live.py\` is the gated live-LLM test (eligibility-assistant-D-79).** Gated live-LLM integration test; needs a real bearer key` in the file's own closed form (`docs/todo.md:81-83`), with no `blocked_by` key and no other key added; TODO-9 (`:52`) stays `[ ]` and verbatim, and nothing else in the file references TODO-10 (`grep -n TODO-10 docs/todo.md` → `:53` only, 2026-08-27) (eligibility-assistant-SPEC-32 / eligibility-assistant-D-79)
- `adr/0019-*.md` — trace section appended (eligibility-assistant-D-42)
- `CLAUDE.md` §6 — baseline line (`:141`, `1317 passed, 19 deselected, 1 xfailed`) updated to the post-PR-2 count, carrying the `2 skipped` term; deselected and xfailed do not move (eligibility-assistant-SPEC-40 — this ticket owns the row, contract `## Delivery`; eligibility-assistant-D-43)

Gate interactions (existing steps that already enforce part of a new check):
- CI `workflow-doc-cap` (400 lines, contract file): `docs/workflow/eligibility-assistant.md` is 339 lines after the 2026-08-26 re-freeze; six Deliveries share 61 lines — each Delivery is its ticket-table row plus evidence by reference (eligibility-assistant-D-55), and the impl gate re-checks the count pre-push.
- `make eval` / `eval/rag/check_drift.py`: scope is `eval/rag/REPORT.md` + `db/seed/seed.sql`; eligibility-assistant touches neither, so the drift gate is unaffected (not re-run).
- `tests/test_compose_topology.py`: no `ports:` change, no new service, no new env template; unaffected.
- Baseline (`CLAUDE.md` §6, `:141` `1317 passed, 19 deselected, 1 xfailed`, measured 2026-08-16): the line predates w4, which moved it **+16** to `1333 passed, 19 deselected, 1 xfailed` (`docs/workflow/w4.md:189` "= baseline 1317 + 16", re-stated at `:213` from the impl gate's own re-run) — the "+17" this bullet carried until 2026-08-27 was wrong (gate r1 f7). The `Baseline at branch:` cell of this ticket's row is measured fresh at its own cut and is the number this PR moves from; `1 skipped` appears at `turn`, `2 skipped` here (eligibility-assistant-D-43).

Verification (runnable, expected output stated; numbering carried from the item plan):
8. `.venv/bin/python -m pytest -m "not integration" --collect-only -q | grep -o 'EVAL-0[0-9][0-9]\(-[a-z]*\)*' | sort -u | wc -l` → **`33` after this ticket** — `EVAL-019` (`test_eval_019_payload_allowlist`, the trace-payload kind) is the one case id this ticket parametrises, taking the count from the `32` `turn` leaves (`turn.md:64`) to 33. The per-earlier-ticket values are their own files' (`corpus.md` → `2`, `retrieval-eval.md:35` → `2`, `turn.md:64` → `32`) and are dropped here (gate r1 f9, eligibility-assistant-D-60 second pass). **SPEC-37 is `turn`'s row** (eligibility-assistant-D-55); the value is pinned here so this ticket's own addition cannot move it silently — the `retrieval-eval.md:35` precedent (eligibility-assistant-D-53 for the subcase-aware pattern; the `-m` deselection does not drop the live tests, they are skips, eligibility-assistant-D-43)
9. `.venv/bin/python -m pytest -m "not integration" -q` → `<Baseline at branch: passed + 14> passed, 19 deselected, 1 xfailed, 2 skipped`. **The 14 (gate r1 f7; 12 → 13 at gate r2 f2; 13 → 14 at gate r3 f3):** `tests/test_tracing.py` × 3 (`test_tracing_copies_are_byte_identical`, `test_tracing_copies_share_the_hide_layer`, `test_tracing_copies_produce_identical_output`) + `tests/test_a1_trace.py` × 11 (`test_single_tree_eight_stages`, `test_eval_019_payload_allowlist[EVAL-019]`, `test_negative_scan_every_path` × 8, `test_assistant_alone_roots_on_correlation_id`). The **second** `skipped` is `test_live_run_record` (`test_eval_014_resists` is `turn`'s first, eligibility-assistant-D-43); deselected and xfailed never move — a moved one is a finding, not a number to update. The claim-worthy run is `make test-docker` with the same numbers (eligibility-assistant-SPEC-40; this ticket)
11. `docker build -q -t riverbend-test:py312 -f Dockerfile.test . && docker run --rm --network none -e AWS_BEARER_TOKEN_BEDROCK= -e LANGSMITH_TRACING= -v "$PWD":/repo -w /repo riverbend-test:py312 pytest -m "not integration" -q` → green with `2 skipped`, no network, no keys. **This ticket's own regression check on its own diff, not a SPEC-36 proof:** SPEC-36 is `turn`'s row (eligibility-assistant-D-55) and `turn.md:67` runs it there; what is re-run here is that the gateway's **new `langsmith` dependency** and both `tracing.py` copies stay import-safe and no-op with no key and no network — the fail-open contract (ADR 0006 decision 3). The `1c` leg is dropped (gate r1 f9, eligibility-assistant-D-60 second pass) (eligibility-assistant-D-22, eligibility-assistant-D-41; this ticket)
12. `.venv/bin/python -m pytest tests/test_a1_live.py -q` → `2 skipped` (default — `turn`'s `test_eval_014_resists` plus this ticket's `test_live_run_record`; the `1c` leg is `turn.md:68`'s and is dropped here, gate r1 f9). Record-producing leg: **`A1_LIVE_BEDROCK=1 LANGSMITH_TRACING=true` `.venv/bin/python -m pytest tests/test_a1_live.py::test_live_run_record -q -s` with the owner's `.env` key loaded** → `1 passed` and the five SPEC-32 fields printed — sha, trace reference, date, `mode=real`, model id — which the operator copies into `docs/eligibility-assistant-live-run.md`. **`LANGSMITH_TRACING` must be set on the command:** `.env` at rest is `false` (E-7; template `.env.example:224`), so the earlier "with the owner's `.env` loaded" alone emitted no run and could produce no trace reference (gate r1 f2, eligibility-assistant-D-78). No `.env*` file is edited (eligibility-assistant-SPEC-32; this ticket)
13. `.venv/bin/python -m pytest tests/test_a1_trace.py -q` → passed; break-then-revert: emit `req.message` as `retrieval` span metadata → `test_negative_scan_every_path` red on all eight paths; revert → green. Second break, the eligibility-assistant-D-68 half (gate r1 f3): attach the `LookupRecord`'s four resolved axis values to the `retrieval` span → `test_eval_019_payload_allowlist[EVAL-019]` red on the key set (`topic` / `payer` / `product` / `state` are not allowlist members) **and** `test_negative_scan_every_path` red on the paths that reach a lookup (the axis values appear under keys the token-exact leg does not exclude); revert → green. Third break (**gate r2 f1** — the exclusion is not a hole): append the turn's resolved payer value to the `retrieval` span's `document_ids` as a fake id → `test_eval_019_payload_allowlist` red on the value domain (`document_ids ⊄` the manifest's ids) while `test_negative_scan_every_path`, which excludes that key's values by rule, stays green — the two tests split the work by design and the split is what this break shows; revert → green. Fourth break (**gate r3 f2**, eligibility-assistant-D-81 — the four admitted keys are `bedrock`-only): put `input_tokens` on the `retrieval` span → `test_eval_019_payload_allowlist` red on the key set for that run name while the `bedrock` run's own `input_tokens` stays legal; revert → green. The **SPEC-12 half is dropped** — `tests/test_a1_prompt_boundary.py` and its `_build_model1_prompt` break are `turn`'s row and `turn.md:69` verbatim (gate r1 f9, eligibility-assistant-D-60 second pass) (eligibility-assistant-SPEC-28/29; this ticket)
18. `.venv/bin/python -m pytest tests/test_a1_trace.py::test_single_tree_eight_stages -q -v` → passed, listing the eight span names under one root whose id equals the request's `X-Correlation-Id`. Break-then-revert (**gate r2 f2**): append one byte to `services/gateway/tracing.py` → `tests/test_tracing.py::test_tracing_copies_are_byte_identical` red; `git checkout -- services/gateway/tracing.py` → green. Second break: install the capturing fake on `app_mod.tracing` only → `test_single_tree_eight_stages` red (the root and the three gateway spans are absent from `sent`); restore the second `monkeypatch.setattr` → green. Third break (**gate r2 f5**): make the gateway `span` a `traceable`-decorated generator that yields the run → the same test red on the string scan and on the once-ended assertion; revert → green. Fourth break (**gate r3 f1**): drop the `traced_stack` re-wrap of the scripted queue's `create` → `test_single_tree_eight_stages` red on `bedrock` (seven names captured, `wrap_create` having returned the bare `create_fn` at import under no key), and `test_eval_019_payload_allowlist` sees no `bedrock` run at all; restore → green. Fifth break (**gate r3 f3**): make `tracing_context` with no parent skip the root → `test_assistant_alone_roots_on_correlation_id` red (five top-level runs, none with the correlation id); revert → green (eligibility-assistant-SPEC-27/28/29; PR-2)
19. `git diff --stat main -- db/ config/ services/gateway/authz.py services/gateway/security.py services/eligibility-service/ services/ai-assistant/eligibility_client.py docker-compose.yml eval/` → empty on every PR (structural exclusion check; every PR)
20. `.venv/bin/python -m pytest tests/test_eligibility_budget_alignment.py tests/test_compose_topology.py tests/test_gateway_proxy_error_contract.py -q` → passed, unchanged counts. **Cited to this ticket's own Landmines not-touched entries, not to SPEC-18** (`turn`'s row, eligibility-assistant-D-55; gate r1 f9): it is the tracked proof of **Inline eligibility call — not touched** (`test_eligibility_budget_alignment.py`, the pinned timeout/breaker values), **Domain services network-internal — not touched** (`test_compose_topology.py`'s allowlist) and **ROI / booking / duplicate-patients / HL7 — not touched** (`test_gateway_proxy_error_contract.py`'s route-set closure case, which reddens on a new gateway route). This ticket adds span emission and header forwarding inside an existing route, so all three must be unchanged (this ticket; every one of its PRs)
21. `grep -c "eligibility-assistant" docs/phi-logging-policy.md` → ≥ 1, every hit inside the register table, the row heading naming the trace path (eligibility-assistant-D-51); `wc -l docs/workflow/eligibility-assistant.md` → ≤ 400 (eligibility-assistant-SPEC-31; PR-2 / every PR)
22. `make up`, then the runbook's eligibility-assistant demo section hand-run from `make up` alone by the impl-gate session: login `frontdesk` / `portal123`, four menus + `AETN1224`, cited reply with mode `real` visible, trace reference located; from the **host**, `make ps` shows `records-service` with no host port mapping (`expose`-only, ADR 0016 — the tracked proof is verification 20's `tests/test_compose_topology.py`, this is the demo-time spot check; a literal domain-service host-port probe is what that test forbids in docs) and `curl -sS localhost:8070/healthz` → 200; observation recorded in Delivery (eligibility-assistant-SPEC-34/35; PR-2 `gate:`)

## Landmines

This block is verbatim for **this ticket's own diff** and no other's (eligibility-assistant-D-60): it names the §1 zones the `trace` change list enters, and a zone another ticket enters is that ticket's block to write, not a citation from here.

- **PHI handling (§1) — entered.** One new PHI-adjacent code path, covering **three** of this ticket's four ⚠ rows — eligibility-assistant-SPEC-28, -29, -31. **The fourth, SPEC-32, is not in this zone (gate r1 f1):** its ⚠ comes from REQ-9, whose gate is *secrets* — "live keys stay in `.env`, never tracked" (`docs/workflow/eligibility-assistant.md:125`), the `docs/landmines.md:77-81` "Secrets in git history" zone — and eligibility-assistant-D-56's enumerated approval set is SPEC-7–10, 12, 20, 28–31 (extended the same day to 60/61/63), which does not name SPEC-32 and was never asked to. SPEC-32's zone entry, and its coverage, are in the **Secrets** bullet below, where the live run and its record are written out:
  **The trace payloads (SPEC-28/29/31, ⚠ REQ-8‴).** One LangSmith tree per turn is emitted from **two processes** — the gateway opens the root run in `proxy_visit_chat` and emits `portal_entry` / `gateway_entry` / `persistence`; the assistant attaches its subtree through the forwarded distributed-trace headers and emits `eligibility`, `retrieval`, `bedrock`, `validation`, `final_outcome` (eligibility-assistant-D-41). The metadata is an **allowlist, not a filter**, and the allowlist is a **closed key set**: `document_ids` · `section_labels` · `mode` · `reason` · `outcome` · `duration_ms` · `model` · `correlation_id`, plus on the `bedrock` run alone `request_id` · `input_tokens` · `output_tokens` · `estimated_cost_usd` — one key per SPEC-28 member as amended 2026-08-28 (`docs/workflow/eligibility-assistant.md:286`, eligibility-assistant-D-81: the scalars `llm_client.py:618-640` already attaches under ADR 0009's metadata-only constraint, each bounded to a scalar domain, none carrying text; **gate r3 f2**) — with `inputs == {}` and `outputs in (None, {})`. The `bedrock` run reaches the tests only through the `traced_stack` fixture's re-wrap of the rig's scripted `create` (`wrap_create` decides at wrap time, `tracing.py:136-137`; **gate r3 f1**), so the run carrying model-call metadata is scanned rather than absent; and an assistant reached with no trace parent roots its own tree on the correlation id (**gate r3 f3**), so a direct caller never produces orphan runs outside the scanned tree. **The `retrieval` span carries exactly those fields and nothing else — the `LookupRecord` `retrieval-eval` emits is NOT attached to it** (**eligibility-assistant-D-68**, owner-confirmed 2026-08-27; it spends SPEC-63's own plan-stage "log line or retrieval span" choice on the log line, for the whole item). The reason is the two frozen rows: the record's four resolved axis values *are* the lookup's query, which SPEC-29 (`:287`) forbids in any emitted run payload on any path, and resolved filter values and row counts are not members of SPEC-28's allowlist. This corrects, on 2026-08-27, the sentence this bullet carried until gate r1 f3 — "the `retrieval` span's metadata is exactly the `LookupRecord` … inside the same allowlist (eligibility-assistant-D-58)" — which asserted a membership the frozen rows do not grant; attaching the record would need a stage-2 amendment of SPEC-28 and SPEC-29 by owner decision, named as the declined alternative in eligibility-assistant-D-68, not a plan-stage move. Three mechanisms hold the boundary and each is separately proven: the **ADR 0006 two-layer hide** (`LANGSMITH_HIDE_INPUTS` / `HIDE_OUTPUTS` plus the in-code `_blank`) runs on **both** clients — the gateway copy is a **byte-identical** per-service copy of `tracing.py` (gate r2 f2, eligibility-assistant-D-41 note — both helper sets in one file, each service calling its own, so whichever copy a test process binds is correct), pinned as bytes by `tests/test_tracing.py::test_tracing_copies_are_byte_identical` and as behaviour by `::test_tracing_copies_share_the_hide_layer` / `::test_tracing_copies_produce_identical_output` (the `tests/test_redaction.py:99` / `:153` idiom, gate r1 f4) — so the layer the PHI argument rests on cannot drift between the two; the payload is **never streamed on either side** — the assistant's binding cannot stream (SPEC-30's structural guarantee, landed in `llm-seam`) and the gateway's `turn_root` / `span(name)` open one `RunTree` with `inputs={}` and end it once with `outputs={}`, no `traceable` generator and no streamed run (gate r2 f5, pinned in `test_single_tree_eight_stages`' gateway leg) — which is what makes the hide un-bypassable in both processes; and the negative scan `tests/test_a1_trace.py::test_negative_scan_every_path` plants PHI (matched case-insensitively as substrings over every serialised run in full) **and the turn's four resolved lookup axis values** — SPEC-29's "query", read as any lookup argument value whether or not it is an enum member — matched **token-exact and case-sensitive over every payload string except the values of the two allowlisted keys `document_ids` / `section_labels`**, whose value domain `::test_eval_019_payload_allowlist[EVAL-019]` bounds to the vendored manifest's own ids and labels (gate r2 f1: 13 of the 87 manifest ids contain `CA` and the payer names sit inside ids and labels, so an unscoped substring scan would redden a correct implementation on a Medicaid or Humana turn; a resolved value can reach those two keys only inside a manifest string, which the turn's selections never enter, and any other placement — a new key, a run name, `inputs`, `outputs` — reddens one test or both) — and walks **every captured payload on all eight paths**: success, payer error, provider error, budget stop, validation reject, no retrieval, emergency, cross-patient, so an error-path trace cannot leak what the happy path does not. `::test_eval_019_payload_allowlist[EVAL-019]` pins the key set itself, and both breaks are verification 13 (emit `req.message` as `retrieval` span metadata; attach the `LookupRecord`'s axis values to the span). A **new `docs/phi-logging-policy.md` register row** records the trace path for both services (SPEC-31) — this ticket does not rely on an existing row.
  Approval of record for entering the zone: **eligibility-assistant-D-56** — the owner's §1 approval of code entry on eligibility-assistant-SPEC-7–10, 12, 20, **28–31**, of which **28, 29 and 31 are this ticket's**, given **2026-08-25** in session at plan review r1 (gate r1 f1), unconditional, and extended by the owner the same day to SPEC-60, 61, 63 (eligibility-assistant-D-48); registered as a decision 2026-08-26. **SPEC-32 is deliberately absent from that set and this bullet does not claim it** (gate r1 f1) — it is the Secrets bullet's row. The approval stands on the owner's freeze of the ⚠ rows REQ-8‴ and SPEC-28–31 (contract `## Spec` Status, 2026-08-24, with SPEC-27's Notes and Exclusions amended in place 2026-08-26 by eligibility-assistant-D-52 to name the gateway as the span emitter) and on eligibility-assistant-D-41 (trace shape, confirmed by the owner 2026-08-25). A change to the mechanisms named here — the two-layer hide on both clients, the never-stream guarantee, the eight-path negative scan, the closed allowlist key set — re-opens it, and this round changed one of them: eligibility-assistant-D-68 narrows what the `retrieval` span may carry, which is a narrowing, not a widening, and was itself owner-confirmed 2026-08-27 in the same act. **Re-opened by gate r3 f2 and re-closed the same day (2026-08-28):** eligibility-assistant-D-81 widens the closed allowlist key set by four Bedrock-call scalars on one run; the owner's call under the round-3 rule on 2026-08-28, executed as the SPEC-28 amendment at stage 2, is the act that re-closes this approval on the key set as now written — nothing stands re-opened.
  **Deliberate defects in reach are preserved.** D13 stays OPEN: the trace adds a *metadata-only* egress, not a model egress, so ADR 0011 constraint 4 is unchanged, there is still no BAA, and the payer answer is still the only coverage source. The D13 clause this ticket falsifies is **corrected in scope, not closed** — "`/ai/*` is the estate's only vendor-egress path" becomes "`/ai/*` remains the only PHI-adjacent *model* egress; the gateway now emits metadata-only trace runs", and every other sentence of the row stays verbatim with the row still OPEN. `adr/0011:83`'s matching line is a decision-as-taken record and is **not** rewritten in place — it is covered by the eligibility-assistant-D-42 status note. The `README.md` HIPAA overstatement (TODO-12) is untouched, human-gated. The D1 intake body-logging and D3 plaintext-PHI-column defects are not touched by any file in this change list. D11 (session↔patient binding) is not in reach — no chart route, no session code.
  **Accepted residuals on this ticket's rows:** the gateway becomes a **second vendor-egress process** — metadata-only, under the same two-layer hide, no PHI — which is a real widening of the estate's egress surface and is accepted rather than avoided, because persistence and the correlation id's birth happen outside the assistant and an assistant-rooted tree could not carry eight stages. It is corrected in `CLAUDE.md` §2, `docs/debt-log.md` D13 and the `docs/phi-logging-policy.md` `:92` rationale opener by this ticket's own change rows. A missing LangSmith key is **fail-open** — no span, turn unaffected (ADR 0006 decision 3) — so a trace outage never becomes a clinical outage; the converse is that absence of a tree is not evidence the turn did not run. The LangSmith key was provisioned 2026-08-26 (eligibility-assistant-D-8 resolved, E-7: auth and one metadata-only run through `tracing.wrap_create` verified against project `Riverbend`, `inputs: {}` / `outputs: None`), and `LANGSMITH_TRACING` stays `false` at rest; SPEC-27–29 are proven **keyless** against a capturing fake, and the live tree is SPEC-32's `gate:` observation only. The rest are in the residual-honesty bullet below.
- **Secrets (§1, `docs/landmines.md:77-81` "Secrets in git history") — no secret file touched; this is where SPEC-32 lives (gate r1 f1).** The zone's rule is "Do not add more secrets, and flag before rotating"; this ticket adds none and rotates none. `.env` and every secret file stay untouched, and no key or token enters the tracked tree: the LangSmith API key and the Bedrock bearer stay in the owner's gitignored `.env` (eligibility-assistant-D-8) and are read from the environment only — including by verification 12's `A1_LIVE_BEDROCK=1 LANGSMITH_TRACING=true` leg, which sets two non-secret switches on the command line and edits no file (eligibility-assistant-D-78). **The live run (SPEC-32, ⚠ REQ-9 — the secrets gate, `docs/workflow/eligibility-assistant.md:125`).** `tests/test_a1_live.py::test_live_run_record` runs one real turn against the checked-out sha and **prints** five fields; the operator copies them into the tracked `docs/eligibility-assistant-live-run.md`. The record carries a sha, a trace reference, a date, `mode=real` and the model id — **never a key, never a payload, never a member id**; the test writes no tracked file, so nothing a live provider returns can reach the tree by machine, and the field list is fixed by this plan so a later hand-edit that adds one is a visible diff. The turn it runs is the same closed-vocabulary turn the default suite exercises, so the live leg adds no new payload class, only a real endpoint. The gateway gains a `langsmith` dependency (`services/gateway/requirements.txt`), which reads its key from the environment and no-ops when it is absent. **One `.env.example` edit, comment-only (gate r1 f5):** the LangSmith block header at `:214` is corrected to name both services now that the gateway is handed the same shared `.env` (`docker-compose.yml:79-82`); no key is added, no value changed, and `LANGSMITH_API_KEY=` stays empty (`:229`). The file's two non-secret `A1_*` template keys remain `turn`'s change row, not this ticket's. CI's `secret-scan` (gitleaks v8.18.4, `--no-git`, `.github/workflows/ci.yml:120-129`) covers everything this ticket adds, including `docs/eligibility-assistant-live-run.md`, and verification 22's demo is run from `make up` with the owner's own `.env`, never a tracked one.
- **Migrations / schema (§1) — not touched.** Nothing under `db/`: no `db/schema.sql` edit, no new `db/migrations/00N_*.sql`, no seed change. This ticket persists nothing — a span is emitted, never stored by us — so no hand-sync obligation arises and `tests/integration/test_schema_upgrade_path.py` is untouched. The one `db/` *reference* is read-only: the runbook's "Demo accounts" line is corrected against the seeded per-role accounts at `db/seed/generate_seed.py:80-93`.
- **Domain services network-internal (§1, ADR 0016) — not touched.** No `ports:` is added, no new service, no `docker-compose.yml` edit; `tests/test_compose_topology.py`'s allowlist is unchanged and is verification 20's tracked proof. The gateway's new outbound LangSmith call is egress from an already host-published service, not a new listener — no domain service gains a port, and verification 22's host-side spot check confirms `records-service` still has no host mapping.
- **Auth / sessions (§1) — not touched.** No line of `services/gateway/authz.py`, `security.py`, `config/roles.yaml` or `EXPECTED_ROUTE_CAPABILITIES` moves; `proxy_visit_chat` keeps `require_capability("ai.use")` exactly as `turn` left it. What this ticket adds inside that route is span emission and header forwarding — observability, not authentication. The never-expiring session (`security.py:275-279`) is untouched, and the `staff` known limitation (eligibility-assistant-D-17) is *documented* in the runbook here, not changed.
- **IDOR on chart reads (§1, D11) — not touched.** No chart or records route is edited and no session is bound to a patient; the D11 xfail stays visible. The correlation id identifies a request, never a patient, so it introduces no new join between a session and a chart.
- **Inline eligibility call (§1, ADR 0010) — not touched.** `services/ai-assistant/eligibility_client.py` and `services/eligibility-service/` are not edited; no timeout, retry or breaker value moves. The `eligibility` span wraps the existing call without changing its budget, and `tests/test_eligibility_budget_alignment.py` is re-run unchanged as verification 20.
- **ROI / disclosures, booking race, duplicate patients, HL7 mapping (§1) — not touched.** No file under `services/roi-service/`, `services/scheduling-service/`, `services/intake-service/` or `services/interop-service/` appears in this change list; no new gateway route is added, so `tests/test_gateway_proxy_error_contract.py`'s route-set closure case is unchanged.
- **Intake registration (§1) — not touched.** No file under `services/intake-service/` is edited; `ConsentKind`, `proxy_intake` on `_post_checked` and `contracts/intake-registration.json` are outside this change list (`docs/landmines.md:87`).
- **Structural exclusion (this ticket).** Nothing changes under `db/`, `config/`, `services/gateway/authz.py`, `services/gateway/security.py`, `services/eligibility-service/`, `services/ai-assistant/eligibility_client.py`, `docker-compose.yml` (no port, no service) or `eval/` — the spec Exclusions and the §1 zones stated not-touched above hold **structurally**, proven by verification 19's empty diff and verification 20's unchanged counts rather than by these sentences.
- **Partial coverage named honestly (residual honesty check).** On this ticket's rows:
  (a) **Five of this ticket's nine rows carry a `gate:` check, and four of the five have no automated proof.** The five are eligibility-assistant-SPEC-32 / 34 / 35 / 39b / 40 (contract `## Spec` check column, `docs/workflow/eligibility-assistant.md:290-299`; the "four" this bullet said until 2026-08-27 omitted SPEC-40 — corrected with gate r1 f7). SPEC-32 / 34 / 35 / 39b are observed only at this ticket's impl gate — the live run (verification 12's `A1_LIVE_BEDROCK=1 LANGSMITH_TRACING=true` leg), the runbook demo hand-run and the host-side topology spot check (verification 22) — and nothing in the default suite reddens if the runbook drifts from the system it describes. SPEC-40 is the exception among the five: its `gate:` is "CI green at the merge sha; counts in Delivery", and CI is machinery, so what stays human there is only the deliberate reading of the counts (verification 9).
  (b) **SPEC-27's eight stages are the agent path's tree, not every turn's.** A deterministic turn produces one tree carrying the stages it actually ran — no `bedrock` span on a zero-call turn, by construction — and the root metadata carries the mode so the shorter tree is readable as intended rather than as a dropped span. `test_single_tree_eight_stages` asserts the full shape on the agent path only.
  (c) **The trace is proven keyless; the live tree is proven once.** SPEC-27–29 are asserted against a capturing fake extending `tests/test_tracing.py::_FakeLangSmith`, so CI proves span names, parents, metadata and the eight-path negative scan **without a key**. What a real LangSmith project renders is SPEC-32's single opt-in turn against one sha — the full SPEC-27 tree, since `test_live_run_record` drives the two-stack chain (`:18`, gate r3 f3) — plus E-7's one metadata-only smoke run. One live turn is not a live regression suite: a rendering or ingestion difference that appears only at volume is not covered.
  (d) **This ticket is where the LangChain composition first runs live under the repo's pins.** E-4's live 12/12 ran on `langsmith==0.11.1`, the repo pins `0.10.5`, and E-5 measured that composition offline only (gate r1 f12, E-5 "Not measured"). `turn`'s SPEC-17 and this ticket's SPEC-32 are the first live exercises; a live-only incompatibility surfaces in an opt-in run, never in the default suite. Both are opt-in, so a red live leg does **not** block a merge — it is reported, not gated.
  (e) **SPEC-31's check counts a token, not a claim.** Verification 21 greps `"eligibility-assistant"` in `docs/phi-logging-policy.md` and requires every hit to be inside the register table with the row heading naming the trace path (eligibility-assistant-D-51, re-keyed in place at the 2026-08-26 re-freeze because the old `a1` token had gone dead). The grep proves a row exists; that the row *describes the egress accurately* is a review judgement, not a test.
  (f) **The item's collect count reaches 33 on this ticket's PR, and that is this ticket's fact even though the row is not.** SPEC-37 is `turn`'s row (eligibility-assistant-D-55) and is proven there; what lands here is `EVAL-019` (`test_eval_019_payload_allowlist`, the trace-payload kind), the one case id this ticket parametrises, taking the count from `turn`'s 32 to 33 (verification 8, eligibility-assistant-D-53). Until this ticket lands, no run of the item's suite can show 33 — a claim of "33/33" before it is false — which is why the value is pinned in this file's own verification rather than left to a sibling's.
  (g) **SPEC-40's baseline gains a second skip.** The default summary carries `2 skipped` from this ticket (`test_eval_014_resists` from `turn` plus `test_live_run_record`), and deselected and xfailed never move (eligibility-assistant-D-43). A moved deselected or xfailed count is a finding, not a number to update.
  (h) **"The `bedrock` run" is shorthand, and there are two of them (gate r4 f1).** The run `wrap_create` produces is named `_RUN_NAME` = `bedrock.invoke_model` (`services/ai-assistant/tracing.py:45`, same bytes in both copies; E-7 recorded it so), and an agent-path turn makes exactly two model calls (SPEC-22), so the tree holds two such runs, each carrying `_trace_metadata`'s keys. Accepted by the owner 2026-08-28 as a named residual rather than a plan rewrite: every "`bedrock`" in the `tracing.py` row (`:15`), the `test_a1_trace.py` row (`:17`), verification 18 (`:42`) and the Landmines span list (`:53`) reads as `_RUN_NAME`; `test_single_tree_eight_stages` asserts eight **distinct stage names** under the root (the two model-call runs are one stage), and `test_eval_019_payload_allowlist` scopes the four D-81 scalars to runs whose name is `_RUN_NAME` — any other name carrying one fails. The impl gate reads the tests against this bullet.
  (i) **Two locators stand corrected here, not in place (gate r4 f2).** `tests/test_tracing.py:28` — not `:27` — is `tracing = load_module("services/ai-assistant/tracing.py", "ai_tracing")`; and register eligibility-assistant-D-41's note of 2026-08-27 still reads "twelve test files … `test_w4_exposure_sweep.py:55`" where `:13` (gate r3 f4) holds the corrected eleven / `:65` — `:13` is the record, and the register note takes its dated correction with the first stage-4 commit that touches the register.

## Findings

Gate rounds start at 1 for this ticket; carried findings: f3 (SPEC-31 — the `turn` `docs/phi-logging-policy.md` amendment made `grep -c a1 …` pass one PR before the trace row exists; **class closed 2026-08-26 by eligibility-assistant-D-51**, re-freeze landed in PR #90 — verification 21 follows the re-keyed cell).

### Gate — round 1, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only, no
characterization received; tree read is `main` with the uncommitted working tree. Run
alongside the `corpus` / `llm-seam` r7 and `turn` / `lifecycle` gates (independent agents).
Round 1 for this ticket file (no prior `checked:` hash; item rounds 1–2 carried f3 only,
dispositioned via eligibility-assistant-D-51) — tags are `new` by the round-1 rule unless a
durable disposition of the same class exists. Round 1 does not fire the round-3 rule. Plan
returns to stage 3; spec unchanged.

| # | anchor | finding | disposition |
|---|---|---|---|
| f1 | eligibility-assistant-SPEC-32 (⚠) · Landmines PHI entry (`trace.md:49`, `:52`) · eligibility-assistant-D-56 | **new** — ⚠ coverage: the block says the PHI-handling entry covers "this ticket's four ⚠ rows (SPEC-28, -29, -31, -32)" and cites D-56 as the approval of record, but D-56's enumerated set is SPEC-7–10, 12, 20, 28–31 (+60/61/63) — SPEC-32 is not in it, and its ⚠ derives from REQ-9's *secrets* gate, not REQ-8‴. Either the owner extends D-56 to SPEC-32, or the block restates SPEC-32's zone as Secrets, not entered, with the Secrets not-touched entry as its coverage (the reading item gate r1/r2 `checked:` lines took). As written the cited decision does not say what the block says |**Disposition 2026-08-27 — A.** The block over-claimed; SPEC-32 is not in the PHI-handling zone and never needed eligibility-assistant-D-56 extended. Taken the second way the finding names. The PHI-handling entry header (`:52`) now reads "**three** of this ticket's four ⚠ rows — SPEC-28, -29, -31" and states why the fourth is elsewhere: SPEC-32's ⚠ comes from REQ-9, whose gate is *secrets* — "live keys stay in `.env`, never tracked" (`docs/workflow/eligibility-assistant.md:125`) — which is the `docs/landmines.md:77-81` "Secrets in git history" zone, whose rule is "Do not add more secrets, and flag before rotating". The approval paragraph (`:54`) now says out loud that **SPEC-32 is deliberately absent from eligibility-assistant-D-56's enumerated set** (SPEC-7–10, 12, 20, 28–31, extended the same day to 60/61/63) and that this bullet does not claim it, so the cited decision and the sentence citing it now say the same thing. The old paragraph **(2) The live run** is **moved out of the PHI bullet and into the Secrets bullet** (`:57`), which is retitled with the zone's own name and its rule, states that no secret file is touched and no secret added, and carries SPEC-32's coverage in full: the printed-not-written record, the fixed five-field list, the `A1_LIVE_BEDROCK=1 LANGSMITH_TRACING=true` leg that sets two non-secret switches on the command line and edits no file, and gitleaks over the tracked record. No owner act is needed: eligibility-assistant-D-56 is unchanged and uncited for SPEC-32. **Sites changed:** `trace.md:52` (PHI-handling header, three rows + why SPEC-32 is not here) · `trace.md:53` (paragraph re-titled, `(1)` numbering dropped — one path, not two) · `trace.md:54` (approval paragraph, SPEC-32's absence stated) · `trace.md` (old paragraph `(2)` deleted from the PHI bullet) · `trace.md:57` (Secrets bullet retitled with the §1 zone name and rule, absorbs the live run in full). |
| f2 | eligibility-assistant-SPEC-32 · rows `trace.md:18`, `:19` · verification 12 (`:37`) · frozen `gate:` cell | **new** — Sequencing and env are unstated. The record is "produced against the exact merged sha" yet sits in this PR's diff, which cannot carry its own squash sha; the only post-merge write the README defines is the plan-file delete commit (`docs/workflow/README.md:215`), and the plan does not name it as the carrier; the impl gate is pre-push, so "reproduced at impl gate" runs against the branch HEAD. Row 18 says the test "writes the record's fields", verification 12 says "printed" — which file, which sha field. And verification 12 runs "with the owner's `.env` loaded", whose at-rest value is `LANGSMITH_TRACING=false` (E-7; `.env.example:224`), so that command as written emits no run and cannot produce the record's trace reference. Minor beside it: `docs/a1-live-run.md` puts the dead `a1` item token (D-51) into a tracked doc name, not a code identifier |**Disposition 2026-08-27 — A.** All four legs accepted and settled as a decision, **eligibility-assistant-D-78** (**PROPOSED** — owner call open, listed in the report) [**owner call made 2026-08-27 (in session): confirmed as written — two runs, one file**; dated bracket per the `corpus` r6 f4 precedent, cell text left as authored]. **(a) Name:** the record becomes `docs/eligibility-assistant-live-run.md` (`:19`) — `a1` is a dead item token in a tracked *doc name*, the class eligibility-assistant-D-51 closed for the SPEC-31 check cell, while `a1` stays by design in code identifiers (`tests/test_a1_live.py`, `A1_LIVE_BEDROCK`). **(b) Sequencing and carrier:** two runs, one file. The **branch-HEAD** run lands in the PR and is what the frozen `gate:` cell has the impl gate reproduce — the impl gate is pre-push (`docs/workflow/README.md:46-49`) — and the **merged-sha** run is made once post-merge and written by the `noncode-merge` **stamp commit**, the *second* of the two post-merge commits the README landing rule defines (`docs/workflow/README.md:236-241`: delete the merged plan file, then stamp `delivery MERGED <merge-sha>` and record the deletion sha in `## Delivery`). The round-1 finding cited `README:215` for "the plan-file delete commit"; re-read this session, `:215` is the ticket-row example inside the Tickets rule and the landing rule's two-commit sentence is at `:236-241` — the delete commit is the first of the two and the stamp commit is the carrier, which is the one the plan now names. **(c) Which file, which field:** row `:18` now says the test **prints** the five fields under `-s` and writes no tracked file (so nothing a live provider returns reaches the tree by machine), the operator copies them in — the hand-copy shape `retrieval-eval` verification 1 already uses for SPEC-65 (eligibility-assistant-D-67) — and the `sha:` field is `git rev-parse HEAD` at the producing run. **(d) Env:** verification 12 (`:40`) now runs the record leg as `A1_LIVE_BEDROCK=1 LANGSMITH_TRACING=true … -s`, because `.env` at rest is `LANGSMITH_TRACING=false` (E-7; template `.env.example:224` read this session) and the round-1 command could emit no run and produce no trace reference. No `.env*` file is edited. **Sites changed:** `trace.md:18` (`tests/test_a1_live.py` row) · `trace.md:19` (record row, renamed + two-runs sequencing) · `trace.md:40` (verification 12) · `trace.md:57` (Secrets bullet, record + env) · `plans/eligibility-assistant.md` (NEW eligibility-assistant-D-78, confirmed by owner 2026-08-27). |
| f3 | eligibility-assistant-SPEC-28 / -29 (⚠) · `services/ai-assistant/tracing.py` row (`trace.md:15`) · Landmines (1) (`:50`) · eligibility-assistant-D-58 | **new** — The `retrieval` span carries the `LookupRecord`'s four resolved axis values (`topic`, `payer`, `product`, `state` — `retrieval-eval.md:13`), which are `lookup`'s query arguments. SPEC-29 forbids a "query" in any emitted run payload (REQ-8‴: "retrieval queries") and SPEC-28's allowlist enumerates no filter values or row counts; the plan asserts "inside the SPEC-28 allowlist" without stating the reading `test_eval_019_payload_allowlist` pins or how `test_negative_scan_every_path` reads "query" (closed enum value vs free text). `retrieval-eval` gate r1 f7 (open, undispositioned) names this ticket's test as the one that must settle it. Write the reading — a decision or the allowlist definition the test pins — so the plan's own scan does not redden on the plan's own span |**Disposition 2026-08-27 — A.** The tension is already resolved in the item's register and the plan now carries the reading rather than an assertion. **eligibility-assistant-D-68** (owner-**CONFIRMED** 2026-08-27, `retrieval-eval` gate r1 f7) spends SPEC-63's own plan-stage "log line or retrieval span" choice on the **log line, for the whole item**, so no emitted run payload carries the `LookupRecord` and SPEC-28/-29 stay true as frozen; that disposition had already swept `trace.md:15`, and this cell closes the same class from this side. What was still missing — the reading the two tests pin — is now written at `:15` and repeated in the Landmines paragraph (`:53`): `test_eval_019_payload_allowlist[EVAL-019]` is a **key-set** assertion, not a substring scan (every captured run serialised; each metadata key a member of the closed set `document_ids` · `section_labels` · `mode` · `reason` · `outcome` · `duration_ms` · `model` · `correlation_id`, one key per SPEC-28 member at `docs/workflow/eligibility-assistant.md:286`, with `inputs == {}` and `outputs in (None, {})`), and `test_negative_scan_every_path` reads SPEC-29's "query" (`:287`) as **any lookup argument value, enum member or not** — the turn's resolved `topic` / `payer` / `product` / `state` are planted alongside the clerk text and asserted absent on all eight paths. The Landmines sentence the finding quotes — "the `retrieval` span's metadata is exactly the `LookupRecord` … inside the same allowlist (eligibility-assistant-D-58)" — is **deleted and replaced** with eligibility-assistant-D-68's reading and the reason the frozen rows do not grant that membership. Self-consistency, walked: with nothing attaching the axis values to a span, neither test has to distinguish a legal filter value from a leak, so the plan's own scan cannot redden on the plan's own span; and verification 13 (`:41`) gains the second break that proves it — attach the record's four axis values to the `retrieval` span → `test_eval_019_payload_allowlist` red on the key set **and** `test_negative_scan_every_path` red on the paths that reach a lookup. Putting the record on the span stays an owner call on SPEC-28/-29 at stage 2, named as the declined alternative in eligibility-assistant-D-68. **Sites changed:** `trace.md:15` (`tracing.py` row, the two tests' reading) · `trace.md:41` (verification 13, second break) · `trace.md:53` (Landmines PHI paragraph, D-58 sentence replaced by the D-68 reading). |
| f4 | eligibility-assistant-SPEC-28 / -29 (⚠) · rows `trace.md:13`, `:15`, `:16` · Landmines (1) (`:50` "parity test … so the two cannot drift") | **new** — Self-consistency: the two `tracing.py` files are different by design (gateway copy adds `turn_root(correlation_id)` / `span(name)`; assistant copy adds a `tracing_context(parent=headers)` entry and `span(name, metadata)`), while the test is named `test_copies_are_identical` and the cited `test_redaction.py` idiom compares exported constants and function outputs (`test_copies_have_identical_phi_fields` `:99`, `test_copies_produce_identical_output` `:153`), not file bytes. What parity pins is unstated, and the PHI argument ("the ADR 0006 two-layer hide runs on both clients") rests on it. Name the compared surface (`_blank`, `_tracing_enabled`, `_langsmith`, `wrap_create`, …) or make the files byte-identical with both helper sets in both |**Disposition 2026-08-27 — A.** Self-consistency accepted: the two files are different by design, so a test named `test_copies_are_identical` could only have passed by not testing what the block claims. The compared surface is now named, and it is the **hide layer** — the only layer the PHI argument rests on. Row `:16` replaces the one test with **two**, on the `tests/test_redaction.py` idiom re-read this session (`test_copies_have_identical_phi_fields` `:99` compares an exported constant; `test_copies_produce_identical_output` `:153` compares function outputs; neither compares bytes): `test_tracing_copies_share_the_hide_layer` asserts both modules expose `_blank` / `_tracing_enabled` / `_langsmith` and that **each module's own client-construction site** builds its LangSmith `Client` with `hide_inputs=_blank, hide_outputs=_blank` (captured through each module's `_langsmith` seam with the fake the file already has, `tests/test_tracing.py::_FakeLangSmith:93`; the assistant's construction site is `services/ai-assistant/tracing.py:142`); `test_tracing_copies_produce_identical_output` asserts `_blank` returns `{}` for both over a table of payload shapes including a PHI-bearing dict, and that `_tracing_enabled()` agrees for both over the `LANGSMITH_TRACING` table — unset · `""` · `"false"` · `" TRUE "` · `"true"` — the case/whitespace contract at `services/ai-assistant/tracing.py:49-53`. What parity does **not** pin is stated in the same row and in the Landmines paragraph: the helper sets differ (`turn_root` / `span(name)` on the gateway, `tracing_context(parent=headers)` / `span(name, metadata)` on the assistant) and are each proven by this ticket's own span tests, so "the two cannot drift" is narrowed to the layer that is actually shared. Self-consistency, walked: neither test can pass on the unmodified tree — `services/gateway/tracing.py` does not exist today (`git ls-files` this session), so both fail at import until the copy lands. Baseline effect is +2, not +1, and verification 9 (`:38`) counts them. **Sites changed:** `trace.md:16` (`tests/test_tracing.py` row, two named tests) · `trace.md:38` (verification 9, the 12 enumerated) · `trace.md:53` (Landmines PHI paragraph, "parity test … cannot drift" narrowed to the hide layer). |
| f5 | Falsified-claims sweep · `trace.md:22` · Landmines (`:53`) | **class-repeat** (item gate r1 f9 and r2 f6 — falsified-claims sweep, each closed by a scoped sweep; `turn` gate r1 f5 the same class) — three hits the gateway `langsmith` row falsifies are unswept: `adr/0006-*.md:129-130` decision 4 "`langsmith` is added to the ai-assistant `requirements.txt` **only**. No other service traces LLM calls" (if the D-42 status note — landed by `corpus`, `corpus.md:27` — is the answer as it is for `adr/0011:83`, say so in the row); `.env.example:214` "Observability: LangSmith tracing (ai-assistant, see ADR 0006)" now that the gateway reads `LANGSMITH_*` from the shared `.env` (`docker-compose.yml:79-82`); `tests/test_tracing.py:2` "Unit tests for the ai-assistant LangSmith tracing wrapper" once the gateway parity test lands in that file. Close by sweep with the scope enumerated |**Disposition 2026-08-27 — A**, class closed by an enumerated sweep, not a scope phrase. **The three named hits, each now a change row or an explicit not-rewritten entry:** `adr/0006-*.md:129-133` decision 4 — only its **first** clause is falsified ("`langsmith` is added to the ai-assistant `requirements.txt` **only**"), by the `services/gateway/requirements.txt` row; its second clause "No other service traces LLM calls (none other calls an LLM)" stays **true** (the gateway emits spans and calls no LLM) and stays verbatim, and the falsified clause is **not rewritten in place** — it is a decision-as-taken covered by the eligibility-assistant-D-42 `Extended by ADR 0019 (eligibility-assistant)` status note `corpus` lands (`corpus.md:27`) with ADR 0019's trace section as the current record, exactly the answer this file already gives for `adr/0011:83`, so the class has one rule and not two. `.env.example:214` — a live template comment, not a record: **corrected in place** by a new change row (`:23`), comment-only, no key added and no value changed (`LANGSMITH_API_KEY=` stays empty at `:229`), because the gateway container is handed the same shared `.env` (`docker-compose.yml:79-82`, read this session). `tests/test_tracing.py:2` — module docstring, **corrected in place** by a new change row (`:24`) with the two parity tests that land in that file. **The sweep, run this session and enumerated with its outcome** (terms: `langsmith` / `LangSmith` case-insensitive, `tracing`, `vendor-egress`, `egress point`, `traces LLM`, and `TODO\|FIXME\|XXX\|HACK\|DEBT` on the edited files): falsified and corrected in place — `CLAUDE.md:51`, `docs/debt-log.md:289`, `docs/phi-logging-policy.md:92`, `.env.example:214`, `tests/test_tracing.py:2`; falsified, not rewritten (D-42 status notes) — `adr/0011-*.md:83`, `adr/0006-*.md:129-133` first clause; **clean, zero hits this diff falsifies** — `docs/landmines.md` (0), `docs/todo.md` (0 `langsmith`/`tracing`; TODO-9/10 are f8's row), `ARCHITECTURE.md` §7 (0), `docs/runbook.md` (0), `README.md` (TODO-12 human-gated exemption), `tests/README.md:36` ("tracing (`test_tracing.py`)" stays true — and this ticket adds **no new test file**, so it carries no `tests/README.md` row where five siblings do), `adr/0009-*.md:125-131` (LangSmith "planned … metadata only" for the ai-assistant — still true, and a record), `services/ai-assistant/tracing.py:1-37` (describes the assistant copy, unchanged here); **records of what happened, verbatim under the doc-archive rule** — `docs/review-loop-metrics.md:376`, `docs/design/w4-multi-agent-assembly.html:247`, `docs/workflow/w1/plan.md:488`, `w1/pr-body.md:68`, `w2/plan.md:37`, `w2/pr-body.md:97`, `w3/plan.md:248`, `w3/pr-body.md:101`, `w3/requirements.md:65`, `w7.md:56`, `w8.md:11`/`:38`/`:42`/`:44`; **in-code markers** — `grep -rnE "TODO\|FIXME\|XXX\|HACK\|DEBT" services/gateway/app.py services/ai-assistant/tracing.py services/ai-assistant/app.py tests/test_tracing.py` → **no match**. The whole list is written into the plan as its own change-list bullet (`:25`) so the next round reads the sweep, not a claim that one ran. Landmines consequence: the Secrets bullet (`:57`) now names the one `.env.example` comment edit and re-states that no key is added. **Sites changed:** `trace.md:22` (egress-clause row, sweep-scope phrase removed) · `trace.md:23` (NEW `.env.example` change row) · `trace.md:24` (NEW `tests/test_tracing.py:2` docstring change row) · `trace.md:25` (NEW enumerated falsified-claims sweep bullet) · `trace.md:57` (Secrets bullet, the comment-only `.env.example` edit). |
| f6 | eligibility-assistant-SPEC-34 · runbook row (`trace.md:20`) | **new** — The handoff record's known-limitations list (D-17 `staff`, AB 3030, HIPAA-eligible, D13) omits the residual the frozen contract says is "named in the handoff record": the prose-only second patient (Spec Exclusions; REQ-15″ Notes; eligibility-assistant-D-44). This ticket's own residual (d) (langsmith 0.10.5 live leg unmeasured, E-5) and `turn`'s payer-menu residual (D-36) are candidates too but not contract-mandated |**Disposition 2026-08-27 — A.** Accepted; the contract-mandated omission is added and two more are named. The runbook row (`:20`) now carries **six** known limitations and says which are contract-mandated: SPEC-34's own four (eligibility-assistant-D-17 `staff`, the CA AB 3030 line, HIPAA-eligible ≠ compliant, D13 — `docs/workflow/eligibility-assistant.md:292`) **plus the prose-only second patient** — a second patient named in prose alone, with no member id and no closed cross-patient phrase, is not detected (eligibility-assistant-D-44; SPEC-49 as narrowed by eligibility-assistant-D-50), which the frozen contract twice says belongs here: Spec Exclusions `:326` "recorded residual …, **named in the handoff record**" and REQ-15″ Notes `:139`. Two more are named although not contract-mandated, because this record is the item's single known-limitations home and the client reads it: this ticket's own residual (d) — the LangChain composition measured live only on `langsmith==0.11.1` (E-4) while the repo pins `0.10.5` (E-5 "Not measured") — and `turn`'s payer-menu residual (eligibility-assistant-D-36, the clerk's payer selection not cross-checked against the member-id prefix catalog), which `turn` files as a `docs/todo.md` row on its own PR (`turn.md:47`). Naming a sibling's residual in this record is not a Landmines cross-citation and does not breach eligibility-assistant-D-60: the Landmines blocks stay each ticket's own, and this is the handoff document a frozen row of *this* ticket requires. **Sites changed:** `trace.md:20` (runbook row, known-limitations list). |
| f7 | eligibility-assistant-SPEC-40 · gate-interactions baseline bullet (`trace.md:31`) · verification 9 (`:35`) | **new** — Wrong fact and missing expected value. "`1317 …` was measured before w4 (`f471b70`, **+17**)" — the record says 1333 = 1317 + **16** (`docs/workflow/w4.md:189`, `:213`). Verification 9 leaves `N` unstated though the change list fixes it: +11 passed (`test_copies_are_identical`, `test_single_tree_eight_stages`, `test_eval_019_payload_allowlist`, `test_negative_scan_every_path` ×8) and +1 skipped (`test_live_run_record`); `llm-seam` 9 states `+ 6` after its r3 f4 |**Disposition 2026-08-27 — A**, both halves. **Wrong fact:** the record says **+16**, not +17 — `docs/workflow/w4.md:189` "= baseline 1317 + 16", re-stated at `:213` from w4's own impl gate re-run, both read this session; the gate-interactions bullet (`:34`) now carries the corrected figure with its locator and names the post-w4 line `1333 passed, 19 deselected, 1 xfailed`. **Missing expected value:** verification 9 (`:38`) now states `<Baseline at branch: passed + 12> passed, 19 deselected, 1 xfailed, 2 skipped` and enumerates the 12 — `tests/test_tracing.py` ×2 (the two parity tests f4 replaced the single one with) plus `tests/test_a1_trace.py` ×10 (`test_single_tree_eight_stages`, `test_eval_019_payload_allowlist[EVAL-019]`, `test_negative_scan_every_path` ×8) — with `test_live_run_record` the **second** skip (the first is `turn`'s `test_eval_014_resists`, eligibility-assistant-D-43). The finding's arithmetic said +11 against the round-1 change list; f4's fix makes the parity row two tests, so the figure is 12 and is stated as a count of named tests rather than a bare N, the `llm-seam.md:35` form. A third fact fell out of the same read and is corrected here: residual (a) (`:67`) said "**Four** of nine rows are `gate:` rows" while the contract's check column gives **five** — SPEC-32 / 34 / 35 / 39b / **40** (`docs/workflow/eligibility-assistant.md:290-299`); the bullet now says five, and separates SPEC-40 (its `gate:` is CI at the merge sha, machinery) from the four with no automated proof. The `CLAUDE.md` §6 change row (`:28`) gains the `:141` locator, the counted text and the `2 skipped` term. **Sites changed:** `trace.md:34` (baseline gate-interaction bullet, +17 → +16 with locator) · `trace.md:38` (verification 9, the 12 enumerated) · `trace.md:28` (`CLAUDE.md` §6 row, locator + counted text) · `trace.md:67` (residual (a), four → five `gate:` rows). |
| f8 | `docs/todo.md` row (`trace.md:23`) · eligibility-assistant-SPEC-32 / -39a | **new** — TODO-9's text (`docs/todo.md:52`: capture a real Bedrock response and pin it as a fixture so the mocks match AWS's shape) is not delivered by the record's five fields nor by `test_live_run_record`; nothing in the change list writes a captured response shape. State what closes it (the live test writing the closed-vocabulary response shape as a rig fixture, or a superseded-closure note). The row also cites SPEC-39a, `turn`'s row |**Disposition 2026-08-27 — A**, and TODO-9 is left open rather than claimed. Re-read this session: `docs/todo.md:52` TODO-9 asks to "Capture a real Bedrock response from a live `complete()` / `complete_structured()` call and pin it as a fixture, so the mocks match a shape AWS actually returned"; `:53` TODO-10 asks for a "Gated live-LLM integration test; needs a real bearer key". **TODO-10 closes** — `tests/test_a1_live.py` is exactly that. **TODO-9 does not**, and the row (`:26`) now says so and leaves the entry verbatim: the agent path calls neither `complete()` nor `complete_structured()` (its only egress is `llm_client._call` → `client.messages.create`, eligibility-assistant-D-40), the one endpoint that still calls `complete_structured` is `/ai/intake-instructions`, out of scope by eligibility-assistant-D-5, and capturing a live response body into the tracked tree is separately refused because this ticket's §1 block fixes the live record at "never a payload" — a payload-derived tracked artifact would re-open the eligibility-assistant-D-56 approval to satisfy a hygiene TODO. Recorded as **eligibility-assistant-D-79** (**PROPOSED** — owner call open, listed in the report) [**owner call made 2026-08-27 (in session): amend REQ-9's Notes at stage 1**, execution pending; dated bracket per the `corpus` r6 f4 precedent, cell text left as authored]: the same row states that REQ-9's Notes clause "Closes `docs/todo.md` TODO-9 / TODO-10" (`docs/workflow/eligibility-assistant.md:125`) is over-broad as written, and that amending a Notes cell of an AGREED requirement is the owner's act — the plan is unchanged either way, since no frozen SPEC row cites TODO-9. The row also gains **one new `docs/todo.md` entry** for this ticket's own residual (d) (the `langsmith==0.10.5` live leg unmeasured, E-5), in the file's own line format (`docs/todo.md:18-22`) under its registry contract (`:11-16`), with the id allocated at landing — highest today is **TODO-68** (`grep -o "TODO-[0-9]*" docs/todo.md \| sort -u -t- -k2 -n \| tail -1`, run this session). The stray `eligibility-assistant-SPEC-39a` cite the finding notes is **dropped** — 39a is `turn`'s row (eligibility-assistant-D-55) — which is the f9 class, swept there. **Sites changed:** `trace.md:26` (`docs/todo.md` row rewritten: TODO-10 only, TODO-9 open, new residual row, SPEC-39a cite dropped) · `plans/eligibility-assistant.md` (NEW eligibility-assistant-D-79, confirmed by owner 2026-08-27). |
| f9 | verification 13 (`trace.md:38`, SPEC-12 half tagged "1c") · verification 20 (`:41`, SPEC-18) | **class-repeat** (eligibility-assistant-D-60 second pass, closing `corpus` gate r1 f2 / `llm-seam` gate r1 f2 as one class: "a check on another ticket's rows is dropped" — it dropped `llm-seam` 20 (SPEC-18) and 21, but swept `trace` only for the three citation forms) — both checks serve `turn`'s rows. Keep 20 if it is the Inline-eligibility not-touched proof (`:60` cites it), cited to that entry rather than SPEC-18; drop the SPEC-12 half of 13 (it is `turn.md:67` verbatim) |**Disposition 2026-08-27 — A**, class closed with every site enumerated. **The two named instances.** Verification 13's **SPEC-12 half is dropped** (`:41`) — `tests/test_a1_prompt_boundary.py` and the `_build_model1_prompt` break are `turn`'s row and stand verbatim at `turn.md:69`; what remains is this ticket's own SPEC-28/-29 break, plus the new eligibility-assistant-D-68 break f3 added. Verification 20 is **kept and re-cited** (`:44`): it is the tracked proof of three of this ticket's own Landmines not-touched entries — Inline eligibility call (`test_eligibility_budget_alignment.py`, cited at `:62`), Domain services network-internal (`test_compose_topology.py`, `:59`) and ROI / booking / duplicate-patients / HL7 (`test_gateway_proxy_error_contract.py`'s route-set closure case, `:63`) — so the SPEC-18 tag is replaced by those three entries; SPEC-18 is `turn`'s row (eligibility-assistant-D-55) and `turn.md:76` keeps its own copy. **The sweep, every site listed with its outcome (verifications, gate interactions, change rows).** *Verifications:* **8** — kept and re-cited, `retrieval-eval.md:35` precedent (SPEC-37 is `turn`'s row; the value is pinned here so this ticket's own `EVAL-019` cannot move it silently), and the per-earlier-ticket legs `2 after 1a / 2 after 1b / 32 after 1c` are **dropped** as other tickets' values; **9** — SPEC-40, this ticket's, kept; **11** — the `1c` leg **dropped** and the SPEC-36 tag replaced (SPEC-36 is `turn`'s, run at `turn.md:67`); the run is kept as this ticket's own regression check that the gateway's new `langsmith` dependency and both `tracing.py` copies stay import-safe and no-op with no key and no network; **12** — the `1c` legs and the SPEC-17 / SPEC-39a tags **dropped** (both `turn`'s), SPEC-32 kept, env fixed under f2; **13** — SPEC-12 half **dropped** as above; **18** — SPEC-27, kept; **19** — structural exclusion, this ticket's, kept; **20** — re-cited as above; **21** — SPEC-31, kept; **22** — SPEC-34/35, kept (its reference to verification 20's `test_compose_topology.py` still resolves, since 20 keeps that test). *Gate interactions:* **31** `workflow-doc-cap` — item-wide, no SPEC tag, kept; **32** `make eval` — item-wide, kept; **33** `test_compose_topology.py` — this ticket's own, kept; **34** baseline — SPEC-40, this ticket's, kept (figure corrected under f7). *Change rows, checked for a SPEC tag outside this `Scope:` line:* `:13` SPEC-27 · `:14` SPEC-27 · `:15` SPEC-27/28/29 · `:16` SPEC-28/29 · `:17` SPEC-27/28/29 · `:18` SPEC-32 · `:19` SPEC-32 · `:20` SPEC-34/35/39b · `:21` SPEC-31 · `:22` no SPEC tag · `:23` no SPEC tag · `:24` no SPEC tag · `:25` no SPEC tag · `:26` SPEC-32 (the SPEC-39a tag **dropped**, f8) · `:27` D-42 only · `:28` SPEC-40 — **all clean after that one removal**, no other row serves a sibling's row. **Sites changed:** `trace.md:37` (verification 8, re-cited and per-ticket legs dropped) · `trace.md:39` (verification 11, `1c` leg and SPEC-36 tag dropped) · `trace.md:40` (verification 12, `1c` legs and SPEC-17/39a tags dropped) · `trace.md:41` (verification 13, SPEC-12 half dropped) · `trace.md:44` (verification 20, re-cited to the three Landmines entries) · `trace.md:26` (`docs/todo.md` row, SPEC-39a tag dropped). |

**Line-cite re-point (stage 3, 2026-08-27).** This round's edits inserted three change rows (`.env.example:214`, the `tests/test_tracing.py:2` docstring, the enumerated falsified-claims sweep) and deleted one Landmines paragraph, shifting this file's Verification block by +3 and its `docs/todo.md` row from `:23` to `:26`. The five sibling files and the item plan were grepped for `trace.md:<n>` and **today's** cells re-pointed, history cells left as read at their round. **Re-pointed:** `lifecycle.md:38`, `:39`, `:50`, `:52` (`trace.md:34` / `:35` → `:37` / `:38`, verifications 8 and 9) · `turn.md:47`, `:49` (`trace.md:23` → `:26`, the `docs/todo.md` row). **Checked, unchanged — the cited row did not move:** `plans/eligibility-assistant.md:74` and `retrieval-eval.md:74` (`trace.md:15`) · `lifecycle.md:28` and `:111` (`trace.md:20`) · `llm-seam.md:183` and `:405` (`trace.md:17`) · `llm-seam.md:175` (`trace.md:1`) · `turn.md:156` (`trace.md:4`). **Left as history — round cells of their own gates, never renumbered:** `lifecycle.md:103`, `:104` · `turn.md:124`, `:151` · `llm-seam.md:344`.

Per-SPEC verdicts: SPEC-27 residual-named (deterministic turns carry fewer stages, residual (b); keyless proof, (c); design verified — gateway persists visit memory `services/gateway/app.py:1192`, `_post_checked` takes `headers=` `:1150-1156`, `test_visit_chat_phi.py` runs the real gateway → assistant chain, langsmith 0.10.5 has `tracing_context(parent=…)` + `RunTree.to_headers`, the gateway container receives `.env` `docker-compose.yml:79`); SPEC-28 FINDING (f3, f4); SPEC-29 FINDING (f3, f4); SPEC-31 satisfied (register row planned; cmd returns 0 today, non-vacuous; residual (e) named); SPEC-32 FINDING (f1, f2); SPEC-34 FINDING (f6); SPEC-35 residual-named (gate row; subset named — `EVAL-007` conflict, `EVAL-016` emergency; residual (a)); SPEC-39b residual-named (gate row; list in the runbook section; residual (a)); SPEC-40 FINDING (f7).

checked: eligibility-assistant-SPEC-27, 28, 29, 31, 32, 34, 35, 39b, 40 (9 ids, the `trace` `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, `trace.md` Changes (13 rows) / Gate interactions (4) / Verification 8, 9, 11–13, 18–22 / Landmines / Findings, the five sibling ticket files' `Scope:` + `Depends on:` lines and the contract `## Delivery` table · check map 66/66 single-mechanism (9/9 in set: `test:` 27/28/29 · `cmd:` 31 · `gate:` 32/34/35/39b/40) · ⚠ rows in set: 28, 29, 31, 32 (4) — PHI-handling entered on 28/29/31 with eligibility-assistant-D-56 cited (owner 2026-08-25, registered 2026-08-26); 32 cited to D-56, which does not name it (f1); Secrets, Migrations, Domain-internal, Auth, IDOR, Inline-eligibility, ROI/HL7, Intake stated not-touched with verification 19/20 as structural proof · freeze scope: no `DEFERRED → <item>` requirement, no row on a superseded REQ · ownership: 66/66 frozen rows each owned exactly once across the six `Scope:` lines and the `## Delivery` table (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated) — no orphan, no double owner; `Scope:` = table row · cites: every SPEC, decision and E-n id the plan cites resolves · facts verified in tree vs `main`: no `services/gateway/tracing.py` today; `langsmith==0.10.5` at `services/ai-assistant/requirements.txt:9` + `requirements-dev.txt:13`; gateway `proxy_visit_chat` `:1059`, `_post_checked(headers=)` `:1150-1156`, `visit_memory_save` `:1192`, `/healthz` `:179`; `tests/test_tracing.py::_FakeLangSmith` `:93`; `test_visit_chat_phi.py:4`, `:70-77` (real chain); venv langsmith 0.10.5 `tracing_context(parent: RunTree|Mapping|str)`, `RunTree.to_headers`/`from_headers`; `docker-compose.yml:79-82` gateway `env_file: .env`; seed users `db/seed/generate_seed.py:80-92`, `DEMO_PASSWORD = "portal123"` `:56`, `AETN1224` `db/seed/seed.sql:456`; runbook `:94-97` "role `staff`" line, no eligibility section today; `CLAUDE.md:51`, `docs/debt-log.md:289`, `docs/phi-logging-policy.md:92`, `adr/0011:83` clauses as quoted; ADR 0006 decisions 1/3 (two-layer hide, fail-open) and `:129-130` decision 4 (f5); `.env.example:214`, `:224`, `:233-234`; `corpus.md:27` lands ADR 0019 + the 0006/0011 status notes; `llm-seam.md:16` creates `tests/test_a1_trace.py`; `turn.md:21` sends `X-Portal-Sent-At`, `:35` creates `tests/test_a1_live.py`; `lifecycle.md:19` matches "whichever lands first"; `retrieval-eval.md:13`, `:41` LookupRecord fields + log line; `.github/workflows/ci.yml:120-129` gitleaks v8.18.4 `--no-git`, `workflow-doc-cap` 400 `:135-150`, `services` matrix includes `gateway` `:58`; contract 339 lines; `docs/landmines.md:87`; `security.py:277-281` (`create_session`, no-TTL note `:279`); `EXPECTED_ROUTE_CAPABILITIES` `tests/test_gateway_authz.py:105`; `Dockerfile.test`, `tests/integration/test_schema_upgrade_path.py`, `tests/test_gateway_ai_chat_controls.py` exist; Makefile `up`/`ps`/`eval`/`test-docker`; `eval/rag/check_drift.py` scope REPORT.md + seed.sql; TODO-9/10 open (`docs/todo.md:52-53`); `docs/workflow/w4.md:189`, `:213` = 1317 + 16 (f7); `tests/test_redaction.py:99`, `:153` parity idiom (f4); SPEC-31 cmd → 0 on today's tree (non-vacuous) · wrong facts: 1 (f7 "+17") · plan-text hash read: `git hash-object docs/workflow/plans/eligibility-assistant/trace.md` = `d6f26e381df60d4f73767ef367791cb38dd79e56`; gate round 1 for this ticket file (no previous `checked:` line; carried item-round f3 dispositioned by eligibility-assistant-D-51) · 9 findings · **origin: 7 new · 0 pre-existing · 2 class-repeat**

### Gate — round 2, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names only (branch
`main` @ `762f0fd` with the uncommitted working tree), no characterization received.
**Process change this wave (owner, 2026-08-27, cost):** two agents grouped by mechanism
instead of six — this ticket read in one context with `llm-seam` and `turn`. Origin tags
derived by the agent from the record — round 1's `checked:` plan-text hash (`d6f26e38…`),
the `Sites changed:` lists on all nine r1 dispositions, plus the cross-ticket edit to `:15`
recorded in `retrieval-eval.md:74` and D-61's sweep note. Four findings are `class-repeat`
against classes dispositioned in this ticket's r1 or a sibling's rounds; the agent also
recorded the same-ticket-only fallback derivation (f1 new · f2 pre-existing · f3 new ·
f4 new · f5 new). Round 2: the round-3 rule does not apply. f4 and f5 are minor. Plan
returns to stage 3; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-28 / SPEC-29 (⚠) · `services/ai-assistant/tracing.py` row (`trace.md:15`, the `test_negative_scan_every_path` reading) · verification 13 (`:41`) · Landmines PHI paragraph (`:53`) | **class-repeat** (r1 f4 — self-consistency: the plan's own named test cannot do what the block claims; the item-wide class `llm-seam` r3 f1 / r7 f1; same-ticket-only fallback → new, all three anchors on r1 f3's `Sites changed:` list). The scan "plants the turn's four resolved lookup axis values … asserted absent from every captured payload on all eight paths", but on the success path the **allowlisted** `document_ids` and `section_labels` legitimately contain those closed values: 13 of the 87 manifest ids contain `CA` (every `DOC-FED-MEDICAID-*`, `DOC-FED-GET-STARTED-MEDICARE`, `DOC-PAY-ANTHEM-CA-ELIG` literally `-CA-`), `DOC-PAY-HUMANA-*` ×3 / `DOC-PAY-AETNA-*` ×2 / `DOC-PAY-CIGNA-*` ×3 / `DOC-FED-CHIP` carry the payer or product value, and labels carry `Humana Healthcare for Providers`, `Medicare Second…` ×10, `… Eligible for Medicaid?` (package `document-manifest.json`, 87 rows joined 2026-08-27). As written a correct implementation reddens on a state-`CA` Medicaid/Anthem turn or a `humana` / `aetna` / `cigna` / `medicare` turn with a payer document cited, or the test passes only by a matching rule (case-sensitive, token-exact, allowlist-key-excluded) the plan does not state. State the matching rule and the planted values such that allowlist members cannot collide — or narrow the reading to the clerk text + non-allowlisted identifiers and record the axis-value leg as covered by the key-set test alone | **Disposition 2026-08-27 — A.** The collision is real and re-derived from the package this session: 13 of the 87 manifest ids contain `CA` (`DOC-FED-MEDICAID-*` ×5, `DOC-FED-GET-STARTED-MEDICARE`, `DOC-FED-MEDICARE-COSTS`, `DOC-FED-SSA-MEDICARE`, `DOC-PAY-ANTHEM-CA-ELIG`, `DOC-SYN-REVERIFICATION-TRIGGERS`, `DOC-SYN-MEDICARE-TRAINING-SUMMARY`, `DOC-SYN-MEDICAID-MC-TRAINING`, `DOC-ESCALATION-MATRIX`), and `HUMANA` ×4, `AETNA` ×3, `CIGNA` ×4, `MEDICAID` ×6, `MEDICARE` ×4, `CHIP` ×1 appear in ids, with `Humana Healthcare for Providers`, `Medicare Second…` (×3 rows) and `… Eligible for Medicaid?` in labels (`document-manifest.json`, substring counts 2026-08-27). The matching rule is now stated at `:15` and repeated in the Landmines paragraph (`:53`), and it takes **both** of the finding's options at once, split by test: (a) `test_eval_019_payload_allowlist[EVAL-019]` becomes a **key-set and value-domain** assertion — `document_ids` ⊆ the manifest's ids and `section_labels` == those ids' label strings (read from the vendored manifest), `mode` / `reason` / `outcome` in their closed sets, `model` == `settings.bedrock_model_id`, `correlation_id` the request's UUIDv4, `duration_ms` an integer — so the two string-bearing allowlisted keys can carry only manifest strings; (b) `test_negative_scan_every_path` matches the free-form plants (clerk text, name / DOB / SSN, a second member id, the held id, session / user / visit ids, the bearer, a raw payer body, a raw provider error) case-insensitively as substrings over every serialised run in full, after asserting each plant absent from every manifest id and label so it cannot collide, and matches the four resolved axis values **token-exact, case-sensitive, over every payload string except the values under `document_ids` / `section_labels`** — the keys (a) bounds. Self-consistency walked: a correct implementation citing `DOC-PAY-ANTHEM-CA-ELIG` on a `state=CA` Anthem turn passes both (the id is a manifest id under an excluded key); a resolved value under `metadata.state`, in a run name, in `inputs` or `outputs` fails (b) and, for a new key, (a); a resolved value smuggled as a fake id under `document_ids` fails (a)'s value domain while (b) stays green by rule — verification 13's new third break shows exactly that split, and its second break still reddens both tests because `topic` / `payer` / `product` / `state` keys are neither allowlisted nor excluded. Class (r1 f4 — a named test that cannot do what the block claims) closed with an enumerated walk of every test this file names, each asked whether it can pass on a correct implementation and fail on the defect it is for: `test_single_tree_eight_stages` (eight named spans under one root — reachable only with the fake on both processes' copies, f2; holds after f2); `test_eval_019_payload_allowlist` (key set + value domain — **changed**); `test_negative_scan_every_path` ×8 (matching rule — **changed**); `test_tracing_copies_are_byte_identical` / `_share_the_hide_layer` / `_produce_identical_output` (bytes, the hide layer through each copy's `_langsmith` seam, `_blank` / `_tracing_enabled` outputs — f2, hold); `test_live_run_record` (prints five fields under `-s`, writes nothing — holds); `test_model_call_not_streamed` (`llm-seam`'s, unchanged). **Sites changed:** `trace.md:15` (`tracing.py` row — value domains and the matching rule); `trace.md:41` (verification 13 — second break restated, third break added); `trace.md:53` (Landmines PHI paragraph — the rule); `trace.md:3` (Status line). |
| 2 | SPEC-27 (and 28/29 — the scan walks both processes' payloads) · `tests/test_a1_trace.py` row (`trace.md:17`, "the real gateway → assistant chain, the `test_visit_chat_phi.py` rig") · `services/gateway/tracing.py` row (`:13`) | **class-repeat** (`llm-seam` r3 f1 / `corpus` r7 f1 → D-66, cross-ticket: two same-named modules in one process, the pin unstated; same-ticket-only reading → pre-existing, `:17` on no list). This ticket creates a **second `tracing` module under the same bare name** in a second service, and the rig the test rests on pins neither: `tests/test_visit_chat_phi.py` `_AI_PINNED` (`:40-50`: config, logging_config, schemas, llm_client, templates, visit_templates, breaker, eligibility_client) and `_GW_PINNED` (`:70-77`: config, logging_config, db, models, security) omit `tracing`, while `llm_client.py:728` `import tracing` caches the ai-assistant copy under the bare name at the ai-stack load and it is never popped. The gateway `app.py`'s new `import tracing` then resolves to the assistant copy — no `turn_root` / `span(name)` — so the in-process chain the eight-stage test needs cannot be built without a per-stack pin plus identity assertions (`gw.tracing is <gateway copy>`, `ai_app.llm_client.tracing is <assistant copy>`), and the capturing fake must be installed on **both** copies' `_langsmith` seams. State the pin (D-66 shape) and the two-seam fake in the row, with the break (drop the gateway pin → first assertion red) | **Disposition 2026-08-27 — A**, with a different mechanism from the one the finding sketches, because the class is wider than the one test. Re-derived this session: `tests/conftest.py:16-26` `load_module` inserts each service dir at **`sys.path[0]`** and never pops a bare name; `llm_client.py:728` `import tracing` caches the assistant copy under the bare name at the first ai-stack load; `tests/test_visit_chat_phi.py`'s `_AI_PINNED` (`:34-43`, not `:40-50`) and `_GW_PINNED` (`:71`, not `:70-77`) omit `tracing`, as the gate read; and — the part the finding did not reach — **twelve** test files load `services/gateway/app.py` with pinned sets of `config` / `logging_config` / `db` / `models` / `security` (+ `authz` in four) and none pins `tracing` (`grep -l` 2026-08-27, listed in `:13`). With two *different* `tracing.py` files, every one of those rigs would bind the assistant's cached copy after any `tests/test_a1_*` file ran and `proxy_visit_chat`'s `turn_root` would raise `AttributeError` on every `/ai/visit-chat` case; a `-k` run that loaded a gateway file first would hand the assistant the gateway's copy and switch its tracing silently off through `llm_client.py:727-732`'s guard. Pinning `tracing` per rig would touch twelve gateway rigs plus every ai rig, and each unpinned future rig would re-open it. **Resolution: the two copies are byte-identical** — one file carrying both helper sets, each service calling only its own — so whichever copy a bare `import tracing` binds is correct and the collision class is dissolved rather than managed; recorded as a dated note on eligibility-assistant-D-41 ("a copied `tracing.py`" — this revises the r1 f4 disposition's "deliberately not byte-identical", on the new evidence). The parity pin becomes **three tests**: `test_tracing_copies_are_byte_identical` (bytes — stricter than the `test_redaction.py` idiom, whose copies differ at their docstring, `cmp` 2026-08-27) beside the two hide-layer / output tests r1 f4 named, each copy loaded under its own name (`ai_tracing`, `tests/test_tracing.py:27`; new `gw_tracing`). **The two-seam fake is stated as the finding asks:** `test_single_tree_eight_stages` installs the capturing fake on **both apps' bound `tracing` objects** (`app_mod.tracing._langsmith`, `gw_app.tracing._langsmith` — the `_use_fake` idiom, `tests/test_tracing.py:137-139`; one fake, one `sent` list), opens with `app_mod.llm_client.tracing is app_mod.tracing`, and asserts the helpers it drives exist on each object; the gateway stack is loaded on the `test_visit_chat_phi.py:71-85` idiom. Breaks in verification 18: one byte appended to the gateway copy → the byte test red; the fake on one seam only → the eight-stage test red (the gateway spans absent). Baseline +12 → **+13** (verification 9, `:38`); the `tests/test_tracing.py:2` docstring row (`:24`) says three. **Sites changed:** `trace.md:13` (`services/gateway/tracing.py` row — byte-identical copy, the twelve rigs, the never-stream half); `trace.md:15` (`services/ai-assistant/tracing.py` row — both helper sets); `trace.md:16` (`tests/test_tracing.py` row — three tests); `trace.md:17` (`tests/test_a1_trace.py` row — two-stack chain, fake on both objects, identity assertion); `trace.md:24` (docstring row — three); `trace.md:38` (verification 9 — `+ 13`, the three named); `trace.md:42` (verification 18 — two breaks); `trace.md:53` (Landmines PHI paragraph — byte-identical copy); `trace.md:3` (Status line); `plans/eligibility-assistant.md` eligibility-assistant-D-41 (dated note). |
| 3 | SPEC-32 · `docs/todo.md` row (`trace.md:26`) · eligibility-assistant-D-79 (its executed note) · `docs/todo.md:53` | **class-repeat** (`turn` gate r2 f10 / r1 f4, cross-ticket — a register note not propagated into the ticket; same-ticket-only fallback → new, `:26` on r1 f8's list). D-79's note, written when the owner's REQ-9 amendment was executed, records that `docs/todo.md:53` is `TODO-10 | … | blocked_by: TODO-9` and says "`trace`'s `docs/todo.md` change row must also clear or re-word that field; raised here rather than left for the next gate". The row (`:26`) closes TODO-10, leaves TODO-9 verbatim, and says nothing about the `blocked_by:` field — so the landing edit as planned closes a row that still declares itself blocked by an open one. Name what happens to the field | **Disposition 2026-08-27 — A.** Named: the field is **removed** in the closing edit. Re-read this session — `docs/todo.md:53` is `- [ ] TODO-10 | Gated live-LLM integration test; needs a real bearer key | blocked_by: TODO-9`, `:29` defines `blocked_by` as a precondition ("a week, a debt id, or another `TODO-n`"), and the file's closed form is `- [x] TODO-n | **CLOSED by \`<item>\` (<date>) — …** <statement> | <keys>` (`:81-83`). A closed row has no precondition, and this one's was never real for the test that closes it — `tests/test_a1_live.py` hits real Bedrock and needs no captured fixture — so the row (`:26`) now spells the closed line with the `blocked_by` key dropped and no key added, TODO-9 left `[ ]` and verbatim, and records that `:53` is the only line in the file that references TODO-10. eligibility-assistant-D-79 gains the closing clause. Class (`turn` r2 f10 / r1 f4 — a register note not propagated into the ticket) closed with an enumerated sweep of every register decision or dated note that names `trace` or a `trace` row, each checked for a site in this file: eligibility-assistant-D-41 (gateway langsmith, the three egress corrections — `:13`, `:22`, holds; note added this round — `:13`, `:16`, `:17`); D-42 note (the ADR trace paragraph — `:27`, holds); D-43 (`2 skipped` — `:28`, `:38`, holds); D-46 (the root id the assistant subtree attaches under — `:15`, holds); D-51 / D-52 (SPEC-31 re-key, SPEC-27 emitter — `:45`, `:15`, hold); D-55 (SPEC-30 to `llm-seam`, `test_a1_trace.py` extended here, `docs/todo.md` row to SPEC-32/39a with 39a since dropped — `:17`, `:26`, hold); D-58 note (the record not attached to the span, D-68 — `:15`, holds); D-59 (independent of `lifecycle` — `:5`, holds); D-60 second pass (the dropped and re-cited verifications — `:37`–`:44`, hold); D-61 sweep (`:15` section label verbatim, holds); D-67 (the hand-copy shape — `:18`, holds); D-68 (`:15`, `:53`, hold); D-77 (runbook section "whichever lands first" — `:20`, holds); D-78 (name, two runs, env — `:18`, `:19`, `:40`, `:57`, hold); D-79 executed note (`blocked_by` — **changed**, `:26`). Fifteen notes, one site changed. **Sites changed:** `trace.md:26` (`docs/todo.md` row — the field's removal, the closed line's form); `plans/eligibility-assistant.md` eligibility-assistant-D-79 (closing clause on its note). |
| 4 | SPEC-34 · runbook row (`trace.md:20`) | **class-repeat** (r1 f7 — a count stated wrong on this file's own text: "+17", "four of nine"; same-ticket-only fallback → new, `:20` on r1 f6's list). "known limitations — **six**, of which five the contract names" is followed by seven: SPEC-34's four, the prose-only second patient (5, contract-mandated), the live-leg pin gap (6), and `turn`'s payer-menu residual, "named in this list too" (7); the r1 f6 cell says "six … Two more are named" — 4 + 1 + 2 = 7. Minor | **Disposition 2026-08-27 — A.** Seven, and the row (`:20`) now says so with the arithmetic beside it: SPEC-34's four plus the prose-only second patient are the five the contract names; the live-leg pin gap and `turn`'s payer-menu residual make seven. Class (r1 f7 — a count stated wrong on this file's own text) closed with an enumerated sweep of every count in this file's plan text, each re-derived this session: `:13` twelve gateway-loading test files, seven gateway pins (hold — `grep -l`; `services/gateway/requirements.txt:1-7`); `:16` three parity tests (holds, f2); `:17` ×8 paths, eight stages, ×10 tests in the file (hold — 1 + 1 + 8); `:18` five record fields (holds); `:19` two runs, one file (holds); `:20` six → **seven** (**changed**), five contract-named (holds), the live replay subset of five ids (holds); `:22` three egress clauses (holds — `CLAUDE.md:51`, `docs/debt-log.md:289`, `docs/phi-logging-policy.md:92`); `:25` five falsified-and-corrected sites, two not rewritten (hold — each has its row or its D-42 note); `:26` highest TODO-68 (holds); `:31` 339 lines, 61 shared (hold); `:34` +16, 1333 (hold — `docs/workflow/w4.md:189`, `:213`); `:37` 33 (holds); `:38` +13 = 3 + 10, second skip (hold, f2); `:39` / `:40` `2 skipped` (hold); `:41` eight paths, three breaks (hold); `:42` eight span names, three breaks (hold); `:44` three tests (holds); `:52` three of four ⚠ rows (holds — 28 / 29 / 31 of 28 / 29 / 31 / 32); `:53` two processes, eight keys, three mechanisms, eight paths (hold); `:57` five fields, two switches (hold); `:67` five `gate:` rows of nine, four without automated proof (hold — contract `:290-299`); `:68` eight stages (holds); `:72` 33 (holds); `:73` `2 skipped` (holds). Twenty-six count sites, one changed. **Sites changed:** `trace.md:20` (runbook row — seven). |
| 5 | SPEC-28 / SPEC-29 (⚠) · Landmines PHI paragraph (`trace.md:53`, "the payload is never streamed … SPEC-30's structural guarantee, landed in `llm-seam`") · `llm-seam.md:66` residual (b) | **new** (`:53` on r1 f3 / f4 lists; no dispositioned class matches). `llm-seam` residual (b) hands this ticket an obligation by name: "the gateway's own LangSmith client is a second process … its never-stream property is `trace`'s row to state and prove." This block rests the two-process hide argument on SPEC-30, which is the **assistant's** row, and states nothing about the gateway side — one sentence that the gateway emits `turn_root` / `span` runs synchronously and streams nothing (no `traceable` generator, no streamed run), with the parity or span test that shows it, closes the handoff. Minor | **Disposition 2026-08-27 — A.** The sentence is written and pinned. `services/gateway/tracing.py` row (`:13`): `turn_root` / `span(name)` are `contextmanager`s that open one `RunTree` with `inputs={}`, attach allowlisted metadata and end it **once** with `outputs={}` — no `traceable`-decorated generator, no streamed run, no chunked output — so the ADR 0006 hide layer has no streaming bypass on the gateway side; `services/gateway/app.py` references only `tracing.turn_root` / `tracing.span`. Pinned in `test_single_tree_eight_stages`' gateway leg (`:17`): each captured gateway run was created once with `inputs == {}` and ended once with `outputs in (None, {})`, no run receives a second end or a streamed chunk, and a string scan of `services/gateway/app.py` finds no `wrap_create` / `traceable` / `stream` token — the SPEC-30 reference-half idiom (`llm-seam.md:16`) on the gateway's caller (the scan is of the caller, not of `tracing.py`, because the byte-identical copy legitimately carries `wrap_create`'s `traceable` call, `services/ai-assistant/tracing.py:143`, which the gateway never invokes). Verification 18's third break (make the gateway `span` a `traceable` generator → red) is its negative check. The Landmines paragraph (`:53`) now says "never streamed **on either side**" and names both halves; `llm-seam.md:66` residual (b)'s handoff is closed here and that file is not edited (it is GATED). **Sites changed:** `trace.md:13` (`services/gateway/tracing.py` row — the never-stream sentence); `trace.md:17` (`tests/test_a1_trace.py` row — the gateway leg); `trace.md:42` (verification 18 — third break); `trace.md:53` (Landmines PHI paragraph — both halves). |

Per-SPEC verdicts (9 ids): FINDING — SPEC-27 (f2), SPEC-28 (f1, f2, f5), SPEC-29 (f1, f2), SPEC-32 (f3), SPEC-34 (f4) · residual-named — SPEC-35 (gate row; subset named — `EVAL-001`, `EVAL-007` conflict, `EVAL-016` emergency, `EVAL-012`, `EVAL-014-resists`; residual (a)), SPEC-39b (gate row; list in the runbook section; residual (a)) · satisfied — SPEC-31 (register row planned; `grep -c "eligibility-assistant" docs/phi-logging-policy.md` → 0 today, non-vacuous; residual (e) is a note on the cmd's strength, not a gap), SPEC-40 (+12 enumerated and matching: `test_tracing.py` ×2 + `test_a1_trace.py` 1 + 1 + 8; second skip `test_live_run_record`; w4 `+16` at `docs/workflow/w4.md:189`, `:213` verified).

checked: eligibility-assistant-SPEC-27, 28, 29, 31, 32, 34, 35, 39b, 40 (9 ids, the `trace` `Scope:` line) against Decisions eligibility-assistant-D-1..79, Evidence E-1..7, `trace.md` Changes (16 rows incl. the sweep bullet) / Gate interactions (4) / Verification 8, 9, 11–13, 18–22 / Landmines / Findings r1, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `907d40af53fc5a8a4e131eb4fc977694aed72e8c`** (round 1 recorded `d6f26e381df60d4f73767ef367791cb38dd79e56`; all nine r1 dispositions carry `Sites changed:` lists, plus the cross-ticket edits to `:15` recorded in `retrieval-eval.md:74` and D-61's sweep note — tags settled from the record) · r1 disposition sites re-read and holding: PHI header three rows + SPEC-32 in the Secrets bullet (f1), `docs/eligibility-assistant-live-run.md` + two runs + `LANGSMITH_TRACING=true` leg (f2), D-68 key-set reading + verification 13 second break (f3), two parity tests on the hide layer (f4), enumerated sweep bullet `:25` + `.env.example` / `test_tracing.py:2` rows (f5), six-limitations list (f6 — count wrong, f4 above), +16 / +12 / five `gate:` rows (f7), TODO-10-only row (f8 — `blocked_by` unhandled, f3 above), verification re-cites 8/11/12/13/20 (f9) · **origin: 1 new · 0 pre-existing · 4 class-repeat** (same-ticket-only fallback: f1 new · f2 pre-existing · f3 new · f4 new · f5 new) · check map 66/66 (9/9 in set: `test:` 27/28/29 · `cmd:` 31 · `gate:` 32/34/35/39b/40) · ⚠ rows in set: 28, 29, 31, 32 (4) — PHI-handling entered on 28/29/31 with D-56 cited with date (owner 2026-08-25, registered 2026-08-26) and D-68's narrowing recorded as owner-confirmed; 32 covered by the Secrets bullet (zone not entered; D-56 deliberately absent, the r1 f1 reading) · freeze scope: 0 `DEFERRED`, no row on a superseded REQ · ownership 66/66 once · cites: every SPEC / decision / evidence id resolves (D-67, D-68, D-78, D-79 present and confirmed; REQ-9 Notes amended at contract `:125`; `## Requirements` Status `:9` dated D-79) · change list closes both ways (every row traces to an in-scope SPEC, D-41 / D-42 / D-78 / D-79 or named registry upkeep; no row serves a sibling's row) · **tree facts verified:** no `services/gateway/tracing.py`; `services/gateway/requirements.txt` carries no `langsmith` (7 pins), gateway `Dockerfile:5-6` installs it, `:8` `COPY . .`; `services/ai-assistant/tracing.py:1-3` docstring, `_RUN_NAME` :45, `_tracing_enabled` :49-53, `_langsmith` seam :56-77 (lazy import), `_blank` :79, `wrap_create` :117, `ls.Client(hide_inputs=_blank, hide_outputs=_blank)` :142; `llm_client.py:728` `import tracing`; `tests/test_tracing.py:2` docstring, `_FakeLangSmith` :93, `_langsmith` injection :139/:164/:176; `tests/test_redaction.py:99`, `:153`; `tests/test_visit_chat_phi.py:4`, `_AI_PINNED` :40-50, `_GW_PINNED` :70-77 (neither pins `tracing`); `services/gateway/app.py` `proxy_visit_chat` :1059, `_post_checked(headers=)` :1150-1156, `visit_memory_save` :1192; `docker-compose.yml:79-82` gateway `env_file: .env`; `.env.example` (via `git show HEAD:`) :214 header ai-assistant-only, :224 `LANGSMITH_TRACING=false`, :229 `LANGSMITH_API_KEY=`; `docs/workflow/README.md:46-49`, `:215`, `:236-241` (two post-merge commits; stamp second); `docs/workflow/w4.md:189`, `:213` = 1317 + 16; `db/seed/generate_seed.py:56` `DEMO_PASSWORD`, `:80-93` USERS (`frontdesk` = `front_desk`); `AETN1224` in `seed.sql`; `docs/runbook.md:94-97` "role `staff`" line, no eligibility section; `CLAUDE.md:51`; `docs/debt-log.md:289`; `docs/phi-logging-policy.md:92` rationale opener, `eligibility-assistant` count 0; `adr/0011:83`; `adr/0006:129-133` decision 4 (first clause "only"); `adr/0009:125-131`; `tests/README.md:36`; `docs/todo.md:52-53` (TODO-10 `blocked_by: TODO-9`), highest TODO-68; `docs/landmines.md:77-81`, `:87`; `security.py:275-279`; contract `:125`, `:139`, `:253`, `:286-287`, `:290-299`, `:292`, `:326`, `:337`; `ci.yml` :58 (gateway in `services` matrix), :120-129, :135-155, :207; archive hits `review-loop-metrics:376`, `w4-multi-agent-assembly.html:247`, `w1/plan.md:488`, `w7.md:52/:56`, `w8.md:11/:34/:38/:42/:44` all present; in-code markers on the four edited files → 0; `langsmith 0.10.5` in `.venv` · client package: `document-manifest.json` 87 rows, id/label substring counts as stated in f1 · wrong facts: 1 (f4 count) · **5 findings** (2 minor).

### Gate — round 3, 2026-08-28

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names only (no
branch named — the agent's report says "+ branch", which the prompt did not carry; agent
read `main` @ `762f0fd` with the uncommitted working tree), no characterization received.
Two agents by mechanism (owner, 2026-08-27): this ticket read in one context with `turn`.
Origin tag derived by the agent from the record — round 2's `checked:` plan-text hash
(`907d40af…`) and the `Sites changed:` lists on all five r2 dispositions and on the one
cross-ticket edit since (`lifecycle` r2 f3's `:25` re-point). **Round-3 rule fires:** round
3 with four open `class-repeat` findings (same-ticket-only fallback: f1 new · f2 new ·
f3 pre-existing · f4 new — fires on either reading), so the owner adjudicates each
(accept as named residual · overrule · change the spec) before stage 3 dispositions it.
f2 surfaces a stage-2 call on SPEC-28 the plan cannot settle. Plan returns to stage 3
after adjudication; spec unchanged by this round.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-27 / SPEC-28 / SPEC-29 (⚠) · `tests/test_a1_trace.py` row (`trace.md:17`, `test_single_tree_eight_stages` "listing the eight span names") · `services/ai-assistant/tracing.py` row (`:15`, "`wrap_create` … unchanged") · verification 18 (`:42`) · `turn.md:25` (rig patches `app_mod.llm_client.client`) | **class-repeat** (r2 f2 / `llm-seam` r3 f1 / `corpus` r7 f1 → D-66: the object the test drives is not the one the code binds; same-ticket-only fallback → new, `:17` / `:42` on r2 f2 and f5 lists). The `bedrock` span exists only through `wrap_create`, which decides **at wrap time** — `services/ai-assistant/tracing.py:136-137` `if not _tracing_enabled(): return create_fn` — and is applied once, at `llm_client` import (`llm_client.py:727-732`). The ai stack is the corpus rig's `app_mod`, imported keyless (default suite; verification 11 `-e LANGSMITH_TRACING=`), so `client.messages.create` is the unwrapped seam; the `turn` rig then replaces `app_mod.llm_client.client` with the scripted queue, whose `.messages.create` is never wrapped. A later `monkeypatch.setenv("LANGSMITH_TRACING","true")` + fake `_langsmith` cannot retro-wrap it. So no `bedrock` run reaches the capturing fake: the eight-stage test can list at most seven, and `test_eval_019_payload_allowlist` / `test_negative_scan_every_path` never see the one span carrying model-call metadata. State the re-wrap (the test or rig wrapping the queue's `create` with `app_mod.tracing.wrap_create(…, app_mod.llm_client._trace_metadata)` after the env and fake are installed) and add the break (drop the re-wrap → eight-stage test red on `bedrock`) |  **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A.** The re-wrap stated as the module fixture `traced_stack` in the `tests/test_a1_trace.py` row (`:17`), which every test in the file takes, in the load-bearing order — env, the fake on both bound `tracing` objects, then `monkeypatch.setattr` of the scripted queue's `create` to `app_mod.tracing.wrap_create(<queue create>, app_mod.llm_client._trace_metadata)`, restored at teardown — with the wrap-time reason cited (`services/ai-assistant/tracing.py:136-137`; import-time wrap `llm_client.py:727-732`; rig replacement `turn.md:25`). The same row states the fixture's chain router forwards `headers` — `tests/test_visit_chat_phi.py:209-210`'s router drops them, which would sever the parent link (`:14`) — found while writing f3 and closed here because it is the same class (the object the test drives is not the one the code binds). Break added as verification 18's fourth (`:42`): drop the re-wrap → eight-stage test red on `bedrock`, allowlist test sees no `bedrock` run. Landmines `:53` names the re-wrap as the way the model-call run reaches the scan. Self-consistency walked: with the re-wrap the `bedrock` run carries `_trace_metadata`'s keys — f2's subject, resolved by eligibility-assistant-D-81 the same day, so the plan's own allowlist test is green on a correct implementation. Sites changed: `trace.md:17` (`tests/test_a1_trace.py` row), `:42` (verification 18), `:53` (Landmines), `:3` (Status line). |
| 2 | SPEC-28 (⚠) · `services/ai-assistant/tracing.py` row (`trace.md:15`, closed key set "one key per allowlist member") · Landmines PHI paragraph (`:53`) · `services/ai-assistant/llm_client.py:618-640` | **class-repeat** (r2 f1 — the allowlist test's domains vs what a correct implementation emits; same-ticket-only fallback → new, `:15` / `:53` on r2 f1 / f2 / f5 lists). Wrong fact by omission: the `bedrock` run's metadata is `_trace_metadata` (`llm_client.py:618-640`) — `model`, `request_id`, and when usage ints are present `input_tokens`, `output_tokens`, `estimated_cost_usd` (E-7 saw `request_id` only because the fake response had no usage). The plan leaves `wrap_create` unchanged and pins "each metadata key must be a member of the closed set `document_ids` · `section_labels` · `mode` · `reason` · `outcome` · `duration_ms` · `model` · `correlation_id`" over **every** captured run — so once f1 makes the span observable the plan's own test reddens on a correct implementation, and in the live tree SPEC-28's "only allowlisted content" is false as frozen: token counts and cost are not on its list (`docs/workflow/eligibility-assistant.md:286` — document ids + section labels, modes, fallback reason, outcome, timings, model/profile identifier, correlation metadata; `request_id` is arguably correlation metadata, tokens/cost are not). Either a change row narrows `_trace_metadata` to the allowlisted scalars (losing the tokens/cost ADR 0006/0009 put the span there for), or the four keys are admitted by owner reading/amendment of SPEC-28 at stage 2 — a call the plan must surface, not settle |  **owner adjudicated 2026-08-28 (round-3 rule): change the spec → executed at stage 2 the same day (eligibility-assistant-D-81, spec-tagged; the eligibility-assistant-D-65 / D-74 precedent), then stage 3.** **Disposition 2026-08-28 — A.** The owner chose amendment over narrowing `_trace_metadata` (which would drop the cost signal ADR 0006 / 0009 put there, move `tests/test_tracing.py:61-74` / `:211-214` and falsify `adr/0009-*.md:125-131`). Executed: SPEC-28's row gains "request id, token counts and cost estimate of the model call" with a dated Notes clause (`docs/workflow/eligibility-assistant.md:286`), the `## Spec` Status line dated (`:253`), wording only, 66 rows, `wc -l` 339. Plan propagation: the closed key set is twelve keys — the eight common plus `request_id` · `input_tokens` · `output_tokens` · `estimated_cost_usd` on the `bedrock` run **only** — with domains (string; non-negative ints; non-negative float; the three usage keys together or not at all, `llm_client.py:637-640`) and the `model` domain's fallback stated (`:634`) so the fixture's scripted responses are constrained (`:15`); Landmines `:53` restated; the approval paragraph `:54` records D-56 re-opened by the widening and re-closed by the owner's call the same day; verification 13's fourth break (`:41`) proves the per-run scoping (`input_tokens` on `retrieval` → red); the sweep line `:25` notes ADR 0009 is now mirrored by SPEC-28. `wrap_create` and `_trace_metadata` are unchanged — no change row. Sites changed: `docs/workflow/eligibility-assistant.md:286` (SPEC-28 row), `:253` (`## Spec` Status); register eligibility-assistant-D-81 (new row); `trace.md:15` (`tracing.py` row, two clauses), `:25` (sweep line), `:41` (verification 13), `:53` (Landmines), `:54` (approval paragraph), `:3` (Status line). |
| 3 | SPEC-32 · `tests/test_a1_live.py` row (`trace.md:18`) · residual (c) (`:69`) · `turn.md:35` / `:25` | **class-repeat** (r1 f2 — the record-producing run under-specified: file, sha, env; now the stack; same-ticket-only fallback → pre-existing, `:18` on r1 f2's list, on none since r2). `test_live_run_record` "runs one real turn … prints … trace reference", but which stack it drives is unstated. `turn.md:35` / `:25` have `tests/test_a1_live.py` use the rig's selection helpers and leave `app_mod.llm_client.client` real — the ai app alone — while the root run, `portal_entry`, `gateway_entry` and `persistence` are the gateway's (`:13`, `:14`, `turn_root(correlation_id)`). An assistant-only live turn has no root and no forwarded parent headers; what the assistant's `tracing_context(parent=…)` does with none (mints a root on the correlation id? five top-level runs?) is not stated at `:15`, so "trace reference" has no defined referent, and residual (c) "what a real LangSmith project renders is SPEC-32's single opt-in turn" overstates — that turn is not the SPEC-27 tree. State the stack (ai-only via `app_mod`, or the `:17` two-stack chain with a real key) and the reference's identity, and re-word (c) |  **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A.** (1) Stack stated at `:18`: `test_live_run_record` drives the two-stack chain (`:17`'s gateway `TestClient` → assistant `TestClient` with the header-forwarding router, `app_mod.llm_client.client` real per `turn.md:25`, real key, real Bedrock), so the tree is the SPEC-27 tree; the trace reference is `<project>/<root run id>`, root run id = the request's `X-Correlation-Id` (`:42`), project `Riverbend` (E-7). (2) The no-parent behaviour defined at `:15` — the assistant opens its own root on the correlation id, never five top-level runs — and pinned by NEW `test_assistant_alone_roots_on_correlation_id` (`:17`; verification 18 fifth break, `:42`), the count moving **+13 → +14** (verification 9, `:38`: `test_a1_trace.py` × 11). (3) Residual (c) re-worded (`:69`). Dated note on eligibility-assistant-D-78. Count sweep: `+13` / `thirteen` appeared only in this file (`:3`, `:38`; `grep -n` over the six ticket files and the register, 2026-08-28); siblings cite `trace.md:38` for the `2 skipped` term only (lifecycle.md:39, lifecycle.md:52, turn.md:65, turn.md:187), which does not move. Sites changed: `trace.md:15` (`tracing.py` row), `:17` (`tests/test_a1_trace.py` row — new test), `:18` (`tests/test_a1_live.py` row), `:38` (verification 9), `:42` (verification 18), `:53` (Landmines), `:69` (residual (c)), `:3` (Status line); register eligibility-assistant-D-78 (dated note 2026-08-28). |
| 4 | SPEC-27 · `services/gateway/tracing.py` row (`trace.md:13`, "**twelve** test files load `services/gateway/app.py`") | **class-repeat** (r1 f7 / r2 f4 — a count stated wrong on this file's own text; same-ticket-only fallback → new, `:13` on r2 f2 / f5 lists). Eleven load it; `tests/test_eligibility_budget_alignment.py` reads the source as text (`:405` `read_text()`, `:448`) and loads only `config.py` modules (`:240`, `:296`) — it has no pinned set and never binds `tracing`; the `grep -l` locator matched a docstring/path string. The argument survives on eleven. Minor |  **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A.** `:13` now reads **eleven**, with the twelfth `grep -l` match named and excluded for the reason (`tests/test_eligibility_budget_alignment.py:405` `read_text()`, `:448`; loads only `config.py` at `:240`, `:296`); the argument survives on eleven. Re-derived 2026-08-28: `grep -l "services/gateway/app.py" tests/*.py` → 12 files; `test_w4_exposure_sweep.py`'s load is at `:65` (`gw = load_module("services/gateway/app.py", "gw_sweep_app")`), not the `:55` the row carried — corrected in the same edit. Class sweep (a count stated on this file's own text; r1 f7 / r2 f4): `twelve` outside Findings tables now only at `:15` ("twelve keys", eligibility-assistant-D-81 — 8 + 4, correct by construction); `+13` → f3. Sites changed: `trace.md:13` (`services/gateway/tracing.py` row), `:3` (Status line). |

Per-SPEC verdicts: satisfied — SPEC-31 (register row planned; cmd → 0 today, non-vacuous; `turn`'s four edits carry no token), SPEC-40 (+13 = 3 + 10 enumerated; second skip; w4 +16 at `docs/workflow/w4.md:189` / `:213`); residual-named — SPEC-34 (gate row; seven limitations, five contract-named, residual (a)), SPEC-35 (gate row; subset of five ids named), SPEC-39b (gate row; list in the runbook section); FINDING — SPEC-27 (f1, f4), SPEC-28 (f1, f2), SPEC-29 (f1), SPEC-32 (f3).

checked: eligibility-assistant-SPEC-27, 28, 29, 31, 32, 34, 35, 39b, 40 (9 ids, the `trace` `Scope:` line) against Decisions eligibility-assistant-D-1..80, Evidence E-1..7, `trace.md` Changes (16 rows incl. the sweep bullet) / Gate interactions (4) / Verification 8, 9, 11–13, 18–22 / Landmines / Findings r1–r2, the five sibling `Scope:` + `Depends on:` lines, the item ticket table and the contract `## Delivery` table · **plan-text hash read: `3fa89794479b682856e04971a316d3ffd8ea6fa8`** (round 2 recorded `907d40af53fc5a8a4e131eb4fc977694aed72e8c`; all five r2 dispositions carry `Sites changed:` lists, and the one cross-ticket edit since — `lifecycle` r2 f3's `:25` re-point — is on that disposition's list; tags settled from the record) · **origin: 0 new · 0 pre-existing · 4 class-repeat** (same-ticket-only fallback: f1 new · f2 new · f3 pre-existing · f4 new) · r2 disposition sites re-read and holding: `:15` value domains + matching rule and the 13-`CA` / 4 / 3 / 4 / 6 / 4 / 1 substring counts and three label hits re-derived from the package manifest (87 rows); `:13` / `:15` / `:16` / `:17` / `:24` / `:38` / `:42` / `:53` byte-identical copy, three parity tests, fake on both bound objects, +13; `:26` TODO-10 closed form with `blocked_by` removed; `:20` seven limitations; `:13` / `:17` / `:42` / `:53` gateway never-stream half · check map 66/66 (9/9 in set: `test:` 27/28/29 · `cmd:` 31 · `gate:` 32/34/35/39b/40) · ⚠ rows in set: 28, 29, 31, 32 (4) — PHI-handling entered on 28/29/31 with D-56 cited with date and D-68's narrowing owner-confirmed; 32 in the Secrets bullet (zone not entered, D-56 deliberately absent) · freeze scope: 0 `DEFERRED`, no row on a superseded REQ · ownership 66/66 once · cites: every SPEC / decision / E-n id resolves (D-78, D-79 confirmed; REQ-9 Notes amended at contract `:125`) · change list closes both ways (every row → an in-scope SPEC, D-41 / D-42 / D-78 / D-79 or named registry upkeep; no row serves a sibling's row) · tree facts verified: no `services/gateway/tracing.py`; `services/gateway/requirements.txt` 7 pins, no `langsmith`; `services/ai-assistant/requirements.txt:9` + `requirements-dev.txt:13` `langsmith==0.10.5`, no `langchain` yet; gateway `Dockerfile:5-6`, `:8`; ai-assistant `Dockerfile:3`, `:8` · `services/ai-assistant/tracing.py:1-3`, `:38-41` stdlib-only, `:45`, `:49-53`, `:56`, `:79`, `:100-115` (`_attach_metadata` → `run.metadata.update`), `:117`, **`:136-137` wrap-time enable check**, `:142-143` · `llm_client.py:618-640` `_trace_metadata` keys, `:727-732` import-time wrap · `tests/test_tracing.py:2`, `:27` `ai_tracing`, `:79` `_FakeRunTree`, `:84` `_FakeClient`, `:93` `_FakeLangSmith` (fakes `Client` / `get_current_run_tree` / `traceable` only), `:137-139`, `:164`, `:176` · `tests/test_redaction.py:12-14` (ai-assistant vs intake-service), `:99`, `:153`; `cmp` → "differ: char 26, line 1" ✓ · `tests/test_visit_chat_phi.py:4`, `:34-43`, `:71-77` · eleven gateway-loading rigs at the cited lines (`:25` / `:20` / `:23` / `:31` / `:24` / `:20` / `:31` / `:246` / `:26` / `:55` / `:71`), none pins `tracing`; `tests/test_eligibility_budget_alignment.py:240`, `:296`, `:405`, `:448` reads text, loads no gateway `app` (f4) · `services/gateway/app.py:179`, `:1059`, `:1150-1156`, `:1192`; no `tracing` in either `app.py` today · `docker-compose.yml:79-82`, `:10`, `:267` · `.env.example` (via `git show HEAD:`) `:214`, `:224`, `:229`, `:233-234` · `db/seed/generate_seed.py:56`, `:80-93`; `AETN1224` in `seed.sql` · `docs/runbook.md:94-97`, no eligibility section · `Makefile:43` `ps` · `CLAUDE.md:51`, `:141`; `docs/debt-log.md:289`; `docs/phi-logging-policy.md:92`, count 0; `adr/0006:129-133`; `adr/0009:125-131`; `adr/0011:83`; `tests/README.md:36`; `docs/todo.md:29`, `:52-53`, `:81-83`, highest TODO-68 · `docs/workflow/README.md:46-49`, `:215`, `:236-241`; `docs/workflow/w4.md:189`, `:213` · `docs/landmines.md:77-81`, `:87` · `ci.yml:58`, `:120-129`, `:135-155`, `:207` · `eval/rag/check_drift.py` scope REPORT.md + seed.sql · `.venv` `langsmith 0.10.5` · contract 339 lines; `:125`, `:139`, `:253`, `:286-287`, `:290-299`, `:292`, `:326`, `:337` · sibling cites `corpus.md:28`, `llm-seam.md:16` / `:66`, `retrieval-eval.md:35` / `:74`, `lifecycle.md:28`, `turn.md:47` / `:64` / `:67-69`, `plans/eligibility-assistant.md:102` resolve · client package `document-manifest.json`: 87 rows, 25 categories, `front-desk-scripts` = `DOC-SYN-NEVER-STATE` alone, substring counts as stated at `:15` · wrong facts: 1 (f4 count) · 4 findings (1 minor).

### Gate — round 4, 2026-08-28

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names + branch
(`main`, working tree) only — no characterization received; agent read `main` @ `762f0fd`
with the uncommitted working tree. Two agents by mechanism (owner, 2026-08-27): this
ticket read in one context with `turn`. Origin tag derived by the agent from the record —
round 3's `checked:` plan-text hash (`3fa89794…`) and the `Sites changed:` lists on all
four r3 dispositions; no sibling 2026-08-28 disposition (`lifecycle` r3, `corpus` r9,
`retrieval-eval` r3) names `trace.md`. **Round-3 rule fires:** round 4 with two open
`class-repeat` findings (same-ticket text-record fallback: f1 new · f2 pre-existing —
fires on either reading), so the owner adjudicates each (accept as named residual ·
overrule · change the spec) before stage 3 dispositions it. Both minor. Plan returns to
stage 3 after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-27 / SPEC-28 (⚠) · `services/ai-assistant/tracing.py` row (`trace.md:15`, "on the `bedrock` run only … a run of any other name carrying one of them fails the key-set assertion") · `tests/test_a1_trace.py` row (`:17`, "listing the eight span names") · verification 18 (`:42`) · Landmines (`:53`, "emits `eligibility`, `retrieval`, `bedrock`, `validation`, `final_outcome`") | **class-repeat** (r2 f1 / r3 f2 — the allowlist test as described vs what a correct implementation emits; same-ticket fallback → new, all four anchors on r3 f1/f2/f3 lists). Two facts about the run the unchanged `wrap_create` produces are missing from the plan. (a) Its name is `bedrock.invoke_model`, not `bedrock` — `_RUN_NAME` at `services/ai-assistant/tracing.py:45`, which `:13` itself says is the same bytes in both copies, and E-7 recorded the run as `bedrock.invoke_model`; the plan's per-run-name scoping of the four D-81 scalars and its enumerated span names use a name no run carries. (b) An agent-path turn makes exactly two model calls (SPEC-22), so the tree holds **two** `bedrock.invoke_model` runs, each with `_trace_metadata`'s keys — the plan says "the `bedrock` run" singular everywhere and never states whether `test_single_tree_eight_stages` asserts eight *names* or eight *children* under the root. Name the run by `_RUN_NAME`, state the two-runs shape and what the eight-stage and allowlist tests key on. Minor, one row and its three echoes | **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A, accepted as named residual (h) (owner, in session; owner override 2026-08-28, in session: stamped in the gate session without a fresh re-run; the drift-gate rule "never stamp with an open finding" set aside by explicit owner decision for this wave).** Residual (h) fixes the reading: every "the `bedrock` run" in this file is shorthand for the run named `_RUN_NAME` = `bedrock.invoke_model` (`services/ai-assistant/tracing.py:45`); an agent-path turn holds two such runs (SPEC-22), each carrying `_trace_metadata`'s keys; `test_single_tree_eight_stages` asserts eight distinct stage names under the root (the two model-call runs are one stage), and the allowlist test scopes the four D-81 scalars to runs named `_RUN_NAME`. No mechanism, count or decision moves. Sites changed: `trace.md` Landmines residual (h) (new), `:3` (Status line), this cell. |
| 2 | SPEC-28 / SPEC-29 · `tests/test_tracing.py` row (`trace.md:16`, "`ai_tracing` at `tests/test_tracing.py:27`") · `services/gateway/tracing.py` row (`:13`) vs the eligibility-assistant-D-41 note it cites | **class-repeat** (r3 f4 / r1 f7 — a number on this ticket's own record missing its fact; same-ticket fallback → pre-existing, `:16` on r2 f2's list only). Two locators. `tests/test_tracing.py:27` is a blank line; `tracing = load_module("services/ai-assistant/tracing.py", "ai_tracing")` is `:28`. And r3 f4 corrected `:13` to eleven rigs / `test_w4_exposure_sweep.py:65` but its `Sites changed:` list (`:13`, `:3`) did not reach the D-41 note of 2026-08-27, which `:13` cites as the record of the byte-identical choice and which still reads "**twelve** test files … `test_w4_exposure_sweep.py:55`, `test_eligibility_budget_alignment.py`" — the register and the plan text disagree on the count and the locator one round after the fix. Minor | **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A, accepted as named residual (i) (owner, in session; owner override 2026-08-28, in session: stamped in the gate session without a fresh re-run; the drift-gate rule "never stamp with an open finding" set aside by explicit owner decision for this wave).** Residual (i) records the two locators: `tests/test_tracing.py:28` is the `ai_tracing` load (`:27` is blank), and register eligibility-assistant-D-41's 2026-08-27 note still reads twelve / `test_w4_exposure_sweep.py:55` where `:13` (r3 f4) holds the corrected eleven / `:65` — `:13` is the record; the register note takes its dated correction with the first stage-4 commit that touches the register (D4 of the plans-ride-the-branch rule). Sites changed: `trace.md` Landmines residual (i) (new), `:3` (Status line), this cell. |

Per-SPEC verdicts: satisfied — SPEC-29 (eight paths, matching rule, D-68 log-line-only), SPEC-31 (register row planned; `grep -c "eligibility-assistant" docs/phi-logging-policy.md` → 0 today, non-vacuous), SPEC-40 (+14 = 3 + 11 enumerated; second skip; w4 +16 at `docs/workflow/w4.md:189` / `:213`); residual-named — SPEC-32 (gate row; D-78 two runs, two-stack chain; residual (c)), SPEC-34 (gate row; seven limitations, five contract-named; residual (a)), SPEC-35 (gate row; five-id subset), SPEC-39b (gate row; list in the runbook section); FINDING — SPEC-27, SPEC-28 (f1); f2 is a locator/register gap, not a row verdict.

checked: eligibility-assistant-SPEC-27, 28, 29, 31, 32, 34, 35, 39b, 40 (9 ids, the `trace` `Scope:` line) against Decisions eligibility-assistant-D-1..81, Evidence E-1..7, `trace.md` Changes (16 rows incl. the sweep bullet) / Gate interactions (4) / Verification 8, 9, 11–13, 18–22 / Landmines / Findings r1–r3, the four sibling `Scope:` + `Depends on:` lines, the item ticket table (`plans/eligibility-assistant.md:105-112`) and the contract `## Delivery` table · **plan-text hash read: `0b0a0b0d09a3b7b7cda2ec763e1f379270fd429a`** (round 3 recorded `3fa89794479b682856e04971a316d3ffd8ea6fa8`; r3 f1–f4 carry lists; no cross-ticket edit since) — every tag settled from the record · **origin: 0 new · 0 pre-existing · 2 class-repeat** (same-ticket text-record fallback: f1 new · f2 pre-existing) · check map 66/66 single-mechanism (trace 9/9: `test:` 27/28/29 · `cmd:` 31 · `gate:` 32/34/35/39b/40) · ⚠ rows 28, 29, 31, 32 (4) — PHI-handling entered on 28/29/31 with D-56 cited by id and date, re-opened by D-81 and re-closed 2026-08-28 in the block and the register; 32 in the Secrets bullet (zone not entered, D-56 deliberately absent) · freeze scope: 0 `DEFERRED`, no row on a superseded REQ · ownership 66/66 once (turn 39 · corpus 7 · llm-seam 2 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated) · cites: every SPEC / decision / E-n id resolves (D-81 present, executed; contract `:286` amended, `:253` dated, 66 rows, `wc -l` 339) · change lists close both ways · tree facts verified: `services/ai-assistant/tracing.py:1-37`, :38-41, :45 `_RUN_NAME = "bedrock.invoke_model"`, :49-53, :56, :79, :117, :136-137, :142-143 · `llm_client.py:618-640` (`model` :634, usage guard :637-640), :723-732 · `services/gateway/app.py:641`, :646, :957-1015, :1150-1156; `gateway/config.py:101`; `services/gateway/requirements.txt` 7 pins, no `langsmith`; no `services/gateway/tracing.py` · `tests/test_tracing.py:2`, :28 (not :27), :61-74, :79, :84, :93, :137-139, :164, :176, :211-214 · eleven gateway-loading rigs at the cited lines; `grep -l` → 12 with `test_eligibility_budget_alignment.py` (:240, :296, :405, :448 reads text) · `tests/test_visit_chat_phi.py:209-210` · `tests/conftest.py:16-26` · `.env.example` (via `git show HEAD:`) :214, :224, :229 · `ci.yml:58`, :72-76, :120-129, :135-155, :207 · `Makefile:34-35`, :43, :46-47, :76-77 · `docker-compose.yml:10`, :79-82, :267 · `docs/runbook.md:94-97`, no eligibility section · `docs/landmines.md:63`, :77-81, :87 · `docs/debt-log.md:289` (`:239` clause present), :290 · `docs/phi-logging-policy.md:46-47`, :51-52, :58-59, :92, :96, `grep -c` → 0 · `docs/todo.md:52-53`, TODO-10 at :53 only · `docs/workflow/README.md:46-49`, :215, :236-241; `w4.md:189`, :213 · `adr/0009:60`, :125-131; `0011:70` / :83 / :294 / :310 / :372-373 / :421 / :783 / :1026 · `Dockerfile.test`, `.gitleaks.toml`, `tests/integration/test_schema_upgrade_path.py` exist; `tests/test_a1_trace.py`, `docs/eligibility-assistant-live-run.md` absent (NEW true) · `.venv` langsmith 0.10.5 · sibling cites `corpus.md:24-28`, :181-182, :234; `llm-seam.md:16`, :35, :51, :66 (GATED); `retrieval-eval.md:13`, :21, :35, :41, :74; `lifecycle.md:28`; `turn.md:25` / :47 / :64 / :67-69 / :76 resolve · client package `document-manifest.json`: 87 rows, 25 categories, ids containing `CA` 13 / `HUMANA` 4 / `AETNA` 3 / `CIGNA` 4 / `MEDICAID` 6 / `MEDICARE` 4 / `CHIP` 1, labels `Humana Healthcare for Providers` ×1, `Medicare Second` ×3, `Eligible for Medicaid?` ×1 — as `trace.md:15` states · counts re-summed: +14 (3 + 11), 33 · wrong facts: 2 (`:16`'s `:27`; the run name `bedrock`) · 2 findings (both minor, class-repeat).

**Stamped `plan GATED 2026-08-28` under owner override** (in session, 2026-08-28): f1 / f2 accepted as named residuals (h) / (i) and dispositioned in the same act; no fresh re-run — the `drift-gate` "never stamp with an open finding" rule set aside by explicit owner decision for this wave, recorded here and in the Status line. Writes by the gate session under the override: the two cells, residuals (h) / (i), the Status line, the contract ticket row. Plan-text hash read this round (`0b0a0b0d…`) is the pre-override text; the stamped text differs from it by those sites only.
