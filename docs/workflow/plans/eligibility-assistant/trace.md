# eligibility-assistant / trace

Status: plan DRAFT 2026-08-25 (re-sliced from item plan; gate rounds 1–2 on the monolithic plan are in plans/eligibility-assistant.md)
Scope: SPEC rows eligibility-assistant-SPEC-27, -28, -29, -30, -31, -32, -34, -35, -40 (PR-2 — trace, live proof, handoff; eligibility-assistant-D-3 as amended by D-34). SPEC-30's `test_model_call_not_streamed` lands in the `llm-seam` change list; SPEC-39b (handoff record) is placed in PR-1c by D-34 as written — gate round 2 finding f4, open
Depends on: turn

## Plan

Changes (file level):

- `services/gateway/tracing.py` — NEW copy of the ai-assistant module (source noted in header) plus `turn_root(correlation_id)` / `span(name)` helpers; `services/gateway/requirements.txt` — `langsmith==0.10.5` (eligibility-assistant-SPEC-27 / eligibility-assistant-D-41)
- `services/gateway/app.py` — `proxy_visit_chat` opens the root run, emits `portal_entry` / `gateway_entry` / `persistence`, forwards the distributed-trace headers on `_post_checked` (eligibility-assistant-SPEC-27 / eligibility-assistant-D-41)
- `services/ai-assistant/tracing.py` — `tracing_context(parent=headers)` entry, `span(name, metadata)` helper with the same swallow-on-failure contract; `app.py` / `agent_turn.py` emit `eligibility`, `retrieval`, `validation`, `final_outcome` with allowlisted metadata only (document ids + section labels, mode, reason, outcome, timings, model id, correlation id) (eligibility-assistant-SPEC-27/28 / eligibility-assistant-D-41)
- `tests/test_tracing.py` — `test_copies_are_identical` (gateway copy parity, the `test_redaction.py` idiom) (eligibility-assistant-D-41)
- `tests/test_a1_trace.py` — `test_single_tree_eight_stages` (capturing fake across the real gateway → assistant chain, the `test_visit_chat_phi.py` rig), `test_eval_019_payload_allowlist[EVAL-019]`, `test_negative_scan_every_path` ×8 (success, payer error, provider error, budget stop, validation reject, no retrieval, emergency, cross-patient — PHI planted, every captured payload scanned for the never-list) (eligibility-assistant-SPEC-27/28/29)
- `tests/test_a1_live.py` — `test_live_run_record` (opt-in; runs one real turn against the checked-out sha, asserts `mode == "real"`, and writes the record's fields) (eligibility-assistant-SPEC-32)
- `docs/a1-live-run.md` — NEW: the live run record — merged sha, trace reference, date, `mode=real`, model id; produced against the exact merged sha (eligibility-assistant-SPEC-32 / REQ-9)
- `docs/runbook.md` — NEW "Eligibility assistant demo (eligibility-assistant)" section: `make up` → `.env` keys → login as `frontdesk` → the four menus + a member id → cited turn → trace location; real/fixture/fallback per component; known limitations (eligibility-assistant-D-17 `staff`, CA AB 3030 line, HIPAA-eligible ≠ compliant, D13); the two `stop` surfaces; the default-green / live-gated case list; handoff owner (eligibility-assistant-D-7); the named live replay subset — `EVAL-001`, `EVAL-007` (conflict), `EVAL-016` (emergency), `EVAL-012` (cross-patient), `EVAL-014-resists`; the "Demo accounts" line "role `staff`" corrected to the seeded per-role accounts (`db/seed/generate_seed.py:80-93`) (eligibility-assistant-SPEC-34/35/39b / eligibility-assistant-D-24, eligibility-assistant-D-47; fix-wrong-claims rule)
- `docs/phi-logging-policy.md` — NEW register row for the trace path (both services, metadata-only, negative-scanned on every path; residual: metadata-only egress to LangSmith cloud from two processes) (eligibility-assistant-SPEC-31)
- `docs/debt-log.md` D13 (`:289` "`/ai/*` is the estate's only vendor-egress path"), `CLAUDE.md` §2 (`:51` "the only vendor-egress path in the estate"), `docs/phi-logging-policy.md` register row `:92` (rationale opener "The only vendor-egress service.") — each clause corrected to: `/ai/*` remains the only *PHI-adjacent model* egress; the gateway now emits metadata-only trace runs; every other sentence in those rows stays verbatim (eligibility-assistant-D-41; falsified-claims sweep, scope in gate r1 f9's disposition). `adr/0011-*.md:83` ("ai-assistant is the only egress point") is a decision-as-taken record and is covered by the eligibility-assistant-D-42 status note, not rewritten in place
- `docs/todo.md` — TODO-9 and TODO-10 closed by the live record and the opt-in live test (eligibility-assistant-SPEC-32/39); new entries for the residuals named below, ids allocated at landing (collision rule)
- `adr/0019-*.md` — trace section appended (eligibility-assistant-D-42)
- `CLAUDE.md` §6 — baseline line updated to the post-PR-2 count (eligibility-assistant-SPEC-40)

Structural exclusion (every ticket):

Nothing changes under `db/`, `config/roles.yaml`, `services/gateway/authz.py`, `services/gateway/security.py`, `services/eligibility-service/`, `services/ai-assistant/eligibility_client.py`, `docker-compose.yml` (no port, no service), or `eval/` — the spec Exclusions and the §1 zones not entered hold structurally (verifications 19–21).

Gate interactions (existing steps that already enforce part of a new check):
- CI `workflow-doc-cap` (400 lines, contract file): `docs/workflow/eligibility-assistant.md` is 316 lines now; four Deliveries share 84 lines — Delivery is written per the evidence-by-reference rule and the impl gate re-checks the count pre-push.
- `make eval` / `eval/rag/check_drift.py`: scope is `eval/rag/REPORT.md` + `db/seed/seed.sql`; eligibility-assistant touches neither, so the drift gate is unaffected (not re-run).
- `tests/test_compose_topology.py`: no `ports:` change, no new service, no new env template; unaffected.
- Baseline (`CLAUDE.md` §6): `1317 passed, 19 deselected, 1 xfailed` was measured before w4 (`f471b70`, +17); the `Baseline at branch:` header is measured fresh at each cut and is the number each PR moves from; `1 skipped` appears from 1c, `2 skipped` from PR-2 (eligibility-assistant-D-43).

Verification (runnable, expected output stated; numbering carried from the item plan):
8. `.venv/bin/python -m pytest -m "not integration" --collect-only -q | grep -o 'EVAL-0[0-9][0-9]' | sort -u | wc -l` → `2` after 1a (`EVAL-031`, `EVAL-023`), `2` after 1b, `31` after 1c, `32` after PR-2 (`EVAL-019` lands with the trace) (eligibility-assistant-SPEC-37; the `-m` deselection does not drop the live tests — they are skips, eligibility-assistant-D-43)
9. `.venv/bin/python -m pytest -m "not integration" -q` → `<baseline passed + N> passed, 19 deselected, 1 xfailed` after 1a/1b; `…, 1 skipped` term appears after 1c and becomes `2 skipped` after PR-2; deselected and xfailed never move (eligibility-assistant-SPEC-40; every PR); the claim-worthy run is `make test-docker` with the same numbers
11. `docker build -q -t riverbend-test:py312 -f Dockerfile.test . && docker run --rm --network none -e AWS_BEARER_TOKEN_BEDROCK= -e LANGSMITH_TRACING= -v "$PWD":/repo -w /repo riverbend-test:py312 pytest -m "not integration" -q` → green with `1 skipped` at 1c / `2 skipped` at PR-2, no network, no keys (eligibility-assistant-SPEC-36 / eligibility-assistant-D-22; 1c, PR-2)
12. `.venv/bin/python -m pytest tests/test_a1_live.py -q` → `1 skipped` at 1c / `2 skipped` at PR-2 (default); `A1_LIVE_BEDROCK=1` with the owner's `.env` loaded → `1 passed` at 1c / `2 passed` at PR-2 with the SPEC-32 record's fields printed (eligibility-assistant-SPEC-17/32/39a; 1c has 1 test, PR-2 has 2)
13. `.venv/bin/python -m pytest tests/test_a1_prompt_boundary.py tests/test_a1_trace.py -q` → passed; then break-then-revert: interpolate `req.message` into `_build_model1_prompt` → `test_egress_excludes_clerk_text_and_ids` red; revert → green (eligibility-assistant-SPEC-12; 1c). Break-then-revert: emit `req.message` as `retrieval` span metadata → `test_negative_scan_every_path` red on all eight paths; revert → green (eligibility-assistant-SPEC-29; PR-2)
18. `.venv/bin/python -m pytest tests/test_a1_trace.py::test_single_tree_eight_stages -q -v` → passed, listing the eight span names under one root whose id equals the request's `X-Correlation-Id` (eligibility-assistant-SPEC-27; PR-2)
19. `git diff --stat main -- db/ config/ services/gateway/authz.py services/gateway/security.py services/eligibility-service/ services/ai-assistant/eligibility_client.py docker-compose.yml eval/` → empty on every PR (structural exclusion check; every PR)
20. `.venv/bin/python -m pytest tests/test_eligibility_budget_alignment.py tests/test_compose_topology.py tests/test_gateway_proxy_error_contract.py -q` → passed, unchanged counts (eligibility-assistant-SPEC-18; every PR)
21. `grep -c "a1" docs/phi-logging-policy.md` → ≥ 1 inside the register table; `wc -l docs/workflow/eligibility-assistant.md` → ≤ 400 (eligibility-assistant-SPEC-31; PR-2 / every PR)
22. `make up`, then the runbook's eligibility-assistant demo section hand-run from `make up` alone by the impl-gate session: login `frontdesk` / `portal123`, four menus + `AETN1224`, cited reply with mode `real` visible, trace reference located; from the **host**, `make ps` shows `records-service` with no host port mapping (`expose`-only, ADR 0016 — the tracked proof is verification 20's `tests/test_compose_topology.py`, this is the demo-time spot check; a literal domain-service host-port probe is what that test forbids in docs) and `curl -sS localhost:8070/healthz` → 200; observation recorded in Delivery (eligibility-assistant-SPEC-34/35; PR-2 `gate:`)

## Landmines

- **PHI handling (§1) — entered.** Three new PHI-adjacent code paths: (1) the model payloads (eligibility-assistant-SPEC-12, ⚠ REQ-3‴) — two Bedrock calls per agent-path turn carrying closed vocabulary plus vendored synthetic corpus text, never the clerk's message, the member id, or any identifier, proven by `tests/test_a1_prompt_boundary.py::test_egress_excludes_clerk_text_and_ids` on both calls and every path; (2) the visit-memory shape (eligibility-assistant-SPEC-20, ⚠ REQ-5′) — `VisitFacts.last_citations` carries document ids and version only, `extra="forbid"` kept, TTL unchanged, under the phi-logging-policy rule-6 controls the register's ACCEPTED 2026-07-26 row already grants, with that row amended in 1c; (3) the trace payloads (eligibility-assistant-SPEC-28/29/30/31, ⚠ REQ-8‴) — one LangSmith tree per turn from two processes (gateway root + assistant subtree, eligibility-assistant-D-41) carrying only the REQ-8‴ allowlist, the ADR 0006 two-layer hide on both clients, never streamed, negative-scanned on all eight paths, and a new register row in PR-2. Approval of record for entering the zone: **the owner's §1 approval of code entry on SPEC-7–10, 12, 20, 28–31 as designed in this block, given 2026-08-25 in session at plan review r1 (gate r1 f1), unconditional**, standing on the owner's freeze of the ⚠ rows REQ-3‴ / 5′ / 8‴ and SPEC-7–10, 12, 18–20, 28–31, 47–49 on 2026-08-24 (contract `## Spec` Status), the D13 gate decision eligibility-assistant-D-14 (nothing free-text reaches the model), and eligibility-assistant-D-41 (trace shape, confirmed by the owner 2026-08-25). This is the record the `.claude/skills/implementation/` entry checklist requires before code on these rows; a change to the mechanisms named here re-opens it. Deliberate defects in reach are preserved: D13 stays OPEN (synthetic corpus, closed prompt, no BAA — ADR 0011 constraint 4 unchanged, the payer answer is still the only coverage source); the `README.md` HIPAA overstatement (TODO-12) is untouched; the D1/D3 log-and-column defects are not touched by any eligibility-assistant file. Accepted residuals: the gateway becomes a second metadata-only vendor-egress process (LangSmith, no PHI by the same two-layer hide), corrected in CLAUDE.md §2 / D13 in PR-2; the LangSmith key is not provisioned (eligibility-assistant-D-8, needed by 2026-08-27) — SPEC-27–29 are proven keyless against a capturing fake, the live tree is SPEC-32's gate only.
- **Secrets (§1) — not touched.** `.env` and every secret file stay untouched; `.env.example` gains two non-secret `A1_*` keys; the Bedrock bearer and the LangSmith key stay in the owner's gitignored `.env` (eligibility-assistant-D-8); the live-run record (`docs/a1-live-run.md`) carries a sha, a trace reference and a date, never a key. Vendored corpus and fixtures pass CI's gitleaks scan (verification 10).
- **Migrations / schema (§1) — not touched.** Nothing under `db/`.
- **Partial coverage named honestly (residual honesty check):** eligibility-assistant-SPEC-32 / 34 / 35 / 39b are `gate:` rows whose observation exists only after PR-2's live run — until then the item's "32/32" is 31 default-green + `EVAL-014-resists` live-gated (eligibility-assistant-D-24); eligibility-assistant-SPEC-37's count is **2 after 1a** (`EVAL-031` corpus gate + `EVAL-023` retriever test), 2 after 1b, **31 after 1c**, and **32 only at PR-2**, where `EVAL-019` (`test_eval_019_payload_allowlist`, the trace-payload kind) lands with the trace — the 32-count is a PR-2 fact, not a 1c one (gate r1 f3); the E-4 live leg ran on `langsmith 0.11.1` while the repo pins `0.10.5` — E-5 proves the composition offline (resolve, compile, bounded loop, tracer import) and the live composition is first exercised at SPEC-17 / SPEC-32's opt-in runs, so a live-only incompatibility would surface there, not in the default suite (gate r1 f12); eligibility-assistant-SPEC-27's eight-stage assertion is over the agent path — a deterministic turn produces one tree with the stages it actually ran (no `bedrock` span on a zero-call turn by construction, root metadata carries the mode); model₂'s decision is a validated JSON text block, not provider structured output (eligibility-assistant-D-37) — its live robustness is measured only by SPEC-17/32, and a parse miss degrades to `validation_reject`, never to an invented citation; Open (5) subset-vs-exact on `expected_source_ids` is unresolved — SPEC-45 renders the third citation on EVAL-024 as frozen.

## Findings

Gate rounds start at 1 for this ticket; carried findings: f3 (SPEC-31 — the 1c `docs/phi-logging-policy.md` amendment makes `grep -c a1 …` pass one PR before the trace row exists).
