# eligibility-assistant / corpus — ticket plan file (deleted at its merge; contract: docs/workflow/eligibility-assistant.md)

Status: plan GATED 2026-08-28 · delivery DRAFT 2026-08-29 (**owner override 2026-08-28, in session: stamped in the gate session without a fresh re-run; the drift-gate rule "never stamp with an open finding" set aside by explicit owner decision for this wave; gate r10's one finding, `pre-existing`, accepted as named residual (e) and dispositioned A in the same act; plan-text hash at stamp recorded in r10**) (**gate r9 dispositioned 2026-08-28 under the round-3 rule** — owner adjudicated the one finding 2026-08-28 as accepted → stage 3, A: the stale proposed-state token on the `policy_index.py` row's D-80 clause (`:15`) replaced with the confirmed record; the token sweep over the six ticket files, the register and the contract enumerated, no other site; no mechanism, test or count moves; no owner call open) (**gate r8 dispositioned 2026-08-27 under the round-3 rule** — owner adjudicated the one finding 2026-08-27 as accepted → stage 3, A: the `tests/a1_corpus_rig.py` row's locator corrected (`app.py:30-36` imports seven of the eight pinned names; `breaker` is `eligibility_client.py:29`'s), the membership-statement sweep enumerated, no mechanism moves; **cross-ticket revision received the same day from `lifecycle` gate r2 f2 — eligibility-assistant-D-80, confirmed by the owner 2026-08-27, no owner call open:** `categories(root=None)` stated manifest-wide with the index's `rows` / `categories` shape in the `policy_index.py` row, the `test_topic_enum_equals_manifest_categories` clause, verification 4l's second break and the Landmines carry-forward sentence, which is re-opened on that clause by D-80 and re-closed by its confirmation the same day; no test of this ticket moves) (**cross-ticket revisions received 2026-08-27 from `lifecycle` gate r1's stage-3 disposition:** f7 named `policy_index.CorpusLoadError` in the `policy_index.py` change row and in `test_startup_hook_fails_boot_on_corpus_error`; f5 added the second `index.json` curation obligation, `DOC-SYN-NEVER-STATE` at `payers`/`products`/`states` = `*`, eligibility-assistant-D-77; no mechanism of this ticket changes) (gate r7 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated both findings 2026-08-27 as accepted → stage 3, both A, with the resolution chosen per finding; NEW eligibility-assistant-D-66 (one pinned bare-name module set, `tests/a1_corpus_rig.py`) **confirmed by owner 2026-08-27 — no owner call open**, eligibility-assistant-D-35 note 3; gate r6 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated all four findings 2026-08-27 as accepted → stage 3, all A, eligibility-assistant-D-63 note (whole-tree scan), D-42 note (cross-ticket from `llm-seam` r6 f1), r5 f3 record corrected, no owner call open; gate r5 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated all five findings 2026-08-27 as accepted → stage 3, all A, eligibility-assistant-D-38 note 3, D-36 / D-61 cap clauses corrected, verification 4o, no owner call open; gate r4 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated all five findings 2026-08-27 as accepted → stage 3, eligibility-assistant-D-64 amended (cap 6 → 5), D-35 note 2, no owner call open; gate r3 dispositioned 2026-08-27 under the round-3 rule — eligibility-assistant-D-64 confirmed by owner 2026-08-27, f4 amended in place per eligibility-assistant-D-65, f7 confirmed — no owner call open; gate r2 dispositioned 2026-08-27 — eligibility-assistant-D-61 / D-62 / D-63 confirmed by owner 2026-08-27; re-sliced 2026-08-25 from the item plan; gate rounds 1–2 on the monolithic plan are in plans/eligibility-assistant.md — carried f2 dispositioned 2026-08-26)
Scope: SPEC rows eligibility-assistant-SPEC-7, -8, -9, -10, -11, -38, -43 (eligibility-assistant-D-34, PR-1a — corpus, index, retriever, tier, fixtures; lands dark, not wired to the request path)
Depends on: none
Sibling ticket files (impl-gate r1 f2, 2026-08-29): the five siblings' stamped text is **not on this branch** — `main` and this branch carry only the pre-stamp `llm-seam.md` / `trace.md` / `turn.md` (30 / 56 / 73 lines) and no `lifecycle.md` / `retrieval-eval.md` at all. Every `<sibling>.md:N` cite in this file resolves against `origin/wip/eligibility-assistant-plans` **@6a773fd** (426 / 188 / 246 / 189 / 151 lines; the branch-cut rule's uncut-sibling push, pushed 2026-08-29). Landing order eligibility-assistant-D-59: corpus → llm-seam → retrieval-eval → turn → {lifecycle, trace}; each ticket's file first reaches `main` in its own code PR, so a cite here is only ever resolvable at that ref until then. Cites in `## Plan` / `## Landmines` are live and were re-read against @6a773fd this session (five: `llm-seam.md:15`, `turn.md:25`, `lifecycle.md:30`, `retrieval-eval.md:19`, `:20` — all hold). Cites inside `## Findings` round logs are **history, correct at their round and not rewritten** (the rule this file already states at `:397`); of those, `lifecycle.md:5` / `:14` / `:17` and `retrieval-eval.md:13` / `:18` hold at @6a773fd, while `lifecycle.md:21` / `:45` and `retrieval-eval.md:29` / `:30` / `:31` have since moved (`lifecycle` gate r2–r4, `retrieval-eval` gate r1, both 2026-08-27/28) — each still names the claim it was written for.

## Plan

Status: GATED 2026-08-28 (owner override, in session — see the header Status line and the closing gate round)

Changes (file level):

- `services/ai-assistant/policy_corpus/document-manifest.json` + 87 vendored files under `documents/` and `procedures/` at their package paths — NEW, byte-identical to the package (eligibility-assistant-SPEC-7 / eligibility-assistant-D-6, eligibility-assistant-D-35)
- `services/ai-assistant/policy_corpus/index.json` — NEW curated enum index, one entry per approved row; its `sections` value is the manifest `section_labels` parsed on `|`, informational only — the row the retriever returns carries the manifest string verbatim (eligibility-assistant-SPEC-11 / eligibility-assistant-D-36, eligibility-assistant-D-61). **Curation obligation carried for `retrieval-eval` (`retrieval-eval` gate r1 f3 / f8(b), eligibility-assistant-D-70):** the curation must leave `("medicaid-managed-care", "aetna", "commercial", "unconfirmed")` **empty** (the seven `medicaid-managed-care` rows are Medicaid/CHIP publishers with state-Medicaid applicability — package `document-manifest.json`, read 2026-08-27 — so none may take payer `aetna` or product `commercial`) and each of `("coordination-of-benefits", "medicare", …)`, `("medicaid-managed-care", "medicaid", …)` and `("payer-eligibility-public", "humana", …)` at **≥ 2 rows**; `retrieval-eval`'s `test_record_fields_and_provenance` and `test_ranking_isolated_from_filtering` assert all four, so a curation that broke one reddens there rather than passing silently. **Second curation obligation, carried for `lifecycle` (`lifecycle` gate r1 f5, eligibility-assistant-D-77):** `DOC-SYN-NEVER-STATE` — the sole row of category `front-desk-scripts` (one of the manifest's eight single-row categories; applicability "All visits" — package `document-manifest.json`, read 2026-08-27) — must be curated `payers` / `products` / `states` = `*`, so `("front-desk-scripts", <any payer>, "unconfirmed", "unconfirmed")` returns exactly that one row and, once `lifecycle` withdraws it in a `tmp_path` variant, none; `lifecycle`'s `test_withdrawn_sole_source_refuses` asserts both halves, so a curation that broke it reddens there. Nothing in this ticket's own tests depends on either obligation
- `services/ai-assistant/policy_index.py` — NEW: `load(root=None)` — corpus loader with sha256 verification against the manifest (a mismatch or an unlisted file raises **`policy_index.CorpusLoadError`** — the loader's own exception type, named and exported here and reused by `lifecycle` for its lifecycle failures (**`lifecycle` gate r1 f7**) — before any lookup; SPEC-7's failure mode is fail-closed in code as well as in the suite); **scan scope (gate r2 f2):** the loader walks the **whole `policy_corpus/` tree** at every depth (**gate r6 f3** — the r2 wording "the manifest path roots" was narrower than SPEC-7's frozen "an unlisted file … shall fail"): every file found is either one of the two named root-level non-document files, `document-manifest.json` and `index.json`, or the `path` of a manifest row with a matching sha — 87 paths today, 83 under `documents/` (depths 2–3) and 4 under `procedures/` (depth 1), package `document-manifest.json` counted 2026-08-27 — and any other file at any depth (`policy_corpus/README.md`, `policy_corpus/notes/rogue.md`, `policy_corpus/documents/x12/extra.md`) raises, with exactly one exempt **name**, `.DS_Store`, at any depth (**gate r2 f3**, eligibility-assistant-D-63 — Finder writes it into any browsed folder; the package's own `documents/.DS_Store` is not vendored, and `.dockerignore` keeps the name out of the image); a manifest row whose `approval_status` is not `approved` raises at load, never skipped (**gate r2 f5**, eligibility-assistant-D-63); `root` defaults to the service's `policy_corpus/` and is overridable so later tickets build `tmp_path` variants (eligibility-assistant-D-57). **Row shape (gate r2 f1, eligibility-assistant-D-61):** one row per approved manifest row — `id` = `document_id`, `title`, `section` = the manifest `section_labels` string verbatim, `version` = `version_effective` and `retrieval_date` verbatim, `section-text` = the whole vendored file (no heading split, no `|` parse on the row — 62/87 documents have no label that is a heading, and the files are ≤ 2,448 bytes each); the cap therefore counts documents. `_filter(topic, payer, product, state)` (query values are eligibility-assistant-D-36 enum members only — `unconfirmed` non-filtering; `*` is an **index-row** value that matches any query value, and a query `*` or any non-enum value raises `ValueError` before the index is read — eligibility-assistant-D-64, **gate r3 f2**) then `rank(rows)` (**key (gate r2 f2, eligibility-assistant-D-62):** eligibility-assistant-D-38 tier rank asc, then `retrieval_date` desc, then `document_id` asc — a total order on manifest fields, no `version_effective` parse; the one ordering site, kept as its own function so SPEC-66's substitution test lands in `retrieval-eval` without a refactor) then the `A1_RETRIEVAL_MAX_ROWS` cut (the module also exports `PROMPT_RESERVE_BYTES = 5000`, `categories()` and `MAX_ROW_BYTES`, eligibility-assistant-D-64 as amended 2026-08-27 / gate r3 f3, r4 f1). **Loader semantics (gate r4 f4):** `load()` is the **one** manifest reader and is not memoised — every call re-walks the roots, re-verifies every sha and rebuilds the module-level index it returns; `categories()` calls `load()` (so `policy_tool`'s import-time enum is built from a verified corpus, never from a second reader outside SPEC-7's boundary; **`categories(root=None)` mirrors `load(root=None)` and is manifest-wide — eligibility-assistant-D-80, `lifecycle` gate r2 f2, confirmed by the owner 2026-08-27 (the stale proposed-state token replaced 2026-08-28, gate r9 f1):** the object `load()` returns carries, from its one parse, `rows` — the approved rows `_filter` / `lookup` / `fetch_by_id` / `rank` read and `MAX_ROW_BYTES` is computed over, which `lifecycle` narrows to `current` (eligibility-assistant-D-75) — and `categories`, the sorted tuple of distinct `category` values over **every** approved manifest row whatever lifecycle status a later ticket gives it; `categories(root)` returns `load(root=root).categories`, so a topic whose every row is withdrawn stays in the model-facing enum and a lookup on it returns zero rows, which is SPEC-61's refusal, not a missing menu topic; `_INDEX` holds that one object, and its non-publish rule below is unchanged) and `lookup` / `fetch_by_id` use the index of the last **default-root** `load()`, calling it first if none has run (the lazy path for direct module callers, eligibility-assistant-D-35). **Module state is published by the default root only (gate r5 f3):** `load()` with no `root` sets the module-level index `policy_index._INDEX` and `MAX_ROW_BYTES` — **both `None` until the first default-root `load()`** (gate r6 f1: the pre-load value is stated so a module-state assertion is writable in either branch; `MAX_ROW_BYTES` is first published by `import policy_tool`'s `categories()` load — which the pin rig `tests/a1_corpus_rig.py` performs at **its own** import, before any test in this ticket's three files runs, so inside this ticket the branch is settled at `(<the default-root index>, 2789)` and the two `None`s are the module's initial state rather than a state a test observes; **gate r7 f1**, eligibility-assistant-D-66) — and returns it; `load(root=…)` on any other root **returns** the index it built and leaves module state untouched, so a `tmp_path` variant can never repoint `lookup` / `fetch_by_id` / `MAX_ROW_BYTES` at a torn-down tree for a later test file — `tests/test_a1_corpus.py` collects before `tests/test_a1_retriever.py`, whose `test_index_covers_every_row`, `test_in_process_read_only_capped` and `test_cap_binds_on_a_legal_call` all read that state — all three through the one `policy_index` object `tests/a1_corpus_rig.py` pins, which is what makes "that state" a single object at all (**gate r7 f1**, eligibility-assistant-D-66) — and `lifecycle`'s eligibility-assistant-D-57 variants mutate rows rather than copying them byte-identically; a test that needs `lookup` / `fetch_by_id` to serve a variant installs the index `load(root=…)` returned as `policy_index._INDEX` through `monkeypatch` for its own duration — `lifecycle`'s `tests/a1_rig.py` row is the one such site, and it is restored at teardown. `MAX_ROW_BYTES` is set by `load()` as the maximum over index rows of the UTF-8 byte total of every field value the row carries — `id`, `title`, `section`, `version`, `retrieval_date` and `section-text` — 2,789 today (`DOC-FED-COB-PROVIDER-SERVICES`: file 2,448 + metadata 341; package `document-manifest.json` and files, summed 2026-08-27), composed by `lookup(topic, payer, product, state)` (in-process, read-only, no network), `fetch_by_id(ids)` (the application-side entry the reason table uses), `tier(document)` (eligibility-assistant-SPEC-7/9/10/43 / eligibility-assistant-D-13, eligibility-assistant-D-32, eligibility-assistant-D-35 amended, eligibility-assistant-D-36, eligibility-assistant-D-38)
- `services/ai-assistant/app.py` — a FastAPI lifespan startup hook (none exists today, `app.py:51`) calls `policy_index.load()` so a sha mismatch or an unlisted file fails the container at boot; `policy_index` also loads lazily on first use for direct module callers; `app.py` also gains a **dark `import policy_tool`** (no call site — `llm-seam` gate r3 f2 class: CI's `services` smoke is `python -c "import app"` (`.github/workflows/ci.yml:76`) after `pip install -r services/ai-assistant/requirements.txt` (`:73`), so only what `app` imports is proven against the service requirements; today `app.py:30-36` imports `config`, `logging_config`, `eligibility_client`, `llm_client`, `templates`, `visit_templates`, `schemas`, none of which reach `langchain_core`); **because `policy_tool` builds its `topic` enum at import through `policy_index.load()`, `import app` now loads and sha-verifies the corpus at import (gate r4 f4; eligibility-assistant-D-35 note 2)** — the CI `services` smoke therefore reddens on a corpus fault before any hook, and the lifespan hook's own `load()` call at boot is a second, independent verification (the one `test_startup_hook_fails_boot_on_corpus_error` pins by monkeypatching `load`); nothing else in `app.py` changes on this ticket — the request path is not wired (eligibility-assistant-SPEC-7 / eligibility-assistant-D-35 amended 2026-08-26, note 2 2026-08-27)
- `services/ai-assistant/.dockerignore` — gains **`**/.DS_Store`** (today's three entries `__pycache__/`, `*.pyc`, `.venv/` stay; file read 2026-08-27, three lines) so the image built by `COPY . .` (`Dockerfile:8`, `WORKDIR /app` `:3`; build context `./services/ai-assistant`, `docker-compose.yml:223`) carries no Finder artefact under the corpus roots. **Pattern (gate r4 f2):** Docker matches `.dockerignore` lines with Go `filepath.Match` against the context-relative path and `**` is the only any-depth wildcard, so a bare `.DS_Store` is root-only and never matches `policy_corpus/documents/.DS_Store` — unlike `.gitignore:6`, where the bare name matches at any depth. Belt to the scan exemption's braces (**gate r2 f3**; eligibility-assistant-SPEC-7 / eligibility-assistant-D-63)
- `services/ai-assistant/policy_tool.py` — NEW: `make_policy_lookup(payer, product, state)` returns the one-argument `policy_lookup(topic)` LangChain tool bound to the turn's selections. **Schema mechanism (gate r1 f4):** the tool is built from an *explicit* `args_schema` — a pydantic model `PolicyLookupArgs` with `model_config = ConfigDict(extra="forbid")` and the single field `topic: Literal[*policy_index.categories()]` — the `Literal` is **built at import from the loaded manifest's distinct `category` values** (`policy_index.categories()`, a sorted tuple; 25 today — package `document-manifest.json`, 2026-08-27) — `categories()` runs the sha-verifying `load()`, so importing this module (and therefore `app`) reads and verifies the corpus (gate r4 f4) — never a hand list, and pinned both ways by `tests/test_a1_retriever.py::test_topic_enum_equals_manifest_categories` (**gate r3 f3**) — passed to `StructuredTool.from_function(..., args_schema=PolicyLookupArgs)`, **not** the `@tool`-inferred schema: in the pinned `langchain-core==1.6.0` the inferred subset model is rebuilt by `utils/pydantic._create_subset_model_v2` with `ConfigDict(arbitrary_types_allowed=True)` only, so `tools/base.py`'s `_SchemaConfig.extra = "forbid"` does not survive and `StructuredTool._parse_input` validates-then-dumps the declared fields — an **extra key is silently dropped, not rejected** (wheel inspected at gate r1). With the explicit schema all three SPEC-9 clauses hold by one mechanism: an extra argument raises `ValidationError` (`extra="forbid"`), and a free-text value or a document id offered as `topic` raises on the `Literal` (eligibility-assistant-SPEC-9 / eligibility-assistant-D-25)
- `services/ai-assistant/config.py` — `a1_retrieval_max_rows` (`A1_RETRIEVAL_MAX_ROWS`, default **5**, clamped ≥ 1; sized against the binding byte gate `LLM_MAX_INPUT_TOKENS` 20,000 (`config.py:64`) per eligibility-assistant-D-64 as amended 2026-08-27 — 5 × `MAX_ROW_BYTES` 2,789 B (text **and** row metadata) + `PROMPT_RESERVE_BYTES` 5,000 = 18,945 ≤ 20,000; 6 would give 21,734 — and below the largest legal result set (7 rows), so the cap binds on a legal call; **gate r3 f1 / f2, r4 f1**) (eligibility-assistant-SPEC-10 / eligibility-assistant-D-64)
- `services/ai-assistant/requirements.txt`, root `requirements-dev.txt` (the file CI's `tests` job installs, `.github/workflows/ci.yml:86`) — `langchain==1.3.16`, `langchain-core==1.6.0`, `langgraph==1.2.11` (the E-4 pins; `langsmith==0.10.5` stays — the composition is measured offline in E-5, the live leg under it is a named residual); `policy_tool.py` imports `langchain_core.tools`, so the first ticket that needs them is this one and the pins land here; `llm-seam` consumes them through `Depends on: corpus` (eligibility-assistant-SPEC-9 / eligibility-assistant-D-2, eligibility-assistant-D-37, eligibility-assistant-D-59, E-5)
- `tests/fixtures/a1/eligibility-assistant-evaluations.jsonl` — NEW, verbatim (eligibility-assistant-D-10, eligibility-assistant-D-35)
- `tests/fixtures/a1/case_selections.json` — NEW, curated: for each of the 27 harness cases naming a `DOC-*` / `procedures/*` source, its `question_type`, `payer`, `product`, `state` (eligibility-assistant-D-36 enums) and `topic` (a manifest category), read from the case scenario; consumed by `retrieval-eval` (recall) and `turn` (the rig), landed here so neither depends on the other (eligibility-assistant-D-58)
- `tests/fixtures/a1/fix_neg/FIX-NEG-*.md` — NEW ×7, verbatim, tests-only home (eligibility-assistant-SPEC-8/38 / eligibility-assistant-D-6)
- `tests/a1_corpus_rig.py` — NEW module-level pin rig, imported by bare name from this ticket's three test files (`tests/` is on `sys.path` for a test module — `tests/test_ai_visit_chat.py:38-39` already does `import conftest` / `from conftest import load_module`) (**gate r7 f1**, eligibility-assistant-D-66): there is no shared package (ADR 0001) and `tests/conftest.py:16-26`'s `load_module(relpath, name)` puts the service directory on `sys.path` (`:21-22`) and executes the file under a **unique** module name, so two test files that each load `policy_index.py` get two module objects with their own `_INDEX` / `MAX_ROW_BYTES`, while `app`'s `import policy_index` / dark `import policy_tool` (change row above) resolve by **bare** name — an unpinned load executes a further copy. The rig runs the house idiom once (`tests/test_ai_visit_chat.py:41-73` — save and pop the bare names, pin ai-assistant's copies, load the dependent module, restore; `CLAUDE.md:138-140` "pin `sys.modules` first"): it saves and pops the eight bare names that file pins (`config`, `logging_config`, `schemas`, `llm_client`, `templates`, `visit_templates`, `breaker`, `eligibility_client` — `tests/test_ai_visit_chat.py:41-50`; `app.py:30-36` imports **seven** of them — `config`, `logging_config`, `eligibility_client`, `llm_client`, `templates`, `visit_templates`, `schemas` — and `breaker` is `eligibility_client.py:29`'s import (`from breaker import CircuitBreaker, EligibilityBreakerOpen`), never `app.py`'s (its two `breaker` hits, `:508` and `:848`, are comments); that file pins it so the copy `eligibility_client` binds is the pinned one, and this rig keeps the same eight for the same reason — **gate r8 f1**, re-read 2026-08-27) plus `policy_index` and `policy_tool`; pins ai-assistant's `config` / `logging_config` first, then `sys.modules["policy_index"] = load_module("services/ai-assistant/policy_index.py", "a1_policy_index")` and `sys.modules["policy_tool"] = load_module("services/ai-assistant/policy_tool.py", "a1_policy_tool")`, then `app_mod = load_module("services/ai-assistant/app.py", "a1_corpus_app")` — whose imports therefore bind the pinned objects — and restores every saved name. Loading `app` at rig import is keyless and offline, as it already is at `tests/test_ai_visit_chat.py:68`. It exports `policy_index`, `policy_tool`, `app_mod` and `settings` (the pinned `config.settings`, the one object both `policy_index` and `app` hold, so a cap `monkeypatch` is seen by both). **First assertion of every test in the three files:** `policy_tool.policy_index is policy_index` and `policy_index.settings is app_mod.settings`; `tests/test_a1_corpus.py` adds `app_mod.policy_index is policy_index` and `app_mod.policy_tool is policy_tool` — the identities its lifespan monkeypatch rests on — so an unpinned second copy reddens before any `load`, `lookup` or `TestClient` (verification 4p), the shape `llm-seam` gate r3 f1 landed for the binding (`agent_binding.llm_client is llm_mod`, `llm-seam.md:15`). Because the `app_mod` load imports `policy_tool`, whose `categories()` runs a default-root `load()`, the rig's own import is what publishes `_INDEX` / `MAX_ROW_BYTES`. `turn`'s `tests/a1_rig.py` (`turn.md:25`) is a different, later file — the Bedrock and payer fakes and the selection helpers — and imports this one for the module set rather than loading a second copy (eligibility-assistant-SPEC-7/9/10/43 / eligibility-assistant-D-66)
- `tests/test_a1_corpus.py` — NEW (**rig, gate r7 f1**: the file imports `tests/a1_corpus_rig.py` for `policy_index` / `policy_tool` / `app_mod` / `settings`, and every test opens with that row's four identity assertions, so the lifespan monkeypatch and the module-state snapshots below act on the one pinned object — eligibility-assistant-D-66): `test_manifest_sha_pinned` (every vendored file's sha256 == manifest `content_sha256`; an unlisted file under the corpus path fails — `.DS_Store` the one exempt name, eligibility-assistant-D-63), `test_eval_031_fixture_isolation[EVAL-031]` (no `FIX-NEG-*` id, path, or content hash in the corpus, the index, or the manifest's approved set), `test_fix_neg_negative[7 ids]` (each fixture fed to `policy_index.lookup` / `fetch_by_id` as an unlisted document is rejected, and its text never appears in any returned row), `test_non_approved_row_fails_load` (**gate r2 f5** — a `tmp_path` copy of the vendored corpus through `load(root=…)` with one manifest row's `approval_status` set to `pending` raises the loader's error before any lookup; the same copy with `documents/.DS_Store` touched loads clean — the one exempt name, eligibility-assistant-D-63; **gate r5 f3, restated at gate r6 f1** — both legs assert `policy_index` module state as a before/after snapshot: `(policy_index._INDEX, policy_index.MAX_ROW_BYTES)` read before the `load(root=…)` call is element-wise `is`-identical after it — `(<the default-root index>, 2789)` in these files, since the rig publishes at its own import (**gate r7 f1**), with `(None, None)` the module's pre-load state the same snapshot form still covers; the snapshot is what "unchanged across the call" means and is writable in either branch, where the earlier wording pinned `2,789` in a branch that has no `MAX_ROW_BYTES` yet), `test_load_with_root_does_not_publish_module_state` (**gate r5 f3** — a default-root `load()` first, then `load(root=tmp_path_copy)` with one row's text truncated; the returned index carries the truncated row while `policy_index.lookup` / `fetch_by_id` still serve the vendored row and `MAX_ROW_BYTES` is still 2,789), `test_startup_hook_fails_boot_on_corpus_error` (**gate r1 f5** — the boot-fail hook is pinned, not left to hand verification: `with TestClient(app_mod.app)` against a `policy_index.load` monkeypatched to raise the loader's own error — `app_mod` and `policy_index` both from `tests/a1_corpus_rig.py`, whose `app_mod.policy_index is policy_index` assertion is what makes the patch reach the hook (**gate r7 f1**) (the type is `policy_index.CorpusLoadError`, named and landed here; `lifecycle` reuses it for its lifecycle failures — **`lifecycle` gate r1 f7**) → the context manager raises out of the lifespan and no request is served; the same `with` block against the real vendored corpus enters and exits clean. This is the first ai-assistant rig to use `with` — every existing one constructs `TestClient(app_mod.app, …)` bare, so lifespan never runs there and the retained tests are unaffected) (eligibility-assistant-SPEC-7/8/38 / eligibility-assistant-D-35 amended)
- `tests/test_a1_retriever.py` — NEW (**rig, gate r7 f1**: imports `tests/a1_corpus_rig.py` for `policy_index` / `policy_tool` / `settings`, every test opening with `policy_tool.policy_index is policy_index` and `policy_index.settings is app_mod.settings`, so the tool under test and the index it reads are the objects the cap monkeypatches and the `_INDEX` reads act on — eligibility-assistant-D-66): `test_tool_arg_topic_only_app_binds_rest`, `test_tool_rejects_extra_keys` (**gate r1 f4** — the negative half of SPEC-9, one case per clause: invoking the tool with `{"topic": <valid>, "payer": "aetna"}` raises `ValidationError` on the extra key rather than silently dropping it, and the call is asserted **not** to have reached `policy_index.lookup`; `{"topic": "DOC-SYN-EMERGENCY"}` and `{"topic": "is this covered today?"}` each raise on the `Literal`; the test asserts on the tool's declared `args_schema.model_config["extra"] == "forbid"` as well as on the raise, so replacing the explicit schema with the `@tool`-inferred one reddens it — verification 4d), `test_in_process_read_only_capped` (**gate r2 f4** — one named mechanism per SPEC-10 clause, for the duration of one `lookup` and one `fetch_by_id`: *no network* — `socket.socket` monkeypatched to raise; *in-process* — `subprocess.Popen` and `os.fork` monkeypatched to raise the same way; *read-only* — `builtins.open` monkeypatched to raise on any mode containing `w`, `a`, `x` or `+`, and the sha256 of every file under the corpus root asserted equal before and after; *capped* — a **legal** call (**gate r3 f2**): `topic="coordination-of-benefits"`, `payer="medicare"`, `product` / `state` `unconfirmed` → 7 candidate rows (`DOC-FED-COB-*` ×4, `DOC-FED-MSP-CRS`, `DOC-FED-MEDIGAP`, `DOC-SYN-COB-FRONT-DESK`; package read 2026-08-27) returns exactly `settings.a1_retrieval_max_rows` = 5 rows, the first 5 of `rank`'s order; returns 7 with `a1_retrieval_max_rows` monkeypatched to 7 and 1 with it monkeypatched to 1 — asserted on `len(rows)` and the ids, in this ticket's own terms: the `LookupRecord` and its `truncated` flag are `retrieval-eval`'s row (SPEC-63, eligibility-assistant-D-58) and land after this ticket (**gate r4 f3**); and `payer="*"` raises `ValueError` before any row is read), `test_cap_binds_on_a_legal_call` (**gate r3 f1 / f2, r4 f1**, eligibility-assistant-D-64 as amended 2026-08-27: (i) `settings.a1_retrieval_max_rows × policy_index.MAX_ROW_BYTES + policy_index.PROMPT_RESERVE_BYTES ≤ settings.llm_max_input_tokens`, with `MAX_ROW_BYTES` **recomputed by the test itself** from the vendored manifest and files — max over rows of the UTF-8 bytes of `document_id` + `title` + `section_labels` + `version_effective` + `retrieval_date` + file — and asserted equal to the module's value and to the pinned 2,789, so the bound is executable from the tree and a metadata or file growth that breaks it reddens here, not in `turn`; (ii) the test drives **`policy_index._filter` over the loaded index `policy_index._INDEX`** — the module state the rig publishes at import, never a re-parse of `index.json` from disk (**gate r7 f2**) — once per enum combination (topic × payer × product × state), and the maximum candidate count over those combinations exceeds `settings.a1_retrieval_max_rows`: the cap has a legal exercise. So (i) is asserted against the tree as vendored, by the test's own recompute from the manifest and files, and (ii) against the loaded index — which is what makes verification 4l's second break, a `monkeypatch` of `_INDEX`, redden (ii) with (i) still green), `test_topic_enum_equals_manifest_categories` (**gate r3 f3**: the set of `PolicyLookupArgs.topic` `Literal` args == the set of manifest `category` values == the set of `index.json` `topics`, both directions, 25 today — every set taken over every approved manifest row whatever its lifecycle status, eligibility-assistant-D-80), `test_unconfirmed_axis_non_filtering[EVAL-023]`, `test_index_covers_every_row` (every approved row indexed under at least one argument set, **and** the row shape of eligibility-assistant-D-61: exactly 87 rows, each row's `section` equal to its manifest `section_labels` string and its `section-text` equal to the vendored file's text — a heading split or a `|` split moves the count off 87 and reddens it), `test_case_selections_closed` (every selection value is in its enum, every topic a manifest category, every case id in the vendored jsonl, exactly the 27 cases that name a `DOC-*` / `procedures/*` source) (eligibility-assistant-SPEC-9/10/11; eligibility-assistant-D-58)
- `tests/test_a1_conflict.py` (1a half) — NEW (**rig, gate r7 f1**: imports `tests/a1_corpus_rig.py` for `policy_index` and opens every test with `policy_tool.policy_index is policy_index` / `policy_index.settings is app_mod.settings`, so `tier` and `rank` are the pinned module's — eligibility-assistant-D-66): `test_tier_rank_in_code` (tier function pinned on named ids **and over the whole manifest** (**gate r3 f5**, eligibility-assistant-D-38 amendment note 2): the partition of the 87 vendored rows by `tier(document)` is exactly 9 / 32 / 16 / 23 / 7, tier 1 is exactly the nine `DOC-FED-EMTALA-*` / `DOC-FED-HIPAA-164-*` ids (so `DOC-FED-EMTALA-CMS` — the one public-domain id on the eligibility-assistant-D-19 `emergency` row — stays tier 1; the row's other citations, `DOC-SYN-EMERGENCY` and, on question type `emergency`, `DOC-COVERAGE-QUESTION-CHEAT-SHEET` (contract `:205`), are **tier 4** — `emergency-care-boundary` and `front-desk-procedure` × "Original synthetic training material", package `document-manifest.json` read 2026-08-27 — and the reason table cites by id, never by tier, so the mixed-tier row is by design (**gate r6 f2**)), `DOC-SYN-EMERGENCY` and `DOC-SYN-PRIVACY-FD` are tier 4, and the tier function reads `license_disposition` prefix × `category` only — an id-fragment rule reddens on `DOC-FED-EMTALA-CMS`; order by rank — the input is fed in **reversed manifest order** and the output asserted in eligibility-assistant-D-62's key, including one same-tier pair ordered by `document_id`, so an implementation that falls through to input order reddens (gate r2 f2); **EVAL-006's stale set is tier-4-only** — its expected source `DOC-SYN-CITATION-RECENCY` is category `source-recency`, license "Original synthetic training material", so eligibility-assistant-D-38 as amended 2026-08-27 assigns it tier 4, not tier 5; tier 5 is category `payer-training-summary` alone (7 rows). `refuse_definitive` still follows from D-38's "no row of tier ≤ 3", unchanged. EVAL-024 regulation ranks above the cheat sheet) (eligibility-assistant-SPEC-43 / eligibility-assistant-D-38 amended); the EVAL-007/013/020/023 turn tests join in 1c
- `adr/0019-model-directed-policy-retrieval-on-the-invoke-model-seam.md` — NEW, `Accepted`, `Debt: D13`; **this ticket authors the frame and its own paragraph only** — context, the decision list as eligibility-assistant-D-42 states it, `Debt: D13`, and the deterministic retriever / index / tier-rule paragraph (the mechanisms this ticket's diff lands); every later ticket appends the paragraph for the mechanism its own diff lands, each as a change row in that ticket's plan file — `llm-seam` (seam binding, `SystemMessage` → `system`, twin guard, never-stream), `turn` (bounded loop, outcome derivation), `trace` (trace shape), `lifecycle`, `retrieval-eval` — so no review-reopenable decision's record rests on a plan file that is deleted at merge (**`llm-seam` gate r6 f1**, eligibility-assistant-D-42 note 2026-08-27); `adr/0006-*.md` and `adr/0011-*.md` Status lines gain `Extended by ADR 0019 (eligibility-assistant)` (eligibility-assistant-D-42)
- `tests/README.md` — "AI paths" bullet (`tests/README.md:32`) gains **this ticket's three files only**: `test_a1_corpus.py`, `test_a1_retriever.py`, `test_a1_conflict.py`; each later ticket that lands a new test file carries its own row (`llm-seam` gate r3 f3 class — sweep: `llm-seam.md` row added, `turn.md` row added, `lifecycle.md:30` and `retrieval-eval.md:20` already present (the `lifecycle` row was `:21` when this sweep ran and moved to `:30` with `lifecycle`'s gate r1 disposition, 2026-08-27) (**gate r5 f5** — `retrieval-eval.md:19` is that ticket's `adr/0019-*.md` row; the `tests/README.md` row shifted to `:19` when gate r4 f3's disposition inserted a `tests/test_a1_retriever.py` row above it, and to `:20` when `retrieval-eval` gate r1 f1's disposition inserted a `tests/test_a1_corpus.py` row above that, 2026-08-27), `trace.md` lands no new file) (registry upkeep)
- `CLAUDE.md` §6 — baseline line (`:141`, `1317 passed, 19 deselected, 1 xfailed`) updated to the post-ticket count; deselected and xfailed do not move and no `skipped` term appears yet (falsified-claims sweep — this ticket's additions falsify the stated count; eligibility-assistant-SPEC-40 / eligibility-assistant-D-43)

Verification (runnable, expected output stated; numbering carried from the item plan):
1. `.venv/bin/python -m pytest tests/test_a1_corpus.py tests/test_a1_retriever.py tests/test_a1_conflict.py -q` → all passed, 0 failed; `EVAL-031` and `EVAL-023` visible in `-v` ids (eligibility-assistant-SPEC-7/8/9/10/11/38/43; 1a)
2. sha pin, out of band: `cd services/ai-assistant/policy_corpus && python3 -c "import json,hashlib,sys; m=json.load(open('document-manifest.json'))['documents']; bad=[d['document_id'] for d in m if hashlib.sha256(open(d['path'],'rb').read()).hexdigest()!=d['content_sha256']]; print(len(m), 'rows', len(bad), 'mismatched')"` → `87 rows 0 mismatched` (eligibility-assistant-SPEC-7; 1a)
3. break-then-revert: append one byte to `policy_corpus/documents/synthetic/DOC-SYN-EMERGENCY.md` → `tests/test_a1_corpus.py::test_manifest_sha_pinned` red; `git checkout -- services/ai-assistant/policy_corpus` → green (eligibility-assistant-SPEC-7; 1a)
4. break-then-revert: `cp tests/fixtures/a1/fix_neg/FIX-NEG-INVENTED-COVERAGE.md services/ai-assistant/policy_corpus/documents/synthetic/` → `test_eval_031_fixture_isolation[EVAL-031]` red **and** `test_manifest_sha_pinned` red (unlisted file); remove → green (eligibility-assistant-SPEC-8; 1a)
4b. break-then-revert (scan scope, gate r2 f2): `touch services/ai-assistant/policy_corpus/README.md` → `test_manifest_sha_pinned` red (a third root-level file); `rm` it → green; `mkdir -p services/ai-assistant/policy_corpus/notes && touch services/ai-assistant/policy_corpus/notes/rogue.md` → the same test red (an unlisted file under a subdirectory that is no manifest path root — the nested case, **gate r6 f3**); `rm -r services/ai-assistant/policy_corpus/notes` → green; `touch services/ai-assistant/policy_corpus/documents/.DS_Store` → still green (the one exempt name, eligibility-assistant-D-63; gate r2 f3), `rm` it; and `cd services/ai-assistant && ../../.venv/bin/python -c "import policy_index; policy_index.load(); print('ok')"` → `ok` on the tree as vendored, with `index.json` and `document-manifest.json` present at the root (the two named exemptions) (eligibility-assistant-SPEC-7; corpus)
4c. `cd services/ai-assistant && ../../.venv/bin/python -c "from fastapi.testclient import TestClient; import app; TestClient(app.app).__enter__(); print('booted')"` → `booted`; break-then-revert: append one byte to `policy_corpus/procedures/coverage-question-cheat-sheet.md` → the same command raises **at `import app`** (the import-time load through `policy_tool`, gate r4 f4) before `TestClient` is constructed — the traceback's innermost frame is `policy_index.load`, reached from `policy_tool`; `git checkout --` → `booted`. This command therefore proves the import-time verification; the **hook** path is proven separately by `test_startup_hook_fails_boot_on_corpus_error` (verification 4e), whose monkeypatched `load` is reached only from the lifespan (eligibility-assistant-SPEC-7 / eligibility-assistant-D-35 amended, note 2; corpus)
4d. break-then-revert (tool schema, gate r1 f4): in `policy_tool.py` drop the explicit `args_schema=PolicyLookupArgs` and let `langchain-core` infer the schema from the function signature → `tests/test_a1_retriever.py::test_tool_rejects_extra_keys` red on the extra-key case (the key is dropped, no raise, and the `args_schema.model_config` assertion fails); restore the explicit schema → green (eligibility-assistant-SPEC-9; corpus)
4e. break-then-revert (boot-fail hook is pinned, gate r1 f5): delete the `lifespan=` argument from the `FastAPI(...)` construction in `services/ai-assistant/app.py` → `tests/test_a1_corpus.py::test_startup_hook_fails_boot_on_corpus_error` red (the `with TestClient(...)` block enters even though `load` raises); `git checkout -- services/ai-assistant/app.py` → green (eligibility-assistant-SPEC-7; corpus)
4f. `git ls-files services/ai-assistant/policy_corpus/index.json services/ai-assistant/policy_corpus/document-manifest.json | wc -l` → `2` (SPEC-11 "tracked artifact" — the clause no test can prove; gate r2 f4 class sweep) (eligibility-assistant-SPEC-11; corpus)
4g. break-then-revert (SPEC-10 clauses, gate r2 f4): in `policy_index.lookup` write a one-line cache file under `policy_corpus/` → `tests/test_a1_retriever.py::test_in_process_read_only_capped` red (write-mode `open` raises; the digest would differ); revert → green. Second break: have `lookup` call `subprocess.run(["true"])` → the same test red (`Popen` raises); revert → green (eligibility-assistant-SPEC-10; corpus)
4h. break-then-revert (approval gate, gate r2 f5): delete the `approval_status` check from `policy_index.load` → `tests/test_a1_corpus.py::test_non_approved_row_fails_load` red (the `pending` row loads); revert → green (eligibility-assistant-SPEC-7 / eligibility-assistant-D-63; corpus)
4i. break-then-revert (rank key, gate r2 f2): drop the `document_id` key from `rank` → `tests/test_a1_conflict.py::test_tier_rank_in_code` red (reversed input, same-tier pair comes out reversed); revert → green (eligibility-assistant-SPEC-43 / eligibility-assistant-D-62; corpus)
4j. break-then-revert (row shape, gate r2 f1): split rows on `section_labels` `|` in `policy_index.load` → `tests/test_a1_retriever.py::test_index_covers_every_row` red (142 rows, not 87); revert → green (eligibility-assistant-SPEC-10 / eligibility-assistant-D-61; corpus)
4k. `cd services/ai-assistant && touch policy_corpus/documents/.DS_Store && docker build -q -t a1-corpus-check . >/dev/null && docker run --rm a1-corpus-check find /app/policy_corpus -name .DS_Store | wc -l; rm policy_corpus/documents/.DS_Store` → `0` (the image carries no `.DS_Store` at any depth; `WORKDIR /app`, `Dockerfile:3`); break-then-revert (**gate r4 f2**): change the `.dockerignore` line to a bare `.DS_Store` → the same command prints `1` (root-only pattern; the nested file is copied); restore `**/.DS_Store` → `0` (eligibility-assistant-SPEC-7 / eligibility-assistant-D-63; corpus)
4l. break-then-revert (cap sizing, gate r3 f1 / f2, r4 f1, eligibility-assistant-D-64 as amended): set the `config.py` default to 6 → `tests/test_a1_retriever.py::test_cap_binds_on_a_legal_call` red on (i) (6 × 2,789 + 5,000 = 21,734 > 20,000 — the row-metadata term is what reddens it; the pre-r4 file-bytes rule would have passed at 19,688); revert → green. Second break (**gate r5 f4**): **no cap default isolates (ii)** — rule (i) is asserted first and fails for every cap ≥ 6 (7 × 2,789 + 5,000 = 24,523 > 20,000), while (ii) needs cap ≥ 7, so a default change can only ever redden (i). (ii) is reddened by the corpus/index side, exactly as eligibility-assistant-D-64 anticipates ("a re-curation or `lifecycle` change that drops every legal set to ≤ cap reddens (ii) deliberately"): monkeypatch `policy_index._INDEX` — the loaded index rule (ii) drives `_filter` over (**gate r7 f2**) — to an index whose `rows` are the four `eligibility-verification` rows only (its `categories` untouched — the `rows` / `categories` shape of eligibility-assistant-D-80) → the largest legal argument set is 4 ≤ 5 and the same test red on (ii), with (i) still green (it recomputes `MAX_ROW_BYTES` from the manifest and files on disk and compares it to the module's, neither of which the `_INDEX` patch moves) (package `document-manifest.json`, per-category counts 2026-08-27: the only categories above 5 rows are `coordination-of-benefits` 7, `medicaid-managed-care` 7, `payer-training-summary` 7, `emergency-care-boundary` 6 and `payer-eligibility-public` 14); revert → green. Third break: make `MAX_ROW_BYTES` count `section-text` only → the same test red (module value 2,448 ≠ the test's recomputed 2,789); revert → green. Fourth break: let `_filter` accept `"*"` as a query value → `::test_in_process_read_only_capped` red (the `payer="*"` call returns rows instead of raising); revert → green (eligibility-assistant-SPEC-9/10; corpus)
4m. break-then-revert (enum source, gate r3 f3): replace `Literal[*policy_index.categories()]` in `policy_tool.py` with a hand-written `Literal` missing `source-recency` → `tests/test_a1_retriever.py::test_topic_enum_equals_manifest_categories` red (manifest ⊄ `Literal`); revert → green (eligibility-assistant-SPEC-9; corpus)
4n. break-then-revert (tier rule, gate r3 f5): in `policy_index.tier` classify tier 1 by the id fragments `42CFR` / `EMTALA-42CFR` / `HIPAA-164-` instead of license × category → `tests/test_a1_conflict.py::test_tier_rank_in_code` red (`DOC-FED-EMTALA-CMS` falls to tier 2 — it carries neither fragment, as do `-CRS`, `-ER-RIGHTS` and `-OIG`; only `DOC-FED-EMTALA-42CFR-489-24` and the four `DOC-FED-HIPAA-164-*` ids (`-502-MIN-NEC`, `-514`, `-506-TPO`, `-508-AUTH`) keep tier 1 by fragment (**gate r6 f2** — 9 − 4 + 2 = 7, the partition's own arithmetic; the r5 f1 cell had it right and this site did not) — while `DOC-FED-42CFR-435-POINTER` and `DOC-FED-42CFR-438-POINTER` are promoted **into** tier 1 on the bare `42CFR` fragment though both are `category: medicaid-managed-care` and tier 2 under the rule; partition **7 / 34 / 16 / 23 / 7**, recomputed over all 87 rows from the package `document-manifest.json` 2026-08-27 against the rule's 9 / 32 / 16 / 23 / 7 — **gate r5 f1**, the third round to correct a tier figure stated without re-deriving it); revert → green (eligibility-assistant-SPEC-43 / eligibility-assistant-D-38 amendment note 2; corpus)
4o. break-then-revert (module state, gate r5 f3): make `policy_index.load` assign `_INDEX` and `MAX_ROW_BYTES` on **every** call, `root` or not → `tests/test_a1_corpus.py::test_load_with_root_does_not_publish_module_state` red (`lookup` serves the truncated row and `_INDEX` is no longer the default-root object) and `::test_non_approved_row_fails_load` red on its `.DS_Store` leg (the clean variant load moved module state); revert → green. The break is confined to `policy_index.py`, so a `tests/test_a1_retriever.py` run alone stays green either way — the isolation is the point: the leak only shows across files, which is why the state assertion lives in the file that collects first (eligibility-assistant-SPEC-7/10 / eligibility-assistant-D-57; corpus)
4p. break-then-revert (pin rig, gate r7 f1, eligibility-assistant-D-66): drop the `sys.modules["policy_index"] = …` pin from `tests/a1_corpus_rig.py`, keeping its `load_module` call → every test in `tests/test_a1_corpus.py`, `tests/test_a1_retriever.py` and `tests/test_a1_conflict.py` red on its first assertion `policy_tool.policy_index is policy_index` (loading `policy_tool` executed a second `policy_index` with its own `_INDEX` / `MAX_ROW_BYTES`); the identity check precedes every `load` / `lookup` / `TestClient`, so nothing is read or served before the raise; restore the pin → green. Second break: drop the `sys.modules["policy_tool"] = …` pin → `tests/test_a1_corpus.py` red on `app_mod.policy_tool is policy_tool` (`app`'s dark import executed a second `policy_tool`, with its own import-time `categories()` load); restore → green. This is `llm-seam` verification 7f's shape for the corpus module set (eligibility-assistant-SPEC-7/9/10; corpus)
5. `cd services/ai-assistant && env -i PATH="$PATH" ../../.venv/bin/python -c "import app, policy_index, policy_tool; print('ok')"` → `ok` (keyless, offline import — the CI `services` smoke shape; from this ticket the import reads and sha-verifies the vendored corpus on disk (gate r4 f4) and still makes no network call; begins on this ticket and repeats on `llm-seam` with `agent_binding` and on `turn` with `agent_turn` / `outcome`) (eligibility-assistant-SPEC-7/9; corpus)
8. `.venv/bin/python -m pytest -m "not integration" --collect-only -q | grep -o 'EVAL-0[0-9][0-9]\(-[a-z]*\)*' | sort -u | wc -l` → `2` after this ticket (`EVAL-031` from the corpus gate, `EVAL-023` from the retriever test — the only two bracketed case ids it lands). SPEC-37 is `turn`'s row (eligibility-assistant-D-55); the count is pinned here so it cannot move silently (eligibility-assistant-SPEC-37 as amended by eligibility-assistant-D-53; corpus)
9. `.venv/bin/python -m pytest -m "not integration" -q` → `<Baseline at branch: passed + N> passed, 19 deselected, 1 xfailed`, with `N` = this ticket's additions reported deliberately in Delivery (CLAUDE.md §6); **no `skipped` term yet** — the live tests land in `turn` (eligibility-assistant-D-43). Deselected and xfailed never move. The claim-worthy run is `make test-docker` with the same numbers (eligibility-assistant-SPEC-40; corpus)
10. `docker run --rm -v "$PWD:/repo" zricethezav/gitleaks:v8.18.4 detect --source=/repo --config=/repo/.gitleaks.toml --no-git --redact --exit-code 1` → exit 0 over the newly vendored corpus, the harness jsonl and the seven `FIX-NEG-*` fixtures (the CI `secret-scan` shape, `.github/workflows/ci.yml:120-129`; this ticket is the one that grows that scan's surface) (corpus)
19. `git diff --stat main -- db/ config/ services/gateway/authz.py services/gateway/security.py services/eligibility-service/ services/ai-assistant/eligibility_client.py docker-compose.yml eval/` → empty (structural exclusion, every ticket) (corpus)

Gate interactions (existing steps that already enforce part of a new check):

- CI `services` import smoke (`pip install -r services/ai-assistant/requirements.txt`, then `python -c "import app"` from the service dir — `.github/workflows/ci.yml:72-76`, keyless and offline): this ticket is the first to put `langchain_core` on that install, and the smoke proves only what `app` imports (`app.py:30-36` today) — so the proof exists **because** `app.py` gains the dark `import policy_tool` (change row above; `llm-seam` gate r3 f2 class); without it the job would install the pins and import nothing that uses them. `import app` runs **no** FastAPI lifespan hook — but it **does** load and sha-verify the corpus, because `policy_tool` builds its `topic` enum at import through `policy_index.load()` (gate r4 f4; eligibility-assistant-D-35 note 2): the smoke is offline (files on disk, no network) and now reddens on a corpus fault at import, ahead of the hook. Attribution: a red `services` job on a sha mismatch is the import-time load, not the lifespan; the hook is pinned by `test_startup_hook_fails_boot_on_corpus_error` and the two are told apart in verification 4c. Verification 5 is the same shape locally.
- CI `secret-scan` (gitleaks v8.18.4, `--no-git`, tracked tree — `ci.yml:120-129`): already scans the tracked tree, so the 87 vendored documents, the manifest, the harness jsonl and the seven `FIX-NEG-*` fixtures are scanned the moment they land — no new job, a larger surface. Verification 10 runs the same image before push.
- CI `docker-build` (`docker compose build`, `ci.yml:222` and the note above it: "these steps prove every image COMPILES, never that any of them runs"): the boot-fail hook is **not** exercised by CI. `test_startup_hook_fails_boot_on_corpus_error` is its only automated proof and verification 4c the hand check — named as a residual below.
- SPEC-37's collect-count cmd (`turn`'s row, eligibility-assistant-D-53): this ticket moves it from 0 to 2; verification 8 pins the value.
- The byte gate at `llm_client._call` (`_enforce_budget` on `max_input_tokens`, `llm_client.py:458-461` / `:494-495`, `LLM_MAX_INPUT_TOKENS` 20,000 `config.py:64`) is the constraint the row cap is sized under (eligibility-assistant-D-64): `corpus` guarantees the rows — text **and** the metadata fields each row carries, `MAX_ROW_BYTES` (gate r4 f1) — fit inside 20,000 − `PROMPT_RESERVE_BYTES`; `turn` owns proving its prompt + tool schema + turn messages + the per-row serialisation overhead for `cap` rows fit the reserve across both model calls. A `spend_stop` on a legal retrieval is therefore a `turn` sizing defect, never an expected outcome (gate r3 f1).
- The `rank(rows)` ordering site is landed here as its own function precisely so `retrieval-eval`'s SPEC-66 substitution test lands without a refactor (eligibility-assistant-D-58) — the interaction is one-way: nothing in this ticket depends on `retrieval-eval`.

## Landmines

This block is verbatim for **this ticket's own diff** and no other's (eligibility-assistant-D-60): it names the §1 zones the `corpus` change list enters, and a zone another ticket enters is that ticket's block to write, not a citation from here.

- **PHI handling (§1) — entered.** Three new PHI-adjacent code paths, all four of this ticket's ⚠ rows (eligibility-assistant-SPEC-7, 8, 9, 10):
  **(1) The vendored corpus itself (SPEC-7/8/38).** 87 client-supplied approved documents plus a verbatim `document-manifest.json` land inside `services/ai-assistant/`, and the seven `FIX-NEG-*` fixtures plus the harness jsonl land under `tests/`. No patient data, no PHI column and no clinical record is created: the package is public-domain regulation, citation-only bibliographic records, and original synthetic clinic material (E-2, E-3), and it is read-only on disk. What makes the boundary hold is that the corpus is a **closed set pinned by sha** — every vendored file's sha256 equals its manifest `content_sha256`, no unlisted file may exist anywhere under `policy_corpus/`, at any depth (gate r6 f3; the single exempt name is `.DS_Store`, a Finder artefact that is not a document and has no path into the index — eligibility-assistant-D-63), and a manifest row that is not `approved` fails the load rather than being dropped — so nothing can be added to what the assistant may cite without the manifest change being visible in the diff, and `FIX-NEG-*` text cannot enter the citable set by any path (`tests/test_a1_corpus.py::test_manifest_sha_pinned`, `::test_eval_031_fixture_isolation[EVAL-031]`, `::test_fix_neg_negative` ×7, `::test_startup_hook_fails_boot_on_corpus_error`). A mismatch fails the container at boot, before any turn is served.
  **(2) The model-facing retriever tool (SPEC-9, the one surface the model can call).** `policy_lookup` takes exactly one argument, `topic`, from the closed 25-value manifest-category enumeration; payer, product and state are bound by the **application** from the turn's clerk selections at tool-construction time and are never taken from the model. This is what makes REQ-3‴'s "the clerk's text never enters a query" true by construction rather than by assertion: there is no free-text argument to carry it, no document-id argument, and no key the model can add — the explicit `args_schema` with `extra="forbid"` rejects an extra key instead of silently dropping it (gate r1 f4; `tests/test_a1_retriever.py::test_tool_arg_topic_only_app_binds_rest`, `::test_tool_rejects_extra_keys`, verification 4d). This ticket lands the tool **dark**: it is not wired to the request path and no Bedrock call is made from any code it adds, so no payload leaves the process on this ticket at all.
  **(3) The by-id application entry on the same index (SPEC-7/9/10).** `fetch_by_id(ids)` exists so the eligibility-assistant-D-19 reason table can render fixed citations through the one bounded index (REQ-3″). It is reachable only from application code; it is deliberately **not** a tool argument and not exposed to the model, which is the whole reason the reason table can cite without giving the model an id channel. Each row the retriever returns is one whole vendored document with the manifest's own `section_labels` string as its section (eligibility-assistant-D-61): the assistant cites text the client labelled, never a boundary this code invented. The retriever is in-process, read-only and makes no network call, and returns at most `A1_RETRIEVAL_MAX_ROWS` rows — default 5, sized so the rows, text and metadata fields together (`MAX_ROW_BYTES`), fit inside the byte gate that actually bounds vendor egress (`LLM_MAX_INPUT_TOKENS` 20,000 bytes) with a named reserve for the prompt, and small enough to bind on a legal call (eligibility-assistant-D-64 as amended 2026-08-27; gate r3 f1 / f2, r4 f1); query values are enum members only, `*` never crosses from the model or the application into a query (`tests/test_a1_retriever.py::test_in_process_read_only_capped` — one guard per clause: `socket`, `subprocess`/`fork`, write-mode `open` plus a corpus digest, and the cap on a legal call; `::test_cap_binds_on_a_legal_call`; gate r2 f4, r3 f1 / f2).
  Approval of record for entering the zone: **eligibility-assistant-D-56** — the owner's §1 approval of code entry on eligibility-assistant-SPEC-7–10, 12, 20, 28–31 **as designed in this block**, given **2026-08-25** in session at plan review r1 (gate r1 f1), unconditional, and extended by the owner the same day to SPEC-60, 61, 63 (eligibility-assistant-D-48); registered as a decision 2026-08-26. It stands on the owner's freeze of the ⚠ rows REQ-3‴ and SPEC-7–10 (contract `## Spec` Status, 2026-08-24) and on the D13 gate decision eligibility-assistant-D-14 (nothing free-text reaches the model). This is the record the `.claude/skills/implementation/` entry checklist requires before code on these rows; a change to the mechanisms named here — the sha pin, the `topic`-only tool schema, the app-bound axes, the app-side by-id entry — re-opens it. **Re-opened and carried forward (gate r3 f7):** eligibility-assistant-D-61 (row shape, 2026-08-27), D-62 (rank key, 2026-08-27), D-63 (scan boundary and `approval_status`, 2026-08-27) and D-64 (cap sizing and query values, 2026-08-27) each changed a mechanism this block names after the 2026-08-25 approval; the owner's confirmation of each decision, on its date, is the act that carries D-56's approval forward to the block as now written — D-64 confirmed the same day. D-63's dated note of 2026-08-27 (whole-tree scan, gate r6 f3) widens the sha pin's scan without changing its rule; the owner's acceptance of that finding 2026-08-27 under the round-3 rule is the act that carries D-56 forward to it. **Re-opened and re-closed the same day by eligibility-assistant-D-80 (`lifecycle` gate r2 f2, proposed 2026-08-27, confirmed by the owner 2026-08-27 in session):** it fixes what the `topic`-only tool schema is built over — every approved manifest row, whatever lifecycle status — a mechanism this block names; the owner's confirmation of D-80 on 2026-08-27 is the act that carries D-56 forward to that clause, so no part of this approval stands re-opened.
  **Deliberate defects in reach are preserved.** D13 (no BAA on the vendor path) stays OPEN and is not narrowed or "fixed": the corpus is synthetic and public-domain, ADR 0011 constraint 4 is unchanged, and the payer answer remains the only coverage source — this ticket adds no vendor egress at all. The `README.md` HIPAA overstatement (TODO-12) is untouched, human-gated. The D1 intake body-logging and D3 plaintext-PHI-column defects are not touched by any file in this change list. D11 (session↔patient binding) is not in reach of this ticket and no chart route is edited.
  **Accepted residuals on this ticket's rows:** none beyond the four in the residual-honesty bullet below — this ticket persists nothing, logs no new field, and egresses nothing.
- **Secrets (§1) — not touched.** No `.env`, `.env.ai-proxy`, `.env.redis`, `.env.registration` or any other secret file is read or edited, and no key or token is vendored: what enters the tracked tree is 87 client documents, a manifest, a harness jsonl and seven negative fixtures. The new setting `A1_RETRIEVAL_MAX_ROWS` is a `config.py` default with a plain `os.getenv` read (`.env.example` is `turn`'s change row, not this ticket's). Because this is the ticket that grows the tracked tree by ~131 KB of vendored text, CI's `secret-scan` — gitleaks v8.18.4, `--no-git`, tracked-tree recurrence guard, `.github/workflows/ci.yml:120-129` — now scans all of it; **verification 10 runs the same image over the same tree before push**, so a credential-shaped string inside a client document is caught by us rather than by CI. The §1 note that `.env` secrets remain in git history is unaffected: nothing here rotates or scrubs history.
- **Migrations / schema (§1) — not touched.** Nothing under `db/` — no `db/schema.sql` edit, no new `db/migrations/00N_*.sql`, no `db/seed/` change. This ticket persists nothing: the corpus is files on disk inside the service image and the index is a tracked JSON artifact, so there is no hand-sync obligation and `tests/integration/test_schema_upgrade_path.py` is untouched.
- **Domain services network-internal (§1, ADR 0016) — not touched.** No `ports:` is added, no new service, no `docker-compose.yml` edit. The corpus ships inside the existing ai-assistant image through the Dockerfile's `COPY . .` (`.dockerignore` excludes `__pycache__/`, `*.pyc`, `.venv/` and, from this ticket, `**/.DS_Store` — any depth, gate r4 f2), so the image grows and the topology does not; `tests/test_compose_topology.py`'s allowlist is unchanged.
- **Auth / sessions (§1) — not touched.** No line of `services/gateway/` moves: `authz.py`, `security.py`, `config/roles.yaml`, `EXPECTED_ROUTE_CAPABILITIES` and `require_capability` are all outside this change list. This ticket lands dark and adds no route, no request-path branch and no caller.
- **IDOR on chart reads (§1, D11) — not touched.** No chart or records route is edited, no session is bound to a patient, and nothing here reads patient data at all; the D11 xfail stays visible.
- **Inline eligibility call (§1, ADR 0010) — not touched.** `services/ai-assistant/eligibility_client.py` and `services/eligibility-service/` are not edited; no timeout, retry or breaker value moves, and `tests/test_eligibility_budget_alignment.py` is untouched. The payer call's site is `turn`'s change, not this one.
- **ROI / disclosures, booking race, duplicate patients, HL7 mapping (§1) — not touched.** No file under `services/roi-service/`, `services/scheduling-service/`, `services/intake-service/` or `services/interop-service/` appears in this change list.
- **Intake registration (§1) — not touched.** No file under `services/intake-service/` is edited; `ConsentKind`, `proxy_intake` on `_post_checked` and `contracts/intake-registration.json` are outside this change list (`docs/landmines.md:87`).
- **Structural exclusion (this ticket).** Nothing changes under `db/`, `config/`, `services/gateway/authz.py`, `services/gateway/security.py`, `services/eligibility-service/`, `services/ai-assistant/eligibility_client.py`, `docker-compose.yml` or `eval/` — the spec Exclusions and the §1 zones stated not-touched above hold **structurally**, proven by verification 19's empty diff rather than by this sentence.
- **Partial coverage named honestly (residual honesty check).** Four residuals on this ticket's rows:
  (a) **SPEC-11 proves indexing, not curation.** `test_index_covers_every_row` proves every approved manifest row is indexed under at least one argument set; it does **not** prove the curated `payers` / `products` / `states` / `topics` axes match the manifest's 81 distinct prose applicability strings (E-3), which are read by hand at vendoring. A curation error narrows a bucket silently and no test on this ticket sees it — `retrieval-eval`'s recall baseline (SPEC-64) is the first measurement that would, and it lands after this ticket.
  (b) **The boot-fail hook has no CI proof in a running container.** CI `services` is `python -c "import app"` and runs no lifespan — it does run the import-time corpus load and sha check (gate r4 f4), so the *verification* is CI-proven, but the *hook* that repeats it at boot is not; `docker-build` builds every image and runs none (`ci.yml:222`, its own note). `test_startup_hook_fails_boot_on_corpus_error` (gate r1 f5) is the automated proof and verification 4c the hand check — a real container refusing to boot is observed for the first time at `lifecycle`'s verification 2.
  (c) **SPEC-8/38 prove isolation, not resistance.** The corpus gate and the seven `FIX-NEG-*` negatives prove the fixtures are absent from the corpus, the index and the approved set and are rejected as inputs. They do **not** prove the model resists fixture text placed in front of it — that is `turn`'s SPEC-13 (`[EVAL-014-subverted]`) and the opt-in SPEC-17 (`[EVAL-014-resists]`), both later tickets.
  (d) **The LangChain composition is measured offline only.** E-4's live 12/12 ran on `langsmith==0.11.1`; the pins this ticket lands sit beside the repo's `langsmith==0.10.5`. E-5 measured that composition offline — pip resolves the four pins without conflict, `create_agent` compiles with the run-limit middleware nodes, the 2-model-call / 1-tool-call bound holds, and `langchain_core.tracers.langchain` imports against 0.10.5 with `LANGSMITH_TRACING=false` — but the **live** Bedrock leg under 0.10.5 is un-run (gate r1 f12, E-5 "Not measured"). It is first exercised at SPEC-17 / SPEC-32's opt-in runs in `turn` / `trace`, so a live-only incompatibility surfaces there and never in the default suite. Accepted: this ticket lands dark and makes no model call, so the residual cannot bite inside its own scope.
  (e) **The rig as written pins four of the eight popped bare names (gate r10 f1).** `tests/a1_corpus_rig.py` pins `config`, `logging_config`, `policy_index`, `policy_tool` by path (eligibility-assistant-D-66) and pops the other six unpinned, so `app.py:36`'s `schemas` (and `llm_client`, `eligibility_client`, `templates`, `visit_templates`, `breaker` transitively) would resolve by `sys.path` order — masked in the default run (the rig's first `load_module` puts `services/ai-assistant` at `sys.path[0]`, `tests/conftest.py:21-22`), exposed by an explicit mixed invocation that has another service's dir ahead. Accepted by the owner 2026-08-28 as a named residual rather than a plan revision; the implementation pins all eight by path as the row's cited idiom (`tests/test_ai_visit_chat.py:51-67`) does, which the row's "runs the house idiom once" already covers. The impl gate reads the rig against this bullet.

## Delivery evidence

Stage-4 evidence for this ticket, split out of the contract's `## Delivery` record by
eligibility-assistant-D-82 (impl-gate r1 f1 — six per-ticket records at the shipped 74-line shape
put `docs/workflow/eligibility-assistant.md` at ~784 against CI's 400-line `workflow-doc-cap`).
The contract keeps the references — shas, baseline movement, traceability, residual IDs; this
section keeps the deviations and the live-run detail. It dies with this file at merge; the ticket
row's `plan-file deletion sha` is what resolves it.

**Deviations** (plan fact, trivial; no mechanism moved):
1. `Row` is a frozen dataclass with attribute access (`row.id`, `row.section_text` — the form the
   cut-session tests use) **and** mapping access on the plan's six keys (`row["section-text"]`,
   the form `lifecycle`'s runbook command uses); `as_dict()` is the six-key dict. Both forms carry
   the same six values, so `MAX_ROW_BYTES` is unchanged (2,789).
2. The tool's return value, which the plan does not state: a list of six-key row dicts. `turn` owns
   the serialisation the model sees.
3. Verification 3 / 4 / 4b redden as a **collection error** on the corpus tests, not a test failure:
   the rig loads `app` at import, whose `import policy_tool` runs the sha-verifying `load()`, so a
   broken corpus raises before any test collects. Stricter than the plan's wording, same clause.
4. Verification 4p second break (drop the `policy_tool` pin) reddens the two `with_app=True` tests
   in `test_a1_corpus.py` (`test_manifest_sha_pinned`, `test_startup_hook_fails_boot_on_corpus_error`),
   not the whole file — those are the two that assert `app_mod.policy_tool is policy_tool`.
5. Verification 10 as written (`--no-git` over `$PWD`) scans the local `.venv/` and
   `frontend/node_modules/` too — 183 hits, all outside the tracked tree. Re-run in the CI shape
   over an export of `git ls-files -co --exclude-standard` (473 files): `no leaks found`, exit 0.
No planned change row is absent from the diff (18 rows, all present).

**Stamp-commit scope** (impl-gate r1 f2, record shape — no revert): the branch-cut commit @0504b7e
carries contract edits beyond this ticket's row — in-place frozen-text amendments owned by other
tickets (SPEC-48 / eligibility-assistant-D-74, `turn`; REQ-9 Notes / eligibility-assistant-D-79 and
SPEC-28 / eligibility-assistant-D-81, `trace`) and the five sibling `GATED` stamps. Each is a
registered owner decision in the same commit's register and no code scope leaks; the sibling ticket
files those stamps refer to are anchored by the `Sibling ticket files:` header line above.

**Live-run evidence** (isolation: every run is read-only against files in this tree — no DB, no
service, no §1 zone at runtime; the ai-assistant image built for 4k was tagged locally and removed):
- V1 `pytest tests/test_a1_corpus.py tests/test_a1_retriever.py tests/test_a1_conflict.py` →
  21 passed; `[EVAL-031]` and `[EVAL-023]` present in the collected ids.
- V2 sha pin → `87 rows 0 mismatched`. V4f `git ls-files … | wc -l` → `2`. V5 keyless `env -i`
  import → `ok`. V8 bracketed EVAL id count → `2`. V19 exclusion diff → empty.
- V4b `policy_index.load()` on the tree → `ok`; V4c `TestClient(app.app).__enter__()` → `booted`,
  and with one byte appended to the cheat sheet the raise is at `import app`, innermost frame
  `policy_index.load` reached from `policy_tool.categories` (`CorpusLoadError: sha256 mismatch
  (DOC-COVERAGE-QUESTION-CHEAT-SHEET)`); reverted → `booted`.
- V4k image scan → `0`; bare `.DS_Store` pattern → `1`; `**/.DS_Store` restored → `0`; in-image
  `import app, policy_index, policy_tool` → `ok`.
- V9 `make test-docker` → `1355 passed, 19 deselected, 1 xfailed` (36.7s) = baseline 1334 + 21;
  deselected and xfailed unmoved; no `skipped` term. Local `.venv` run identical.
- V10 gitleaks (CI shape, see deviation 5) → exit 0.
- Break-then-revert, each red on the named test and green after revert: 3 (sha), 4 (fixture copied
  in → `test_eval_031_fixture_isolation` + `test_manifest_sha_pinned`), 4b (root file, nested
  `notes/rogue.md`; `documents/.DS_Store` stays green), 4d (inferred schema), 4e (`lifespan=`
  dropped), 4g (cache write; `subprocess.run`), 4h (approval check), 4i (`document_id` key), 4j
  (`|` split → 142 rows), 4l (cap 6 → (i); `_INDEX` narrowed to the four `eligibility-verification`
  rows → (ii) `assert 4 > 5` with (i) green; text-only `MAX_ROW_BYTES`; query `*` accepted), 4m
  (hand `Literal`), 4n (id-fragment tier), 4o (publish on every load → both named tests), 4p
  (both pins). Process note: a same-size edit inside one mtime second served a stale `__pycache__`
  once; the battery was re-run with `PYTHONDONTWRITEBYTECODE=1` and caches cleared.

## Findings

Gate rounds start at 1 for this ticket; carried findings: f2 (gate round 2 on the monolithic plan — SPEC-7 corpus-scan scope, `index.json` / `document-manifest.json` as unlisted files) — **dispositioned 2026-08-26** in the `policy_index.py` change row and verification 4b (scope: manifest path roots + two named root-level files); the item-level cell in plans/eligibility-assistant.md carries the record.

### Gate — round 1, 2026-08-26

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket + branch (`main`,
working tree), no characterization received. Run as wave 1 alongside the `llm-seam` gate
(two agents, independent reads; owner decision in session — the remaining four tickets gate
after `corpus` / `llm-seam` stamp). Plan returns to stage 3; spec unchanged. Two findings
are **cross-ticket classes** the same wave surfaced independently in `llm-seam.md` Gate r1:
f1/f2 (Landmines block content by citation, not verbatim — matches llm-seam r1 f2, and the
r1 f1 / r2 f5 site on the monolithic plan dispositioned via eligibility-assistant-D-56) and
f6 (LangChain pin ownership vs eligibility-assistant-D-34 1b — matches llm-seam r1 f1).
Stage 3 closes each as a class: one sweep over all six ticket files' Landmines blocks for
the verbatim rule, and one D-34/D-55 reconciliation naming where the pins land and which
ticket depends on which; each disposition cell records the sweep scope, not the site fix.
f3 and f4 are wrong-fact / wrong-mechanism on `Scope:` rows and need a text change, not a
cite. f5 and f6 are minor.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | Landmines block (PHI entry) · SPEC-7/8/9/10 (⚠) | The entry describes five "new PHI-adjacent code paths" — SPEC-12, 20, 28–31, 60/61, 63 — none of which this ticket lands; the corpus ticket's own zone entry (the vendored corpus, the `topic`-only tool with app-bound payer/product/state, the by-id application entry on the same index — SPEC-7–10) is undescribed, so "approval of code entry on SPEC-7–10 **as designed in this block**" (eligibility-assistant-D-56) cites a design the block does not contain, and a PR "Risk & landmines" drafted from it would name code absent from the corpus diff. **Class:** Landmines block — see preamble. | A · 2026-08-27 — class closed by sweep under new **eligibility-assistant-D-60** (blocks are verbatim per ticket, scoped to that ticket's own diff). **Scope: swept Landmines blocks of all six ticket files: corpus, llm-seam, retrieval-eval, turn, lifecycle, trace** — each rewritten to name only the §1 zones its own change list enters, every zone in reach stated entered or not-touched, eligibility-assistant-D-56 cited with its date (2026-08-25, registered 2026-08-26), deliberate defects in reach stated preserved, and that ticket's residuals written in the block. This block now carries the corpus zone entry the finding names: the sha-pinned vendored corpus (SPEC-7/8/38), the `topic`-only tool with app-bound payer/product/state (SPEC-9), and the app-side `fetch_by_id` entry on the same index (SPEC-7/9/10) |
| 2 | Landmines block (cross-reference bullet) · Verification cross-references · SPEC-7–10 | Secrets (§1), Migrations (§1), the structural-exclusion statement and the residual-honesty bullet are replaced by "recorded in `trace.md`" — the plan-authoring rule is verbatim, never a citation; two corpus-ticket facts live only there: the Secrets entry's "vendored corpus and fixtures pass CI's gitleaks scan (verification 10)" and the E-5 live-leg residual the `requirements.txt` change row calls "a named residual". Verifications 5, 8, 9, 10, 19, 20, 21 likewise exist here by number only, with text in `turn.md` / `trace.md` — DRAFT siblings that land after this ticket and can be revised after this file is gated. **Class:** Landmines block — see preamble. | A · 2026-08-27 — same class, same sweep as f1 (**scope: swept Landmines blocks of all six ticket files: corpus, llm-seam, retrieval-eval, turn, lifecycle, trace**; eligibility-assistant-D-60). Secrets (§1), Migrations/schema (§1), the structural-exclusion statement and the residual-honesty bullet are now written out here, scoped to this ticket — including the gitleaks fact (Secrets entry + verification 10) and the E-5 live-leg residual, which lands as residual (d) here because this ticket lands the pins. Verification cross-references resolved the same way: 5, 8, 9, 10 and 19 are written out with this ticket's own expected values (5 `ok`; 8 → `2`; 9 no `skipped` term yet; 10 exit 0; 19 empty diff), and 20 / 21 are **dropped** — SPEC-18 and SPEC-31 are `turn`'s and `trace`'s rows, nothing in this change list touches either, and verification 19 already proves the exclusion. A Gate-interactions section was added with its own text (CI `services` smoke, `secret-scan`, `docker-build`-never-runs, the SPEC-37 count, `rank` as `retrieval-eval`'s seam), so no claim in this file now depends on a DRAFT sibling |
| 3 | SPEC-43 · `tests/test_a1_conflict.py` row ("EVAL-006 stale tier-5-only set") · eligibility-assistant-D-38 | Wrong fact: EVAL-006's expected source is `DOC-SYN-CITATION-RECENCY` — category `source-recency`, license "Original synthetic training material" (manifest, read-only). D-38's tier 5 is category `payer-training-summary` (7 rows, all `DOC-SYN-*-TRAINING-SUMMARY` / `-TRAINING`), so under D-38 the EVAL-006 set is tier 4, not tier 5. `test_tier_rank_in_code` pins the tier function on named ids — state the tier D-38 assigns (`refuse_definitive` still follows from "no row of tier ≤ 3") or amend D-38. | A · 2026-08-27 — both: the `tests/test_a1_conflict.py` change row now reads **tier-4-only** and names why (`DOC-SYN-CITATION-RECENCY`, category `source-recency`, original synthetic), and eligibility-assistant-D-38 carries a dated amendment note (appended, not rewritten) making tier 4 the *rule* — every original-synthetic row whose category is not `payer-training-summary` — rather than the illustrative clinic-procedure list its parenthetical read as. Tier 5 stays `payer-training-summary` alone (7 rows); EVAL-006 still reaches `refuse_definitive` through "no row of tier ≤ 3", unchanged |
| 4 | SPEC-9 · `services/ai-assistant/policy_tool.py` row ("any other argument … rejected by the schema") | Mechanism claim false for one of the three clauses: in `langchain-core==1.6.0` (pinned wheel, inspected in scratchpad) the `@tool`-inferred args schema is rebuilt by `utils/pydantic._create_subset_model_v2` with `ConfigDict(arbitrary_types_allowed=True)` only — `tools/base.py:253` `_SchemaConfig.extra = "forbid"` does not survive the subset — and `StructuredTool._parse_input` (`tools/base.py:834-836`) `model_validate`s then dumps declared fields, so an **extra argument is silently dropped**, not rejected. The `Literal` rejects a free-text or document-id *value*; it does not reject an extra *key*. Name the mechanism (explicit `args_schema` with `extra="forbid"`, or equivalent). | A · 2026-08-27 — mechanism named in the `policy_tool.py` change row: an explicit `args_schema=PolicyLookupArgs` (pydantic, `model_config = ConfigDict(extra="forbid")`, single `topic: Literal[…]` field) passed to `StructuredTool.from_function`, never the `@tool`-inferred subset schema, with the langchain-core 1.6.0 reason stated in the row so a later reader cannot "simplify" it back. Negative check made red-able: new `tests/test_a1_retriever.py::test_tool_rejects_extra_keys` asserts the extra key **raises** and never reaches `policy_index.lookup`, and asserts on the declared `args_schema.model_config["extra"]`, so dropping the explicit schema reddens it — break-then-revert added as verification 4d. No new decision: this is the implementation mechanism of eligibility-assistant-D-25's `policy_lookup(topic)`-only tool, not a change to it |
| 5 | SPEC-7 · `services/ai-assistant/app.py` row · eligibility-assistant-D-35 amended | "Fails the container at boot" is exercised by nothing tracked: every ai-assistant rig constructs `TestClient(app_mod.app, …)` without a `with` block (`tests/test_ai_visit_chat.py:77`, `test_visit_chat_phi.py:91`, `test_ai_intake_instructions.py:66` — lifespan never runs), CI `services` is `python -c "import app"` (`ci.yml:76`), and `docker-build` is `docker compose build` only (`ci.yml:222-227`: "never that any of them runs"). The hook rests on hand verification 4c alone; a regression that drops it stays green. Pin a test (e.g. `with TestClient(app_mod.app)` against a monkeypatched broken `load(root=…)`) or write it as a residual in the block. Minor. | A · 2026-08-27 — **pinned a test** (the first option): `tests/test_a1_corpus.py::test_startup_hook_fails_boot_on_corpus_error` enters `with TestClient(app_mod.app)` against a `policy_index.load` monkeypatched to raise, asserts the lifespan raises out and no request is served, and re-enters clean against the real corpus; break-then-revert (delete `lifespan=` from the `FastAPI(...)` construction) is verification 4e. The retained rigs are unaffected — they construct `TestClient(...)` bare, so lifespan still never runs there. The finding's *other* half is kept honest rather than dropped: residual (b) in the Landmines block records that no CI job runs the container's lifespan (`services` is `import app`, `docker-build` builds only, `ci.yml:222`), so the pinned test plus verification 4c are the whole automated proof until `lifecycle`'s verification 2 boots a real container |
| 6 | Changes · `requirements.txt` row cites eligibility-assistant-SPEC-21 · eligibility-assistant-D-34 | Off-scope cite: SPEC-21 is `turn`'s row; the pins serve SPEC-9's tool here (`langchain_core.tools`). D-34's 1b clause still places "LangChain v1 pins" in `llm-seam`, whose change list lands none — recite to SPEC-9 and align D-34 (as read through D-55) with where the pins actually land. Minor. **Class:** pin ownership — matches llm-seam r1 f1; see preamble. | A · 2026-08-27 — class closed by one reconciliation: **eligibility-assistant-D-59** supersedes eligibility-assistant-D-34's 1b "LangChain v1 pins" clause (dated note on D-34) — the pins stay in `corpus` where SPEC-9's `policy_tool` imports `langchain_core.tools`, and `llm-seam` reads `Depends on: corpus` (pins); this change row recited SPEC-21 → SPEC-9 and now names the root `requirements-dev.txt` as CI's `tests`-job install (`ci.yml:86`). **`Depends on:` sweep, all six ticket files:** corpus `none` (correct, first in order) · llm-seam `none` → `corpus` (fixed) · turn `corpus, llm-seam` · trace `turn` · lifecycle `corpus, turn` · retrieval-eval `corpus` — llm-seam was the only wrong line; the item plan's ticket-table `depends on` column carries the same fix and landing order corpus → llm-seam → retrieval-eval → turn → {lifecycle, trace} is unchanged |

Verified and **not** a finding, for the record: `langchain_core` defers its `langsmith`
import (`callbacks/manager.py:99`), so `services/ai-assistant/requirements.txt:6-7`
("Imported lazily and only when LANGSMITH_TRACING=true … no import-time or keyless-smoke
dependency") survives this ticket true; verification 2's command works against the manifest
shape (`{"retrieved", "documents"}` top level, `path` relative to the package root, 87 rows /
0 mismatched); verification 3/4c paths (`documents/synthetic/DOC-SYN-EMERGENCY.md`,
`procedures/coverage-question-cheat-sheet.md`) match the manifest; only `documents/.DS_Store`
is unlisted under the roots; `FIX-NEG` appears nowhere in the manifest; exactly 27 of 32
cases name a `DOC-*` / `procedures/*` source (the five others are EVAL-010/011/029/030/031).

Per-SPEC verdicts: SPEC-7 **FINDING** (f1, f2, f5) · SPEC-8 **FINDING** (f1, f2) · SPEC-9
**FINDING** (f1, f2, f4) · SPEC-10 **FINDING** (f1, f2) · SPEC-11 satisfied · SPEC-38
satisfied · SPEC-43 **FINDING** (f3).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..58, Evidence E-1..7, `corpus.md` Changes / Verification 1–4c / Landmines, the five sibling `Scope:` lines and the contract `## Delivery` table · check map 66/66 single-mechanism · ⚠ rows in set: 7, 8, 9, 10 (4) — PHI-handling zone entered with approval of record eligibility-assistant-D-56 (owner 2026-08-25, registered 2026-08-26); Domain-internal stated not touched; Secrets / Migrations only by cross-reference (f2) · freeze scope: no `DEFERRED` requirement, no row on a superseded REQ · ownership: 66/66 rows each owned once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4), table = Scope lines · cites: every SPEC and decision id resolves; SPEC-21 cited off-scope (f6) · change list closes both ways · facts verified in tree vs `main`: `app.py:51` `app = FastAPI(...)`, no lifespan/`on_event`; `config.py` plain `Settings`/`os.getenv`; `langsmith==0.10.5` in `services/ai-assistant/requirements.txt` and root `requirements-dev.txt` (the file `Dockerfile.test` installs), no `langchain*` tracked; ai-assistant `Dockerfile` `COPY . .` + `.dockerignore` (`__pycache__/ *.pyc .venv/`); ADRs 0001–0018 present, 0019 absent, 0006/0011 `**Status:** Accepted…`; `tests/README.md:32` "AI paths"; `tests/fixtures/` absent; `tests/conftest.py:16 load_module`; CI `services` smoke `:76`, gitleaks v8.18.4 `:127`, doc cap `docs/workflow/*.md` top-level only, `docker-build` build-only; ai-assistant rigs use `TestClient(...)` without `with`; `.venv` py3.12.13 with `fastapi.testclient` importable; D-36 prefix residual present in `turn.md:68` · client package (outside repo, read-only): 87/87 `approved`, 25 categories, federal 41 / synthetic 26 / payers 14 / x12 2 / procedures 4, all reason-table ids present at the planned paths, 0 sha mismatch, `documents/.DS_Store` the only unlisted file, no lifecycle keys, no `FIX-NEG` in the manifest, 7 `FIX-NEG-*` fixtures, 32 cases / 27 naming a manifest source, EVAL-006 → `DOC-SYN-CITATION-RECENCY` (`source-recency`) · `langchain-core==1.6.0` wheel (scratchpad only): `langsmith` deferred at import, inferred args schema not `extra="forbid"` · wrong facts: 1 (f3) + 1 mechanism (f4) · 6 findings (1 Landmines content, 1 Landmines/verification by citation, 1 wrong fact, 1 mechanism, 2 minor).

### Gate — round 2, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run as wave 1 r2 alongside the `llm-seam` r2 gate (two agents, independent reads). All six
r1 dispositions verified holding on the re-read (Landmines block verbatim under D-60,
verification cross-references written out, f3/f4 text changes present, D-59 pin ownership,
`Depends on:` lines consistent). Plan returns to stage 3; spec unchanged. f1 and f2 are
**unnamed mechanisms on a false premise of the client package** — the plan cites manifest
fields (`section_labels`, `version_effective`) as if they carried structure they do not;
stage 3 must state the mechanism and which artifact carries it. f4 is the **same class as
r1 f4** (one clause of a SPEC row without a named mechanism) — stage 3 closes it as a
class: sweep every change row in this ticket for clauses of its cited SPEC row that no
named mechanism covers, and record the sweep scope in the cell. f3 and f5 are minor.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-10 · `policy_index.py` row ("one row per (document, section) with id/title/section/version/section-text") · eligibility-assistant-D-36 (`index.json` `sections` = manifest `section_labels`) | Unnamed mechanism on a false premise: the manifest's `section_labels` is a `\|`-delimited **string**, not a list (142 labels over 87 rows, 39 rows multi-label), and a label equals a markdown heading in its file for only 55/142 — **62 of 87 documents** (all 41 federal, all 14 payer, both x12, all 4 procedures, 1 synthetic) have no label that is a heading (e.g. `DOC-FED-HETS-OVERVIEW` labels `HETS purpose` / `Permitted uses of eligibility data` vs headings `CMS HIPAA Eligibility Transaction System (HETS) — eligibility purpose` / `Business use` / …). The plan names no rule for splitting a file into section rows, deriving "section-text" for a label with no heading, or parsing the `\|` string — yet the row count the `A1_RETRIEVAL_MAX_ROWS` cap cuts (`test_in_process_read_only_capped`) and the (document, section) identity every later ticket cites (SPEC-4/12/64) rest on it. State the mechanism (heading match with whole-document fallback, curated section offsets in `index.json`, or one row per document) and which artifact carries it. For the record, `turn`'s concern not this ticket's: the harness's `expected_sections` equal manifest labels on EVAL-001..006 but not on EVAL-023 (`Citation-only record`) or EVAL-024 (`Front-desk eligibility implication`) — the section vocabulary the corpus exposes decides whether SPEC-4 can be asserted there. | A · 2026-08-27 — **eligibility-assistant-D-61** (confirmed owner 2026-08-27): one row per document, `section` = manifest `section_labels` verbatim, `section-text` = the whole file, cap counts documents; D-36 / D-58 amended with dated notes. `policy_index.py` row states the shape; `test_index_covers_every_row` pins 87 rows with those two equalities; verification 4j is the break-then-revert. The `expected_sections` note is carried to `turn` as D-61's consequence, not dispositioned here |
| 2 | SPEC-43 · `rank(rows)` "tier rank, then version date" (eligibility-assistant-D-36, D-58) · `tests/test_a1_conflict.py::test_tier_rank_in_code` ("order by rank") | The secondary sort key has no value in the corpus: `version_effective` is prose on **87/87** rows (0 ISO dates; 48 distinct strings such as `Training-only; effective 2026-08-24 unless a current official source is newer`, `CMS page as retrieved 2026-08-24`, `HETS 270/271 5010 Companion Guide Version 15-0; Date of Last Revision February 2026`) — which eligibility-assistant-D-48 itself records as unparseable without curation — and `retrieval_date` is `2026-08-24` on all 87, so within a tier the order is input order. Name the tiebreak (a curated date in `index.json`, or a deterministic key such as `document_id`) so the pinned "order by rank" and the cap cut are determined, and the SPEC-66 ranking unit is defined before `retrieval-eval` substitutes it. | A · 2026-08-27 — **eligibility-assistant-D-62** (confirmed owner 2026-08-27): key = tier asc, `retrieval_date` desc, `document_id` asc; no `version_effective` parse (D-48). `test_tier_rank_in_code` feeds reversed manifest order and asserts a same-tier pair; verification 4i |
| 3 | SPEC-7 · `policy_index.py` scan rule ("any other file at the root or under the roots raises") × ai-assistant `.dockerignore` (`__pycache__/`, `*.pyc`, `.venv/` only) × `Dockerfile` `COPY . .` | Gate interaction on the demo machine: macOS Finder writes `.DS_Store` into any browsed folder (present today at `./.DS_Store`, `./frontend/.DS_Store`; gitignored at `.gitignore:6`; the client package itself shipped `documents/.DS_Store`). On the owner's Mac — where `make up` runs the demo (SPEC-34) — browsing `policy_corpus/documents/` reddens `test_manifest_sha_pinned` locally and, since `.DS_Store` is not dockerignored, is copied into the image so the boot-fail hook refuses to start on a Finder side-effect, not a tampered corpus. Exempt `.DS_Store`/dotfiles by name in the scan or add `.DS_Store` to the ai-assistant `.dockerignore`, say which, and adjust verification 4b. Minor. | A · 2026-08-27 — **eligibility-assistant-D-63** (confirmed owner 2026-08-27): both — the scan exempts exactly the name `.DS_Store` and `.dockerignore` gains it (new change row). Verification 4b gains the `touch … .DS_Store` → still-green leg; 4k proves the image carries none; Landmines PHI (1) and Domain-services entries updated |
| 4 | SPEC-10 · `tests/test_a1_retriever.py::test_in_process_read_only_capped` ("socket guard + cap") | The row has four clauses — in-process, read-only, no network, capped — and the plan names a mechanism for no-network (socket guard) and cap, none for **read-only**. State what the test asserts (corpus-tree digest unchanged across `lookup` / `fetch_by_id`, or a write-mode `open` guard beside the socket guard). Minor. **Class:** one clause of a row without a named mechanism — same class as r1 f4; see preamble. | A · 2026-08-27 — class closed by sweep: **scope: every clause of every `Scope:` row (SPEC-7, 8, 9, 10, 11, 38, 43) walked against the change rows and tests for a named mechanism.** Gaps found and filled: SPEC-10 *read-only* (write-mode `open` guard + corpus digest), SPEC-10 *in-process* (`Popen`/`fork` guard), SPEC-11 *tracked artifact* (verification 4f, `git ls-files`), SPEC-7 *exactly the approved rows* (f5, D-63); every other clause already had one (SPEC-8 id/path/hash ×3, SPEC-9 ×3 from r1 f4, SPEC-38 ×7, SPEC-43 code-computed + order). Verification 4g is the break-then-revert for both SPEC-10 additions |
| 5 | SPEC-7 ("exactly the manifest's approved rows") · `policy_index.load` row · `test_manifest_sha_pinned` | The manifest carries `approval_status` (87/87 `approved` today) and the plan never says what the loader does with a row that is not `approved` — exclude it from the index and the citable set, or fail at load. Today's manifest makes any check of the clause vacuous, and `lifecycle` adds a second status axis (`status`, D-57) to the same rows; one sentence keeps the two from being conflated. Minor. | A · 2026-08-27 — **eligibility-assistant-D-63** (confirmed owner 2026-08-27): a non-`approved` row raises at load, never skipped; `status` (D-57) is a separate axis the check does not read. New `test_non_approved_row_fails_load` on a `tmp_path` copy via `load(root=…)`; verification 4h |

Verified and **not** a finding, for the record: the SPEC-9 mechanism claim (r1 f4's fix)
holds on the pinned wheel — `StructuredTool.from_function` infers a schema only when
`args_schema is None` (`tools/structured.py:203-205`), `_parse_input` on a `BaseModel`
schema is `model_validate(tool_input)` then `model_dump()` (`tools/base.py:778+`), so
`extra="forbid"` raises on an extra key, while the inferred subset model is built with
`ConfigDict(arbitrary_types_allowed=True)` only (`utils/pydantic.py:261`). `.DS_Store`
aside, `documents/.DS_Store` is the only unlisted file under the package roots; 0 sha
mismatches; path counts federal 41 / synthetic 26 / payers 14 / x12 2 / procedures 4;
license classes partition cleanly into D-38's tiers (16 citation-only = 14 payer + 2 x12;
30 original-synthetic of which 7 `payer-training-summary`); EVAL-006 →
`DOC-SYN-CITATION-RECENCY` (`source-recency`, original synthetic) = tier 4 as amended;
27/32 cases name a `DOC-*`/`procedures/*` source. `app.py:51` has no lifespan; CI lines
`:72-76` / `:86` / `:120-129` / `:222` as cited; `requirements.txt:6-7` comment survives
(langchain_core defers `langsmith`); every ai-assistant rig constructs `TestClient(...)`
bare and no `with TestClient` exists in `tests/`; `.venv` is 3.12.13 without
`langchain_core`; `adr/0019` absent, 0006/0011 `**Status:** Accepted`; `CLAUDE.md:141`
baseline line; `tests/README.md:32`; verification 19 paths exist; `.gitleaks.toml` extends
defaults. `adr/0011:373` "no tools exposed" survives this ticket true (tool lands dark) —
`turn`'s sweep, not this one. The `CLAUDE.md §6` and `tests/README.md` rows trace to
registry upkeep; `case_selections.json`, `rank` as its own function and `load(root=…)`
trace to D-58/D-57, so the change list closes both ways.

Per-SPEC verdicts: SPEC-7 **FINDING** (f3, f5) · SPEC-8 satisfied · SPEC-9 satisfied
(mechanism verified in the wheel) · SPEC-10 **FINDING** (f1, f4) · SPEC-11 residual-named
(Landmines residual (a): indexing proven, curation not) · SPEC-38 satisfied · SPEC-43
**FINDING** (f2).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..60, Evidence E-1..7, `corpus.md` Changes / Gate interactions / Landmines / Verification 1–4e, 5, 8, 9, 10, 19, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · check map 66/66 single-mechanism · ⚠ rows in set: 7, 8, 9, 10 (4) — PHI-handling zone entered with approval of record eligibility-assistant-D-56 (owner 2026-08-25, registered 2026-08-26) cited by ID and date; Secrets / Migrations / Domain-internal / Auth / IDOR / Inline-eligibility / ROI written out not-touched in this ticket's own block; deliberate defects stated preserved; four residuals written · freeze scope: no `DEFERRED` requirement, no row on a superseded REQ · ownership: 66/66 rows each owned once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4), Scope lines = table; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC, decision and evidence id resolves; no off-scope cite in a change row · change list closes both ways · facts verified in tree vs `main`: `app.py:51` no lifespan/`on_event`; `config.py` plain `os.getenv`; `langsmith==0.10.5` in both requirements files, no `langchain*` tracked; ai-assistant `Dockerfile` `COPY . .` + `uvicorn` CMD, `.dockerignore` three entries (no `.DS_Store`), `.gitignore:6 .DS_Store`, `.DS_Store` present at repo root and `frontend/`; CI `services` smoke `:72-76`, `tests` `:86`, gitleaks v8.18.4 `:120-129`, `docker-build` build-only `:222`; `tests/README.md:32`; `CLAUDE.md:141`; ADRs 0001–0018 present, 0019 absent; ai-assistant rigs `TestClient(...)` bare, no `with TestClient` in `tests/`; `.venv` 3.12.13, `langchain_core` absent; `test_compose_topology.py` doc scan covers `docs/**/*.md`, `eval/`, `tests/` (not `services/`); `.env.example` pins are knob-specific (`test_eligibility_budget_alignment.py`), no all-keys parity · client package (outside repo, read-only): 87/87 `approved`, 25 categories, federal 41 / synthetic 26 / payers 14 / x12 2 / procedures 4, 0 sha mismatch, `documents/.DS_Store` the only unlisted file, no lifecycle keys, no `FIX-NEG` in the manifest, 7 `FIX-NEG-*` fixtures, 32 cases / 27 naming a manifest source, EVAL-006 → `DOC-SYN-CITATION-RECENCY` (`source-recency`), `section_labels` a `|`-string (142 labels, 55 match a heading, 62/87 docs match none), `version_effective` prose 87/87, `retrieval_date` uniform · `langchain-core==1.6.0` wheel (scratchpad only): explicit `args_schema` used as-is, `_parse_input` `model_validate`s, inferred subset schema not `extra="forbid"` · wrong facts / unnamed mechanisms: 2 (f1, f2) · 5 findings (1 mechanism on a false premise, 1 undefined ordering key, 3 minor).

### Gate — round 3, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run as wave 1 r3 alongside the `llm-seam` r3 gate (two agents, independent reads). All five
r2 dispositions verified holding on the re-read (D-61 one-row-per-document shape, D-62 rank
key, D-63 `.DS_Store` exemption + non-`approved` raise, the clause sweep, the D-36/D-58
dated notes). **Round-3 rule fired** (`.claude/skills/drift-gate/`): a third round with any
open finding stops the loop. The plan does **not** return to stage 3 on this round's own
authority — the owner decides per finding: accept as a named residual, overrule, or change
the spec (stage 2, explicit decision). Each call is recorded in that finding's disposition
cell below, and the next gate run honors recorded owner decisions rather than re-flagging
them. Spec unchanged by this round.

Shape of the round: f1 and f2 are **substantive and paired** — both say the 12-document cap
introduced by eligibility-assistant-D-61 is not the binding constraint it was sized against
(f1: the real gate is a 20,000-**byte** budget, not `LLM_MAX_INPUT_CHARS`; f2: no legal
argument set can produce more than ~7 rows, so the cap clause has no legal-path exercise).
f3 is an unpinned enum. f4 is a **frozen-text vs plan-decision** question of the same family
as eligibility-assistant-D-51 / D-52, which were resolved by amending the contract in place
— it is an owner call about the frozen artifact, not a plan edit. f5 is the **r1 f3 class**
(eligibility-assistant-D-38 tier statement read as a rule, not a list) surviving at tier 1;
if the owner takes it, the disposition must close the class with a sweep over all five
tiers, not the cited tier alone. f6 and f7 are minor Landmines-block bookkeeping.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-10 · `services/ai-assistant/config.py` `a1_retrieval_max_rows` (default 12) · eligibility-assistant-D-61 headroom claim | Wrong config fact: D-61 sizes the 12-document cap against `LLM_MAX_INPUT_CHARS` (80,000), but the binding preflight is `_enforce_budget` on `max_input_tokens`, which is the **UTF-8 byte length** of system + messages + `extra_body` (`services/ai-assistant/llm_client.py:328-331`, `:368-375`, `:494`, `:458-461`) checked against `LLM_MAX_INPUT_TOKENS` = **20,000** (`config.py:64`). 12 documents at the manifest max 2,448 B = 29,376 B; the 12 largest actually total 23,896 B; the whole `payer-eligibility-public` topic is 24,496 B — each over the gate before any prompt overhead, so a full-cap result is `spend_stop` at model₂, not headroom. Bound the cap in bytes, or restate the default against the gate that binds. | A · 2026-08-27 — **eligibility-assistant-D-64** (confirmed owner 2026-08-27, in session). Fact corrected with its locators: the binding gate is `_enforce_budget` on `max_input_tokens`, UTF-8 bytes of system + messages + `extra_body` + 8 B base + 8 B/message (`llm_client.py:336-337`, `:368-375`, `:458-461`, `:494-495`) vs `LLM_MAX_INPUT_TOKENS` 20,000 (`config.py:64`); `LLM_MAX_INPUT_CHARS` (`config.py:75`) is the backstop. Default **12 → 6**: 6 × 2,448 (largest vendored file — package `document-manifest.json` paths sized 2026-08-27: 87 files, 131,032 B, max 2,448; the gate's 23,896 / 24,496 reproduced) + `PROMPT_RESERVE_BYTES` 5,000 = 19,688 ≤ 20,000, pinned by `test_cap_binds_on_a_legal_call` (i) with break 4l; `turn` proves its prompt fits the reserve and lowers the default by decision if not. `retrieval-eval.md` cites the setting by name only (`:45`) — unchanged [**corrected 2026-08-27, gate r4 f5:** `retrieval-eval.md:29` also carried the value, as `recall@12`, and this sweep missed it]. **Sites changed:** `plans/eligibility-assistant.md` D-64 (new), D-61 (note), D-36 (note); `corpus.md` `config.py` row, `policy_index.py` row, Landmines (3), Gate interactions (new byte-gate bullet), Status line; `turn.md:41` `.env.example`; `turn.md` verification 18 (new). |
| 2 | SPEC-10 (capped clause) / SPEC-9 · `tests/test_a1_retriever.py::test_in_process_read_only_capped` (`payer="*"`) | `*` is not a bindable payer: eligibility-assistant-D-36's payer enum is seven values, no wildcard and no `unconfirmed`, and SPEC-9 has the application bind the axis from the clerk selection — so the cap case as written needs `lookup` to accept a query value no turn can produce, an unnamed mechanism on the one axis SPEC-9 closes ("`*` matches any" in the `_filter` clause reads as the index-row value, not the query). With a legal argument set the largest reachable result is ~7 rows (largest category under one payer plus its `*` rows; `payer-eligibility-public`'s 14 rows split 1/3/3/4/3/2 across payers), so the 12-row cap never binds on a legal call — the cap clause has no legal-path exercise, and `retrieval-eval`'s `truncated` flag and recall@cap inherit that. | A · 2026-08-27 — same decision (**eligibility-assistant-D-64**): `lookup` accepts D-36 enum values only, `*` is an index-row value, a query `*` raises `ValueError` — the `_filter` clause now says so and `test_in_process_read_only_capped` asserts the raise. The capped clause runs on a legal call: `topic="coordination-of-benefits"`, `payer="medicare"`, product / state `unconfirmed` → 7 candidates (`DOC-FED-COB-*` ×4, `DOC-FED-MSP-CRS`, `DOC-FED-MEDIGAP`, `DOC-SYN-COB-FRONT-DESK` — every row Medicare or non-payer-specific, so 7 under any curation; 11,363 B; `medicaid-managed-care` / `medicaid` is the other 7-row set, 9,307 B; package read 2026-08-27) → exactly 6, `truncated` true; 7 with the cap at 7; the gate's `payer-eligibility-public` bound holds — ≤ 3 under one payer + 3 non-payer-specific rows = ≤ 6. `test_cap_binds_on_a_legal_call` (ii) pins that a legal set exceeds the default (break 4l second half), so `retrieval-eval`'s `truncated` / recall@cap inherit a real exercising case. **Sites changed:** `corpus.md` `policy_index.py` row (`_filter` clause), `tests/test_a1_retriever.py` row (capped clause, `test_cap_binds_on_a_legal_call`), verification 4l (new). |
| 3 | SPEC-9 · `services/ai-assistant/policy_tool.py` `PolicyLookupArgs.topic: Literal[…25 manifest categories]` | The tool enum's membership has no named source of truth and no pin: nothing asserts the `Literal` equals the manifest's 25 `category` values (`test_index_covers_every_row` pins index rows; `test_case_selections_closed` pins the fixture's topics). A manifest category absent from the `Literal` makes those rows unreachable to the model with nothing red, and `lifecycle` adds rows to the same manifest afterwards. Name the derivation (generated from the manifest at load, or a test pinning set equality both ways). | A · 2026-08-27 — `PolicyLookupArgs.topic` = `Literal[*policy_index.categories()]`, built at import from the manifest's distinct `category` values (25 — package read 2026-08-27), never a hand list; pinned both ways by `tests/test_a1_retriever.py::test_topic_enum_equals_manifest_categories` (`Literal` args == manifest categories == `index.json` `topics`), break 4m. `policy_tool.py` row and `policy_index.py` row (`categories()`) updated; `lifecycle` rows added to the manifest reach the enum with no edit. **Sites changed:** `corpus.md` `policy_tool.py` row, `policy_index.py` row (`categories()`), `tests/test_a1_retriever.py` row (`test_topic_enum_equals_manifest_categories`), verification 4m (new). |
| 4 | SPEC-7 (frozen EARS: "a mismatch or **an unlisted file** shall fail the suite") · eligibility-assistant-D-63 `.DS_Store` exemption | A plan-only decision narrows a frozen row's clause: SPEC-7 as frozen admits no unlisted file; D-63 exempts one name. The item's own precedent for an owner decision that changes what a frozen row asserts is amendment **in place** in the contract (D-51/D-52 — "a plan choosing an answer does not remove a contradiction from the frozen artifact"). The contract carries no note, so a reviewer reading `## Spec` alone reads a different rule than the implementer follows. | A · 2026-08-27 — **owner call (round-3 rule): amend in place** — **eligibility-assistant-D-65** (spec-tagged, D-51 / D-52 precedent). SPEC-7 row (`docs/workflow/eligibility-assistant.md:265`) now reads "a mismatch or an unlisted file — other than the single exempt name `.DS_Store` (eligibility-assistant-D-63; amended in place 2026-08-27, eligibility-assistant-D-65) — shall fail the suite"; `## Spec` Status line (`:253`) carries the dated note. Contract 339 lines, under the 400 cap. Overrule and drop-the-exemption declined, reasons in D-65. **Sites changed:** `docs/workflow/eligibility-assistant.md:253` (Spec Status note), `:265` (SPEC-7 row); `plans/eligibility-assistant.md` D-65 (new). |
| 5 | SPEC-43 · eligibility-assistant-D-38 tier-1 parenthetical · `tests/test_a1_conflict.py::test_tier_rank_in_code` | The r1 f3 class survives at tier 1: D-38 states tier 1 as "public-domain regulation (`42CFR` / `EMTALA-42CFR` / `HIPAA-164-*` ids, categories `emergency-care-boundary` / `privacy-minimum-necessary`)", while SPEC-43's inputs are category + license disposition only — and 4 of the 9 federal rows in those two categories (`DOC-FED-EMTALA-CMS`, `-ER-RIGHTS`, `-CRS`, `-OIG`) carry none of the listed id fragments, so read as the rule the `emergency` reason-table citation drops out of tier 1. State tier 1 as a rule (public-domain license × the two categories; the two `DOC-SYN-*` rows in them fall to tier 4) the way tier 4 was restated. Minor. **Class:** D-38 tier statement as list vs rule — matches r1 f3; a disposition closes it with a sweep over all five tiers. | A · 2026-08-27 — class closed over **all five tiers** (r1 f3 class): **eligibility-assistant-D-38 amendment note 2** states each tier as a rule on `license_disposition` prefix × `category`; partition 9 / 32 / 16 / 23 / 7 = 87 computed from the package 2026-08-27 [the counter-partition verification 4n stated in this disposition, 5 / 36 / 16 / 23 / 7, was **not** computed — it is 7 / 34 / 16 / 23 / 7; corrected in 4n 2026-08-27, gate r5 f1] (tier 1 = the nine `DOC-FED-EMTALA-*` / `DOC-FED-HIPAA-164-*` ids — `-CMS`, `-ER-RIGHTS`, `-CRS`, `-OIG` included; `DOC-SYN-EMERGENCY` / `DOC-SYN-PRIVACY-FD` tier 4; the two MAC pages tier 3 by their `Citation-only` prefix). `test_tier_rank_in_code` row pins the partition and the tier-1 id set; break 4n reddens an id-fragment rule. Sweep enumerated: tier 1 (restated), 2 (restated), 3 (restated, MAC rows resolved), 4 (r1 note stands, `DOC-SYN-*` in tier-1 categories named), 5 (unchanged, 7 rows). **Sites changed:** `plans/eligibility-assistant.md` D-38 (amendment note 2); `corpus.md` `tests/test_a1_conflict.py` row, verification 4n (new). |
| 6 | Landmines block · `docs/landmines.md` §1 zone list · eligibility-assistant-D-60 | One §1 zone is neither stated entered nor not-touched: "Intake registration — the guards are load-bearing" (`ConsentKind`, `proxy_intake` on `_post_checked`, `contracts/intake-registration.json`). Eleven of the twelve §1 zones are named; D-60 requires every zone in reach stated either way (the paths are covered indirectly by the Auth and ROI bullets, but the zone is not named). Minor. | A · 2026-08-27 — Intake registration bullet added (not touched; `docs/landmines.md:87`). Sweep enumerated over all six blocks — every one named 11 of 12 zones and lacked this one: `corpus.md`, `llm-seam.md`, `turn.md`, `trace.md`, `lifecycle.md`, `retrieval-eval.md` — bullet added to each. **Sites changed:** Landmines block, Intake registration bullet — `corpus.md`, `llm-seam.md`, `turn.md`, `trace.md`, `lifecycle.md`, `retrieval-eval.md`. |
| 7 | Landmines block ("a change to a mechanism named here re-opens it") · eligibility-assistant-D-56 · D-61 / D-62 / D-63 | The block's own re-open clause fired and the record does not say so: D-63 changed what the sha-pin scan admits and what a non-`approved` row does, and D-61 changed what a returned row contains — both mechanisms the block names — after the 2026-08-25 approval it cites. The owner's 2026-08-27 confirmations are recorded on the decisions but not as carrying the §1 approval forward. One line closes it. Minor. | A · 2026-08-27 — one sentence added to the approval paragraph: D-61 / D-62 / D-63 (confirmed 2026-08-27) and D-64 (proposed) each changed a named mechanism after the 2026-08-25 approval; the owner's confirmation of each decision carries D-56 forward to the block as now written, D-64's on the day it is confirmed. **Owner confirmed 2026-08-27, in session:** the decision confirmations are the re-approval act; the sentence stands as written, no separate decision. **Sites changed:** `corpus.md` Landmines block, approval paragraph (one sentence). |

Per-SPEC verdicts: SPEC-7 **FINDING** (f4; f7 touches its block) · SPEC-8 satisfied ·
SPEC-9 **FINDING** (f3; f2 in part) · SPEC-10 **FINDING** (f1, f2) · SPEC-11
residual-named (Landmines residual (a): indexing proven, curation not; the "tracked
artifact" clause covered by verification 4f) · SPEC-38 satisfied · SPEC-43 **FINDING**
(f5).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..63, Evidence E-1..7, `corpus.md` Changes (17 rows) / Verification 1–4k, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r2, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · spawning prompt = item + ticket only, no branch and no characterization (read `main` + the uncommitted working tree) · check map 66/66 single-mechanism (scripted over the frozen table) · ⚠ rows in set: 7, 8, 9, 10 (4) — PHI-handling zone entered, approval of record eligibility-assistant-D-56 cited by ID and date (owner 2026-08-25, registered 2026-08-26); 11 of 12 §1 zones written out entered/not-touched in this ticket's own block (f6), deliberate defects stated preserved, four residuals written · freeze scope: no `DEFERRED` requirement, every row on a current REQ (3‴, 11‴, 13′) · ownership: 66/66 rows owned exactly once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated), Scope lines = `## Delivery` table = item ticket table; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC / decision / evidence id resolves; no off-scope cite in a change row (`CLAUDE.md` §6 and `tests/README.md` rows trace to registry upkeep + D-43; `case_selections.json` / `load(root=…)` / `rank` to D-57/D-58) · change list closes both ways · facts verified in tree vs `main`: `app.py:51` `app = FastAPI(...)`, no `lifespan`/`on_event`; `config.py:64` `LLM_MAX_INPUT_TOKENS` 20,000, `:75` `llm_max_input_chars = tokens × 4`; `llm_client.py` byte-based `max_input_tokens` `:351` / `:368-375`, `_enforce_budget` `:458-461`, `:494`; `langsmith==0.10.5` in `services/ai-assistant/requirements.txt` and root `requirements-dev.txt`, no `langchain*` tracked; ai-assistant `Dockerfile` `WORKDIR /app` `:3` + `COPY . .`, `.dockerignore` three entries (no `.DS_Store`), compose `build: ./services/ai-assistant` so the new `.dockerignore` entry is honoured; `.gitignore:6 .DS_Store`; CI `services` smoke `:73-76`, `tests` installs `requirements-dev.txt` `:86`, gitleaks v8.18.4 `:120-129`, `docker-build` build-only `:222`, `workflow-doc-cap` 400 lines (contract 339); `tests/README.md:32`; `CLAUDE.md:141` baseline; ADRs 0001–0018 present, 0019 absent, 0006/0011 `**Status:** Accepted`; `tests/fixtures/` absent; no `with TestClient` anywhere in `tests/`; `test_compose_topology.py:648-649` host-port scan covers `docs/**/*.md` + `tests/**/*.py` — no ticket file trips it · client package (outside repo, read-only): 87/87 `approved`, 25 categories, 131,032 B total / max 2,448 / median 1,445, `retrieval_date` `2026-08-24` on 87/87, `section_labels` a `|`-string (142 labels, 39 multi-label), largest category `payer-eligibility-public` 14 rows / 24,496 B split 1/3/3/4/3/2 across payers, 32 harness cases of which 27 name a `DOC-*`/`procedures/*` source, EVAL-006 → `DOC-SYN-CITATION-RECENCY` (`source-recency`, original synthetic) = tier 4 as amended, tier-1 categories resolve cleanly on license × category (9 federal, 2 synthetic) · verification 2's command executed against the package → `87 rows 0 mismatched`, exactly as stated · falsified-claims sweep over landmines / debt-log / phi-policy register / todo / CLAUDE.md / ARCHITECTURE.md / runbook / tests README / ADR 0006 / 0011 for `services/ai-assistant/`, corpus, retrieval, dependency and boot claims: `ARCHITECTURE.md` and `docs/runbook.md` mention the assistant nowhere at all (nothing to falsify), CLAUDE.md §6 baseline is the only falsified clause and has a change row · wrong facts / unnamed mechanisms: 2 (f1 config value, f2 axis value) + 1 unpinned enum (f3) · 7 findings (2 substantive on the cap, 1 enum pin, 1 frozen-text vs plan decision, 3 minor).

### Gate — round 4, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run as wave 2 r4 alongside the `llm-seam` r4 gate (two agents, independent reads). First
round under the origin-tag rule (PR #91 @762f0fd): each finding cell opens with the
agent's tag derived from the record. Rounds 1–3 carry no plan-text hash on their `checked:`
lines and rounds 1–2 no `Sites changed:` lists, so **no finding this round is settleable as
`pre-existing`** — untaggable tags to `new` by rule, which is why four cells read `new`;
from r4 forward the hash below makes the derivation real. **Round-3 rule fired**
(`.claude/skills/drift-gate/`): a fourth round with open `new` / `class-repeat` findings
stops the loop. The plan does **not** return to stage 3 on this round's own authority — the
owner decides per finding: accept as a named residual, overrule, or change the spec (stage
2, explicit decision). Each call is recorded in that finding's disposition cell below, and
the next gate run honors recorded owner decisions rather than re-flagging them. Spec
unchanged by this round.

Shape of the round: the r3 dispositions verified holding on the re-read, and the two
mechanisms r3 most changed were re-derived **empirically** — D-64's byte figures reproduced
exactly from the client package (12 largest 23,896 B; COB 7 rows / 11,363 B), and the
SPEC-9 closed-enum mechanism executed against a downloaded `langchain-core==1.6.0` (explicit
`args_schema` + `Literal[*tuple]` raises `ValidationError` on an extra key, on a document
id and on free text, function never reached; the `@tool`-inferred schema silently drops the
key), so verification 4d holds as written. The five findings are: f1, the same cap-sizing
class r3 f1 opened, now missing the **row-metadata** term (97–341 B per row, in neither the
`cap × file bytes` term nor `turn`'s reserve, which verification 18 measures "excluding
retrieved row text"); f2, a wrong mechanism — a bare `.DS_Store` in `.dockerignore` is
root-only under Go `filepath.Match` and never matches `policy_corpus/documents/.DS_Store`,
so verification 4k prints `1`; f3, a change-list closure break — the capped test asserts on
`LookupRecord.truncated`, which `retrieval-eval` lands *after* this ticket; f4, a
self-consistency break the r3 dark-import fix opened — `import app` now reaches
`policy_tool`, whose `topic` enum is built at import from the manifest, so the "smoke runs
no corpus load" bullet and verification 4c's isolation both need re-stating; f5, a wrong
fact inside a filled r3 disposition cell (`retrieval-eval.md:29` still pins the pre-D-64
`recall@12`). Minor: f5.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-10 / SPEC-9 · `config.py` change row (`:19`) · `tests/test_a1_retriever.py::test_cap_binds_on_a_legal_call` (`:25`) · eligibility-assistant-D-64 sizing rule (i) | **class-repeat** (r3 f1 — "the cap's byte sizing does not bound the payload the gate measures"; that finding moved the gate from chars to bytes, this one says a term is still missing). Rule (i) counts only `cap × max vendored **file** bytes` (6 × 2,448 + `PROMPT_RESERVE_BYTES` 5,000 = 19,688 ≤ 20,000 — 312 B of slack), but the row that egresses is REQ-3‴ / eligibility-assistant-D-61's `id / title / section / version / section-text`: the four non-text fields are 97–341 B per row (package `document-manifest.json`, `document_id + title + section_labels + version_effective + retrieval_date`, read 2026-08-27; median 200, max 341), and `turn.md:71` (verification 18) measures the reserve **"excluding retrieved row text"**, so the metadata is in neither term. Worst case under the plan's own construction: 6 × 2,789 B (`DOC-FED-COB-PROVIDER-SERVICES`, file 2,448 + meta 341) + 5,000 = 21,734 > `LLM_MAX_INPUT_TOKENS` 20,000 (`services/ai-assistant/config.py:64`) — 6 × 341 = 2,046 B against 312 B of slack, so the pinned inequality passes while the bound it claims does not hold. No legal call trips it today (largest legal set = 7 COB rows, 11,363 B text + 1,564 B metadata; package read 2026-08-27), so this is the guarantee, not today's behaviour. Put the metadata in (i) (`cap × max(file + row-metadata)`), or in the reserve and say so in `turn`'s verification 18. | **Owner 2026-08-27: accepted → stage 3.** Finding real and not editorial: with the row-metadata term counted, cap 6 no longer fits the gate (6 × 2,789 + 5,000 = 21,734 > 20,000). Amend eligibility-assistant-D-64 rule (i) to `cap × max(file + row-metadata)`, cap 6 → 5, and make the bound executable (a test computes it from the vendored manifest) rather than hand arithmetic — the cap math has been wrong in r2, r3 and r4. **Disposition 2026-08-27 — A**, eligibility-assistant-D-64 **amended** (owner 2026-08-27, this adjudication). Figures re-derived from the package this session (`~/Documents/Work/ClientInput/Armaan-Eligibility-Client-Inputs-Only-2026-08-24/document-manifest.json` + files, five metadata fields UTF-8-summed per row): metadata 97–341 B, max 341 on `DOC-FED-COB-PROVIDER-SERVICES` (file 2,448) → max(file + metadata) = 2,789 — the gate's figures reproduce exactly. Rule (i) now `cap × policy_index.MAX_ROW_BYTES + PROMPT_RESERVE_BYTES ≤ llm_max_input_tokens`, `MAX_ROW_BYTES` set by `load()` over every field value the row carries (2,789) and **recomputed independently by the test** from manifest + files, so the bound is executable, not prose; per-row serialisation overhead is named as the reserve's, in `turn`'s verification 18. 6 × 2,789 + 5,000 = 21,734 > 20,000, so the default is **6 → 5** (18,945; 1,055 B slack); (ii) unchanged — COB 7 and Medicaid 7 exceed 5, and `payer-eligibility-public`'s 6 now does too. Class closed by construction: text and metadata in (i), everything else in the reserve, both named. Sweep for the old value, enumerated (`grep -rn "6 × 2,448\|19,688\|14,688\|default \*\*6\*\*\|= 6 rows\|A1_RETRIEVAL_MAX_ROWS=6\|recall@12\|default 6" docs/workflow/`, run 2026-08-27): `corpus.md:19` config row — changed; `:25` capped clause and cap test — changed; `:46` 4l — changed; `:71` Landmines (3) — changed; `:61` byte-gate bullet — changed; `turn.md:41` `.env.example` `=6` — changed to `=5`; `turn.md:71` verification 18 (`6 × 2,448`) — changed; `turn.md:90` `=6` — changed; `retrieval-eval.md:29` `recall@12` — changed (f5); register D-64 — amended in place; hits in `checked:` lines and filled prior-round cells are round records, left as written. **Sites changed:** `plans/eligibility-assistant.md` D-64 (amendment note); `corpus.md` Status line, `policy_index.py` row (`MAX_ROW_BYTES`), `config.py` row, `tests/test_a1_retriever.py` row (cap test (i)), verification 4l (breaks 1 and 3 new), Gate interactions byte-gate bullet, Landmines PHI (3); `turn.md:41`, `turn.md:71`, `turn.md:90`. |
| 2 | SPEC-7 · `services/ai-assistant/.dockerignore` change row (`:17`) · verification 4k (`:45`) · Landmines PHI (1) + Domain-services bullets (`:69`, `:77`) · eligibility-assistant-D-63 | **new** (r3's `checked:` line records no plan-text hash, so no tag can be settled from the record; untaggable → `new`). Wrong mechanism: a bare `.DS_Store` line in `.dockerignore` does not exclude the file D-63 exists for. Docker matches ignore patterns with Go `filepath.Match` against the **context-relative path**, `**` being the only any-depth wildcard; the build context is `services/ai-assistant` (`docker-compose.yml` build stanza; `Dockerfile:3` `WORKDIR /app`, `:8` `COPY . .`), so the Finder artefact's path is `policy_corpus/documents/.DS_Store` and a root-only pattern never matches it. Verification 4k (`touch policy_corpus/documents/.DS_Store` → build → `find /app/policy_corpus -name .DS_Store \| wc -l` → expected `0`) prints `1`, and the Landmines claim "the image built by `COPY . .` carries no Finder artefact under the corpus roots" is unsupported. The pattern must be `**/.DS_Store`. Contrast is why this reads right: `.gitignore:6`'s bare `.DS_Store` **does** match at any depth. Note also that today's three entries (`__pycache__/`, `*.pyc`, `.venv/`, verified) are root-only too — harmless while every service dir is flat, and `policy_corpus/` is the first nested tree in a service build context. (Docker daemon unavailable in the gate session, so this rests on documented `filepath.Match` ignore semantics, not an executed build.) | **Owner 2026-08-27: accepted → stage 3.** Wrong mechanism; pattern becomes `**/.DS_Store`. Mechanical. **Disposition 2026-08-27 — A.** Mechanism corrected: the entry is `**/.DS_Store`. Facts read this session: `services/ai-assistant/.dockerignore` is three lines (`__pycache__/`, `*.pyc`, `.venv/`), `Dockerfile:3` `WORKDIR /app`, `:8` `COPY . .`, build context `./services/ai-assistant` (`docker-compose.yml:223`), `.gitignore:6` bare `.DS_Store`. Verification 4k gains the break the gate described (bare pattern → `1`) so the root-only mistake reddens rather than passing silently; the Docker daemon was unavailable this session too, so 4k stays a pre-push hand check, as the r3 cell already recorded. The three root-only existing entries are left as they are — every service dir is flat today and widening them is not this ticket's row. **Sites changed:** `corpus.md` `.dockerignore` change row, verification 4k (break added), Landmines Domain-services bullet. |
| 3 | SPEC-10 · `tests/test_a1_retriever.py::test_in_process_read_only_capped` (`:25`, "with `LookupRecord.truncated` true … (`truncated` false)") · `policy_index.py` change row (`:15`) · `Depends on: none` (`:5`) | **new** (untaggable — no recorded hash in r3's `checked:` line). Change-list closure: the capped clause asserts on `LookupRecord.truncated`, but `LookupRecord`, `lookup(...) -> (rows, record)` and the `truncated` flag are landed by **`retrieval-eval`** (`retrieval-eval.md:13`, SPEC-63 / eligibility-assistant-D-58), which lands *after* `corpus` in the fixed order corpus → llm-seam → retrieval-eval → turn (eligibility-assistant-D-59). Nothing in this ticket's `policy_index.py` row names a record type or a truncation flag, and `Depends on:` is `none`, so the test as written cannot pass on this ticket's own PR — and landing the record here would be a change serving SPEC-63, another ticket's row. State the cap assertion in terms this ticket lands (returned row count vs `settings.a1_retrieval_max_rows`), or move the flag into this ticket's `policy_index` row by decision and re-scope SPEC-63's ownership. | **Owner 2026-08-27: accepted → stage 3.** Restate the capped assertion in this ticket's own terms (returned row count vs `settings.a1_retrieval_max_rows`); SPEC-63 ownership and D-58/D-59 unchanged. **Disposition 2026-08-27 — A.** The capped clause of `test_in_process_read_only_capped` is restated in this ticket's own terms — `len(rows)` and the ids at the default, at 7 and at 1 — with no `LookupRecord` and no `truncated`; SPEC-63's ownership, D-58 and the D-59 landing order are unchanged. Closure the other way: when `retrieval-eval` changes `lookup` / `fetch_by_id` to return `(rows, record)` (`retrieval-eval.md:13`), this ticket's call sites must unpack — `retrieval-eval.md` had no change row for `tests/test_a1_retriever.py`, so one is added there (the record is that ticket's, so the shape change is too). **Sites changed:** `corpus.md` `tests/test_a1_retriever.py` row (capped clause); `retrieval-eval.md` new change row `tests/test_a1_retriever.py`. |
| 4 | SPEC-7 / SPEC-9 · Gate interactions bullet 1 (`:57`) · `app.py` change row (`:16`, dark `import policy_tool`) · `policy_tool.py` row (`:18`, `Literal[*policy_index.categories()]`) · verification 4c (`:37`) | **new** (untaggable — no recorded hash in r3's `checked:` line). Self-consistency: the bullet says "`import app` runs **no** FastAPI lifespan hook, so the startup corpus load does not execute there; `policy_index`'s lazy first-use load keeps the smoke offline" — but `app.py` now imports `policy_tool`, whose `PolicyLookupArgs.topic` is "built **at import** from the loaded manifest's distinct `category` values (`policy_index.categories()`)". So `python -c "import app"` (`.github/workflows/ci.yml:76`) does read the corpus at import; whether it *sha-verifies* it turns on an unnamed choice — if `categories()` goes through `load()`, the CI `services` smoke now runs the whole corpus gate at import (and verification 4c's break, "raises **from the startup hook** before `booted`", in fact raises at `import app`, so 4c no longer isolates the hook it was written to check); if it does not, `categories()` is a second manifest reader outside the sha-verified loader — a path into the model-facing enum that SPEC-7's boundary does not cover. Name which, and re-state the bullet and 4c to match. | **Owner 2026-08-27: accepted → stage 3.** `categories()` goes through the single sha-verified `load()`; the smoke loads and verifies the corpus at `import app`, and the bullet and verification 4c are restated to say so (the bullet's "lazy first-use load" also contradicts the D-35 amendment). A second manifest reader is rejected — it is a hole in SPEC-7's boundary. **Disposition 2026-08-27 — A**, choice named: `categories()` goes through the single sha-verifying `load()` — there is no second manifest reader, so SPEC-7's boundary has one path into the model-facing enum. `load()` is not memoised (re-walk, re-verify, rebuild on every call); `lookup` / `fetch_by_id` use the last-loaded index and call `load()` if none. Consequence stated as fact everywhere it was stated otherwise: `import app` loads and verifies the corpus at import (offline, files on disk; ~131 KB of sha256), so the CI `services` smoke reddens on a corpus fault before any hook; the lifespan hook's `load()` is a second, independent verification, which is exactly what `test_startup_hook_fails_boot_on_corpus_error` pins (monkeypatched `load`, reached only from the hook). Verification 4c's break now raises at `import app`, stated; the hook path is 4e's. D-35's clause "CI's `services` import smoke … is unchanged" was the falsified one — struck with a dated note 2 (not a new decision: D-35's mechanism stands, one consequence sentence was wrong). Residual (b) narrowed to what is true: the verification is CI-proven at import, the hook is not. Suite-side cost: every test that loads `app` now sha-checks 87 files at import (milliseconds) — no behaviour change. **Sites changed:** `plans/eligibility-assistant.md` D-35 (note 2); `corpus.md` `policy_index.py` row (loader semantics), `app.py` row, `policy_tool.py` row, verification 4c, verification 5, Gate interactions bullet 1, Landmines residual (b). |
| 5 | SPEC-10 · Gate r3 f1 disposition cell (`:217`, "`retrieval-eval.md` cites the setting by name only (`:45`) — unchanged") · eligibility-assistant-D-64 | **new** (untaggable — no recorded hash in r3's `checked:` line). Wrong fact in a filled disposition cell (fact-trail rule): `retrieval-eval.md:29` pins verification 1's expected output as `min recall@12 = <value> (<case>)` — the pre-D-64 cap — so the sweep that moved the default 12 → 6 left a stale expected value in a sibling ticket, and the cell records that it checked and found none. `:45` is by-name-only as stated; `:29` is not. Minor. | **Owner 2026-08-27: accepted → stage 3.** Minor; folds into f1's sweep, which this time is an enumerated grep with every hit listed under `Sites changed:`. **Disposition 2026-08-27 — A.** `retrieval-eval.md:29` → `min recall@5 = <value> (<case>)` (the value follows f1's amended default, not the r3 one). The r3 f1 cell's wrong claim ("cites the setting by name only (`:45`)") is corrected in place with a dated bracket — the fact-trail rule keeps the record honest rather than quietly right. The sweep this time is the enumerated grep in f1's cell, with `:29` among its hits. **Sites changed:** `retrieval-eval.md:29`; `corpus.md` Gate r3 f1 disposition cell (dated correction). |

Per-SPEC verdicts:

- **SPEC-7 — FINDING** (f2, f4).
- **SPEC-8 — satisfied.**
- **SPEC-9 — FINDING** (f4; f1 in part). The mechanism itself verified **empirically**
  against a downloaded `langchain-core==1.6.0` (`tools/base.py:253-256`
  `_SchemaConfig.extra="forbid"`; `utils/pydantic.py:261` subset model;
  `tools/structured.py:203-205` infers only when `args_schema is None`;
  `tools/base.py:834-836` `model_validate` → `model_dump`): explicit `args_schema` +
  `Literal[*tuple]` raises `ValidationError` on an extra key, on a document id and on free
  text with the function never reached, while the `@tool`-inferred schema silently drops the
  extra key and calls the function. Verification 4d holds as written. `import
  langchain_core.tools` pulls **no** `langsmith` module, so `requirements.txt:6-7`'s
  lazy-import comment survives this ticket true.
- **SPEC-10 — FINDING** (f1, f3).
- **SPEC-11 — residual-named** (Landmines residual (a): indexing proven, curation not; the
  "tracked artifact" clause covered by verification 4f).
- **SPEC-38 — satisfied.** D-38 note-2's tier partition recomputed from the package by
  `license_disposition` × `category` = **9 / 32 / 16 / 23 / 7**, tier 1 exactly the nine
  EMTALA/HIPAA-164 ids, with `DOC-SYN-EMERGENCY` / `DOC-SYN-PRIVACY-FD` at tier 4 (an
  id-fragment rule instead gives 5/36/16/23/7, which is verification 4n's expectation
  [corrected 2026-08-27, gate r5 f1: the id-fragment counter-partition is **7 / 34 / 16 / 23 / 7** —
  the bare `42CFR` fragment also promotes `DOC-FED-42CFR-435-POINTER` / `-438-POINTER`; 4n now
  states that figure]).
- **SPEC-43 — satisfied** (rank key re-derived over all 87 rows).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, `corpus.md` Changes (17 rows) / Verification 1–4n, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r3, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `git hash-object docs/workflow/plans/eligibility-assistant/corpus.md` → `e28cbedb525ec37d738cf02d29c42f6abb58a230`** · previous round (r3) recorded **no** plan-text hash and rounds 1–2 carry no `Sites changed:` lists, so no finding this round is settleable as `pre-existing` — untaggable → `new` by rule · **origin: 4 new · 0 pre-existing · 1 class-repeat** · spawning prompt = item + ticket only, no branch and no characterization (read `main` + the uncommitted working tree) · check map 66/66 rows carry exactly one `test:` / `cmd:` / `gate:` token (scripted over the frozen table) · ⚠ rows in set: 7, 8, 9, 10 (4) — PHI-handling zone entered, approval of record eligibility-assistant-D-56 cited by ID and date (owner 2026-08-25, registered 2026-08-26) with the D-61/62/63/64 carry-forward sentence; all **12** of `docs/landmines.md` §1 zones written out entered/not-touched in this ticket's own block (13 `⚠️ **` hits in §1, one the nested `auth.yaml` sub-note), Intake bullet's locator `docs/landmines.md:87` holds; deliberate defects stated preserved; four residuals written; every ⚠ row carries a negative check (3/4/4b, 4, 4d, 4g/4j/4l) · freeze scope: no `DEFERRED` requirement anywhere in the contract, every row on a current REQ · ownership: 66/66 rows owned exactly once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated), `Scope:` lines = `## Delivery` table; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC / decision / evidence id resolves; change list closes both ways except f3 · **facts verified in tree vs `main`**: `app.py:51` `app = FastAPI(...)`, no `lifespan`/`on_event`; `app.py:30-36` imports `config`, `logging_config`, `eligibility_client`, `llm_client`, `templates`, `visit_templates`, `schemas`; `config.py:64` `LLM_MAX_INPUT_TOKENS` 20,000, `:75` chars = tokens × 4; `llm_client.py:336-337` 8 B base / 8 B per message, `:368-375` UTF-8 byte total incl. `extra_body`, `:458-461` `_enforce_budget`, `:494-495` call site; `langsmith==0.10.5` in `services/ai-assistant/requirements.txt:9` and root `requirements-dev.txt`, no `langchain*` tracked; ai-assistant `Dockerfile:3` `WORKDIR /app`, `:8` `COPY . .`, `.dockerignore` three entries (no `.DS_Store`), all nine service/frontend `.dockerignore` files root-only; `.gitignore:6 .DS_Store`; CI `services` install `:73` / smoke `:76`, `tests` `:86`, gitleaks v8.18.4 `:120-129`, `docker-build` build-only note `:222`, `workflow-doc-cap` 400 lines (contract 339); `tests/README.md:32` "AI paths"; `CLAUDE.md:141` baseline `1317 passed, 19 deselected, 1 xfailed`; ADRs 0001–0018 present, 0019 absent, 0006/0011 `**Status:** Accepted`, `adr/0011:373` "no tools exposed" survives (tool lands dark); `tests/fixtures/` absent; no `with TestClient` anywhere in `tests/`; `.venv` 3.12.13; `tests/conftest.py` `load_module`; `test_compose_topology.py` host-port scan covers `docs/**/*.md` + `eval/**/*.py` + `tests/**/*.py` (not `services/`, not `tests/**/*.md`) — no ticket file trips it; `.gitleaks.toml` extends defaults · **falsified-claims sweep** (landmines / debt-log / phi-policy register / todo / CLAUDE.md / ARCHITECTURE.md / runbook / tests README / ADR 0006 / 0011 for ai-assistant, corpus, retrieval, dependency and boot claims): `ARCHITECTURE.md` and `docs/landmines.md` name the assistant nowhere; `runbook.md`'s two hits are the RIV-160 eval and a seed-duplication sentence; `debt-log.md` / `todo.md` corpus hits are `eval/rag` and the Postgres PHI corpus — CLAUDE.md §6 baseline is the only falsified clause and has a change row · **client package** (`~/Documents/Work/ClientInput/…-2026-08-24`, read-only): 87/87 `approved`, 25 categories, 131,032 B / max 2,448 / median 1,445, `retrieval_date` `2026-08-24` on 87/87, `version_effective` prose, `section_labels` a `|`-string (142 labels, 39 multi-label, 55 heading-matching, **62/87 docs match none**), 0 sha mismatch (verification 2's command run verbatim → `87 rows 0 mismatched`), paths `documents/` 83 + `procedures/` 4, D-38 note-2 tier partition recomputed = 9 / 32 / 16 / 23 / 7; 32 harness cases, 27 naming a `DOC-*`/`procedures/*` source (others EVAL-010/011/029/030/031), 7 `FIX-NEG-*` fixtures, EVAL-006 → `DOC-SYN-CITATION-RECENCY` (`source-recency`); D-64's cited byte figures reproduced exactly (12 largest 23,896; `payer-eligibility-public` 14 rows / 24,496 B, ≤ 3 per payer + 3 non-payer-specific; COB 7 rows / 11,363 B; `medicaid-managed-care` 7 rows / 9,307 B) · **`langchain-core==1.6.0`** wheel downloaded to scratchpad plus a live scratch venv (`langsmith==0.10.5` / `pydantic==2.10.4`): SPEC-9 mechanism and verification 4d executed and holding (per-SPEC verdict above) · docker daemon unavailable, so f2 rests on Docker's documented `filepath.Match` ignore semantics, not on an executed build · wrong facts / unbounded terms: 1 sizing term (f1), 1 wrong mechanism (f2), 1 stale sibling value (f5) · 5 findings.

### Gate — round 5, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run alongside the `llm-seam` gate (two agents, independent reads). Origin tags derived by
the agent from the record — round 4's `checked:` plan-text hash (`e28cbedb…`) and the
`Sites changed:` list on every round-4 disposition — not from recall. **Round-3 rule fires:**
three `class-repeat` findings and one `new` are open, so the owner adjudicates each finding
(accept as named residual · overrule · change the spec) before stage 3 dispositions them.
f4 is `pre-existing` and rides the same pass. f4 and f5 are minor. Plan returns to stage 3
after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-43 · verification 4n (`:48`) · r4 per-SPEC verdict (`:295-298`) · eligibility-assistant-D-38 amendment note 2 | **class-repeat** (r1 f3 / r3 f5 — a D-38 tier figure stated from a partial read of the manifest rather than derived over all 87 rows; r3 f5's disposition claimed the partition was "computed from the package 2026-08-27", but its counter-partition was not). Wrong fact: 4n's break ("classify tier 1 by the id fragments `42CFR` / `EMTALA-42CFR` / `HIPAA-164-` instead of license × category → partition **5 / 36** / 16 / 23 / 7") is wrong — `42CFR` also matches `DOC-FED-42CFR-435-POINTER` and `DOC-FED-42CFR-438-POINTER`, both `category: medicaid-managed-care` / `license_disposition: US Government work; public domain` (tier **2** under D-38 note 2), so the fragment rule promotes them and the real counter-partition is **7 / 34 / 16 / 23 / 7**. Re-derived this session over all 87 rows: license × category gives 9 / 32 / 16 / 23 / 7 (correct; tier 1 = the nine EMTALA / HIPAA-164 ids). The break still reddens `test_tier_rank_in_code` (`DOC-FED-EMTALA-CMS` does fall to tier 2), but the test is pinned on the partition, so an implementer following 4n asserts a number the tree does not produce. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Wrong fact fixed at its site: verification 4n now states the id-fragment counter-partition as **7 / 34 / 16 / 23 / 7** with the two `-POINTER` promotions named, recomputed this session over all 87 manifest rows (`python3` over the package `document-manifest.json`, `license_disposition` prefix × `category` → 9 / 32 / 16 / 23 / 7; substring rule `42CFR` / `EMTALA-42CFR` / `HIPAA-164-` on `document_id` → 7 / 34 / 16 / 23 / 7, the delta being exactly `DOC-FED-42CFR-435-POINTER`, `DOC-FED-42CFR-438-POINTER` in and the four fragment-less EMTALA ids `DOC-FED-EMTALA-CMS`, `-CRS`, `-ER-RIGHTS`, `-OIG` out — 9 − 4 + 2 = 7; only `DOC-FED-EMTALA-42CFR-489-24` and the four `HIPAA-164-` ids keep tier 1 by fragment; this cell's first draft said one id out and was corrected against the same recompute before the plan went back). The break still reddens `test_tier_rank_in_code` on the same three ids, so the test and 4n's expectation agree again. Class closed at the record, not only the plan: the r3 f5 cell's "computed from the package" claim carries a dated bracket saying the counter-partition was not, and the r4 SPEC-38 verdict's `5/36` carries the same bracket; eligibility-assistant-D-38 gains **note 3** (record only, no rule change) so the register holds the figure a fourth round would otherwise re-derive. Sweep for other tier figures stated in this file's plan text: `tests/test_a1_conflict.py` row (9 / 32 / 16 / 23 / 7, nine tier-1 ids, tier 5 = 7 — all rule-side, hold), verification 4n (fixed), Landmines (no partition stated) — three sites, one changed. Test count unchanged. **Sites changed:** `corpus.md` verification 4n, Gate r3 f5 disposition cell (dated bracket), Gate r4 per-SPEC verdict SPEC-38 (dated bracket); `plans/eligibility-assistant.md` D-38 (note 3). |
| 2 | SPEC-10 · eligibility-assistant-D-36 (item plan `:42`) · eligibility-assistant-D-61 (`:67`) · `config.py` change row (`:19`) | **class-repeat** (r4 f5 — the D-64 sweep left a stale cap value in a site whose cell recorded an enumerated sweep). Two decisions this plan cites still state the retrieval cap default as **6** — D-36 "capped by `A1_RETRIEVAL_MAX_ROWS` (default 12; **6 from 2026-08-27, eligibility-assistant-D-64**)" and D-61 "the default is 6 and the arithmetic is D-64's" — while D-64 as amended 2026-08-27 and this file's `config.py` row say **5**. The r4 f1 disposition's enumerated grep (`:272`) matched neither site, so the sweep reported clean while the register disagrees with the plan on the one number three rounds have moved. Sibling tickets are correct (`turn.md:41`, `:90`; `retrieval-eval.md:30`). | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Both register sites corrected in place with dated brackets: eligibility-assistant-D-36's cap clause now reads "6 from 2026-08-27, D-64; 5 from the same day's D-64 amendment", and D-61's headroom amendment now reads "6, then **5** from D-64's same-day amendment". Class (r4 f5 — a sweep whose enumerated grep missed a site) closed with a **phrase** sweep this time, not a line grep: every plan-text occurrence of a cap value stated as six — `default 6`, `default is 6`, `becomes **6**`, `**6 from`, `6 rows`, `≤ 6`, `to 6`, `= 6`, `cap of 6`, `6 documents`, `6 (was 12)` — over the contract, the item plan and all six ticket files, Findings sections excluded (history): `plans/eligibility-assistant.md` D-36 (**changed**), D-61 (**changed**), D-64 ("default becomes **6** (was 12)" — its own in-row amendment states 5 two sentences later, dated; left, the amendment is the record); `eligibility-assistant.md:3` and `:253` ("66 rows" — the spec row count, not a cap; no change); `corpus.md` Status line ("cap 6 → 5", history of r4; no change); `llm-seam.md`, `turn.md`, `trace.md`, `lifecycle.md`, `retrieval-eval.md` — zero hits (`turn.md:41` `.env.example` and `retrieval-eval.md:30` `recall@5` re-read, both 5). Two sites changed, nine checked. **Sites changed:** `plans/eligibility-assistant.md` D-36 (cap clause), D-61 (headroom amendment clause). |
| 3 | SPEC-7 · `policy_index.py` change row (`:15`, "Loader semantics (gate r4 f4)") · `tests/test_a1_corpus.py::test_non_approved_row_fails_load` (`:24`) · eligibility-assistant-D-57 | **new** (the anchored text is on gate r4 f4's `Sites changed:` list — "`corpus.md` `policy_index.py` row (loader semantics)"). Unnamed mechanism the r4 fix opened: `load()` "is not memoised — every call re-walks the roots, re-verifies every sha and **rebuilds the module-level index it returns**", `MAX_ROW_BYTES` "is set by `load()`", `lookup` / `fetch_by_id` "use the index of the last `load()`", and `root` is "overridable so later tickets build `tmp_path` variants" — but the plan never says whether `load(root=…)` mutates that module-level state. `test_non_approved_row_fails_load` calls `load(root=…)` on a `tmp_path` copy that loads clean (the `.DS_Store` leg); under the stated semantics that replaces the module index and `MAX_ROW_BYTES` for the rest of the session, pointing `lookup` / `fetch_by_id` at a torn-down tree — and `tests/test_a1_corpus.py` collects before `tests/test_a1_retriever.py`, whose `test_index_covers_every_row`, `test_in_process_read_only_capped` and `test_cap_binds_on_a_legal_call` read that state. Benign only by accident today (the copy is byte-identical); `lifecycle`'s D-57 `tmp_path` variants mutate rows. State the rule — a non-default `root` returns an index without touching module state, or a fixture re-`load()`s the vendored corpus. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Rule stated, first option taken: a non-default `root` **returns** the index and never publishes — the `policy_index.py` row now names the module attribute (`policy_index._INDEX`, `None` until the first default-root `load()`) and says `load(root=…)` leaves `_INDEX` / `MAX_ROW_BYTES` untouched; the one legitimate way to point `lookup` / `fetch_by_id` at a variant is a `monkeypatch` install of the returned index, restored at teardown. Pinned twice in `tests/test_a1_corpus.py`: both legs of `test_non_approved_row_fails_load` assert module state unchanged across the call, and a new `test_load_with_root_does_not_publish_module_state` (default-root load, then a variant with one row's text truncated → the returned index carries the truncation, `lookup` / `fetch_by_id` still serve the vendored row, `MAX_ROW_BYTES` still 2,789); verification **4o** is its break-then-revert (publish on every call → both tests red, `test_a1_retriever.py` alone still green — the leak is cross-file by construction). Cross-ticket consequence closed rather than left: `lifecycle`'s rig row (`tests/a1_rig.py`, `corpus_variant` + "the rig's turn helper accepts the root") relied on the unstated publish, so it now states the `monkeypatch` install, and `test_lookup_and_fetch_return_current_only` cites it; `retrieval-eval` / `turn` / `trace` name no `load(root=…)` call (swept: `load(`, `tmp_path`, `MAX_ROW_BYTES`, `module state` over the four sibling files — hits in `lifecycle.md` only) [**record corrected at gate r6 f4:** the grep also hits `turn.md:71` (`:72` after the `llm-seam` r6 f1 ADR-row insert) — verification 18's definition of what the prompt reserve excludes, "exactly what `corpus`'s `policy_index.MAX_ROW_BYTES` counts"; a definition, not a `load(root=…)` call or a module-state read, so the sweep's conclusion stands and its hit count was wrong]. Self-consistency: `categories()` runs a default-root `load()`, so `import policy_tool` publishes `_INDEX` — verification 4c / 5 and `test_cap_binds_on_a_legal_call`'s read of `MAX_ROW_BYTES` are unaffected. Test count +1 (`test_load_with_root_does_not_publish_module_state`); verification 9 reports `N` in Delivery, no fixed count exists in this file. **Sites changed:** `corpus.md` `policy_index.py` row (module-state clause, `_INDEX`), `tests/test_a1_corpus.py` row (`test_non_approved_row_fails_load` legs, `test_load_with_root_does_not_publish_module_state`), verification 4o (new); `lifecycle.md` `tests/a1_rig.py` row (install clause), `tests/test_a1_lifecycle.py` row (`test_lookup_and_fetch_return_current_only`). |
| 4 | SPEC-10 · verification 4l break 2 (`:46`, "set the default to 7 → the same test red on (ii)") · eligibility-assistant-D-64 sizing rule | **pre-existing** (r4 recorded plan-text hash `e28cbedb…`; r4 f1's `Sites changed:` enumerates "verification 4l (breaks 1 and 3 new)", so break 2 is on none of the r4 lists). Wrong attribution — (ii) has no isolating break: at `a1_retrieval_max_rows = 7`, rule (i) is `7 × 2,789 + 5,000 = 24,523 > 20,000` (`LLM_MAX_INPUT_TOKENS`, `services/ai-assistant/config.py:64`), which the plan states is asserted first, so the test reddens on (i) and (ii) is never reached. No cap value isolates (ii): (i) fails for every cap ≥ 6, (ii) needs cap ≥ 7. Either state that (ii) can only be reddened by a corpus/index change (dropping the claim that break 2 exercises it), or order the assertions so break 2 does what the cell says. Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** First option taken — the claim is dropped, not the assertion order: verification 4l's second break now states that **no cap default isolates (ii)** (rule (i) is asserted first and reddens for every cap ≥ 6, (ii) needs ≥ 7) and reddens (ii) from the corpus/index side instead — the loaded index monkeypatched to the four `eligibility-verification` rows → largest legal set 4 ≤ 5, (ii) red with (i) green — which is exactly the failure eligibility-assistant-D-64 wrote (ii) to catch ("a re-curation or `lifecycle` change that drops every legal set to ≤ cap"). Per-category counts behind the break recomputed from the package manifest 2026-08-27 (categories above 5 rows: `coordination-of-benefits` 7, `medicaid-managed-care` 7, `payer-training-summary` 7, `emergency-care-boundary` 6, `payer-eligibility-public` 14; `eligibility-verification` 4). Nothing else moves. **Sites changed:** `corpus.md` verification 4l (second break). |
| 5 | Registry upkeep · `tests/README.md` change row (`:28`, "`lifecycle.md:21` and `retrieval-eval.md:18` already present") | **class-repeat** (r4 f5 — a disposition's own edit left a stale cross-reference its sweep did not catch). Locator does not say what the claim says: `retrieval-eval.md:18` is the `adr/0019-*.md` change row; the `tests/README.md` row is at `retrieval-eval.md:19`. The shift is a consequence of gate r4 f3's own disposition, which added a `tests/test_a1_retriever.py` change row to `retrieval-eval.md` (its `Sites changed:` names the new row but not the line shift). `lifecycle.md:21` holds as cited. Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Locator corrected in the `tests/README.md` change row: `retrieval-eval.md:19` (re-read this session — `:18` is `adr/0019-*.md`, `:19` the `tests/README.md` row; `lifecycle.md:21` holds). Class (r4 f5) closed for this file's sibling line-cites: `lifecycle.md:21` (holds), `retrieval-eval.md:19` (**changed**), `turn.md:41` (`.env.example`, holds), `turn.md:71` / verification 18 (holds), `retrieval-eval.md:30` (`recall@5`, holds) — five cites, one changed; the cell that introduced the shift (r4 f3) is history and is not rewritten. **Sites changed:** `corpus.md` `tests/README.md` row. |

Verified and **not** a finding, for the record: the D-64 sizing mechanism re-derived and
correct — `MAX_ROW_BYTES` = 2,789 (`DOC-FED-COB-PROVIDER-SERVICES`, file 2,448 + 341 of
metadata; metadata range 97–341 over 87 rows), `5 × 2,789 + 5,000 = 18,945 ≤ 20,000`,
`6 × 2,789 + 5,000 = 21,734 > 20,000`; (ii)'s legal exercise holds (COB 7 rows / 11,363 B,
`medicaid-managed-care` 7 rows / 9,307 B, `payer-eligibility-public` max 6 under one payer);
the D-38 note-2 tier *rule* is right (license × category = 9 / 32 / 16 / 23 / 7, tier 1
exactly the nine `DOC-FED-EMTALA-*` / `DOC-FED-HIPAA-164-*` ids); SPEC-9's closed-schema
mechanism holds (explicit `args_schema=PolicyLookupArgs`, `extra="forbid"`,
`topic: Literal[*policy_index.categories()]`, 25 distinct `category` values in the package).

Per-SPEC verdicts: SPEC-7 **FINDING** (f3) · SPEC-8 satisfied · SPEC-9 satisfied · SPEC-10
**FINDING** (f2, f4) · SPEC-11 residual-named (Landmines residual (a); the "tracked artifact"
clause covered by verification 4f) · SPEC-38 satisfied · SPEC-43 **FINDING** (f1).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, `corpus.md` Changes (17 rows) / Verification 1–4n, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r4, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `43c7ac87cfdb0362a1de0580c7c2f935fa07df4d`** (round 4 recorded `e28cbedb525ec37d738cf02d29c42f6abb58a230`; every r4 disposition carries a `Sites changed:` list, so tags are derived from the record) · **origin: 1 new · 1 pre-existing · 3 class-repeat** · check map 66/66 rows carry exactly one `test:` / `cmd:` / `gate:` token (scripted over the frozen table, contract `:259-324`) · ⚠ rows in set: 7, 8, 9, 10 (4) — PHI-handling zone entered, approval of record eligibility-assistant-D-56 cited by id and date (owner 2026-08-25, registered 2026-08-26) with the D-61/62/63/64 carry-forward sentence; all 12 `docs/landmines.md` §1 zones written out entered/not-touched in this ticket's own block (13 `⚠️ **` hits in §1, one the nested `auth.yaml` sub-note; the Intake bullet's locator `docs/landmines.md:87` holds); deliberate defects stated preserved; four residuals written · freeze scope: no `DEFERRED` requirement anywhere in the contract, every row on a current REQ (3‴, 11‴, 13′) · ownership: 66/66 rows owned exactly once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated), `Scope:` lines = `## Delivery` table; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC / decision / evidence id resolves · change list closes both ways (17 rows: 15 to `Scope:` ids, `tests/README.md` + `CLAUDE.md` §6 to named registry upkeep; `case_selections.json` / `load(root=…)` / `rank`-as-a-function to D-57/D-58; the harness jsonl to D-10/D-35) · facts verified in tree vs `main`: `services/ai-assistant/app.py:51` `app = FastAPI(...)` with no `lifespan`/`on_event` in the file, `:30-36` imports; `config.py:64` `LLM_MAX_INPUT_TOKENS` 20,000, `:75` chars = tokens × 4; `llm_client.py:336-337` overheads = 8, `:368-375` UTF-8 byte total incl. `extra_body`, `:458-461` `_enforce_budget`, `:494-495` call site; `.dockerignore` three entries, `Dockerfile:3`/`:8`, `docker-compose.yml:223`; `.gitignore:6` bare `.DS_Store`; CI `services` matrix includes `ai-assistant` (install `:73`, smoke `:76`), `tests` installs `requirements-dev.txt` `:86`, `eval` runs `check_drift.py` (no globs), gitleaks v8.18.4 `:120-129`, `docker-build` build-only `:222`, `workflow-doc-cap` 400 lines over `docs/workflow/*.md` top level only (contract 339); `tests/README.md:32` "AI paths"; `CLAUDE.md:141` baseline `1317 passed, 19 deselected, 1 xfailed`; ADRs 0001–0018 present, 0019 absent, 0006/0011 Accepted; `tests/fixtures/` absent; no `with TestClient` anywhere in `tests/`; `tests/test_compose_topology.py:648-649` doc scan = `docs/**/*.md` + `eval/**/*.py` + `tests/**/*.py` · falsified-claims sweep (landmines / debt-log / phi-policy register / todo / CLAUDE.md / ARCHITECTURE.md §7 / runbook / tests README / ADR 0004, 0006, 0008, 0009, 0011 / CONTRIBUTING): no python dependency-addition policy exists (ADR 0008 is frontend-only); ARCHITECTURE.md and runbook name the assistant nowhere; debt-log D13/D14 and todo TODO-57/TODO-60 survive this ticket true; `CLAUDE.md` §6 baseline is the only falsified clause and has a change row · client package (read-only): 87/87 `approved`, 25 categories, 131,032 B / max file 2,448, path split federal 41 · synthetic 26 · payers/citation-only 14 · x12/citation-only 2 · procedures 4, `documents/.DS_Store` the only unlisted file and no manifest path missing, `retrieval_date` `2026-08-24` on 87/87, `version_effective` prose (0 ISO, 48 distinct), `section_labels` a `|`-string (142 labels, 39 multi-label, 55 heading-matching, 62/87 docs match none), row metadata 97–341 B → `max(file + metadata)` = 2,789, license prefixes 41 public-domain / 16 citation-only / 30 original-synthetic, D-38 note-2 partition 9 / 32 / 16 / 23 / 7 vs the id-fragment counter-partition **7 / 34 / 16 / 23 / 7** (f1), COB 7 / 11,363 B, `medicaid-managed-care` 7 / 9,307 B, `payer-eligibility-public` 14 rows split 1/2/3/3/2 across payers + 3 non-payer-specific, 32 harness cases, EVAL-006 → `DOC-SYN-CITATION-RECENCY` (`source-recency`) = tier 4 · docker daemon not exercised (verification 4k and r3 f2's `**/.DS_Store` rest on Docker's documented pattern semantics, as r4 recorded) · wrong facts / unnamed mechanisms: 2 wrong facts (f1 partition, f2 register default), 1 unnamed mechanism (f3), 1 wrong attribution (f4), 1 wrong locator (f5) · 5 findings.

### Gate — round 6, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run alongside the `llm-seam` gate (two agents, independent reads). Origin tags derived by
the agent from the record — round 5's `checked:` plan-text hash (`43c7ac87…`) and the
`Sites changed:` list on every round-5 disposition — not from recall. **Round-3 rule fires:**
all four open findings are `class-repeat`, so the owner adjudicates each finding (accept as
named residual · overrule · change the spec) before stage 3 dispositions them. f1, f2 and f4
are minor. Plan returns to stage 3 after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-7 / SPEC-10 · `policy_index.py` change row (`:15`, "Module state is published by the default root only") · `tests/test_a1_corpus.py` row (`:24`, `test_non_approved_row_fails_load`) | **class-repeat** (r5 f3 — `policy_index` module-state semantics left unnamed; the anchored text is also on r5 f3's `Sites changed:` list, so `new` by rule 2 if the class match is declined). The r5 rule names `_INDEX`'s pre-load value (`None` until the first default-root `load()`) but never `MAX_ROW_BYTES`'s, while the new assertion pins both in one breath: "`policy_index._INDEX` is `None` if no default-root `load()` has run, else the object it returned, and `MAX_ROW_BYTES` still 2,789". In the `_INDEX is None` branch `MAX_ROW_BYTES` cannot be 2,789 — it is whatever the module initialises it to, which the plan never states, so one half of the leg is unwritable as specified. (The sibling `test_load_with_root_does_not_publish_module_state` is safe: it does a default-root `load()` first; and r5 f3's cell covers `test_cap_binds_on_a_legal_call` via `import policy_tool` → `categories()` → `load()`.) State `MAX_ROW_BYTES`'s initial value, or force a default-root `load()` in the fixture, or assert module state as the before/after snapshot the phrase "unchanged across the call" describes. Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** First and third options together: the `policy_index.py` row now states `MAX_ROW_BYTES`'s pre-load value — **`None`, like `_INDEX`, until the first default-root `load()`** — and the `test_non_approved_row_fails_load` legs are restated as a before/after snapshot, `(policy_index._INDEX, policy_index.MAX_ROW_BYTES)` read before the `load(root=…)` call and `is`-identical after it — `(None, None)` or `(<default-root index>, 2789)` depending on whether `import policy_tool` has run in the session — so the leg is writable in either branch and asserts exactly what "unchanged across the call" says. Verification 4o is unchanged: a publish-on-every-call break moves the snapshot on the clean leg either way, and the `pending` leg raises before any assignment under both the plan and the break, so it stays green there as 4o already states. Self-consistency walked: `test_load_with_root_does_not_publish_module_state` does a default-root `load()` first and pins `2,789` after it — correct as written. Class (r5 f3 — a `policy_index` module-state fact left unnamed) closed with an enumerated sweep of every module-state site in the six ticket files: `corpus.md` `policy_index.py` row — `_INDEX` pre-load value (named), `MAX_ROW_BYTES` pre-load value (**now named**), the default-root publish site, `categories()`'s default-root load, `lookup` / `fetch_by_id`'s lazy default-root load, `load(root=…)` non-publish (all named); `corpus.md` `tests/test_a1_corpus.py` row — snapshot legs (**changed**), `test_load_with_root_does_not_publish_module_state` (holds); `corpus.md` `tests/test_a1_retriever.py` row — `test_cap_binds_on_a_legal_call` reads `MAX_ROW_BYTES` after `import policy_tool` (published — holds); `turn.md:72` (verification 18; `:71` before this round's `turn.md` ADR-row insert, `llm-seam` r6 f1) — reads `policy_index.MAX_ROW_BYTES` after `import app` (published — holds); `lifecycle.md:17` — the rig installs `_INDEX` only, never `MAX_ROW_BYTES`, and no `lifecycle` test reads `MAX_ROW_BYTES` (`grep -n MAX_ROW_BYTES lifecycle.md` → `:17` alone, re-run this session — holds); `lifecycle.md:14` / `:45` — the `load(root=None, required_current=())` signature, no state read (holds); `llm-seam.md`, `trace.md`, `retrieval-eval.md` — no `_INDEX` / `MAX_ROW_BYTES` site (grep clean, re-run this session). No test count moves. **Sites changed:** `corpus.md` `policy_index.py` row (module-state clause, `MAX_ROW_BYTES` pre-load value), `tests/test_a1_corpus.py` row (`test_non_approved_row_fails_load` snapshot legs). |
| 2 | SPEC-43 · `tests/test_a1_conflict.py` change row (`:26`) · verification 4n (`:48`) · eligibility-assistant-D-38 notes 2/3 | **class-repeat** (r1 f3 / r3 f5 / r5 f1 — a D-38 tier-1 membership statement asserted without deriving it over the ids it names). Two tier-1 statements are wrong as written. (a) `:26` "tier 1 is exactly the nine `DOC-FED-EMTALA-*` / `DOC-FED-HIPAA-164-*` ids (**so the `emergency` reason-table citations stay tier 1**)" — the D-19 `emergency` row cites `DOC-FED-EMTALA-CMS` **and** `DOC-SYN-EMERGENCY`, plus `DOC-COVERAGE-QUESTION-CHEAT-SHEET` on question type `emergency` (contract `:205`), and the latter two are **tier 4** under D-38 note 2 (package `document-manifest.json` re-read 2026-08-27: `DOC-SYN-EMERGENCY` = `emergency-care-boundary` × "Original synthetic training material…"; `DOC-COVERAGE-QUESTION-CHEAT-SHEET` = `front-desk-procedure` × "Original synthetic training material") — which the same row states two clauses later. (b) verification 4n "only `DOC-FED-EMTALA-42CFR-489-24` keeps tier 1 by fragment" — under the fragment counter-rule the four `HIPAA-164-` ids keep it too; 4n's own partition (7 = 1 + 4 + 2 `-POINTER` promotions, re-derived this round) requires them, and r5 f1's disposition states it correctly ("only … **and the four `HIPAA-164-` ids**"), so 4n is the one site the r5 fix left short. No pinned expectation moves — the partition and the tier-1 id set in the test row are correct — this is the fourth round on the same class of loose tier prose. Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Both wrong statements fixed at their sites, each derived over the ids it names from the package `document-manifest.json` this session: (a) the `tests/test_a1_conflict.py` row's parenthetical now reads that **`DOC-FED-EMTALA-CMS`** — the `emergency` row's one public-domain id — stays tier 1, while the row's other citations `DOC-SYN-EMERGENCY` (`emergency-care-boundary` × original synthetic) and, on question type `emergency`, `DOC-COVERAGE-QUESTION-CHEAT-SHEET` (`front-desk-procedure` × original synthetic; contract `:205`) are tier 4, the reason table citing by id and never by tier; (b) verification 4n now reads "only `DOC-FED-EMTALA-42CFR-489-24` **and the four `DOC-FED-HIPAA-164-*` ids** (`-502-MIN-NEC`, `-514`, `-506-TPO`, `-508-AUTH`) keep tier 1 by fragment", the 9 − 4 + 2 = 7 arithmetic the partition already carried. No pinned expectation moves; `test_tier_rank_in_code`'s id set and partition are unchanged. Class (r1 f3 / r3 f5 / r5 f1 — a tier-membership statement not derived over its ids) closed with an enumerated sweep of every tier statement in plan text (Findings excluded), each re-derived this session against the manifest's `license_disposition` × `category`: `corpus.md:26` "tier 1 is exactly the nine `DOC-FED-EMTALA-*` / `DOC-FED-HIPAA-164-*` ids" (holds — 5 + 4), its `emergency` parenthetical (**changed**), "`DOC-SYN-EMERGENCY` and `DOC-SYN-PRIVACY-FD` are tier 4" (holds — `emergency-care-boundary` / `privacy-minimum-necessary` × original synthetic), "`DOC-SYN-CITATION-RECENCY` … tier 4, not tier 5" (holds — `source-recency` × original synthetic), "tier 5 is category `payer-training-summary` alone (7 rows)" (holds — 7 rows counted), "an id-fragment rule reddens on `DOC-FED-EMTALA-CMS`" (holds); `corpus.md:48` verification 4n — `-CMS` / `-CRS` / `-ER-RIGHTS` / `-OIG` carry no fragment (holds), the fragment-keepers clause (**changed**), the two `-POINTER` promotions and 7 / 34 / 16 / 23 / 7 (hold, eligibility-assistant-D-38 note 3); `corpus.md` Landmines block — no tier-membership statement (grep `tier [1-5]` over `:61-93`, none); siblings — `retrieval-eval.md:31` "make the default ranker drop tier-5 rows" (a break instruction, no membership claim; holds), no other `tier [1-5]` site in `turn.md` / `trace.md` / `lifecycle.md` (grep re-run this session). The register (eligibility-assistant-D-38 notes 2 / 3) already states both facts correctly and is not edited. **Sites changed:** `corpus.md` `tests/test_a1_conflict.py` row (`emergency` parenthetical), verification 4n (fragment-keepers clause). |
| 3 | SPEC-7 (⚠, frozen: "a mismatch or an unlisted file … shall fail the suite") · `policy_index.py` row (`:15`, scan scope) · `tests/test_a1_corpus.py` row (`:24`, "an unlisted file **under the corpus path** fails") · verification 4b (`:36`) | **class-repeat** (carried f2 and r3 f4 — SPEC-7's unlisted-file clause narrowed by a plan-side scan rule). The loader's stated walk is "the manifest path roots `documents/` and `procedures/` under `policy_corpus/`" plus a two-file allowance "at the root"; a file under any *other* subdirectory of `policy_corpus/` (`policy_corpus/notes/rogue.md`) is at neither site, so the loader does not raise and no test reddens, while the frozen row and this ticket's own test row both say an unlisted file **under the corpus path** fails. Verification 4b breaks only the root-level case (`touch policy_corpus/README.md`). Bounded in consequence — rows are built from the manifest, so a rogue file is not citable and the Landmines "closed set pinned by sha" claim holds for the citable set — but the enforcement is narrower than the frozen clause, the change row and the test description, and this is the class r3 f4 resolved by amending the contract rather than by leaving plan and frozen text apart. State the scan over the whole `policy_corpus/` tree (every file is one of the two named root files or a manifest row; `.DS_Store` exempt at any depth) and give 4b the nested case. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Scan stated over the whole tree, as the frozen clause reads: the `policy_index.py` row now says the loader walks **all of `policy_corpus/` at every depth** and every file found is one of the two named root-level files or a manifest-row `path` with a matching sha — 87 paths today, 83 under `documents/` (depths 2–3) and 4 under `procedures/` (depth 1), counted from the package `document-manifest.json` this session — with any other file at any depth raising and `.DS_Store` the one exempt name at any depth; verification 4b gains the nested break (`mkdir -p policy_corpus/notes && touch policy_corpus/notes/rogue.md` → `test_manifest_sha_pinned` red; `rm -r` → green); the Landmines (1) sentence "no unlisted file may exist under the manifest path roots" now reads "anywhere under `policy_corpus/`, at any depth"; the `tests/test_a1_corpus.py` row ("an unlisted file under the corpus path fails") already said this and is unchanged. Register: eligibility-assistant-D-63 gains a dated note (scan = whole tree, `.DS_Store` exempt at any depth; no rule change — the r2 plan wording was narrower than SPEC-7's frozen text), and the D-56 carry-forward sentence names it (the owner's acceptance 2026-08-27 is the act that carries the approval to the widened scan). Self-consistency: the tree as vendored has no file outside the 87 paths and the two root files (`.DS_Store` aside), so `load()` on the vendored tree is green under the wider walk; `.dockerignore` `**/.DS_Store` and verification 4k are unaffected. Class (carried f2 / r3 f4 — a plan-side scan rule narrower than the frozen SPEC-7 clause) closed with an enumerated sweep of every site stating the scan's scope: `policy_index.py` row (**changed**), `tests/test_a1_corpus.py` row (holds), verification 4b (**changed**), verification 4 (`cp` into `documents/synthetic/` — a case under a root, holds), Landmines (1) (**changed**), eligibility-assistant-D-63 register text "at the corpus root and under the roots" (**dated note added**), the Findings preamble at `corpus.md:93` ("scope: manifest path roots + two named root-level files" — the r2 disposition's record, history, not rewritten), contract SPEC-7 as amended by eligibility-assistant-D-65 (frozen text says "an unlisted file" with no scope qualifier — holds, no spec change). **Sites changed:** `corpus.md` `policy_index.py` row (scan-scope clause), verification 4b (nested case), Landmines PHI (1) (closed-set sentence; D-56 carry-forward sentence); `plans/eligibility-assistant.md` eligibility-assistant-D-63 (dated note). |
| 4 | Gate r5 f3 disposition cell (`:324`, "swept: `load(`, `tmp_path`, `MAX_ROW_BYTES`, `module state` over the four sibling files — hits in `lifecycle.md` only") · `turn.md:71` | **class-repeat** (r4 f5 / r5 f2 / r5 f5 — an enumerated sweep recorded clean at a site that has a hit). Wrong fact in a filled disposition cell: `turn.md:71` (verification 18) reads "where 'row content' is exactly what `corpus`'s `policy_index.MAX_ROW_BYTES` counts" — a `MAX_ROW_BYTES` hit outside `lifecycle.md` (`grep -n "MAX_ROW_BYTES\|load(root\|tmp_path\|_INDEX"` over the five sibling files, re-run this round: `lifecycle.md:5,14,17,45`, `turn.md:71`). The sweep's *conclusion* survives — turn's use is a definition of what the prompt reserve excludes, not a read of published module state — so the fix is the record, not the rule. Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Record corrected in place: the gate r5 f3 disposition cell now carries a dated bracket naming the `turn.md:71` hit (`:72` after this round's `turn.md` ADR-row insert, `llm-seam` r6 f1) and why the conclusion stands (a definition of what the prompt reserve excludes — not a `load(root=…)` call or a module-state read). Class (r4 f5 / r5 f2 / r5 f5 — an enumerated sweep recorded clean at a site that has a hit) closed by **re-running every sweep the r5 dispositions recorded** and enumerating the hits: (i) r5 f3's grep `MAX_ROW_BYTES|load(root|tmp_path|_INDEX|module state` over the five siblings → `lifecycle.md:5` (Depends-on line), `:14` / `:45` (the `load(root=None, required_current=())` signature), `:17` (rig install — the one `MAX_ROW_BYTES` mention), `:28` / `:44` (`tmp_path` copies), `turn.md:72` (verification 18 — the missed hit, now recorded; `:71` when r5 swept); `llm-seam.md`, `trace.md`, `retrieval-eval.md` clean; (ii) r5 f2's cap-as-six phrase sweep (`default 6`, `default is 6`, `becomes **6**`, `6 rows`, `≤ 6`, `cap of 6`, `6 documents`, `= 6`) over the six ticket files, the item plan and the contract → hits only in gate `checked:` lines and the contract's Status lines (history) and in eligibility-assistant-D-64's own text, which carries its 6 → 5 amendment in the same row; no plan-text site — holds; (iii) r5 f5's five sibling line-cites — `lifecycle.md:21` (`tests/README.md` row), `retrieval-eval.md:19` (`tests/README.md` row), `retrieval-eval.md:30` (`recall@5`), `turn.md:41` (`.env.example`), `turn.md:91` (`A1_RETRIEVAL_MAX_ROWS=5` in the Secrets entry; `:90` before the insert) — all hold, re-read this session. The `turn.md` ADR-row insert this round shifts that file's cites after `:44` by one; no plan-text site in any ticket file cites a `turn.md` line (grep `turn.md:[0-9]` this session — hits only in Findings cells), today's cells cite the post-insert numbers, and earlier cells keep the numbers as read at their round (history, not rewritten). **Sites changed:** `corpus.md` gate r5 f3 disposition cell (dated record bracket). |

Verified and **not** a finding, for the record. Cap sizing re-derived independently this
round and correct: `MAX_ROW_BYTES` = 2,789 (`DOC-FED-COB-PROVIDER-SERVICES`, file 2,448 +
metadata 341; metadata range 97–341 over 87 rows), 5 × 2,789 + 5,000 = 18,945 ≤ 20,000,
6 × 2,789 + 5,000 = 21,734 > 20,000; eligibility-assistant-D-64 (ii)'s legal exercise holds
(COB × medicare = 7 rows, `medicaid-managed-care` 7, `eligibility-verification` 4 for 4l's
second break). The tier *rule* itself is right: license-prefix × category over all 87
package rows gives **9 / 32 / 16 / 23 / 7**, tier 1 exactly the nine `DOC-FED-EMTALA-*` /
`DOC-FED-HIPAA-164-*` ids; the id-fragment counter-rule gives **7 / 34 / 16 / 23 / 7** as 4n
now states. SPEC-9's mechanism re-checked against the r4 wheel findings and unchanged
(explicit `args_schema`, `Literal[*policy_index.categories()]`, both-ways enum pin, `*`-query
raise). 32 harness cases, 27 naming a `DOC-*` / `procedures/*` source, **no case expecting
more than 2 manifest sources** — so cap 5 leaves recall@cap mechanically reachable. One
off-by-one noted and deliberately not raised: the plan cites `services/ai-assistant/app.py`
imports at `:30-36` where the block is `:29-46` (`config` at `:29`); the set of names is
correct and nothing rests on the range.

Per-SPEC verdicts: SPEC-7 **FINDING** (f1, f3) · SPEC-8 satisfied · SPEC-9 satisfied ·
SPEC-10 **FINDING** (f1) · SPEC-11 residual-named (Landmines residual (a): indexing proven,
curation not; "tracked artifact" clause covered by verification 4f) · SPEC-38 satisfied ·
SPEC-43 **FINDING** (f2).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, `corpus.md` Changes (17 rows) / Verification 1–4o, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r5, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `66accc3abeac169406a39801bef790acd9619818`** (round 5 recorded `43c7ac87cfdb0362a1de0580c7c2f935fa07df4d`; all five r5 dispositions carry `Sites changed:` lists, so tags are derived from the record, not recall) · **origin: 0 new · 0 pre-existing · 4 class-repeat** · spawning prompt = item + ticket only, no branch and no characterization (read `main` + the uncommitted working tree) · check map **66/66** rows carry exactly one leading `test:` / `cmd:` / `gate:` token (scripted over the frozen table, contract `:259-324`) · ⚠ rows in set: 7, 8, 9, 10 (4) — PHI-handling zone entered, approval of record eligibility-assistant-D-56 cited by id and date (owner 2026-08-25, registered 2026-08-26) with the D-61/62/63/64 carry-forward sentence; all **12** `docs/landmines.md` §1 zones written out entered/not-touched in this ticket's own block (13 `⚠️ **` hits in §1, one the nested `auth.yaml` sub-note; Intake bullet locator `docs/landmines.md:87` holds); deliberate defects stated preserved; four residuals written; every ⚠ row carries a negative check (3/4/4b, 4, 4d, 4g/4j/4l) · freeze scope: no `DEFERRED` anywhere in the contract, every row on a current REQ (3‴, 11‴, 13′) · ownership: 66/66 rows owned exactly once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated), `Scope:` lines = contract `## Delivery` = item plan ticket table; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC / decision / evidence id resolves · change list closes both ways (17 rows: 15 to `Scope:` ids; `tests/README.md` + `CLAUDE.md` §6 to named registry upkeep + D-43; the harness jsonl / `case_selections.json` / `load(root=…)` / `rank`-as-a-function to D-10 / D-35 / D-57 / D-58) · **facts verified in tree vs `main` this round**: `services/ai-assistant/app.py:51` `app = FastAPI(...)`, no `lifespan` / `on_event` in the file, imports at `:29-46`; `config.py:64` `LLM_MAX_INPUT_TOKENS` 20,000, `:75` chars = tokens × 4; `llm_client.py:336-337` `_BASE_` / `_PER_MESSAGE_TOKEN_OVERHEAD = 8`, `:368-375` UTF-8 byte total incl. `extra_body`, `:458` `_enforce_budget`, `:494-495` call site; `services/ai-assistant/requirements.txt` 9 lines, `langsmith==0.10.5` at `:9`, lazy-import comment `:6-8`, no `langchain*`; root `requirements-dev.txt` langsmith 0.10.5, no `langchain*`; `.dockerignore` exactly three entries (`__pycache__/`, `*.pyc`, `.venv/`), `Dockerfile:3` `WORKDIR /app`, `:8` `COPY . .`, `docker-compose.yml:223` `build: ./services/ai-assistant`; `.gitignore:6` bare `.DS_Store`; `ci.yml:73` per-service install, `:76` `python -c "import app"` (working-directory `:75`), `:86` `requirements-dev.txt`, `:120-129` gitleaks v8.18.4 `--no-git`, `:222` "these steps prove every image COMPILES" / `:226-227` `docker compose build`; `tests/README.md:32` "AI paths"; `CLAUDE.md:141` `1317 passed, 19 deselected, 1 xfailed`; `docs/landmines.md:87` Intake registration; `tests/test_compose_topology.py:648-649` doc scan = `docs/**/*.md` + `eval/**/*.py` + `tests/**/*.py` (no `tests/**/*.md`, so the vendored fixtures do not enter it), and its `.env.example` assertions are key-specific (no all-keys parity, so `A1_RETRIEVAL_MAX_ROWS` landing in `config.py` ahead of `turn`'s `.env.example` row trips nothing); sibling line-cites all hold — `retrieval-eval.md:19` = `tests/README.md` row, `:30` = `recall@5`, `lifecycle.md:21` = `tests/README.md` row and `:17` carries the r5 f3 `monkeypatch` install clause, `turn.md:41` / `:71` / `:90` all `=5` / reserve · **client package** (`~/Documents/Work/ClientInput/…-2026-08-24`, read-only, recomputed this round): 87/87 `approval_status: approved`, 25 categories, 131,032 B total / max file 2,448 / row metadata 97–341 → `max(file + metadata)` = 2,789 on `DOC-FED-COB-PROVIDER-SERVICES`; license prefixes 41 public-domain / 16 citation-only / 30 original-synthetic; D-38 note-2 partition **9 / 32 / 16 / 23 / 7**, id-fragment counter-partition **7 / 34 / 16 / 23 / 7**; COB × medicare = the 7 rows the plan names exactly, `medicaid-managed-care` 7, `eligibility-verification` 4, categories above 5 rows = `payer-eligibility-public` 14 / COB 7 / `medicaid-managed-care` 7 / `payer-training-summary` 7 / `emergency-care-boundary` 6 (verification 4l's list, exact); 32 harness cases, 27 naming a `DOC-*` / `procedures/*` source (others EVAL-010 / 011 / 029 / 030 / 031), no case expecting more than 2 manifest sources; `DOC-SYN-EMERGENCY` / `DOC-COVERAGE-QUESTION-CHEAT-SHEET` / `DOC-SYN-CITATION-RECENCY` all original-synthetic → tier 4 (f2) · falsified-claims sweep (landmines / debt-log / phi-policy register / todo / `CLAUDE.md` / `ARCHITECTURE.md` §7 / runbook / tests README / ADR 0006, 0011): `CLAUDE.md` §6 baseline is the only falsified clause and it has a change row; `requirements.txt:6-8`'s langsmith lazy-import comment survives (`langchain_core.tools` pulls no `langsmith`, r4-executed) · docker daemon not exercised (4k and the `**/.DS_Store` pattern rest on Docker's documented `filepath.Match` semantics, as r4 / r5 recorded) · wrong facts / unnamed mechanisms: 1 unnamed initial value (f1), 2 wrong tier statements (f2), 1 scan-scope narrowing (f3), 1 wrong sweep record (f4) · 4 findings, all minor-to-moderate, all class-repeat.

### Gate — round 7, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run alongside the `llm-seam` r7 and `turn` / `lifecycle` / `trace` gates (independent
agents). Origin tags derived by the agent from the record — round 6's `checked:` plan-text
hash (`66accc3a…`) and the `Sites changed:` list on every round-6 disposition (including
the `llm-seam` r6 f1 disposition that touched this file). **Round-3 rule fires:** both open
findings are `class-repeat`, so the owner adjudicates each (accept as named residual ·
overrule · change the spec) before stage 3 dispositions them. f2 is minor. Plan returns to
stage 3 after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-7 / SPEC-10 · `tests/test_a1_corpus.py` row (`corpus.md:24`, `test_startup_hook_fails_boot_on_corpus_error` "against a `policy_index.load` monkeypatched to raise"; the snapshot legs) · `policy_index.py` row (`:15`, "Module state is published by the default root only … `tests/test_a1_corpus.py` collects before `tests/test_a1_retriever.py`, whose … all read that state") · `tests/test_a1_retriever.py` row (`:25`, "asserted equal to the module's value") | **class-repeat** (r5 f3 / r6 f1 — a `policy_index` module-state fact left unnamed; and `llm-seam` r3 f1 — no `sys.modules` pin, so a second copy of the module under test executes and the patch/identity assertion misses it. The anchored rows are also on r6 f1's `Sites changed:` list, so `new` by rule 2 if the class match is declined). Unnamed mechanism: four tests in three files rest on there being **one** `policy_index` module object shared with `app` — the lifespan test's monkeypatch must hit the object `app`'s hook calls, the `(_INDEX, MAX_ROW_BYTES)` snapshot legs and `test_load_with_root_does_not_publish_module_state` assert state that r5 f3 says leaks *across files*, and `test_cap_binds_on_a_legal_call` reads `MAX_ROW_BYTES` "published by `import policy_tool`". The repo convention loads service modules by path under **unique names** (`tests/conftest.py:16-27` `load_module(relpath, name)`; `tests/test_ai_visit_chat.py:51-73` pops the bare names, pins `config` / `llm_client` / … as `vc_*` copies, loads `app`, then **restores** the bare names; CLAUDE.md `:138-140` "pin `sys.modules` first"), and `policy_index` needs `config` (`settings.a1_retrieval_max_rows`, `:15`) so it is inside that collision class. A file that does `load_module("…/policy_index.py", "<its own name>")` gets its own `_INDEX` / `MAX_ROW_BYTES`, `app`'s bare `import policy_tool` → `import policy_index` gets another, and the lifespan monkeypatch, the cross-file state reasoning and the `MAX_ROW_BYTES` reads silently miss. `corpus.md` names no rig, no pin and no module-identity rule for its three test files (grep `sys.modules` / `load_module` / `bare name` → hits only in `checked:` lines). Name it: one pinned bare-name `policy_index` / `policy_tool` (and a pinned ai-assistant `config`) shared by `test_a1_corpus.py`, `test_a1_retriever.py`, `test_a1_conflict.py` and the `app_mod` load, with a first-assertion identity check the way `llm-seam` r3 f1 closed it (`agent_binding.llm_client is llm_mod`), or state that each file re-loads and the cross-file claims are dropped | **Owner 2026-08-27: accepted → stage 3.** Resolution chosen by the owner: **name the pinned rig**. **Disposition 2026-08-27 — A.** The rig is named and landed as a change row: `tests/a1_corpus_rig.py` (`:24`), the single site that loads the shared module set, following the house idiom re-read this session — `tests/conftest.py:16-26` `load_module(relpath, name)` (unique name; service dir onto `sys.path` at `:21-22`), `tests/test_ai_visit_chat.py:41-73` (the eight bare names at `:41-50`, pop/save `:51`, pin `:52-67`, `app_mod = load_module(…, "visit_chat_app")` `:68`, restore `:69-73`), `tests/test_llm_client.py:32-43` (the two-name form), `CLAUDE.md:138-140` "pin `sys.modules` first", `docs/onboarding-seam-map.md:13` (the same caveat as a seam rule), and `tests/test_ai_visit_chat.py:38-39` `import conftest` as the proof a sibling module under `tests/` is importable by bare name. It saves and pops the eight bare names plus `policy_index` / `policy_tool`, pins ai-assistant's `config` / `logging_config`, pins `policy_index` and `policy_tool` under their bare names, loads `app` as `a1_corpus_app` (so `app.py:30-36`'s imports and the two new ones bind the pinned objects), restores, and exports `policy_index` / `policy_tool` / `app_mod` / `settings`. **First assertion of every test** in the three files: `policy_tool.policy_index is policy_index` and `policy_index.settings is app_mod.settings`, with `app_mod.policy_index is policy_index` and `app_mod.policy_tool is policy_tool` added in `tests/test_a1_corpus.py` — the two the lifespan monkeypatch rests on — the `llm-seam` r3 f1 shape (`agent_binding.llm_client is llm_mod`, `llm-seam.md:15`); verification **4p** is its break-then-revert, both pins, reddening before any load, lookup or `TestClient`. Self-consistency walked over the fix: the rig's `app_mod` load imports `policy_tool`, whose `categories()` runs a default-root `load()`, so the rig's own import is what publishes `_INDEX` / `MAX_ROW_BYTES` — which settles the r6 f1 snapshot branch at `(<the default-root index>, 2789)` for these three files (the `(None, None)` pre-load form still holds, and the `policy_index.py` row now says so), leaves 4o's "a `tests/test_a1_retriever.py` run alone stays green" true (that file imports the rig too), and makes `test_cap_binds_on_a_legal_call`'s `MAX_ROW_BYTES` read and the cap `monkeypatch` act on one object. Class (r5 f3 / r6 f1 / `llm-seam` r3 f1 — a `policy_index` module-state or module-identity fact left unnamed) closed with an enumerated sweep of every site in the six ticket files, the item plan and the contract that loads one of these modules or reads its state: **`corpus.md`** `:15` `policy_index.py` row (**changed** — publication is the rig's import; "that state" named as one object), `:16` `app.py` row (holds — states the import, not a test), `:24` the rig (**new**), `:25` `tests/test_a1_corpus.py` (**changed** — rig import, four identity assertions, snapshot branch, the lifespan patch target named), `:26` `tests/test_a1_retriever.py` (**changed** — rig import + identity), `:27` `tests/test_a1_conflict.py` (**changed** — rig import + identity), `:38` verification 4c and `:52` verification 5 (hold — a subprocess `import` from the service dir, one interpreter, no bare-name collision), `:50` verification 4o (holds), `:51` verification 4p (**new**), the Gate-interactions CI `services` bullet (holds — same subprocess shape), the Landmines block (holds — no module-object statement; `grep -n "sys.modules \\| load_module \\| _INDEX"` over it, none); **`llm-seam.md`** `:15` binding rig with its pin and identity assert (holds — the class's origin and this fix's model), `:16` `tests/test_a1_trace.py` on the same rig (holds), `:17` dark `import agent_binding`, `:23` verification 5, `:26` verification 7f (hold); **`turn.md`** `:25` `tests/a1_rig.py` (**changed** — imports and re-exports the corpus rig instead of loading a second copy), `:26` the re-seamed `tests/test_ai_visit_chat.py` / `test_visit_chat_phi.py` (covered by `:25`), `:52` gate interaction and `:61` verification 5 (hold — subprocess), `:72` verification 18 (holds — a definition of what the reserve excludes, r6 f4's record, and the value it names is now the pinned module's); **`trace.md`** — no `policy_index` / `policy_tool` / `sys.modules` / `load_module` site (grep clean this session); **`lifecycle.md`** `:14` the `load(root=None, required_current=())` signature and `:16` the startup hook (hold — no state read), `:17` `tests/a1_rig.py` install clause (**changed** — the `monkeypatch` install targets the pinned object, else it lands on a copy the turn never serves), `:18` `tests/test_a1_lifecycle.py` (covered by `:17`), `:32` verification 2 (holds — subprocess), `:45` Landmines (holds); **`retrieval-eval.md`** `:13` / `:14` (hold — no module-object claim), `:15` `tests/test_a1_retriever.py` call sites (holds — `corpus`'s file, covered by its rig), `:16` `tests/test_a1_retrieval_record.py` (**changed** — imports the corpus rig), `:17` `tests/test_a1_retrieval_eval.py` (**changed** — same), `:27` gate interaction and `:33` verification 4 (hold — subprocess); **`plans/eligibility-assistant.md`** D-35 (**note 3 added** — its rationale's "tests reach them through `conftest.load_module`'s service dir" is true but not sufficient), D-57 (holds — its install clause lives in `lifecycle.md` and changed there), D-58 / D-61 / D-62 / D-63 / D-64 (hold — `grep -n "sys.modules \\| load_module \\| _INDEX"` over the register: no hit), **D-66 new**; **contract `docs/workflow/eligibility-assistant.md`** (holds — zero hits for `policy_index` / `policy_tool` / `sys.modules`; the frozen rows name tests, never module loading, so no spec change). Thirty-eight sites checked, eight changed, two new. Falsified-claims re-check on the fix: `docs/onboarding-seam-map.md:13` and `CLAUDE.md:138-140` are strengthened, not falsified; `tests/README.md:32`'s "AI paths" bullet lists test files and the rig is not one, so its change row is unchanged. Test count unmoved (the rig collects nothing). **Sites changed:** `corpus.md` `policy_index.py` row (`:15`, publication + one-object clauses), `tests/a1_corpus_rig.py` row (`:24`, new), `tests/test_a1_corpus.py` row (`:25`, rig + identity + snapshot branch + lifespan patch target), `tests/test_a1_retriever.py` row (`:26`, rig + identity), `tests/test_a1_conflict.py` row (`:27`, rig + identity), verification 4p (`:51`, new), Status line (`:3`); `turn.md` `tests/a1_rig.py` row (`:25`); `lifecycle.md` `tests/a1_rig.py` row (`:17`); `retrieval-eval.md` `tests/test_a1_retrieval_record.py` row (`:16`), `tests/test_a1_retrieval_eval.py` row (`:17`); `plans/eligibility-assistant.md` eligibility-assistant-D-66 (new), eligibility-assistant-D-35 (note 3). |
| 2 | SPEC-10 · verification 4l second break (`corpus.md:46`, "monkeypatch the loaded index to the four `eligibility-verification` rows only → … the same test red on (ii), with (i) still green") · `tests/test_a1_retriever.py` row (`:25`, rule (ii) "the maximum candidate count **from the vendored `index.json`** … both asserted against the tree as vendored") | **class-repeat** (r5 f4 — a 4l break claimed to redden (ii) that does not follow from the test as written; the anchored text is r5 f4's own `Sites changed:` site, in place at r6's hash and on no r6 list, so `pre-existing` if the class match is declined). Self-consistency: rule (i) is written to be **independent** of module state ("recomputed by the test itself from the vendored manifest and files … asserted equal to the module's value"); rule (ii) is written in the same register ("from the vendored `index.json`"). If (ii) parses `index.json` from disk, monkeypatching `policy_index._INDEX` cannot redden it and break 2 is a no-op; if (ii) drives `_filter` over the loaded index, it works. The plan does not say which, so the break's stated outcome rests on an unnamed choice. State (ii)'s data source as the loaded index (`_filter` over `policy_index._INDEX` for every enum combination), or re-write break 2 against whatever (ii) actually reads. Minor | **Owner 2026-08-27: accepted → stage 3.** Resolution chosen by the owner: **pin rule (ii) to the loaded index**. **Disposition 2026-08-27 — A.** The `tests/test_a1_retriever.py` row (`:26`) now states rule (ii) as: the test drives `policy_index._filter` over the loaded index `policy_index._INDEX` — the module state the rig publishes at import (r7 f1), never a re-parse of `index.json` from disk — once per enum combination (topic × payer × product × state), and the maximum candidate count over those combinations exceeds `settings.a1_retrieval_max_rows`. The two rules are now explicitly split by source: (i) against the tree as vendored, by the test's own recompute from the manifest and files compared to the module's `MAX_ROW_BYTES`; (ii) against the loaded index. Verification 4l's second break (`:47`) names its target accordingly — `monkeypatch` of `policy_index._INDEX` to the four `eligibility-verification` rows — and states why (i) stays green under it (the patch moves neither the on-disk recompute nor `MAX_ROW_BYTES`), so the break does what r5 f4's disposition claimed. Self-consistency walked: `_INDEX` is published before any test in the file runs (the rig, r7 f1), so (ii) has an index to drive `_filter` over; the `eligibility-verification` count of 4 and the >5 categories (COB 7, `medicaid-managed-care` 7, `payer-training-summary` 7, `emergency-care-boundary` 6, `payer-eligibility-public` 14) are the package figures 4l already carries, unchanged. Class (r5 f4 — a verification break whose stated outcome rests on an unnamed data source) closed with an enumerated sweep of every break-then-revert in the six ticket files' plan text, each read for whether the thing the break moves is named: **`corpus.md`** — 3 (the vendored file vs its manifest sha), 4 (the copied fixture under `documents/synthetic/`), 4b ×3 legs (files touched under the tree vs the loader's whole-tree walk), 4c (the vendored file, read at import through `policy_tool`), 4d (the tool's declared `args_schema`), 4e (`app.py`'s `lifespan=`), 4g ×2 (the monkeypatched `open` / `Popen` guards inside `test_in_process_read_only_capped`), 4h (the loader's `approval_status` check on the `tmp_path` copy), 4i (`rank`'s key over reversed manifest order), 4j (the index build, 142 vs 87 rows), 4k ×2 (the built image's file list), 4l break 1 (rule (i)'s recompute vs the module value), break 3 (`MAX_ROW_BYTES` module value vs the test's recompute), break 4 (the `payer="*"` call in `test_in_process_read_only_capped`), 4m (manifest categories vs the hand-written `Literal`), 4n (the tier partition over all 87 manifest rows), 4o (`_INDEX` / `MAX_ROW_BYTES` module state and the truncated `tmp_path` copy) — all hold; 4l break 2 (**changed**) and 4p (**new**, which names the rig's two pins as what it drops); **`llm-seam.md`** verifications 7, 7b, 7c, 7d, 7e, 7f, 7g, 7h, 7i — each names its source (the captured `invoke_model` body, the stubbed runtime's `calls`, the `caplog` records, the rig pin), hold; **`turn.md`** 13 (captured Bedrock bodies), 14 ×2 (the once-guard; the lowered cost cap in the rig), 17 (the schema), 18 (`MAX_ROW_BYTES` as the stated exclusion) — hold; **`trace.md`** 13 ×2 (captured bodies; emitted span metadata) — hold; **`lifecycle.md`** 2 (the manifest row on the real tree), 3 (`_filter`'s `status` predicate over the installed variant index), 4 (`load`'s `required_current` check) — hold; **`retrieval-eval.md`** 2 ×3 (the `LookupRecord` construction; the record's serialised fields; the filtered set the ranker substitution runs over, stated at `:17` "the same id set as the default ranker") — hold. Sixteen break-carrying entries in `corpus.md` (24 individual breaks) plus 22 in the five siblings: one changed, one new, the rest named at their site. **Sites changed:** `corpus.md` `tests/test_a1_retriever.py` row (`:26`, rule (ii) data source), verification 4l second break (`:47`). |

Verified and **not** a finding, for the record: the r6 "off-by-one noted and deliberately not raised" was itself wrong — `services/ai-assistant/app.py:30-36` is exactly `from config import settings` through `from schemas import (` and `app = FastAPI(...)` is `:51`, so the plan's locator holds as written. `fastapi==0.115.6` (`requirements.txt:1`) accepts `lifespan=`; the image is `python:3.12-slim` (`Dockerfile:1`) so `Literal[*tuple]` parses; `pydantic==2.10.4` carries `ConfigDict`. Verification 5's shape executed on today's tree (`env -i PATH=… python -c "import app"` from the service dir, no bytecode written) → `ok`. No `EVAL-0` token exists under `tests/` today, so verification 8's `2` is the ticket's own. The package figures reproduce exactly: 87/87 approved, 25 categories, paths `documents/` 67 at depth 2 + 16 at depth 3 = 83, `procedures/` 4 at depth 1, `documents/.DS_Store` the only unlisted file, 0 sha mismatch, max file 2,448, row metadata 97–341, `MAX_ROW_BYTES` 2,789 on `DOC-FED-COB-PROVIDER-SERVICES`; license × category partition 9 / 32 / 16 / 23 / 7 with tier 1 exactly the nine `DOC-FED-EMTALA-*` / `DOC-FED-HIPAA-164-*` ids, id-fragment counter-partition 7 / 34 / 16 / 23 / 7; `DOC-SYN-EMERGENCY`, `DOC-COVERAGE-QUESTION-CHEAT-SHEET`, `DOC-SYN-CITATION-RECENCY`, `DOC-SYN-PRIVACY-FD` all original-synthetic → tier 4 (r6 f2 holds); COB × medicare = the seven named rows, every one CMS/CRS/synthetic, none payer-specific; categories above 5 rows = exactly 4l's list, `eligibility-verification` = 4; 32 cases / 27 naming a `DOC-*`/`procedures/*` source; 7 `FIX-NEG-*` fixtures. `langchain-core==1.6.0` wheel re-downloaded to the scratchpad this round: `tools/structured.py:203-205` infers only when `args_schema is None`, `tools/base.py:253-256` `_SchemaConfig.extra = "forbid"`, `:834-835` `model_validate` → `model_dump`, `utils/pydantic.py:261` subset model `ConfigDict(arbitrary_types_allowed=True)` only, no `langsmith` import under `tools/` — SPEC-9's mechanism and `requirements.txt:6-8`'s lazy-import comment survive. Starlette's `TestClient.__enter__` re-raises a lifespan-startup exception (`wait_startup` → `task.result()`), so the pinned boot-fail shape is sound. Every sibling line-cite holds (`turn.md:41` / `:72` / `:91`, `retrieval-eval.md:18` / `:19` / `:30`, `lifecycle.md:17` / `:21`, `llm-seam.md:18` and `turn.md:46` README rows). The `llm-seam` r6 f1 `Sites changed:` list names the `corpus.md` `adr/0019-*.md` row, so that row's origin is settleable; its text matches D-42's dated note. One loose phrase left unraised: `:24` "**the same copy** with `documents/.DS_Store` touched loads clean" — read literally the copy still holds the `pending` row; "loads clean" and 4o's "clean variant" make the intent unambiguous.

Per-SPEC verdicts: SPEC-7 FINDING (f1); SPEC-8 satisfied; SPEC-9 satisfied (mechanism re-verified on the wheel); SPEC-10 FINDING (f1, f2); SPEC-11 residual-named (Landmines residual (a); "tracked artifact" clause covered by verification 4f); SPEC-38 satisfied; SPEC-43 satisfied (partition, tier-1 id set and the r6 f2 tier-4 statements recomputed over all 87 rows).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, `corpus.md` Changes (17 rows) / Verification 1–4o, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r6, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `cca1e6ec021c47a83bd97899b2e4cb4fd836812a`** (round 6 recorded `66accc3abeac169406a39801bef790acd9619818`; all four r6 dispositions and the `llm-seam` r6 f1 disposition that touched this file carry `Sites changed:` lists, so tags are derived from the record) · **origin: 0 new · 0 pre-existing · 2 class-repeat** · spawning prompt = item + ticket only, no branch and no characterization (read `main` + the uncommitted working tree) · check map 66/66 rows carry exactly one leading `test:` / `cmd:` / `gate:` token (scripted over the frozen table; SPEC-57 unallocated) · ⚠ rows in set: 7, 8, 9, 10 (4) — PHI-handling zone entered, approval of record eligibility-assistant-D-56 cited by id and date (owner 2026-08-25, registered 2026-08-26) with the D-61/62/63/64 carry-forward sentence and the D-63 whole-tree note; all 12 `docs/landmines.md` §1 zones written out entered/not-touched in this ticket's own block (Intake locator `:87` holds); deliberate defects stated preserved; four residuals written; every ⚠ row carries a negative check · freeze scope: no `DEFERRED` anywhere in the contract, every row on a current REQ (3‴, 11‴, 13′) · ownership: 66/66 rows owned exactly once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4), `Scope:` lines = contract `## Delivery` = item plan ticket table; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam, retrieval-eval · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC / decision / evidence id resolves · change list closes both ways (17 rows: 15 to `Scope:` ids; `tests/README.md` + `CLAUDE.md` §6 to registry upkeep + D-43; harness jsonl / `case_selections.json` / `load(root=…)` / `rank`-as-a-function / `adr/0019` to D-10 / D-35 / D-57 / D-58 / D-42) · **facts verified in tree vs `main` this round**: `services/ai-assistant/app.py:30-36` the seven named imports, `:51` `app = FastAPI(...)`, no `lifespan` / `on_event`; `config.py:64` 20,000, `:75` × 4; `llm_client.py:336-337` / `:368-375` / `:458-461` / `:494-495`; `requirements.txt` 9 lines — `fastapi==0.115.6`, `pydantic==2.10.4`, `langsmith==0.10.5` at `:9`, lazy-import comment `:6-8`, no `langchain*`; root `requirements-dev.txt` same `fastapi` / `pydantic` / `langsmith`, no `langchain*`; `.dockerignore` three entries, `Dockerfile:1` `python:3.12-slim`, `:3` `WORKDIR /app`, `:8` `COPY . .`, `docker-compose.yml:223` `build: ./services/ai-assistant`; `.gitignore:6` bare `.DS_Store`; `ci.yml:73` install / `:76` `import app` smoke (python 3.12, `:71`) / `:86` `requirements-dev.txt` / `:120-129` gitleaks v8.18.4 `--no-git` / `:222` "COMPILES" note; `tests/README.md:32` "AI paths"; `CLAUDE.md:141` `1317 passed, 19 deselected, 1 xfailed`, `:138-140` pin rule; `docs/landmines.md:87`; ADR 0006 / 0011 `**Status:** Accepted`, 0019 absent; `tests/conftest.py:16-27` `load_module` unique-name loader, `tests/test_ai_visit_chat.py:51-73` pop/pin/restore rig; `tests/test_compose_topology.py:648-649` scan = `docs/**/*.md` + `eval/**/*.py` + `tests/**/*.py`; no `EVAL-0` under `tests/`; verification 5's import shape → `ok` on today's tree; contract `:205` emergency row, `:253` Spec status, `:265` SPEC-7 as amended by D-65; sibling cites `turn.md:41` / `:72` / `:91`, `retrieval-eval.md:18` / `:19` / `:30`, `lifecycle.md:17` / `:21` all hold · **client package** (`~/Documents/Work/ClientInput/Armaan-Eligibility-Client-Inputs-Only-2026-08-24`, read-only, recomputed this round): 87/87 `approved`, 25 categories, depths 67 + 16 under `documents/` + 4 `procedures/`, `documents/.DS_Store` the only unlisted file, 0 sha mismatch, max file 2,448, metadata 97–341, `MAX_ROW_BYTES` 2,789 on `DOC-FED-COB-PROVIDER-SERVICES`, partition 9 / 32 / 16 / 23 / 7 vs fragment 7 / 34 / 16 / 23 / 7, tier-1 = the nine EMTALA / HIPAA-164 ids, the four named synthetic ids tier 4, COB × medicare = the seven named rows, categories > 5 = COB 7 / medicaid-managed-care 7 / payer-training-summary 7 / emergency-care-boundary 6 / payer-eligibility-public 14, `eligibility-verification` 4, 32 cases / 27 naming a manifest source, 7 `FIX-NEG-*` · **`langchain-core==1.6.0`** wheel downloaded to the scratchpad and inspected: SPEC-9 mechanism lines hold, no `langsmith` import under `tools/` · falsified-claims sweep (landmines / debt-log / phi-policy register / todo / `CLAUDE.md` / `ARCHITECTURE.md` / runbook / seam map / ADR 0006 / 0011): no registry names `policy_corpus` / `policy_index` / `policy_tool` / `langchain` outside ADR 0006's LangSmith URLs; `CLAUDE.md` §6 baseline is the only falsified clause and has a change row · docker daemon not exercised (4k / `**/.DS_Store` rest on Docker's documented `filepath.Match` semantics, as r4–r6 recorded) · unnamed mechanisms: 1 module-identity / pin rule (f1), 1 break data-source (f2) · 2 findings, both class-repeat, one minor.

### Gate — round 8, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names only (no
branch named; agent read `main` with the uncommitted working tree), no characterization
received. **Process change this wave (owner, 2026-08-27, cost):** two agents grouped by
mechanism instead of six — this ticket read in one context with `lifecycle` and
`retrieval-eval`; `llm-seam` / `turn` / `trace` in the other. Origin tag derived by the
agent from the record — round 7's `checked:` plan-text hash (`cca1e6ec…`) and the
`Sites changed:` lists on both r7 dispositions and on every cross-ticket disposition that
touched this file (`lifecycle` r1 f5/f7, `retrieval-eval` r1 f1/f3/f8(b)). **Round-3 rule
fires:** the one open finding is `class-repeat` (`new` by rule 2 if the class match is
declined — either way it fires), so the owner adjudicates it (accept as named residual ·
overrule · change the spec) before stage 3 dispositions it. Minor. Plan returns to stage 3
after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-7 / SPEC-9 / SPEC-10 · `tests/a1_corpus_rig.py` row (`corpus.md:24`, "the eight bare names that file pins (`config`, …, `breaker`, `eligibility_client` — `tests/test_ai_visit_chat.py:41-50`; `app.py:30-36` imports all of them)") | **class-repeat** (r6 f2 / r5 f1 — a membership statement asserted over the names it lists without deriving it; the anchored row is on r7 f1's `Sites changed:` list, so `new` by rule 2 if the class match is declined). Wrong fact: `services/ai-assistant/app.py:30-36` imports seven names — `config`, `logging_config`, `eligibility_client`, `llm_client`, `templates`, `visit_templates`, `schemas` — and `breaker` is not among them; `app.py` never imports `breaker` (its only two hits, `:508` and `:848`, are comments), it is `eligibility_client.py:29`'s import. Nothing rests on it (pinning `breaker` is the retained idiom and harmless), so the locator, not the rig, is wrong. Minor | **owner adjudicated 2026-08-27 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-27 — A.** Locator corrected at its site: `services/ai-assistant/app.py:30-36` imports **seven** names — `config`, `logging_config`, `eligibility_client`, `llm_client`, `templates`, `visit_templates`, `schemas` (re-read 2026-08-27) — and `breaker` is not among them: its only two hits in `app.py` are comments (`:508`, `:848`), and it is `eligibility_client.py:29`'s import (`from breaker import CircuitBreaker, EligibilityBreakerOpen`), pinned at `tests/test_ai_visit_chat.py:41-50` so that the copy `eligibility_client` binds is the pinned one. The rig row (`:24`) now says exactly that: eight names pinned, seven of them `app.py`'s direct imports and `breaker` the transitive one, so pinning it is the retained idiom and not a claim about `app.py`. No mechanism, test or count moves. Class (r6 f2 / r5 f1 — a membership or count statement asserted over the names it lists without deriving it) closed with an enumerated sweep of every such statement in this file's plan text, each re-derived this session from the tree or the read-only package: `:14` seven `medicaid-managed-care` rows, all CMS / Medicaid.gov / eCFR publishers (holds — `payer_publisher` read on all seven), eight single-row categories and `front-desk-scripts` = `DOC-SYN-NEVER-STATE` alone, applicability "All visits" (holds); `:15` 87 manifest paths = 83 under `documents/` (67 at depth 2 + 16 at depth 3) + 4 under `procedures/` at depth 1 (holds), `MAX_ROW_BYTES` 2,789 on `DOC-FED-COB-PROVIDER-SERVICES` (holds — recomputed, five fields + file); `:16` the seven `app.py:30-36` imports (holds); `:17` `.dockerignore` three lines (holds, r8 `checked:`); `:18` 25 distinct `category` values (holds); `:19` 5 × 2,789 + 5,000 = 18,945 ≤ 20,000 and 6 × 2,789 + 5,000 = 21,734 (hold); `:22` 27 of 32 cases naming a `DOC-*` / `procedures/*` source, the other five EVAL-010 / 011 / 029 / 030 / 031 (holds — harness joined against the manifest); `:23` seven `FIX-NEG-*` fixtures (holds); `:24` the eight pinned names (holds — `tests/test_ai_visit_chat.py:41-50`) and "`app.py:30-36` imports all of them" (**changed** — seven of eight); `:26` the seven COB × medicare rows by id (holds — `DOC-FED-COB-OVERVIEW`, `-RECOVERY-OVERVIEW`, `-PROVIDER-SERVICES`, `-GETTING-STARTED`, `DOC-FED-MSP-CRS`, `DOC-FED-MEDIGAP`, `DOC-SYN-COB-FRONT-DESK`); `:27` partition 9 / 32 / 16 / 23 / 7 by `license_disposition` prefix × `category`, tier 1 exactly the nine `DOC-FED-EMTALA-*` / `DOC-FED-HIPAA-164-*` ids, tier 5 = 7 rows (all hold); `:29` this ticket's three test files (holds); `:47` the five categories above 5 rows and `eligibility-verification` = 4 (holds). Fifteen sites, one changed. **Cross-ticket edits received in the same session, listed here so this file's next tag derivation has them:** `lifecycle` gate r2 f2 (eligibility-assistant-D-80) edited `corpus.md:15`, `:26`, `:47`, `:75` — its cell carries them. **Sites changed:** `corpus.md:24` (`tests/a1_corpus_rig.py` row — the seven-plus-`breaker` clause with `eligibility_client.py:29`); `corpus.md:3` (Status line). |

Verified and **not** a finding, for the record: every cross-ticket edit since r7 holds — `:15` / `:25` name `policy_index.CorpusLoadError` (`lifecycle` r1 f7), `:14` carries both curation obligations and both reproduce from the package (`front-desk-scripts` is a single-row category holding `DOC-SYN-NEVER-STATE` alone, applicability "All visits"; `DOC-FRONT-DESK-SCRIPTS` is `front-desk-procedure`; the seven `medicaid-managed-care` rows are all CMS / Medicaid.gov / eCFR publishers; `DOC-PAY-AVAILITY-ELIG`'s publisher line names Humana); `:29`'s sibling cites `lifecycle.md:30` / `retrieval-eval.md:19` / `:20` hold. Package recomputed this round: 87/87 approved, 25 categories, 0 sha mismatch, `MAX_ROW_BYTES` 2,789 on `DOC-FED-COB-PROVIDER-SERVICES`, tier partition 9/32/16/23/7 by license prefix × category, categories > 5 rows exactly 4l's list, `retrieval_date` `2026-08-24` on 87/87, 32 cases / 27 naming a retrievable source, 7 `FIX-NEG-*`, all seven reason-table ids present. Tree: `Dockerfile:1/:3/:8`, `.dockerignore` three lines, `docker-compose.yml:222-247` (`:223` build, no `volumes:` / `restart:`), `.gitignore:6`, `ci.yml:72-76/:86/:120-129/:135-155/:207/:222`, `conftest.py:16-26`, `test_ai_visit_chat.py:41-73`, `tests/README.md:32`, `CLAUDE.md:97-98/:138-140/:141`, `docs/landmines.md:87` all as cited. Registry sweep re-run: no registry names `policy_corpus` / `policy_index` / `document-manifest`; `CLAUDE.md:141` is the only falsified clause and has its row.

Per-SPEC verdicts: SPEC-7 FINDING (f1, wrong fact on the shared rig row; every mechanism verified); SPEC-8 satisfied; SPEC-9 FINDING (f1, same row); SPEC-10 FINDING (f1, same row); SPEC-11 residual-named (Landmines residual (a); tracked-artifact clause via 4f); SPEC-38 satisfied; SPEC-43 satisfied (partition and tier-1 id set recomputed over all 87 rows).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..79, Evidence E-1..7, `corpus.md` Changes (18 rows) / Verification 1–4p, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r7, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `0135a3d7439fd98c8183c20d10cc22defb890358`** (round 7 recorded `cca1e6ec021c47a83bd97899b2e4cb4fd836812a`; both r7 dispositions and every cross-ticket disposition that touched this file — `lifecycle` r1 f5/f7, `retrieval-eval` r1 f1/f3/f8(b) — carry `Sites changed:` lists, so the tag is derived from the record) · **origin: 0 new · 0 pre-existing · 1 class-repeat** · check map 66/66 single-mechanism (scripted over contract `:259-324`) · ⚠ rows in set 7, 8, 9, 10 — PHI-handling entered, approval of record eligibility-assistant-D-56 (owner 2026-08-25, registered 2026-08-26) with the D-61/62/63/64 carry-forward sentence and the D-63 whole-tree note; all 12 §1 zones written out in this ticket's own block; four residuals written · freeze scope: 0 `DEFERRED` · ownership 66/66 once (`Scope:` lines = `## Delivery` table = item ticket table; SPEC-57 unallocated) · cites resolve; change list closes both ways · facts verified in tree and package as listed above · docker daemon not exercised (as r4–r7 recorded) · 1 finding, minor, class-repeat.

### Gate — round 9, 2026-08-28

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names only (no
branch named; agent read `main` @ `762f0fd` with the uncommitted working tree), no
characterization received. Two agents by mechanism (owner, 2026-08-27): this ticket read
in one context with `lifecycle` and `retrieval-eval`; `turn` / `trace` in the other
(`llm-seam` GATED, excluded). Origin tag derived by the agent from the record — round 8's
`checked:` plan-text hash (`0135a3d7…`) and the `Sites changed:` lists on r8 f1 and on
every cross-ticket disposition since (`lifecycle` r2 f2 — `corpus.md:15/:26/:47/:75/:3` —
and r2 f3, `retrieval-eval` r2 f1/f2). **Round-3 rule fires:** the one open finding is
`class-repeat` (`new` by rule 2 if the class match is declined — `:15` is on `lifecycle`
r2 f2's `Sites changed:` list — either way it fires), so the owner adjudicates it (accept
as named residual · overrule · change the spec) before stage 3 dispositions it. Minor.
Plan returns to stage 3 after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-7 / SPEC-9 / SPEC-10 · `policy_index.py` row (`corpus.md:15`, "`categories(root=None)` … — eligibility-assistant-D-80, `lifecycle` gate r2 f2, **PROPOSED**:") vs Status line (`:3`), Landmines (`:75`), register D-80 | **class-repeat** (r5 f2 / r5 f5 / r6 f4 — a disposition's own edit left a stale token in a site its later step should have updated). Wrong fact on a decision's state: the change row that defines the loader states D-80 as PROPOSED, while the register ("confirmed (owner 2026-08-27, in session)"), this file's `:3` ("confirmed by the owner 2026-08-27, no owner call open") and `:75` ("re-opened and re-closed the same day by D-80 … confirmed") all record it confirmed and D-56 re-closed on it. `grep -n PROPOSED` over the three plan texts → this one site. No mechanism, test or count moves. Minor |  **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A.** The token replaced at `:15` with the confirmed record (owner 2026-08-27, in session) — the row now agrees with `:3`, `:75` and register D-80. Class sweep (a stale state token on a decision the record holds confirmed), `grep -n PROPOSED` over the six ticket files, the register and the contract, 2026-08-28: outside Findings tables (quoted findings are records) the only site was `corpus.md:15`; the register's rows use "Proposed <date>; **confirmed**" and every D-59..D-81 row carries its confirmation; contract 0 hits. The sibling class `retrieval-eval` r3 f1 closed the same day (`grep -n "in front of the owner"` over the six files → 0 hits outside Findings tables). No mechanism, test or count moves. Sites changed: `corpus.md:15` (`policy_index.py` row, one token), `:3` (Status line). |

Per-SPEC verdicts: SPEC-7 FINDING (f1 — stale state token on the row; every mechanism verified); SPEC-8 satisfied; SPEC-9 FINDING (f1, same row — the `categories()` clause that feeds the `topic` enum); SPEC-10 FINDING (f1, same row); SPEC-11 residual-named (Landmines residual (a); tracked-artifact clause via 4f); SPEC-38 satisfied; SPEC-43 satisfied (partition 9/32/16/23/7, tier-1 = the nine EMTALA/HIPAA-164 ids, id-fragment counter-partition 7/34/16/23/7 — all recomputed over the 87 package rows this round).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..80, Evidence E-1..7, `corpus.md` Changes (18 rows) / Verification 1–4p, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r8, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `3baddbaf12c8363d15eaeb18252e5852a5447035`** (round 8 recorded `0135a3d7439fd98c8183c20d10cc22defb890358`; r8 f1 and every cross-ticket disposition since carry `Sites changed:` lists, so the tag is derived from the record) · **origin: 0 new · 0 pre-existing · 1 class-repeat** · check map 66/66 single-mechanism (scripted over contract `:259-324`; SPEC-57 unallocated) · ⚠ rows 7, 8, 9, 10 — PHI-handling entered, D-56 cited by id and date with the D-61/62/63/64 carry-forward and the D-80 re-close; all 12 §1 zones written out; four residuals written · freeze scope: 0 `DEFERRED` · ownership 66/66 once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; `Scope:` lines = `## Delivery` = item ticket table) · cites resolve; change list closes both ways · facts verified in tree: `app.py:30-36` seven imports / `:51` / `breaker` only at `:508`, `:848` (comments), `eligibility_client.py:29`; `tests/test_ai_visit_chat.py:38-39/:41-50/:68`, `conftest.py:16-22`; `config.py:64`; `llm_client.py:458-461/:494-495/:728`; `ci.yml:73/:76/:86/:120-129/:135-155/:207/:222`; `Dockerfile:3/:8`, `.dockerignore` 3 lines, `.gitignore:6`, `docker-compose.yml:223`; `requirements*.txt` langsmith 0.10.5, no `langchain*`; `tests/README.md:32`, `CLAUDE.md:97-98/:139-141`, `docs/landmines.md:87`; no `adr/0019-*`, no `policy_*`/`tests/a1_*`/`tests/fixtures` on the tree · package (read-only, recomputed): 87/87 approved, 25 categories, paths 41/14/26/2/4 at depths 67+16+4, 0 sha mismatch, max file 2,448, `MAX_ROW_BYTES` 2,789 on `DOC-FED-COB-PROVIDER-SERVICES`, 142 `|`-labels / 62 docs with no heading label, 81 applicability strings, `retrieval_date` 2026-08-24 ×87, categories >5 = COB 7 / MMC 7 / PTS 7 / ECB 6 / PEP 14, `eligibility-verification` 4, eight single-row categories, 32 cases / 27 retrievable, seven reason ids present · `langchain-core==1.6.0` wheel re-downloaded to the scratchpad: `tools/structured.py:203-205`, `tools/base.py:253-256`, `:834-835`, `utils/pydantic.py:261` say what `:18` says; no `langsmith` under `tools/` · registry sweep re-run: no registry names `policy_corpus` / `policy_index` / `document-manifest`; `CLAUDE.md:141` the only falsified clause, has its row · docker daemon not exercised · 1 finding, minor, class-repeat.

### Gate — round 10, 2026-08-28

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names + branch
(`main`, working tree) only — no characterization received; agent read `main` @ `762f0fd`
with the uncommitted working tree. Two agents by mechanism (owner, 2026-08-27): this
ticket read in one context with `lifecycle` and `retrieval-eval`; `turn` / `trace` in the
other (`llm-seam` GATED, excluded). Origin tag derived by the agent from the record —
round 9's `checked:` plan-text hash (`3baddbaf…`) and r9 f1's `Sites changed:` list
(`:15`, `:3`); no cross-ticket disposition since r9 touched this file (`lifecycle` r3
f1/f2 and `retrieval-eval` r3 f1 list only their own files and register D-77).
**Round-3 rule does not fire:** the one open finding is `pre-existing` (the read widened;
the plan did not regress) — ordinary round, back to stage 3, no owner adjudication.
Minor. Spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-7 / SPEC-9 / SPEC-10 · `tests/a1_corpus_rig.py` row (`corpus.md:24`, "runs the house idiom once (`tests/test_ai_visit_chat.py:41-73` — save and pop the bare names, pin ai-assistant's copies, load the dependent module, restore)") · register eligibility-assistant-D-66 | **pre-existing** (r9 hash `3baddbaf…` recorded; r9 f1's list is `:15`, `:3`; `:24` on neither. Not r7 f1's class — that was duplicate copies of one module under `load_module`'s unique names, and the `policy_index` / `policy_tool` pins exist). The row says the rig runs the house idiom, but the idiom at `tests/test_ai_visit_chat.py:51-67` pins **all eight** popped names by path (`config`, `logging_config`, `schemas`, `llm_client`, `templates`, `visit_templates`, `breaker`, `eligibility_client`), while the rig as written (and D-66) pins four — `config`, `logging_config`, `policy_index`, `policy_tool` — and pops the other six without pinning, so `app.py:36`'s `from schemas import (…)` (and `llm_client` / `eligibility_client` / `templates` / `visit_templates`, `breaker` transitively) resolves by `sys.path` order — the cross-service bare-name collision `CLAUDE.md:138-140` says to pin against (`schemas.py` exists in seven services). The default run is masked: `test_a1_*` collects first and the rig's own first `load_module` inserts `services/ai-assistant` at `sys.path[0]` (`tests/conftest.py:21-22`), but any explicit invocation that has already loaded ai-assistant's dir and then another service's dir ahead of it (e.g. `pytest tests/test_visit_chat_phi.py tests/test_a1_corpus.py` — that file loads both ai-assistant and gateway apps, D-41 note) binds the other service's `schemas` and the rig import raises at `app.py:36`. Either pin all eight by path as the cited idiom does, or state that only four are pinned and why the six are safe. Minor | **Disposition 2026-08-28 — A, accepted as named residual (e) (owner, in session; owner override 2026-08-28, in session: stamped in the gate session without a fresh re-run; the drift-gate rule "never stamp with an open finding" set aside by explicit owner decision for this wave).** The rig row stands as written; residual (e) names the four-of-eight pin and states that the implementation pins all eight by path as the cited idiom (`tests/test_ai_visit_chat.py:51-67`) does — within the row's own cite, no mechanism, count or decision moves. Sites changed: `corpus.md` Landmines residual (e) (new), `:3` (Status line), this cell. |

Per-SPEC verdicts: SPEC-7 FINDING (f1, rig row; sha pin / whole-tree scan / `.DS_Store` / `approval_status` mechanisms all verified); SPEC-8 satisfied; SPEC-9 FINDING (f1, same row; explicit `args_schema` mechanism verified); SPEC-10 FINDING (f1, same row; cap arithmetic 5×2,789+5,000=18,945 and the 7-row COB×medicare set re-derived); SPEC-11 residual-named (Landmines residual (a); `4f` for the tracked clause); SPEC-38 satisfied; SPEC-43 satisfied (partition 9/32/16/23/7 and the nine tier-1 ids recomputed over the 87 package rows).

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against Decisions eligibility-assistant-D-1..81, Evidence E-1..7, `corpus.md` Changes (18 rows) / Verification 1–4p, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r9, the five sibling `Scope:` + `Depends on:` lines, the item ticket table and the contract `## Delivery` table · **plan-text hash read: `79f465824551c29478729ad3dc74fd044e68e32f`** (round 9 recorded `3baddbaf12c8363d15eaeb18252e5852a5447035`; r9 f1 carries its `Sites changed:` list; no cross-ticket disposition since r9 touched this file — `lifecycle` r3 f1/f2 and `retrieval-eval` r3 f1 list only their own files and register D-77) · **origin: 0 new · 1 pre-existing · 0 class-repeat** · check map 66/66 single-mechanism (machine-counted over contract `:259-324`) · ⚠ rows 7, 8, 9, 10 — PHI-handling entered, D-56 cited by id and date with the D-61/62/63/64 carry-forward and the D-80 re-close; all 12 §1 zones written out; four residuals written · freeze scope 0 `DEFERRED` · ownership 66/66 once (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; `Scope:` lines = `## Delivery` = item ticket table; SPEC-57 unallocated) · cites resolve; change list closes both ways · facts verified in tree: `app.py:30-36` seven imports, `:51` bare `FastAPI(...)`, `breaker` only at `:508`/`:848` (comments), `eligibility_client.py:29`; `Dockerfile:3/:8`, `.dockerignore` 3 lines, `.gitignore:6`, `docker-compose.yml:222-247` (`:223` build, `expose` only, `volumes:` only at `:10`/`:267`, zero `restart:`), `Makefile:34-35/:43-44/:46-47`, `ci.yml:72-76/:86/:120-129/:135-155/:207/:222`, `conftest.py:16-26`, `test_ai_visit_chat.py:38-39/:41-50/:51-73/:68`, `config.py:64/:75`, `llm_client.py:336-337/:368-375/:458-461/:494-495/:727-732`, `tests/README.md:32`, `CLAUDE.md:97-98/:138-141`, `docs/landmines.md:87`, `requirements*.txt` langsmith 0.10.5 and no `langchain*`, no `adr/0019-*`, no `policy_*` / `a1_*` on the tree · package recomputed (read-only): 87/87 approved, 25 categories, 0 sha mismatch, max file 2,448, `MAX_ROW_BYTES` 2,789 on `DOC-FED-COB-PROVIDER-SERVICES`, paths 41/26/14/2/4 at depths 67+16+4, `retrieval_date` 2026-08-24 ×87, categories >5 = COB 7 / MMC 7 / PTS 7 / ECB 6 / PEP 14, `eligibility-verification` 4, 142 labels / 62 docs with no heading label, 81 applicability strings, eight single-row categories, 32 cases / 27 retrievable, seven reason ids present, 7 `FIX-NEG-*`, no `status`/`superseded_by` key · docker daemon and the `langchain-core` wheel not re-exercised (r9 record stands) · 1 finding, minor, pre-existing.

**Stamped `plan GATED 2026-08-28` under owner override** (in session, 2026-08-28): f1 accepted as named residual (e) and dispositioned in the same act; no fresh re-run — the `drift-gate` "never stamp with an open finding" rule set aside by explicit owner decision for this wave, recorded here and in the Status line. Writes by the gate session under the override: this cell, residual (e), the Status line, the contract ticket row. Plan-text hash read this round (`79f46582…`) is the pre-override text; the stamped text differs from it by those sites only.

### Impl gate — round 1, 2026-08-29

Fresh-context session (this session wrote none of the branch); spawned `impl-gate-agent` and
`adv-reviewer-agent` in parallel with item + ticket + branch only, no characterization; neither
agent reported receiving any. Branch `feat/noref-eligibility-corpus`, 5 commits `0504b7e..e9eaaff`
over `main` @8b2a23b; `git diff main...HEAD --stat` 117 files, +7093/−58; uncommitted sibling
ticket files in the working tree excluded from the read. **Three findings — no stamp; back to
stage 4.** One red (CI-blocking), two minor record-shape. Spec unchanged. Mechanisms, tests and
counts all verified clean — nothing below moves code.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | Contract-file cap · `docs/workflow/eligibility-assistant.md` (414 lines; `main` 339) · `.github/workflows/ci.yml:135-160` `workflow-doc-cap` (cap 400, ratchet exemption list `e5b.md` only, "never add one") · `verify` `needs:` it | **Red.** The `### corpus — delivery DRAFT` record (`:341-414`, 74 lines, landed @e9eaaff) tips the contract over the 400-line cap; CI reddens at `workflow-doc-cap` and `verify` never runs, whatever the register says. The `implementation` evidence rule (references + ≤5-line notes) is the lever — the break-then-revert bullet is 9 lines, the deviations list 15. **Class, not instance:** the ticket table promises five more per-ticket records in this same file, so the fix is a compact per-ticket record shape that holds for six, not a one-off trim of this one; if the shape needs a README rule, that is a process change to record, not a silent convention. | |
| 2 | Change-list closure · stamp commit `0504b7e` (README landing rule: "the contract's ticket-row edit") · Delivery ticket table `:334-339` · `corpus.md:329` (`lifecycle.md:21`, `retrieval-eval.md:1x`) and the Delivery record ("the form `lifecycle`'s runbook command uses") | The stamp commit carries contract edits beyond the `corpus` ticket row — in-place frozen-text amendments owned by other tickets (SPEC-48 / D-74 `turn`; REQ-9 Notes / D-79 and SPEC-28 / D-81 `trace`) and the five sibling rows stamped `GATED`. All are registered owner decisions in the same commit's register and no code scope leaks — record shape. But `lifecycle.md` and `retrieval-eval.md` exist on neither HEAD nor `main` (`git ls-tree`: corpus / llm-seam / trace / turn only; both live only on local `wip/eligibility-assistant-plans`), and this branch's committed `corpus.md` and Delivery record cite them by line number — a reviewer of this PR cannot resolve the cites. Adjacent to gate r2 f2's class (a claim here depending on a DRAFT sibling); if stage 4 judges it the same class, closure is a sweep of every sibling-file cite in the branch's committed files plus a stated landing order for the `wip/` files, not a one-cite fix. Minor. | |
| 3 | SPEC-7 / 9 / 10 · `tests/a1_corpus_rig.py:37-55` vs the rig change row (`corpus.md:24`) · Landmines residual (e) (`:92`) · register eligibility-assistant-D-66 · Delivery "Deviations" | Plan text and D-66 say the rig pins four bare names (`config`, `logging_config`, `policy_index`, `policy_tool`); the shipped rig pins all eight house-idiom names plus the two — the form residual (e) said the implementation would take. Delivery says "residual (e) is closed, not carried" but does not list four→eight under Deviations, and the committed residual (e) / D-66 still describe the four-pin rig. Record-only — no mechanism, test or count moves; the Landmines block dies at merge but D-66 in `plans/eligibility-assistant.md` outlives this ticket, so the register is the site that needs the note. Minor. | |

Verified clean, for the record: all seven `test:` cells (SPEC-7, 8, 9, 10, 11, 38, 43) filled with
the frozen ids, each resolving to a real test in the diff, none renamed / removed / rewritten; no
`cmd:` or `gate:` cell in the checked set (**human observation required: none** — SPEC-40's `gate:`
is `trace`'s row); 87 documents + manifest + harness jsonl + seven `FIX-NEG-*` `cmp`-identical to the
client package; V2 `87 rows 0 mismatched`; V4f → `2`; V5 keyless `env -i` → `ok`; V8 → `2`; V19 empty;
V10 gitleaks over a clean `git archive HEAD` export (471 files) → `no leaks found`; index.json
curation obligations and cap arithmetic (5×2,789+5,000 = 18,945 ≤ 20,000; largest legal set 7 > cap)
re-derived from the tree; tier partition 9/32/16/23/7; all 18 change rows present in the diff;
idiom sweep clean (no routes, no logging in new code, no `Co-Authored-By` trailers, D-56 cited by
id + date in the PHI §1 entry); D13 untouched, no §1 / debt-log site in the `app.py` hunk; ADR 0019
carries every required template section. Break-then-revert (3, 4, 4b, 4d, 4e, 4g–4p) and 4k
accepted as recorded — re-running them mutates the tree.

**Baseline observed:** `make test-docker` → **1355 passed, 19 deselected, 1 xfailed** (35.38s);
`.venv` 3.12 `pytest -m "not integration" -q` → same (26.27s); no `skipped` term. The three new
files collect 21 (12 + 8 + 1); branch tree with them ignored → 1334 / 19 / 1 = the ticket row's
`Baseline at branch`; Δ +21 exactly the tests the branch adds, deselected and xfailed unmoved.

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 (7 ids, the `corpus` `Scope:` line) against the contract `## Spec` / `## Delivery` (ticket row + corpus DRAFT record), `plans/eligibility-assistant.md` Decisions D-1..81 / Evidence E-1..7, `corpus.md` Changes (18 rows) / Verification 1–4p, 5, 8, 9, 10, 19 / Gate interactions / Landmines / Findings r1–r10, `.github/workflows/ci.yml` · plan-text hash of `corpus.md` @HEAD `eaa569b433bdf308b48154d61f3746d61ac264d9` · checks: pinned-test diff 7/7 · contract cap **red 414 > 400** · `cmd:` none · `gate:` none · spec-first read · change-list closure both ways except the stamp-commit surplus (f2) · planted defects preserved · baseline 1355/19/1 (docker + venv) · idiom sweep clean · delivery evidence present, residuals as IDs · **3 findings: 1 red · 2 minor; 0 class-repeat of a dispositioned impl-gate class (first round)**

### Adv review — round 1, 2026-08-29

`adv-reviewer-agent`, frozen Spec + diff only, never the plan. **Clean — no findings.** Ran the
three new test files (21 passed) and the full default suite under a 3.12 venv (1355 / 19 / 1,
matching the Delivery record); independently recomputed manifest count 87, categories 25, tier
partition 9/32/16/23/7 = 87, `FIX-NEG` count 7, and confirmed `document-manifest.json` /
`index.json` are git-tracked (SPEC-11 "tracked artifact"). One observation recorded, **not a
finding** (test-coverage nuance, no production-code bug, no stated-spec violation):
`tests/test_a1_corpus.py::test_fix_neg_negative`'s "no marker text leaks into a retrievable row"
sub-check iterates `policy_index.lookup(category, "medicare", "unconfirmed", "unconfirmed")` per
category, so payer-specific rows for the other five payers are not exercised by that sub-check;
SPEC-8 stays independently satisfied by the id / hash / index-membership checks and the
whole-corpus `section_text != text` scan over all 87 rows in the same test.

checked: eligibility-assistant-SPEC-7, 8, 9, 10, 11, 38, 43 against `git diff main...HEAD` (117 files, +7093/−58) · `policy_index.py`, `policy_tool.py`, `config.py`, `app.py` lifespan hook, the three test files, `a1_corpus_rig.py`, `policy_corpus/`, `tests/fixtures/a1/` · 0 findings · 1 observation (above)
