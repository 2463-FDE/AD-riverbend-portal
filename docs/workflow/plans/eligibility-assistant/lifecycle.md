# eligibility-assistant / lifecycle — ticket plan file (deleted at its merge; contract: docs/workflow/eligibility-assistant.md)

Status: plan GATED 2026-08-28 (**owner override 2026-08-28, in session: stamped in the gate session without a fresh re-run; the drift-gate rule "never stamp with an open finding" set aside by explicit owner decision for this wave; gate r4's one finding accepted under the round-3 rule as named residual (i) and dispositioned A in the same act**) (**gate r3 dispositioned 2026-08-28, stage 3** — f1 `new`, **owner adjudicated 2026-08-28 under the round-3 rule: accepted → A**: the demo's four selections named in the runbook row and verification 6 (`covered today` · Aetna `AETN1224` · `unconfirmed` · `unconfirmed`), a deterministic in-container `policy_index.lookup('front-desk-scripts', …)` leg added to both, model₁'s unforceable topic choice written as residual (h), dated note on eligibility-assistant-D-77; f2 `pre-existing`, **A**: `reason_citation_ids()` returns a sorted tuple, stated once at `:15`, both pins compare without a wrap; item-plan cites re-pointed `:102` → `:103` after eligibility-assistant-D-81's row; twelve tests unchanged, no owner call open) (**gate r2 dispositioned 2026-08-27, stage 3** — all three findings **A**: f1 the Landmines approval-of-record paragraph brought to the confirmed record (D-75 / D-57 clause (c) confirmed and re-closing, the load-time exclusion named in place of the filter before `rank`); f2 NEW eligibility-assistant-D-80 (`categories()` manifest-wide over every approved row whatever its `status`, the index carrying `rows` and `categories` from one parse — **confirmed by the owner 2026-08-27 in session, no owner call open**; it re-opened eligibility-assistant-D-56 on this block and on `corpus`'s and the confirmation re-closed both the same day) with NEW `test_categories_survive_withdrawal` (11 → **12** tests), the runbook's 25-category pin, and cross-ticket edits to `corpus.md` (`:15`, `:26`, `:47`, `:75`); f3 the two item-plan locators re-cited by section per the README stable-ID rule, the six-file sibling-line-cite sweep enumerated (81 ticket-file resolutions plus 7 in register rows, five changed — `trace.md:25` among them)) (**gate r1 dispositioned 2026-08-27, stage 3** — all ten findings, every one **A**; NEW eligibility-assistant-D-75 (lifecycle state stays loader-side: the index is built from `current` rows only and carries no lifecycle field; amends eligibility-assistant-D-57), D-76 (the operator step is an image rebuild and recreate, not `docker compose restart`; amends eligibility-assistant-D-48 / D-57), D-77 (the named lifecycle document `DOC-SYN-NEVER-STATE` and the `index.json` curation obligation it puts on `corpus`) — **all three confirmed by the owner 2026-08-27 (in session)**, and the same act re-closed eligibility-assistant-D-56's §1 PHI approval on the amended mechanism (D-75's load-time exclusion in place of "the `current`-only filter before `rank`"); a dated note on eligibility-assistant-D-57 carries the two-load-site and `superseded_by` clarifications. **Cross-ticket revisions this round made:** `corpus.md` `policy_index.py` / `test_a1_corpus.py` rows now name `policy_index.CorpusLoadError` (f7) and `corpus.md`'s `index.json` row carries the second curation obligation (f5)) (**D-59..D-65 propagation sweep 2026-08-27, stage 3** — eligibility-assistant-D-63 propagated into the `load()` change row, separating `status` from `approval_status`; enumerated site lists in the eligibility-assistant-D-61 / D-62 / D-63 sweep notes in the decision register) (`tests/a1_rig.py` row rewritten 2026-08-27 by `corpus` gate r7 f1, eligibility-assistant-D-66)
Scope: SPEC rows eligibility-assistant-SPEC-58, -59, -60, -61, -62 (REQ-18; eligibility-assistant-D-48, eligibility-assistant-D-57 — corpus lifecycle: status field, startup enforcement, supersession and withdrawal turns, the runbook update procedure)
Depends on: corpus (the loader, `_filter` / `fetch_by_id` / `rank`, `load(root=…)`), turn (the turn rig `tests/a1_rig.py`, `outcome.py`'s reason table, `VisitFacts.last_citations` — SPEC-59 reads the reason set, SPEC-60/61 are asserted through the turn)

## Plan

Status: GATED 2026-08-28 (owner override, in session — see the header Status line and the closing gate round)

Changes (file level):

- `services/ai-assistant/policy_corpus/document-manifest.json` — every row gains `status: "current"` and `superseded_by: null`; nothing else in the file changes and no document file changes. **This is the one place the vendored manifest stops being byte-identical to the package (gate r1 f8):** `corpus` states that three ways — `corpus.md:13` "byte-identical to the package", `corpus.md:72` "a verbatim `document-manifest.json`", and the register's eligibility-assistant-D-35 "beside a verbatim `document-manifest.json`" — and eligibility-assistant-D-57 already carries the reconciling clause ("`corpus` lands the manifest verbatim … `lifecycle` adds the two keys to every row as `current`"), so the three sentences are scoped by that decision rather than rewritten; this ticket's `adr/0019-*.md` row states the delta in the record that survives these files. The sha pin is untouched: `content_sha256` pins each **document file**, never the manifest itself (`corpus.md:15`), so adding two keys to every row moves no digest, and the client artifact stays recoverable as this file minus the two keys (eligibility-assistant-D-57) (eligibility-assistant-SPEC-58 / eligibility-assistant-D-57)
- `services/ai-assistant/policy_index.py` — `load(root=None, required_current=())` validates lifecycle on every manifest row and raises **`policy_index.CorpusLoadError`** — the loader's own type, named and landed by `corpus` (`corpus.md:15`, `:25`; gate r1 f7 closed that class in the ticket that owns the symbol) — naming the offending id, before any lookup: `status` present and in the closed set `current` / `superseded` / `withdrawn`; **`superseded_by` non-null iff `status == "superseded"`** (gate r1 f9 — a `superseded` row carrying `null` and a `current` or `withdrawn` row carrying an id both raise; the "`null` unless `superseded`" wording left both legal), and where non-null a manifest id whose chain ends on a `current` row (a chain reaching a `withdrawn` row, a dangling id, and a cycle are each unresolved); every id in `required_current` `current`. `load()` already raises on a manifest row whose `approval_status` is not `approved` (landed by `corpus`, eligibility-assistant-D-63); `status` is a **separate closed field with its own values** and neither check reads the other's field — this ticket adds a validation beside that one and does not replace, widen or narrow it (eligibility-assistant-D-63's own clause: "`lifecycle`'s `status` axis (eligibility-assistant-D-57) is a separate field with its own values and is not read by this check"). **Where the lifecycle state lives — nowhere on the index row (gate r1 f3, eligibility-assistant-D-75, amending eligibility-assistant-D-57):** sha verification and the SPEC-7 whole-tree scan run over **every** manifest row whatever its status, so a superseded or withdrawn row's file stays vendored and pinned; the loader then builds the in-memory index from the approved rows that are `current`, **only**, so `lookup` / `_filter` / `fetch_by_id` are current-only by construction and before `rank`, with no per-row status field to read. **What is *not* narrowed to `current` — the model-facing enum (gate r2 f2, eligibility-assistant-D-80, confirmed by the owner 2026-08-27):** `policy_index.categories(root=None)` mirrors `load(root=None)` and returns `load(root=root).categories`, the sorted tuple of distinct `category` values over **every approved manifest row whatever its `status`** — the object `load()` returns carries `rows` (current-only) and `categories` (manifest-wide) from its one parse (`corpus.md:15`), so there is no second manifest reader (SPEC-7's boundary, `corpus` gate r4 f4) and no second module-level structure (D-75's non-publish reasoning; the `_INDEX` a `lifecycle` variant installs carries its manifest-wide categories with it). Consequence for this ticket's own rows: `front-desk-scripts` is a single-row category (package `document-manifest.json`, re-read 2026-08-27), so after the runbook's withdrawal step the topic **stays** in `PolicyLookupArgs.topic`, model₁ can still choose it, `lookup` returns zero rows and the turn renders SPEC-61's `refuse_definitive` + `escalate` with no citation — the demo's observation; a current-only enum would instead drop the topic from the tool schema and model₁ could never reach the refusal. `corpus`'s `test_topic_enum_equals_manifest_categories` (`corpus.md:26`) stays a three-way equality over that one manifest-wide set, because `index.json` keeps an entry per approved row whatever its status (`corpus.md:14`); the 25-category count moves only when a row in a **new** category is added, never on a supersession or withdrawal within an existing one. Pinned here by `test_categories_survive_withdrawal` (row below), break in verification 3. The index row therefore keeps exactly eligibility-assistant-D-61's six values (`id`, `title`, `section`, `version`, `retrieval_date`, `section-text`), `policy_index.MAX_ROW_BYTES` stays **2,789** and `corpus`'s three pins on it hold unchanged (`test_cap_binds_on_a_legal_call`'s recompute-and-compare, `corpus.md:26`; `test_load_with_root_does_not_publish_module_state`'s "still 2,789" and `test_non_approved_row_fails_load`'s `(…, 2789)` snapshot, `corpus.md:25`), and **no lifecycle value can egress in a model payload** because the row that egresses has no field to carry it. `index.json` — the SPEC-11 **tracked** artifact — is untouched by this ticket: it keeps one entry per approved row whatever its status (`corpus.md:14`), so `test_index_covers_every_row` and its 87 are unmoved, all 87 vendored rows landing `current` (eligibility-assistant-SPEC-58/59 / eligibility-assistant-D-57, eligibility-assistant-D-75)
- `services/ai-assistant/outcome.py` — exports `reason_citation_ids()`, returning a **sorted `tuple[str, ...]`** of the distinct document ids the reason table (eligibility-assistant-D-19) fetches (a tuple, not a set, so the hook passes it through unchanged and the pin below compares equal by value — **gate r3 f2**) — seven ids on today's contract table (`docs/workflow/eligibility-assistant.md:203-210`: `DOC-FED-EMTALA-CMS`, `DOC-SYN-EMERGENCY`, `DOC-COVERAGE-QUESTION-CHEAT-SHEET`, `DOC-SYN-PRIVACY-FD`, `DOC-SYN-NO-INVENTION`, `DOC-SYN-SPEND-STOP`, `DOC-SYN-MODEL-FAILURE`); `app.py`'s startup hook passes it as `required_current`. The set is exported from `outcome` rather than read by `policy_index` because `outcome` already imports `policy_index` for `fetch_by_id` (`turn.md:15`) — the loader cannot read it without a cycle, which is why SPEC-59's reason-id half is injected at one site (eligibility-assistant-SPEC-59)
- `services/ai-assistant/app.py` — the FastAPI lifespan startup hook (landed by `corpus`, `corpus.md:16`) becomes `policy_index.load(required_current=outcome.reason_citation_ids())`, and `app.py` gains `import outcome` in the module-import idiom its neighbours use (`app.py:30-36` — `import eligibility_client` / `import llm_client` / `import templates` / `import visit_templates`), so `app_mod.outcome` is the object the hook calls and a test can pin the argument against it. **The argument is pinned, not assumed (gate r1 f2):** `tests/test_a1_lifecycle.py::test_startup_hook_requires_current_reason_ids` spies on `app_mod.policy_index.load` through the boot and asserts the one call carried `required_current == app_mod.outcome.reason_citation_ids()` (both sides the sorted tuple the `outcome.py` row states, so equality is determinate — gate r3 f2), non-empty — verification 4b is its break. No request-path change: the turn already cites only what `lookup` / `fetch_by_id` return, so SPEC-60/61 follow from the loader (eligibility-assistant-SPEC-59/60/61 / eligibility-assistant-D-75)
- `tests/a1_rig.py` — `corpus_variant(tmp_path, *, supersede=(), withdraw=())` copies `policy_corpus/` into `tmp_path`, writes a successor file `documents/synthetic/<id>-V2.md` with its computed sha and manifest row + `index.json` entry for each superseded id, marks rows, and returns the root for `policy_index.load(root=…)`; the rig's turn helper accepts the root. **Install (`corpus` gate r5 f3):** a non-default `root` never publishes module state — `load(root=…)` returns the index and leaves `policy_index._INDEX` / `MAX_ROW_BYTES` untouched (`corpus`'s `policy_index.py` row) — so the helper installs the returned index as `policy_index._INDEX` through `monkeypatch` for the test's duration (restored at teardown; nothing leaks into a later file), which is the only way `lookup` / `fetch_by_id` and the turn see the variant (eligibility-assistant-D-57). Installing `_INDEX` alone is **sufficient**, and that is a consequence of eligibility-assistant-D-75: the index `load(root=…)` returns is already current-only, so there is no second module-level structure to install and none to restore (gate r1 f3). **Module object (`corpus` gate r7 f1, eligibility-assistant-D-66):** the `policy_index` the install targets is the one `corpus`'s `tests/a1_corpus_rig.py` pins and `tests/a1_rig.py` re-exports — the same object `app` holds — so the install is visible to the turn; an unpinned second copy would take the install and leave the served index untouched. `outcome` needs no pin of its own: this ticket's tests reach it as **`app_mod.outcome`**, the attribute the pinned `app` module binds at its own `import outcome` (`app.py` change row above), which is by construction the object the startup hook calls — so `test_startup_hook_requires_current_reason_ids` compares the hook's argument against the same module the hook read it from
- `tests/test_a1_lifecycle.py` — NEW, **twelve tests** (7 as authored, +1 for the hook argument, +1 for the model-offered superseded id, +2 chain cases — gate r1 f2 / f4 / f9 / f10; +1 for the manifest-wide enum — gate r2 f2, eligibility-assistant-D-80). The lineage every turn-level test uses is eligibility-assistant-D-77's: `DOC-SYN-NEVER-STATE` (category `front-desk-scripts`, the sole row of that category — package `document-manifest.json`, read 2026-08-27) superseded by `DOC-SYN-NEVER-STATE-V2`. Every direct `lookup` / `fetch_by_id` call below **unpacks the `(rows, record)` two-tuple** and asserts on `rows` alone — the return shape `retrieval-eval` lands (eligibility-assistant-D-58 / D-69), which sits ahead of this ticket in eligibility-assistant-D-59's order, and which `retrieval-eval.md:15`'s own change-list closure already counts this file as written against; the record is discarded here, its log line being `retrieval-eval`'s emitter (eligibility-assistant-D-68).
  `test_lookup_and_fetch_return_current_only` (on a `corpus_variant` root installed per the rig row: a superseded and a withdrawn row are absent from `lookup` and from `fetch_by_id` of their own ids, the successor is present, and the loaded index's row count equals manifest rows minus non-current rows — the eligibility-assistant-D-75 exclusion asserted directly, not only through its effect);
  `test_noncurrent_file_retained_and_pinned` (the superseded file still exists under the variant root and a one-byte change to it still raises `CorpusLoadError` — SPEC-7 holds on non-current rows because the sha pass runs over every manifest row whatever its status);
  `test_load_fails_on_unresolved_chain` **×4** (gate r1 f9: `superseded_by` → a `withdrawn` row; the cycle `A → B → A`; `status: superseded` with `superseded_by: null`; `status: current` with a non-null `superseded_by` — each raising `CorpusLoadError` naming the row);
  `test_load_fails_on_noncurrent_reason_id` (withdraw `DOC-SYN-NO-INVENTION` → `CorpusLoadError` naming it, before any turn);
  `test_startup_hook_requires_current_reason_ids` (**gate r1 f2** — the hook's *argument* is what is pinned, since the check inside `load` has one caller: `app_mod.policy_index.load` is monkeypatched to a recording spy that delegates nothing, `with TestClient(app_mod.app)` boots against the real vendored corpus, and the single recorded call is asserted to carry `required_current == app_mod.outcome.reason_citation_ids()` — the sorted tuple the `outcome.py` row states as the return type, compared without a wrap (gate r3 f2) — non-empty and equal to the seven ids of the contract reason table (`docs/workflow/eligibility-assistant.md:203-210`); the identity `app_mod.policy_index is policy_index` the rig asserts is what makes the patch reach the hook, eligibility-assistant-D-66);
  `test_superseded_never_cited` (through the turn rig: turn 1 on the unmodified corpus cites `DOC-SYN-NEVER-STATE`; on the superseded variant a turn on topic `front-desk-scripts` renders `DOC-SYN-NEVER-STATE-V2`'s four fields, **the turn's retrieved set is asserted to be exactly `{DOC-SYN-NEVER-STATE-V2}`** from the row keys the rig captures in the model₂ message (**gate r1 f4** — without that assertion the scripted model₂ names the successor either way and the test cannot redden), and no citation carries the superseded id — on that turn and on the next turn of the same visit whose `last_citations` still holds the old id);
  `test_superseded_id_offered_by_model_is_rejected` (**gate r1 f4 / f10** — the negative form that actually proves "shall not cite the superseded id": on the same variant model₂ is scripted to select `DOC-SYN-NEVER-STATE`, an id outside the turn's retrieved set, so SPEC-13's `citation_ids ⊆ retrieved` gate rejects it → reason `validation_reject`, mode `fallback`, citation `DOC-SYN-NO-INVENTION` (SPEC-52, contract reason table) and the superseded id renders nowhere. SPEC-60's "shall cite the current version" is model₂'s bounded choice on the agent path (SPEC-2); what the loader guarantees is that the successor is retrievable and the predecessor is **not citable** — this test is that half, and verification 3's break reddens it);
  `test_withdrawn_sole_source_refuses` (**control + withdrawn, gate r1 f5** — the topic's only row is tier 4 (original synthetic × category ≠ `payer-training-summary`, eligibility-assistant-D-38 note 2), so eligibility-assistant-D-38's applicability rule "no row of tier ≤ 3" already yields `refuse_definitive` + `escalate` **before** any withdrawal: the outcome does not discriminate and the **citation set** does. Control leg, unmodified corpus: `lookup("front-desk-scripts", …)` returns exactly `[DOC-SYN-NEVER-STATE]` and the turn renders `refuse_definitive` + `escalate` **citing it**. Withdrawn leg: the same lookup returns **zero** rows and the same turn renders `refuse_definitive` + `escalate` with **no citation** and no verdict line — the EVAL-006/013 shape); `test_categories_survive_withdrawal` (**gate r2 f2**, eligibility-assistant-D-80 — the default suite sees the enum's row set, which the `_INDEX`-only install of a variant otherwise hides: on the eligibility-assistant-D-77 variant with `DOC-SYN-NEVER-STATE` withdrawn, `policy_index.categories(root=variant)` still contains `front-desk-scripts` while `load(root=variant).rows` holds no row of that topic — so an implementation that derived `categories` from the current-only rows reddens here, and the runbook's withdrawal turn is asserted reachable before the demo runs it);
  no `[EVAL-n]` parametrisation on any of them (eligibility-assistant-D-54; the SPEC-37 count stays where `turn` left it) (eligibility-assistant-SPEC-58/59/60/61 / eligibility-assistant-D-75, eligibility-assistant-D-77)
- `docs/runbook.md` — "Eligibility assistant (eligibility-assistant)" section (created here or by `trace`, whichever lands first — `trace.md:20` states the same "whichever lands first"); this ticket owns its "Corpus update procedure" subsection, written against the **image**, not a mount (**gate r1 f1, eligibility-assistant-D-76**): supersede (vendor `documents/synthetic/DOC-SYN-NEVER-STATE-V2.md`, add its manifest row with the `sha256sum` output and its `index.json` entry, set the predecessor `status: superseded` + `superseded_by`, then **`docker compose up -d --build ai-assistant`** — an image rebuild and recreate, because `Dockerfile:8` is `COPY . .` and the service has no bind mount, so `docker compose restart` would re-run the same image); withdraw (`status: withdrawn`, same rebuild-and-recreate); what the rebuild refuses (a reason-table id withdrawn → the container exits with `policy_index.CorpusLoadError` naming the id and `make ps` (`docker compose ps`, `Makefile:43-44`) shows it exited — shown once in the demo and reverted); the **named demo document, eligibility-assistant-D-77**: `DOC-SYN-NEVER-STATE`, category `front-desk-scripts`, a synthetic `DOC-SYN-*` row that is in no acceptance case's `expected_source_ids` and is the sole row of its category; the two turns the demo runs after each step — **entered as question type `covered today`, payer Aetna (the seeded demo member `AETN1224`, `trace.md:46`), product `unconfirmed`, state `unconfirmed` (gate r3 f1; eligibility-assistant-D-77 note of 2026-08-28)** — and what each shows (the cited id changes on supersession, the citation disappears on withdrawal — the outcome is `refuse_definitive` on both, per eligibility-assistant-D-77's tier-4 clause), **paired with the observation model₁'s choice cannot move:** from the host, `docker compose exec ai-assistant python -c "import policy_index; rows, _ = policy_index.lookup('front-desk-scripts', 'aetna', 'unconfirmed', 'unconfirmed'); print([r['id'] for r in rows])"` → `['DOC-SYN-NEVER-STATE-V2']` after the supersession step and `[]` after the withdrawal step (`lookup` returns the `(rows, record)` two-tuple, eligibility-assistant-D-58 / D-69; `id` is the eligibility-assistant-D-61 row key; eligibility-assistant-D-77's curated `*` axes make any payer value legal; the service is `ai-assistant`, `docker-compose.yml:222-247`, `WORKDIR /app`, `Dockerfile:3`) — the runbook states that the portal turn's topic is model₁'s (SPEC-1, eligibility-assistant-D-25), that a turn model₁ routed to another topic is re-run once, and that the in-container lookup is the observation of record either way (residual (h)); and one operator warning: a **real** corpus update is a code change, so it also updates the counts `corpus`'s `test_manifest_sha_pinned` and `test_index_covers_every_row` pin (87 rows today) — and the 25-category pin `test_topic_enum_equals_manifest_categories` **only if the added row opens a new category**: `categories()` is manifest-wide (eligibility-assistant-D-80), so a supersession or withdrawal within an existing category leaves it at 25 (**gate r2 f2**) — whereas the demo's edit is reverted with `git checkout --` and moves nothing (eligibility-assistant-SPEC-62 / eligibility-assistant-D-48, eligibility-assistant-D-57, eligibility-assistant-D-76, eligibility-assistant-D-77)
- `adr/0019-*.md` — lifecycle paragraph appended (eligibility-assistant-D-42 pattern, note of 2026-08-27: each ticket appends the paragraph for the mechanism its own diff lands, so the record survives this file's deletion at merge): the two manifest keys and that they are **our** addition to an otherwise verbatim client manifest (gate r1 f8), the closed status set and the non-null-iff-superseded rule, the current-only index (eligibility-assistant-D-75), startup enforcement at the two load sites, retention plus sha pin on non-current rows, and no reload — the update is a rebuild-and-recreate (eligibility-assistant-D-76)
- `tests/README.md` — "AI paths" bullet (`tests/README.md:32`) gains `test_a1_lifecycle.py` (registry upkeep; `llm-seam` gate r3 f3 class, eligibility-assistant-D-60)
- `CLAUDE.md` §6 — baseline line (`:141`, `1317 passed, 19 deselected, 1 xfailed`) updated to the post-ticket count. **Cited as registry upkeep + eligibility-assistant-D-43, not as this ticket's SPEC row (gate r1 f8):** SPEC-40 is `trace`'s (contract `## Delivery` ticket table; `trace.md` `Scope:`), the reading `turn.md:48` and `retrieval-eval.md:21` already take on the same `CLAUDE.md:141` line. The clause every ticket's line moves is `:141` itself; deselected and xfailed never move (eligibility-assistant-D-43)
- **Falsified-claims sweep — run 2026-08-27, this session, over every registry the plan-authoring skill names** (gate r1 f8). Terms swept with `git grep -i` over `docs/landmines.md`, `docs/debt-log.md`, `docs/phi-logging-policy.md`, `docs/todo.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `docs/runbook.md`: `policy_corpus` · `document-manifest` · `manifest` · `superseded` · `withdrawn` · `corpus` · `restart` · `lifecycle`. **Enumerated outcome.** `policy_corpus`, `document-manifest`: **0 hits** — nothing of this item has landed. `manifest`: one hit, `docs/todo.md:74` TODO-68, which is the `workflow-doc-cap` exemption manifest — unrelated, verbatim. `superseded`: `CLAUDE.md:159`, `:219` (ADRs 0012–0015 / dead names), `docs/debt-log.md:30`, `docs/todo.md:6`, `:37`, `:58`, `:93` — all the doc-archive rule and superseded ADRs, none about a corpus document, all verbatim. `withdrawn`: `docs/debt-log.md:217` (a withdrawn redaction proposal) — unrelated, verbatim. `corpus`: `docs/debt-log.md:268`, `:288` (D11, the records-service PHI corpus), `:329` and `docs/runbook.md:37` (the doubled seed corpus), `docs/todo.md:62` TODO-43 / `:88` TODO-41 (`eval/rag/` — the RIV-160 chart-record eval, a different corpus, `CLAUDE.md:62`) — none is the policy corpus, all verbatim. `restart`: `docs/runbook.md:261`, `:277` (Redis blip, DB reconnect) — unrelated, verbatim. `lifecycle`: `ARCHITECTURE.md:44` "Request lifecycle" — the English word, verbatim. **The one claim this diff falsifies is inside the item's own artifacts**, not the registries: the manifest-verbatim class at `corpus.md:13`, `corpus.md:72` and register eligibility-assistant-D-35, scoped by eligibility-assistant-D-57 and stated in the manifest change row above and in the ADR row. `README.md` is the TODO-12 human-gated exemption. In-code debt markers: this ticket edits no pre-existing source file except `services/ai-assistant/app.py` (one hook argument and one import) and the vendored manifest, and `grep -rnE "TODO|FIXME|XXX|HACK|DEBT" services/ai-assistant/*.py` → no match (run 2026-08-27)

Gate interactions (existing steps that already enforce part of a new check):
- `corpus`'s `tests/test_a1_corpus.py::test_manifest_sha_pinned` (`corpus.md:25`) already fails on a successor file vendored without a manifest row, so half of the supersession procedure is enforced before this ticket; this ticket adds the status half.
- **"Fail at load" has two sites, not one (gate r1 f2; eligibility-assistant-D-57 note of 2026-08-27).** (a) *Import time*: `import app` → the dark `import policy_tool` → `categories()` → `policy_index.load()` (`corpus.md:16`, `:60`; eligibility-assistant-D-35 note 2), which carries **no** `required_current` — so the closed-set, `superseded_by` and chain checks redden the CI `services` smoke (`python -c "import app"` after the per-service install, `.github/workflows/ci.yml:72-76`) and its local twin, `corpus` verification 5 (`corpus.md:52`), while a withdrawn reason-table id does **not**. (b) *The FastAPI lifespan hook* (`corpus.md:16`), the only caller that passes `required_current`, pinned by `corpus`'s `test_startup_hook_fails_boot_on_corpus_error` for the raise and by this ticket's `test_startup_hook_requires_current_reason_ids` for the argument. The asymmetry is forced: `policy_index` cannot read the reason set itself, because `outcome` imports `policy_index` for `fetch_by_id` (`turn.md:15`). Attribution at the impl gate: a red `services` job on a lifecycle fault is the import-time load; a container that boots and then exits is the hook.
- **`corpus`'s pins on `MAX_ROW_BYTES` and the index row (gate r1 f3).** Three existing assertions recompute or snapshot `2,789` (`corpus.md:25`, `:26`) and `turn.md:74` sizes its prompt reserve on it. This ticket adds no field to the index row (eligibility-assistant-D-75), so none of them moves; verification 8 re-runs `corpus`'s three test files unchanged as the standing proof of that.
- SPEC-37's collect-count cmd: this ticket adds no `[EVAL-n]` id (eligibility-assistant-D-54), so it moves the count by zero — but the *value* depends on landing position, since `lifecycle` and `trace` are independent of each other in eligibility-assistant-D-59's order (eligibility-assistant-D-59's order clause, restated in the item plan's `## Plan` `Status:` line — "→ {**lifecycle**, **trace**} (independent of each other)"; cited by section per the README stable-ID rule, `plans/eligibility-assistant.md:103` as read 2026-08-28 after eligibility-assistant-D-81's row — gate r2 f3): `32` if this ticket lands before `trace` (`turn.md:64`), `33` after it (`trace.md:37`, `EVAL-019`). Verification 5 states both with the condition (gate r1 f6).
- **The suite line's `skipped` term has the same dependence (gate r1 f6):** `1 skipped` before `trace`, `2 skipped` after (eligibility-assistant-D-43; `turn.md:65`, `trace.md:38`). Verification 7 states both. Deselected and xfailed never move.
- CI `secret-scan` (gitleaks v8.18.4, `--no-git`, over the tracked tree — `.github/workflows/ci.yml:120-129`): the successor files the tests write live in `tmp_path`, never in the tree; the only tracked bytes this ticket adds are two keys per manifest row, so the scan's surface is unchanged.
- SPEC-11's tracked artifact is `index.json`, which keeps one entry per approved row whatever its status (`corpus.md:14`); this ticket changes no entry, so `test_index_covers_every_row` is unaffected. What becomes current-only is the **loaded** index (eligibility-assistant-D-75).

Verification (runnable, expected output stated):
1. `.venv/bin/python -m pytest tests/test_a1_lifecycle.py -q` → `12 passed` (the chain test is ×4, `test_withdrawn_sole_source_refuses` carries its control and withdrawn legs in one test, and `test_categories_survive_withdrawal` is the twelfth — gate r2 f2) (eligibility-assistant-SPEC-58/59/60/61)
2. break-then-revert (startup enforcement, in process **and** in a real container — **gate r1 f1**, eligibility-assistant-D-76): set `DOC-SYN-EMERGENCY`'s row to `"status": "withdrawn"` in `services/ai-assistant/policy_corpus/document-manifest.json`. In process: `cd services/ai-assistant && ../../.venv/bin/python -c "import policy_index, outcome; policy_index.load(required_current=outcome.reason_citation_ids())"` raises `policy_index.CorpusLoadError: … DOC-SYN-EMERGENCY …`. In the container, which needs an **image rebuild, not a restart** — the corpus is baked in by `COPY . .` (`services/ai-assistant/Dockerfile:8`, `WORKDIR /app` `:3`), the service declares `build: ./services/ai-assistant` with no `volumes:` and no `restart:` policy (`docker-compose.yml:222-247`; the file's only bind mounts are postgres `:10` and the migration runner `:267`, and `restart:` has zero hits in the file), and `make up` is `docker compose up -d` (`Makefile:34-35`), which never rebuilds an existing image: `docker compose up -d --build ai-assistant` → `make ps` (`docker compose ps`, `Makefile:43-44`) shows `ai-assistant` exited. Revert: `git checkout -- services/ai-assistant/policy_corpus/document-manifest.json && docker compose up -d --build ai-assistant` → healthy. This is the first time the refusal is observed in a real container (eligibility-assistant-SPEC-59)
3. break-then-revert (the current-only index, eligibility-assistant-D-75): make `load()` build the index from every approved row regardless of `status` → `test_lookup_and_fetch_return_current_only` red (the superseded and withdrawn rows are served, and the row count is the manifest's), `test_superseded_never_cited` red on its retrieved-set assertion (the set is `{predecessor, successor}`), `test_superseded_id_offered_by_model_is_rejected` red (the old id is back inside the retrieved set, so model₂'s selection validates and the superseded id renders — the leg gate r1 f4 asked for), **and** `test_withdrawn_sole_source_refuses` red on its withdrawn leg (the withdrawn row is served, so the lookup returns one row where zero is asserted) — four tests, one break; revert → green. Second break (**gate r2 f2**, eligibility-assistant-D-80): derive `categories` from the current-only `rows` instead of from every approved manifest row → `test_categories_survive_withdrawal` red (`front-desk-scripts` gone from `categories(root=variant)`), every other test green — the isolation is the point: only that test sees the enum's row set; revert → green (eligibility-assistant-SPEC-58/60/61)
4. break-then-revert: drop the `required_current` check from inside `load` → `test_load_fails_on_noncurrent_reason_id` red; revert → green (eligibility-assistant-SPEC-59)
4b. break-then-revert (**gate r1 f2** — the hook's argument, a different site from verification 4): drop `required_current=outcome.reason_citation_ids()` from the lifespan hook's `load()` call in `services/ai-assistant/app.py` → `test_startup_hook_requires_current_reason_ids` red (the recorded call carries the default empty tuple) while verification 4 stays green, which is the point: the check and its one wiring fail independently; revert → green (eligibility-assistant-SPEC-59)
4c. break-then-revert (**gate r1 f9**): relax the `superseded_by` rule to "`null` is always legal" → `test_load_fails_on_unresolved_chain` red on its `superseded`-with-`null` case; second break: stop rejecting a non-null `superseded_by` on a `current` row → the same test red on its fourth case; revert each → green (eligibility-assistant-SPEC-58/59)
5. `.venv/bin/python -m pytest -m "not integration" --collect-only -q | grep -o 'EVAL-0[0-9][0-9]\(-[a-z]*\)*' | sort -u | wc -l` → **`32` if this ticket lands before `trace`, `33` if after** — this ticket adds no `[EVAL-n]` id (eligibility-assistant-D-54), `turn` leaves the count at 32 (`turn.md:64`) and `trace`'s `EVAL-019` takes it to 33 (`trace.md:37`); the two tickets are independent of each other in eligibility-assistant-D-59's landing order (its order clause, restated in the item plan's `## Plan` `Status:` line — `plans/eligibility-assistant.md:103` as read 2026-08-28 after eligibility-assistant-D-81's row; gate r2 f3), so the value is conditioned rather than pinned (**gate r1 f6**; SPEC-37 is `turn`'s row per eligibility-assistant-D-55, the value stated here so it cannot move silently)
6. `make up`, then the runbook's "Corpus update procedure" hand-run by the impl-gate session on the named demo document `DOC-SYN-NEVER-STATE` (eligibility-assistant-D-77): supersede → `docker compose up -d --build ai-assistant` → a portal turn entered as question type `covered today` · payer Aetna (`AETN1224`) · product `unconfirmed` · state `unconfirmed` (**gate r3 f1**), when model₁ routes it to `front-desk-scripts`, cites `DOC-SYN-NEVER-STATE-V2` and not the predecessor (the outcome is `refuse_definitive` + `escalate` on both sides of the step — the topic's only row is tier 4, eligibility-assistant-D-77 — so the observation is the **cited id**), **and the deterministic leg:** the runbook row's `docker compose exec ai-assistant python -c "… policy_index.lookup('front-desk-scripts', 'aetna', 'unconfirmed', 'unconfirmed') …"` prints `['DOC-SYN-NEVER-STATE-V2']`; withdraw the successor too → rebuild → the same turn renders `refuse_definitive` + `escalate` with **no citation** and the lookup prints `[]`; withdraw a reason-table id → rebuild refused, container exited with the id named → `git checkout --` + rebuild; observation recorded in Delivery (eligibility-assistant-SPEC-62 `gate:` / eligibility-assistant-D-76, eligibility-assistant-D-77)
7. `git diff --stat main -- db/ config/ services/gateway/authz.py services/gateway/security.py services/eligibility-service/ services/ai-assistant/eligibility_client.py docker-compose.yml eval/` → empty (structural exclusion, every ticket); `.venv/bin/python -m pytest -m "not integration" -q` → `<Baseline at branch: passed + 12> passed, 19 deselected, 1 xfailed`, with the `skipped` term **`1 skipped` if this ticket lands before `trace`, `2 skipped` if after** (eligibility-assistant-D-43; `turn.md:65`, `trace.md:38`; **gate r1 f6**). Deselected and xfailed never move. The claim-worthy run is `make test-docker` with the same numbers (registry upkeep + eligibility-assistant-D-43 — SPEC-40 is `trace`'s row, gate r1 f8)
8. `.venv/bin/python -m pytest tests/test_a1_corpus.py tests/test_a1_retriever.py tests/test_a1_conflict.py -q` → all passed, unchanged from `corpus`'s verification 1 (**gate r1 f3**): this ticket adds no field to the index row, so `policy_index.MAX_ROW_BYTES` is still 2,789 and `corpus`'s three pins on it hold — the recompute-and-compare in `test_cap_binds_on_a_legal_call` (`corpus.md:26`), the "still 2,789" in `test_load_with_root_does_not_publish_module_state` and the `(…, 2789)` snapshot in `test_non_approved_row_fails_load` (`corpus.md:25`) — and `test_index_covers_every_row`'s 87 is unchanged because every vendored row lands `current`. Break-then-revert: add `status` to the index row `load()` builds → `test_cap_binds_on_a_legal_call` red on rule (i) (the module's `MAX_ROW_BYTES` becomes 2,796 while the test recomputes 2,789 from the manifest's five fields plus the file); revert → green (eligibility-assistant-SPEC-58 / eligibility-assistant-D-75)

## Landmines

This block is verbatim for **this ticket's own diff** and no other's (eligibility-assistant-D-60): it names the §1 zones the `lifecycle` change list enters, and a zone another ticket enters is that ticket's block to write, not a citation from here.

- **PHI handling (§1) — entered.** Two new PHI-adjacent code paths, covering this ticket's two ⚠ rows (eligibility-assistant-SPEC-60, -61) and the loader change they rest on (SPEC-58/59):
  **(1) The lifecycle turn (SPEC-60/61, ⚠ REQ-18).** A `superseded` or `withdrawn` manifest row changes exactly one thing about a clerk-facing turn: **which vendored document is cited, or whether a coverage answer renders at all**. It introduces **no new input** (the clerk's menus and the request body are unchanged — nothing about lifecycle reaches the request), **no new egress** (no additional model call, no additional payer call, no new vendor payload), **no new persisted field** (`VisitFacts` is untouched by this ticket; the provenance field is `turn`'s) and **no new log field**. The document a superseded turn stops citing and the successor it starts citing are both vendored synthetic clinic material, never patient data. The assertions run through the `turn` rig on `tmp_path` copies of the real corpus (eligibility-assistant-D-57): `tests/test_a1_lifecycle.py::test_superseded_never_cited` proves the superseded id appears in no citation on that turn **or on the next turn of the same visit whose `last_citations` still holds it**, and asserts the turn's retrieved set is the successor alone; `::test_superseded_id_offered_by_model_is_rejected` proves the harder direction — a model₂ that *selects* the superseded id is rejected into `validation_reject` + `DOC-SYN-NO-INVENTION` rather than rendering it; and `::test_withdrawn_sole_source_refuses` proves a topic whose only applicable rows are withdrawn renders `refuse_definitive` + `escalate` with **no citation and no verdict line**, against a control leg on the unmodified corpus where the same turn cites the one row — so the failure direction is refusal, never a stale or invented coverage answer, and the assertion that discriminates is the citation set (eligibility-assistant-D-77).
  **(2) Load-time lifecycle enforcement (SPEC-58/59).** `policy_index.load(root=None, required_current=())` validates every manifest row's `status` against the closed set, enforces `superseded_by` **non-null iff `superseded`** and resolves each chain to a `current` row (a chain reaching a withdrawn row, a dangling id and a cycle are each unresolved), and requires every reason-table id (`outcome.reason_citation_ids()`, the eligibility-assistant-D-19 set) to be `current` — any failure raises `policy_index.CorpusLoadError` **naming the id, before any lookup and before any turn is served**. That happens at **two load sites**, and the difference is stated rather than blurred: the import-time load (`import app` → the dark `import policy_tool` → `categories()` → `load()`, eligibility-assistant-D-35 note 2) enforces every check except the reason-id one, and the startup hook `corpus` landed is the only caller that passes `required_current`, because `outcome` imports `policy_index` and the loader cannot read the set back without a cycle. Non-current rows are **excluded from the loaded index at load** (eligibility-assistant-D-75), so `lookup`, `_filter` and `fetch_by_id` are current-only by construction, before `rank`, and a non-current row cannot reach a citation by any ordering — and no lifecycle value rides the row that egresses, because that row has no field to carry it. A non-current row's file stays on disk and stays sha-pinned under SPEC-7, since the sha pass and the whole-tree scan run over every manifest row whatever its status (`::test_noncurrent_file_retained_and_pinned`) — retention, not deletion, so the record of what was once cited survives. SPEC-58/59/62 are not ⚠ rows; they are named here because they are the mechanism the two ⚠ rows rest on.
  Approval of record for entering the zone: **eligibility-assistant-D-56** — the owner's §1 approval of code entry, given **2026-08-25** in session at plan review r1 (gate r1 f1), unconditional, and **extended by the owner the same day to SPEC-60, 61, 63** (eligibility-assistant-D-48, the REQ-18 / REQ-19 amendment); registered as a decision 2026-08-26. SPEC-60/61 are covered by that same-day extension, not by the original SPEC-7–10/12/20/28–31 list. It stands on the owner's freeze of the ⚠ row REQ-18 and SPEC-60–61 (contract `## Spec` Status, 2026-08-26) and on eligibility-assistant-D-48's own mechanism decision (status not date; no hot reload; the demo document is a synthetic `DOC-SYN-*`, never client public-domain text). A change to the mechanisms named here — the closed status set, the `superseded_by` non-null-iff-`superseded` rule, the chain resolution, `required_current`, the load-time exclusion of non-current rows from the index's `rows` (eligibility-assistant-D-75) — re-opens it. **It was re-opened by gate r1 (f3 / f9) and re-closed the same day (gate r2 f1 brings this paragraph to the record the Status line and the register already hold):** eligibility-assistant-D-75 moved the `current`-only guarantee from a filter before `rank` to an exclusion at load, and eligibility-assistant-D-57's note clause (c) tightened `superseded_by` to non-null iff `superseded`; both were **confirmed by the owner 2026-08-27 (in session)**, and that confirmation is the act that re-closed this approval on the mechanisms as now written — the shape `retrieval-eval` used for eligibility-assistant-D-68 / D-69. eligibility-assistant-D-76 (rebuild-and-recreate) and eligibility-assistant-D-77 (the named document) touch the operator procedure and the demo, not a mechanism this paragraph names, and were confirmed in the same act. **Re-opened again by this round's eligibility-assistant-D-80 (gate r2 f2; proposed 2026-08-27, confirmed by the owner 2026-08-27 in session):** it fixes what the model-facing `topic` enum is built over — every approved manifest row whatever its `status`, beside the current-only `rows` — which is a mechanism `corpus`'s block names (the `topic`-only tool schema) and the one this block's SPEC-61 demonstration rests on; the owner's confirmation of D-80 on 2026-08-27 is the act that re-closed this approval on that clause, here and in `corpus`'s block, so nothing stands re-opened.
  **Deliberate defects in reach are preserved.** D13 (no BAA on the vendor path) stays OPEN and is neither narrowed nor fixed: ADR 0011 constraint 4 is unchanged and the payer answer remains the only coverage source. The `README.md` HIPAA overstatement (TODO-12) is untouched, human-gated. The D1 intake body-logging and D3 plaintext-PHI-column defects are not touched by any file in this change list. D11 (session↔patient binding) is not in reach — no chart route, no session code; the cross-patient refusal is `turn`'s and is unchanged here.
  **Accepted residuals on this ticket's rows:** SPEC-60's "on that or any later turn" is proven for two consecutive turns of one visit through the rig, not across a service restart — see the residual-honesty bullet below, where this and the rest are written out.
- **Secrets (§1) — not touched.** No `.env*` change; the manifest edit is a tracked non-secret file; the demo's live edit is a working-tree change to the vendored manifest, reverted after the demo.
- **Migrations / schema (§1) — not touched.** Nothing under `db/`; `VisitFacts` is unchanged by this ticket (the provenance field is `turn`'s).
- **Domain services network-internal (§1, ADR 0016) — not touched.** No `ports:`, no new service, no `docker-compose.yml` edit — the ai-assistant service stays `expose`-only (`docker-compose.yml:222-247`) and `tests/test_compose_topology.py`'s allowlist is unchanged. The procedure's step is `docker compose up -d --build ai-assistant` (**gate r1 f1**, eligibility-assistant-D-76 — an image rebuild and recreate, because `Dockerfile:8` is `COPY . .` and the service declares no bind mount), which rebuilds and recreates the same expose-only service and publishes no port.
- **Auth / sessions (§1) — not touched.** No line of `services/gateway/` moves: `authz.py`, `security.py`, `config/roles.yaml`, `EXPECTED_ROUTE_CAPABILITIES` and `require_capability` are outside this change list. This ticket adds no route and changes no request-path branch — SPEC-60/61 follow from the loader (eligibility-assistant-D-75), so `app.py`'s only edits are the startup hook's `required_current=` argument and the `import outcome` line that supplies it.
- **IDOR on chart reads (§1, D11) — not touched.** No chart or records route is edited, no session is bound to a patient, nothing here reads patient data; the D11 xfail stays visible.
- **Inline eligibility call (§1, ADR 0010) — not touched.** `services/ai-assistant/eligibility_client.py` and `services/eligibility-service/` are not edited; no timeout, retry or breaker value moves and `tests/test_eligibility_budget_alignment.py` is untouched. The payer call's site and once-guard are `turn`'s and are unchanged here.
- **ROI / disclosures, booking race, duplicate patients, HL7 mapping (§1) — not touched.** No file under `services/roi-service/`, `services/scheduling-service/`, `services/intake-service/` or `services/interop-service/` appears in this change list.
- **Intake registration (§1) — not touched.** No file under `services/intake-service/` is edited; `ConsentKind`, `proxy_intake` on `_post_checked` and `contracts/intake-registration.json` are outside this change list (`docs/landmines.md:87`).
- **Structural exclusion (this ticket).** Nothing changes under `db/`, `config/`, `services/gateway/authz.py`, `services/gateway/security.py`, `services/eligibility-service/`, `services/ai-assistant/eligibility_client.py`, `docker-compose.yml` or `eval/` — the spec Exclusions and the §1 zones stated not-touched above hold **structurally**, proven by verification 7's empty diff rather than by this sentence.
- **Partial coverage named honestly (residual honesty check):**
  (a) SPEC-60's "on that or any later turn" is asserted for two consecutive turns of one visit through the rig, with `last_citations` holding the pre-supersession id — the rig cannot rebuild and recreate the service, so the rebuild-then-turn form is verification 6's hand-run, not a test.
  (b) **SPEC-60's positive half is model₂'s bounded choice, not a loader guarantee (gate r1 f10).** "Shall cite the current version" is a choice model₂ makes from the retrieved set (SPEC-2); what the loader guarantees is that the successor is retrievable and the predecessor is **not citable** — an old id in `citation_ids` is outside the turn's retrieved set and is rejected by SPEC-13's gate into `validation_reject` + `DOC-SYN-NO-INVENTION`. `::test_superseded_id_offered_by_model_is_rejected` is that negative half in the default suite; the positive half runs against a scripted model₂ there and against the real model only in verification 6's live demo — and whether model₁ *reaches* the topic on that demo turn at all is residual (h) (gate r3 f1).
  (c) SPEC-62 is a `gate:` row whose observation exists only at the impl gate.
  (d) **The demo lineage renders `refuse_definitive` on both steps (eligibility-assistant-D-77).** `DOC-SYN-NEVER-STATE`'s category holds one row and that row is tier 4, so eligibility-assistant-D-38's "no row of tier ≤ 3" already refuses a definitive answer before any lifecycle change; the observation that discriminates is the **citation set**, not the outcome, and both the test and the runbook say so. No single-row `DOC-SYN-*` category can do better — a tier ≤ 3 row in the same topic is what "sole source" excludes.
  (e) A successor written by the operator is approved by **us**, not the client — `approval_status: approved` on that row is an engineering act, stated in the runbook.
  (f) **A real in-tree corpus update moves `corpus`'s pinned counts** — `test_manifest_sha_pinned` and `test_index_covers_every_row` (87 today) — so a supersession that lands is a code change with test updates in the same diff; the demo's edit is reverted with `git checkout --` and moves nothing. The runbook says this; nothing enforces it, because there is no in-tree supersession to enforce against until one is made.
  (g) **The two manifest keys are ours, not the client's.** This is the one place the vendored manifest stops being byte-identical to the package (eligibility-assistant-D-57); the client artifact is recoverable as this file minus the two keys, and the document files and their shas are untouched, but nothing in the suite asserts that recoverability.
  (h) **The demo's portal turn depends on model₁'s topic choice, which the application cannot force (gate r3 f1).** On the agent path the topic is model₁'s one free choice from the closed enum (SPEC-1, eligibility-assistant-D-25); `_build_model1_prompt` carries the four selections, the turn count and the topic list, never free text (`turn.md:17`), and `A1_MODEL_FIXTURE` labels the mode without scripting a model (eligibility-assistant-D-71), so no input the operator enters can make a live model₁ pick `front-desk-scripts` — and `DOC-SYN-NEVER-STATE` is in no acceptance case, so no `case_selections.json` entry names it (eligibility-assistant-D-77). The deterministic proofs of SPEC-60/61 are the scripted default suite — `test_superseded_never_cited`, `test_withdrawn_sole_source_refuses` — and the runbook's in-container lookup, which reads the served index directly; the portal turn is the client-facing demonstration, run on the named selections, and SPEC-62's Delivery observation records whether model₁ reached the topic on the day.
  (i) **The chain test as planned has no leg for a dangling `superseded_by` id (gate r4 f1).** The loader rule (`:14`, `:61`) names three unresolved shapes — a chain reaching `withdrawn`, a dangling id, a cycle — and `test_load_fails_on_unresolved_chain`'s four legs exercise the first and third plus the two null/id pairings; none sets `superseded_by` to an id that is not a manifest row, so a `.get()`-style resolver that treats a missing target as resolved passes every leg while accepting the commonest operator error (a mistyped successor id in the runbook's supersede step). Accepted by the owner 2026-08-28 as a named residual rather than a plan revision: the implementation adds the fifth parametrized case (`superseded_by: "DOC-SYN-NOPE"` → `CorpusLoadError` naming the row) and its break; the function count (twelve) is unchanged and the impl gate reports the collected-case move. The impl gate reads the test against this bullet.

## Findings

Gate rounds start at 1 for this ticket; carried findings: none.

### Gate — round 1, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only, no
characterization received; tree read is `main` with the uncommitted working tree. Run
alongside the `corpus` / `llm-seam` r7 and `turn` / `trace` gates (independent agents).
Round 1 for this ticket — no prior `checked:` hash, so every tag is `new` by rule. Plan
returns to stage 3; spec unchanged.

| # | anchor | finding | disposition |
|---|---|---|---|
| f1 | eligibility-assistant-SPEC-62 / -59 · runbook row (`lifecycle.md:19`) · verification 2 (`:32`), 6 (`:36`) · Landmines Domain-services bullet (`:51`) · Gate-interactions bullet 2 (`:26`) | **new** — Wrong mechanism: the corpus is baked into the image, not mounted. `services/ai-assistant/Dockerfile:8` is `COPY . .`; `docker-compose.yml:222-223` is `build: ./services/ai-assistant` with **no `volumes:`** on the service (the file's only bind mounts are postgres `:10-13` and the migration runner `:267-269`) and no `restart:` policy anywhere. So `docker compose restart ai-assistant` restarts the same image, and `make up` (`docker compose up -d`, `Makefile:34-35`) does not rebuild an existing image — a working-tree manifest edit never reaches the container. The runbook's supersede/withdraw steps change nothing the service sees, verification 2's "`make up` → `make ps` shows `ai-assistant` exited" cannot occur, and verification 6's demo turns keep citing the old corpus. The operator step is an image rebuild + recreate (`make build`, `Makefile:46-47`, or `docker compose up -d --build ai-assistant`); SPEC-62's "service restart, no runtime mutation or reload" and D-48's "a restart keeps the sha pin" need restating against that — the plan's "first time the refusal is observed in a real container" claim rides on it | **Disposition 2026-08-27 — A.** Accepted; the mechanism was wrong, and every site that said "restart" is corrected. Re-verified in the tree this session: `services/ai-assistant/Dockerfile:8` `COPY . .` under `WORKDIR /app` (`:3`); `docker-compose.yml:222-247` — `build: ./services/ai-assistant` at `:223`, `expose` only, **no `volumes:`** (the file's only bind mounts are postgres `:10` and the migration runner `:267`) and **zero** `restart:` hits; `make up` = `docker compose up -d` (`Makefile:34-35`), `make build` = `docker compose build` (`:46-47`), `make ps` = `docker compose ps` (`:43-44`). New **eligibility-assistant-D-76** (confirmed by owner 2026-08-27) makes the operator step `docker compose up -d --build ai-assistant` in the runbook row, verification 2 and verification 6, and records why the frozen SPEC-62 clause "with a service restart and no runtime mutation or reload" is **satisfied, not amended** — the service is stopped and recreated from a freshly built image, the corpus is read once at load, and nothing mutates or reloads at runtime. eligibility-assistant-D-48's and eligibility-assistant-D-57's own "restart" wording is corrected by a dated note on eligibility-assistant-D-57 (clause d). **Sites changed:** `lifecycle.md` — Status line; runbook change row; Gate-interactions "two load sites" bullet (rewritten, carries the container attribution); verification 2 (rewritten, in-process leg + container leg with the rebuild); verification 6 (rewritten); Landmines "Domain services network-internal" bullet. `plans/eligibility-assistant.md` — NEW `eligibility-assistant-D-76`; `eligibility-assistant-D-57` dated note clause (d).
| f2 | eligibility-assistant-SPEC-59 · `app.py` row (`:16`) · Gate-interactions bullet 2 (`:26`) · verification 4 (`:34`) | **new** — The `required_current` check has one caller, the lifespan hook, and nothing reddens if the hook omits the argument: `test_load_fails_on_noncurrent_reason_id` calls `load(required_current=…)` directly (`:18`), `corpus`'s `test_startup_hook_fails_boot_on_corpus_error` monkeypatches `load` wholesale (`corpus.md:24`), and verification 4 breaks the check inside `load`, not the hook's argument. Meanwhile the import-time load (`import policy_tool` → `categories()` → `load()`, `corpus.md:16`/`:18`, D-35 note 2) carries no `required_current`, so the CI `services` smoke (`.github/workflows/ci.yml:76`) and corpus verification 5's shape never observe a withdrawn reason-table id. "Fail at load has one site and one test shape" is false — two load sites, only the hook checks reason ids, and that wiring has no automated pin (the llm-seam r2 f1 class: mechanism with no reddening test) | **Disposition 2026-08-27 — A.** Accepted on both halves. **(i) The false claim is withdrawn:** "fail at load has one site and one test shape" is replaced by an enumerated two-site bullet — import time (`import app` → the dark `import policy_tool` → `categories()` → `load()`, `corpus.md:16`, `:60`, eligibility-assistant-D-35 note 2) enforces the closed set, the `superseded_by` rule and chain resolution but carries **no** `required_current`; the lifespan hook (`corpus.md:16`) is the only caller that passes it. The asymmetry is forced and is now stated with its reason: `policy_index` cannot read the reason set itself because `outcome` imports `policy_index` for `fetch_by_id` (`turn.md:15`), so the set is injected at one site. **(ii) The wiring gains its reddening test** (the `llm-seam` r2 f1 class fix — pin the argument, not the checked code): `tests/test_a1_lifecycle.py::test_startup_hook_requires_current_reason_ids` monkeypatches `app_mod.policy_index.load` to a recording spy, boots `with TestClient(app_mod.app)` against the real vendored corpus, and asserts the single recorded call carried `required_current == tuple(app_mod.outcome.reason_citation_ids())`, non-empty and equal to the seven ids of the contract reason table (`docs/workflow/eligibility-assistant.md:203-210`). It reaches the hook through eligibility-assistant-D-66's `app_mod.policy_index is policy_index` identity, and it compares against `app_mod.outcome` — the object `app.py`'s own `import outcome` binds — so the assertion is about the module the hook actually read. Verification 4b is its break (drop the argument from the hook; verification 4, which breaks the check *inside* `load`, stays green — the two fail independently). `app.py`'s change row now names the `import outcome` the test rests on, in the module-import idiom of `app.py:30-36`. Test count 7 → 8, carried into verifications 1 and 7. **Sites changed:** `lifecycle.md` — Status line; `outcome.py` change row (the seven ids named with their locator, and why the set is exported rather than read); `app.py` change row (`import outcome`, the pinned argument); `tests/a1_rig.py` change row (`app_mod.outcome` reachability); `tests/test_a1_lifecycle.py` change row (new test); Gate-interactions bullet 2 (rewritten as the two-site bullet); verification 1 (`11 passed`); NEW verification 4b; verification 7 (`+ 11`); Landmines PHI entry (2). `plans/eligibility-assistant.md` — `eligibility-assistant-D-57` dated note clause (b).
| f3 | eligibility-assistant-SPEC-58 · `policy_index.py` row (`:14`) · `corpus` `MAX_ROW_BYTES` pins | **new** — The row never says whether `status` / `superseded_by` ride the index row `lookup` returns (and `turn` egresses); `_filter` reading `status == "current"` implies they do. `corpus` defines `MAX_ROW_BYTES` as the max over index rows of "the UTF-8 byte total of every field value the row carries" (`corpus.md:15`) and pins 2,789 three ways — `test_cap_binds_on_a_legal_call` recomputes from exactly five manifest fields + file and asserts equality with the module value (`corpus.md:25`), `test_load_with_root_does_not_publish_module_state` asserts "still 2,789", `test_non_approved_row_fails_load`'s snapshot is `(…, 2789)` (`corpus.md:24`); `turn.md:72` sizes the reserve on it. A row carrying `"current"` (7 B) moves the module value to 2,796 ≠ the recompute → red; if the field also egresses, D-64's sizing has an unlisted term. State the row's field set, whether lifecycle fields egress, and the consequence for the three pins | **Disposition 2026-08-27 — A.** Accepted; the arithmetic is right and the plan had left the question open. Answer, entered as **eligibility-assistant-D-75** (confirmed by owner 2026-08-27, amending eligibility-assistant-D-57): the lifecycle fields ride **nothing** the retriever returns. `load()` runs the sha pass and the SPEC-7 whole-tree scan over **every** manifest row whatever its status — so retention and the pin hold on non-current rows — validates the lifecycle fields, and then builds the in-memory index from the approved rows that are `current`, **only**. `lookup` / `_filter` / `fetch_by_id` are therefore current-only by construction and before `rank`, with no per-row status to read; the index row keeps exactly eligibility-assistant-D-61's six values, so `policy_index.MAX_ROW_BYTES` stays **2,789** and all three `corpus` pins hold unchanged — the recompute-and-compare in `test_cap_binds_on_a_legal_call` (`corpus.md:26`), the "still 2,789" in `test_load_with_root_does_not_publish_module_state` and the `(…, 2789)` snapshot in `test_non_approved_row_fails_load` (`corpus.md:25`) — and `turn.md:74`'s reserve sizing is unmoved. Nothing lifecycle adds can egress, because the row that egresses has no field to carry it. The two alternatives are declined in the decision with their costs: `status` on the row moves the module value to 2,796 against a test that recomputes 2,789 from five manifest fields plus the file; a second module-level map changes `load()`'s single return value, which `corpus`'s non-publish rule and this ticket's rig install both rest on. NEW verification 8 re-runs `corpus`'s three test files as the standing proof and breaks it by adding `status` to the row. `index.json` — SPEC-11's *tracked* artifact — keeps one entry per approved row whatever its status, so `test_index_covers_every_row`'s 87 is untouched (all 87 vendored rows land `current`; package `document-manifest.json` read 2026-08-27). **Sites changed:** `lifecycle.md` — Status line; `policy_index.py` change row (rewritten: "where the lifecycle state lives"); `tests/test_a1_lifecycle.py` change row (`test_lookup_and_fetch_return_current_only` asserts the index row count); Gate-interactions NEW `corpus`-pins bullet and NEW SPEC-11 bullet; verification 3 (break restated as the load-side exclusion); NEW verification 8; Landmines PHI entry (2) and the approval-of-record paragraph (re-open clause fires). `plans/eligibility-assistant.md` — NEW `eligibility-assistant-D-75`; `eligibility-assistant-D-57` dated note clause (a).
| f4 | eligibility-assistant-SPEC-60 · verification 3 (`:33`) · `test_superseded_never_cited` (`:18`) | **new** — The second red is not derivable from the test as written. With the `_filter` predicate removed, `lookup` returns predecessor and successor; the rig's scripted model₂ (D-40 / D-37 — `citation_ids` are the script's) still names the successor, validation passes, and "renders the successor's four fields and no citation carries the superseded id" stays green. It reddens only if the test asserts the retrieved set / model₂'s row keys, or scripts the old id and expects `validation_reject` — neither is written | **Disposition 2026-08-27 — A.** Accepted; the second red was not derivable, and both remedies the finding names are taken. **(i)** `test_superseded_never_cited` now asserts the turn's **retrieved set** is exactly `{DOC-SYN-NEVER-STATE-V2}`, read from the row keys the rig captures in the model₂ message — so an index that still carries the predecessor reddens it whatever the scripted model₂ says. **(ii)** NEW `::test_superseded_id_offered_by_model_is_rejected` scripts model₂ to select the superseded id: it is outside the turn's retrieved set, SPEC-13's `citation_ids ⊆ retrieved` gate rejects it, and the turn renders reason `validation_reject`, mode `fallback`, citation `DOC-SYN-NO-INVENTION` (SPEC-52, contract reason table) with the superseded id rendered nowhere. That is the leg the break in verification 3 reddens: with the eligibility-assistant-D-75 exclusion removed, the old id is back inside the retrieved set, model₂'s selection validates, and the superseded id renders. Verification 3 is rewritten against the eligibility-assistant-D-75 mechanism (the `_filter` predicate the round-1 wording broke no longer exists) and names all three tests it reddens. This also closes f10's ask. Test count 8 → 9. **Sites changed:** `lifecycle.md` — `tests/test_a1_lifecycle.py` change row (`test_superseded_never_cited` retrieved-set assertion; NEW `test_superseded_id_offered_by_model_is_rejected`); verification 1 (`11 passed`); verification 3 (rewritten); verification 7 (`+ 11`); Landmines PHI entry (1); residual-honesty bullet (b).
| f5 | eligibility-assistant-SPEC-61 / -62 · `test_withdrawn_sole_source_refuses` (`:18`) · runbook row (`:19`) · residual bullet (`:58`) | **new** — No bucket or document named, and the existence claim carries no locator: "a topic whose only applicable rows are withdrawn" and "a `DOC-SYN-*` row not in `reason_citation_ids()` and the sole row of its topic × payer bucket, chosen at implementation". Package `document-manifest.json` (read 2026-08-27, 87 rows / 25 categories): eight categories have one row, five of them `DOC-SYN-*` outside the reason table — `DOC-SYN-EFFECTIVE-TERM-DATES`, `-MEMBER-ID-MISMATCH`, `-DOWNTIME`, `-NEVER-STATE`, `-VISIBILITY-OUTCOMES` (all `payer_publisher` "Synthetic training mix", so the payer axis is `corpus`'s unlanded `index.json` curation). The rule is satisfiable; the plan states neither the fact nor a candidate, and the test's bucket is the reachability class D-64 settled by naming one | **Disposition 2026-08-27 — A.** Accepted; one is named, as eligibility-assistant-D-64 and eligibility-assistant-D-70 named theirs. **eligibility-assistant-D-77** (confirmed by owner 2026-08-27) fixes the lineage at **`DOC-SYN-NEVER-STATE`** → `DOC-SYN-NEVER-STATE-V2`, category `front-desk-scripts`, path `documents/synthetic/DOC-SYN-NEVER-STATE.md`. Facts re-derived from the read-only package this session (`document-manifest.json` and `evaluations/eligibility-assistant-evaluations.jsonl`, joined 2026-08-27): eight single-row categories, three of them reason-table ids (`DOC-SYN-NO-INVENTION`, `-MODEL-FAILURE`, `-SPEND-STOP`); of the five that remain, `DOC-SYN-NEVER-STATE` is the **only one that is the `expected_source_ids` of no acceptance case** (`DOC-SYN-DOWNTIME` EVAL-015, `-EFFECTIVE-TERM-DATES` EVAL-002/032, `-MEMBER-ID-MISMATCH` EVAL-021, `-VISIBILITY-OUTCOMES` EVAL-019), so withdrawing it moves no harness case and no recall figure; applicability "All visits". Because the category holds one manifest row and an index entry's `topics` is the manifest `category` (eligibility-assistant-D-36), no curation can put a second row under that topic; what curation *can* break is the payer/product/state axes, so a **second `index.json` curation obligation** is carried on `corpus`'s change row — this row at `payers`/`products`/`states` = `*` — and asserted by `test_withdrawn_sole_source_refuses`, the eligibility-assistant-D-70 shape. **A consequence the finding did not reach, found walking the test against eligibility-assistant-D-38:** the row is **tier 4**, so D-38's applicability rule "no row of tier ≤ 3" already yields `refuse_definitive` + `escalate` before any withdrawal — the outcome does not discriminate and the test as authored would have passed on the unmodified corpus. The test therefore carries a **control leg** (unmodified: `lookup` returns exactly `[DOC-SYN-NEVER-STATE]` and the turn cites it) beside the withdrawn leg (zero rows, no citation, no verdict line); the runbook and residual (d) say the same, and the demo's observation is the citation set, not the outcome. **Sites changed:** `lifecycle.md` — Status line; `tests/test_a1_lifecycle.py` change row (lineage named; `test_withdrawn_sole_source_refuses` rewritten with its control leg); runbook change row (document named, the two turns' observation stated); verification 6; Landmines PHI entry (1) and residual-honesty bullet (d). `corpus.md` — Status line; `index.json` change row (second curation obligation). `plans/eligibility-assistant.md` — NEW `eligibility-assistant-D-77`.
| f6 | eligibility-assistant-SPEC-40 / D-54 · verification 5 (`:35`), 7 (`:37`) | **new** — Landing-position hedge: `lifecycle` and `trace` are "independent of each other" (item plan `:87`), so the pinned `32` and `1 skipped` hold only if this ticket lands before `trace`; after it they are `33` (`trace.md:34`) and `2 skipped` (`trace.md:35`, D-43). Fix the order by decision or state both values with the condition | **Disposition 2026-08-27 — A.** Accepted; the second remedy is taken — both values, with the condition — because eligibility-assistant-D-59's order deliberately leaves `{lifecycle, trace}` unordered (`plans/eligibility-assistant.md:96`, "→ {**lifecycle**, **trace**} (independent of each other)") and pinning it would add a landing constraint that buys only a shorter sentence. Verification 5 now reads **`32` before `trace`, `33` after** (this ticket adds no `[EVAL-n]`, eligibility-assistant-D-54; `turn.md:64` leaves 32, `trace.md:34` takes it to 33 with `EVAL-019`), and verification 7's suite line reads **`1 skipped` before `trace`, `2 skipped` after** (eligibility-assistant-D-43; `turn.md:65`, `trace.md:35`). Both conditions are also stated as their own Gate-interactions bullets so the impl gate reads the dependence before it reads the number, and deselected / xfailed are restated as never moving. **Sites changed:** `lifecycle.md` — Gate-interactions SPEC-37 bullet (rewritten) and NEW `skipped`-term bullet; verification 5 (rewritten); verification 7 (rewritten).
| f7 | eligibility-assistant-SPEC-59 · fact trail · `CorpusLoadError` (`:14`, `:18`, `:19`, `:32`) + bare facts | **new** — The exception name has no source: `corpus` lands "the loader's own error (the type is `policy_index`'s … `lifecycle` reuses it)" (`corpus.md:24`) and never names it (0 hits for `CorpusLoadError` in `corpus.md`); verification 2's expected output pins a name nothing decides. Other facts stated without locator (all verified true): `make ps` (`Makefile:43-44`), `docker compose restart ai-assistant` (service at `docker-compose.yml:222`), "CI `secret-scan` (gitleaks over the tracked tree)" (`ci.yml:120-129`), "`turn`'s 32" (`turn.md:62`), "`corpus`'s `test_manifest_sha_pinned`" (`corpus.md:24`), "the startup hook (landed by `corpus`)" (`corpus.md:16`), `VisitFacts.last_citations` (`turn.md:13`), `tests/a1_rig.py` (`turn.md:25`) | **Disposition 2026-08-27 — A.** Accepted on both halves. **(i) The exception type is named where it is landed**, not here: `corpus`'s `policy_index.py` change row and its `test_startup_hook_fails_boot_on_corpus_error` now say **`policy_index.CorpusLoadError`** (cross-ticket edit, the class fix — a symbol this ticket pins in an expected output must have a source in the ticket whose diff creates it), and every `lifecycle` site cites it as `policy_index.CorpusLoadError` with that locator. **(ii) Every bare fact the finding lists now carries its locator, each re-read this session:** `Dockerfile:8` / `:3`, `docker-compose.yml:222-247` (`:223` build, `:10` and `:267` the only bind mounts, zero `restart:` hits), `Makefile:34-35` (`make up`), `:43-44` (`make ps`), `:46-47` (`make build`); CI `secret-scan` = gitleaks v8.18.4 `--no-git` at `.github/workflows/ci.yml:120-129`; the CI `services` smoke at `:72-76`; `turn`'s 32 at `turn.md:64` and `trace`'s 33 at `trace.md:34`; `corpus`'s `test_manifest_sha_pinned` and `test_startup_hook_fails_boot_on_corpus_error` at `corpus.md:25`; the startup hook at `corpus.md:16`; `VisitFacts.last_citations` at `turn.md:13`; `tests/a1_rig.py` at `turn.md:25`; `app.py:30-36` for the module-import idiom and `app.py:51` for "no lifespan today" (via `corpus.md:16`); the seven reason-table ids at `docs/workflow/eligibility-assistant.md:203-210`; `tests/README.md:32`; `CLAUDE.md:141`; `docs/landmines.md:87`. **Sites changed:** `lifecycle.md` — `policy_index.py`, `outcome.py`, `app.py`, runbook, `tests/README.md` and `CLAUDE.md` change rows; NEW falsified-claims-sweep row; all seven Gate-interactions bullets; verifications 2, 5, 6, 7 and NEW 8; Landmines PHI entry (2) and "Domain services network-internal" bullet. `corpus.md` — Status line; `policy_index.py` change row; `tests/test_a1_corpus.py` change row (`test_startup_hook_fails_boot_on_corpus_error`).
| f8 | four checks · falsified-claims sweep · `CLAUDE.md` row (`:22`) | **new** — No sweep is recorded (contrast `corpus.md:29`, `turn.md:43-47`). Cold sweep of the registries on today's tree finds no corpus claim (nothing has landed), but the class this diff falsifies is already in the record — the manifest "verbatim" / "byte-identical to the package" (D-6, D-35, `corpus.md:13`, and `corpus`'s ADR 0019 paragraph once landed); D-57 records the two-key delta, the plan scopes it against none of those sentences and its ADR row only "appends". The one sweep-derived row, `CLAUDE.md` §6, is tagged SPEC-40 alone — a row `## Delivery` scopes to `trace` (retrieval-eval r1 f9 class) | **Disposition 2026-08-27 — A.** Accepted on both halves. **(i) The sweep is run and enumerated** as its own change row — `git grep -i` over `docs/landmines.md`, `docs/debt-log.md`, `docs/phi-logging-policy.md`, `docs/todo.md`, `CLAUDE.md`, `ARCHITECTURE.md` and `docs/runbook.md` on `policy_corpus` · `document-manifest` · `manifest` · `superseded` · `withdrawn` · `corpus` · `restart` · `lifecycle`, with every hit listed and its outcome: 0 hits on the first two; `docs/todo.md:74` (the cap-exemption manifest), `CLAUDE.md:159`/`:219`, `docs/debt-log.md:30`/`:217`, `docs/todo.md:6`/`:37`/`:58`/`:93` (superseded ADRs and the doc-archive rule), `docs/debt-log.md:268`/`:288`/`:329`, `docs/runbook.md:37`, `docs/todo.md:62`/`:88` (the records-service PHI corpus, the doubled seed, the `eval/rag/` RIV-160 corpus), `docs/runbook.md:261`/`:277` (Redis blip, DB reconnect), `ARCHITECTURE.md:44` ("Request lifecycle") — **all verbatim, none falsified**. In-code debt markers: `grep -rnE "TODO|FIXME|XXX|HACK|DEBT" services/ai-assistant/*.py` → no match (run 2026-08-27); the only pre-existing source file this ticket edits is `app.py`. `README.md` is the TODO-12 human-gated exemption. **(ii) The class the finding actually names is scoped, not left implicit:** the manifest-verbatim claim lives at `corpus.md:13` ("byte-identical to the package"), `corpus.md:72` ("a verbatim `document-manifest.json`") and register `eligibility-assistant-D-35` ("beside a verbatim `document-manifest.json`"); eligibility-assistant-D-57 already carries the reconciling clause, so the manifest change row now names all three sites and cites the decision that scopes them, states that `content_sha256` pins each **document file** and never the manifest (so no digest moves), and the `adr/0019-*.md` row states the two-key delta in the record that survives this file's deletion. Residual (g) says the recoverability of the client artifact is asserted by nothing in the suite. **(iii) The `CLAUDE.md` row is re-tagged** from eligibility-assistant-SPEC-40 to registry upkeep + eligibility-assistant-D-43, the reading `turn.md:48` and `retrieval-eval.md:21` already take on the same `CLAUDE.md:141` line — SPEC-40 is `trace`'s per the contract `## Delivery` ticket table. **Sites changed:** `lifecycle.md` — manifest change row (three sites named, sha-pin clause); `adr/0019-*.md` change row; `CLAUDE.md` change row (re-tagged, `:141` locator); NEW falsified-claims-sweep change row; residual-honesty bullet (g); verification 7 (tag corrected).
| f9 | eligibility-assistant-SPEC-58 / -59 · `policy_index.py` row (`:14`) | **new** — The closed-set validation omits two states D-57's own shape makes possible: a `superseded` row with `superseded_by: null` ("`null` unless `superseded`") and a `current` / `withdrawn` row with a non-null `superseded_by`; `test_load_fails_on_unresolved_chain` ×2 covers "→ withdrawn" and a cycle only. Minor | **Disposition 2026-08-27 — A.** Accepted. The rule is tightened from "`superseded_by` a manifest id whose chain ends on a `current` row" to **`superseded_by` non-null iff `status == "superseded"`**, and where non-null a manifest id whose chain ends on a `current` row — so a `superseded` row carrying `null` and a `current` or `withdrawn` row carrying an id each raise `policy_index.CorpusLoadError` naming the row. `test_load_fails_on_unresolved_chain` goes ×2 → **×4**: `superseded_by` → a `withdrawn` row; the cycle `A → B → A`; `superseded` with `null`; `current` with a non-null id. NEW verification 4c breaks each new leg separately (relax `null` to always legal; stop rejecting a non-null id on a `current` row). Test count 9 → 11, carried into verifications 1 and 7. The tightening is recorded as clause (c) of the dated note on eligibility-assistant-D-57, whose "`null` unless `superseded`" wording is what left both states legal, and it is named in the Landmines approval paragraph as part of what re-opens eligibility-assistant-D-56. **Sites changed:** `lifecycle.md` — `policy_index.py` change row; `tests/test_a1_lifecycle.py` change row (chain test ×4); verification 1 (`11 passed`); NEW verification 4c; verification 7 (`+ 11`); Landmines PHI entry (2) and approval-of-record paragraph. `plans/eligibility-assistant.md` — `eligibility-assistant-D-57` dated note clause (c).
| f10 | eligibility-assistant-SPEC-60 · residual bullets (`:48`, `:58`) | **new** — "Shall cite the current version" is model₂'s bounded choice on the agent path (SPEC-2); the loader guarantees only that the successor is retrievable and the predecessor is not citable (an old id in `citation_ids` → `validation_reject` → `DOC-SYN-NO-INVENTION`, SPEC-13). The rig scripts the successor and verification 6's live demo depends on real model₂ choosing it; neither residual bullet names this, and the negative form (script the old id → `validation_reject`) — the assertion that actually proves "shall not cite the superseded id" — is not in the test. Minor | **Disposition 2026-08-27 — A.** Accepted, and closed with f4's fix rather than beside it. The negative form the finding names is now a test — `::test_superseded_id_offered_by_model_is_rejected`: model₂ is scripted to select the superseded id, which is outside the turn's retrieved set, so SPEC-13's gate rejects it into reason `validation_reject`, mode `fallback`, citation `DOC-SYN-NO-INVENTION` (SPEC-52). The division of labour the finding asks for is written into residual bullet (b): SPEC-60's positive half ("shall cite the current version") is model₂'s bounded choice on the agent path (SPEC-2); what the loader guarantees is that the successor is **retrievable** and the predecessor is **not citable**; the default suite proves the negative half against a scripted model₂ and the positive half only against a scripted one, with the real model exercised in verification 6's live demo alone. **Sites changed:** `lifecycle.md` — `tests/test_a1_lifecycle.py` change row (the new test, and the SPEC-60/SPEC-2 division stated in it); residual-honesty bullet (b); Landmines PHI entry (1).

Per-SPEC verdicts: SPEC-58 FINDING (f3, f9); SPEC-59 FINDING (f1 on verification 2, f2, f7); SPEC-60 FINDING (f4, f10); SPEC-61 FINDING (f5); SPEC-62 FINDING (f1, f5). Design otherwise sound: status-not-date mechanism, retention + sha pin on non-current rows, current-only filter before `rank`, chain resolution, `tmp_path` variants with monkeypatched `_INDEX` per corpus r5 f3, no `[EVAL-n]` ids (D-54), Landmines block verbatim with the dated D-56 approval and all twelve §1 zones stated.

checked: eligibility-assistant-SPEC-58, -59, -60, -61, -62 (5 ids, the `lifecycle` `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, `lifecycle.md` Changes (10 rows) / Gate interactions (4) / Verification 1–7 / Landmines / Findings, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · check map 66/66 single-mechanism (5/5 in set: 4 `test:`, 1 `gate:` — SPEC-62) · ⚠ rows in set: 60, 61 (2) — PHI-handling zone entered with the approval of record eligibility-assistant-D-56 cited by ID and date (owner 2026-08-25 in session, extended same day to SPEC-60/61/63 per D-48, registered 2026-08-26); Secrets, Migrations, Domain-internal, Auth, IDOR, Inline-eligibility, ROI/booking/duplicates/HL7, Intake stated not-touched — every `docs/landmines.md` §1 zone covered · freeze scope: no `DEFERRED → <item>` requirement, no row on a superseded REQ · ticket scope: 66/66 frozen rows owned exactly once across the six ticket files and the `## Delivery` table (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated) — no orphan, no double owner; every change row traces to a scoped SPEC, a decision, or named registry upkeep except the `CLAUDE.md` row's off-scope tag (f8) · cites: every SPEC and decision id resolves (D-19/42/48/54/56/57/60/63; SPEC-7/37/40) · facts verified in tree: `Dockerfile:8` `COPY . .`, `docker-compose.yml:222-247` (build context, `expose` only, healthcheck, no volumes, no restart policy), `Makefile:34-35`/`:43-44`/`:46-47`, `ci.yml:72-76`/`:120-129`, `docs/landmines.md:87` = Intake bullet, `tests/README.md:32`, `CLAUDE.md:141`, `app.py:51` (no lifespan today), `.dockerignore` 3 lines, no `adr/0019-*`, no `policy_index` / `outcome` / `tests/a1_rig.py` / `tests/test_a1_*` on `main`, Compose v2.1.1; sibling text: `corpus.md:15-16/:24-25/:87`, `turn.md:13/:25/:62/:72`, `trace.md:20/:34-35`, item plan `:87` · package (outside repo, read-only): manifest 87 rows / 25 categories, no `status` / `superseded_by` key, all seven reason-table ids + `DOC-SYN-DOWNTIME` present, `documents/synthetic/` ×26, eight single-row categories · plan-text hash read: `git hash-object docs/workflow/plans/eligibility-assistant/lifecycle.md` = `ced933998354cdd93e45e9cb6a55eda5ae23a81d`; round 1 for this ticket · 10 findings (1 wrong mechanism, 1 unpinned mechanism, 1 cross-ticket gate interaction, 1 self-consistency, 2 fact-trail, 1 unpinned count, 1 sweep, 2 minor) · **origin: 10 new · 0 pre-existing · 0 class-repeat**

### Gate — round 2, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names only (no
branch named; agent read `main` with the uncommitted working tree), no characterization
received. **Process change this wave (owner, 2026-08-27, cost):** two agents grouped by
mechanism instead of six — this ticket read in one context with `corpus` and
`retrieval-eval`. Origin tags derived by the agent from the record — round 1's `checked:`
plan-text hash (`ced93399…`) and the `Sites changed:` list on all ten r1 dispositions.
Round 2: the round-3 rule does not apply. Plan returns to stage 3; spec unchanged. f3 is
minor.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-60 / SPEC-61 (⚠) · Landmines approval-of-record paragraph (`lifecycle.md:62`) vs Status line (`:3`) and register eligibility-assistant-D-75 / D-76 / D-77 | **new** (`:62` is on r1 f3's and f9's `Sites changed:` lists). The block — the graded surface and the ⚠ coverage record for this ticket's two ⚠ rows — still reads "**It is re-opened by this round and is in front of the owner now** … both are **PROPOSED**, and the owner's confirmation of them is the act that re-closes this approval", while the same file's Status line (`:3`) and the register (`plans/eligibility-assistant.md` D-75: "confirmed (owner 2026-08-27, in session) — … that same act re-closes the eligibility-assistant-D-56 §1 PHI approval on the amended mechanism"; D-76, D-77 confirmed the same day) record the confirmation and the re-close. The paragraph's own re-open list also still names "the `current`-only filter before `rank`" — the mechanism D-75 replaced with the load-time exclusion. The block states the §1 approval as open when the record says closed; `retrieval-eval.md:46` shows the post-confirmation form this paragraph says it copies | **Disposition 2026-08-27 — A.** The block was a round behind the record. The approval-of-record paragraph (`:62`) is rewritten to the post-confirmation form `retrieval-eval.md:46` carries: the re-open list names the mechanisms as they now stand — the closed status set, the `superseded_by` non-null-iff-`superseded` rule (eligibility-assistant-D-57 note clause (c)), the chain resolution, `required_current`, and the **load-time exclusion of non-current rows (eligibility-assistant-D-75)** in place of "the `current`-only filter before `rank`", the mechanism D-75 replaced — and the re-open clause is stated as **fired by gate r1 (f3 / f9) and re-closed the same day**: D-75 and the clause-(c) tightening both **confirmed by the owner 2026-08-27 (in session)**, that confirmation being the act that re-closes the approval, exactly as the register's D-75 row ("that same act re-closes …") and this file's Status line (`:3`) record; D-76 / D-77 named as confirmed in the same act and touching no named mechanism. Self-consistency walked after the edit: no clause of this block now reads PROPOSED where the register reads CONFIRMED (`grep -n PROPOSED lifecycle.md` over plan text this session → only the new eligibility-assistant-D-80 clause, which is PROPOSED in the register too and is named in the same paragraph as re-opening the approval until confirmed — f2). **Sites changed:** `lifecycle.md:62` (Landmines approval-of-record paragraph); `lifecycle.md:3` (Status line). |
| 2 | SPEC-61 / SPEC-62 · `policy_index.py` row (`:14`, D-75 "builds the in-memory index from the approved rows that are `current`, only") · runbook row (`:28`, the operator warning) vs `corpus.md:18` (`topic: Literal[*policy_index.categories()]` "built at import from the loaded manifest's distinct `category` values") | **class-repeat** (r1 f3 — a consequence of the lifecycle change on a `corpus`-landed structure left unstated; `:14` is on r1 f3/f7/f9's lists, so `new` by rule 2 if declined). Unnamed mechanism: neither this row nor D-75 says whether `categories()` — and with it the model-facing `topic` enum and `corpus`'s `test_topic_enum_equals_manifest_categories` (Literal == manifest categories == `index.json` topics) — reads all approved rows or `current` rows only. For the D-77 lineage it decides the real-tree behaviour SPEC-61/62 are demonstrated on: `front-desk-scripts` is a single-row category (package manifest, re-read this round), so after the runbook's withdrawal step a current-only `categories()` drops the topic from the tool schema and model₁ can no longer choose it — the demo's "the same turn renders `refuse_definitive` + `escalate` with no citation" only occurs if the topic survives withdrawal; a manifest-wide `categories()` keeps it, and then `test_topic_enum_equals_manifest_categories` must read the manifest the same way. The default suite cannot see the choice — the Literal is built from the real tree at rig import and the variant is installed as `_INDEX` alone. State which, and add the 25-category count to the runbook warning's list of pins a real withdrawal moves (it names only the two 87s) | **Disposition 2026-08-27 — A.** Stated, and pinned. **eligibility-assistant-D-80** (proposed here; **confirmed by the owner 2026-08-27, in session** — register cell and both Landmines blocks updated the same day): `categories()` is **manifest-wide** — `policy_index.categories(root=None)` mirrors `load(root=None)` and returns `load(root=root).categories`, the sorted tuple of distinct `category` values over **every approved manifest row whatever its lifecycle status**, while the index's `rows` are current-only (D-75). Mechanism: the object `load()` returns carries `rows` and `categories` from its one parse — no second manifest reader (`corpus` gate r4 f4's SPEC-7 boundary holds) and no second module-level structure (D-75's non-publish reasoning holds; `_INDEX` is still one object, and the variant a `lifecycle` test installs as `_INDEX` carries its manifest-wide categories with it). Why manifest-wide and not current-only: `front-desk-scripts` is a single-row category (package `document-manifest.json`, re-read 2026-08-27 — eight single-row categories, this one holding `DOC-SYN-NEVER-STATE` alone), so a current-only enum would drop the topic from `PolicyLookupArgs.topic` after the runbook's withdrawal step and model₁ could not choose it; the demo's "same turn renders `refuse_definitive` + `escalate` with no citation" (SPEC-61/62) exists only if the topic survives, and under D-80 it does — `lookup` returns zero rows and the refusal is SPEC-61's. Consequences for `corpus`'s pins, each read this session: `test_topic_enum_equals_manifest_categories` (`corpus.md:26`) stays a three-way equality over one manifest-wide set, since `index.json` keeps an entry per approved row whatever its status (`corpus.md:14`); the 25-category count moves only when a row in a **new** category is added, never on a supersession or withdrawal within an existing one — added to the runbook warning (`:28`) beside the two 87s, with that condition. **The default suite now sees the choice:** NEW `tests/test_a1_lifecycle.py::test_categories_survive_withdrawal` (`:26`) — on the D-77 withdrawn variant, `policy_index.categories(root=variant)` still contains `front-desk-scripts` while `load(root=variant).rows` holds no row of that topic; verification 3 gains the break (derive `categories` from the current-only `rows` → that test red, every other green). Test count 11 → **12**, carried into `:18`, verification 1 and verification 7. Self-consistency walked: `categories()` with no `root` still runs the default-root `load()` at `import policy_tool` (`corpus.md:18`), so the rig's publication of `_INDEX` / `MAX_ROW_BYTES` (eligibility-assistant-D-66) is unchanged and `MAX_ROW_BYTES` is still computed over `rows` — 2,789 (D-75); `corpus` verification 4l's second break, which monkeypatches `_INDEX`, now names the `rows` it replaces. Landmines: D-80 changes what the `topic`-only tool schema is built over — a mechanism `corpus`'s block names — so it re-opens eligibility-assistant-D-56 on both blocks until the owner confirms it, stated in each. **Sites changed:** `lifecycle.md:14` (`policy_index.py` row — the manifest-wide `categories(root=None)` clause, D-80); `lifecycle.md:18` (test count twelve); `lifecycle.md:26` (`tests/test_a1_lifecycle.py` row — NEW `test_categories_survive_withdrawal`); `lifecycle.md:28` (runbook row — the 25-category pin and its condition); `lifecycle.md:44` (verification 1 → `12 passed`); `lifecycle.md:46` (verification 3 — second break); `lifecycle.md:52` (verification 7 → `+ 12`); `lifecycle.md:62` (Landmines approval paragraph — the D-80 re-open clause); `lifecycle.md:3` (Status line). `corpus.md:15` (`policy_index.py` row — `categories(root=None)` manifest-wide, the `rows` / `categories` shape); `corpus.md:26` (`test_topic_enum_equals_manifest_categories` — manifest-wide clause); `corpus.md:47` (verification 4l second break — an index whose `rows` are the four); `corpus.md:75` (Landmines approval paragraph — D-80 carry-forward clause); `corpus.md:3` (Status line). `plans/eligibility-assistant.md` — NEW eligibility-assistant-D-80 (PROPOSED); eligibility-assistant-D-75 dated note (pointer to D-80). |
| 3 | checked set · fact trail · Gate-interactions SPEC-37 bullet (`:38`) and verification 5 (`:50`) cite "`plans/eligibility-assistant.md:96`" for D-59's "{**lifecycle**, **trace**} (independent of each other)"; register D-59 note 2 cites the same clause at `:87` | **class-repeat** (r1 f7 — fact trail: a locator that does not say what the claim says; both sites are on r1 f6's `Sites changed:` list, so `new` by rule 2 if declined). `plans/eligibility-assistant.md:96` is Evidence row E-6 and `:87` is `### Evidence`; the `## Plan` Status line carrying the landing-order clause is now `:101` — D-66..D-79 were inserted above it after the cites were written. The clause itself holds (`:101`: "corpus → llm-seam → retrieval-eval (dark) → turn → {lifecycle, trace} (independent of each other)"). Minor | **Disposition 2026-08-27 — A.** Both locators corrected, and the class closed at its cause. The three sites that cited a line for eligibility-assistant-D-59's order clause — `lifecycle.md:38`, `:50` (`:96`) and the register's D-59 note 2 (`:87`) — now cite it the way the README's stable-ID rule asks (`docs/workflow/README.md:218-221`: artifacts by stable ID, never by line): "eligibility-assistant-D-59's order clause, restated in the item plan's `## Plan` `Status:` line", with the line given for the record only — **`:102` as read 2026-08-27 after this round's eligibility-assistant-D-80 row** (`:101` when the gate read it; `:96` / `:87` when the cites were written, before D-66..D-80 were inserted above the line). Class (r1 f7 / `retrieval-eval` r1 f12 and r2 f1 — a locator into a sibling artifact that a later insert moved) closed with an enumerated sweep of **every** sibling-artifact line cite in the plan text (above `## Findings`) of all six ticket files and in the register's decision rows, each resolved this session against the file as it stands (`re.finditer` over `<name>.md:<n>` — 81 resolutions in the five ticket files' plan text and 7 in register rows, all listed by the script's output this session): **into `plans/eligibility-assistant.md`** — `lifecycle.md:38`, `:50`, `retrieval-eval.md:5`, register D-59 note 2 (**four, all changed** — this cell and `retrieval-eval` r2 f1); **into `corpus.md`** — `lifecycle.md:13` (`:13`, `:15`, `:72`), `:14` (`:14`, `:15`, `:25`, `:26`), `:16` (`:16`), `:32` (`:13`, `:72`), `:35` (`:25`), `:36` (`:16`, `:52`), `:37` (`:25`), `:41` (`:14`), `:53` (`:25`, `:26`); `retrieval-eval.md:3` (`:29`), `:13` (`:26`, `:27`), `:15` (`:25`), `:16` / `:17` / `:18` (`:14`, `:18`, `:19`, `:26`), `:27` (`:15`, `:65`), `:29` (`:53`), `:30` (`:16`, `:60`), `:33` (`:19`), `:34` (`:27`), `:35` (`:53`); `turn.md:48` (`:178`, `:181`), `:49` (`:28`, `:180`); register D-57 (`:15`, `:16`), D-75 (`:25`, `:26`) — all hold; `trace.md:25` (`:27`) — **changed to `:28`**, the `adr/0019-*.md` row having moved when `corpus` r7 f1 inserted the rig row at `:24` (recorded in `trace`'s r2 f4 cell as well, since that file's cells this round cite it); **into `turn.md`** — `lifecycle.md:15` (`:15`), `:31` (`:48`), `:36` (`:15`), `:37` (`:74`), `:38` / `:39` / `:50` / `:52` (`:64`, `:65`); `retrieval-eval.md:15` (`:5`), `:29` (`:64`); `trace.md:20`, `:26` (`:47`), `:37` (`:64`), `:39` (`:67`), `:40` (`:68`), `:41` (`:69`); `corpus.md:24` (`:25`); register D-57 (`:15`), D-59 (`:5`) — all hold, and this round's one `turn.md` insert (verification 20b, after `:76`) moves none of them; **into `trace.md`** — `lifecycle.md:28` (`:20`), `:38` / `:50` (`:37`), `:39` / `:52` (`:38`); `turn.md:47`, `:49` (`:26`); register D-68 (`:15`) — all hold; **into `retrieval-eval.md`** — `corpus.md:29` (`:19`, `:20`); `lifecycle.md:18` (`:15`), `:31` (`:21`); `trace.md:3` (`:74`), `:37` (`:35`); `turn.md:48` (`:21`) — all hold; **into `lifecycle.md`** — `corpus.md:29` (`:30`); `retrieval-eval.md:13`, `:15` (`:18`) — all hold, and no line is inserted in this file this round (the new test rides `:26`, its break `:46`); **into `llm-seam.md`** — `corpus.md:24` (`:15`), `retrieval-eval.md:29`, `:35` (`:34`) — hold. Eighty-eight resolutions, five changed. **Sites changed:** `lifecycle.md:38` (Gate-interactions SPEC-37 bullet — cite by section, `:102` for the record); `lifecycle.md:50` (verification 5 — same); `plans/eligibility-assistant.md` eligibility-assistant-D-59 note 2 (same form); `trace.md:25` (`corpus.md:27` → `:28`); `retrieval-eval.md:5` is `retrieval-eval` r2 f1's site and is listed there. |

Verified and **not** a finding: every other locator holds — `Dockerfile:3/:8`, `docker-compose.yml:222-247` (`:223`; bind mounts only at `:10`, `:267`; zero `restart:`), `Makefile:34-35/:43-44/:46-47`, `ci.yml:72-76/:120-129`, `app.py:30-36/:51`, `tests/README.md:32`, `CLAUDE.md:141`, `docs/landmines.md:87`, contract `:203-210`, `corpus.md:13/:14/:15/:16/:25/:26/:52/:60/:72`, `turn.md:13/:15/:25/:48/:64/:65/:72/:74`, `trace.md:20/:37/:38`; the eight-term registry sweep re-run reproduces every hit the row lists and no other; in-code debt markers 0; the eleven-test count sums (1+1+4+1+1+1+1+1); D-77's facts reproduce (eight single-row categories, five non-reason-table, `DOC-SYN-NEVER-STATE` in no case's `expected_source_ids`, tier 4, the other four cited by EVAL-015 / 002+032 / 021 / 019). Not raised: the four malformed-chain variants (cycle, `superseded`+`null`, `current`+id) are not producible by `corpus_variant(supersede=, withdraw=)` as described — an implementation detail, a direct manifest edit on the copy.

Per-SPEC verdicts: SPEC-58 satisfied; SPEC-59 satisfied; SPEC-60 FINDING (f1); SPEC-61 FINDING (f1, f2); SPEC-62 FINDING (f2).

checked: eligibility-assistant-SPEC-58, 59, 60, 61, 62 (5 ids, the `lifecycle` `Scope:` line) against Decisions eligibility-assistant-D-1..79, Evidence E-1..7, `lifecycle.md` Changes (11 rows) / Gate interactions (7) / Verification 1–8 / Landmines / Findings r1, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `b6a47d500d825b08a5949c719f540c8bbecb08ed`** (round 1 recorded `ced933998354cdd93e45e9cb6a55eda5ae23a81d`; all ten r1 dispositions carry `Sites changed:` lists, so tags are derived from the record) · **origin: 1 new · 0 pre-existing · 2 class-repeat** · check map 66/66 (5/5 in set: 4 `test:`, 1 `gate:` SPEC-62) · ⚠ rows 60, 61 — PHI-handling entered, eligibility-assistant-D-56 cited by id and date (extended 2026-08-25 to SPEC-60/61/63) but the block's re-open clause is stated open (f1); other 11 §1 zones written out not-touched · freeze scope 0 `DEFERRED` · ownership 66/66 once · cites resolve; every change row traces to a scoped SPEC, a decision, or named registry upkeep · 3 findings (1 Landmines bookkeeping on the ⚠ record, 1 unnamed mechanism, 1 fact-trail minor).

### Gate — round 3, 2026-08-28

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names only (no
branch named; agent read `main` @ `762f0fd` with the uncommitted working tree), no
characterization received. Two agents by mechanism (owner, 2026-08-27): this ticket read
in one context with `corpus` and `retrieval-eval`. Origin tag derived by the agent from
the record — round 2's `checked:` plan-text hash (`b6a47d50…`) and the `Sites changed:`
lists on all three r2 dispositions. **Round-3 rule fires:** f1 is `new` (`:51` is on no
`Sites changed:` list, so the anchor cannot be settled as pre-existing — fails toward
escalation), so the owner adjudicates it (accept as named residual · overrule · change the
spec) before stage 3 dispositions it; f2 is `pre-existing`, ordinary stage 3. Plan returns
to stage 3 after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-62 (`gate:`) / SPEC-60 · verification 6 (`lifecycle.md:51`, "a portal turn on question type / payer resolving to topic `front-desk-scripts`") · runbook row (`:28`, "the two turns the demo runs after each step") · residual bullets (b), (d) | **new** (`:28` is on r2 f2's `Sites changed:` list; `:51` is on none). The demo observation is stated as if the topic were an input, but on the agent path the topic is model₁'s choice from the four closed selections (SPEC-1, D-25) and the application cannot force it; the plan names neither the question type / payer / product / state the operator enters for the two demo turns nor a fallback when the live model picks another of the 25 categories, and `DOC-SYN-NEVER-STATE` is in no acceptance case so `case_selections.json` carries none. SPEC-62 is `gate:` "hand-run from the runbook alone": as written the runbook cannot reproduce its own observation. Residual (b) covers only which version model₂ cites, not whether model₁ reaches the topic. Name the four selections in the runbook row and verification 6, and write the model₁-choice dependence as a residual (the default suite's scripted model₁ being the deterministic proof) |  **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A.** (1) The four selections named in the runbook row (`:28`) and verification 6 (`:51`): question type `covered today`, payer Aetna — the seeded demo member `AETN1224` `trace`'s demo already uses (`trace.md:46`) — product `unconfirmed`, state `unconfirmed`; eligibility-assistant-D-77's curated `*` axes make any payer value legal (`corpus.md:14`). (2) A deterministic leg model₁ cannot move, in both sites: `docker compose exec ai-assistant python -c "import policy_index; rows, _ = policy_index.lookup('front-desk-scripts', 'aetna', 'unconfirmed', 'unconfirmed'); print([r['id'] for r in rows])"` → `['DOC-SYN-NEVER-STATE-V2']` after supersession, `[]` after withdrawal — the two-tuple return of eligibility-assistant-D-58 / D-69, the `id` key of D-61, service `ai-assistant` (`docker-compose.yml:222-247`), `WORKDIR /app` (`Dockerfile:3`). The fixture flag was checked as a fallback and rejected: `A1_MODEL_FIXTURE` labels the mode and scripts no model (eligibility-assistant-D-71; `turn.md:15`), so no deployed switch forces the topic. (3) The dependence written as residual (h) (`:82`), cross-referenced from (b) (`:76`); the runbook states the topic is model₁'s and that a turn routed elsewhere is re-run once, the lookup being the observation of record either way. Dated note on eligibility-assistant-D-77. Four checks over the fix: self-consistency — the lookup command's shape matches `corpus.md:15` / D-69 (two-tuple, `id`); gate interaction — the in-container lookup emits one `LookupRecord` log line (eligibility-assistant-D-68) and nothing else; residual honesty — (h); falsified claims — none (the runbook section is this item's own, `docs/runbook.md` has no eligibility section today). Sites changed: `lifecycle.md:28` (runbook row), `:51` (verification 6), `:76` (residual (b)), `:82` (new residual (h)), `:3` (Status line); register eligibility-assistant-D-77 (dated note 2026-08-28). |
| 2 | SPEC-59 · `outcome.py` row (`:15`, "`reason_citation_ids()`, the set of document ids"), `app.py` row (`:16`, `load(required_current=outcome.reason_citation_ids())`), `test_startup_hook_requires_current_reason_ids` (`:23`, "`required_current == tuple(app_mod.outcome.reason_citation_ids())`"), verification 4b (`:48`) | **pre-existing** (r2 hash recorded; both r2 dispositions carry lists; none names `:15`/`:16`/`:23`/`:48`). Self-consistency: the hook passes the function's return unchanged and the test compares it to `tuple(...)` of the same return. If `reason_citation_ids()` returns a `set`/`frozenset` — the plain reading of "the set of document ids" — `set == tuple` is `False` and the plan's own test reds on a correct hook; it is green only if the return is already a tuple, in which case the `tuple(...)` wrap is vacuous. State the return type once at `:15` (a sorted tuple), so 4b's red/green is determinate. Minor |  **Disposition 2026-08-28 — A.** Return type stated once at `:15` — `reason_citation_ids()` returns a sorted `tuple[str, ...]` — and both pins compare it without a wrap: `:16` and `:23` now read `required_current == app_mod.outcome.reason_citation_ids()`, so 4b's red/green is determinate (a `set` return now reddens the pin on a correct hook, which is the direction the test should fail). Sweep of the same claim elsewhere: `:14` ("every id in `required_current` `current`" — type-neutral, unchanged), `:36` ("carries **no** `required_current`" — unchanged), `:48` ("the default empty tuple" — agrees), `:61` ("the eligibility-assistant-D-19 set" — English, unchanged); no sibling file names `reason_citation_ids` outside register D-77 ("is **not** in `outcome.reason_citation_ids()`", membership only — `grep -n reason_citation_ids` over the five siblings and the register, 2026-08-28). The gate's not-raised note (`import outcome` possibly idempotent) needs no edit: `app.py:30-36` imports no `outcome` today (read 2026-08-28), so the row's "gains" is literal. Sites changed: `lifecycle.md:15` (`outcome.py` row), `:16` (`app.py` row), `:23` (`test_startup_hook_requires_current_reason_ids`), `:3` (Status line). |

Per-SPEC verdicts: SPEC-58 satisfied; SPEC-59 FINDING (f2, minor); SPEC-60 residual-named (residuals (a), (b) written); SPEC-61 satisfied (control + withdrawn legs; tier-4 consequence stated); SPEC-62 FINDING (f1).

Not raised, for the record: `turn.md:17` does not state whether the wired `app.py` already imports `outcome`, so `:16`'s "gains `import outcome`" may be idempotent — harmless either way.

checked: eligibility-assistant-SPEC-58, 59, 60, 61, 62 (5 ids, the `lifecycle` `Scope:` line) against Decisions eligibility-assistant-D-1..80, E-1..7, `lifecycle.md` Changes (12 rows) / Gate interactions (7) / Verification 1–8 / Landmines / Findings r1–r2, sibling `Scope:` + `Depends on:` lines, contract `## Delivery` · **plan-text hash read: `d7da1c3c78442e8f3663298d227bf8b9c6b4d82a`** (round 2 recorded `b6a47d500d825b08a5949c719f540c8bbecb08ed`; all three r2 dispositions carry `Sites changed:` lists) · **origin: 1 new · 1 pre-existing · 0 class-repeat** — round 3 with an open `new`: the round-3 rule fires · check map 66/66 (5/5 in set: 4 `test:`, 1 `gate:` SPEC-62) · ⚠ rows 60, 61 — PHI-handling entered, D-56 cited by id and date, extended 2026-08-25 to SPEC-60/61/63, re-opened by D-75/D-57(c) and D-80 and re-closed by the owner's confirmations 2026-08-27 as the paragraph now reads; other 11 §1 zones written out · freeze scope 0 `DEFERRED` · ownership 66/66 once · cites resolve (D-75/76/77/80 present and confirmed; `corpus.md:13/:14/:15/:16/:25/:26/:52/:60/:72`, `turn.md:5/:13/:15/:25/:48/:64/:65/:74`, `trace.md:20/:37/:38`, `retrieval-eval.md:15/:21`, item plan `:102`, contract `:203-210`, `README.md:218-221` all hold); change list closes both ways (`CLAUDE.md` row as upkeep + D-43) · twelve-test count sums (1+1+4+1+1+1+1+1+1) · facts verified in tree: `Dockerfile:3/:8`, `docker-compose.yml:222-247` (`:223` build, `expose` only, bind mounts only `:10` / `:267`, zero `restart:`), `Makefile:34-35/:43-44/:46-47`, `ci.yml:72-76/:120-129`, `app.py:30-36/:51`, `tests/README.md:32`, `CLAUDE.md:141`, `docs/landmines.md:87`; eight-term registry sweep re-run reproduces every listed hit and no other (`todo.md:58` does carry "superseded"); in-code debt markers 0 · package: eight single-row categories, `front-desk-scripts` = `DOC-SYN-NEVER-STATE` alone ("All visits", original synthetic → tier 4), in no case's `expected_source_ids`; the other four singles cited by EVAL-015 / 002+032 / 021 / 019; manifest carries no `status` / `superseded_by` key · 2 findings (1 residual/reproducibility on the `gate:` row, 1 self-consistency minor).

### Gate — round 4, 2026-08-28

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names + branch
(`main`, working tree) only — no characterization received; agent read `main` @ `762f0fd`
with the uncommitted working tree. Two agents by mechanism (owner, 2026-08-27): this
ticket read in one context with `corpus` and `retrieval-eval`. Origin tag derived by the
agent from the record — round 3's `checked:` plan-text hash (`d7da1c3c…`) and the
`Sites changed:` lists on both r3 dispositions (f1: `:28`, `:51`, `:76`, `:82`, `:3`;
f2: `:15`, `:16`, `:23`, `:3`). **Round-3 rule fires:** round 4 with one open
`class-repeat` finding (`pre-existing` if the class match is declined — fires on the
class-repeat reading), so the owner adjudicates it (accept as named residual · overrule ·
change the spec) before stage 3 dispositions it. Minor. Plan returns to stage 3 after
adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-59 (SPEC-58) · `policy_index.py` row (`lifecycle.md:14`, "a chain reaching a `withdrawn` row, a **dangling id**, and a cycle are each unresolved"; same clause in the Landmines load-time entry `:61`) vs `test_load_fails_on_unresolved_chain` ×4 (`:21`) and verification 4c (`:49`) | **class-repeat** (r1 f9 — an unresolved-chain shape the loader rule names that the chain test does not exercise; `:14` / `:21` / `:49` are on no r3 `Sites changed:` list, so `pre-existing` if the class match is declined). The rule enumerates three unresolved shapes; the test's four legs are `→ withdrawn`, the cycle, `superseded`+`null`, `current`+id — no leg sets `superseded_by` to an id that is **not a manifest row**, and no break in 4 / 4b / 4c reddens on it. An implementation that resolves `superseded_by` with a `.get()` and treats a missing target as "not withdrawn / not a cycle" passes every leg while accepting the commonest real operator error (a mistyped successor id in the runbook's supersede step). Add the fifth leg (`superseded_by: "DOC-SYN-NOPE"` → `CorpusLoadError` naming the row) and its break. Minor | **owner adjudicated 2026-08-28 (round-3 rule): accepted → stage 3.** **Disposition 2026-08-28 — A, accepted as named residual (i) (owner, in session; owner override 2026-08-28, in session: stamped in the gate session without a fresh re-run; the drift-gate rule "never stamp with an open finding" set aside by explicit owner decision for this wave).** Residual (i) names the missing leg and states that the implementation adds it as a fifth parametrized case of `test_load_fails_on_unresolved_chain` (`superseded_by: "DOC-SYN-NOPE"` → `CorpusLoadError` naming the row) — the test function count (twelve) is unchanged; the impl gate reports the collected-case move against SPEC-40's baseline line. No mechanism or decision moves. Sites changed: `lifecycle.md` Landmines residual (i) (new), `:3` (Status line), this cell. |

Not raised by the agent, for the record: (i) the runbook supersede step says "add … its `index.json` entry" without saying the entry copies the predecessor's `*` axes — verification 6's `['DOC-SYN-NEVER-STATE-V2']` rests on that; the rig's `test_superseded_never_cited` enforces it for the test variant, the runbook text will have to say it for the operator. (ii) `turn.md` never states whether its wired `app.py` imports `outcome`, so `:16`'s "gains `import outcome`" may be idempotent — harmless. (iii) register D-59 note 2 still carries `:102` dated "as read 2026-08-27" — a dated for-the-record line under the stable-ID rule, not a live cite.

Per-SPEC verdicts: SPEC-58 satisfied (closed status set, non-null-iff-superseded, retention + sha pin on non-current rows, current-only `rows` per D-75); SPEC-59 FINDING (f1; the two load sites, the hook-argument pin and `reason_citation_ids()`'s sorted-tuple return all verified consistent); SPEC-60 residual-named (residuals (a), (b), (h) written; negative `test_superseded_id_offered_by_model_is_rejected` present); SPEC-61 satisfied (control + withdrawn legs; tier-4 consequence stated; D-80 keeps the topic reachable); SPEC-62 satisfied for a `gate:` row (D-76 rebuild-and-recreate verified against `Dockerfile:8` / compose; the four demo selections named; deterministic in-container lookup leg; residual (h)).

checked: eligibility-assistant-SPEC-58, 59, 60, 61, 62 (5 ids, the `lifecycle` `Scope:` line) against Decisions eligibility-assistant-D-1..81, Evidence E-1..7, `lifecycle.md` Changes (12 rows) / Gate interactions (7) / Verification 1–8 / Landmines / Findings r1–r3, sibling `Scope:` + `Depends on:` lines, item ticket table, contract `## Delivery` · **plan-text hash read: `c1a69e484a0d9068b9afac2f4d7db3ed30a1b21e`** (round 3 recorded `d7da1c3c78442e8f3663298d227bf8b9c6b4d82a`; both r3 dispositions carry `Sites changed:` lists) · **origin: 0 new · 0 pre-existing · 1 class-repeat** · check map 66/66 (5/5 in set: 4 `test:`, 1 `gate:` SPEC-62) · ⚠ rows 60, 61 — PHI-handling entered, D-56 cited by id and date, extended 2026-08-25 to SPEC-60/61/63, re-opened by D-75 / D-57(c) / D-80 and re-closed by the owner's 2026-08-27 confirmations as the paragraph reads; other 11 §1 zones written out · freeze scope 0 `DEFERRED` · ownership 66/66 once · cites resolve (D-75/76/77/80 confirmed, D-77 dated note of 2026-08-28 present; `corpus.md:13/:14/:15/:16/:25/:26/:52/:60/:72`, `turn.md:5/:13/:15/:17/:25/:48/:64/:65/:74`, `trace.md:20/:37/:38/:46`, `retrieval-eval.md:15/:21`, item plan `:103`, contract `:203-210`, `README.md:218-221` all hold) · change list closes both ways (`CLAUDE.md` row as upkeep + D-43) · twelve-test count sums (1+1+4+1+1+1+1+1+1) · facts verified in tree: `Dockerfile:3/:8`, `docker-compose.yml:222-247` / `:10` / `:267` / zero `restart:`, `Makefile:34-35/:43-44/:46-47`, `ci.yml:72-76/:120-129`, `app.py:30-36/:51` (no `outcome` import today), `tests/README.md:32`, `CLAUDE.md:141`, `docs/landmines.md:87`, `db/seed/seed.sql:456` `AETN1224` Aetna active, `docs/runbook.md` has no eligibility-assistant section; eight-term registry sweep reproduces every listed hit · package (read-only): `front-desk-scripts` = `DOC-SYN-NEVER-STATE` alone ("All visits", original synthetic → tier 4), in no case's `expected_source_ids`; the other four non-reason singles cited by EVAL-015 / 002+032 / 021 / 019; seven reason ids present; no lifecycle key · 1 finding, minor, class-repeat.

**Stamped `plan GATED 2026-08-28` under owner override** (in session, 2026-08-28): f1 accepted as named residual (i) and dispositioned in the same act; no fresh re-run — the `drift-gate` "never stamp with an open finding" rule set aside by explicit owner decision for this wave, recorded here and in the Status line. Writes by the gate session under the override: this cell, residual (i), the Status line, the contract ticket row. Plan-text hash read this round (`c1a69e48…`) is the pre-override text; the stamped text differs from it by those sites only.
