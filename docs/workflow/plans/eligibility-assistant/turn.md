# eligibility-assistant / turn

Status: plan DRAFT 2026-08-25 (re-sliced from item plan; gate rounds 1–2 on the monolithic plan are in plans/eligibility-assistant.md)
Scope: SPEC rows eligibility-assistant-SPEC-1–6, 12–23, 25–26, 33, 36–37, 39, 41–42, 44–56 (eligibility-assistant-D-34, PR-1c — the wired turn: backend, gateway, portal, harness). SPEC-39 was split into 39a / 39b by eligibility-assistant-D-47 after D-34 was written; which ticket owns 39b is gate round 2 finding f4, open
Depends on: corpus, llm-seam

## Plan

Changes (file level):

- `services/ai-assistant/schemas.py` — `VisitChatRequest` gains `question_type`, `payer`, `product`, `state` (closed `str, Enum`s from eligibility-assistant-D-36, required) and `emergency: bool` (default false), `extra="forbid"` kept; `VisitFacts` gains `last_citations: list[Citation] | None` where `Citation` is `document_id` + `version` only (`extra="forbid"`); `VisitChatResponse` gains `citations: list[RenderedCitation]` (title, document_id, section, version — four fields), `mode`, `reason: str | None`, `outcome`, `correlation_id`, `model`; `AgentDecision`; the `VisitTurn` transcript shape is untouched (eligibility-assistant-SPEC-4/14/20/33/55 / eligibility-assistant-D-15, eligibility-assistant-D-33, eligibility-assistant-D-39)
- `services/ai-assistant/visit_templates.py` — the eleven new catalog ids with fixed copy; `a1_default_selection(a1_status, question_type)` / `a1_allowed_selection` alongside today's functions; `verdict_line` gains the `unavailable`, `conflict`, `refuse_definitive`, `refuse`, `stop`, `care_first` sentences — `unknown`/`unavailable` never read as a denial (eligibility-assistant-SPEC-13/15/16/42 / eligibility-assistant-D-38, eligibility-assistant-D-39)
- `services/ai-assistant/outcome.py` — NEW: outcome derivation, applicability check, reason table (reason → fixed `(document_id, section)` citations, action set), mode derivation (eligibility-assistant-SPEC-3/6/14/15/42/45/52/53 / eligibility-assistant-D-19, eligibility-assistant-D-38)
- `services/ai-assistant/agent_turn.py` — NEW: `TurnState`, `TurnMiddleware` (`before_model` payer hook + status injection; `after_model` bounds), `PayerGate` once-guard, `run_agent_path()` → (decision | reason, egress flag, model-call count, retrieved rows) (eligibility-assistant-SPEC-1/2/19/21/22/23/50/51/56 / eligibility-assistant-D-18, eligibility-assistant-D-26, eligibility-assistant-D-27, eligibility-assistant-D-29, eligibility-assistant-D-37)
- `services/ai-assistant/app.py` — `visit_chat` reordered per SPEC-50: emergency gate → cross-patient gate (eligibility-assistant-D-44) → `_derive_intent` → the `awaiting_id` / `ambiguous_id` short-circuit (today's `_reply_items` no-freedom branch, now mode `no_lookup`) → `run_agent_path` → validate (`required ⊆ selection ⊆ allowed` over the extended sets, citation ids ⊆ retrieved keys) → fallback per reason → ground → phrase; the fallback path calls `PayerGate` when the hook did not; `_build_visit_prompt` split into `_build_model1_prompt` (intent, question type, payer, product, state, turn count, topic list) and `_build_model2_message` (a1 status, row keys, catalog ids) — clerk text, member id and any identifier in neither; `_reply_items` retired, its egress/health contract carried by `run_agent_path`; `X-Correlation-Id` read from the request headers (the `x-internal-auth` idiom at `:187`), UUIDv4-validated or minted, echoed as `VisitChatResponse.correlation_id` (eligibility-assistant-SPEC-26 / eligibility-assistant-D-46); `visit_chat_log_metadata` gains `mode`, `reason`, `outcome`, `model_calls`, `correlation_id` (closed values / UUID); docstrings at `:14-18` and `:832-835` corrected — the model is no longer "consulted last" (eligibility-assistant-SPEC-1/2/3/5/12/13/14/19/26/45/46/49/50/51/52/56 / eligibility-assistant-D-27, eligibility-assistant-D-28, eligibility-assistant-D-46)
- `services/ai-assistant/config.py` — `a1_model_fixture` (`A1_MODEL_FIXTURE`) (eligibility-assistant-SPEC-33 / eligibility-assistant-D-39)
- `services/gateway/app.py` — `_VisitChatRequest` gains the four enum fields (mirrored closed sets) + `emergency`, still `extra="forbid"`, body forwarded as `{message, turns, facts, question_type, payer, product, state, emergency}`; `X-Correlation-Id` read (UUIDv4 shape) or minted, added to the headers dict `_post_checked` already receives beside `X-Internal-Auth` (`:1150-1156`), echoed in the response (eligibility-assistant-D-46); `_VisitChatFacts` learns `last_citations` (shape-validated, ids + version only); `_VisitChatDownstream` validates `citations` (four bounded strings each), `mode`, `reason`, `outcome`, `model` as bounded snake-case shapes (not enum copies — the round-6 rule) and relays them; no new route, no authz change, `require_capability("ai.use")` untouched (eligibility-assistant-SPEC-20/26/41/47/48 / eligibility-assistant-D-16, eligibility-assistant-D-30). The route is the seam-map "Gateway fan-out" wiring point; the surrounding wall is not restructured.
- `contracts/visit-chat-turn.json` — NEW declaration (eligibility-assistant-D-45)
- `frontend/app/api/ai/visit-chat/route.ts` — mints the correlation id, sends `X-Correlation-Id` + `X-Portal-Sent-At` (eligibility-assistant-SPEC-25 / eligibility-assistant-D-16, eligibility-assistant-D-41); `frontend/app/lib/gateway.ts` — `proxy()` accepts extra headers (eligibility-assistant-SPEC-25)
- `frontend/app/lib/gateway.test.ts` — NEW file: `correlation id minted per turn` (eligibility-assistant-SPEC-25)
- `frontend/app/assistant/page.tsx` — closed `<select>`s for question type / payer / product / state, an `emergency` checkbox, the five selections sent on every turn; reply renders the citations list (four fields each), a mode badge, the reason line on a fallback, and the outcome; `isVisitChat` extended to the new fields (eligibility-assistant-SPEC-41/44/54 / eligibility-assistant-D-14, eligibility-assistant-D-25); `frontend/app/lib/types.ts` — `Citation`, the mode/outcome unions, `VisitChatResponse` extended
- `frontend/app/assistant/page.test.tsx` — `renders citations, mode and reason`, `emergency flag sent`, `question type, payer, product and state menus closed` (eligibility-assistant-SPEC-41/44/54); `frontend/app/assistant/turn.contract.test.ts` — NEW (eligibility-assistant-D-45)
- `tests/a1_rig.py` — NEW shared rig: scripted `client.messages.create` fake, payer fake, selection-building helpers, the harness loader keyed on `EVAL-n` (eligibility-assistant-D-40)
- `tests/test_ai_visit_chat.py`, `tests/test_visit_chat_phi.py` — re-seamed onto the rig, no retained test deleted or renamed; `test_every_verdict_line_is_clinical_term_free` (`:727`) has its fixed four-status parametrise list extended to the six new `verdict_line` statuses (the `CATALOG`-keyed `test_catalog_copy_is_clinical_term_free` at `:721` needs no edit) (eligibility-assistant-D-40 amended)
- `tests/test_a1_agent_turn.py` — NEW: `test_model1_chooses_topic_app_binds_selections[EVAL-001]`, `test_model2_chooses_citations_and_action[EVAL-001]`, `test_framework_executes_turn` (the retriever runs inside the compiled graph's `tools` node, asserted from the graph's node set and the call stack — not from application code), `test_loop_bound` ×3 (`no_retrieval`, second tool call, run-limit exit), `test_budget_gate_each_call_spend_stop[EVAL-017]` ×2 sites (cap lowered below the fixture payload) (eligibility-assistant-SPEC-1/2/21/22/23)
- `tests/test_a1_deterministic_turn.py` — NEW: `test_no_model_output_reaches_reply` ×6 reasons, `test_zero_model_calls_emergency_cross_patient` ×2, `test_eval_016_024_emergency[EVAL-016] [EVAL-024]`, `test_emergency_ignores_eligibility_state`, `test_cross_patient_refused[EVAL-012]` (eligibility-assistant-SPEC-3/45/46/49)
- `tests/test_a1_citations.py` — NEW: `test_citation_four_fields[EVAL-001]`, `test_unretrieved_id_never_renders`, `test_reason_citations_fetched_by_id` (eligibility-assistant-SPEC-4/5/6)
- `tests/test_a1_prompt_boundary.py` — NEW: `test_egress_excludes_clerk_text_and_ids` (PHI planted in message, facts and a second id; every captured Bedrock body on both calls and every path scanned), `test_selections_closed_reach_prompt` (eligibility-assistant-SPEC-12/55)
- `tests/test_a1_validation.py` — NEW: `test_eval_014_subverted[EVAL-014-subverted]` (`FIX-NEG-PROMPT-INJECTION` text planted as a retrieved row; an out-of-vocabulary selection → `validation_reject` → fallback citing `DOC-SYN-NO-INVENTION`), `test_outcome_enum_closed`, `test_no_result_never_inactive[EVAL-003] [EVAL-015] [EVAL-018]`, `test_eval_008_no_guarantee[EVAL-008]`, `test_mode_explicit` ×6 (eligibility-assistant-SPEC-13/14/15/16/33)
- `tests/test_a1_turn_order.py` — NEW: `test_payer_never_model_triggered`, `test_call_sequence` (+ gate precedence ×2), `test_payer_at_most_once` (intents × reasons × sites), `test_fallback_per_reason[EVAL-018] [EVAL-017]` ×4 reasons, `test_unavailable[EVAL-015]` (eligibility-assistant-SPEC-19/50/51/52/53)
- `tests/test_a1_conflict.py` (1c half) — `test_eval_007_020_013_023[EVAL-007] [EVAL-020] [EVAL-013] [EVAL-023]` (eligibility-assistant-SPEC-42)
- `tests/test_a1_harness.py` — NEW: the remaining Appendix turn cases, one parametrised test per id (`[EVAL-002] [EVAL-004] [EVAL-005] [EVAL-006] [EVAL-009] [EVAL-021] [EVAL-022] [EVAL-025] [EVAL-026] [EVAL-027] [EVAL-028] [EVAL-032]`), each asserting outcome, action ids, mode/reason, and the `DOC-*` citations the Appendix assigns (eligibility-assistant-SPEC-37 / eligibility-assistant-D-12, eligibility-assistant-D-31)
- `tests/test_a1_live.py` — NEW, `skipif(not A1_LIVE_BEDROCK)`: `test_eval_014_resists[EVAL-014-resists]` (eligibility-assistant-SPEC-17 / eligibility-assistant-D-24, eligibility-assistant-D-43); the SPEC-32 live-run test joins in PR-2
- `tests/test_a1_turn_contract.py` — NEW (eligibility-assistant-D-45)
- `tests/test_visit_memory.py` — `test_a1_provenance_fields_no_text` (round-trips `last_citations`, rejects a `text` key, TTL unchanged) (eligibility-assistant-SPEC-20)
- `tests/test_gateway_authz.py` — `test_visit_chat_roles[EVAL-010] [EVAL-011]` (403 before any fan-out), `test_ai_use_grants[EVAL-029] [EVAL-030]` (`config/roles.yaml` `ai.use` grants == exactly `front_desk`, `admin`, `staff`; `admin` reaches the proxy) — tests only, `EXPECTED_ROUTE_CAPABILITIES` unchanged (eligibility-assistant-SPEC-47/48 / eligibility-assistant-D-17, eligibility-assistant-D-30)
- `tests/test_gateway_ai_proxy.py` — `test_a1_correlation_header_forwarded` (eligibility-assistant-SPEC-26)
- `tests/test_ai_visit_chat.py` — `test_a1_llm_egress_covers_both_calls` ×3 (stop at model₁ → `False`; bearer fail-closed → `False`; stop at model₂ → `True`) (eligibility-assistant-SPEC-56); the gateway refund test in `tests/test_gateway_ai_chat_controls.py` is unchanged
- `.env.example` — `A1_RETRIEVAL_MAX_ROWS=12`, `A1_MODEL_FIXTURE=false` under the ai-assistant block (template only, no secret; this session could not open the file — permission-denied — and verified its `LANGSMITH_*` block through `git grep` only; the impl session reads it before editing)
- `docs/onboarding-seam-map.md` — the "Gateway fan-out" example cell "(pending — ai-assistant not yet routed)" is stale since W3 and is the seam 1c uses; corrected to name `/ai/visit-chat` (registry upkeep, fix-wrong-claims rule)
- `docs/debt-log.md` — D13 row: the clause "`tests/test_visit_chat_phi.py:239` asserts the prompt is byte-identical to the deterministic build" is replaced by the eligibility-assistant mechanism (two closed-vocabulary payloads plus vendored synthetic corpus text, pinned by `tests/test_a1_prompt_boundary.py`); the row stays OPEN (eligibility-assistant-SPEC-12; falsified-claims sweep)
- `docs/phi-logging-policy.md` — register row "Redis holds `facts.insurance_id` at rest" amended to name `last_citations` (document ids + version, no text) under the same ACCEPTED controls (eligibility-assistant-SPEC-20; falsified-claims sweep)
- `CLAUDE.md` §6 — baseline line updated to the post-1c count, noting the `skipped` term (eligibility-assistant-SPEC-40 / eligibility-assistant-D-43); §4 "the model is consulted last" is not stated in CLAUDE.md, no other clause falsified by 1c

Gate interactions (existing steps that already enforce part of a new check):
- `tests/test_gateway_proxy_error_contract.py` route-set closure: no new gateway route, so the closure case is unchanged; `_post_checked` stays the fan-out helper (its header parameter carries the new headers).
- `tests/test_gateway_authz.py::test_route_capability_wiring_is_pinned` and `test_roles_yaml_matches_enforced_map`: unchanged and still the authz proof; SPEC-47/48 add cases, not wiring.
- CI `services` import smoke (`python -c "import app"`, keyless, offline): the LangChain import and the corpus loader must be import-safe — the corpus loads lazily (eligibility-assistant-D-35) and `create_agent` compiles offline (E-1 dry leg).
- CI `secret-scan` (gitleaks, `--no-git`, tracked tree): the vendored corpus and fixtures are scanned; verification 10 runs it before push.
- The clinical-vocabulary screen over `visit_templates` copy is `tests/test_ai_visit_chat.py::test_catalog_copy_is_clinical_term_free` (`:721`, parametrised on `CATALOG` keys, borrowing `_CLINICAL_TERMS` from `tests/test_ai_intake_instructions.py` — which itself never imports `visit_templates`): the eleven new catalog strings are screened by construction; the sibling `test_every_verdict_line_is_clinical_term_free` (`:727`) is parametrised on a fixed four-status list, so the six new `verdict_line` sentences are screened only once that list is extended (1c change row, eligibility-assistant-D-40).

Verification (runnable, expected output stated; numbering carried from the item plan):
5. `cd services/ai-assistant && env -i PATH="$PATH" ../../.venv/bin/python -c "import app, policy_index, policy_tool; print('ok')"` → `ok` (keyless, offline import — CI `services` smoke shape; 1a, repeat 1b/1c with `agent_binding`, `agent_turn`, `outcome`)
10. `docker run --rm -v "$PWD:/repo" zricethezav/gitleaks:v8.18.4 detect --source=/repo --config=/repo/.gitleaks.toml --no-git --redact --exit-code 1` → exit 0 over the vendored corpus and fixtures (CI `secret-scan` shape; 1a, 1c)
14. `.venv/bin/python -m pytest tests/test_a1_turn_order.py tests/test_a1_agent_turn.py -q` → passed; break-then-revert: drop the `PayerGate` once-guard so the fallback path calls `check_coverage` after the hook did → `test_payer_at_most_once` red; revert → green (eligibility-assistant-SPEC-51; 1c). Break: lower `LLM_MAX_COST_PER_REQUEST_USD` to `0.000001` in the rig → `test_budget_gate_each_call_spend_stop[EVAL-017]` shows `spend_stop` at model₁ with zero egress and the payer verdict persisted-not-rendered (eligibility-assistant-SPEC-23/52; 1c)
15. `.venv/bin/python -m pytest tests/test_gateway_authz.py tests/test_gateway_ai_proxy.py tests/test_gateway_ai_chat_controls.py -q` → passed with `EVAL-010`, `EVAL-011`, `EVAL-029`, `EVAL-030` ids present; break-then-revert (working tree only, never committed — the enforced twin is `services/gateway/authz.py`, `roles.yaml` is declarative, `authz.py:4-8`): add `"ai.use"` to the `clinician` frozenset in `authz.py` `ROLE_CAPABILITIES` (`:49-56`) → `test_visit_chat_roles[EVAL-010]` red (clinician now reaches the proxy) **and** `test_roles_yaml_matches_enforced_map` red (twins diverge); `git checkout -- services/gateway/authz.py` → green. Break-then-revert for the yaml side: grant `ai.use` to `clinician` in `config/roles.yaml` only → parity test red, `[EVAL-010]` still green — which is the point: yaml alone gates nothing; revert → green (eligibility-assistant-SPEC-47/48; 1c). Break-then-revert: drop `X-Correlation-Id` from the headers dict at `services/gateway/app.py:1155` → `test_a1_correlation_header_forwarded` red; revert → green (eligibility-assistant-SPEC-26; 1c)
16. `cd frontend && npm install && npm run typecheck && npm run lint && npm test -- --run && npm run build` → all green, new tests listed (`gateway.test.ts`, `page.test.tsx` eligibility-assistant cases, `turn.contract.test.ts`) (eligibility-assistant-SPEC-25/41/44/54; 1c)
17. `.venv/bin/python -m pytest tests/test_a1_turn_contract.py -q` and `cd frontend && npx vitest run app/assistant/turn.contract.test.ts` → both green; break-then-revert: add a fifth product value to `schemas.py` only → the python contract test red; revert → green (eligibility-assistant-D-45; 1c)

- Cross-reference: verifications 8, 9, 11, 12, 13, 19, 20, 21 span this ticket and `trace`; full text in `trace.md`.
- Cross-reference: the CI `workflow-doc-cap`, `make eval`, `test_compose_topology.py` and baseline gate interactions are item-wide; full text in `trace.md`.

## Landmines

- **PHI handling (§1) — entered.** Three new PHI-adjacent code paths: (1) the model payloads (eligibility-assistant-SPEC-12, ⚠ REQ-3‴) — two Bedrock calls per agent-path turn carrying closed vocabulary plus vendored synthetic corpus text, never the clerk's message, the member id, or any identifier, proven by `tests/test_a1_prompt_boundary.py::test_egress_excludes_clerk_text_and_ids` on both calls and every path; (2) the visit-memory shape (eligibility-assistant-SPEC-20, ⚠ REQ-5′) — `VisitFacts.last_citations` carries document ids and version only, `extra="forbid"` kept, TTL unchanged, under the phi-logging-policy rule-6 controls the register's ACCEPTED 2026-07-26 row already grants, with that row amended in 1c; (3) the trace payloads (eligibility-assistant-SPEC-28/29/30/31, ⚠ REQ-8‴) — one LangSmith tree per turn from two processes (gateway root + assistant subtree, eligibility-assistant-D-41) carrying only the REQ-8‴ allowlist, the ADR 0006 two-layer hide on both clients, never streamed, negative-scanned on all eight paths, and a new register row in PR-2. Approval of record for entering the zone: **the owner's §1 approval of code entry on SPEC-7–10, 12, 20, 28–31 as designed in this block, given 2026-08-25 in session at plan review r1 (gate r1 f1), unconditional**, standing on the owner's freeze of the ⚠ rows REQ-3‴ / 5′ / 8‴ and SPEC-7–10, 12, 18–20, 28–31, 47–49 on 2026-08-24 (contract `## Spec` Status), the D13 gate decision eligibility-assistant-D-14 (nothing free-text reaches the model), and eligibility-assistant-D-41 (trace shape, confirmed by the owner 2026-08-25). This is the record the `.claude/skills/implementation/` entry checklist requires before code on these rows; a change to the mechanisms named here re-opens it. Deliberate defects in reach are preserved: D13 stays OPEN (synthetic corpus, closed prompt, no BAA — ADR 0011 constraint 4 unchanged, the payer answer is still the only coverage source); the `README.md` HIPAA overstatement (TODO-12) is untouched; the D1/D3 log-and-column defects are not touched by any eligibility-assistant file. Accepted residuals: the gateway becomes a second metadata-only vendor-egress process (LangSmith, no PHI by the same two-layer hide), corrected in CLAUDE.md §2 / D13 in PR-2; the LangSmith key is not provisioned (eligibility-assistant-D-8, needed by 2026-08-27) — SPEC-27–29 are proven keyless against a capturing fake, the live tree is SPEC-32's gate only.
- **Auth / sessions (§1) — not touched.** No authz line moves: `require_capability("ai.use")` on `/ai/visit-chat`, `services/gateway/authz.py`, `config/roles.yaml`, `security.py` and `EXPECTED_ROUTE_CAPABILITIES` are unchanged; eligibility-assistant-SPEC-47/48 add gateway-suite *tests* pinning today's behaviour as EVAL-010/011/029/030 (eligibility-assistant-D-30). `staff` keeps `ai.use` — the eligibility-assistant-D-17 known limitation, named in the runbook; the never-expiring session (D-row "Session token in browser localStorage", `security.py:275-279`) is untouched. The gateway body/header additions in `proxy_visit_chat` are payload shape, not authentication.
- **IDOR on chart reads (§1, D11) — not touched.** The cross-patient refusal (eligibility-assistant-SPEC-49, eligibility-assistant-D-11, eligibility-assistant-D-44) is detection on the assistant path; the session↔patient binding is not introduced anywhere, no chart route is edited, the D11 xfail stays visible. Residual: a second patient named in prose with no id and no closed phrase is not detected (eligibility-assistant-D-44) — filed as a TODO at landing.
- **Inline eligibility call (§1) — values not touched.** `services/ai-assistant/eligibility_client.py` and `services/eligibility-service/` are not edited; timeout, retry and breaker values stay pinned by `tests/test_eligibility_budget_alignment.py` (eligibility-assistant-SPEC-18). What moves is the *call site*: the payer call runs inside the agent step's `before_model` hook or on the fallback path, under one once-guard, still on the deterministic intent only (eligibility-assistant-SPEC-19/51, eligibility-assistant-D-18, eligibility-assistant-D-26). Residual: the clerk's payer menu is not cross-checked against the member-id prefix catalog (eligibility-assistant-D-36) — a mismatch yields documents for the selected payer beside the payer's own verdict for the id; the verdict line is still the payer's; filed as a TODO at landing.
- Cross-reference: the Secrets (§1) entry names `.env.example` (a 1c change row) as well as the PR-2 live-run record; it is recorded whole in `trace.md`, together with Migrations/schema (§1), the structural-exclusion statement and the partial-coverage residual-honesty bullet.

## Findings

Gate rounds start at 1 for this ticket; carried findings: f1 (SPEC-49 / eligibility-assistant-D-44 — the single-contradicting-id cross-patient form; **decided 2026-08-26, eligibility-assistant-D-50: refuse** — stage 3 folds rule (a) as amended into the change list before the ticket gate), f6 (eligibility-assistant-D-40 — falsified in-code markers in `tests/test_ai_visit_chat.py`, close by sweep over `tests/` + `frontend/`).
