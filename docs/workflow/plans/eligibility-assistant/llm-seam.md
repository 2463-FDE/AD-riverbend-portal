# eligibility-assistant / llm-seam — ticket plan file (deleted at its merge; contract: docs/workflow/eligibility-assistant.md)

Status: plan GATED 2026-08-27 · delivery DRAFT 2026-08-30 (gate r8 clean 2026-08-27, dry round; gate r7 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated the one finding 2026-08-27 as accepted → stage 3, A, `tool_calls` asserted per field against the pinned `langchain-core==1.6.0`, no owner call open; gate r6 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated both findings 2026-08-27 as accepted → stage 3, both A, `adr/0019-*.md` change row added here and in `turn`, eligibility-assistant-D-42 note, no owner call open; gate r5 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated all three findings 2026-08-27 as accepted → stage 3, all A, verification 7i, no owner call open; gate r4 dispositioned 2026-08-27 under the round-3 rule — owner adjudicated both findings 2026-08-27 as accepted → stage 3, both A, no owner call open; gate r3 dispositioned 2026-08-27 under the round-3 rule — all six A, no owner call pending on this ticket; gate r2 dispositioned 2026-08-27; re-sliced 2026-08-25 from the item plan; gate rounds 1–2 on the monolithic plan are in plans/eligibility-assistant.md)
Scope: SPEC rows eligibility-assistant-SPEC-24, -30 (eligibility-assistant-D-34 as read through eligibility-assistant-D-55, PR-1b — the egress seam and its never-stream guarantee; lands dark, not wired to the request path)
Depends on: corpus (the LangChain v1 pins — `langchain` / `langchain-core` / `langgraph` in `services/ai-assistant/requirements.txt` and the root `requirements-dev.txt`; eligibility-assistant-D-59)

## Plan

Status: GATED 2026-08-27

Changes (file level):

- `services/ai-assistant/llm_client.py` — `_adapt` becomes the E-4 superset: content blocks carry `id` / `name` / `input` on `tool_use` and the response carries `stop_reason`; `_result_from_response` untouched in code; its comment at `:576-577` ("Both complete() and complete_structured() flow through here, so the guard lives here once") is amended to name the binding's twin guard in `agent_binding.py` and the parity test that pins the two equal (**gate r4 f2** — the comment would otherwise be falsified by the new caller); the receive half is pinned by `tests/test_llm_client.py::test_a1_binding_round_trip_through_seam` (**gate r2 f1**) (eligibility-assistant-SPEC-24 / eligibility-assistant-D-9, E-4 delta i)
- `services/ai-assistant/agent_binding.py` — NEW: `SeamChatModel(BaseChatModel)` — `_generate` → `llm_client._call`; `bind_tools` → Anthropic `tools` in `extra_body`; message conversion — `HumanMessage` → `user`, `AIMessage` (text plus `tool_calls` → `tool_use` blocks) → `assistant`, `ToolMessage` → a `tool_result` block carrying `tool_use_id` inside a `user` turn, **`SystemMessage` → `_call`'s `system` argument** (**gate r4 f1**: exactly one `SystemMessage`, and only as the leading message, maps to `system` — the body key Bedrock carries at `body["system"]` (`llm_client.py:244`) and `max_input_tokens` counts on its own line (`:368`), never a `user` turn; no `SystemMessage` → `system=None`; a `SystemMessage` anywhere else, or more than one, raises `ValueError` before `_call` with zero egress) — with the role-alternation merge (consecutive same-role messages fold into one turn); **`_call`'s other arguments (gate r4 f1):** `max_tokens` is always `settings.llm_max_output_tokens` (`config.py:65`, the same source `complete()` uses at `llm_client.py:650`), never a framework kwarg; `_generate(messages, stop=None, run_manager=None, **kwargs)` (`langchain_core` 1.6.0 `language_models/chat_models.py:2198-2204`, wheel read 2026-08-27) refuses a non-`None` `stop` with `ValueError` before `_call`, and of `**kwargs` reads only `tools` (what `bind_tools` binds); **post-egress twin guard (gate r4 f2):** after `_call` returns, `_generate` applies the same two fail-closed checks `_result_from_response` applies for `complete()` (`llm_client.py:578-595`) — the response must carry at least one content block of type `text` **or** `tool_use` (the agent path's valid tool-only turn is why this cannot simply reuse `_result_from_response`, whose text-required guard `test_non_text_content_block_raises_through_adapter` pins for `complete()`), and `usage.input_tokens` / `output_tokens` must be explicit integers — each failure `LLMResponseError` carrying the request id only — and emits the same metadata-only `llm call model=… in_tokens=… out_tokens=… cost=… latency=… request_id=…` line (`:606-614` format, `estimate_cost`, `time.monotonic()` from before `_call`), so ADR 0004's typed-failure and telemetry controls hold on the agent path (`adr/0004:38-40`, `:57`); **the `AIMessage` it returns — the receive half, every field of the adapted response named (gate r5 f1):** `content` = the `text` blocks' `text` in response order, joined (`""` on a tool-only turn, the agent path's valid answer) — this string is what `turn` parses into `AgentDecision` (eligibility-assistant-D-37, "model₂'s text"), so a binding that drops it fails first here, not in `turn`; `tool_calls` built from the `tool_use` blocks' `id` / `name` / `input` — handed to `AIMessage(tool_calls=[…])` as those three keys and **returned as four** (**gate r7 f1**): `AIMessage`'s `mode="before"` validator `_backwards_compat_tool_calls` (`langchain_core/messages/ai.py:307-309`, normalisation `:331-338`) rebuilds every entry through `tool_call()` (`messages/tool.py:242-258`), which returns `ToolCall(name=…, args=…, id=…, type="tool_call")` — `ToolCall` is a `TypedDict` (`tool.py:206`) whose `type` is `NotRequired[Literal["tool_call"]]` (`:238`) — so the package supplies the `type: "tool_call"` key and the block's `input` lands under `args`; the field is the package's, not the binding's, and every `tool_calls` assertion in this ticket is therefore **per field**, never one equality against a literal dict (`langchain-core==1.6.0` wheel read read-only 2026-08-27); `response_metadata["stop_reason"]` from the response's `stop_reason`; `usage` is consumed by the twin guard and the `llm call` line only and is **not** placed on the message (no `usage_metadata` — spend is `_call`'s `LLMBudgetExceeded` before egress, never a post-hoc count); `id` / `model` reach the log line only. Nothing else of the adapted response is carried; `_stream` deliberately unimplemented (raises) so the binding cannot stream (eligibility-assistant-SPEC-24/30 / eligibility-assistant-D-37, E-4 delta iii)
- `tests/test_llm_client.py` — **binding rig (gate r3 f1):** the file loads `llm_client` as `llm_mod` and restores `config` / `logging_config` afterwards (`tests/test_llm_client.py:34-43`), leaving nothing under the bare name `llm_client`, while `agent_binding.py` reaches the seam by the service idiom `import llm_client` (`app.py:33` shape). So the binding is loaded through the house pin (`tests/test_ai_visit_chat.py:51-73`; CLAUDE.md §6): `sys.modules["config"]` / `["logging_config"]` / `["llm_client"] = llm_mod` set, then `agent_binding = load_module("services/ai-assistant/agent_binding.py", "ai_agent_binding")`, then the saved names restored — one module-level rig beside the existing one. **First assertion of every binding test: `agent_binding.llm_client is llm_mod`**, so an unpinned second copy reddens before any `invoke` (verification 7f) and `_stub_runtime_returning`'s patch of `llm_mod.client` (`:659`, `:669`) is the runtime the binding reaches. `test_a1_binding_uses_seam_fail_closed` (runtime stubbed with `_stub_runtime_returning` so "zero egress" is `calls == []` (gate r3 f5); bearer unset → `LLMConfigError(egressed=False)` through the binding with zero egress; budget and char caps run on the tool payload; typed errors propagate) plus a characterization of `_adapt` on a text-only body, plus `test_a1_binding_round_trip_through_seam` (**gate r2 f1 and its class sweep** — the send and receive halves of SPEC-24 against a stubbed runtime: bearer set by the file's autouse fixture (`tests/test_llm_client.py:47`), runtime stubbed with the file's `_stub_runtime_returning` on a scripted body `{"content": [{"type": "text", "text": "Looking that up."}, {"type": "tool_use", "id": "toolu_1", "name": "policy_lookup", "input": {"topic": "eligibility-verification"}}], "stop_reason": "tool_use", "usage": {…}}`; `SeamChatModel().bind_tools([one StructuredTool]).invoke([SystemMessage, HumanMessage, HumanMessage])` (**gate r4 f1** — the system prompt is in the round trip), then a second `invoke` with the returned `AIMessage` and a `ToolMessage` appended. Asserts on the captured `invoke_model` `body` JSON (`llm_client.py:249-254`): `body["system"]` equals the `SystemMessage` text and that text appears in no `messages` block, `body["max_tokens"] == settings.llm_max_output_tokens`, `tools[0]["name"]` and `input_schema` present (send: `bind_tools` → `extra_body`), `messages` roles strictly alternate with the two human messages folded into one `user` turn, the second call's last `user` turn carries a `tool_result` block with `tool_use_id == "toolu_1"`; and on the return: `AIMessage.content == "Looking that up."` (**gate r5 f1** — the text block survives the binding), `len(AIMessage.tool_calls) == 1` with `tool_calls[0]["id"] == "toolu_1"`, `["name"] == "policy_lookup"` and `["args"] == {"topic": "eligibility-verification"}` asserted **individually** (**gate r7 f1** — the package rebuilds the entry with its own `type: "tool_call"` key, so a three-key equality is false on a *correct* binding), and `response_metadata["stop_reason"] == "tool_use"`; and `llm_client._adapt` called directly on the same body → the `tool_use` block exposes `.id` / `.name` / `.input` and the response `.stop_reason` (receive)), plus `test_a1_binding_system_message_rules` (**gate r4 f1** — `[HumanMessage, SystemMessage]` and `[SystemMessage, SystemMessage, HumanMessage]` each raise `ValueError` with `calls == []`; `invoke(..., stop=["x"])` raises `ValueError` with `calls == []`; `[HumanMessage]` alone → the captured body has no `system` key), plus `test_a1_binding_guard_parity` (**gate r4 f2** — the binding is fed, through `_stub_runtime_returning`, the same malformed bodies the file feeds `complete()`: `{"usage": {…}}` with no content (`:673-680` shape) → `LLMResponseError`; a text block with no `usage` (`:698-703` shape) → `LLMResponseError`; then a **valid tool-only** body (`tool_use` with `id` / `name` / `input`, `usage` present) → returns an `AIMessage` with one `tool_call` and `content == ""` (**gate r5 f1** — the tool-only turn's empty text is the mapping, not a dropped block) **and**, under `caplog.at_level(logging.INFO)` (the `test_prompt_never_reaches_logs` idiom, `:788-797`), exactly one record whose `getMessage()` matches the **six-field pattern** `^llm call model=\S+ in_tokens=\d+ out_tokens=\d+ cost=\$\d+\.\d{4} latency=\d+\.\d{2}s request_id=\S+$` — `llm_client.py:607`'s format rendered — with `in_tokens=` / `out_tokens=` equal to the body's usage, `cost=` equal to `f"${llm_mod.estimate_cost(in_tokens, out_tokens):.4f}"` (`llm_client.py:323`) and `request_id=` equal to the scripted body's `id` (`"msg_parity"`, carried for this purpose; `_result_from_response` reads it at `:572`); then, under the same `caplog`, `llm_mod.complete("x")` against `_stub_runtime_returning` on a text body → its one `llm call` record matches the **same pattern** — the parity half, one pattern over both callers (**gate r6 f2**: a binding line that drops `cost=` / `latency=` / `request_id=` fails the pattern while `complete()`'s still matches, so the failure attributes to the binding); and no record containing the human or system text) (eligibility-assistant-SPEC-24)
- `tests/test_a1_trace.py` — NEW here with one test, `test_model_call_not_streamed` (no `invoke_model_with_response_stream` reference in the service; the binding's `_stream` raises), loading the binding through the same `sys.modules` pin rig as `tests/test_llm_client.py` (**gate r3 f1** — its own `llm_mod` pinned under `llm_client` before `load_module` of `agent_binding`, identity asserted first); the `trace` ticket extends the file (eligibility-assistant-SPEC-30 / eligibility-assistant-D-55)
- `services/ai-assistant/app.py` — a **dark `import agent_binding`** (no call site, no route; `app.py:30-36` today imports `config`, `logging_config`, `eligibility_client`, `llm_client`, `templates`, `visit_templates`, `schemas`) so CI's `services` smoke — `pip install -r services/ai-assistant/requirements.txt` then `python -c "import app"` (`.github/workflows/ci.yml:73-76`) — actually imports the binding against the **service** requirements; without it the job proves nothing about this ticket (**gate r3 f2**; `corpus` carries the same row for `policy_tool`) (eligibility-assistant-SPEC-24)
- `tests/README.md` — "AI paths" bullet (`tests/README.md:32`) gains `test_a1_trace.py` (created here; `trace` extends it) (**gate r3 f3**; registry upkeep — eligibility-assistant-D-60: a ticket carries its own registry entry)
- `adr/0019-*.md` — seam-binding paragraph appended (eligibility-assistant-D-42 pattern; **gate r6 f1**): `SeamChatModel` on the `invoke_model` seam as the framework's only egress through `_call` (the eligibility-assistant-D-9 shape-(ii) reason), `SystemMessage` → `_call`'s `system`, the post-egress twin guard and its `llm call` line as the ADR 0004 `:57` justification (the two post-egress controls applied by a second caller of `_call`, pinned equal by `test_a1_binding_guard_parity`), and the raising `_stream` (SPEC-30) — the durable record of what this file's Landmines block says, which is deleted at merge; the ADR itself is created by `corpus` (`corpus.md`'s `adr/0019-*.md` row) and this ticket lands after it (`Depends on:`) (eligibility-assistant-SPEC-24/30 / eligibility-assistant-D-42)
- `CLAUDE.md` §6 — baseline line (`:141`, `1317 passed, 19 deselected, 1 xfailed`) updated to the post-ticket count: this ticket's six added tests (`tests/test_llm_client.py::test_a1_binding_uses_seam_fail_closed`, `::test_a1_binding_round_trip_through_seam`, `::test_a1_binding_system_message_rules`, `::test_a1_binding_guard_parity`, the `_adapt` text-only characterization, `tests/test_a1_trace.py::test_model_call_not_streamed`; four → six at gate r4 f1 / f2) are a **deliberately reported baseline move**, reported in this ticket's Delivery; deselected and xfailed do not move and no `skipped` term appears yet — the live tests land in `turn` (falsified-claims sweep — this ticket's additions falsify the stated count; eligibility-assistant-SPEC-40 / eligibility-assistant-D-43)

Verification (runnable, expected output stated; numbering carried from the item plan):
5. `cd services/ai-assistant && env -i PATH="$PATH" ../../.venv/bin/python -c "import app, policy_index, policy_tool, agent_binding; print('ok')"` → `ok` (keyless, offline import — the CI `services` smoke shape; this ticket's repeat of it adds `agent_binding`, so the `langchain_core` import `corpus` pinned is proven import-safe keyless and offline before any wiring) (eligibility-assistant-SPEC-24; 1b)
6. `.venv/bin/python -m pytest tests/test_llm_client.py tests/test_a1_trace.py -q -k "a1 or adapt or streamed"` → passed (eligibility-assistant-SPEC-24/30; 1b)
7. break-then-revert (**restated, gate r3 f5** — the break must not be able to egress): in `agent_binding._generate` call `llm_client.client.messages.create(...)` directly, skipping `_call` → `test_a1_binding_uses_seam_fail_closed` red (the stubbed runtime records a call — `calls != []` — and no `LLMConfigError` is raised: the bearer guard `_require_bearer_token` (`llm_client.py:428-455`) was bypassed); the runtime is `_stub_runtime_returning`'s, so nothing leaves the process; revert → green (eligibility-assistant-SPEC-24; 1b)
7f. break-then-revert (binding rig, gate r3 f1): drop the `sys.modules["llm_client"] = llm_mod` pin from the rig in `tests/test_llm_client.py` → every binding test red on its first assertion `agent_binding.llm_client is llm_mod` (a second `llm_client` module was executed; no call is made — the identity check precedes `invoke`, and module import constructs a `boto3` client without a network call); restore the pin → green (eligibility-assistant-SPEC-24; 1b)
7b. break-then-revert (SPEC-30 negative check, `docs/landmines.md` §3; gate r1 f3): implement `SeamChatModel._stream` to yield one chunk instead of raising → `tests/test_a1_trace.py::test_model_call_not_streamed` red (the binding can stream); `git checkout -- services/ai-assistant/agent_binding.py` → green. Second break, the reference half of the same test: add an `invoke_model_with_response_stream` call site in `services/ai-assistant/agent_binding.py` → the same test red (a streaming API reference in the service); revert → green (eligibility-assistant-SPEC-30 / eligibility-assistant-D-55 — SPEC-30 and its test are this ticket's; 1b)
7c. break-then-revert (SPEC-24 receive half, gate r2 f1): in `llm_client._adapt` drop `stop_reason` from the returned namespace → `tests/test_llm_client.py::test_a1_binding_round_trip_through_seam` red (`response_metadata["stop_reason"]` missing); `git checkout -- services/ai-assistant/llm_client.py` → green. Second break (**restated, gate r7 f1** — the old wording, "`tool_calls` empty", was indistinguishable from the always-red state the pre-r7 three-key equality produced on a correct binding): keep `stop_reason` but restore the `tool_use` block to today's `type`/`text`-only `_adapt` shape (`llm_client.py:204-207`), so the adapted block carries no `.id` / `.name` / `.input` → the same test red **on its receive-half tool-call assertions only** — `_generate` cannot read the three fields off the block (an `AttributeError` inside the call, or `tool_calls == []` and an `IndexError` on `tool_calls[0]` if the binding skips a block it cannot read) — while the send-half assertions on the captured body (`body["system"]`, `body["max_tokens"]`, `tools[0]["name"]`, the alternating roles, the second call's `tool_result`) and `AIMessage.content == "Looking that up."` all still pass, so the red attributes to the receive half and the test is **green before the break**; revert → green (eligibility-assistant-SPEC-24; 1b)
7d. break-then-revert (SPEC-24 send half — tool definitions, gate r2 class sweep): in `agent_binding.py` make `bind_tools` return the model without writing `extra_body["tools"]` → the same test red (no `tools` in the captured body); revert → green (eligibility-assistant-SPEC-24; 1b)
7e. break-then-revert (SPEC-24 send half — tool results and role merge, gate r2 class sweep): in `agent_binding.py` drop the role-alternation merge → the same test red (two consecutive `user` turns in the body); revert → green. Second break: convert `ToolMessage` to a text block instead of a `tool_result` block → the same test red (no `tool_result` with `tool_use_id`); revert → green (eligibility-assistant-SPEC-24; 1b)
7g. break-then-revert (SPEC-24 send half — system prompt, gate r4 f1): in `agent_binding.py` convert the leading `SystemMessage` into a `user` turn instead of `_call`'s `system` argument → `test_a1_binding_round_trip_through_seam` red (`body["system"]` absent and the system text found in a `messages` block); revert → green. Second break: accept a `SystemMessage` at any position → `test_a1_binding_system_message_rules` red (`[HumanMessage, SystemMessage]` reaches the stub — `calls != []` — instead of raising); revert → green (eligibility-assistant-SPEC-24; 1b)
7h. break-then-revert (post-egress twin guard, gate r4 f2): delete the guard and the log call from `agent_binding._generate` → `test_a1_binding_guard_parity` red on the no-content body (an `AIMessage` with empty content is returned, no `LLMResponseError`) and on the log assertion (no `llm call model=` record); restore → green. Second break (**gate r6 f2**): keep the guard and emit the line as `llm call model=%s in_tokens=%d out_tokens=%d` only → the same test red on the six-field pattern (the `complete()` record in the same test still matches, so the red is the binding's); revert → green. The stubbed runtime records the call either way, so nothing leaves the process (eligibility-assistant-SPEC-24; 1b)
7i. break-then-revert (SPEC-24 receive half — text, gate r5 f1): in `agent_binding._generate` build the `AIMessage` with `content=""` regardless of the response's `text` blocks (the twin guard still passes — a `tool_use` block is present) → `test_a1_binding_round_trip_through_seam` red (`content` is `""`, not `"Looking that up."`); revert → green. The stubbed runtime records the call either way, so nothing leaves the process (eligibility-assistant-SPEC-24; 1b)
8. `.venv/bin/python -m pytest -m "not integration" --collect-only -q | grep -o 'EVAL-0[0-9][0-9]\(-[a-z]*\)*' | sort -u | wc -l` → `2` after this ticket — unchanged from `corpus`'s Delivery value (`EVAL-031`, `EVAL-023`), because none of this ticket's six tests is `[EVAL-n]`-parametrised (four → six at gate r4 f1 / f2; this site corrected at gate r5 f3). SPEC-37 is `turn`'s row (eligibility-assistant-D-55); the count is pinned here so it cannot move silently (eligibility-assistant-SPEC-37 as amended by eligibility-assistant-D-53; 1b)
9. `.venv/bin/python -m pytest -m "not integration" -q` → `<Baseline at branch: passed + 6> passed, 19 deselected, 1 xfailed`, the 6 being this ticket's five `tests/test_llm_client.py` cases and `tests/test_a1_trace.py::test_model_call_not_streamed` (four → six at gate r4 f1 / f2), reported deliberately in Delivery (CLAUDE.md §6); **no `skipped` term yet** — the live tests land in `turn` (eligibility-assistant-D-43). Deselected and xfailed never move. The claim-worthy run is `make test-docker` with the same numbers (eligibility-assistant-SPEC-40; 1b)
19. `git diff --stat main -- db/ config/ services/gateway/authz.py services/gateway/security.py services/eligibility-service/ services/ai-assistant/eligibility_client.py docker-compose.yml eval/` → empty (structural exclusion, every ticket; 1b)

Gate interactions (existing steps that already enforce part of a new check):

- CI `services` import smoke (`pip install -r services/ai-assistant/requirements.txt`, then `python -c "import app"` from the service dir — `.github/workflows/ci.yml:72-76`, keyless and offline): `corpus` puts `langchain_core` on that install; this ticket is the first to *import* it from service code (`agent_binding.py`'s `SeamChatModel(BaseChatModel)`), and the smoke proves only what `app` imports — so the proof exists **because** this ticket adds the dark `import agent_binding` to `app.py` (change row above; **gate r3 f2**, class shared with `corpus`'s first bullet, restated there for `policy_tool`). No new job. `import app` runs no FastAPI lifespan; since `corpus` gate r4 f4 it does load and sha-verify the corpus at import through `policy_tool` (that ticket's row and D-35 note 2), which is offline and adds nothing this ticket must attribute. Verification 5 is the same shape locally, with `agent_binding` named.
- CI `tests` job (`pip install -r requirements-dev.txt`, `.github/workflows/ci.yml:86`): this is the job that reddens on import if this ticket lands ahead of `corpus` — the `Depends on: corpus` edge (eligibility-assistant-D-59) is what keeps it green, and no requirements row is added here.
- SPEC-37's collect-count cmd (`turn`'s row, eligibility-assistant-D-53): this ticket moves it not at all — it adds no `[EVAL-n]` id — and verification 8 pins the value at `corpus`'s 2.

## Landmines

This block is verbatim for **this ticket's own diff** and no other's (eligibility-assistant-D-60): it names the §1 zones the `llm-seam` change list enters, and a zone another ticket enters is that ticket's block to write, not a citation from here.

- **PHI handling (§1) — entered.** Two new PHI-adjacent code paths, covering this ticket's one ⚠ row (eligibility-assistant-SPEC-30) and the seam its other row lands (SPEC-24):
  **(1) The model egress seam (SPEC-24).** `llm_client._adapt` becomes the E-4 superset — content blocks carry `id` / `name` / `input` on `tool_use`, and the response carries `stop_reason` — and `services/ai-assistant/agent_binding.py` adds `SeamChatModel(BaseChatModel)` whose **only** egress is `llm_client._call`, with tool definitions riding `extra_body["tools"]`. This is PHI-adjacent because it is the process's vendor egress: the four **pre-egress** controls — the byte-based char cap, the per-request budget gate, the bearer fail-closed guard and the typed errors — sit on `_call` (`llm_client.py:493-496`), so routing the framework's payloads through it keeps those controls on the bytes that actually leave (the D-9 shape-(ii) reason: a Converse client would have been gated on a payload that is not the egress payload). The system prompt rides `_call`'s `system` argument and is counted by the byte gate on its own line (`:368`), never smuggled into a `user` turn (gate r4 f1). The two **post-egress** controls ADR 0004 names — the malformed-200 fail-closed guard and the per-call metadata telemetry — live in `_result_from_response` (`:566-614`), which `_call` does not reach; the binding therefore carries a **twin guard** with **three** checks (content present, with `tool_use` admitted beside `text` because a tool-only turn is the agent path's valid answer; every field the receive half reads shape-checked — `id` / `name` str and `input` dict on a `tool_use` block, `text` str on a text block — errors carrying the field name and the request id only; explicit integer usage). The third check is the impl gate r1 f1 fix, adjudicated by the owner 2026-08-30 (option A, D-56 approval re-granted in session): it is what keeps this path's failure TYPED and payload-free, since without it a malformed `tool_use` block leaves the guard as a pydantic `ValidationError` carrying the offending value and emits the same metadata-only `llm call` line, pinned equal to `complete()`'s — one six-field pattern (`model`, `in_tokens`, `out_tokens`, `cost`, `latency`, `request_id`; the `llm_client.py:607` format rendered) asserted on the binding's record and on `complete()`'s in the same test — by `tests/test_llm_client.py::test_a1_binding_guard_parity` (gate r4 f2, r6 f2; verification 7h). **ADR 0004 `:57` ("must justify any bypass"):** this is not a bypass — every control the ADR names is applied on the agent path, four on `_call` and three by the twin; the one code comment that said the guard "lives here once" (`:576-577`) is amended to say so, and the justification is recorded durably in `adr/0019-*.md`'s seam-binding paragraph (change row above; gate r6 f1), so it survives this plan file's deletion at merge. The change is **additive** — `_result_from_response` is untouched in code, so `tests/test_llm_client.py::test_non_text_content_block_raises_through_adapter` still pins the text-required rejection for `complete()` — and the failure mode is fail-closed with zero egress when the bearer is unset (`tests/test_llm_client.py::test_a1_binding_uses_seam_fail_closed`, verification 7). The send and receive halves — tool definitions in the body, the model's `text` blocks onto `AIMessage.content` and `tool_use` blocks and `stop_reason` back, `tool_result` blocks in, roles alternating — are pinned against a stubbed runtime by `::test_a1_binding_round_trip_through_seam` (gate r2 f1, r5 f1, r7 f1; verifications 7c–7e, 7i), so an `_adapt` that ships unchanged reddens here and not first in `turn` — the `tool_calls` assertions being per field, because `AIMessage` rebuilds each entry with the package's own `type: "tool_call"` key (`langchain_core/messages/ai.py:331-338`, `messages/tool.py:242-258`).
  **(2) The never-stream guarantee (SPEC-30, ⚠ REQ-8‴).** `SeamChatModel._stream` is deliberately unimplemented and raises. The mechanism is **structural, not a policy line**: LangSmith's `hide_inputs` / `hide_outputs` redaction — the ADR 0006 two-layer hide this estate relies on to keep trace payloads metadata-only — is bypassed on streamed payloads (the research finding folded at req-review r1 #8), so a binding that *cannot* stream removes the bypass by construction rather than by asking the caller not to use it. `tests/test_a1_trace.py::test_model_call_not_streamed` pins both halves: no `invoke_model_with_response_stream` reference exists anywhere in `services/ai-assistant/`, and the binding's `_stream` raises.
  **This ticket lands dark — two senses, kept apart (gate r5 f2).** *In the diff:* the ticket adds **one new log site** on the estate's only vendor-egress path — the twin guard's `llm call model=… in_tokens=… out_tokens=… cost=… latency=… request_id=…` line in `agent_binding.py`, metadata-only and in the `:606-614` format, pinned by `test_a1_binding_guard_parity` to carry all six metadata fields and no human or system text — and no other logging code; a "Risk & landmines" section drafted from this block must say so. *At runtime:* the binding is not wired to the request path — `app.py` imports it (a dark import so CI's `services` smoke proves it against the service requirements) and no route calls it, so no Bedrock call is made and that line is never emitted by anything on this ticket; no new field is persisted to Redis or Postgres, and no clerk text, member id or identifier is read by any file in this change list.
  Approval of record for entering the zone: **eligibility-assistant-D-56** — the owner's §1 approval of code entry on eligibility-assistant-SPEC-7–10, 12, 20, **28–31** as designed in these blocks, given **2026-08-25** in session at plan review r1 (gate r1 f1), unconditional, and extended by the owner the same day to SPEC-60, 61, 63 (eligibility-assistant-D-48); registered as a decision 2026-08-26. It stands on the owner's freeze of the ⚠ row REQ-8‴ and SPEC-28–31 (contract `## Spec` Status, 2026-08-24) and on eligibility-assistant-D-41 (trace shape, confirmed by the owner 2026-08-25). SPEC-24 is not a ⚠ row and needs no approval of its own; it is named here because it is the seam the ⚠ row's guarantee is built on. A change to the mechanisms named here — the single `_call` egress, the `SystemMessage` → `system` mapping, the post-egress twin guard, the additive `_adapt` superset, the raising `_stream` — re-opens the approval.
  **Deliberate defects in reach are preserved.** D13 (no BAA on the vendor path) stays OPEN and is neither narrowed nor fixed: ADR 0011 constraint 4 is unchanged, the payer answer remains the only coverage source, and this ticket adds no egress that was not already gated. The `README.md` HIPAA overstatement (TODO-12) is untouched, human-gated. The D1 intake body-logging and D3 plaintext-PHI-column defects are not touched by any file in this change list. D11 (session↔patient binding) is not in reach — no chart route, no session code.
  **Accepted residuals on this ticket's rows:** the two in the residual-honesty bullet below; nothing else, because this ticket persists nothing, egresses nothing, and its one new log site (above) is metadata-only and unreached at runtime.
- **Secrets (§1) — not touched.** No `.env`, `.env.example` or any secret file is read or edited on this ticket, and no key, token or credential enters the tracked tree. The Bedrock bearer stays in the owner's gitignored `.env` and is *read* only through the existing `_require_bearer_token` guard, which this ticket exercises as a fail-closed test with the bearer **unset** — the test asserts zero egress, so no live credential is needed to run it. CI's `secret-scan` (gitleaks v8.18.4, `--no-git`, `.github/workflows/ci.yml:120-129`) covers what this ticket adds — two Python source files — with no growth in its surface.
- **Migrations / schema (§1) — not touched.** Nothing under `db/`: no `db/schema.sql` edit, no new `db/migrations/00N_*.sql`, no seed change. This ticket persists nothing, so there is no hand-sync obligation and `tests/integration/test_schema_upgrade_path.py` is untouched.
- **Domain services network-internal (§1, ADR 0016) — not touched.** No `ports:`, no new service, no `docker-compose.yml` edit; the binding lives inside the existing ai-assistant image and `tests/test_compose_topology.py`'s allowlist is unchanged.
- **Auth / sessions (§1) — not touched.** No line of `services/gateway/` moves: `authz.py`, `security.py`, `config/roles.yaml`, `EXPECTED_ROUTE_CAPABILITIES` and `require_capability` are outside this change list. The ticket adds no route and no request-path branch.
- **IDOR on chart reads (§1, D11) — not touched.** No chart or records route is edited, no session is bound to a patient, nothing here reads patient data; the D11 xfail stays visible.
- **Inline eligibility call (§1, ADR 0010) — not touched.** `services/ai-assistant/eligibility_client.py` and `services/eligibility-service/` are not edited; no timeout, retry or breaker value moves and `tests/test_eligibility_budget_alignment.py` is untouched.
- **ROI / disclosures, booking race, duplicate patients, HL7 mapping (§1) — not touched.** No file under `services/roi-service/`, `services/scheduling-service/`, `services/intake-service/` or `services/interop-service/` appears in this change list.
- **Intake registration (§1) — not touched.** No file under `services/intake-service/` is edited; `ConsentKind`, `proxy_intake` on `_post_checked` and `contracts/intake-registration.json` are outside this change list (`docs/landmines.md:87`).
- **Structural exclusion (this ticket).** Nothing changes under `db/`, `config/`, `services/gateway/authz.py`, `services/gateway/security.py`, `services/eligibility-service/`, `services/ai-assistant/eligibility_client.py`, `docker-compose.yml` or `eval/` — the spec Exclusions and the §1 zones stated not-touched above hold **structurally**, proven by the every-ticket structural-exclusion diff rather than by this sentence.
- **Partial coverage named honestly (residual honesty check).** Two residuals on this ticket's rows:
  (a) **SPEC-24's composition is measured live only on `langsmith 0.11.1`.** E-4's live 12/12 — the run that proved shape (ii), the `tool_use` round trip, the `_adapt` deltas and the fail-closed bearer path — ran on `langsmith==0.11.1`, while the repo pins `0.10.5`. E-5 measured that composition **offline** (pip resolves the four pins without conflict, `create_agent` compiles with the run-limit middleware nodes, the 2-model-call / 1-tool-call bound holds, `langchain_core.tracers.langchain` imports against 0.10.5 with `LANGSMITH_TRACING=false`) and explicitly did **not** measure the live Bedrock leg under 0.10.5 (gate r1 f12, E-5 "Not measured"; gate r2 recorded SPEC-24 as residual-named on exactly this). The live composition is first exercised at SPEC-17 / SPEC-32's opt-in runs in `turn` / `trace`, so a live-only incompatibility surfaces there and never in the default suite. Accepted: this ticket lands dark and makes no model call, so the residual cannot bite inside its own scope.
  (b) **SPEC-30 is proven for this service, not for the estate.** `test_model_call_not_streamed` scans `services/ai-assistant/` and the binding; the gateway's own LangSmith client is a second process that does not exist until `trace` lands, and its never-stream property is `trace`'s row to state and prove. Within this ticket the guarantee is exactly "the assistant's model binding cannot stream", which is what SPEC-30 says.
  Beyond those two, SPEC-24's fail-closed proof is over the **binding in isolation**: that the bearer guard, char cap and budget gate each run on a tool-carrying payload. The two-call agent composition those controls are spent by — `run_limit=2`, the once-guarded payer call, `spend_stop` at either site — is `turn`'s SPEC-21/22/23/56 and is not asserted here.

## Findings

Gate rounds start at 1 for this ticket; carried findings: none.

### Gate — round 1, 2026-08-26

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket + branch (`main`,
working tree), no characterization received. Run as wave 1 alongside the `corpus` gate
(two agents, independent reads; owner decision in session — the remaining four tickets gate
after `corpus` / `llm-seam` stamp so their rounds do not rot on upstream text). Plan returns
to stage 3; spec unchanged. Finding 2 sits on the same site as r1 f1 / r2 f5 on the
monolithic plan (Landmines block, dispositioned via eligibility-assistant-D-56) with a
different defect — compression instead of a missing approval record — so stage 3 closes it
as a class: sweep every ticket file's Landmines block for the verbatim rule and record the
sweep scope in the cell, not just this block's fix.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | eligibility-assistant-SPEC-24 · `agent_binding.py` change row · `Depends on: none` · eligibility-assistant-D-34 (1b "LangChain v1 pins") | Change-list closure / wrong fact: `SeamChatModel(BaseChatModel)` imports `langchain_core`, which nothing in this ticket supplies — the tree has no langchain pin today (`services/ai-assistant/requirements.txt`, `requirements-dev.txt` verified); the pins `langchain==1.3.16` / `langchain-core==1.6.0` / `langgraph==1.2.11` are a **`corpus`** change row, yet this ticket (and the item ticket table) declares `Depends on: none`. D-34 assigns "LangChain v1 pins" to 1b (this ticket); `corpus.md` lands them in 1a — the decision and the two ticket files disagree. Landed as written ahead of `corpus`, `tests/test_llm_client.py` and CI's `tests` job go red on import. State `Depends on: corpus` (pins) or carry the requirements rows here, and reconcile D-34's 1b clause. | A · 2026-08-27 — class closed by one reconciliation: **eligibility-assistant-D-59** supersedes eligibility-assistant-D-34's 1b "LangChain v1 pins" clause (dated note on D-34) — the pins stay in `corpus` (SPEC-9's `policy_tool` imports `langchain_core.tools`; that change row recited SPEC-21 → SPEC-9 and names the root `requirements-dev.txt` CI installs at `ci.yml:86`), and this ticket's line becomes `Depends on: corpus` (pins). **`Depends on:` sweep, all six ticket files:** corpus `none` · llm-seam `none` → `corpus` (fixed) · turn `corpus, llm-seam` · trace `turn` · lifecycle `corpus, turn` · retrieval-eval `corpus` — this was the only wrong line; the item plan's ticket-table `depends on` column carries the same fix and landing order corpus → llm-seam → retrieval-eval → turn → {lifecycle, trace} is unchanged |
| 2 | Landmines block · eligibility-assistant-SPEC-30 (⚠) · eligibility-assistant-SPEC-24 residual | Block is a summary plus citations, not the verbatim block: the PHI-handling entry SPEC-30 enters is "recorded verbatim in `corpus.md`, `turn.md` and `trace.md`" rather than here; the approving act is cited by ID (D-56) without its date; "deliberate defects in reach are preserved" is not stated; and SPEC-24's accepted residual — E-4 live leg ran on langsmith 0.11.1, repo pins 0.10.5, E-5 proved the composition offline only, live first exercised at SPEC-17/32 (gate r2 verdict "residual-named") — sits only in `trace.md`'s residual-honesty bullet. plan-authoring: the block is "verbatim, never compressed, summarized, or replaced by a citation"; README Tickets rule: each ticket's `## Plan` carries its own block. **Class match:** r1 f1 / r2 f5 site (Landmines block) — stage 3 sweeps all six ticket files' blocks. | A · 2026-08-27 — class closed by sweep under new **eligibility-assistant-D-60** (blocks are verbatim per ticket, scoped to that ticket's own diff; it also records why the five-path list the tickets shared is now split). **Scope: swept Landmines blocks of all six ticket files: corpus, llm-seam, retrieval-eval, turn, lifecycle, trace.** This block is now written out: the SPEC-30 PHI entry in full (the ADR 0006 hide-bypass-on-streaming reason the raising `_stream` closes structurally) beside the SPEC-24 egress seam it rests on, eligibility-assistant-D-56 cited **with its date** (2026-08-25 in session at plan review r1, registered 2026-08-26), the deliberate defects in reach stated preserved (D13 OPEN, TODO-12, D1/D3, D11), every §1 zone in reach stated entered or not-touched, the structural-exclusion statement, and SPEC-24's langsmith 0.10.5-vs-0.11.1 residual written **here** as residual (a) rather than borrowed from `trace.md` (`corpus` carries the same residual against the pins it lands) |
| 3 | eligibility-assistant-SPEC-30 (⚠, landmines §3 negative-test rule) · Verification 6–7 | No break-then-revert on SPEC-30: verification 7 breaks only the SPEC-24 seam. Add e.g. implement `SeamChatModel._stream` to yield a chunk (or plant an `invoke_model_with_response_stream` reference in `services/ai-assistant/`) → `tests/test_a1_trace.py::test_model_call_not_streamed` red; revert → green. | A · 2026-08-27 — verification **7b** added, both halves of the named test (`_stream` yielding a chunk instead of raising; then an `invoke_model_with_response_stream` call site planted in `agent_binding.py`), each red-then-green on `git checkout --`; ownership unchanged and consistent with eligibility-assistant-D-55 — SPEC-30 is this ticket's and `tests/test_a1_trace.py` is created here with `test_model_call_not_streamed` alone (`trace` only extends the file), so the negative check sits with the row it proves |

Per-SPEC verdicts: SPEC-24 **FINDING** (f1, f2) — design otherwise sound and fact-checked
(`_adapt` `services/ai-assistant/llm_client.py:192` keeps only `type`/`text`, no
`stop_reason`, so E-4 delta (i) is real; `_call` `:471` returns the raw adapted response and
runs `_enforce_char_cap` / `_enforce_budget` / `_require_bearer_token` before egress, raising
`LLMConfigError(egressed=False)` at `:454`; `_result_from_response` `:566` untouched keeps
`tests/test_llm_client.py::test_non_text_content_block_raises_through_adapter` `:683` green
under the superset). SPEC-30 **FINDING** (f2, f3) — mechanism holds (no
`invoke_model_with_response_stream` anywhere in `services/ai-assistant/` today; `_stream`
raising was the E-5 dry-leg shape).

checked: eligibility-assistant-SPEC-24, -30 (ticket Scope) against Decisions D-1..58, Evidence E-1..7, the ticket's Changes (4 rows), Verification 6–7 + cross-refs 5/8/9/19/20/21 (present in `turn.md` / `trace.md` as cited), Landmines block · whole-Spec ownership: 66/66 frozen rows owned by exactly one ticket across the six `Scope:` lines and the contract `## Delivery` table (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated), no orphan, no double-own · check map 66/66 single-mechanism · ⚠ coverage: SPEC-30 covered by eligibility-assistant-D-56 (names SPEC-28–31, 2026-08-25), SPEC-24 not ⚠ · freeze scope: 0 `DEFERRED` requirements, both rows on current REQ-6‴ / REQ-8‴ · cites: every SPEC / decision / evidence id resolves · facts verified in tree vs `main`: `llm_client.py` `_adapt`:192 / `_BedrockMessages.create`:229 / `max_input_tokens`:351 / `_require_bearer_token`:428 / `_enforce_budget`:458 / `_call`:471 / `_result_from_response`:566; `tests/test_llm_client.py` 54 tests, `:647` / `:653` comments survive, `:683` tool_use case; `services/ai-assistant/agent_binding.py` and `tests/test_a1_trace.py` absent (NEW claims true); no langchain / `BaseChatModel` pin or import anywhere in the tree (f1); `tracing.wrap_create` installed at `llm_client` import; CI `services` job = `pip install -r services/<svc>/requirements.txt` + `python -c "import app"`; `.venv` python 3.12.13 present · falsified-claims sweep over landmines / debt-log / phi-policy / todo / CLAUDE.md / ARCHITECTURE / runbook / seam-map / tests README / ADR 0006 / ADR 0011 for `_adapt`, `complete_structured`, `invoke_model`, `llm_client`, stream: no clause falsified by this ticket's diff (ADR 0011:70 `complete_structured` reuse is falsified by `turn`, not here) · 3 findings (1 closure/fact, 1 Landmines block, 1 missing negative check).

### Gate — round 2, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run as wave 1 r2 alongside the `corpus` r2 gate (two agents, independent reads). All three
r1 dispositions verified holding on the re-read: f1 (`Depends on: corpus`, D-59, dated note
on D-34, item ticket table agrees), f2 (block verbatim, D-56 cited with date, deliberate
defects stated preserved, residual (a) written here), f3 (verification 7b, both halves).
Plan returns to stage 3; spec unchanged. One finding, **adjacent to r1 f3's class**
(a planned mechanism with no check that reddens if it is wrong) — stage 3 closes it as a
class: sweep this ticket's change list for every row whose mechanism no verification
reddens on, and record the sweep scope in the cell, not just the `_adapt` fix.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | eligibility-assistant-SPEC-24 · `services/ai-assistant/llm_client.py` `_adapt` change row · `tests/test_llm_client.py` change row · Verification 6–7 | The receive half of SPEC-24 (`tool_use` blocks with id/name/input, plus stop reason, through the seam) is planned but has no check that reddens if it is wrong: the only `_adapt` test in the change list is a **text-only** characterization (pins the old shape, not the superset), the pinned fail-closed test asserts zero egress with the bearer unset, and verification 7 breaks only the egress route. Today `_adapt` (`llm_client.py:192-218`) drops `id`/`name`/`input` and has no `stop_reason`; an implementation that ships it unchanged, or without `stop_reason`, passes every verification in this ticket and surfaces only in `turn`. Either add a test that drives a scripted `tool_use` body through the binding and asserts the tool-call fields and `stop_reason` arrive (break-then-revert: drop `stop_reason` from `_adapt` → red; revert → green), or write the gap into residual-honesty as an accepted residual. **Class:** verification does not cover the row's mechanism — adjacent to r1 f3; see preamble. | A · 2026-08-27 — `tests/test_llm_client.py::test_a1_binding_round_trip_through_seam` added (scripted `tool_use` body through the binding: `tool_calls` id/name/args and `response_metadata["stop_reason"]` asserted; `_adapt` asserted directly), verification 7c both halves. **Class closed by sweep — scope: all five change rows of this ticket walked for a mechanism no verification reddens on.** Found and filled: `_adapt` receive half (f1 → 7c), `bind_tools` → `extra_body["tools"]` (no check → 7d), role-alternation merge and `ToolMessage` → `tool_result` (no check → 7e, and the `agent_binding.py` row now states the conversion); already covered: `_generate` → `_call` (7), `_stream` (7b both halves), `test_a1_trace.py` (7b), CLAUDE.md count (9). Baseline move 3 → 4, verification 9 and the CLAUDE.md row updated |

Verified and **not** a finding, for the record: send side and fail-closed path of SPEC-24
are sound (`_call` `:471` runs `_enforce_char_cap` / `_enforce_budget` /
`_require_bearer_token` before egress; `max_input_tokens` `:494` counts `extra_body` so
`tools` sit inside the byte gate; `_BedrockMessages.create` `:245-248` merges `extra_body`;
`LLMConfigError(egressed=False)` at `:454`). SPEC-30's reference half is non-vacuous (no
`invoke_model_with_response_stream` anywhere in `services/ai-assistant/` today). Implementer
note, not a finding: that reference check is a string scan, so `agent_binding.py`'s
docstring must not name the streaming API. Noted, not a finding: `corpus` / `llm-seam` /
`trace` / `turn` title lines lack the plan-authoring template suffix that `lifecycle` /
`retrieval-eval` carry.

Per-SPEC verdicts: SPEC-24 **FINDING** (f1) · SPEC-30 satisfied (⚠ covered by D-56;
negative checks 7b both halves; residual (b) written down).

checked: eligibility-assistant-SPEC-24, -30 (ticket `Scope:`) against Decisions D-1..60, Evidence E-1..7, the ticket's Changes (5 rows), Verification 5/6/7/7b/8/9/19, Gate interactions, Landmines block · whole-Spec ownership: 66/66 frozen rows owned exactly once across the six `Scope:` lines and the contract `## Delivery` table (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated), table = Scope lines, no orphan, no double-own · check map 66/66 single-mechanism · ⚠ coverage: SPEC-30 → D-56 (2026-08-25, registered 2026-08-26); SPEC-24 not ⚠ · freeze scope: 0 `DEFERRED` requirements, both rows on current REQ-6‴ / REQ-8‴ · cites: every SPEC / decision / evidence id resolves (D-59, D-60 present; D-34 carries the dated D-59 note) · change list closes both ways (CLAUDE.md §6 row traced to D-43 + named registry upkeep; its SPEC-40 cite contextual, `trace`'s row) · facts verified in tree: `llm_client.py` `_adapt`:192 (type/text only, no `stop_reason`) / `_BedrockMessages.create`:229 / `_require_bearer_token`:428 / `egressed=False`:454 / `_enforce_budget`:458 / `_call`:471 / `_result_from_response`:566 / `wrap_create` install :730; `tests/test_llm_client.py` 54 tests, `:647` and `:653` comments survive true, `:683` tool_use case stays green under an additive superset; `agent_binding.py` and `tests/test_a1_trace.py` absent; no `langchain*` / `BaseChatModel` / `langgraph` tracked outside docs; no streaming reference in the service; `requirements.txt` / `requirements-dev.txt` carry `langsmith==0.10.5` only; CI `services` job :72-76, `tests` job :86, `secret-scan` gitleaks v8.18.4 `--no-git` :120-129; CLAUDE.md baseline `:141`; `.venv` python 3.12.13; `Dockerfile.test` installs `requirements-dev.txt`; every file the not-touched entries name exists (`authz.py`, `security.py`, `test_compose_topology.py`, `test_eligibility_budget_alignment.py`, `tests/integration/test_schema_upgrade_path.py`, `EXPECTED_ROUTE_CAPABILITIES` in `tests/test_gateway_authz.py:105`); corpus verification 5/8 agree with this ticket's 5/8 (`ok`, `2`) · Landmines block names every `docs/landmines.md` §1 zone (entered: PHI handling; not touched: Auth, IDOR, Domain-internal, ROI, Inline eligibility, Booking, Duplicates, HL7, Secrets, Migrations; intake guards via gateway/intake-service untouched) · falsified-claims sweep (landmines / debt-log / phi-policy register / todo / CLAUDE.md / ARCHITECTURE / runbook / seam-map / tests README / ADR 0006 / ADR 0011, plus in-code markers at `llm_client.py:487`, `test_llm_client.py:647`) for `_adapt`, `invoke_model`, `llm_client`, `complete_structured`, stream/streaming: no clause falsified by this ticket's diff (ADR 0011:70 is `turn`'s; the `_adapt` docstring `:193-195` is inside the edited function) · wrong facts: 0 · 1 finding (SPEC-24 receive half unverified in-ticket).

### Gate — round 3, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run as wave 1 r3 alongside the `corpus` r3 gate (two agents, independent reads). The r2
disposition verified holding on the re-read (`test_a1_binding_round_trip_through_seam`
added, verifications 7c–7e present, baseline moved to +4). **Round-3 rule fired**
(`.claude/skills/drift-gate/`): a third round with any open finding stops the loop. The
plan does **not** return to stage 3 on this round's own authority — the owner decides per
finding: accept as a named residual, overrule, or change the spec (stage 2, explicit
decision). Each call is recorded in that finding's disposition cell below, and the next
gate run honors recorded owner decisions rather than re-flagging them. Spec unchanged by
this round.

Shape of the round: the SPEC-24 / SPEC-30 **designs are sound and fact-checked against the
tree** (per-SPEC verdicts below record what was verified line by line); every finding is in
how the plan's own instruments reach the binding, or in bookkeeping. f1 is the substantive
one and is **self-consistency**, not drift: the r2 test added to close r2 f1 cannot capture
what it asserts on, because the binding would execute a second `llm_client` module unless
`sys.modules` is pinned first (CLAUDE.md §6; the sibling rig at `tests/test_ai_visit_chat.py:51-73`
is the house idiom) — and the unpinned second copy runs its real `_BedrockClient` under the
autouse fake bearer, i.e. a default-suite egress attempt. f2 is a **cross-ticket class**:
the same "CI `services` smoke proves the new import" claim sits in `corpus.md`'s first
Gate-interactions bullet for `policy_tool` / `langchain_core`, so a disposition must close
it as a class over both tickets, not the cited bullet alone. f3 and f4 are registry /
record upkeep. f5 (a break step that would egress) and f6 (title-line template deviation,
raised at r2 as "noted, not a finding" and left unclosed — `corpus` / `turn` / `trace`
share it) are minor.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-24 / SPEC-30 · `tests/test_llm_client.py` change row · `tests/test_a1_trace.py` change row · verifications 6, 7c–7e | Self-consistency: the plan's own new test cannot do what the plan says it does. `tests/test_llm_client.py` loads the client as `llm_mod = load_module("services/ai-assistant/llm_client.py", "ai_llm_client")` (`:38`) and deliberately **restores** `config` / `logging_config` afterwards (`:39-43`) — nothing is left in `sys.modules` under the bare name `llm_client`. `agent_binding.py` must reach `llm_client._call` by the service idiom (`app.py:33` `import llm_client`), and `load_module` puts `services/ai-assistant/` on `sys.path`, so loading `agent_binding` executes a **second, independent** `llm_client` module. `_stub_runtime_returning` patches `llm_mod.client` (`:669`) — the copy the binding does not call — so `calls` stays empty (`test_a1_binding_round_trip_through_seam` and breaks 7c–7e assert on a body that is never captured), and the second copy's real `_BedrockClient` is live under the autouse fake bearer (`:46-52`), i.e. a real `invoke_model` attempt from the default suite. The house fix is the pin the sibling rig already uses: `sys.modules["llm_client"] = load_module(...)` before loading the dependent module (`tests/test_ai_visit_chat.py:51-73`, and CLAUDE.md §6 "pin `sys.modules` first"). Same applies to `tests/test_a1_trace.py`, which must import the binding to assert `_stream` raises. State the loading/pin step in the two test change rows, or state that the stub does not reach the binding. | A · 2026-08-27 — the r2 fix could not capture what it asserted on: `tests/test_llm_client.py:34-43` loads `llm_client` as `ai_llm_client` and restores `config` / `logging_config`, so a service-idiom `import llm_client` in `agent_binding` would execute a second module. Both test rows now load the binding through the house pin (`sys.modules["llm_client"] = llm_mod` before `load_module`, restore after — `tests/test_ai_visit_chat.py:51-73`, CLAUDE.md §6) and open with `agent_binding.llm_client is llm_mod`, so the stub on `llm_mod.client` (`:659`, `:669`) is the runtime the binding reaches and an unpinned copy reddens before any `invoke`. Verification 7f breaks the pin without egress. Self-consistency walked over the fix: `_stub_runtime_returning` patches the pinned module; `test_a1_binding_round_trip_through_seam`, 7c–7e assert on a body that is now captured. **Sites changed:** `llm-seam.md` `tests/test_llm_client.py` row (rig, identity assertion, stub), `tests/test_a1_trace.py` row (rig), verification 7f (new). |
| 2 | SPEC-24 · Gate interactions, bullet 1 (CI `services` import smoke) | Wrong fact / unsupported gate-interaction claim: "the existing smoke becomes the standing proof that the binding imports keyless and offline, with no new job". The smoke is `pip install -r services/ai-assistant/requirements.txt` then `python -c "import app"` (`ci.yml:73-76`), and this ticket's change list contains **no `app.py` row** — it "lands dark … no route calls it" — so `import app` never imports `agent_binding` and the job proves nothing about the binding. Nothing in CI exercises `agent_binding` against the **service** requirements until `turn` wires it; the CI `tests` job (bullet 2) proves the import only against root `requirements-dev.txt`. Verification 5 is a hand-run local check, not a gate. Either restate the bullet (hand-check only, residual until `turn`) or name a mechanism that runs. **Class:** CI `services` smoke claimed as standing proof of a new import — `corpus.md`'s first Gate-interactions bullet carries the same defect for `policy_tool` / `langchain_core`; a disposition closes it across both tickets. | A · 2026-08-27 — **class closed across both tickets** (sites: `llm-seam.md` Gate-interactions bullet 1; `corpus.md` Gate-interactions bullet 1 for `policy_tool` / `langchain_core`). Mechanism, not a residual: `import app` (`ci.yml:76`) proves only what `app.py:30-36` imports, so each ticket adds a **dark import** to `app.py` — `corpus` `import policy_tool`, this ticket `import agent_binding` (new change row) — and the smoke then imports the binding against `services/ai-assistant/requirements.txt` (`ci.yml:73`) with no new job. Both bullets restated to say the proof exists because of the import. The `tests` job (`ci.yml:86`, root `requirements-dev.txt`) stays the second leg. **Sites changed:** `llm-seam.md` `services/ai-assistant/app.py` row (new), Gate interactions bullet 1, Landmines (1) "lands dark" sentence; `corpus.md` `app.py` row, Gate interactions bullet 1. |
| 3 | SPEC-30 · `tests/test_a1_trace.py` change row · falsified-claims sweep | Registry upkeep missing: this ticket lands a **new tracked test file** (`tests/test_a1_trace.py`) and carries no `tests/README.md` change row, while `corpus.md` carries one ("AI paths" bullet names the eligibility-assistant test files) — and `corpus` lands first, so it cannot name a file that does not exist yet. Per eligibility-assistant-D-60 a ticket may not rest an entry on a sibling: either add the `tests/README.md` row here or state the omission. | A · 2026-08-27 — `tests/README.md` row added here for `test_a1_trace.py` (`tests/README.md:32` "AI paths"); `corpus.md`'s row narrowed to its own three files. Class swept over the six tickets by NEW test files vs README rows: `corpus` 3 files / row narrowed; `llm-seam` 1 / row added; `turn` 9 files / **0 rows → row added** (`turn.md`); `trace` 0 new files (extends `test_a1_trace.py`) / none needed; `lifecycle` 1 / `lifecycle.md:21` present; `retrieval-eval` 2 / `retrieval-eval.md:18` present. **Sites changed:** `llm-seam.md` `tests/README.md` row (new); `corpus.md` `tests/README.md` row; `turn.md` `tests/README.md` row (new). |
| 4 | eligibility-assistant-D-60 (item plan, "Second pass 2026-08-27") vs verification 9 / `CLAUDE.md` change row | Stale record: D-60 pins this ticket's rewritten checks as "llm-seam … 9 (baseline + 3, no `skipped` term yet)", but the r2 disposition moved the baseline to **+4** ("Baseline move 3 → 4, verification 9 and the CLAUDE.md row updated") and both the change row and verification 9 now say four tests. The decision register and the ticket contradict each other on the number the Delivery record will carry. | A · 2026-08-27 — eligibility-assistant-D-60's llm-seam clause carries a dated note: `9 (baseline + 3 — + 4 since the llm-seam gate r2 disposition 2026-08-27)`; verification 9 and the `CLAUDE.md` row already said four. No other register row names the count (`grep -n "baseline + 3" plans/eligibility-assistant.md` → the one D-60 hit). **Sites changed:** `plans/eligibility-assistant.md` D-60 (dated note). |
| 5 | SPEC-24 · verification 7 | The break plants a live vendor egress in a suite that must be network-free: "replace the `llm_client._call` egress with a direct `boto3` call" runs with `AWS_BEARER_TOKEN_BEDROCK` deleted, so botocore falls through to the ambient credential chain — exactly the path `_require_bearer_token` (`llm_client.py:428-455`) exists to forbid — and on a machine with `~/.aws/credentials` a signed Bedrock request leaves. A break that bypasses the guards without egressing (e.g. call `_BedrockMessages.create` on the stubbed runtime directly, skipping `_call`) reddens the same assertion. Minor. | A · 2026-08-27 — verification 7 restated: the break bypasses `_call` by calling `llm_client.client.messages.create(...)` on the **stubbed** runtime (`_stub_runtime_returning`, `tests/test_llm_client.py:659`), so the test reddens on `calls != []` + no `LLMConfigError` and nothing can leave the process; the `boto3` form is gone. `test_a1_binding_uses_seam_fail_closed`'s row now states the runtime is stubbed. The `_require_bearer_token` locator (`llm_client.py:428-455`) verified in tree. **Sites changed:** `llm-seam.md` verification 7, `tests/test_llm_client.py` row (`test_a1_binding_uses_seam_fail_closed` stub clause). |
| 6 | title line (`llm-seam.md:1`) · `.claude/skills/plan-authoring/` template | Template deviation still open: the title is `# eligibility-assistant / llm-seam`, missing the template's `— ticket plan file (deleted at its merge; contract: docs/workflow/eligibility-assistant.md)` suffix that `lifecycle.md` and `retrieval-eval.md` carry — the line that points at the contract and records the delete-at-merge obligation. Raised at r2 as "noted, not a finding" and left unclosed; `corpus` / `turn` / `trace` share it. Minor. | A · 2026-08-27 — title line brought to the template (`.claude/skills/plan-authoring/` template line 1). Sweep enumerated over the six ticket files: `corpus.md`, `llm-seam.md`, `turn.md`, `trace.md` — suffix added; `lifecycle.md`, `retrieval-eval.md` — already carried it. **Sites changed:** title line — `corpus.md:1`, `llm-seam.md:1`, `turn.md:1`, `trace.md:1`. |

Verified and **not** a finding, for the record: the frozen `test:` pin for SPEC-24 names
only `test_a1_binding_uses_seam_fail_closed` while the row's send/receive halves are now
proven by the added `test_a1_binding_round_trip_through_seam` — the plan supplements rather
than changes the pinned id, matching the precedent `corpus` set at its own r2 (a new
`test_startup_hook_fails_boot_on_corpus_error` under SPEC-7's `test_manifest_sha_pinned`
pin), so no owner decision is implied; `tests/test_a1_trace.py` is created here and only
extended by `trace` (`trace.md:17` agrees); "carried findings: none" is correct under the
monolithic r2 tagging (f1/f6 → turn, f2 → corpus, f3 → trace, f4/f5 → item);
`workflow-doc-cap` / `make eval` / `test_compose_topology.py` are parked in `turn` by D-60
and the contract sits at 339 of 400 lines; no `docs/landmines.md` §1 zone is missing from
this block (the intake-registration zone is covered by file path rather than by name).

Per-SPEC verdicts:

- **SPEC-24 — FINDING** (f1, f2; f5 minor). Design sound and fact-checked against the tree:
  `_adapt` (`services/ai-assistant/llm_client.py:192-218`) keeps only `type`/`text` and
  carries no `stop_reason`, so E-4 delta (i) is real; the superset is additive and
  `_result_from_response` (`:566`) still fails closed on a no-text body, keeping
  `tests/test_llm_client.py::test_non_text_content_block_raises_through_adapter` (`:683`)
  green; `_call` (`:471`) runs `_enforce_char_cap` / `max_input_tokens` / `_enforce_budget` /
  `_require_bearer_token` before the single egress and raises `LLMConfigError(egressed=False)`
  (`:454`); `_extra_body_text` (`:340-348`) serialises `extra_body`, so `tools` sit inside
  both the char cap and the token gate, and `_enforce_budget` (`:463`) calls `estimate_cost`
  → `_resolve_pricing`, so SPEC-24's *pricing* fail-closed guard does apply on the binding
  path even though the plan never names it; message content is byte-bounded by `str(content)`
  (`:371`), covering list-content `tool_result` turns (E-4 delta ii). `tracing.wrap_create`
  is installed on the module-level client (`:730`) and `_trace_metadata` (`:618-641`) reads
  scalars only, so the superset adds no trace content. Residual (a) (E-4 live on
  `langsmith 0.11.1`, repo pins `0.10.5`, E-5 offline only, live first at SPEC-17/32) is
  written down here and matches E-4/E-5 verbatim.
- **SPEC-30 — FINDING** (f1 second half, f3). Mechanism holds and is non-vacuous: no
  `invoke_model_with_response_stream` reference exists anywhere under
  `services/ai-assistant/` today, and an overridden raising `_stream` is what
  `BaseChatModel.stream()` dispatches to; the negative check is complete (7b, both halves)
  and residual (b) — proven for this service, not the estate — is written down.

checked: eligibility-assistant-SPEC-24, -30 (ticket `Scope:` line) against Decisions eligibility-assistant-D-1..63, Evidence E-1..7, the ticket's Changes (5 rows), Verification 5/6/7/7b/7c/7d/7e/8/9/19, Gate interactions (3 bullets), Landmines block · spawning prompt = item + ticket only, no branch and no characterization · whole-Spec ownership: 66/66 frozen rows owned by exactly one ticket across the six `Scope:` lines, the contract `## Delivery` ticket table and the item plan's ticket table (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 never allocated), all three tables agree, no orphan, no double-own · check map 66/66 rows, exactly one `test:`/`cmd:`/`gate:` each (scripted) · ⚠ coverage: SPEC-30 → eligibility-assistant-D-56 (SPEC-28–31, owner 2026-08-25 in session at plan review r1, registered 2026-08-26, cited with date in the block); SPEC-24 not ⚠ · freeze scope: 0 `DEFERRED` requirements in the contract, both rows on current REQ-6‴ / REQ-8‴ · cites: every SPEC, decision and evidence id in the file resolves (scripted diff against the contract and the register; D-59/D-60 present, D-34 carries the dated D-59 note, item ticket table's `depends on` agrees with `Depends on: corpus`) · change list closes both ways (CLAUDE.md §6 row → D-43 + registry upkeep; f3 is the one un-served registry) · facts verified in the working tree: `llm_client.py` `_adapt`:192 · `_BedrockMessages.create`:229 · body/`invoke_model`:236-254 · `_extra_body_text`:340 · `max_input_tokens`:351 · `_enforce_char_cap`:392 · `_require_bearer_token`:428 · `egressed=False`:454 · `_enforce_budget`:458 · `_call`:471 · `_result_from_response`:566 · `_trace_metadata`:618 · `wrap_create`:730; `tests/test_llm_client.py` autouse bearer fixture `:46-52`, `_stub_runtime_returning`:659, `:683` tool_use case; `services/ai-assistant/agent_binding.py` and `tests/test_a1_trace.py` absent (NEW claims true); no `langchain*` / `langgraph` / `BaseChatModel` anywhere in the tracked tree, `services/ai-assistant/requirements.txt` and root `requirements-dev.txt` carry `langsmith==0.10.5` only; no streaming API reference in `services/ai-assistant/`; `llm_client.py` exists in one service only (no `tests/test_redaction.py` parity obligation); `CLAUDE.md` baseline `:141`; `ci.yml` services `:72-76`, tests `:86`, secret-scan `:120-129`, `workflow-doc-cap` 400-line cap on `docs/workflow/*.md` top level only (contract 339 lines); `.venv` python 3.12.13; `corpus.md` verifications 5/8 agree with this ticket's 5/8 (`ok`, `2`) · falsified-claims sweep over `docs/landmines.md`, `docs/debt-log.md` (D13), `docs/phi-logging-policy.md` (rules 4/5, register `:92`), `docs/todo.md` (TODO-9/10/12/60), `CLAUDE.md`, `ARCHITECTURE.md`, `docs/runbook.md`, `docs/onboarding-seam-map.md`, `tests/README.md`, ADR 0004/0006/0009/0011 and in-code markers on the edited lines, searched for `_adapt` · `invoke_model` · `llm_client` · `complete_structured` · stream/streaming: one hit un-served (f3, `tests/README.md`); ADR 0009's "keeps the exact `client.messages.create(**kwargs)` seam" and ADR 0006 / `tracing.py`'s "scalar run metadata … and no more" both survive the additive superset · wrong facts: 1 (f2) · 6 findings (1 self-consistency, 1 gate interaction, 1 registry, 1 stale decision record, 2 minor).

### Gate — round 4, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run as wave 2 r4 alongside the `corpus` r4 gate (two agents, independent reads). First
round under the origin-tag rule (PR #91 @762f0fd): each finding cell opens with the
agent's tag derived from the record. The r3 dispositions verified holding on the re-read
(dark `import agent_binding`, the `sys.modules` pin rig and identity assert, stub-only
verification 7 break, the D-60 dated note, title-line suffix on all six ticket files).
**Round-3 rule fired** (`.claude/skills/drift-gate/`): a fourth round with open `new` /
`class-repeat` findings stops the loop. The plan does **not** return to stage 3 on this
round's own authority — the owner decides per finding: accept as a named residual,
overrule, or change the spec (stage 2, explicit decision). Each call is recorded in that
finding's disposition cell below, and the next gate run honors recorded owner decisions
rather than re-flagging them. Spec unchanged by this round.

Shape of the round: both findings are on the **seam payload and the controls below it** —
not on the framework choice, the never-stream guarantee, or the test rig, all of which the
agent re-verified line by line (per-SPEC verdicts below). f1 is the uncovered half of the
row the r2 disposition called "already covered": the message-conversion row closes over
`HumanMessage` / `AIMessage` / `ToolMessage` and never names `SystemMessage`, which
Bedrock carries at `body["system"]` (`llm_client.py:244`) and `max_input_tokens` counts on
its own line (`:368`) — a different body and a different byte accounting than `turn`'s
verification 18 budgets for, with nothing that reddens. f2 is a control-loss claim: the
Landmines block says routing through `_call` "keeps every control on the bytes that
actually leave", but the malformed-200 fail-closed guard and the per-call telemetry live in
`_result_from_response`, which only `complete()` / `complete_structured()` reach — a bypass
ADR 0004 `:57` requires justifying, and one that falsifies `_result_from_response`'s own
in-code comment.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-24 · `services/ai-assistant/agent_binding.py` change row (message conversion) · `tests/test_llm_client.py` row · verifications 7, 7d, 7e | **class-repeat** — same failure class as r2 f1 ("a planned mechanism with no check that reddens if it is wrong"); that disposition's sweep listed the `_generate` → `_call` row as "already covered (7)", and this is the uncovered half of that same row. The conversion the row enumerates is closed — `HumanMessage` → `user`, `AIMessage` → `assistant`, `ToolMessage` → `tool_result` inside a `user` turn — and names no mapping for a `SystemMessage`, nor what `_generate` supplies as `_call`'s `system` and `max_tokens` arguments (`_call(messages, system, max_tokens, extra_body=…)`, `llm_client.py:471-476`). Both are part of the payload SPEC-24 routes through the seam: `max_input_tokens` counts `system` on its own line outside the per-message loop (`:368`) and `_BedrockMessages.create` puts it at `body["system"]`, never in `messages` (`:244`), so a system prompt smuggled in as a `user` message is a different body and a different byte accounting than the gate assumes. The payload is not hypothetical: today's turn egresses one (`app.py:798-801`, `system=_VISIT_SYSTEM_PROMPT`) and `turn` budgets for one (`turn.md:71`, verification 18 counts "system prompt" among the binding's egress bytes, "measured as `llm_client.max_input_tokens` measures them"). Nothing here reddens: the round-trip test invokes `[HumanMessage, HumanMessage]` only, so a binding that drops or mis-routes a `SystemMessage` passes 7 / 7c / 7d / 7e and surfaces first in `turn`. Name the `SystemMessage` → `system` mapping and the `max_tokens` source in the change row, and assert the captured body's `system` key in `test_a1_binding_round_trip_through_seam` with a break-then-revert. | **Owner 2026-08-27: accepted → stage 3.** Name the `SystemMessage` → `system` mapping (leading message → `_call(system=…)`, none → `None`, more than one or non-leading → typed error) and the `max_tokens` source in the change row; assert the captured body's `system` key in the round-trip test with a break-then-revert. **Disposition 2026-08-27 — A.** Mapping named in the change row with its locators: one leading `SystemMessage` → `_call(system=…)` (`body["system"]`, `llm_client.py:244`; counted at `:368`), none → `None`, misplaced or repeated → `ValueError` before `_call`, zero egress; `max_tokens` = `settings.llm_max_output_tokens` (`config.py:65`, `complete()`'s source at `llm_client.py:650`), never a kwarg; `stop` must be `None` (`_generate` signature read from the pinned wheel, `chat_models.py:2198-2204`); of `**kwargs` only `tools` is read. Reddening: the round-trip test now invokes with a leading `SystemMessage` and asserts `body["system"]`, the absence of that text from `messages`, and `body["max_tokens"]`; `test_a1_binding_system_message_rules` covers the raise cases with `calls == []`; breaks 7g. Class sweep (r2 f1 class, "a planned mechanism with no check that reddens"), enumerated over the `agent_binding.py` row's mechanisms: `HumanMessage` → `user` — covered (round trip, 7e merge); `AIMessage` text + `tool_calls` → `assistant` with `tool_use` blocks — covered (second invoke, 7c); `ToolMessage` → `tool_result` — covered (7e second break); `SystemMessage` → `system` — **was uncovered, now 7g**; `bind_tools` → `extra_body["tools"]` — covered (7d); role merge — covered (7e); returned `tool_calls` / `stop_reason` — covered (7c); `max_tokens` — **was unstated, now asserted in the round trip**; `stop` / kwargs — **was unstated, now `test_a1_binding_system_message_rules`**; `_stream` raises — covered (7b); `_generate` → `_call` — covered (7). Baseline move 4 → 6 (with f2). **Sites changed:** `llm-seam.md` Status line, `agent_binding.py` row, `tests/test_llm_client.py` row, `CLAUDE.md` row, verification 7g (new), verification 9, Landmines PHI (1) and its re-open sentence; `plans/eligibility-assistant.md` D-60 (count note). |
| 2 | SPEC-24 · Landmines block (1) "The model egress seam" · `services/ai-assistant/agent_binding.py` change row · residual-honesty bullet | **new** (r3's `checked:` line records no plan-text hash, so the tag cannot be settled from the record). The block states that routing the framework's payloads through `_call` "keeps every control on the bytes that actually leave" and lists four (char cap, budget gate, bearer fail-closed, typed errors) — but `_call` (`llm_client.py:471`) returns the adapted response raw; the malformed-200 fail-closed guard and the per-call telemetry live one level up in `_result_from_response`, which only `complete()` (`:653-654`) and `complete_structured()` (`:701-705`) reach. On the binding path a 200 with no usable content raises no `LLMResponseError` (`:579-583`), a 200 with no `usage` raises none (`:591-594`), and no `llm call model=… in_tokens=… cost=… request_id=…` line is emitted for any agent-path call (`:606-614`) — `wrap_create`'s `_trace_metadata` (`:618`, installed `:730`) is a no-op unless `LANGSMITH_TRACING=true`, and it is `false` at rest (E-7). That is a bypass ADR 0004 names explicitly (`adr/0004-ai-assistant-service-and-llm-wrapper.md:38-40` typed failures incl. `LLMResponseError`; `:57` "must justify any bypass") and one `_result_from_response`'s own comment asserts cannot exist ("Both complete() and complete_structured() flow through here, so the guard lives here once", `llm_client.py:576-577`). The block's "additive — `_result_from_response` is untouched, so `test_non_text_content_block_raises_through_adapter` still pins the non-text rejection" is true for `complete()` and does not cover the new caller. Either apply an equivalent content/usage guard and metadata log in the binding, or write the loss as a named residual (and the falsified in-code comment as a change row). | **Owner 2026-08-27: accepted → stage 3.** Not a residual — silent loss of the fail-closed guard and per-call telemetry on the estate's only vendor-egress path is not acceptable to name and keep. Binding applies an equivalent guard (at least one text *or* `tool_use` block; `usage` present, else `LLMResponseError`) and emits the same `llm call` log line; `_result_from_response` stays untouched so `test_non_text_content_block_raises_through_adapter` keeps pinning `complete()`; the falsified in-code comment is a change row; the Landmines block carries the ADR 0004 `:57` justification. **Disposition 2026-08-27 — A**, not a residual. Facts re-read this session: `_call` returns the adapted response raw (`llm_client.py:471-510`), `_result_from_response` holds the no-text guard (`:578-583`), the usage guard (`:589-595`) and the `llm call` log line (`:606-614`), reached only from `complete()` (`:653`) and `complete_structured()` (`:701`); the comment at `:576-577` says the guard lives there once. Fix: the binding applies a twin guard after `_call` — content must carry at least one `text` **or** `tool_use` block (a tool-only turn is the agent path's valid answer, which is why `_result_from_response`'s text-required check cannot be reused unchanged — and it stays unchanged, so `test_non_text_content_block_raises_through_adapter` keeps pinning `complete()`), usage must be explicit integers, each failure `LLMResponseError` with the request id only — and emits the same metadata-only log line with the same format string, `estimate_cost` and a `monotonic()` start taken before `_call`. Pinned by `test_a1_binding_guard_parity`, which feeds the binding the file's own malformed bodies and asserts the log record under `caplog` with no prompt text in any record; break 7h. The falsified comment is a change row on `llm_client.py`; the Landmines block now states the controls as they are — four pre-egress on `_call`, two post-egress by the twin — and carries the ADR 0004 `:57` justification (not a bypass: every named control applied). `_trace_metadata` / `wrap_create` (`:618`, `:730`) is left as it is: a no-op at rest by E-7 and not a control this ticket claims. **Sites changed:** `llm-seam.md` `llm_client.py` row (comment amendment), `agent_binding.py` row (twin guard), `tests/test_llm_client.py` row (`test_a1_binding_guard_parity`), verification 7h (new), Landmines PHI (1) (controls sentence, ADR 0004 justification) and its re-open sentence. |

Per-SPEC verdicts:

- **SPEC-24 — FINDING** (f1, f2). Design otherwise sound and re-verified line by line:
  `_adapt` (`llm_client.py:192`, blocks built at `:204-207`) keeps only `type`/`text` and
  carries no `stop_reason`, so E-4 delta (i) is real and the superset is additive —
  `_result_from_response` (`:566`) still fails closed on a no-text body, keeping
  `test_non_text_content_block_raises_through_adapter` green; `_call` (`:471`) runs
  `_enforce_char_cap` → `max_input_tokens` → `_enforce_budget` → `_require_bearer_token`
  before the single egress (`:493-496`) and raises `LLMConfigError(egressed=False)` at
  `:454`; `extra_body` is serialised into both the char cap and the byte gate (`:340-348`,
  `:373-375`) and merged into the Bedrock body at `:245-248`, so `tools` sit inside the
  gate; the r3 f1 pin rig is sound — `tests/conftest.py:16-26` puts the service dir on
  `sys.path` and registers nothing under the bare name, `tests/test_llm_client.py:33-43`
  restores `config` / `logging_config`, `tests/test_ai_visit_chat.py:50-72` is the house
  pin idiom, and module import constructs the boto3 client without a network call
  (`llm_client.py:269-284`), so verification 7f's break is genuinely egress-free.
- **SPEC-30 — satisfied.** ⚠ covered by eligibility-assistant-D-56 (SPEC-28–31, owner
  2026-08-25 in session at plan review r1, registered 2026-08-26), cited with its date in
  this ticket's own block; mechanism non-vacuous (no `invoke_model_with_response_stream`
  anywhere under `services/ai-assistant/` today, and an overridden raising `_stream` is
  what `BaseChatModel.stream()` dispatches to); §3 negative check complete (7b, both
  halves); residual (b) — proven for this service, not the estate — written down.

checked: eligibility-assistant-SPEC-24, -30 (the ticket `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, the ticket's Changes (7 rows), Verification 5/6/7/7f/7b/7c/7d/7e/8/9/19, Gate interactions (3 bullets), Landmines block, Findings r1–r3 · plan-text hash read: `git hash-object docs/workflow/plans/eligibility-assistant/llm-seam.md` → `26ea3f9fbd7d9d5114155ad90da882cc06e07255` · origin: 1 new · 0 pre-existing · 1 class-repeat · spawning prompt = item + ticket only, no branch and no characterization (read `main` with the uncommitted working tree) · check map: 66/66 frozen rows carry exactly one `test:`/`cmd:`/`gate:` (scripted over the `## Spec` table) · ⚠ coverage: SPEC-30 → D-56 with date; SPEC-24 not ⚠ · freeze scope: 0 `DEFERRED` requirements, both rows on current REQ-6‴ / REQ-8‴ · ownership: 66/66 rows owned exactly once across the six `Scope:` lines, the contract `## Delivery` table and the item plan's ticket table (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 never allocated), all three agree, no orphan, no double-own; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC / decision / evidence id in the file resolves (scripted against the contract and the register) · change list closes both ways (`tests/README.md` → registry upkeep + D-60; `CLAUDE.md` §6 → D-43 + falsified-claims sweep) · facts verified in the working tree: `llm_client.py` `_adapt`:192 · blocks:204-207 · `_BedrockMessages.create`:229 · `body["system"]`:244 · `invoke_model` body:249-254 · `_extra_body_text`:340 · `max_input_tokens`:351, system term :368 · `_enforce_char_cap`:392 · `_require_bearer_token`:428-455 · `egressed=False`:454 · `_enforce_budget`:458 (calls `estimate_cost` → `_resolve_pricing`, so SPEC-24's pricing guard does apply on the binding path) · `_call`:471-476 · `_result_from_response`:566-614 · `_trace_metadata`:618 · `wrap_create`:730; `tests/test_llm_client.py` rig :33-43, autouse bearer :46-52, `_stub_runtime_returning`:659 / `:669`, `test_non_text_content_block_raises_through_adapter`:681-694; `tests/test_ai_visit_chat.py` pin rig :40-72; `tests/conftest.py::load_module`:16-26; `services/ai-assistant/app.py` imports :30-36, `import llm_client`:33, `complete_structured(system=_VISIT_SYSTEM_PROMPT)`:798-801; `agent_binding.py` and `tests/test_a1_trace.py` absent (NEW claims true); no `langchain*`/`langgraph`/`BaseChatModel` tracked, `services/ai-assistant/requirements.txt:9` and `requirements-dev.txt:13` carry `langsmith==0.10.5` only; no streaming API reference under `services/ai-assistant/`; `ci.yml` services `:73-76`, tests `:86`, secret-scan gitleaks v8.18.4 `:120-129`, `workflow-doc-cap` top-level `docs/workflow/*.md` only (contract 339 lines, D-55's claim holds), no Python linter job so a dark import cannot redden CI; `tests/README.md:32` "AI paths"; `CLAUDE.md:141` baseline `1317 passed, 19 deselected, 1 xfailed`; `docs/landmines.md:87` intake row · r3 disposition sites re-read and holding: `corpus.md:16` dark `import policy_tool` + `:57` bullet restated (f2 class), `corpus.md:28` README row narrowed / `turn.md:45` row added / `lifecycle.md:21` / `retrieval-eval.md:18` present (f3 class), D-60's `baseline + 3 → +4` dated note the only register hit for the count (f4), verification 7 break now stub-only (f5), title suffix on all six ticket files (f6) · falsified-claims sweep over `docs/landmines.md`, `docs/debt-log.md` (D13), `docs/phi-logging-policy.md` register, `docs/todo.md`, `CLAUDE.md`, `ARCHITECTURE.md` (names no LLM/Bedrock claim at all), `docs/runbook.md`, `docs/onboarding-seam-map.md:13` (the `sys.modules`-pin caveat citing `tests/test_llm_client.py` survives, strengthened), `tests/README.md`, ADR 0004/0006/0009/0011 and in-code markers on and near the edited lines, searched for `_adapt` · `invoke_model` · `llm_client` · `complete_structured` · `_result_from_response` · stream/streaming: one clause falsified and unserved (f2 — `llm_client.py:576-577` plus ADR 0004 `:38-40`/`:57`); ADR 0009's exact-`client.messages.create` seam clause and ADR 0011:373 "no tools exposed" both survive this ticket's dark diff · wrong facts: 0 · 2 findings (1 change-list/verification gap on the seam's `system` payload, 1 unnamed control loss below `_call`).

### Gate — round 5, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run alongside the `corpus` gate (two agents, independent reads). Origin tags derived by the
agent from the record — round 4's `checked:` plan-text hash (`26ea3f9f…`) and the
`Sites changed:` list on both round-4 dispositions — not from recall. **Round-3 rule fires:**
two `class-repeat` findings and one `new` are open, so the owner adjudicates each finding
(accept as named residual · overrule · change the spec) before stage 3 dispositions them.
f2 and f3 are minor. Plan returns to stage 3 after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-24 · `services/ai-assistant/agent_binding.py` change row (the returned `AIMessage`) · `tests/test_llm_client.py` row (`test_a1_binding_round_trip_through_seam`) · verification 7c | **class-repeat** — same class as r2 f1 ("a planned mechanism with no check that reddens if it is wrong"), re-fired as r4 f1, whose disposition ran an *enumerated* sweep over "the `agent_binding.py` row's mechanisms" (11 items). That enumeration omits the one the receive half exists for: **the response's `text` blocks → `AIMessage.content`**. The change row states only that the returned `AIMessage` "carries `tool_calls` built from the `tool_use` blocks' `id`/`name`/`input` and `response_metadata["stop_reason"]`" — it never says the model's text survives the binding — and nothing reddens if it does not: the round-trip test's scripted body carries a `text` block, but its return-side assertions are `tool_calls[0]`, `response_metadata["stop_reason"]` and a direct `_adapt` call only, and the r4 f2 twin guard accepts a `text` **or** `tool_use` block on the *adapted* response, so a `_generate` that validates the raw response and then builds an `AIMessage` with empty content passes 7, 7c–7e, 7g, 7h. SPEC-24 is "send and **receive** every model payload … through the existing `invoke_model` seam", and the payload model₂ returns is text (eligibility-assistant-D-37 parses "model₂'s text … into `AgentDecision`"), so a dropped text mapping surfaces first in `turn` — exactly what r2 f1 and r4 f1 each closed elsewhere. Name the text-block → `AIMessage.content` mapping in the change row and assert the scripted text on the returned `AIMessage`, with a break-then-revert. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** The receive half is now enumerated field by field in the `agent_binding.py` row — `content` = the `text` blocks' text in response order (`""` on a tool-only turn; the string `turn` parses into `AgentDecision`, eligibility-assistant-D-37), `tool_calls`, `response_metadata["stop_reason"]`, `usage` → twin guard + log line only (no `usage_metadata`), `id` / `model` → log line only, nothing else carried — so the enumeration is closed, not a list of what happened to be tested. Pinned: the round-trip body's text block gets literal text (`"Looking that up."`) and the return-side assertion `AIMessage.content == "Looking that up."`; the guard-parity tool-only case asserts `content == ""` (the mapping's empty case, distinguished from a dropped block); verification **7i** is the break-then-revert (`content=""` regardless of text blocks → round trip red, twin guard still green because `tool_use` is present — the exact hole the finding names). Class (r2 f1 / r4 f1 — a mechanism with no check that reddens) closed by enumeration of the **receive** half, the half both earlier sweeps enumerated the send side of: `text` → `content` (**added**, 7i), `tool_use` → `tool_calls` (7c second break, holds), `stop_reason` → `response_metadata` (7c first break, holds), `usage` → guard + log (7h, holds), `id` / `model` → log (7h's `llm call model=` record, holds) — five fields, one changed. Test count unchanged (assertions added to two existing tests, no new test — the `CLAUDE.md` row, verification 9 and D-60 stay at +6). Landmines PHI (1) receive sentence names the text mapping. **Sites changed:** `llm-seam.md` `agent_binding.py` row (receive-half enumeration), `tests/test_llm_client.py` row (round-trip body text + `content` assertion; guard-parity `content == ""`), verification 7i (new), Landmines PHI (1) (receive-half sentence). |
| 2 | SPEC-24 · Landmines block, PHI (1) "This ticket lands dark" sentence (`:49`) and "Accepted residuals" sentence (`:52`) | **new** — the anchored text is the r4 f2 disposition's own product (`Sites changed:` "Landmines PHI (1) (controls sentence …)"), and the contradiction it created was not swept. The block now says the binding "emits the same metadata-only `llm call model=… in_tokens=… out_tokens=… cost=… latency=… request_id=…` line" — a **new log site on the estate's only vendor-egress path, in a new file** — while two sentences later the same block says "no new log line or log field is emitted" and "this ticket … logs nothing new". Read charitably both mean "at runtime, because the binding is dark", the same sense as the adjacent "no Bedrock call is made from any code it adds"; but this block is the graded surface the PR body's "Risk & landmines" section is drafted from, and a reviewer drafting from it would state that the diff adds no log line. Distinguish the two senses (the binding's `llm call` line exists in the tree from this ticket and is emitted only when a caller invokes it; no caller exists here) so the PHI-logging claim is not read as "no new logging code". Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** The two senses are now written apart in the Landmines "lands dark" paragraph: *in the diff*, one new log site — the twin guard's metadata-only `llm call` line in `agent_binding.py`, `:606-614` format, pinned text-free by `test_a1_binding_guard_parity` — and no other logging code, stated so a "Risk & landmines" section drafted from the block names it; *at runtime*, no caller exists on this ticket, so the line is never emitted. The "Accepted residuals" sentence's "logs nothing new" is replaced by "its one new log site is metadata-only and unreached at runtime". Swept the block for the third occurrence class ("no new log", "logs nothing"): PHI (1) twin-guard sentence ("emits the same metadata-only `llm call` line" — consistent, holds), the dark paragraph (**changed**), the residuals sentence (**changed**), Secrets / Migrations / Domain-services / Auth / IDOR / Inline / ROI / Intake bullets (no logging claim) — one paragraph and one sentence changed of the block's eleven top-level bullets (counted: `awk` over `## Landmines`, 11). **Sites changed:** `llm-seam.md` Landmines PHI (1) ("lands dark" paragraph, "Accepted residuals" sentence). |
| 3 | SPEC-24 · verification 8 (`:32`) vs the `CLAUDE.md` §6 change row and verification 9 | **class-repeat** — same class as r3 f4 ("stale record: a count a disposition moved, left contradicting itself at another site"). The r4 dispositions moved this ticket's test count 4 → 6 and updated the `CLAUDE.md` row ("this ticket's six added tests"), verification 9 ("passed + 6 … the 6 being this ticket's five `tests/test_llm_client.py` cases and `tests/test_a1_trace.py::test_model_call_not_streamed`") and eligibility-assistant-D-60's count note ("+ 6 since the llm-seam gate r4 dispositions"). Verification 8 still reads "because none of this ticket's **four** tests is `[EVAL-n]`-parametrised". The pinned value `2` is unaffected, but the ticket contradicts itself on the number its Delivery record will carry — the defect r3 f4 closed between D-60 and the ticket, now between two verifications in the same file. Swept: `four` / `+ 4` / `three tests` occurs at no other site in `llm-seam.md` outside the r2/r3/r4 finding cells (history). Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** Verification 8 now reads "six tests (four → six at gate r4 f1 / f2; this site corrected at gate r5 f3)". Class (r3 f4 — a count a disposition moved, left contradicting itself elsewhere) closed with an enumerated sweep of every plan-text count site in this file (lines 1–70, Findings excluded): `CLAUDE.md` §6 row (six, names all six — holds), verification 8 (**changed**), verification 9 (`+ 6`, "five `tests/test_llm_client.py` cases and `test_model_call_not_streamed`" — holds), Status line (no count), `tests/test_a1_trace.py` row ("one test" — holds); register: eligibility-assistant-D-60 count note (`+ 6` — holds). Five sites, one changed; r5 f1 adds no test, so six stays the number. **Sites changed:** `llm-seam.md` verification 8. |

Verified and **not** a finding, for the record: the design is otherwise sound, re-verified
line by line against the working tree. `_adapt` (`llm_client.py:192`, blocks `:204-207`)
keeps only `type`/`text` and carries no `stop_reason`, so E-4 delta (i) is real and the
superset additive — `_result_from_response` (`:566`) still fails closed on a no-text body,
keeping `test_non_text_content_block_raises_through_adapter`
(`tests/test_llm_client.py:683`) green under it. `_call` (`:471-476`) runs
`_enforce_char_cap` → `max_input_tokens` → `_enforce_budget` → `_require_bearer_token`
before its single egress (`:493-496`, `:505`) and raises `LLMConfigError(egressed=False)`
at `:454`; `_enforce_budget` (`:458`) reaches `estimate_cost` → `_resolve_pricing` (`:287`),
so SPEC-24's pricing fail-closed guard applies on the binding path; `extra_body` is
serialised into both the char cap and the byte gate (`_extra_body_text:340`) and merged into
the Bedrock body at `:245-248`. The r4 f1 additions check out (`body["system"]` a distinct
key `:244`; `max_input_tokens` counts `system` outside the per-message loop `:368`;
`settings.llm_max_output_tokens` `config.py:65` is the source `complete()` uses at
`llm_client.py:650`). The r4 f2 twin-guard locators hold (`:579-583`, `:591-595`, log
`:606-614`, reachable only from `complete()` `:653` / `complete_structured()` `:701`,
comment `:576-577`). The r3 f1 pin rig is sound (`tests/conftest.py::load_module:16-26`
registers nothing under a bare name; `tests/test_llm_client.py:34-43` restores
`config`/`logging_config`; `tests/test_ai_visit_chat.py:51-73` is the house idiom;
`_stub_runtime_returning:659`/`:669` patches the module global `_call` resolves at call
time; module import builds the boto3 client with no network call `:269-284`). SPEC-30 is
**satisfied**: the mechanism is non-vacuous (no `invoke_model_with_response_stream`
reference exists anywhere under `services/ai-assistant/`, and an overridden raising
`_stream` is what `BaseChatModel.stream()` dispatches to), the `docs/landmines.md` §3
negative check is complete (7b, both halves), and residual (b) is written down.
One locator could not be verified in-tree and is recorded as such: the
`chat_models.py:2198-2204` `_generate` signature — no `langchain_core` is tracked or
installed in `.venv`, so the cite is a package locator whose form is legal under the
fact-trail rule and whose shape matches the abstract `BaseChatModel._generate`, but the
wheel was not readable this round.

Per-SPEC verdicts: SPEC-24 **FINDING** (f1; f2 and f3 also anchor here) · SPEC-30
satisfied.

checked: eligibility-assistant-SPEC-24, -30 (the ticket `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, the ticket's Changes (7 rows), Verification 5 / 6 / 7 / 7f / 7b / 7c / 7d / 7e / 7g / 7h / 8 / 9 / 19, Gate interactions (3 bullets), Landmines block, Findings r1–r4 · **plan-text hash read: `7bddb340bf171d251ad3cce7b3b0f06d222fd240`** (round 4 recorded `26ea3f9fbd7d9d5114155ad90da882cc06e07255`; both r4 dispositions carry `Sites changed:` lists, so tags are settled from the record) · **origin: 1 new · 0 pre-existing · 2 class-repeat** · check map: 66/66 frozen rows carry exactly one `test:` / `cmd:` / `gate:` token (scripted over the `## Spec` table; 0 rows with 0 or >1) · ⚠ coverage: SPEC-30 → eligibility-assistant-D-56 with date (owner 2026-08-25 in session at plan review r1, registered 2026-08-26); SPEC-24 not ⚠ · freeze scope: 0 `DEFERRED` requirements in the contract, both rows on current REQ-6‴ / REQ-8‴ · ownership (scripted): 66/66 rows owned by exactly one ticket across the six `Scope:` lines and the contract `## Delivery` table (corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 never allocated), tables agree row-for-row, no orphan, no double-own; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites (scripted): every SPEC (7, 24, 30, 37, 40), decision (D-1, 9, 34, 37, 41, 43, 48, 53, 55, 56, 59, 60) and evidence (E-1, 4, 5, 7) id in the file resolves · change list closes both ways: SPEC-24 → `llm_client.py` / `agent_binding.py` / `tests/test_llm_client.py` / `app.py`, SPEC-30 → `agent_binding.py` / `tests/test_a1_trace.py`; `tests/README.md` → registry upkeep + D-60, `CLAUDE.md` §6 → D-43 + the falsified-claims sweep · facts verified in the working tree: `llm_client.py` `_adapt`:192 · blocks:204-207 · `_BedrockMessages.create`:229 · `body["system"]`:244 · `extra_body` merge:245-248 · `invoke_model` body:249-254 · `_BedrockClient`:262 / module client:274-284 · `_resolve_pricing`:287 · `estimate_cost`:323 · `_extra_body_text`:340 · `max_input_tokens`:351 with the system term :368 · `_enforce_char_cap`:392 · `_require_bearer_token`:428-455 · `egressed=False`:454 · `_enforce_budget`:458 · `_call`:471-476, controls :493-496, egress :505 · `_result_from_response`:566, comment :576-577, guards :579-583 / :591-595, log :606-614 · `_trace_metadata`:618 · `complete()` max_tokens :650 · `wrap_create`:730; `config.py:64` 20000 / `:65` `llm_max_output_tokens`; `tests/test_llm_client.py` rig :33-43, autouse bearer :46-52, `_stub_runtime_returning`:659 / setattr :669, no-content body :673-680, tool_use case :683, no-usage body :698-703, caplog idiom :788-797, 54 tests; `tests/test_ai_visit_chat.py` pin rig :51-73; `tests/conftest.py::load_module`:16-26; `app.py` imports :30-36, `import llm_client`:33, `complete_structured(system=_VISIT_SYSTEM_PROMPT)`:798-801; `services/ai-assistant/agent_binding.py` and `tests/test_a1_trace.py` absent (NEW claims true); no `langchain*` / `langgraph` / `BaseChatModel` tracked anywhere, `services/ai-assistant/requirements.txt` and root `requirements-dev.txt` carry `langsmith==0.10.5` only, `.venv` has no `langchain_core`; no streaming API reference under `services/ai-assistant/`; `ci.yml` services install :73 / `import app` :76, tests :86, gitleaks v8.18.4 :120-129; `tests/README.md:32` "AI paths"; `CLAUDE.md:141` baseline `1317 passed, 19 deselected, 1 xfailed`; `adr/0004:38-40` typed failures incl. `LLMResponseError`, `:57-58` "must justify any bypass"; `docs/landmines.md:87` intake row; `docs/onboarding-seam-map.md:13` `sys.modules`-pin caveat survives, strengthened · r4 disposition sites re-read and holding (`SystemMessage` mapping + `max_tokens` + `stop` in the `agent_binding.py` row; `test_a1_binding_system_message_rules` / `test_a1_binding_guard_parity`; verifications 7g / 7h; the `:576-577` comment amendment row; the Landmines PHI (1) rewrite and its re-open sentence; verification 9 and the `CLAUDE.md` row at +6; D-60's "+ 6" note) — **one site missed: verification 8 (f3)** · cross-ticket agreement re-checked: `corpus.md` verifications 5 / 8 (`ok`, `2`) match this ticket's, `corpus.md`'s dark `import policy_tool` and narrowed `tests/README.md` row, `turn.md` verification 18 counting "system prompt" among the binding's egress bytes, and D-37 / D-55 / D-59 / D-60 all agree with this ticket's rows · Landmines block names every `docs/landmines.md` §1 zone (entered: PHI handling; not touched: Auth/sessions, IDOR, Domain-internal, ROI, Inline eligibility, Booking race, Duplicate patients, HL7, Secrets, Migrations/schema, Intake registration) plus the structural exclusion and residual honesty · falsified-claims sweep over `docs/landmines.md`, `docs/debt-log.md` (D13), `docs/phi-logging-policy.md` (rules 3/4/5/6 and the register `:92`), `docs/todo.md` (TODO-9/10/12/60), `CLAUDE.md`, `ARCHITECTURE.md`, `docs/runbook.md`, `docs/onboarding-seam-map.md`, `tests/README.md`, ADR 0004 / 0006 / 0009 / 0011 and in-code markers on and near the edited lines: no clause falsified and unserved (phi-policy rule 4's `llm_client.py` pointer is not an exhaustive site list; ADR 0009's exact-`client.messages.create` seam clause and ADR 0011:373 "no tools exposed" both survive this ticket's dark diff) · wrong facts: 0 · 3 findings (1 receive-half change-list/verification gap on SPEC-24, 1 Landmines self-consistency, 1 stale test count).

### Gate — round 6, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` with the uncommitted working tree), no characterization received.
Run alongside the `corpus` gate (two agents, independent reads). Origin tags derived by the
agent from the record — round 5's `checked:` plan-text hash (`7bddb340…`) and the
`Sites changed:` list on all three round-5 dispositions — not from recall. **Round-3 rule
fires:** both open findings are `class-repeat`, so the owner adjudicates each (accept as
named residual · overrule · change the spec) before stage 3 dispositions them. f2 is minor.
Plan returns to stage 3 after adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-24 · Changes (7 rows) · `services/ai-assistant/agent_binding.py` row · Landmines PHI (1) ADR 0004 sentence | **class-repeat** — same class as r3 f3 ("registry upkeep missing: this ticket lands a new tracked artifact and carries no registry row"), whose sweep enumerated *new test files vs `tests/README.md` rows* only, so the ADR sits in the same class, unswept. This ticket's change list carries **no `adr/0019-*.md` row**, while the ADR `corpus` lands is titled for this ticket's mechanism — `adr/0019-model-directed-policy-retrieval-on-the-invoke-model-seam.md` (`corpus.md:27`), per eligibility-assistant-D-42 whose content list opens "framework on the `invoke_model` seam" — i.e. `corpus` authors the record of `agent_binding.py` before that file exists, and the three later tickets each append their own paragraph (`trace.md:24`, `lifecycle.md:20`, `retrieval-eval.md:18`, "eligibility-assistant-D-42 pattern"). Consequence beyond bookkeeping: the r4 f2 **twin guard** — a new fail-closed control and a new log site on the estate's only vendor-egress path, and the answer to ADR 0004's "must justify any bypass" (`adr/0004:57`) — exists only in this ticket's Landmines block, and the title line says this plan file is *deleted at its merge*; eligibility-assistant-D-42's own rationale is "a decision a review round could reopen needs a record to point at", and eligibility-assistant-D-60 forbids a ticket resting an entry on a sibling file. Either add the `adr/0019-*.md` row here (seam binding, `SystemMessage` → `system`, twin guard + ADR 0004 justification, never-stream) or state where the justification lives after the plan file is deleted. **Class is cross-ticket:** `turn.md` likewise carries no ADR row for the bounded loop / outcome derivation eligibility-assistant-D-42 assigns to the ADR; a disposition closes it across `llm-seam`, `turn` and `corpus.md:27`. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** First option taken, at all three sites the finding names: this ticket's change list gains an `adr/0019-*.md` row — the seam-binding paragraph (`SeamChatModel` as the framework's only egress through `_call`, `SystemMessage` → `system`, the post-egress twin guard and its `llm call` line as the ADR 0004 `:57` justification, the raising `_stream`) — and the Landmines PHI (1) ADR 0004 sentence now says the justification lives there after this file is deleted; `turn.md` gains its own `adr/0019-*.md` row (bounded loop, once-guarded payer call, `spend_stop`, outcome derivation — the eligibility-assistant-D-42 items its diff lands); `corpus.md`'s `adr/0019-*.md` row now states that `corpus` authors the frame and the retriever / index / tier paragraph only, each later ticket appending its own. Register: eligibility-assistant-D-42 gains a dated note recording the per-ticket authorship under the eligibility-assistant-D-55 split (record only, no rule change; consistent with eligibility-assistant-D-60 — no ticket's entry rests on a sibling file). No test count moves; no code file changes. Class (r3 f3 — a new tracked artifact with no registry row) closed with an enumerated sweep of this ticket's new artifacts against every registry the falsified-claims rule names, plus the ADR: `services/ai-assistant/agent_binding.py` → `adr/0019-*.md` (**row added**), `docs/onboarding-seam-map.md` (no `llm_client` / `agent_binding` mention — `grep` this session; the six named seams do not include the model binding, nothing to update), `ARCHITECTURE.md` / `docs/runbook.md` (no `llm_client` mention — `grep` this session; nothing to update), `docs/debt-log.md` D13 and `docs/phi-logging-policy.md` rule 4 (name `llm_client.py` as the wrapper; the binding routes through it, so both hold verbatim); `tests/test_a1_trace.py` → `tests/README.md` row (`:19`, holds) and `CLAUDE.md` §6 row (`:20`, holds); the five new cases in `tests/test_llm_client.py` → an existing file, no registry row (holds). Cross-ticket ADR rows: `trace.md:24`, `lifecycle.md:20`, `retrieval-eval.md:18` (present, hold), `turn.md` (**added**), `corpus.md` (**amended**). **Sites changed:** `llm-seam.md` `adr/0019-*.md` change row (new), Landmines PHI (1) (ADR 0004 `:57` sentence); `turn.md` `adr/0019-*.md` change row (new, after the `docs/phi-logging-policy.md` row — shifts that file's later line cites by one: verification 18 `:71` → `:72`, the Secrets entry `:90` → `:91`; `corpus.md`'s r6 cells cite the post-insert numbers, history cells are not rewritten) and Status line (dated bracket); `corpus.md` `adr/0019-*.md` change row (authorship clause); `plans/eligibility-assistant.md` eligibility-assistant-D-42 (dated note). |
| 2 | SPEC-24 · Landmines PHI (1) twin-guard sentence ("pinned equal to `complete()`'s") · `tests/test_llm_client.py` row (`test_a1_binding_guard_parity`) · verification 7h | **class-repeat** — same class as r2 f1 / r4 f1 / r5 f1 ("a planned mechanism with no check that reddens if it is wrong"). The block claims the binding "emits the same metadata-only `llm call model=… in_tokens=… out_tokens=… cost=… latency=… request_id=…` line … **pinned equal to `complete()`'s** by `tests/test_llm_client.py::test_a1_binding_guard_parity`". The test as specified asserts only "exactly one record containing `llm call model=` with `in_tokens=` / `out_tokens=` equal to the body's usage, and no record containing the human or system text" — nothing compares the binding's line to `_result_from_response`'s format string (`llm_client.py:606-614`; `log.info` at `:606`, format at `:607`), and verification 7h's break deletes the log wholesale. A binding that emits `llm call model=… in_tokens=… out_tokens=…` and drops `cost=` / `latency=` / `request_id=` passes 7h and every other check — and those three are exactly the fields the control is defined by (`adr/0004:41-44` "metadata only: model, tokens, cost, latency, request id"; `docs/phi-logging-policy.md` rule 4, same list), which is what the ADR 0004 `:57` justification in this block rests on. Either assert the record against the same format (all six fields, or a built expected string) with its own break, or soften the claim to what the test pins. Minor. | **Owner 2026-08-27: accepted → stage 3.** **Disposition 2026-08-27 — A.** First option taken — the test asserts the format, not just its presence: `test_a1_binding_guard_parity` now matches the binding's one `llm call` record against the six-field pattern `^llm call model=\S+ in_tokens=\d+ out_tokens=\d+ cost=\$\d+\.\d{4} latency=\d+\.\d{2}s request_id=\S+$` (the `llm_client.py:607` format string rendered; `cost` checked equal to `estimate_cost(in, out)` at `:323`, `request_id` to the scripted body's `id`, `latency` by shape since it is measured) and, under the same `caplog`, matches `complete()`'s record against the **same** pattern — parity is one pattern over both callers, so a binding line missing `cost=` / `latency=` / `request_id=` reddens while `complete()`'s line still matches and the failure attributes to the binding. Verification 7h gains that as its second break; the Landmines twin-guard sentence and the "lands dark" sentence now say what is pinned (all six fields, no human or system text). No test count moves (assertions added to an existing planned test); `_result_from_response` stays untouched in code — no format constant is extracted, the pattern is written in the test — so the `llm_client.py` row's "untouched in code" claim holds. Class (r2 f1 / r4 f1 / r5 f1 — a planned mechanism with no check that reddens if wrong) closed with an enumerated sweep of every mechanism the Landmines PHI (1) / (2) paragraphs claim, each against its reddening check: single `_call` egress → verification 7; `SystemMessage` → `system` and its position rules → 7g / `test_a1_binding_system_message_rules`; `max_tokens` = `settings.llm_max_output_tokens` → asserted on the round-trip body (holds); `stop` refused → `test_a1_binding_system_message_rules` (holds); tool definitions in `extra_body` → 7d; `tool_result` + role merge → 7e; `tool_calls` / `stop_reason` back → 7c; text → `content` → 7i; twin-guard content-present and integer-usage checks → 7h first break; `llm call` line **format** → 7h second break (**now covered**); additive `_adapt` → `test_non_text_content_block_raises_through_adapter` (existing); `_stream` raises and no streaming reference → 7b. Twelve claims, twelve checks, one added. **Sites changed:** `llm-seam.md` `tests/test_llm_client.py` row (`test_a1_binding_guard_parity` assertions), verification 7h (second break), Landmines PHI (1) (twin-guard sentence; "lands dark" sentence). |

Verified and **not** a finding, for the record: the design is otherwise sound, re-verified
line by line against the working tree. `_adapt` (`llm_client.py:192`, blocks `:204-207`)
keeps only `type`/`text` and carries no `stop_reason`, so E-4 delta (i) is real and the
superset additive — `_result_from_response` (`:566`, comment `:576-577`, guards `:579-583`
/ `:591-595`, log `:606-614`, reached only from `complete()` `:650-654` and
`complete_structured()` `:701-705`) stays green for
`test_non_text_content_block_raises_through_adapter` (`tests/test_llm_client.py:683`).
`_call` (`:471-476`) runs `_enforce_char_cap` → `max_input_tokens` → `_enforce_budget` →
`_require_bearer_token` (`:493-496`) before its single egress (`:509`) and raises
`LLMConfigError(egressed=False)` at `:454`; `extra_body` sits inside both the char cap and
the byte gate (`_extra_body_text:340`, `max_input_tokens:351`, system term `:368`,
`str(content)` `:371`) and merges into the Bedrock body at `:245-248` with `body["system"]`
a distinct key at `:244`. The **r4 f1 package locator this gate could not verify last round
verifies now**: `langchain_core` 1.6.0 `language_models/chat_models.py:2198-2204` is exactly
the abstract `_generate(self, messages, stop=None, run_manager=None, **kwargs) -> ChatResult`
(wheel extracted read-only outside the repo). SPEC-30's mechanism is non-vacuous against the
pinned library: no `invoke_model_with_response_stream` reference exists under
`services/ai-assistant/`, and an **overridden** `_stream` flips `_should_stream`'s
`sync_not_implemented` to False (`chat_models.py:547-556`), so `stream()` (`:716-724`) and
every affirmative trigger route into `_stream` and raise rather than silently falling back
to `invoke` — the raise is reachable, not dead code. Two notes deliberately not raised: the
plan describes `_generate` as returning "the `AIMessage`" where the abstract signature
returns `ChatResult` (the field-level enumeration is what the tests assert, and `invoke`
yields that message); and `_llm_type` — an `@abstractmethod` at `chat_models.py:2319-2320`,
without which `SeamChatModel()` cannot instantiate — is unnamed in the change row, but its
absence fails loudly on the first test, not silently. Also clean: `langchain_core` defers
its `langsmith` import (`callbacks/manager.py:2426`, `runnables/config.py:192` — both
function-local), so the dark `import agent_binding` does not falsify the `langsmith` comment
at `services/ai-assistant/requirements.txt:6-8`.

Per-SPEC verdicts: SPEC-24 **FINDING** (f1, f2) · SPEC-30 satisfied.

checked: eligibility-assistant-SPEC-24, -30 (the ticket `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, the ticket's Changes (7 rows), Verification 5 / 6 / 7 / 7f / 7b / 7c / 7d / 7e / 7g / 7h / 7i / 8 / 9 / 19, Gate interactions (3 bullets), Landmines block, Findings r1–r5 · **plan-text hash read: `7760a47475d6d9b9e8c39f9606bb2c3a9ffe61f5`** (round 5 recorded `7bddb340bf171d251ad3cce7b3b0f06d222fd240`; all three r5 dispositions carry `Sites changed:` lists, so tags are settled from the record) · **origin: 0 new · 0 pre-existing · 2 class-repeat** · spawning prompt = item + ticket only, no branch and no characterization (read `main` with the uncommitted working tree) · check map: 66/66 frozen rows carry exactly one `test:` / `cmd:` / `gate:` token (scripted over the `## Spec` table; 0 rows with 0 or >1, 0 duplicate ids) · ⚠ coverage: SPEC-30 → eligibility-assistant-D-56 with date; SPEC-24 not ⚠ · freeze scope: 0 occurrences of `DEFERRED` anywhere in the contract, both rows on current REQ-6‴ / REQ-8‴ · ownership (scripted from the six `Scope:` lines vs the contract `## Delivery` table): 66/66 rows owned exactly once — corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4 (SPEC-57 never allocated); tables agree row-for-row, no orphan, no double-own; `Depends on:` corpus none · llm-seam corpus · turn corpus, llm-seam · trace turn · lifecycle corpus, turn · retrieval-eval corpus · cites: every SPEC (7, 24, 30, 37, 40), decision (D-9, 34, 37, 43, 53, 55, 56, 59, 60) and evidence (E-4, E-5, E-7) id in the file resolves · change list closes both ways (SPEC-24 → `llm_client.py` / `agent_binding.py` / `tests/test_llm_client.py` / `app.py`; SPEC-30 → `agent_binding.py` / `tests/test_a1_trace.py`; `tests/README.md` → registry upkeep + D-60; `CLAUDE.md` §6 → D-43 + falsified-claims sweep) — **one un-served registry: `adr/0019-*.md` (f1)** · facts verified in the working tree: `llm_client.py` `_adapt`:192 / blocks:204-207 / `_BedrockMessages.create`:229 / `body["system"]`:244 / extra_body merge:245-248 / `invoke_model` body:249-254 / `_resolve_pricing`:287 / `estimate_cost`:323 / `_extra_body_text`:340 / `max_input_tokens`:351 with system term :368 and `str(content)`:371 / `_enforce_char_cap`:392 / `_require_bearer_token`:428-455 / `egressed=False`:454 / `_enforce_budget`:458 / `_call`:471-476, controls :493-496, egress :509 / `_result_from_response`:566, comment :576-577, guards :579-583 and :591-595, log :606-614 / `_trace_metadata`:618 / `complete()` max_tokens :650 / `wrap_create`:730; `config.py:64` 20000, `:65` `llm_max_output_tokens`; `tests/test_llm_client.py` rig :33-43, autouse bearer :46-52 (fixture body :47), `_stub_runtime_returning`:659 / setattr :669, no-content body :673-680, tool_use case :683, no-usage body :697-703, caplog idiom :788-797; `tests/test_ai_visit_chat.py` pin rig :50-72; `tests/conftest.py::load_module`:16-26; `app.py` imports :30-36 with `import llm_client`:33 and `complete_structured(system=_VISIT_SYSTEM_PROMPT)`:798-801; `services/ai-assistant/agent_binding.py` and `tests/test_a1_trace.py` absent (NEW claims true); no `langchain*` / `langgraph` / `BaseChatModel` in any tracked non-doc file, `services/ai-assistant/requirements.txt` and root `requirements-dev.txt` carry `langsmith==0.10.5` only; no streaming API reference under `services/ai-assistant/`; `ci.yml` services install :73 / `import app` :76, tests :86, gitleaks v8.18.4 :120-129; `tests/README.md:32` "AI paths"; `CLAUDE.md:141` baseline `1317 passed, 19 deselected, 1 xfailed`; `adr/0004:38-40` typed failures, `:41-44` metadata list, `:57` "must justify any bypass"; `docs/landmines.md:87` intake row; `docs/phi-logging-policy.md` rule 4 · **package locators**: `langchain_core==1.6.0` extracted wheel read read-only — `_generate`:2197-2204, `_stream`:2244, `_llm_type` abstract :2319-2320, `bind_tools`:2355, `_should_stream`:537-572, `stream`:716-790, langsmith imports function-local · r5 disposition sites re-read and holding: receive-half enumeration in the `agent_binding.py` row, round-trip `content == "Looking that up."`, guard-parity `content == ""`, verification 7i, the Landmines two-senses "lands dark" paragraph and the corrected residuals sentence, verification 8 at six (count sites: `CLAUDE.md` row six, verification 8 six, verification 9 `+ 6`, D-60 note `+ 6` — consistent) · falsified-claims sweep over `docs/landmines.md`, `docs/debt-log.md` (D13), `docs/phi-logging-policy.md` (rules 3/4/5/6 + register), `docs/todo.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `docs/runbook.md`, `docs/onboarding-seam-map.md`, `tests/README.md`, ADR 0004 / 0006 / 0009 / 0011, plus in-code markers on and near the edited lines and `services/ai-assistant/requirements.txt:6-8`, searched for `_adapt` · `invoke_model` · `llm_client` · `complete_structured` · `_result_from_response` · stream/streaming · langsmith: no clause falsified and unserved (the `llm_client.py:576-577` comment has its change row; the requirements comment survives on the deferred-import fact above) · wrong facts: 0 · 2 findings (1 missing design record, 1 telemetry-parity claim the named test does not pin).

### Gate — round 7, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket only (no branch
named; agent read `main` @ `762f0fd` with the uncommitted working tree), no characterization
received. Run alongside the `corpus` r7 and `turn` / `lifecycle` / `trace` gates (independent
agents). Non-workflow tree unchanged since round 6 (`git diff --stat HEAD -- . ':!docs/workflow'`
empty), so every tree locator re-verified is against the same code r6 read. Origin tag
derived by the agent from the record — round 6's `checked:` plan-text hash (`7760a474…`)
and the `Sites changed:` lists on both round-6 dispositions. **Round-3 rule fires:** the one
open finding is `class-repeat`, so the owner adjudicates (accept as named residual ·
overrule · change the spec) before stage 3 dispositions it. Plan returns to stage 3 after
adjudication; spec unchanged.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-24 · `tests/test_llm_client.py` change row (`test_a1_binding_round_trip_through_seam`, return-side assertion) · `agent_binding.py` row (receive half, `tool_calls`) · verification 7c | **class-repeat** — same failure class as r3 f1 ("self-consistency: the plan's own new test cannot do what the plan says it does"), this time against the pinned package rather than the rig. The row asserts `AIMessage.tool_calls[0] == {"id": "toolu_1", "name": "policy_lookup", "args": {"topic": "eligibility-verification"}}`. On `langchain-core==1.6.0` (the pin `corpus` lands, eligibility-assistant-D-59) that equality is false by construction: `AIMessage`'s `mode="before"` validator `_backwards_compat_tool_calls` (`langchain_core/messages/ai.py:309`; normalisation `:329-337`) rebuilds every `tool_calls` entry through `tool_call()` (`langchain_core/messages/tool.py:242-258`), which returns `ToolCall(name=…, args=…, id=…, type="tool_call")` — `ToolCall` is a `TypedDict` (`tool.py:206`) with `type: NotRequired[Literal["tool_call"]]` (`:238`) — so `.tool_calls[0]` is a four-key dict and the three-key literal never equals it. As specified, the test reddens on a *correct* binding, and 7c's second break ("`tool_calls` empty") is indistinguishable from that always-red state. Assert the three fields individually (`["id"]`, `["name"]`, `["args"]`) or compare against the four-key form, and name the package-supplied `type: "tool_call"` key in the receive-half enumeration. Origin derivation from the record, if the class match is declined: the clause dates from the r2 disposition and was in place at r6's hash `7760a474…` — r5 f1's `Sites changed:` scopes its edit of this row to the `content` assertion and r6 f2's to the `test_a1_binding_guard_parity` assertions — so the fallback tag is `pre-existing`, never `new` | **Owner 2026-08-27: accepted → stage 3.** Resolution chosen: **assert the fields individually** — `test_a1_binding_round_trip_through_seam`'s return-side assertion asserts `tool_calls[0]["id"]`, `["name"]` and `["args"]` separately rather than one equality against a three-key dict; the package-supplied `type: "tool_call"` key is named in the receive-half enumeration of the `agent_binding.py` change row; and verification 7c's second break is re-written so it is distinguishable from the always-red state the old assertion would have produced. **Disposition 2026-08-27 — A.** Package facts re-derived this session against the pinned `langchain-core==1.6.0` (the prior session's read-only wheel extraction outside the repo; `langchain_core-1.6.0.dist-info/METADATA` `Version: 1.6.0` — nothing in the tree touched, no dependency added). Every locator kept is confirmed, with one correction: `AIMessage._backwards_compat_tool_calls` is the `@model_validator(mode="before")` at `messages/ai.py:307-309` (the finding's `:309` is its `def` line — true), `create_tool_call` is `tool_call` imported at `ai.py:28`, and the normalisation block is **`:331-338`** (`# Ensure "type" is properly set…` `:331`, `create_tool_call(**{k: v for k, v in tc.items() if k not in {"type", "extras"}})` `:334-336`, `for tc in tool_calls` `:337`, `]` `:338`) — the finding's `:329-337` starts two lines high, on the `logger.debug` of the preceding `except`; `tool_call()` `messages/tool.py:242-258` returns `ToolCall(name=…, args=…, id=…, type="tool_call")` (`:258`); `ToolCall` is a `TypedDict` at `:206` with `type: NotRequired[Literal["tool_call"]]` at `:238`. So `.tool_calls[0]` is a four-key dict and the three-key equality is false on a *correct* binding — the finding holds as stated. Fix, three sites: the round-trip assertion becomes `len(tool_calls) == 1` plus `["id"]` / `["name"]` / `["args"]` per field; the `agent_binding.py` receive-half enumeration names the package rebuild and that the block's `input` lands under `args`, and states the per-field rule for every `tool_calls` assertion in this ticket; verification 7c's second break restores the `tool_use` block to today's `type`/`text`-only `_adapt` shape (`llm_client.py:204-207`) and states its red precisely — the receive-half tool-call assertions only (an `AttributeError` inside `_generate`, or `tool_calls == []` and an `IndexError` on `tool_calls[0]`), with every send-half assertion and `content == "Looking that up."` still passing and **the test green before the break**, which is exactly what the old wording could not claim. Self-consistency walked literally over the fix: the package's four-key entry satisfies three per-field assertions and the length check, so a correct binding is green, and 7c's second break is the only step that reddens those assertions. Class sweep (r3 f1 class — the plan's own new artifact cannot do what the plan says it does; here against the pinned package rather than the rig), enumerated over **every** assertion, break and expected-output claim this plan authors that rests on a package-normalised or framework-rebuilt shape. `llm-seam.md` `tests/test_llm_client.py` row: (1) round-trip `tool_calls[0] == {3 keys}` — **changed**; (2) round-trip `AIMessage.content == "Looking that up."` — holds, `content: str \| list[str \| dict[Any, Any]]` (`messages/base.py:103`) and `AIMessage`'s only `mode="before"` validator (`ai.py:307-309`) touches `tool_calls` / `invalid_tool_calls` / `tool_call_chunks` alone, so a `str` content is returned as given; (3) `response_metadata["stop_reason"] == "tool_use"` — holds, `response_metadata: dict[Any, Any] = Field(default_factory=dict)` (`messages/base.py:114`), no normaliser; (4) the round trip's send-half assertions (`body["system"]`, `body["max_tokens"]`, `tools[0]["name"]` / `input_schema`, alternating roles, the second call's `tool_result` / `tool_use_id`) — hold, every one asserts the JSON body the **binding** builds and the stub captures, never package output; the single package field the binding reads is `ToolMessage.tool_call_id` (`messages/tool.py:67`), present; (5) the direct `_adapt` assertions (`.id` / `.name` / `.input` / `.stop_reason`) — hold, our own `SimpleNamespace` (`llm_client.py:204-207` blocks, `:210-218` return); (6) `test_a1_binding_system_message_rules`' `invoke(..., stop=["x"])` — holds, `BaseChatModel.invoke(input, config=None, *, stop=None, **kwargs)` (`chat_models.py:464-471`) forwards `stop` into `_generate` (abstract `:2198-2204`), so the `ValueError` is reachable; (7) the same test's two raise cases and the `[HumanMessage]`-only no-`system`-key body — hold, binding-side; (8) `test_a1_binding_guard_parity`'s valid tool-only body → "an `AIMessage` with one `tool_call` and `content == \"\"`" — holds, a count claim plus an empty string, not a dict equality, on the same non-normalisation as (2); (9) the six-field `llm call` pattern over both callers — holds, `llm_client.py:607`'s own format; (10) guard-parity malformed bodies → `LLMResponseError` — holds, the twin guard is ours. `tests/test_a1_trace.py` row / 7b: (11) "`_stream` raises" is reachable, not dead code — holds, an overridden `_stream` makes `sync_not_implemented = type(self)._stream == BaseChatModel._stream` False (`chat_models.py:548`, read at `:552` / `:555` inside `_should_stream` `:538`), so `stream()` (`:716`) dispatches into `_stream` (abstract `:2244`); (12) the `invoke_model_with_response_stream` string scan — holds, a tree fact. `agent_binding.py` row's package premises: (13) `_generate(messages, stop=None, run_manager=None, **kwargs) -> ChatResult` `chat_models.py:2198-2204` — re-read this session, `def` `:2198`, `) -> ChatResult:` `:2204`, holds; (14) the `bind_tools` override (`chat_models.py:2355`) and the `StructuredTool` the round trip binds (`tools/structured.py:40`) — both present in 1.6.0; (15) `_llm_type`, the abstract property at `chat_models.py:2319-2320` unnamed in the row — checked, **not in this class**: an instantiation requirement, not an assertion on a rebuilt shape, and its absence fails loudly on the first test (r6 recorded it deliberately); unchanged. Verifications walked one by one: 5, 6, 7, 7f, 7d, 7e, 7g, 7h, 8, 9, 19 assert binding-side or tree-side output only (hold); 7b at (11)/(12); 7i's expected red (`content` is `""`) rests on (2)'s non-normalisation (holds); **7c second break — restated**. Cross-ticket text this ticket authors, re-read this session — `corpus.md:16` dark-import row, `:27` ADR authorship clause, `:28` `tests/README.md` row, `:60` Gate-interactions bullet 1; `turn.md:45` `adr/0019-*.md` row, `:46` `tests/README.md` row; the title lines of `corpus.md` / `turn.md` / `trace.md`; `plans/eligibility-assistant.md` D-34 / D-42 / D-60 dated notes — none carries an assertion, break or expected-output claim, so none is in the class. (`corpus.md:18`'s `StructuredTool.from_function` / `_parse_input` mechanism is the same class but is `corpus`'s own row, already closed at that ticket's gate r1 f4 against this wheel; not this ticket's to re-disposition.) **Fifteen package-touching claims plus the fourteen verification steps checked; one claim changed, one break restated.** Gate interaction: none moves — no CI step, no cap, no count; no test is added, only assertions, so the count sites stay mutually consistent at six (`CLAUDE.md` §6 row, verification 8, verification 9 `+ 6`, D-60's `+ 6` note — all re-read this session). Residual honesty: coverage widens, nothing narrows; residuals (a) and (b) unchanged. Falsified-claims sweep: the edits change no tracked registry claim (`grep -rn 'tool_calls\[0\]' docs/` this session → four hits, all in `llm-seam.md`: `:15` and `:28` changed, `:290` / `:397` are history cells left as written). No new decision: the fix is a mechanical correction to an assertion and its change-row enumeration under the existing rows; D-66 stays the high-water mark. **Sites changed:** `llm-seam.md` Status line (r7 bracket), `services/ai-assistant/agent_binding.py` change row (receive-half `tool_calls` enumeration), `tests/test_llm_client.py` change row (round-trip return-side assertion), verification 7c (second break), Landmines PHI (1) (send/receive-halves sentence — gate-cite list and the per-field clause). |

Wheel locators read from a prior session's read-only extraction of the pinned wheel (scratchpad `lcx/langchain_core`, `METADATA` Version 1.6.0); nothing in the repo was touched.

Per-SPEC verdicts: **SPEC-24 FINDING** (f1); everything else re-verified line by line in the working tree — `_adapt` `:192` (blocks `:204-207`, `type`/`text` only, no `stop_reason`) so E-4 delta (i) is real and the superset additive; `_BedrockMessages.create` `:229`, `body["system"]` `:244`, `extra_body` merge `:245-248`, `invoke_model` `:249-254`; module client built without a network call `:274-284`; `estimate_cost` `:323`; `_extra_body_text` `:340`; `max_input_tokens` `:351`, system term `:368`, `str(content)` `:371`; `_input_char_count` `:380-389` also stringifies list content; `_require_bearer_token` `:428-455`, `egressed=False` `:454`; `_enforce_budget` `:458`; `_call` `:471-476`, controls `:493-496`, single egress `:509`, returns the adapted response raw; `_result_from_response` `:566`, comment `:576-577`, guards `:579-583` / `:591-595`, log format `:606-614`, reached only from `complete()` `:653` / `complete_structured()` `:701`; `complete()` `max_tokens` source `:650`; `wrap_create` `:730`; `config.py:64-65`. Test-side: rig `:33-43`, autouse bearer `:46-52`, `_stub_runtime_returning` `:659`/`:669` (its `ResponseMetadata.RequestId` makes `request_id=\S+` hold for the `complete()` half), malformed bodies `:673-680` / `:696-703`, `:683` tool_use case, caplog idiom `:788-797`, 54 tests; `tests/test_ai_visit_chat.py:51-72` pin idiom; `tests/conftest.py:16-26`. `app.py:30-36` imports (`:33` `import llm_client`), `:798-801`. `ci.yml` `:73`/`:76`, `:86`, `:120-129`; `CLAUDE.md:141`; `tests/README.md:32`; `docs/landmines.md:87`; `adr/0004:38-40`, `:41-44`, `:57`; `requirements.txt:9` / `requirements-dev.txt:13` langsmith only. `_generate` abstract signature `chat_models.py:2197-2204` confirmed. Residual (a) written and matches E-4/E-5. **SPEC-30 satisfied** — ⚠ covered by eligibility-assistant-D-56 (SPEC-28–31, owner 2026-08-25, registered 2026-08-26), cited with date in this ticket's own block; mechanism non-vacuous (zero `*response_stream` references under `services/ai-assistant/`); §3 negative check complete (7b, both halves); residual (b) written down.

Noted, not findings: (i) `llm-seam.md:172` (r3 f3 disposition cell) cites `retrieval-eval.md:18` for that file's `tests/README.md` row; the row is at `:19` (the `adr/0019-*.md` row now sits at `:18`) — a history cell shifted by a later insert, left per the item's recorded convention (corpus r5 f5 cell); (ii) `plans/eligibility-assistant.md:93` ticket table gives `turn` `depends on: corpus, llm-seam`, while `turn.md:5` and D-59's dated note say `corpus, llm-seam, retrieval-eval` — outside this ticket's checked set, raised by the `turn` gate (turn r2 f11); (iii) the `_adapt` text-only characterization is the one of six counted tests without a test id; the count claims (`CLAUDE.md` row, verifications 8/9, D-60 note) are mutually consistent at +6; (iv) ADR 0006:30 is a context sentence that survives a dark landing; ADR 0006:109 and ADR 0009:69 hold verbatim under this diff.

checked: eligibility-assistant-SPEC-24, -30 (the ticket `Scope:` line) against Decisions eligibility-assistant-D-1..65, Evidence E-1..7, the ticket's Changes (8 rows), Verification 5 / 6 / 7 / 7f / 7b / 7c / 7d / 7e / 7g / 7h / 7i / 8 / 9 / 19, Gate interactions (3 bullets), Landmines block (11 top-level bullets), Findings r1–r6 · **plan-text hash read: `git hash-object docs/workflow/plans/eligibility-assistant/llm-seam.md` → `dd4196cf955e3ca5bed10c0e19ec5e3dc7ec588e`** (round 6 recorded `7760a47475d6d9b9e8c39f9606bb2c3a9ffe61f5`; both r6 dispositions carry `Sites changed:` lists, so tags are settled from the record) · **origin: 0 new · 0 pre-existing · 1 class-repeat** · spawning prompt = item + ticket only, no branch and no characterization · check map: 66/66 frozen rows carry exactly one `test:`/`cmd:`/`gate:` (scripted over the `## Spec` table) · ⚠ coverage: SPEC-30 → D-56 with date; SPEC-24 not ⚠ · freeze scope: 0 occurrences of `DEFERRED` in the contract, both rows on current REQ-6‴ / REQ-8‴ · ownership (scripted, six `Scope:` lines vs the contract `## Delivery` table): 66/66 rows owned exactly once — corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4 (SPEC-57 never allocated), tables agree row-for-row, no orphan, no double-own; `Depends on:` corpus none · llm-seam corpus · retrieval-eval corpus · turn corpus, llm-seam, retrieval-eval (`turn.md:5`; item table `:93` stale, noted above) · trace turn · lifecycle corpus, turn · cites (scripted): every SPEC (7, 9, 17, 21, 24, 28, 30, 32, 37, 40, 57, 60), decision (D-1, 9, 34, 35, 37, 41, 42, 43, 48, 53, 55, 56, 59, 60) and evidence (E-1, 4, 5, 7) id in the file resolves; D-42's r6 f1 dated note and D-60's `+ 6` note present · change list closes both ways (SPEC-24 → `llm_client.py` / `agent_binding.py` / `tests/test_llm_client.py` / `app.py` / `adr/0019-*.md`; SPEC-30 → `agent_binding.py` / `tests/test_a1_trace.py` / `adr/0019-*.md`; `tests/README.md` → registry upkeep + D-60; `CLAUDE.md` §6 → D-43 + falsified-claims sweep) · cross-ticket rows re-read and holding: `corpus.md:27` ADR authorship clause and `:16` dark `import policy_tool`, `:28` README row narrowed, verifications 5/8 (`ok`, `2`); `turn.md:45` ADR row, `:46` README row (nine files), `:72` verification 18; `trace.md:17` / `:24`; `lifecycle.md:20-21`; `retrieval-eval.md:18-19` · r6 disposition sites re-read and holding: `adr/0019-*.md` change row, Landmines PHI (1) ADR 0004 `:57` sentence, `test_a1_binding_guard_parity` six-field pattern + `complete()` parity half, verification 7h second break, twin-guard and "lands dark" sentences · package locators verified read-only on the extracted `langchain-core==1.6.0` wheel: `_generate` `chat_models.py:2197-2204`; `AIMessage._backwards_compat_tool_calls` `messages/ai.py:309`, normalisation `:329-337`, `tool_calls` field `:170`; `tool_call()` `messages/tool.py:242-258`; `ToolCall` TypedDict `:206`, `type` key `:238` · Landmines block names every `docs/landmines.md` §1 zone (entered: PHI handling; not touched: Auth/sessions, IDOR, Domain-internal, ROI, Inline eligibility, Booking race, Duplicate patients, HL7, Secrets, Schema/migrations, Intake registration) plus structural exclusion and residual honesty · falsified-claims sweep over `docs/landmines.md`, `docs/debt-log.md`, `docs/phi-logging-policy.md` (rule 4 `:40`), `docs/todo.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `docs/runbook.md`, `docs/onboarding-seam-map.md:13`, `tests/README.md`, ADR 0004/0006/0009/0011 for `agent_binding` · `_result_from_response` · `_adapt` · `SeamChatModel` · `invoke_model_with_response_stream`: no clause falsified and unserved (the three `_result_from_response` hits at ADR 0006:30/:109 and ADR 0009:69 survive; `llm_client.py:576-577` has its change row) · wrong facts: 0 · 1 finding (the plan's own round-trip assertion cannot pass on the pinned package).

### Gate — round 8, 2026-08-27

Fresh-session gate (post-`/clear`; this session authored no part of this ticket's plan).
Adversarial read by `drift-gate-agent`, spawning prompt = item + ticket names only (branch
`main` @ `762f0fd` with the uncommitted working tree), no characterization received.
**Process change this wave (owner, 2026-08-27, cost):** two agents grouped by mechanism
instead of six — this ticket read in one context with `turn` and `trace`; `corpus` /
`lifecycle` / `retrieval-eval` in the other. **Clean — dry round; Plan stamped
`GATED 2026-08-27`** (Plan `Status:` line, this file's `Status:` line, contract
`## Delivery` ticket row). The one r7 disposition's `Sites changed:` list (Status line,
`agent_binding.py` row, `tests/test_llm_client.py` row, verification 7c, Landmines PHI (1))
re-read and holding: per-field `tool_calls` assertions, package `type: "tool_call"` key
named, 7c second break green-before-break. Package locators re-read on the pinned
`langchain-core==1.6.0` wheel (downloaded read-only to the session scratchpad; repo untouched).

Per-SPEC verdicts: SPEC-24 residual-named (residual (a): E-4 live leg on `langsmith 0.11.1`, repo pins `0.10.5`, E-5 offline only, live first at SPEC-17/32 — written in the block and matching E-4/E-5; binding-in-isolation scope stated; everything else re-verified against the tree and the wheel); SPEC-30 satisfied (⚠ covered by eligibility-assistant-D-56 — SPEC-28–31, owner 2026-08-25, registered 2026-08-26 — cited with date in this ticket's own block; mechanism non-vacuous: zero `invoke_model_with_response_stream` references under `services/ai-assistant/`, an overridden `_stream` flips `sync_not_implemented` at `chat_models.py:548` so `stream()` `:716` dispatches into it; §3 negative check 7b both halves; residual (b) written).

Noted, not findings: verification 7c's second break — the receive-half red is more likely an `AttributeError` inside the first `invoke` than an assertion failure, so "send-half assertions … all still pass" describes what the break isolates, not what pytest reports; red-then-green holds either way. The r3 f3 cell still cites `retrieval-eval.md:18` for that file's README row (now `:19`) — history cell, left per the item's convention (noted at r7).

checked: eligibility-assistant-SPEC-24, 30 (2 ids, the `llm-seam` `Scope:` line) against Decisions eligibility-assistant-D-1..79, Evidence E-1..7, `llm-seam.md` Changes (8 rows) / Verification 5, 6, 7, 7b–7i, 8, 9, 19 / Gate interactions (3) / Landmines (11 top-level bullets) / Findings r1–r7, the five sibling `Scope:` + `Depends on:` lines and the contract `## Delivery` table · **plan-text hash read: `d395b31522718f92ecf4456243d684457c9cb836`** (round 7 recorded `dd4196cf955e3ca5bed10c0e19ec5e3dc7ec588e`; the one r7 disposition carries its `Sites changed:` list) · **origin: 0 new · 0 pre-existing · 0 class-repeat** · check map 66/66 single-mechanism (scripted over the `## Spec` table) · ⚠ coverage: SPEC-30 → D-56 with date; SPEC-24 not ⚠ · freeze scope: 0 `DEFERRED`, both rows on REQ-6‴ / REQ-8‴ · ownership 66/66 once (`Scope:` lines = `## Delivery` table = item ticket table; corpus 7 · llm-seam 2 · turn 39 · trace 9 · lifecycle 5 · retrieval-eval 4; SPEC-57 unallocated); `Depends on:` lines and table cells agree row-for-row · cites: every SPEC / D-1..79 / E-1..7 id in plan text resolves (scripted) · change list closes both ways (SPEC-24 → `llm_client.py` / `agent_binding.py` / `tests/test_llm_client.py` / `app.py` / `adr/0019-*.md`; SPEC-30 → `agent_binding.py` / `tests/test_a1_trace.py` / `adr/0019-*.md`; `tests/README.md` → registry upkeep + D-60; `CLAUDE.md` §6 → D-43 + sweep) · **tree facts verified:** `services/ai-assistant/llm_client.py` `_adapt` :192 (blocks :204-207 `type`/`text` only; carries `id=payload.get("id") or request_id` and `model`, so the twin guard's `request_id="msg_parity"` claim holds) · `_BedrockMessages.create` :229, `body["system"]` :244, `extra_body` merge :245-248, `invoke_model` :249-254 · `estimate_cost` :323 · overheads :336-337 · `max_input_tokens` system term :368, `str(content)` :371 · `_require_bearer_token` :428, `egressed=False` :454 · `_enforce_budget` :458 · `_call` :471-476, controls :493-496 · `_result_from_response` :566, `request_id = getattr(response,"id")` :572, comment :576-577, no-text guard :578-583, int-usage guard :589-595, six-field log line :606-614 (pattern matches) · `complete()` max_tokens :650 / call :653, `complete_structured()` :701-705, `import tracing` + `wrap_create` :728-730 · `config.py:64` 20000 / `:65` `llm_max_output_tokens` · `tests/test_llm_client.py` rig :33-43, autouse bearer :46-52, `_stub_runtime_returning` :659 (`ResponseMetadata.RequestId`="req_stub_ok") / setattr :669, no-content body :673-680, tool_use case :683, no-usage body :696-703, caplog idiom :788-797, 54 tests · `tests/test_ai_visit_chat.py:51-73` pin idiom (pins `llm_client`) · `app.py:30-36` imports, `:33` `import llm_client` · `agent_binding.py` / `tests/test_a1_trace.py` absent (NEW true) · no `langchain*` / `BaseChatModel` / `langgraph` in any tracked non-doc file; `.venv` (py 3.12.13) has no `langchain_core`, `langsmith 0.10.5`; `requirements.txt:9` / `requirements-dev.txt:13` langsmith only · `ci.yml` services :72-76 (matrix includes gateway :58), tests :86, gitleaks v8.18.4 :120-129 · `tests/README.md:32` · `CLAUDE.md:141` · `adr/0004:38-40`, `:41-44`, `:57` · `corpus.md:16` dark import, `:24` rig row, `:27-28` ADR authorship / README row, `:60` bullet · **package locators verified on the 1.6.0 wheel:** `chat_models.py` `_generate` :2197-2204 (`-> ChatResult`), `_stream` :2244, `_llm_type` :2319-2320, `bind_tools` :2355, `_should_stream` :538 / `sync_not_implemented` :548 / :552 / :555, `stream` :716, `invoke(…, stop=None)` :464-471; `messages/ai.py:28` import, `tool_calls` field :170, validator :307-309, normalisation :331-338; `messages/tool.py:67` `tool_call_id`, `ToolCall` TypedDict :206, `type: NotRequired[Literal["tool_call"]]` :238, `tool_call()` :242-258 returning `type="tool_call"`; `messages/base.py:103` `content`, `:114` `response_metadata`; `callbacks/manager.py:2426` / `runnables/config.py:192` function-local langsmith imports; `tools/structured.py:40` · `grep -rn 'tool_calls\[0\]' docs/` → exactly the four `llm-seam.md` hits the r7 cell states · falsified-claims sweep (landmines / debt-log D13 / phi-policy rules 3–6 + register / todo / CLAUDE.md / ARCHITECTURE / runbook / seam-map:13 / tests README / ADR 0004-0006-0009-0011 / in-code markers) for `_adapt` · `invoke_model` · `llm_client` · `complete_structured` · `_result_from_response` · stream · langsmith: no clause falsified and unserved · wrong facts: 0 · **0 findings** — clean.

### Impl gate — round 1, 2026-08-30

Fresh-session gate (post-`/clear`; this session wrote no part of this branch). Adversarial
read by `impl-gate-agent`, spawning prompt = item + ticket + branch name only
(`feat/noref-eligibility-llm-seam` off `main` @5ae3df0, HEAD @0fc1039, 4 commits), no
characterization received. **Two findings — no stamp; branch returns to stage 4.** Both
re-verified in this session before writing: f1 by reading `agent_binding.py:206-270` and
running `AIMessage(content="", tool_calls=[{"id": "toolu_x", "name": "policy_lookup",
"args": None}])` in the `.venv` (repo untouched) → `pydantic_core.ValidationError:
tool_calls.0.args Input should be a valid dictionary [type=dict_type, input_value=None,
input_type=NoneType]`; f2 by reading the contract record row.

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | SPEC-24 ("typed errors apply") · `services/ai-assistant/agent_binding.py` `_guarded_message` (`:206-244`) and `_message_from_response` (`:258-270`) · `agent_binding.py` change row ("post-egress twin guard") · Landmines PHI (1) "carry only the request id on the error" | **new** (adjacent to drift r4 f2's twin-guard class, but that class was "mechanism with no reddening check"; this is a shape gap in the mechanism itself). The twin guard fails closed on two shapes only — no `text`/`tool_use` block, non-integer usage. A 200 body whose `tool_use` block lacks `name` or `input`, or carries a non-dict `input`, passes both checks (`_adapt`'s superset defaults the missing fields to `None`), the `llm call` line is emitted, then `AIMessage(tool_calls=[{… "args": None}])` raises pydantic `ValidationError` — not `LLMResponseError`, not any `LLMError`. Two consequences: (a) `turn`'s planned `LLMError → model_failure` mapping (eligibility-assistant-D-37) would not catch it, so a malformed 200 becomes an unhandled exception on the agent path instead of the fallback; (b) pydantic embeds `input_value=…` in the message, so a non-dict `input` puts a model-response fragment into the exception string, contrary to the block's "carry only the request id on the error". Closure is the class, not `args` alone: shape-check every field the receive half carries (`id` / `name` str, `input` dict) → `LLMResponseError(request_id)` before the log line, one negative test per field in `test_a1_binding_guard_parity`'s shape, and a 7h-style break; the ADR 0019 twin-guard sentence and Landmines PHI (1) then name three controls, not two. Owner alternative: record as a residual against `turn` by ID — but (b) is a PHI-handling gap on the egress path, so the fix here is the one that does not need reversing. | **A · fixed @15a6b9f.** Owner adjudicated 2026-08-30 in session: option A, fix on the branch, the D-56 approval re-granted for the twin guard's hardening (the Landmines block's "a change to the mechanisms named here re-opens the approval" clause). The class, not `args`: every field the receive half reads is shape-checked before the log line — `id` / `name` str and `input` dict on a `tool_use` block, `text` str on a text block — failing closed as `LLMResponseError(request_id)` whose message names the FIELD and nothing else, so (a) the failure is an `LLMError` that `turn`'s eligibility-assistant-D-37 mapping catches and (b) no response fragment reaches the exception string. Four negatives (one per field, each also asserting the typed error, the request-id-only message and that no `llm call` line was emitted) inside `test_a1_binding_guard_parity`, written red first. `adr/0019-*.md`'s twin-guard bullet and Landmines PHI (1) now say three controls, not two — four on `_call`, three by the twin. Verification 7h gains a third and fourth break (below). Suite unmoved: 1361 / 19 / 1 — the negatives are assertions inside an existing node, not a new test id. |
| 2 | Contract `## Delivery` llm-seam record row (`docs/workflow/eligibility-assistant.md:351`, "Residuals, registry IDs only") · ticket `## Delivery evidence` "Residuals" | **new** — record shape. Residual (b) reads "SPEC-30 proven for this service, not the estate → the `trace` ticket's rows"; a ticket name is not a registry ID. Name the `trace` SPEC row(s) (SPEC-28–31 per eligibility-assistant-D-56, or the exact subset) or a todo id, at both sites. | **A · fixed @2798064.** The exact subset, not the whole 28–31 range: eligibility-assistant-SPEC-28 / SPEC-29, whose "every emitted run payload" is the estate-wide obligation the gateway process falls under and which `trace` owns by its `Scope:` line. SPEC-31 (the register row) and SPEC-30 (this ticket's own) are not it. Written at both sites — contract record row and ticket Residuals. |

Noted, not findings: (i) `bind_tools(**kwargs)` passes `tool_choice` through `self.bind` into `_generate`'s kwargs, where it is silently ignored — plan-conformant ("of `**kwargs` reads only `tools`"; D-37 does not use `tool_choice`), but the binding refuses `stop` for exactly that "silently dropped knob" reason; `turn` should know. (ii) `plans/eligibility-assistant/trace.md` as committed on `main` still lists SPEC-30 in its `Scope:` line (pre-D-55 text) — not in this diff, `trace`'s file; the `llm-seam` `Scope:` line and the contract table agree on 24/30. (iii) the agent's line cites for f1 (`:330-395`) were wrong for a 271-line file; corrected above from the tree.

Mechanical half, all clean: SPEC-24 `test:` = `tests/test_llm_client.py::test_a1_binding_uses_seam_fail_closed`, SPEC-30 `test:` = `tests/test_a1_trace.py::test_model_call_not_streamed`, both already in the frozen table on `main`, both pass by node id; contract diff touches `## Delivery` tables only; `tests/test_llm_client.py` diff purely additive; contract 364/400 lines; no `cmd:` or `gate:` cells in scope (human observation required: none); no `Co-Authored-By` trailer on the 4 commits; no gateway route; one log site in `agent_binding.py`, metadata-only six-field line; §1 PHI-handling entry cites D-56 with date; V19 exclusion diff empty; `grep invoke_model_with_response_stream services/` empty. Planted-defect sweep clean (D13 untouched, `_result_from_response` code untouched, `test_non_text_content_block_raises_through_adapter` green; no debt-log / landmines §1 / phi-register artifact repaired). Change list closes both ways: 8 planned rows + 2 ceremony files in the diff, 0 untraceable. Delivery evidence present with isolation clause, 3 deviations (all confirmed in the diff), test-first split disclosed. V5/V6/V8/V19 re-run by the agent: `ok` / `10 passed, 50 deselected` / 2 / empty. Baseline: `make test-docker` **1361 passed, 19 deselected, 1 xfailed** (36.80s); `.venv` 3.12 agrees (26.23s); vs recorded 1355/19/1 at branch → +6 = the six added tests, deselected / xfailed unmoved.

checked: eligibility-assistant-SPEC-24, -30 (the `llm-seam` `Scope:` line = contract `## Delivery` table) against the GATED Plan (Changes 8 rows, Verification 5 / 6 / 7 / 7f / 7b–7i / 8 / 9 / 19, Gate interactions, Landmines block), the contract `## Delivery` record row and the ticket `## Delivery evidence` · diff `git diff main...HEAD --stat`: 10 files, +1314/−24 · pinned-test diff, contract cap, `cmd:`/`gate:` cells, trailers, planted defects, idiom sweep, closure both ways, baseline re-run (docker + venv) · **2 findings** (f1 SPEC-24 receive-half shape → untyped `ValidationError`, payload-bearing; f2 record shape, residual (b) not an ID) · no stamp.

### Adv review — round 1, 2026-08-30

`adv-reviewer-agent`, same spawning prompt (item + ticket + branch only, no characterization received), frozen `## Spec` + diff only, Plan unread. **Clean — dry round.** Traced send (`_split_system`, `_to_bedrock`, `_content_blocks`, `bind_tools` / `_anthropic_tool`) and receive (`_guarded_message`, `_message_from_response`) halves against SPEC-24 (single egress through `llm_client._call`, bearer / char-cap / budget gates cover the `extra_body` tool payload, zero egress with the bearer unset) and SPEC-30 (`_stream` raises, no `invoke_model_with_response_stream` reference in the service); `_adapt` confirmed a strict superset leaving `complete()` / `complete_structured()` unchanged; `app.py` import dark; ADR 0019 and `tests/README.md` prose match the code; suite re-run 1361/19/1. The agent did not surface Impl gate r1 f1 — its read stopped at the guard's parity with `_result_from_response`, which holds; the malformed-`tool_use` path is a gap the reference lacks too, so parity did not expose it.

checked: full frozen `## Spec` (SPEC-1–66) read, SPEC-24 / SPEC-30 as the rows the code bears on · diff 10 files (plan-file and Delivery-table changes excluded from review input) · 0 findings.

## Delivery evidence

Status: delivery DRAFT 2026-08-30. Deviations and live-run detail live here and die with this
file at merge; the references — shas, baseline movement, the test-first split, traceability,
residual IDs — are in the contract's per-ticket delivery record row.

Branch `feat/noref-eligibility-llm-seam` off `main` @5ae3df0 (`corpus` merged @1430ec3, so the
`Depends on:` edge is landed and the LangChain pins are on both requirements files already).

**Deviations (3, all plan-fact; no mechanism moved).**

1. The `CLAUDE.md` §6 change row cites `:141`, `1317 passed, 19 deselected, 1 xfailed`. `corpus`
   had already moved that line to `1355 passed, 19 deselected, 1 xfailed` (2026-08-29). The row's
   action is unchanged — the line is updated to this ticket's post-count — so it reads
   `1361 passed, 19 deselected, 1 xfailed`, measured 2026-08-30.
2. The `agent_binding.py` row says `bind_tools` → Anthropic `tools` in `extra_body` without naming
   the conversion route. `langchain-core==1.6.0` ships no Anthropic tool converter, only
   `convert_to_openai_function` / `convert_to_openai_tool` (`utils/function_calling.py:377`,
   `:515`). `_anthropic_tool` therefore normalises through `convert_to_openai_tool` and renames
   `parameters` → `input_schema`, rather than walking the tool object a second time. The bound
   payload and the byte-gate consequence are exactly what the row states.
3. The `adr/0019-*.md` row names the seam-binding paragraph only. Two `## Consequences` lines are
   added beside it — the new files, and the tests that now hold the line — because that section
   enumerates both and this diff would otherwise falsify it.

**Test-first split.** Four of the six added tests ran red before the code that turns them green:
`test_a1_adapt_superset_keeps_text_only_shape` (red: `AttributeError` on `content[0].id`),
`test_a1_binding_uses_seam_fail_closed` (red: `agent_binding.py` did not exist),
`test_a1_binding_system_message_rules` (red: `DID NOT RAISE ValueError`) and
`test_a1_binding_guard_parity` (red: `DID NOT RAISE LLMResponseError`). Two did **not**:
`test_a1_binding_round_trip_through_seam` and `tests/test_a1_trace.py::test_model_call_not_streamed`
were green on first run, because the single `agent_binding.py` write that made the fail-closed test
green already carried the leading-`SystemMessage` mapping, the receive half and the raising
`_stream` — writing the module in three partial states would have churned a file whose halves are
one design. The negative checks below are what actually pin those two: 7b, 7c, 7d, 7e, 7g-1, 7i
each redden them from a working tree and go green on revert.

**Planned slices absent from the diff: none.** Every change row landed.

**Impl gate round 1 (2026-08-30), two findings, both fixed on the branch** — dispositions and
their shas are in `## Findings`. f1 hardened the post-egress twin guard to three controls
(@15a6b9f), which re-opened and was re-granted the eligibility-assistant-D-56 §1 approval in
session; the mechanism did not move, its field coverage did. f2 was record shape only (@2798064).
Neither adds a test id: the suite stays `1361 passed, 19 deselected, 1 xfailed`.

**Verification (each command run, outcome stated). Isolation, all of it: every break and every
binding test runs against `_stub_runtime_returning`'s in-process boto3 stub with a fake bearer, so
no request leaves the process, no credential is read and no real service is involved; the import
smokes are keyless and offline (`env -i`).**

- **V5** `cd services/ai-assistant && env -i PATH="$PATH" ../../.venv/bin/python -c "import app,
  policy_index, policy_tool, agent_binding; print('ok')"` → `ok`. The `langchain_core` import is
  proven import-safe keyless and offline before any wiring.
- **V6** `.venv/bin/python -m pytest tests/test_llm_client.py tests/test_a1_trace.py -q -k "a1 or
  adapt or streamed"` → `10 passed, 50 deselected`.
- **V8** `.venv/bin/python -m pytest -m "not integration" --collect-only -q | grep -o
  'EVAL-0[0-9][0-9]\(-[a-z]*\)*' | sort -u | wc -l` → `2` (`EVAL-023`, `EVAL-031`), unchanged from
  `corpus`. No test this ticket adds is `[EVAL-n]`-parametrised.
- **V9** `make test-docker` → `1361 passed, 19 deselected, 1 xfailed`; `.venv` 3.12
  `pytest -m "not integration" -q` agrees. Baseline at branch was `1355 passed, 19 deselected,
  1 xfailed`, so passed grows by exactly the 6 tests this branch adds and deselected / xfailed do
  not move. No `skipped` term — the live tests land in `turn`.
- **V19** `git diff --stat main -- db/ config/ services/gateway/authz.py
  services/gateway/security.py services/eligibility-service/
  services/ai-assistant/eligibility_client.py docker-compose.yml eval/` → empty.

**Break-then-revert (13 breaks over 8 checks; every one red under the break and green on revert).**

- **7** `_generate` egresses through `llm_client.client.messages.create` directly, skipping
  `_call` → `test_a1_binding_uses_seam_fail_closed` red (the stub records a call and no
  `LLMConfigError` is raised: `_require_bearer_token` was bypassed).
- **7f** the `sys.modules["llm_client"] = llm_mod` pin dropped from the rig → all four binding
  tests red on their first assertion, `agent_binding.llm_client is llm_mod`, before any `invoke`.
- **7b** `_stream` yields one chunk → `test_model_call_not_streamed` red. Second break, an
  `invoke_model_with_response_stream` reference in `agent_binding.py` → the same test red.
- **7c** `_adapt` drops `stop_reason` → `test_a1_binding_round_trip_through_seam` red. Second
  break, the `tool_use` block restored to the old `type`/`text`-only shape → the same test red
  with `AttributeError: 'types.SimpleNamespace' object has no attribute 'id'` raised inside
  `_message_from_response` at `agent_binding.py:265` — the receive half, as the r7-restated
  wording requires, and the test was green before the break.
- **7d** `bind_tools` returns the model without binding `tools` → the same test red.
- **7e** the role-alternation merge dropped → the same test red. Second break, `ToolMessage`
  converted to a text block → the same test red.
- **7g** the leading `SystemMessage` folded into a `user` turn → the round-trip test red. Second
  break, a `SystemMessage` accepted at any position → `test_a1_binding_system_message_rules` red.
- **7h** the twin guard and its log call deleted → `test_a1_binding_guard_parity` red. Second
  break, the line emitted as `llm call model=%s in_tokens=%d out_tokens=%d` only → the same test
  red on the six-field pattern, which `complete()`'s record in the same test still matches.
- **7i** the `AIMessage` built with `content=""` regardless of the response's `text` blocks →
  the round-trip test red.
- **7h, third and fourth breaks (impl gate r1 f1, @15a6b9f).** The shape loop deleted from
  `_guarded_message` → `test_a1_binding_guard_parity` red on the first shape case
  (`DID NOT RAISE LLMResponseError` — a `tool_use` block with no `id` becomes a tool call with
  `id=None` and no error at all). Second break, the loop kept but `input`'s `dict` check dropped →
  the same test red with `pydantic_core.ValidationError: tool_calls.0.args Input should be a valid
  dictionary [type=dict_type, input_value='member Jane Doe 123-45-6789'…]` — the untyped,
  payload-bearing failure the finding named, reproduced under the break and gone on revert.

**Traceability.** Both `test:` cells in the frozen spec are filled with the ids they already
planned and neither moved: eligibility-assistant-SPEC-24 →
`tests/test_llm_client.py::test_a1_binding_uses_seam_fail_closed`; eligibility-assistant-SPEC-30 →
`tests/test_a1_trace.py::test_model_call_not_streamed`. No pinned test changed.

**Residuals.** The two the Landmines block already names, as registry IDs only: the live
composition under `langsmith==0.10.5` → eligibility-assistant-SPEC-17 / SPEC-32, E-5 (D13 stays
OPEN in `docs/debt-log.md`, neither narrowed nor fixed); SPEC-30 proven for this service, not the
estate → eligibility-assistant-SPEC-28 / SPEC-29, the `trace` rows whose "every emitted run
payload" spans the gateway process this ticket does not reach (impl gate r1 f2: a ticket name is
not a registry ID). No new residual surfaced, so nothing is filed in
`docs/debt-log.md` or `docs/todo.md` by this ticket.
