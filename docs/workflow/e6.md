# e6 — small-defect batch

Status: plan GATED 2026-08-16 · delivery DRAFT 2026-08-16
Item: engagement-sourced defect batch — record-search corpus bound, patient-name-filter
bound, booking-helper connection leak; plus the migration runner queued from e5b
(e6-D-5, E6-REQ-9, added 2026-08-14)
Baseline at branch: 1300 passed, 5 deselected, 1 xfailed (`make test-docker`, 2026-08-16)

> Converted 2026-08-12 from the five-file `e6/requirements.md` draft (2026-08-10) per the
> owner's one-file adoption decision of 2026-08-12; content unchanged, restated in the
> `docs/workflow/README.md` §Layout shape. Re-homed 2026-08-15 from the parked copy
> (`~/Documents/Work/process-plans/e5b-restart/e6-parked-2026-08-13.md`) after e5b merged;
> code cites re-verified against `main` @3b3b26c, one refreshed (gateway search route).

## Decisions

| ID | Stage | Decision | Why (one sentence) |
|---|---|---|---|
| e6-D-1 | req | Batch of fixes only; the instrumented fast-track variant was recommended and not taken (owner 2026-08-10) | TODO-27's lesson is that a lane is decided on a measurement, and the owner chose not to buy the measurement — the fast-track question stays open and unevidenced, and needs a `docs/todo.md` home at landing to survive |
| e6-D-2 | req | D8 indexing is OUT (owner 2026-08-10) | `db/migrations/009_duplicate_review_queue.sql:11-13` records the zero-index condition as deliberate — "already in the register" is not the entry bar for a defect batch, "not deliberate" is; stays open as D8 |
| e6-D-3 | req | The search bound is an independent defect, not a narrowing of D11 (owner 2026-08-10) | the D11 IDOR fix stays W4-sized (`docs/todo.md` TODO-20); e6 closes only the unbounded read |
| e6-D-4 | req | A capped response must signal truncation (owner 2026-08-10) | a silent cap on a clinical search makes "no more matches" indistinguishable from "matches withheld" — a patient-safety failure mode of its own |
| e6-D-5 | req | The queued `schema-apply` lands here as a **real migration runner** (a `schema_migrations` ledger table, ordered apply of `db/migrations/*.sql`, applied files never re-run), not as a re-run of `db/schema.sql` (owner 2026-08-14) | table-level `IF NOT EXISTS` no-ops any table that already exists, so the column-add migrations (004/006/007/008) never repair on an old volume — the re-run retires the debt-log "No migration runner" headline while leaving half the class open, and it makes the hand-synced `schema.sql` the upgrade executable, so a sync drift would silently corrupt upgrades instead of only breaking fresh volumes; supersedes the PR #76 r6 out-of-scope call for the runner |
| e6-D-6 | req | e6 branches off current `main` (@3b3b26c); nothing in flight blocks it (owner 2026-08-15) | e5b and the mechanical batches (#80–82) are merged and `main` is clean — resolves the sequencing question the parked draft held open |
| e6-D-7 | spec | The portal records-search surface renders the truncation signal (owner 2026-08-16) | the surface exists (`frontend/app/records/page.tsx`) and a withheld-results signal nobody sees fails e6-D-4's intent — resolves E6-REQ-8's recorded rendering question in-scope (TODO-44 lesson) |
| e6-D-8 | spec | The migrations≡schema.sql parity check is a frozen SPEC row (owner 2026-08-16) | replaces the dead schema.sql-re-run mechanics from the e5b harvest with a pin on the hand-sync itself, so a `db/schema.sql`↔`db/migrations/` drift breaks CI instead of silently corrupting upgrades — resolves E6-REQ-9's recorded spec-stage question |
| e6-D-9 | spec | `db/schema.sql` pre-stamps the migration ledger: it creates `schema_migrations` pre-filled with every migration it flattens (owner 2026-08-16) | a schema.sql-built volume has no ledger, so the runner's first invocation would re-run 001–010 and fail on the first bare `CREATE TABLE`; pre-stamping makes fresh volumes born current and keeps the both-files schema-change rule the single sync point — resolves review finding F1 |
| e6-D-10 | spec | e6-SPEC-14 schema equivalence is full structural: tables, columns, types, nullability, defaults, PK/FK/UNIQUE constraints; indexes excluded by name (owner 2026-08-16) | a weaker pin lets a missed NOT NULL or FK drift corrupt upgrades exactly the way e6-D-5 warns; indexes are excluded because zero exist anywhere and the zero-index condition is deliberate D8 (verified in-tree 2026-08-16: all `CREATE INDEX` mentions are comments) — resolves F2 |
| e6-D-11 | spec | e6-SPEC-9's "documented" half is checked by a `gate:` clause on the runbook upgrade section, not pretended into the `test:` (owner 2026-08-16) | a test pins runner behavior, not documentation; the skill rule is out-of-test properties get `gate:` with the observation recorded in Delivery — resolves F3 |
| e6-D-12 | spec | A failed migration leaves none of its changes applied — per-file atomicity is required, not excluded (owner 2026-08-16) | Postgres DDL is transactional so the property is cheap, and without it a half-applied file makes re-run re-execute its first half — a known trap inside the §1 migrations zone — resolves F4 |
| e6-D-13 | plan | e6-SPEC-6 is satisfied by a minimal search UI on the existing records page (owner 2026-08-16) | plan-stage read found no portal UI calls records search — only the pass-through `frontend/app/api/records/search/route.ts` exists — so e6-D-7's "surface exists" held for the page, not a search surface; a search box + hits list + withheld banner on the existing page satisfies the frozen row without a new surface or a spec change |
| e6-D-14 | plan | On a ledger-less database that already has tables, the runner refuses with an instructive error; a documented stamp-only `baseline` mode records already-flattened migrations without running them (owner 2026-08-16) | migrations are bare DDL (zero `IF NOT EXISTS`), so a pre-e6 volume would crash the runner at 001; refuse-plus-baseline is deterministic where an auto-probe is a heuristic on exactly the half-applied states that matter — the one-time legacy step is an accepted residual, named in the Landmines block |
| e6-D-15 | plan | Runner tests land integration-marked in `tests/integration/`; a new CI job with a postgres:15 service container runs them, and a new `make test-migrations` target mirrors it locally (owner 2026-08-16) | the CI `tests` job has no Postgres and deselects `integration`, so without a new job the migration `test:` pins would never run in CI; the deselected count moves deliberately and is enumerated in Delivery |
| e6-D-16 | plan | The bounded search response gets `contracts/records-search.json`, asserted from both suites like `contracts/intake-registration.json` (owner 2026-08-16) | the response-shape change is a §2 public-contract change consumed across the python/TS boundary; the contract-file-plus-two-assertions pattern is the mechanism the repo already has for exactly this failure class |
| e6-D-17 | plan | `db/migrations/011_schema_migrations.sql` ships the ledger DDL alongside the `db/schema.sql` edit (owner 2026-08-16, with plan approval) | the both-files rule (`CLAUDE.md` §9) is literal; the runner also self-creates the ledger (`IF NOT EXISTS`) so 011 is a no-op wherever the runner arrived first, and the parity test pins all three copies together |
| e6-D-18 | plan | The documented command is `make migrate`, running `db/migrate.py` in a compose tool-profile service with no published ports (owner 2026-08-16, with plan approval) | host Python is 3.8 without psycopg2, so the runner needs a container; a profile keeps it out of `make up`, and expose-only keeps `tests/test_compose_topology.py` green |
| e6-D-19 | plan | `baseline` verifies before stamping: it stamps the full on-disk `db/migrations/*.sql` set only after the live schema structurally matches a scratch-database build of that same set (the e6-SPEC-14 comparison, ledger contents excluded); on mismatch it refuses with an instructive error and stamps nothing (owner 2026-08-16, with plan approval) | stamp-only `baseline` masks the column-add subclass on a behind-head legacy volume — the ledger would read current while e6-SPEC-13's guarantee is false; verification turns that silent mask into a refusal, and the scratch build reuses the runner's own apply path so no second schema source is introduced — resolves gate r1 finding 1 |
| e6-D-20 | plan | The `book.py` §1 zone entry is approved by the owner's ⚠ human-gate agreement on E6-REQ-3 at Requirements AGREED, confined to connection lifecycle per e6-SPEC-8 (owner 2026-08-16, recorded with plan approval) | the Landmines block rule requires the approving owner act cited by decision ID and date, and no earlier register row carried the E6-REQ-3 act — resolves gate r1 finding 3 |

## Requirements

Status: AGREED 2026-08-16 (owner, in session)
Source: engagement owner ask, 2026-08-10 — three questions, verbatim: "which debt log
items are the easiest wins?" · "do we have a track for such a process?" · "ponder the
idea of creating a faster track for small, and defined defects". Answer taken: batch,
don't branch (e6-D-1). Engagement-sourced, not a client ask (`docs/workflow/README.md`
§Ground rules); no COO message behind it and none invented.

**The register entries are the requirement** — mechanism and business risk live in
`docs/debt-log.md` D11 and D5b and are not restated here. The three defects are
independent (dropping one changes nothing about the others' fix shape or test surface),
which is what makes one item with one spec legitimate rather than three items wearing one
header. These are planted-defect-adjacent fixes (`CLAUDE.md` §0): the standing risk is
silently closing an exercise, and `docs/landmines.md` §3 treats a moved coverage gap as a
finding. E6-REQ-9 (added 2026-08-14) is the fourth independent item and the exception to
that framing: a main-true operational gap, not planted-defect-adjacent — its risk is the
§1 migrations zone, not a curriculum exercise.

| ID | Requirement | Notes |
|----|-------------|-------|
| E6-REQ-1 | A logged-in holder of `records.search` cannot retrieve the entire record corpus in a single request — neither by supplying LIKE metacharacters as the search term nor by relying on an unbounded result set | ⚠ human-gate — PHI read path, `docs/landmines.md` §1 IDOR zone, §3 negative-test rule. Independent defect per e6-D-3. Site: `records-service/app.py:339` (un-escaped `f"%{q}%"`, no LIMIT), reachable end to end via gateway `:343` (cite refreshed 2026-08-15); nothing under `eval/` calls the route (verified 2026-08-10), so a bound cannot move the drift-gate baseline |
| E6-REQ-2 | The patient-name filter on the patient list is subject to the same property as E6-REQ-1 | ⚠ human-gate — same zone, same mechanism at `records-service/app.py:51-53`; fixing one site and not the other leaves the exposure open |
| E6-REQ-3 | The scheduling booking helpers release their database connection on every path, including when the query or insert raises | D5b trailing note: `book.py` `slot_taken` (`:31`) and `insert_appointment` (`:53`) close on success only. Must not alter the check-then-insert race, the missing `slot_id` UNIQUE, or the missing idempotency key — RIV-175, seeded and load-bearing; `book.py` is a §1 zone for it |
| ~~E6-REQ-4~~ | ~~FK-shaped lookups on the chart-load path are index-backed~~ | WITHDRAWN 2026-08-10 (e6-D-2) |
| E6-REQ-5 | Every seeded defect and deliberate coverage gap that e6 touches remains visible after e6 — the suite baseline moves only by deliberate addition, and any moved gap is reported rather than absorbed | the defect-batch constraint; baseline number lives in the header only |
| E6-REQ-6 | Anything e6 leaves unclosed on a defect it touches is recorded as a named successor entry before e6 merges | the anti-narrowing rule: a batch of small fixes is structurally a partial-fix machine |
| ~~E6-REQ-7~~ | ~~The engagement can decide the fast-track question from a recorded per-stage cost of this item~~ | WITHDRAWN 2026-08-10 (e6-D-1; the consequence is in that decision's Why) |
| E6-REQ-9 | A database volume created before any given migration reaches the current schema through one documented command; migrations already applied are recorded and never re-run, and the command never seeds data | ⚠ human-gate — `docs/landmines.md` §1 migrations zone. Shape fixed by e6-D-5 (runner + ledger, not schema.sql re-run). This is e5b-SPEC-25's documented e6 exclusion arriving (queue-marker in `docs/workflow/e5b.md`); closes the debt-log cross-cutting "No migration runner" row in full, column-add subclass included. Harvest salvage (`~/Documents/Work/process-plans/e5b-restart/harvest.md`): `tests/test_schema_upgrade_path.py` cherry-picks with surgery (its hand-sync pins — every migration table/column reaches the flattened schema, apply-never-seeds — survive; its schema.sql-re-run mechanics don't); debt-log row + runbook "Upgrading a database that predates a migration" § both marked rescue-with-surgery there. A parity check (DB built from migrations ≡ DB built from `schema.sql`) is the spec-stage question that replaces the old re-run test |
| E6-REQ-8 | When a search response is bounded, the caller can tell that results were withheld — a truncated response is distinguishable from an exhausted one | rationale = e6-D-4. A result bound is a public-API contract change, flagged per `docs/landmines.md` §2, and E6-REQ-8 widens it (response shape changes for every caller). Whether the portal *renders* the signal is a live spec-stage question, not an omission (TODO-44 lesson) |

Out of scope: D11 session→patient bind (W4's, TODO-20) · the RIV-175 race itself
(seeded, curriculum week) · D8 indexes (e6-D-2) · N+1 encounter queries
(`records-service/app.py:95,145` — a design change, not indexing) · TODO-47 uvicorn `q`
logging (different layer, already in the phi-logging register) · a fast-track lane
(e6-D-1) · a `docs/landmines.md` §1 edit recording the search-bound/D11-sizing
distinction — owner's call was "independent defect" without the §1 edit, so the next
reader re-derives the distinction; deliberate, revisit if a gate flags it · a new UI
surface (E6-REQ-8's rendering question is the one live edge, recorded above) · D16
raw-stored `submission_id` (filed from e5b r8; reopening e5b-SPEC-18 is owner-only per
the debt-log row).

## Spec

Status: FROZEN 2026-08-16 (owner, in session; review pass findings F1–F4 resolved e6-D-9..D-12)

Check column: `test:` pinned test id (filled at impl 2026-08-16 from the delivered ids;
changes only by owner decision) · `cmd:` impl-gate runnable check · `gate:` human judgment at impl gate,
observation recorded in Delivery.

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| e6-SPEC-1 | When a records search request is received, the records search shall return at most a configured maximum number of hits | test: `tests/test_records_search_bounds.py::test_result_set_is_capped_and_flagged_truncated` | ⚠ E6-REQ-1; §3 negative-test rule inherited |
| e6-SPEC-2 | If the search term contains LIKE metacharacters, then the records search shall match them as literal characters | test: `tests/test_records_search_bounds.py::test_like_metacharacters_are_escaped_to_literals` + `::test_escape_helper_neutralizes_all_three_metacharacters` — negative case: `q=%` must not match the corpus | ⚠ E6-REQ-1 |
| e6-SPEC-3 | If the patient-name filter term contains LIKE metacharacters, then the patient list shall match them as literal characters | test: `tests/test_patient_filter_escape.py::test_name_filter_escapes_like_metacharacters` — negative case: `q=%` must not match the corpus | ⚠ E6-REQ-2 |
| e6-SPEC-4 | The patient list shall return at most a configured maximum number of patients per request | test: `tests/test_patient_filter_escape.py::test_caller_limit_is_applied_to_the_page_query` + `::test_limit_over_server_cap_is_rejected` — pins the existing bound (caller `limit`, server-side cap) so it cannot regress with the escaping change | ⚠ E6-REQ-2 |
| e6-SPEC-5 | When a records search response is bounded, the response shall indicate whether matches were withheld — a truncated response is distinguishable from an exhausted one | test: `tests/test_records_search_bounds.py::test_exact_fill_is_not_truncated` + `::test_under_fill_is_not_truncated` + `::test_response_model_declares_exactly_the_contract_fields`, and `frontend/app/records/search.contract.test.ts` (both suites assert `contracts/records-search.json`, e6-D-16). E6-REQ-8/e6-D-4. Response-shape change is a `docs/landmines.md` §2 public-contract change; whether it gets a `contracts/` declaration is plan-stage | E6-REQ-8 |
| e6-SPEC-6 | When the portal records-search surface receives a truncated response, it shall render an indication that results were withheld | test: `frontend/app/records/page.test.tsx` › "records search surface (e6-SPEC-6)" › "renders the withheld-results banner when the response is truncated" + "shows no withheld banner when the result set is exhausted". e6-D-7 | E6-REQ-8 |
| e6-SPEC-7 | If a booking-helper database operation raises, then the helper shall release its database connection before the error propagates | test: `tests/test_booking_conn_lifecycle.py::test_slot_taken_closes_connection_when_query_raises` + `::test_insert_appointment_closes_connection_when_insert_raises` | ⚠ E6-REQ-3, §1 zone (`book.py`, RIV-175) |
| e6-SPEC-8 | The booking flow's check-then-insert sequence, the absence of a `slot_id` uniqueness constraint, and the absence of an idempotency key shall be unchanged | gate: reviewer confirms the `book.py` diff is confined to connection lifecycle; RIV-175 stays seeded (`docs/landmines.md` §1 zone for it) | ⚠ E6-REQ-3, §1 zone (`book.py`, RIV-175) |
| e6-SPEC-9 | The migration runner shall be invocable as one command that brings a database volume created before any given migration to the current schema by applying `db/migrations/*.sql` in filename order | test: `tests/integration/test_migration_runner.py::test_apply_brings_an_empty_volume_to_current` | ⚠ E6-REQ-9, §1 migrations zone |
| e6-SPEC-10 | When a migration is applied, the runner shall record it in a ledger table, and a recorded migration shall never be re-run | test: `tests/integration/test_migration_runner.py::test_apply_records_every_migration_in_the_ledger` + `::test_a_recorded_migration_is_never_rerun` | ⚠ E6-REQ-9 |
| e6-SPEC-11 | If a migration file fails, then the runner shall not record it as applied, shall leave none of that migration's changes applied, and shall exit non-zero | test: `tests/integration/test_migration_runner.py::test_a_failing_file_records_nothing_and_leaves_no_partial_changes` + `::test_main_exits_nonzero_when_a_migration_fails` + `::test_a_failure_message_names_the_file_and_omits_driver_text` (impl gate r1 f4). e6-D-12 | ⚠ E6-REQ-9 |
| e6-SPEC-12 | The migration runner shall never write seed data | test: `tests/integration/test_schema_upgrade_path.py::test_the_runner_writes_no_seed_data` (e5b harvest pin, salvaged with surgery) | ⚠ E6-REQ-9 |
| e6-SPEC-13 | When the runner completes on a volume created before any given migration, every table and column introduced by any migration shall be present | test: `tests/integration/test_schema_upgrade_path.py::test_every_migration_column_reaches_the_upgraded_volume` (the harvest's surviving hand-sync pin, column-add subclass 004/006/007/008 included) | ⚠ E6-REQ-9 |
| e6-SPEC-14 | A database built by the runner from `db/migrations/*.sql` alone shall be schema-equivalent to a database built from `db/schema.sql` — equivalence covers tables, columns, types, nullability, defaults, and PK/FK/UNIQUE constraints; indexes excluded (D8) | test: `tests/integration/test_schema_upgrade_path.py::test_runner_build_is_schema_equivalent_to_schema_sql`. e6-D-8, e6-D-10 | ⚠ E6-REQ-9 |
| e6-SPEC-15 | The suite baseline shall move from the pinned count only by e6's deliberate additions, with any moved deliberate coverage gap reported as a finding | cmd: `make test-docker` → pinned baseline (`CLAUDE.md` §6) plus e6's additions; deltas enumerated in Delivery | E6-REQ-5 |
| e6-SPEC-16 | Before e6 merges, every defect e6 touches but does not close shall have a named entry in `docs/debt-log.md` or `docs/todo.md` | gate: registry check at impl gate, observation in Delivery | E6-REQ-6 |
| e6-SPEC-17 | When a database volume is initialized from `db/schema.sql`, the migration ledger shall record every migration that schema flattens, and the runner shall apply nothing on that volume | test: `tests/integration/test_schema_upgrade_path.py::test_schema_sql_volume_is_born_prestamped_and_runner_applies_nothing` + `::test_prestamp_set_equals_the_on_disk_migration_set`. e6-D-9 — closes the ledger-bootstrap gap; the both-files schema-change rule (`CLAUDE.md` §9) is the sync point | ⚠ E6-REQ-9, §1 migrations zone |
| e6-SPEC-18 | The runbook's "Upgrading a database that predates a migration" section shall name the runner invocation of e6-SPEC-9 as the single upgrade command | gate: doc check at impl gate, observation recorded in Delivery. e6-D-11; harvest marks the section rescue-with-surgery | ⚠ E6-REQ-9 |

Exclusions: D11 session→patient bind (W4, TODO-20, e6-D-3) · the RIV-175 race itself
(seeded; e6-SPEC-8 pins non-interference) · D8 indexes (e6-D-2) · the fast-track lane
(e6-D-1) · truncation signalling on the patient list — it already paginates with a total
count, so truncated and exhausted are already distinguishable there (e6-SPEC-4 pins the
bound only) · rollback/down-migrations — the runner applies forward only; nothing in
E6-REQ-9 asks for down paths, and inventing them widens the §1 migrations surface ·
D16 raw-stored `submission_id` (owner-only reopen, debt-log row).

## Plan

Status: GATED 2026-08-16

Changes (file level):
- `services/records-service/app.py` — search route: escape LIKE metacharacters, apply the
  configured cap with an over-fetch-by-one truncation probe, return `{hits, truncated}`;
  patient-name filter: same escaping, one local helper for both sites; the in-code D8
  marker (`:332-337` docstring + inline comment) is updated in the same edit — the
  no-limit claims drop, the "NO supporting index" D8 text survives verbatim (e6-SPEC-1/2/3/5;
  SPEC-4 pins the existing limit/offset bound unchanged; marker per gate r3 finding 1,
  e6-SPEC-16/E6-REQ-5)
- `services/records-service/config.py` — `records_search_max_hits` env knob, pattern of
  `relevant_records_max_items` (e6-SPEC-1)
- `services/records-service/schemas.py` — bounded-search response model (e6-SPEC-5)
- `contracts/records-search.json` — new declaration of the search response (e6-D-16)
- `tests/test_records_search_bounds.py` — new (e6-SPEC-1/2/5 + contract producer assert)
- `tests/test_patient_filter_escape.py` — new (e6-SPEC-3/4)
- `frontend/app/records/page.tsx` — minimal search UI: input, hits list, withheld banner,
  failed-state distinct from empty per the e5 idiom (e6-SPEC-6, e6-D-13)
- `frontend/app/lib/types.ts` — search response type (e6-SPEC-6)
- `frontend/app/records/page.test.tsx` — banner on `truncated:true`, absent on `false`,
  failure ≠ empty (e6-SPEC-6)
- `frontend/app/records/search.contract.test.ts` — new, consumer half of the contract,
  mirroring `frontend/app/intake/payload.contract.test.ts` (e6-D-16)
- `services/scheduling-service/book.py` — `try/finally` connection release in
  `slot_taken` and `insert_appointment`; nothing else in the file moves (e6-SPEC-7/8)
- `tests/test_booking_conn_lifecycle.py` — new (e6-SPEC-7; §3 negative-test rule)
- `db/migrate.py` — new runner: self-creates ledger, ordered apply of unrecorded
  `db/migrations/*.sql`, apply+record in one per-file transaction, non-zero exit on
  failure, refuses ledger-less non-empty DBs, verified `baseline` mode (stamps the
  on-disk set only after the live schema matches a scratch-database build of it,
  refuses otherwise), never reads seed files (e6-SPEC-9/10/11/12, e6-D-14/D-19)
- `db/schema.sql` — add `schema_migrations` + pre-stamp rows for every migration file
  including 011 (e6-SPEC-17, e6-D-9); the header comment's "no shared library / tooling
  yet" parenthetical (`:5-6`) is amended in the same edit to name the runner and parity
  test — the "kept in sync … by hand" clause survives verbatim (gate r4 class sweep)
- `db/migrations/011_schema_migrations.sql` — ledger DDL, both-files rule (e6-D-17)
- `docker-compose.yml` — `migrate` tool-profile service, expose-only (e6-D-18)
- `Makefile` — `migrate` and `test-migrations` targets, `##`-documented there only
  (e6-SPEC-9/18, e6-D-15/18)
- `tests/integration/test_migration_runner.py` — new (e6-SPEC-9/10/11; refusal case from
  e6-D-14; baseline cases from e6-D-19 — stamps on a matching volume, refuses and stamps
  nothing on a behind-head volume)
- `tests/integration/test_schema_upgrade_path.py` — new (e6-SPEC-12/13/14/17; e6-D-10
  equivalence scope; pre-stamp set ≡ on-disk migration set)
- `.github/workflows/ci.yml` — new `migrations` job: postgres:15 service container,
  runs the two integration files; `migrations` added to `docker-build`'s terminal
  `needs` fan-in so a red migrations job blocks merge, same reason frontend-boot and
  workflow-doc-cap are in the list (e6-D-15; gate r2 finding 1)
- `docs/runbook.md` — rewrite §"Upgrading a database that predates a migration" around
  `make migrate`; one-time legacy `baseline` paragraph states the verification, the
  refusal, and that a refused behind-head volume needs manual repair to head before
  `baseline` (e6-SPEC-18, e6-D-14/D-19)
- `docs/debt-log.md` — "No migration runner" row → CLOSED by e6 with the scope in the
  row text: closed for volumes born from e6 onward and for legacy volumes passing the
  verified `baseline`; a behind-head legacy volume is refused, its manual repair the
  named residual (gate r1 finding 2). D5b trailing connection-leak note updated, race
  text untouched. D11 row's exposure paragraph updated in the same PR: the un-escaped-`q`
  / no-LIMIT corpus-read vector is marked closed by e6 (dated), stale cites refreshed to
  the current sites (:339, :51-53), and the row stays OPEN — the session→patient bind and
  the id-walking exposure are untouched (e6-SPEC-16, e6-D-3; gate r2 finding 2)
- `docs/landmines.md` — §1 IDOR entry: the measured no-ids-required vector paragraph is
  updated in place — the un-escaped-`q` / bare-`%` whole-corpus / "unbounded by any
  `LIMIT`" claims are marked closed by e6 (dated) and the stale cites (`:48,:159`)
  refreshed to the current sites (`:339`, `:51-53`); everything else in the entry —
  session-not-patient-bound, id-walking, the whole-set sizing instruction — survives
  verbatim, and no sizing-distinction text is added (that §1 edit stays out of scope per
  Requirements, e6-D-3). §1 hand-sync entry: the "there is no migration runner" clause
  is replaced with the runner reference (`db/migrate.py`, `make migrate`); the hand-sync
  fact and the only-schema.sql-runs-on-a-fresh-volume clause survive verbatim,
  SPEC-14-pinned (e6-SPEC-16; gate r4 findings 1–2)
- `docs/todo.md` — fast-track question entry (e6-D-1); reserved ids 63–65 re-checked at
  landing per the collision rule
- `CLAUDE.md` — §2 map line "no runner (see §8)" corrected; §6 baseline restamped from
  the post-e6 measured run (e6-SPEC-15)

Landmine approvals (verbatim, never compressed):
- **PHI read path / IDOR zone (`docs/landmines.md` §1)** — the records search route and
  the patient-name filter are PHI read surfaces. Entry approved by the owner's ⚠
  human-gate agreement on E6-REQ-1 and E6-REQ-2 (Requirements AGREED 2026-08-16) and
  scoped by e6-D-3 (2026-08-10): e6 closes only the unbounded read and the metacharacter
  escape. Deliberate defects in reach are preserved: D11 session→patient binding stays
  open (W4, TODO-20), D8 zero-index stays open (e6-D-2), TODO-47 uvicorn query-string
  logging stays in the phi-logging register. Accepted residual: the truncation signal is
  rendered on the records-page search UI only; the patient list is excluded by the frozen
  spec because it already paginates with a total count.
- **Migrations zone (`docs/landmines.md` §1)** — `db/schema.sql`,
  `db/migrations/011_schema_migrations.sql`, and `db/migrate.py` enter the migrations
  zone. Entry approved by the owner's ⚠ human-gate agreement on E6-REQ-9 and by e6-D-5
  (2026-08-14); shape decisions e6-D-9/D-12/D-14/D-17 are owner acts of 2026-08-16. The
  schema edit adds only the ledger table and its pre-stamp rows; no PHI column is touched
  and no existing migration file is edited. Accepted residual: a volume created before e6
  has no ledger, and its one-command upgrade requires the documented one-time `baseline`
  stamp first (e6-D-14); `baseline` verifies the volume structurally matches the full
  migration set before stamping and refuses otherwise (e6-D-19), so a behind-head legacy
  volume is never falsely recorded as current — its manual repair to head before
  `baseline` is the accepted residual, stated in the runbook, and the debt-log row text
  carries this scope rather than a bare "CLOSED".
- **`book.py` (RIV-175 zone, `docs/landmines.md` §1)** — entry approved by the owner's
  ⚠ human-gate agreement on E6-REQ-3, recorded as e6-D-20 (2026-08-16). The diff is confined to
  connection lifecycle; the check-then-insert race, the absent `slot_id` UNIQUE, the
  absent idempotency key, and the never-written `Slot.status` second path all remain
  seeded and visible (e6-SPEC-8, gate-checked).
- Auth, ROI/disclosure logic, PHI columns, `.env`/secret files: none touched. The
  gateway (`services/gateway/app.py`, the standing wall) is not edited — the new response
  shape flows through the existing `_get_checked` proxy.

Verification (runnable, expected output stated):
1. `make test-docker ARGS="tests/test_records_search_bounds.py tests/test_patient_filter_escape.py tests/test_booking_conn_lifecycle.py -q"`
   → all pass; includes `q=%` returning no corpus-wide match (e6-SPEC-1/2/3/4/5/7)
2. `make test-migrations` → both integration files pass against an ephemeral postgres:15,
   including the e6-D-19 baseline cases: stamps on a matching volume, refuses on a
   behind-head volume (e6-SPEC-9/10/11/12/13/14/17)
3. `make test-docker` → pinned baseline (`CLAUDE.md` §6) plus e6's enumerated additions,
   deltas listed in Delivery (e6-SPEC-15)
4. `cd frontend && npm test` → records suite green incl. withheld-banner and contract
   consumer cases (e6-SPEC-6, e6-D-16)
5. `make config && make test-docker ARGS="tests/test_compose_topology.py -q"` →
   `compose OK`, topology green — the tool service publishes nothing (e6-D-18)
6. `make eval` → green, unchanged — nothing under `eval/` calls the search route
   (E6-REQ-1 note)
7. break-then-revert: reintroduce raw `f"%{q}%"` in the search route → escape case in
   `test_records_search_bounds.py` red; revert → green (e6-SPEC-2)
8. break-then-revert: delete one pre-stamp row from `db/schema.sql` →
   `test_schema_upgrade_path.py` pre-stamp-set case red; revert → green (e6-SPEC-17)
9. break-then-revert: drop one `finally` in `book.py` →
   `test_booking_conn_lifecycle.py` red; revert → green (e6-SPEC-7)
10. gate: fresh `make up` volume, then `make migrate` → "nothing to apply" (born
    stamped); observation recorded in Delivery (e6-SPEC-17, with the runbook check
    e6-SPEC-18 and the `book.py` diff-confinement check e6-SPEC-8)

Four checks (recorded):
- Self-consistency: 011 passes the runner (its DDL is the runner's own `IF NOT EXISTS`
  bootstrap, no-op where the runner arrived first); the pre-stamp set includes 011 so the
  parity and pre-stamp cases stay green; the contract file is asserted by both new
  tests; the compose tool service passes the topology allowlist (no published ports);
  the parity comparison is structural only, so differing ledger *contents* between the
  two builds do not fail it; `baseline`'s scratch build reuses the runner's own apply
  path and the e6-SPEC-14 comparison, so its reference is the same artifact the parity
  test pins — no second schema source (e6-D-19); the scratch database is created by the
  compose `POSTGRES_USER` (`riverbend_app`), which the official postgres image
  initializes as superuser, so `CREATEDB` needs no grant.
- Gate interaction: the new CI `migrations` job is independent of the `tests` job — the
  integration marker keeps the two disjoint, so no test runs twice and a migrations
  failure attributes to its own job; the job enters `docker-build`'s terminal `needs`
  fan-in (the list branch protection reads), so its red blocks merge rather than being
  advisory — the one deliberate `docker-build` edit, nothing else in that job moves;
  `workflow-doc-cap` caps this file at 400 lines (currently well under); `eval` is
  untouched by construction.
- Residual honesty: the legacy `baseline` step (now refusal-plus-manual-repair, e6-D-19),
  the debt-log row's scoped closure text, and the render-surface scope are named above;
  e6-SPEC-15's restamp happens in this PR from a measured run, not projected.
- Falsified-claims sweep: run in full after gate r4 (third instance of the class →
  closed as a class). Every registry named by the stage-3 skill swept by path, route,
  and behavior name for all four e6 behaviors (search bound/escape/truncation, patient
  filter, booking connection, runner/ledger/pre-stamp); every falsified clause has a
  change row, every surviving claim stays verbatim. Full site-by-site scope recorded in
  the r4 f2 disposition cell — not restated here.

## Findings

### Gate — round 1, 2026-08-16

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | e6-D-14 / e6-SPEC-13, e6-SPEC-9 | Stamp-only `baseline` masks the column-add subclass on legacy volumes: a pre-e6 volume behind head schema (the runbook's own path — schema.sql re-apply skips existing tables whole) gets its unapplied migrations recorded as applied with no verification the volume carries them, so the runner never repairs them and the ledger reads current; e6-SPEC-13's every-table-and-column guarantee fails on the baseline path and no planned test can see it — plan states neither what set `baseline` stamps nor any precondition (its own parity machinery could verify schema ≡ head before stamping) | e6-D-19 — `baseline` stamps the on-disk set only after structural verification against a scratch build, refuses otherwise; refusal/stamp cases added to `test_migration_runner.py`, runner/runbook change rows updated |
| 2 | E6-REQ-9 note vs Landmines migrations residual | Closure-scope contradiction: the REQ-9 note and the debt-log change list say the "No migration runner" row closes in full, column-add subclass included, while the migrations residual scopes closure to volumes born from e6 onward — the legacy-volume scope must be carried into the row text at landing, not absorbed by "CLOSED" | resolved via e6-D-19: the debt-log change row now carries the scope in the row text (post-e6 volumes + verified-baseline legacy volumes; refused behind-head volume = named manual-repair residual) and the migrations residual is restated to match |
| 3 | Landmines `book.py` entry / e6-SPEC-7, e6-SPEC-8 | Zone-entry approval cites no decision ID — only "owner's agreement on E6-REQ-3 (Requirements AGREED 2026-08-16)"; the other two blocks cite e6-D-3 and e6-D-5, and the block rule requires the owner act cited by decision ID and date | e6-D-20 records the E6-REQ-3 owner act in the register; the block now cites it by ID and date |

### Gate — round 2, 2026-08-16

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | e6-D-15 / plan `.github/workflows/ci.yml` row, gate-interaction check | The new `migrations` job is never wired into the terminal fan-in: `docker-build`'s `needs` list is what branch protection reads (`ci.yml:162`), and neither the change row nor the gate-interaction check adds `migrations` to it — a red `migrations` job does not block merge, so every migrations-zone `test:` pin (e6-SPEC-9–14, 17) is advisory in CI; the in-file precedent (frontend-boot, workflow-doc-cap both added to `needs` for exactly this reason) makes the omission a known failure class | plan fix under e6-D-15's intent (a `test:` pin that never gates is the failure the decision exists to prevent): ci.yml change row now adds `migrations` to `docker-build`'s `needs`, gate-interaction check names the wiring and confines the `docker-build` edit to that one list entry |
| 2 | e6-SPEC-16 / plan `docs/debt-log.md` row, e6-D-3 | The plan's debt-log edit names only the "No migration runner" row and the D5b trailing note, but e6's fix falsifies the D11 row's exposure paragraph (`docs/debt-log.md:288`: un-escaped `q`, "no LIMIT", "one request, whole corpus, no ids required" — plus stale line cites, current sites :339/:51-53) — landing without updating that row text violates the fix-wrong-claims-in-the-same-PR rule (`CLAUDE.md` preamble) and leaves the D11-sizing record describing a closed vector as open | plan fix under e6-SPEC-16/e6-D-3: debt-log change row now updates the D11 exposure paragraph in the same PR — vector marked closed by e6 (dated), cites refreshed to :339/:51-53, row stays OPEN with session→patient bind and id-walking untouched |

checked (compacted 2026-08-16, impl-gate r1 cap fix — verdicts kept, attestation
narration cut): 16 active rows, check map + ⚠ coverage complete with decision IDs,
freeze scope clean, cites and tree facts vs `main` @3b3b26c, r1 dispositions
consistent · 2 findings

### Gate — round 3, 2026-08-16

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | e6-SPEC-16 / plan `services/records-service/app.py` row, E6-REQ-5 | The in-code D8 marker at `services/records-service/app.py:332-337` ("no index, no limit (deliberate debt)") is falsified in its no-limit half by e6's own edit of those exact lines, and the change list names no marker update — same fix-wrong-claims-in-the-same-PR class as round-2 finding 2 (debt-log D11); the no-index half (D8 proper, e6-D-2) must survive verbatim | plan fix (owner 2026-08-16), under e6-SPEC-16/E6-REQ-5: the records-service change row now updates the marker in the same edit — no-limit claims drop, "NO supporting index" D8 text survives verbatim (D8 stays open, e6-D-2) |
| 2 | round-2 `checked:` line / spec table | Record arithmetic wrong against the frozen table: "16 active … test: 12, gate: 3, cmd: 1" vs 18 rows actual (test: 14 — SPEC-1–7, 9–14, 17; gate: 3 — SPEC-8/16/18; cmd: 1 — SPEC-15); minor same-class nit: round-2 finding 1 cites `ci.yml:162` for `docker-build`'s `needs`, actual line is 169 | correction-of-record (owner 2026-08-16): this finding's text is the correction; the round-2 stamped line stays untouched, no plan change |

Round-3 rule: loop stopped with open findings; owner decides per finding (accept as
named residual, overrule, or spec change), recorded in the disposition cells above.

Per-SPEC verdicts (round 3): SPEC-16 finding 1; SPEC-13 residual-named (e6-D-19);
16 rows satisfied; change-list closure both ways, verification runnable.

checked (compacted): 18 active rows, check map complete (test: 14, gate: 3, cmd: 1),
14 ⚠ rows across three Landmines blocks with decision IDs, freeze scope clean, cites
and tree facts vs @3b3b26c, r1–2 dispositions consistent; notable non-findings —
exactly two un-escaped ILIKE sites both covered, `page.test.tsx` correctly not new,
TODO 63–65 free, e5b.md:289 superseded wording is a stamped record not a live claim
· 2 findings

### Gate — round 4, 2026-08-16

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | e6-SPEC-16 / e6-SPEC-1, e6-SPEC-2; plan change list (no `docs/landmines.md` row) | `docs/landmines.md` §1 IDOR entry is falsified by e6's own fix and no change row updates it: "`q` reaches the search pattern un-escaped (`services/records-service/app.py:48,159`) … matches every row and returns full `Record.body` … unbounded by any `LIMIT`" — after e6-SPEC-1/2 land the vector is closed and the cites (:48,:159) are stale (current :339/:51-53); same fix-wrong-claims-in-the-same-PR class as round-2 f2 and round-3 f1, and the Requirements out-of-scope entry excludes only the sizing-distinction §1 edit, not this falsified factual claim | plan fix under e6-SPEC-16: new `docs/landmines.md` change row — vector clauses marked closed by e6 (dated), cites refreshed to :339/:51-53, all surviving text verbatim, no sizing edit (e6-D-3 out-of-scope entry untouched). Third instance of the class → closed as a class, not an instance: full falsified-claims sweep re-run this session, scope in f2's cell |
| 2 | e6-SPEC-16 / e6-SPEC-9; plan change list (same gap) | `docs/landmines.md` §1 hand-sync entry's "there is no migration runner" clause is falsified by e6-SPEC-9; plan corrects `CLAUDE.md` §2 and the debt-log row but not this line — hand-sync fact itself survives (SPEC-14-pinned), one-clause edit, but landing without it leaves the §1 source of truth describing the runner as absent | plan fix under e6-SPEC-16: same new change row replaces the clause with the runner reference; hand-sync and fresh-volume clauses survive verbatim. **Class-sweep scope (all registries, this session):** falsified sites all now have change rows — landmines §1 ×2 (this row), debt-log runner row / D5b note / D11 paragraph, runbook §"Upgrading…" (:53-90 incl. the :55 "no runner" line), `CLAUDE.md` §2/§6, in-code D8 marker (r3 f1), plus one new stale clause found and folded into the `db/schema.sql` row (header "no … tooling yet", `:5-6`). Checked and **not** falsified: `docs/onboarding-seam-map.md:26` (hand-sync + only-schema.sql-on-fresh-volume both survive — the runner is a separate command, not initdb), debt-log D8 `:287` ("full-table ILIKE … no index" survives — the cap bounds results, not the scan), phi-logging register `:89` ("book.py untouched" describes the 2026-08-05 fix's scope, still true), `ARCHITECTURE.md` `:70-71`/`:109`/`:113` (hand-rolled/hand-synced, RIV-175, full-table scans — all survive), `book.py` header (race claims only, untouched). `docs/review-loop-metrics.md:873/933` are stamped historical records, not live registries (r3 e5b.md:289 precedent) |

Per-SPEC verdicts (round 4): SPEC-16 findings 1–2; SPEC-13 residual-named (e6-D-19);
16 rows satisfied; change-list closure both ways, verification runnable.

checked (compacted): scope as round 3, re-verified cold against @3b3b26c (D8 marker,
gateway :343, book.py closes, bare-DDL migrations, ci.yml needs :169, landmines and
debt-log line cites); r1–3 dispositions consistent; notable non-findings — no existing
test pins the bare-list search response, the compose `migrate` service publishes
nothing, the CI `tests` job deselects `integration` with no Postgres (e6-D-15 premise
holds) · 2 findings

### Gate — round 5, 2026-08-16 (dry — plan stamped GATED)

checked (compacted): scope as rounds 3–4, all cites and tree facts re-verified cold
against @3b3b26c; r1–4 dispositions all present and consistent in final text (the r4
landmines change row covers both §1 clauses with survive-verbatim scoping); the
falsified-claims class sweep re-ran clean with no new site; the ledger design is
self-consistent on all three volume paths; SPEC-13 residual-named, 17 rows satisfied
· 0 findings

### Impl gate — round 1, 2026-08-16

| # | anchor | finding | disposition |
|---|--------|---------|-------------|
| 1 | plan change list / Delivery deviations | `db/Dockerfile` (new, 10 lines — the runner image) appears in no plan change row and is not disclosed as a Delivery deviation; e6-D-18 implies a container but names only `docker-compose.yml`/`Makefile` — change-list closure fails one way | |
| 2 | e6-SPEC-9 / E6-REQ-9, e6-D-18 | Stale-image hazard on the single documented upgrade command: `db/Dockerfile` COPYs `migrate.py` + `db/migrations/` at build time, and `make migrate` runs `docker compose run --rm migrate`, which never rebuilds an existing image — after pulling a change that adds migration 012, `make migrate` can report "nothing to apply" from the stale image while the DB stays behind, the exact silent-failure class E6-REQ-9 closes; fix shape: `--build` on the compose run, or mount `db/` | |
| 3 | spec table `test:` cells / Delivery "every `test:` cell filled" | Freeze-table cells never advanced from planned to final: all five `test:` cells still carry "(planned)" and e6-SPEC-6's cell says only "vitest, records page suite (planned)" with no concrete id (delivered: `frontend/app/records/page.test.tsx` "records search surface (e6-SPEC-6)" block) — fill-at-impl motion unexecuted on the artifact, contradicting the Delivery claim; the planned `.py` names do match the delivered files, so no pinned test moved without decision | |
| 4 | idiom sweep / `db/migrate.py:290` | `print(f"migration failed ({type(e).__name__}): {e}", …)` includes `str(e)`, deviating from the class-name-only exception idiom on a touched path; mitigants: operator CLI stderr not service logging, inputs are tracked DDL with no PHI values, runner never reads seed — disposition (accept-with-note or trim) is a stage-4/owner call | |
| 5 | working-tree state | Untracked `docs/workflow/w4.md` present in the checkout, outside the `main...HEAD` diff — confirm it is parallel-session work and not stray e6 output before push | |
| 6 | verification step 10 `gate:` | The step-10 live observation (fresh `make up` volume → `make migrate` → "nothing to apply") is uncollected: the volume-reset run was denied in this session, and a `gate:` row with no recordable observation is a finding — collect at re-gate | |

checked (compacted for the 400-line cap; verdicts and finding text intact): fresh-context
session, neither agent's spawn prompt characterized the work · impl-gate-agent —
pinned-test diff clean, cmd: e6-SPEC-15 `make test-docker` 1316/18/1 matching Delivery and
`CLAUDE.md` §6, frontend 119/119, planted defects undisturbed (RIV-175, D8, D11,
`Slot.status`, TODO-47), §1 zones covered with decision IDs, no Co-Authored-By trailers ·
adv-reviewer (Spec + diff only, never the plan): **clean, 0 findings** — SPEC-1..18 verified
live incl. `make test-migrations` 13/13 and compose topology 37 green · gate: observations
recorded — SPEC-8 (`book.py` diff is two `try/finally` wraps, RIV-175 still seeded), SPEC-16
(registry rows split closed-vs-open, TODO-63 filed), SPEC-18 (runbook names `make migrate`
as the single upgrade command); step 10 uncollected (finding 6) · 6 findings → no stamp,
branch back to stage 4; re-gate is a full fresh-run, both agents

## Delivery

Status: delivery DRAFT 2026-08-16 (impl gate r1 addressed, 2026-08-16)

Baseline (`make test-docker`) 1300→1317 passed (+16 unit across search-bounds /
patient-filter / booking, +1 compose-mount guard from impl-gate r1 f2), 5→19
deselected (+13 e6 integration, deliberate e6-D-15 — CI `migrations` /
`make test-migrations`; +1 runner failure-message negative test from r1 f4),
xfailed 1 unmoved. `CLAUDE.md` §6 restamped to 1317/19/1 in the same commit
(e6-SPEC-15). Test-first except CI/compose/Docker/Makefile/docs wiring (no seam);
every `test:` cell filled with its delivered id, no slice absent.
Deviations: the search-response guard + field constants live in
`frontend/app/records/search.ts` not `page.tsx` (Next forbids non-page route
exports; trivial, e6-D-13/16 unaffected) · `db/Dockerfile` is a new file no plan
row names — e6-D-18 decided a container, this is its mechanism (r1 f1).

Verification green, all ten steps: 1–6 and the three break-then-revert cases 7/8/9
reddened then reverted (raw `%{q}%`→escape; pre-stamp row→prestamp-set, parity
unaffected as ledger is excluded; `finally`→leak); runner isolation per-test scratch
DBs, real DB untouched. Step 10 collected live (E-1). Residuals (IDs only): behind-head
legacy manual repair — debt-log "No migration runner" + runbook; fast-track — TODO-63;
D11 id-walking — TODO-20.

**E-1 — step-10 live observation + stale-image refutation, 2026-08-16.** Local compose
dev volume only, owner-approved wipe, no real data: `docker compose down -v` → `make up`
(fresh `db/schema.sql`-born volume) → `make migrate` printed `nothing to apply — database
is current`, exit 0 — born stamped (e6-SPEC-17), runbook command works as documented
(e6-SPEC-18). Then on the same volume with **no image rebuild**, a probe
`db/migrations/012_*.sql` dropped into the tree made `make migrate` print `applied 012_…`
— the r1 f2 mount is live, not merely configured. Reverted: probe file deleted, table
dropped, ledger row deleted, `make migrate` back to `nothing to apply`, 11 rows (001–011).
