# w4 — patient-view assembly + IDOR finding

Status: plan GATED 2026-08-18 — re-planned 2026-08-17 (w4-D-19/w4-D-20; was GATED 2026-08-16, returned to stage 3 at impl-gate r4 finding-1), revised for gate r5 2026-08-18 (w4-D-30..33), split to contract + plan files 2026-08-18 (w4-D-34), revised for gate r6 2026-08-18 (w4-D-35/w4-D-36), gated 2026-08-18 (gate round 7 clean) · delivery MERGED f471b70 2026-08-18 (PR #87, codex 2 rounds → approve)
Item: knowledge-graph schema (patient→encounter→provider→record) + a retrieval prototype
that assembles a patient's full view behind an ownership check at the graph boundary, plus
the D11 IDOR finding writeup (HAR repro) and an N+1 note
Baseline at branch: 1317 passed, 19 deselected, 1 xfailed (`make test-docker`, 2026-08-17,
measured at branch cut 7cdc812; carried across the 2026-08-18 rebase onto `main` @3585cc5 —
window docs/tooling-only, w4-D-36)

## Requirements

Status: AGREED 2026-08-16 (source note corrected 2026-08-16, w4-D-14; REQ-5 line cites
corrected 2026-08-17, impl-gate r1 finding 2 — citation-level only)
Source: engagement owner ask, 2026-08-16 (Dr. Maya Okonkwo, COO — Riverbend Community
Health), quoted verbatim below, identifiers redacted per the stage rule. Patient ids in
the HAR (`1042`/`1043`) are seeded patient rows — the seed runs 255 patients, ids
1042–1851 — so the captured cross-patient read is against live seeded charts (w4-D-14).

> Patients keep calling to ask how to see their own labs and visit summaries — right now
> it's all scattered across services. Can you build something that assembles a patient's
> full picture in one view? The portal already has a records endpoint you can build on.
> Make it feel fast, some of the record pages are a bit sluggish today. I'll send the
> network capture our QA person grabbed off the current portal.
>
> WHAT THEY HANDED OVER
> API spec: `GET /patients/{id}/records` — auth required, but no check that the caller's
> token matches `{id}`; `{id}` is a sequential integer.
> QA capture `portal.har`: a logged-in patient (own id 1042) requests
> `/patients/1042/records` → 200, then requests `/patients/1043/records` → 200, returning
> another patient's labs.
> Query log for one patient-view assembly: `SELECT * FROM records WHERE encounter_id = ?`
> runs 37 times (one per encounter), plus a full-table scan on records search.
> Session config `auth.yaml`: `SESSION_TIMEOUT: never` — tokens issued at login don't
> expire.
> Page-load metric: assembled patient view p95 = 3.8s, almost all DB time.
>
> QUESTIONS TO DIG INTO
> In the HAR, change the patient ID in the URL — does the server stop you, or hand over
> the chart? How are patient IDs generated — could you guess the next valid one? Count the
> queries behind one patient view — does it hold up for a real chart with 50+ encounters?
> When a clinician walks away from a shared workstation, when does their session actually
> end?
>
> CURRENT PROBLEMS (STATED / KNOWN)
> Patients want their labs + visit summaries in one place. Some record pages are sluggish.
>
> THIS WEEK'S DELIVERABLE
> A knowledge-graph schema (patient→encounter→provider→record) + a small multi-agent
> retrieval prototype that assembles the patient view with an ownership/authorization check
> at the graph boundary + an IDOR finding writeup (repro from the HAR) and an N+1 note.
> Keep the KG to a seeded sample; the security finding is the headline. — note that
> 'multi-agent' ~= single agent if single agent is sufficient to achieve desired
> functionality. Currently no patient UI exists I believe.

| ID | Requirement | Notes |
|----|-------------|-------|
| w4-REQ-1 | The deliverable defines a knowledge-graph schema over a seeded sample corpus that models the `patient → encounter → provider → record` relationships needed to assemble one patient's view | seeded sample only (w4-D-1); the schema is the retrieval substrate, not a change to `db/schema.sql` |
| w4-REQ-2 | Given an authorized patient, the retrieval prototype assembles that patient's full view — their labs and visit summaries across all of their encounters — as a single result | the "one view / one place" ask; single-agent unless assembly needs more (w4-D-2). "Fast" is w4-REQ-7 |
| w4-REQ-3 | The prototype enforces an ownership/authorization check **at the graph boundary**: a caller may assemble only the patient they are authorized for, and a request for a different patient's graph is refused rather than served | ⚠ human-gate — the headline finding's demonstrated fix; the auth boundary is `docs/landmines.md` §1. No patient principal exists in `config/roles.yaml` today (TODO-20), so the prototype seeds/simulates the caller→patient binding rather than reusing gateway sessions |
| w4-REQ-4 | The IDOR finding writeup reproduces the cross-patient chart read from the handed-over capture — own id returns 200, a sibling id also returns 200 — and explains the two enablers: no session→patient bind on `GET /patients/{id}/records`, and sequential/guessable integer ids | ⚠ human-gate — this is D11 (`docs/landmines.md` §1, `docs/debt-log.md` D11, TODO-20). Evidence: `docs/handover/portal.har` (tracked, cite by path). The writeup sizes the whole exposure set per §1, incl. `/patients/{id}/relevant-records` and the e6-owned search path — it does not re-fix them |
| w4-REQ-5 | An N+1 note documents the production chart-assembly query pattern — one `records`-per-encounter query, ~37 for the captured chart, plus the full-table scan behind records search — and why it fails to hold for a real 50+-encounter chart | evidence: `services/records-service/app.py:128-129` (per-encounter loop, no JOIN/selectinload; the route at `:103-104`, whose docstring at `:112-113` already names D8) and the p95=3.8s metric. Documents the production route; the production-route N+1 *fix* is not w4's (see out-of-scope) |
| w4-REQ-6 | The writeup records the never-expiring-session finding — `auth.yaml` declares `SESSION_TIMEOUT: never` and the behaviour is hardcoded, so a token from a walked-away shared workstation never ends — as a secondary finding | ⚠ human-gate — auth §1; `services/gateway/auth.yaml:2` is declarative-only (nothing parses it), the real behaviour is `services/gateway/security.py:275-279`. **Finding only** per w4-Q4; the code fix is out of scope (§1 auth, unscheduled) |
| w4-REQ-7 | The prototype assembles the patient view without the per-encounter N+1 — a bounded number of retrievals independent of encounter count — so "one view" is demonstrably not the 37-query pattern it replaces | the "make it feel fast" ask, satisfied inside the prototype's own seeded substrate; distinct from w4-REQ-5, which documents the untouched production route |

Out of scope: the **live D11 fix on the production `/patients/{id}/records` route** — the
live session→patient bind lands later (w4-D-4, TODO-20, approval-gated §1) · the
search-corpus bound (e6's, e6-D-3 / E6-REQ-1/2/8) · the production-route N+1 fix (w4-D-5;
e6 out-of-scope also excludes it) · the session-TTL code fix (w4-D-7, §1 auth; finding only
per w4-REQ-6) · a rendered patient-facing portal UI (w4-D-6; TODO-20 blocks one — needs
patient authentication, a `config/roles.yaml` principal, and separate hostnames per
`adr/0015` §4) · patient authentication itself (TODO-20) · column-level PHI encryption
(`docs/debt-log.md` cross-cutting table / TODO-12, out of every weekly scope; w4-D-15).

## Spec

Status: FROZEN 2026-08-16 (open questions resolved as w4-D-8..10; citation-level
correction 2026-08-16, w4-D-15)

Check column: `test:` pinned test id (filled at impl, changes only by owner decision) ·
`cmd:` impl-gate runnable check · `gate:` human judgment at impl gate, observation
recorded in Delivery. "The prototype" = the retrieval prototype of w4-REQ-2; "the graph
boundary" = the single point at which the prototype admits a caller to a patient's
assembled graph. ⚠ rows inherit the `docs/landmines.md` §3 negative-test rule; §1
approval before code on the boundary-check rows.

**w4-REQ-1 — knowledge-graph schema over a seeded sample**

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| w4-SPEC-1 | The knowledge-graph schema shall model the `patient → encounter → provider → record` relationships sufficient to assemble one patient's labs and visit summaries | test: test_kg_prototype.py::test_kg_schema_shape | |
| w4-SPEC-2 | The prototype shall resolve every patient, encounter, provider, and record node from a self-contained seeded sample corpus, reading neither the production records tables nor the live records-service (w4-D-1) | test: test_kg_prototype.py::test_kg_self_contained (+ _positive_control) | |

**w4-REQ-2 — assemble one patient's full view as a single result**

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| w4-SPEC-3 | Given a patient the caller is authorized for, the prototype shall assemble that patient's labs and visit summaries across all of their encounters and return them as a single result | test: test_kg_prototype.py::test_assemble_full_view | |
| w4-SPEC-4 | The assembled result shall include every encounter belonging to the patient — no encounter of that patient is dropped from the view | test: test_kg_prototype.py::test_assemble_complete | |

**w4-REQ-3 — ownership/authorization check at the graph boundary**

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| w4-SPEC-5 | When a caller assembles the patient they are authorized for, the prototype shall serve that patient's view | test: test_kg_boundary.py::test_boundary_authorized_served | ⚠ |
| w4-SPEC-6 | If a caller requests assembly of a patient they are not authorized for, then the prototype shall refuse the request and return no node of that patient's graph — the adversarial cross-patient case (`docs/landmines.md` §3) | test: test_kg_boundary.py::test_boundary_cross_patient_refused | ⚠ |
| w4-SPEC-7 | The prototype shall evaluate the ownership check at the graph boundary — before traversing or returning any node — so refusal does not depend on what the sample contains | test: test_kg_boundary.py::test_boundary_before_traversal | ⚠ |
| w4-SPEC-8 | The prototype shall decide authorization against a seeded/simulated caller→patient binding it is given, not against a gateway session (no patient principal exists in `config/roles.yaml`, TODO-20) | test: test_kg_boundary.py::test_boundary_simulated_principal | ⚠ |

**w4-REQ-4 — IDOR finding writeup (repro from the handed-over capture)**

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| w4-SPEC-9 | The IDOR finding writeup shall reproduce, from `docs/handover/portal.har`, the cross-patient chart read — the caller's own id returns 200 and a sibling id also returns 200, returning another patient's records | gate: writeup repro reviewed at impl gate, observation in Delivery | ⚠ |
| w4-SPEC-10 | The writeup shall name both enablers — no session→patient bind on `GET /patients/{id}/records`, and sequential/guessable integer ids | gate: at impl gate, observation in Delivery | ⚠ |
| w4-SPEC-11 | The writeup shall size the full exposure set per `docs/landmines.md` §1 — including `/patients/{id}/relevant-records` and the e6-owned search path — and record that w4 does not re-fix them (w4-D-4) | gate: at impl gate, observation in Delivery | ⚠ |
| w4-SPEC-17 | The IDOR writeup artifact shall be present and cite `docs/handover/portal.har` (w4-D-8) | cmd: idor-writeup-present — asserts the writeup exists and references `docs/handover/portal.har` | ⚠ |

**w4-REQ-5 — N+1 note on the production chart-assembly path**

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| w4-SPEC-12 | The N+1 note shall document the production chart-assembly pattern — one `records`-per-encounter query (~37 for the captured chart) plus the full-table scan behind records search — citing `services/records-service/app.py` and the p95=3.8s metric | gate: at impl gate, observation in Delivery | |
| w4-SPEC-13 | The N+1 note shall explain why the per-encounter pattern fails to hold for a real 50+-encounter chart | gate: at impl gate, observation in Delivery | |
| w4-SPEC-18 | The N+1 note artifact shall be present and cite `services/records-service/app.py` (w4-D-8) | cmd: n1-note-present — asserts the note exists and references `services/records-service/app.py` | |

**w4-REQ-6 — never-expiring-session finding (secondary, finding only)**

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| w4-SPEC-14 | The deliverable shall record the never-expiring-session finding — `auth.yaml` declares `SESSION_TIMEOUT: never` (declarative-only) while the behaviour is hardcoded in `services/gateway/security.py`, so a token from a walked-away shared workstation never ends — and shall make no session-expiry code change (w4-D-7) | gate: at impl gate, observation in Delivery | ⚠ |
| w4-SPEC-19 | The session finding shall be present and cite both `services/gateway/auth.yaml` and `services/gateway/security.py` (w4-D-8) | cmd: session-finding-present — asserts the finding exists and references both files | ⚠ |

**w4-REQ-7 — bounded retrieval independent of encounter count**

| ID | EARS | Check | ⚠ |
|---|---|---|---|
| w4-SPEC-15 | The prototype shall assemble the patient view with a number of retrievals independent of the patient's encounter count — not one retrieval per encounter | test: test_kg_prototype.py::test_bounded_retrieval_count | |
| w4-SPEC-16 | For a seeded patient with 50+ encounters, the assembly retrieval count shall stay bounded — demonstrably not the 37-query per-encounter pattern it replaces | test: test_kg_prototype.py::test_bounded_at_scale | |

Exclusions: the live D11 session→patient bind on `GET /patients/{id}/records` (w4-D-4,
TODO-20, §1) — the production route stays defective and its IDOR `xfail`
(`tests/README.md:54`, `docs/landmines.md` §3) stays visible; w4 closes neither ·
`db/schema.sql` is not touched — the KG is a seeded sample, not a schema change (w4-D-1) ·
the records-search corpus bound (e6, e6-D-3 / E6-REQ-1/2/8) · the production-route N+1
fix (w4-D-5) · the session-TTL code fix (w4-D-7, §1 auth; SPEC-14 is finding only) · a
rendered patient-facing UI and patient authentication (w4-D-6, TODO-20) · column-level
PHI encryption (`docs/debt-log.md` cross-cutting table / TODO-12; w4-D-15).

## Delivery

Status: delivery MERGED f471b70 2026-08-18 (squash, PR #87; codex 2 rounds → approve, 2 A / 0 B
/ 0 C, both fixed r1; branch deleted). Resumed after the re-plan re-gated (plan GATED, gate round
7 clean). Verification 1–16 re-run fresh this session. The prior HALTED evidence (baseline
1317→1330/19/1, the E-1..E-5 table) was built against the GATED-2026-08-16 plan reopened at
impl-gate r4; it is superseded and lives in branch history.

Branch `feat/w4-kg-prototype` off `main` @3585cc5 (post-rebase, w4-D-36). The KG schema,
corpus, `assemble.py`, `run.py` and the 13 kg tests landed pre-halt (@7496247, carried through
the rebase) and re-verified green here unchanged. The resumed work landed the exposure-sweep
mechanism and the carried r4 corrections:
- `docs/research/w4-findings.md` · `docs/debt-log.md` · `docs/landmines.md` @b33df90 — the swept
  23-route table, D11 re-sizing + chronology (r4-f5), the owner-approved §1 repoint (w4-D-23),
  and the N+1 count correction to source (37 per-encounter → 37 encounters, r4-f2)
- `tests/test_w4_exposure_sweep.py` @4e3e122 — the 3-function route-set-closure + bidirectional
  exact-cell doc-parity guard (w4-D-20)

Full branch commit list: `git log main..HEAD` — the command is the reference; no frozen
count, because the series moves with every artifact commit (impl-gate r5 f1 closed the
r4-f4 class here: cite the moving series by command, never by a number that goes stale).

**Slices — test-first vs not.** The kg prototype slices (schema/corpus/assemble + the 13 kg
tests) were test-first pre-halt. The sweep slice was test-first: `test_w4_exposure_sweep.py`
was written before the findings sweep table (red on doc-parity, green after the table landed),
and its `test_route_set_closure` validated the 23-route partition against the live gateway
before the table existed. The three doc findings have no behavioural seam — their `gate:`/`cmd:`
rows cover them, not TDD.

**Deviations.** None material. `EXPOSURE_SET`/`EXCLUDED` are hardcoded in-test sets keyed
`(method, path)` with per-route reasons — the mirror-test exclusion-list idiom
(`tests/test_gateway_proxy_error_contract.py`), matching w4-D-20's "in-test partition" and
w4-D-24's classification-complete framing; the two-clause predicate (w4-D-30) is the placement
rule a human applies, not executable code. No planned change-list slice is absent from the diff.

**Live-run evidence** (isolation: `eval/kg` reads only its synthetic in-memory sample — no DB,
no service, no PHI, no §1 zone touched at runtime):
- V7 `.venv/bin/python eval/kg/run.py` → 53 encounters / 53 summaries / 53 labs / 3 retrievals;
  cross-patient 1042→1043 refused at the boundary with retrievals 0.
- V2 `make test-docker` → `1333 passed, 19 deselected, 1 xfailed` (31.29s) = baseline 1317 + 16;
  deselected/xfailed unmoved.
- V11 `make eval` → green, unchanged.
- V1/V3–V6/V8/V12/V15 re-run: 13 kg tests pass, har cite 1, records-service cite 2, session
  cites 1+1, self-contained grep empty, exclusions diff empty, sweep test 3 passed, landmines
  tokens all present (no MISSING — the pre-edit tree is the negative control per w4-D-28).
- Break-then-revert negatives all confirmed, each green after revert: V9 (boundary disabled → 2
  boundary tests red), V10 (per-encounter loop → 2 bounded tests red), V13 (route dropped from
  `EXPOSURE_SET` → closure red), V14 (excluded row deleted → excluded parity red), V16
  (`GET /patients` row deleted → exposure parity red, the prefix case w4-D-31 exists for).

**Traceability.** All 13 pinned kg `test:` cells filled and passing; the 3 sweep-test functions
land the w4-SPEC-11 completeness enforcer. `gate:` rows 9–14 and `cmd:` presence rows 17–19
satisfied (observations above).

**Residuals** (registry IDs only): live D11 session→patient bind — `docs/todo.md` TODO-20;
production-route N+1 fix — `docs/debt-log.md` D8 (w4-D-5); session-TTL fix — `docs/debt-log.md`
cross-cutting "Sessions never expire, single role, no MFA" row (w4-D-7); records-search corpus
bound — e6 (w4-D-3); SPEC-11 verdict-correctness stays the `gate:` human read and the TODO-20
bind fix must update `tests/test_w4_exposure_sweep.py` (w4-D-24). No new residual surfaced —
gate round 7 confirmed the 23-route enumeration complete.

**Impl gate** — 2026-08-18, impl-gated fresh-context (round 6, both agents clean, no findings).
Branch `feat/w4-kg-prototype` @09c0d5d, merge-base `3585cc5`. Baseline observed at gate:
`1333 passed, 19 deselected, 1 xfailed` (`make test-docker`, re-run by the gate agent) = 1317 + 16,
deselected/xfailed unmoved. `gate:` rows 9–14 human-observed clean this gate session against
`docs/research/w4-findings.md`: SPEC-9 HAR repro table `:23-26` matches `docs/handover/portal.har`
(same `Bearer <patient-1042-token>` on both requests, 1042→200 own, 1043→200 sibling) · SPEC-10
both enablers `:42-65`, `db/schema.sql:34` verbatim · SPEC-11 swept 23-route table `:67-119` incl.
`/patients/{id}/relevant-records` and the e6 search path, "does not re-fix" `:126` · SPEC-12 N+1
note `:131-169`, `records-service/app.py:104-140,128-129` comment verbatim in source, p95=3.8s ·
SPEC-13 50+-encounter argument `:152-160` · SPEC-14 `auth.yaml:2` + `security.py:275-279` both
verbatim in source, no code change. Residuals accepted here: the registry-ID list above,
unchanged — nothing new. Push stays human-gated.
