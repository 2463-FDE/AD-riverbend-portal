# W3 Spec (EARS)

> Status: DRAFT
> Source: docs/workflow/w3/requirements.md (AGREED 2026-08-06)
>
> Backfill of record for W3-REQ-1..8 and 10: statements are derived from the requirements
> alone; whether the existing `main` artifacts satisfy them is established at the
> verification step after agreement, and a miss there is a finding, not a spec change.
> W3-REQ-9 (portal chat surface) is new build against this contract.

## 1. Statements

### W3-REQ-1 — conversational eligibility check

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-1 | When a front-desk user asks about a patient's insurance eligibility in the chat interface, the assistant shall return a coverage answer for that patient without the user contacting the payer manually | |
| W3-SPEC-2 | When producing a coverage answer, the assistant shall obtain it through the eligibility verification path, issuing at most one eligibility check per user turn | single agent, one tool (requirements §4 assumption) |

### W3-REQ-2 — visit-scoped memory

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-3 | When a front-desk user sends a follow-up message within a visit conversation, the assistant shall use the context of that visit's earlier turns | |
| W3-SPEC-4 | The assistant shall scope conversation context to a single visit; context from one visit shall never influence or be readable from another visit's or another patient's conversation | negative tests (landmines §3) |
| W3-SPEC-5 | Visit context shall persist only for a configured visit lifetime, after which it is no longer retrievable | |

### W3-REQ-3 — grounded verdicts

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-6 | When the assistant presents a coverage verdict, the verdict shall be one returned by the eligibility verification path for that patient, never generated by the model | AI-output-guardrail landmine |
| W3-SPEC-7 | If the eligibility verification path returns an unknown or unverified result, then the assistant shall present it as unverified and shall never present it as a denial of coverage | negative tests (landmines §3) |

### W3-REQ-4 — bounded payer call

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-8 | When the system issues an outbound payer eligibility request, it shall apply a configured time bound covering connection and response read | ⚠ human-gate adjacency: timeout/breaker values pinned (ADR 0010) |
| W3-SPEC-9 | If the time bound is exceeded, then the system shall abort the payer call and classify the outcome as a typed timeout failure | |
| W3-SPEC-10 | The end-to-end eligibility verification attempt, including any retries, shall hold a request worker no longer than a configured total budget | ⚠ human-gate adjacency: pinned values |

### W3-REQ-5 — outage blast radius is eligibility only

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-11 | While the payer is unavailable or degraded, the registration path shall continue to accept and process submissions | scoped to the eligibility dependency, not TODO-1 |
| W3-SPEC-12 | If eligibility verification fails or times out during a registration, then the registration shall proceed with eligibility marked unverified rather than blocking or failing the registration | |
| W3-SPEC-13 | A payer outage shall not exhaust the registration path's request-handling capacity | RIV-141 non-recurrence |

### W3-REQ-6 — degrade fast during sustained outage

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-14 | If consecutive unusable payer outcomes reach a configured threshold, then the system shall stop issuing outbound payer calls | ⚠ human-gate adjacency: pinned values |
| W3-SPEC-15 | While outbound payer calls are suspended, eligibility requests shall receive an immediate degraded answer with no outbound call | |
| W3-SPEC-16 | When a configured recovery interval elapses, the system shall resume attempting payer calls | |

### W3-REQ-7 — degraded answers are explicit

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-17 | If a coverage verdict cannot be verified, then any answer shown to the front desk shall be explicitly marked as unverified or pending | |
| W3-SPEC-18 | The system shall never present an unverified eligibility outcome as a coverage denial on any front-desk surface | negative tests (landmines §3) |

### W3-REQ-8 — ADR of record

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-19 | The engineering org shall maintain an ADR recording the sync→async and graceful-degradation decision, including its honest limits and open follow-ups | |

### W3-REQ-9 — portal chat surface

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-20 | When a front-desk user opens the assistant in the portal, the portal shall provide a chat surface where they can send a message and see the assistant's reply within the visit's conversation | ⚠ human-gate adjacency: vendor-egress path (D13/D14) |
| W3-SPEC-21 | The chat surface shall be usable only within an authenticated portal session of a role authorized for eligibility work | ⚠ human-gate: auth zone; negative tests (landmines §3) |
| W3-SPEC-22 | If the assistant path fails or is unavailable, then the chat surface shall present a deterministic non-PHI fallback rather than a raw error | |

### W3-REQ-10 — vendor PHI boundary

| ID | Statement | Notes |
|----|-----------|-------|
| W3-SPEC-23 | The chat path shall never include PHI — patient identifiers, member/insurance ids, or free-text patient details — in any request sent to the model vendor | ⚠ human-gate; negative tests (landmines §3) |
| W3-SPEC-24 | If a user's chat message cannot be reduced to the non-PHI request contract, then the message content shall not be forwarded to the vendor | ⚠ human-gate; negative tests (landmines §3) |

## 2. Traceability

| REQ | SPECs |
|-----|-------|
| W3-REQ-1 | W3-SPEC-1, W3-SPEC-2 |
| W3-REQ-2 | W3-SPEC-3, W3-SPEC-4, W3-SPEC-5 |
| W3-REQ-3 | W3-SPEC-6, W3-SPEC-7 |
| W3-REQ-4 | W3-SPEC-8, W3-SPEC-9, W3-SPEC-10 |
| W3-REQ-5 | W3-SPEC-11, W3-SPEC-12, W3-SPEC-13 |
| W3-REQ-6 | W3-SPEC-14, W3-SPEC-15, W3-SPEC-16 |
| W3-REQ-7 | W3-SPEC-17, W3-SPEC-18 |
| W3-REQ-8 | W3-SPEC-19 |
| W3-REQ-9 | W3-SPEC-20, W3-SPEC-21, W3-SPEC-22 |
| W3-REQ-10 | W3-SPEC-23, W3-SPEC-24 |

## 3. Open questions

1. **W3-SPEC-11's statement is false on `main` as written.** Registration is entirely
   non-functional (TODO-1: the portal payload 422s at intake-service, the gateway relays
   it as 200). The Notes column scopes the statement to the eligibility dependency; the
   statement text does not, and the backfill verification reads statements. Reword before
   freeze — e.g. "While the payer is unavailable or degraded, the registration path shall
   not reject or block submissions because of the eligibility dependency" — fix the text,
   not the code.
2. **W3-SPEC-5 contradicts the implemented sliding TTL.** "persist only for a configured
   visit lifetime" — but the TTL refreshes on every write
   (`tests/test_visit_memory.py:138`, `test_ttl_is_sliding_and_created_at_is_preserved`),
   so an active visit's context outlives any fixed lifetime indefinitely. Behavior is
   correct; the statement is wrong. Reword to an inactivity lifetime.
3. **W3-SPEC-18 constraint for the portal surface (carries into the plan).** The shared
   `frontend/app/components/StatusBadge.tsx` maps `pending → warn` and has no entry for
   `unknown` or `inactive` — both fall through to `neutral`, so a genuine denial renders
   *quieter* than a failed check. Reusing the shared component silently breaks
   W3-SPEC-18 on the new surface; the plan needs a separate verdict tone map, and the
   negative-test rule (landmines §3) applies to this path regardless.
