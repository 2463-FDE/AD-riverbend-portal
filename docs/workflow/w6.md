# w6 — HL7 feed trust: parser comprehension, pinned behavior, mapping ADR

Status: requirements DRAFT · PARKED 2026-08-23 (client priority)
Item: legacy-comprehension report on the interop HL7 parser + pinned current behavior + a visible failing test for the AL1/RXA drop + an ADR proposing schema-validated mapping — analysis and design, not the fix.
Baseline at branch: not yet cut

## Decisions

| ID | Stage | Decision | Why (one sentence) |
|---|---|---|---|
| w6-D-1 | req | The existing strict xfail in `tests/test_hl7_parser.py` IS the "failing test" deliverable — cited by the report and strengthened in place with per-field asserts; no new xfails (owner 2026-08-17) | Keeps the landmines §3 pinned xfail count at 1; a count move is a gap move and none is intended here. |
| w6-D-2 | req | Comprehension report lands in `docs/research/` (owner 2026-08-17) | Existing precedent for tracked analysis reports; registries stay registries. |
| w6-D-3 | req | TODO-46 stays parked; the report names it as a known adjacent risk, nothing more (owner 2026-08-17) | Stated deliverable is analysis + tests + ADR with no behavior change; the PHI-logging fix is its own item. |
| w6-D-4 | req | The mapping ADR lands `Accepted` with implementation explicitly deferred to a later item (owner 2026-08-17) | Direction is already decided by D6/RIV-160; matches the ADR 0005 accepted-with-deferral pattern. |

## Requirements

Status: DRAFT
Source: engagement owner ask, 2026-08-17

> We're finally wiring up the feed from the hospital's system — it sends us those HL7
> messages. The integration 'works' in that data shows up, but before we lean on it for
> care I want to actually trust it. Can you go through the parser and make sure it's
> solid? I've attached the parser code and a few sample messages our integration guy
> saved. It's been quietly running for months with zero errors, which is reassuring.
> — Dr. Maya Okonkwo, COO — Riverbend Community Health
>
> Deliverable (as stated): an AI-augmented legacy-comprehension report on the parser
> (what it maps, what it drops, by segment) + characterization tests that pin current
> behavior + a failing test demonstrating the dropped AL1/RXA + an ADR for a
> schema-validated mapping. Not a full rewrite.

| ID | Requirement | Notes |
|----|-------------|-------|
| w6-REQ-1 | The engagement owner (and any clinician-facing reader) has a tracked legacy-comprehension report in `docs/research/` on `services/interop-service/hl7_parser.py`: per-segment account of what is mapped vs. silently dropped, a field-by-field diff of `samples/adt_sample.hl7` against the record the parser produces from it, why `except: pass` makes "zero errors in 3 months" absence of signal rather than evidence of health, and who is first harmed when allergies/meds drop (clinician acting on a record missing a penicillin allergy) and when they would find out. | w6-D-2. ⚠ human-gate — report sits on the PHI documentation surface: sample-message fields are reproduced redact-then-quote per `docs/phi-logging-policy.md` rule 2. Cites D6/RIV-160 as the registered defect, not a new finding; names TODO-46 as adjacent risk without acting on it (w6-D-3). |
| w6-REQ-2 | Current parser behavior is pinned by characterization tests per observable behavior class: mapped PID/PV1 fields, unknown segment swallowed, malformed/short known segment swallowed (IndexError path), partial-record output on mixed input — so the future fix changes behavior under green. | `tests/test_hl7_parser.py` already pins 3 classes; gap is the short/malformed-segment swallow and MSH handling. Extension, not duplication. |
| w6-REQ-3 | The suite visibly demonstrates that AL1 (allergy) and RXA (medication) segments from the sample message do not reach the internal record — a failing-by-design test a reader can point to, with per-field asserts (penicillin allergy, amoxicillin medication). | w6-D-1: the existing strict xfail in `tests/test_hl7_parser.py`, strengthened in place; xfail count stays 1. ⚠ `docs/landmines.md` §3 pinned counts. |
| w6-REQ-4 | An ADR exists recording schema-validated mapping as the design for closing D6: validated segment coverage, explicit handling of unmapped/dropped segments (counted, not silent), field-presence validation — `Accepted`, implementation explicitly deferred to a later item; the parser's behavior does not change this item. | w6-D-4. D6 stays OPEN; `docs/landmines.md` §1 "Brittle HL7 mapping" zone untouched behaviorally. |

Out of scope: fixing the parser or adding AL1/RXA to `SEGMENT_MAP` (client: "not a full
rewrite"; D6 stays OPEN, RIV-160) · implementing dropped-segment counting or schema
validation (that is what the w6-REQ-4 ADR proposes, for a later item) · TODO-46
(parse-failure `log.exception` can log raw HL7 PHI — registered OPEN in
`docs/phi-logging-policy.md`; w6-D-3) · `POST /hl7/ingest` auth posture
(D15/ADR 0016) · HL7 timestamp handling (TODO-21 records interop is unaffected) · any
portal/UI surface — the ask names none and interop has no portal surface (TODO-44
lesson: exclusion stated, not silent).
