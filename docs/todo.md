# To-do register

> **Two eras.** The 2026-08-05 frontend descope and the 2026-08-06 rewrite/tooling decisions split
> this repo's history. Era 1 built a SvelteKit rebuild against inherited tooling; it is parked on
> branch `alt/sveltekit-portal` (spec, design set, scaffold, gate track) with its process plan
> superseded in place. Era 2: the inherited Next.js portal is the only frontend and is permanent,
> new work runs through `docs/workflow/`, and tooling is built from scratch under `.claude/`.
> Era-1 items were either narrowed to whatever substance outlived the plan, or deleted (see the
> retired-id ledger below); this file is an era-2 register with era-1 residue, not a history of both.

> **Registry contract.** `docs/workflow/wN/` owns requirements and acceptance criteria for new work
> (`docs/workflow/README.md`); `docs/specs-deprecated/wN.md` is the historical record for weeks
> shipped before 2026-08-06 and is **not written to**. `docs/debt-log.md` owns risk. This file holds
> only **unscheduled loose ends**, each pointing outward via `src:`. Scheduled work is promoted into
> the target week's workflow bundle and the line here closes or narrows to a pointer. Nothing is
> restated across the three.

**Line format** — one item per line, pipe-separated:

```
- [ ] TODO-n | statement | key: value | key: value
```

Checkbox: `[ ]` open · `[~]` in progress · `[x]` done. Keys, all optional:

| Key | Meaning |
|---|---|
| `tags` | comma-separated. `live-defect`, `phi`, `compliance`, `process`, `memory`, `hygiene` |
| `blocked_by` | a week (`W4`), a debt id (`D11`), or another `TODO-n`. Comma-separated |
| `human_gate` | `yes` = parked awaiting a human decision, not actionable work |
| `src` | where the real detail lives — read that, not this line |

**Dead vocabulary** (era 1; a line still using it is stale, and nothing on `main` can execute the
tool names): `FE-R*`, `G0`–`G6`, `P2`–`P7`, `PIPE-n`/`PG-n`, and `address-review`, `spec-lens`,
`/dashboard`, `feature-start`, `diff-reviewer`, `verify-stack`, `doc-drift`, `pr-open`,
`render-pdf`, `regression-proof`, `memory-lint`. See `CLAUDE.md` §11. Occurrences surviving in
`Superseded` or archived files are records of what happened, not instructions.

IDs are allocated once and **never reused, whether or not a line for them is still here.** A closed
item is kept only while its record still teaches something; the era-1 closures were deleted
2026-08-09 once each one's surviving substance had been verified to live somewhere else, and their
full text is in git history — `git show 22e62f8:docs/todo.md`, or
`git log -S'TODO-n |' -- docs/todo.md` for one id.

**Retired ids — allocated, do not reuse:** 3–8, 14–17, 19, 22–26, 48 (era-1, deleted 2026-08-09);
**28–39** (allocated on branch `feat/noref-day-queue-and-p2-doc-corrections`, the parked day-queue
PR #26; never on `main`).

## Open

- [ ] TODO-2 | Run ADR 0011's `make up` end-to-end PHI drill: two-turn visit chat, then confirm via redis-cli that the stored transcript is metadata-only | tags: phi | src: adr/0011-eligibility-agent-and-visit-memory.md
- [ ] TODO-9 | Capture a real Bedrock response from a live `complete()` / `complete_structured()` call and pin it as a fixture, so the mocks match a shape AWS actually returned | tags: hygiene
- [ ] TODO-10 | Gated live-LLM integration test; needs a real bearer key | blocked_by: TODO-9
- [ ] TODO-11 | Reply to the post-merge PR #2 no-ship (the bot's premise was wrong; `output_config.format` is canonical). Reasoning drafted, user said do not post yet | human_gate: yes
- [ ] TODO-12 | `README.md:1,82` assert PHI is encrypted and the system is fully HIPAA compliant; the schema stores plaintext `TEXT`. Client-facing handoff material, so not a unilateral edit | tags: compliance | human_gate: yes | src: docs/debt-log.md
- [ ] TODO-13 | Delete the merged branch `feat/noref-eligibility-agent-visit-memory`; content is fully in squash `074c346`. Local copy is gone, `remotes/origin/` still has it | tags: hygiene
- [ ] TODO-16 | The frontend UX audit's F1–F8 findings exist only in project memory and the PR bodies that shipped their fixes (F3 → PR #9) — `docs/ux-audit-2026-07.md` was never committed and is not gitignored. ADR 0008's dangling citation was already removed 2026-07-30 (`adr/0008-frontend-date-picker-dependency.md:16-20`), so nothing in the tree is broken; the open choice is to write the doc or drop the claim from memory | tags: hygiene
- [ ] TODO-20 | **Patient-facing surface — open, with no frontend plan behind it.** The blocker was never frontend work: the D11 session→`patient_id` bind (W4, approval-gated under `docs/landmines.md` §1). Still parked: patient **authentication**, the patient principal in `config/roles.yaml`, and the real hostnames. The origin constraint outlives its superseded ADR — a patient surface sharing an origin with the staff app puts an XSS in reach of staff credentials, so any future patient surface needs separate hostnames and host-only cookies whatever serves it (`adr/0015` §4, read as reasoning, not current plan). `docs/specs-deprecated/w4.md` scoped W4 as analysis-plus-prototype with the live IDOR fix landing later, so W4 does not unblock this by itself | tags: process, phi | human_gate: yes | blocked_by: D11 | src: adr/0015-portal-origin-and-audience-separation.md §4
- [ ] TODO-21 | **Slot/appointment instants are stored wrong and it is ours to fix** (user call, 2026-07-31). `db/seed/generate_seed.py:159` builds a naive `datetime(2026,6,23,8,0,0)` and `:166` writes a bare `%Y-%m-%d %H:%M:%S` string into `slots.start_at`/`end_at` (`TIMESTAMPTZ`, `db/schema.sql:71-72`) with the session at `Etc/UTC`, so 8:00 ET is stored as `08:00Z`; same for `appointments.scheduled_for` (`:277`). **HL7 ingest is NOT affected** — `services/interop-service/hl7_parser.py:13` maps only `provider` and `location` from PV1, no timestamp. Fix the stored data, not the formatter: the requirement that used to forbid a display-layer workaround left `main` with the descope, so nothing in the tree stops one. Belongs with the W5 booking work, which is the other reason to touch these tables | tags: hygiene | blocked_by: W5 | src: db/seed/generate_seed.py:159, db/schema.sql:71-72
- [ ] TODO-27 | **Path-based CI routing — considered 2026-07-31, deliberately NOT done.** The measurement is stale and should not be re-used: run `30670931154` was 2m20s total with every skippable job (`tests` 25s, eight `services` shards 9–22s, `secret-scan` 9s) running in parallel with the critical path, so routing saved zero wall clock — but that critical path was `portal` + `docker-build`, and `portal` no longer exists (CI is now `frontend`, `frontend-boot`, `services`, `tests`, `eval`, `secret-scan`, `docker-build`). **Two blockers are structural and stand regardless:** (a) `tests/test_compose_topology.py` reads both `docker-compose.yml` and `.github/workflows/ci.yml`, so a diff that looks confined to one routes around the very tests guarding the other — coupling no path glob can see; (b) workflow-level `paths:` filters never report a required check, so a filtered-out job leaves the PR blocked forever, and only job-level `if:` on a changed-files step is safe. It also weakens the one enforcement point on a HIPAA codebase — CI is the only thing that gates a merge. **Revisit when** the critical path passes ~5 min or one genuinely expensive job appears (browser E2E, image vuln scan): re-measure first, then route that *one* job, job-level `if:`, never `tests` | tags: process, ci | src: .github/workflows/ci.yml
- [ ] TODO-42 | **The drift-gate test file is ~17s of the fast-tier wall** — `tests/test_drift_check.py` is ~25 subprocess-heavy e2e cases. Accepted knowingly at PR #29 r8: the layer-isolation design, where each red case forges or refreshes the earlier layers, is what the coverage buys. The original framing charged this against a pre-push hook running the full suite on every push; **no such hook exists on `main`** (tracked `.claude/` is skills only — `CLAUDE.md` §11), so the cost is plain suite wall-clock. If it starts to bite: a module-scoped pristine-copy fixture for the non-mutating cases, or parametrize the needle-only variants over one copy | tags: process, tests | src: PR #29 r8, tests/test_drift_check.py
- [ ] TODO-43 | **Drift gate blind to eval-code changes** — `eval/rag/corpus.sha256` pins the three data files' raw bytes only; a scoring-definition change in `eval/rag/metrics.py` or a ranking change in `eval/rag/retriever.py` leaves the committed embed-path `REPORT.md` §4 score cells (masked before diffing) describing old code with the gate green. Named residual since PR #29 r6 — the naive fix was rejected there, since byte-hashing the `.py` files forces a minutes-long 3.12 embed re-run on any comment edit — re-raised at r10, conceded, parked as the PR's recorded open item at merge. Candidate fixes, each a new design/CI surface: a comment-insensitive code fingerprint (normalized AST / code-object hash) of the score-producing functions; an eval-code version field written only by the embed regen path; or a CI leg re-running scoring stub-side. Pairs with r4's parked cross-interpreter determinism pin (local 3.8 vs CI 3.12, nothing pins `generate_seed.py` output equality) — same shape, take together if either is taken | tags: process, ci | src: PR #29 r10, eval/rag/check_drift.py, docs/review-loop-metrics.md §4
- [ ] TODO-46 | **interop `log.exception("HL7 parse failed")` can log the raw HL7 message** — `services/interop-service/app.py:54` logs the full traceback on a parse failure, and a parse exception's text can embed the offending segment or the whole raw message: an ADT/ORU carrying name, DOB, MRN, diagnoses — more PHI than any DB-bound row. Found during the PR #34 r1 class sweep and deliberately left out of that PR, because it is rule 3's *parse-error* flavor (input-in-exception), not the DB-error class the PR closed; the safe fix needs a look at what the hand-rolled `hl7_parser.py` actually puts in its exception strings before choosing between class-name-only logging and a metadata projection (message type + control id are probably loggable, nothing else is). Registered OPEN in `docs/phi-logging-policy.md` the same day | tags: phi | src: services/interop-service/app.py:54, docs/phi-logging-policy.md
- [ ] TODO-47 | **Uvicorn access logs record PHI-bearing query strings at INFO** — observed live 2026-08-05 during the PR #34 r1 dynamic log check: the `records-service` access log printed `GET /records/search?q=Quentin+DYNSENT-Q-73`, and `q` is in practice a typed patient name (rule 2, never-loggable). Every service with a query-param identifier surface has this (records `q`; the gateway access log sees every proxied URL), and it is uvicorn's access logger rather than any `log.*` call in our code, so the PR #34 class fix does not touch it. Fix shapes to weigh: disable access logs (`--no-access-log`) and rely on our own request logging, or a uvicorn log filter that strips query strings. Pre-existing, not introduced by any PR | tags: phi | src: services/records-service/app.py:144, docs/phi-logging-policy.md
- [ ] TODO-50 | **`next.config.mjs` re-introduces the build-time inlining that `gateway.ts` says it fixed.** `frontend/app/lib/gateway.ts:3-8` states `GATEWAY_URL` is "read at REQUEST time … never constant-folded into the standalone build" — the fix in `ce21d37`. But `frontend/next.config.mjs:3-5` sets an `env:` block, and that config key does exactly the build-time inlining of `process.env.GATEWAY_URL` the comment rules out, so the value is frozen at image build and a runtime override is silently ignored. It also publishes the internal gateway URL into the **client** bundle, where it is neither needed nor reachable. Two files disagree and only one is right; confirm which behaviour the standalone build actually has before deleting either. Found 2026-08-06 | tags: hygiene | src: frontend/next.config.mjs:3-5, frontend/app/lib/gateway.ts:3-8
- [ ] TODO-51 | **The seed's "one teaching fixture" double-booking is actually N.** `db/seed/generate_seed.py:356-359` plants the deliberate collision on slot 88231 (RIV-175's evidence), but `:364` then picks appointment slots with `s_id = random.randint(88200, slot_id-1)` — no collision check, no correlation with `slots.status`, on a column with no UNIQUE (`db/schema.sql:80`). So an unknown number of additional confirmed-on-the-same-slot pairs exist in any generated seed, and the runbook's duplicate-finding query returns them alongside the intended one. Harmless to the lesson, misleading to anyone measuring RIV-175's blast radius or writing the W5 test vectors against seeded data. Count them before relying on the fixture: `SELECT slot_id, count(*) FROM appointments WHERE status='confirmed' GROUP BY slot_id HAVING count(*) > 1`. Deterministic (`random.seed(2026)`), so the count is stable per generator version | tags: data, hygiene | blocked_by: W5 | src: db/seed/generate_seed.py:356-364, docs/runbook.md
- [ ] TODO-54 | **Two backend-exposure candidates from the W3 mockup cut list, queued for a future stage-1 pass.** The W3 visit-chat mockup (2026-08-07, scratch per `.claude/skills/mockup/`) wanted two values the backend deliberately does not serve; per the exposure rule they are requirement candidates for the owner, not frontend hardcodes: (a) turn count/max in the `POST /ai/visit-chat` response — `AI_VISIT_MAX_TURNS` exists only in config (`services/gateway/config.py:152`), so a client-side turn counter drifts from stored state after truncation; (b) a capability field in `GET /me` — it returns `{username, role}` only (`services/gateway/app.py:245`), so hiding unauthorized nav entries would otherwise need a duplicated role→capability map (approval-gated zone, test-pinned twin). Both are owner asks, not defects; W3 requirements are frozen, so neither enters W3 | tags: process, client | src: services/gateway/app.py:243-245, services/gateway/config.py:152, .claude/skills/mockup/
- [ ] TODO-57 | **68 `CLAUDE.md §n` citations across the estate point at the pre-2026-08-06 numbering** (measured 2026-08-08 as 73 during the TODO-52 sweep, which repointed the ten inside its own five files and stopped there; **recount 2026-08-09** — the 5 in `docs/plans/pipeline-upgrade.md` left the tree with that file, so they are not work any more). The rewrite renumbered everything: old §5 Testing strategy → new §6, and the negative-test rule it carried → `docs/landmines.md` §3; old §6 Landmines/do-not-touch → `docs/landmines.md` §1 (new §5 is only the pointer); old §7 Safety rules → `docs/landmines.md` §2; old §9 Known debt → `docs/debt-log.md`; old §10.1/§10.3 → new §10; old §10.5 Conventions → new §9; old §11 Focus and flow → gone, new §11 is Tooling. Pre-rewrite file: `git show c04806d:CLAUDE.md`. **Not a find-and-replace** — each cite must be read for what it claims, and an unknown share are already correct (anything written after the rewrite: `docs/workflow/*`, `adr/0018`, `.claude/skills/`, and `e1`'s §6 test-baseline cites). Distribution: 23 in ADRs (repoint citations only, never decision text — `adr/0011` is the worked example), 17 in `docs/specs-deprecated/*` (**do not edit**; annotate the archive README as TODO-53 did), 15 in `tests/*.py` and `services/ai-assistant/*.py` comments — **so the sweep is a code PR, not doc-only, and needs review** — 7 in workflow artifacts, 6 in other live docs | tags: process, hygiene | src: CLAUDE.md, docs/landmines.md, docs/debt-log.md
- [ ] TODO-58 | **Three `eval/rag/` artifacts deliberately state a root cause that is no longer true.** (Allocated as TODO-57 in the W2 plan; renumbered at landing per the plan's own re-check instruction — the documentation-drift sweep took TODO-57 in the interim, and ids are never reused.) `eval/rag/REPORT.md:19` ("`services/intake-service/intake.yaml` sets `self_service_intake: true` with `match_key: none`"), its renderer `eval/rag/report.py:143` which hardcodes that sentence as prose rather than reading the file, and `eval/rag/data.py:310`'s docstring calling `"none"` "intake's current behavior". W2 flipped that file to `match_key: ssn_corroborated` (ADR 0005 tier 1, flag-and-review). **They stay, deliberately.** The report is the frozen measured baseline the drift gate compares against (W2-SPEC-9/10, `eval/rag/check_drift.py`), and re-rendering it to say otherwise would move that baseline for a reason unrelated to measurement — the eval keeps simulating both match-key modes, and changing the docstring alone buys nothing while changing the mode moves the numbers. Each sentence is true as of the measurement it reports and false as a statement about `main` after 2026-08-08. **Clears when** whichever change next moves the baseline legitimately (a re-measure after HIM merges the existing duplicates, or tier 2 landing) re-renders the report; correct all three together then. Recorded here rather than only in the W2 workflow bundle because a drift sweep looks in this file, not in `docs/workflow/` | tags: hygiene | src: eval/rag/REPORT.md:19, eval/rag/report.py:143, eval/rag/data.py:310
- [ ] TODO-59 | **`docs/landmines.md` §3 claims a push hook enforces the xfail/deselected counts; no such hook exists.** `docs/landmines.md:127` reads "A push hook pins the expected xfail and deselected counts; if one moves, the gap moved" — but `git ls-files .claude` returns nine `SKILL.md` files and nothing else, and neither the `Makefile` nor `.github/workflows/ci.yml` installs one. The count guard is documentation, not enforcement, so the deliberate-coverage-gap invariant that §3 exists to protect rests entirely on a human remembering to compare against `CLAUDE.md` §6. That is also exactly what `CLAUDE.md` §11 warns about: a hook-only check is advisory, and this one is not even that. **Not a rewrite of the rule** — the rule is right and should stay; what needs correcting is the sentence asserting automated enforcement, either by dropping the claim or by landing the gate in `.github/workflows/ci.yml` where it can actually fail a merge. Found 2026-08-09 during W1 implementation, out of every W1 SPEC's scope, so filed rather than fixed. The gate round that raised this also named `docs/todo.md` TODO-42 as a second instance of the same dead-hook class; **that one is already closed** — PR #67 (`4338d45`) rewrote TODO-42 to state no such hook exists, so `docs/landmines.md:127` is the only live instance | tags: process, hygiene | src: docs/landmines.md:127, CLAUDE.md §6, CLAUDE.md §11
- [ ] TODO-60 | **Two ADRs enumerate the ai-assistant's typed LLM failures without `LLMTimeout`, which W1 added.** `adr/0004:38-39` lists the typed failures as exactly `LLMBudgetExceeded` / `LLMUnavailable` / `LLMConfigError` / `LLMResponseError`; `adr/0009:78-80` (Status **Accepted**, the live provider decision) states the botocore mapping as `LLMUnavailable` for "throttling/5xx/connection after retries" — the branch W1 splits, since a connect/read timeout now raises `LLMTimeout(LLMUnavailable)`. Neither statement is *wrong* about behaviour — `LLMTimeout` subclasses `LLMUnavailable`, so every caller contract in both ADRs still holds — but both read as complete enumerations and are not. **Not a decision reversal**: ADR 0009 stays `Accepted` and its reasoning is untouched; the edit is a citation-level amendment naming the fifth class and the timeout branch, of the same class as the `adr/0006`/`adr/0008` D9 cites W1 deliberately left as decisions-as-taken. Raised as W1 gate round 3 finding 1 and **overruled as a plan finding by the owner 2026-08-09** (doc-registry upkeep, not a design gap), with filing here as the recorded disposition. Found 2026-08-09 during W1 implementation | tags: docs, hygiene | src: adr/0004-ai-assistant-service-and-llm-wrapper.md:38-39, adr/0009-ai-assistant-bedrock-provider.md:78-80, services/ai-assistant/llm_client.py
- [ ] TODO-61 | **Registration now collects an `roi_consent` row, and nothing may read it as a 45 CFR 164.508 authorization.** `e4` added a fifth, optional consent to the intake form ("Release of information") so the form's vocabulary and `ConsentKind` are equal sets — the alternative was amending a frozen spec. The wording points at the Notice of Privacy Practices and authorizes no specific disclosure, and nothing in `roi-service` reads the `consents` table today (verified 2026-08-10). **D12 is untouched and stays open:** ROI still goes out with no recorded 164.508 authorization and no accounting trail, and the fix for that is a real authorization record, not this row. Filed so the next person to work on ROI finds the constraint before the shortcut. Found 2026-08-10 (`e4` plan stage, owner decision) | tags: compliance, phi | src: frontend/app/intake/page.tsx, services/intake-service/schemas.py, docs/debt-log.md D12
- [ ] TODO-67 | **A missing or malformed `submission_id` is a non-correctable 422 surfaced in the correctable-at-the-desk branch.** `e5b` made `submission_id` a required, version-4-only root field of the `POST /intake` contract (`e5b-SPEC-4/11/19`). The portal always mints and sends a valid one, so this cannot happen from the real caller — but a request that reaches intake without a well-formed id (a non-portal client, a truncated body) fails pydantic validation as a 422, and the portal renders the class-4 branch as "some details could not be accepted — please correct them at the desk." The operator cannot correct a machine-minted identifier at the desk, so the message is misleading for this one cause. **Accepted at spec (`e5b-D-6`)** over a fifth portal result branch or an optional field: closed-at-birth, re-derives only if a future spec re-takes the required-field + 422 choice. This is the residual TODO-62 recorded against the closed PR #76 branch, now re-derived on `main` where the design actually landed; TODO-62 stays a closed record and ids are never reused. Found 2026-08-13 (`e5b` delivery) | tags: client, hygiene | src: services/intake-service/schemas.py, frontend/app/intake/page.tsx:99-100, docs/workflow/e5b.md e5b-D-6
- [ ] TODO-68 | **The workflow-doc size-cap exemption list is review-enforced only — no job compares it against base.** The `workflow-doc-cap` job hard-codes its ratchet exemptions (`e5b.md ≤ 750`) inline in `ci.yml`; the shrink-only rule ("never add one, never raise one") lives in a comment at the case list and in PR #80's dispositions-of-record. A later PR can grow the list and still go green — the guard is a human reading the reviewed `ci.yml` diff. **Deferred by owner decision at PR #80 review (AD-R1-M3, rounds 1–2):** a machine comparator (tracked cap manifest + base-vs-head diff job) is the escalation if review enforcement is ever observed to fail, not a default. Found 2026-08-15 (PR #80 codex review) | tags: ci, hygiene | src: .github/workflows/ci.yml workflow-doc-cap job, PR #80 review thread
- [ ] TODO-69 | **The eligibility-assistant contract's Delivery-record budget is spent with three tickets left to land.** `eligibility-assistant-D-55` budgeted the six tickets' `## Delivery` records inside the 61 lines spare under the blocking `workflow-doc-cap` 400-line cap (contract measured at 339 lines when `corpus` cut). Measured growth is 19–25 lines per merged ticket, not the ~10 the budget assumed: 339 → 364 (`corpus`, +25) → 383 (`llm-seam`, +19) → 387 (`retrieval-eval` row and recall block, +4) → 397 (its impl-gate paragraph, +10), leaving 3 lines for `turn`, `lifecycle` and `trace`. The next ticket's record plus its impl-gate paragraph will exceed the cap and block its own merge. **Flagged to the owner at the `retrieval-eval` branch cut 2026-08-30 and deferred by owner decision to that ticket's Delivery write; still open after it.** **Measured again at that merge 2026-08-30: the contract stands at exactly 400/400** — its merge stamp (squash sha, codex round summary, deletion sha) was compressed to the 3 remaining lines and the file now has zero slack, so `turn` cannot write a record row at all without one of the three choices below being taken first. This is no longer a forecast. The choices are the record shape (shorter rows, or per-ticket evidence moved behind the deletion sha as `eligibility-assistant-D-82` already does for deviations), an exemption entry (which TODO-68 notes is review-enforced only), or raising the cap. Found 2026-08-30 (`retrieval-eval` implementation) | tags: process, ci, hygiene | blocked_by: TODO-68 | src: docs/workflow/eligibility-assistant.md ## Delivery, .github/workflows/ci.yml workflow-doc-cap job, eligibility-assistant-D-55 / D-67 / D-82
- [ ] TODO-63 | **The fast-track-lane question for small, defined defects is open and unevidenced.** e6's engagement ask included "ponder the idea of creating a faster track for small, and defined defects"; the owner took the batch-don't-branch answer (`e6-D-1`) and explicitly did **not** buy the instrumented fast-track variant that would have measured this item's per-stage cost. So the lane decision has no measurement behind it, and TODO-27's lesson is that a lane is decided on a measurement, not a hunch. Revisit if the small-defect batch cadence recurs enough to justify buying that measurement; the recommended-not-taken instrumentation is the missing evidence. Found 2026-08-16 (e6 requirements, owner decision) | tags: process | human_gate: yes | src: docs/workflow/e6.md e6-D-1, docs/todo.md TODO-27

## Closed (record)

Compressed to pointers 2026-08-09; the full text of each is in git history.

- [x] TODO-62 | **Allocated on the PR #76 branch; closed 2026-08-13 when that branch was closed unmerged.** Recorded a residual of the branch's registration-idempotency design: a malformed or missing `submission_id` rendered as a correctable-at-the-desk 422 the operator cannot correct (that design's plan decisions D-10/D-11). The design never merged, so the residual never reached `main`; the id stays burned because the landed e5 close-out record (`docs/workflow/e5/findings.md`, `pr-body.md`) cites it. The design question carries to e5b, the successor of e5 chunk 2 | tags: client, hygiene | src: docs/workflow/e5/pr-body.md §Status, docs/workflow/e5/plan.md D-10/D-11
- [x] TODO-66 | **Allocated on the PR #76 branch; closed 2026-08-13 when that branch was closed unmerged.** Recorded the branch design's accepted remount residual: the submission-attempt id lived in `useState` alone, so a refresh after an unconfirmed submit minted a fresh id and forked a second chart — raised at review rounds 7 and 8 and again in the undispositioned round 9, standing open at close. The code never merged, so nothing on `main` carries the defect; the id stays burned because the landed close-out record cites it. Two main-true facts outlive it, carried to e5b's requirements: the portal keeps **no draft** (`frontend/app/lib/session.ts` is the only browser-storage writer), and persisting one writes PHI (name/DOB/SSN/insurance) to browser storage on a shared front-desk workstation — a `docs/landmines.md` §1 human-gated decision of its own | tags: client, phi | src: docs/workflow/e5/findings.md §Review rounds 7–9, docs/workflow/e5/pr-body.md §Status
- [x] TODO-1 | **CLOSED by `e4` (2026-08-10) — registration works, and it fails honestly.** All four layers of the intake contract break are fixed: the portal sends the schema's shape and confirms only on a numeric `patient_id`, the gateway route is on `_post_checked` so a 422 arrives as a 422, intake writes patient + coverage + consents in one transaction, and `ConsentKind` carries the five kinds the form collects. The *class* is guarded rather than the instance: `contracts/intake-registration.json` is asserted from both CI jobs, so either side drifting reddens its own. Deliberately not closed here: the thirteen other `_post`/`_get` proxy routes (`e5`), persisting the verdict (D4 residual 3), and register-first. *(The thirteen were converted and both helpers deleted 2026-08-11 by `e5`, with the six unchecked portal read surfaces; the verdict and register-first remain open.)* | tags: live-defect, phi | src: docs/workflow/e4/, docs/debt-log.md "Intake contract break"
- [x] TODO-55 | **CLOSED by `e4` (2026-08-10).** `tests/test_intake_endpoint.py` drives `POST /intake` through a `TestClient` over intake-service — the first one anywhere in `tests/` — with the DB on in-memory SQLite and the match-key and payer hops faked, so the route's own wiring is asserted rather than its helpers'. The cases are the claims that matter at the desk: a complete submission creates every row, an unknown consent kind writes nothing, a mid-transaction fault leaves nothing behind, and a verification fault after the commit does not undo the registration. Not a deliberate gap, so `docs/landmines.md` §3 is unchanged | tags: tests | src: tests/test_intake_endpoint.py
- [x] TODO-56 | **CLOSED by `e4` (2026-08-10).** The intake confirmation renders the verdict through W3's `VerdictBadge`, with an explicit "Insurance eligibility was not checked" line whenever there is no verdict in the eligibility path's four-value vocabulary — so a degraded or missing check reads as unchecked, never as silently fine. Half of the original finding stands: the verdict still reaches no column (D4 residual 3), so it is on a screen and still not in the database | tags: client, hygiene | src: frontend/app/intake/page.tsx, docs/debt-log.md D4
- [x] TODO-18 | Landed 2026-07-31 in squash `aec6ef2` (PR #22): ADRs 0013 + 0014, the ADR 0012 §3 amendment, and the accompanying doc edits | tags: hygiene
- [x] TODO-40 | **CLOSED in PR #29 (r3+r4).** `db/seed/*.csv` and `generate_seed.py` were two independent hardcoded copies of the same five fixture patients with nothing keeping them in agreement — the duplicated-source-of-truth shape in data form. Closed by one direction of derivation: the generator now reads the shared columns from the CSVs (second copy deleted), and `check_drift.py` byte-checks `seed.sql` against the generator | tags: process, data | src: db/seed/generate_seed.py, eval/rag/check_drift.py, PR #29
- [x] TODO-41 | **CLOSED in PR #29 (r6).** The drift gate was blind to the eval's *inputs*: score cells are masked (unreproducible without the model), so anything reaching the report only through them drifted green. Closed by `eval/rag/corpus.sha256`, which pins the raw bytes of all three input files and is checked before the report diff; the embed-path `run.py` writing the default REPORT.md location is the only writer, refreshing report + fingerprint in one step. Residual — nothing can verify the §4 *scores* against new inputs without the model — is TODO-43 | tags: process, ci | src: eval/rag/check_drift.py, eval/rag/corpus.sha256, PR #29 r6
- [x] TODO-44 | **CLOSED by W3 (`docs/workflow/w3/`) — the missing surface is built.** Two AI features had been delivered without a usable screen; `/assistant` is now a front-desk chat over `POST /ai/visit-chat` (`frontend/app/assistant/page.tsx`, proxied by `frontend/app/api/ai/visit-chat/route.ts`, sixth nav entry in `AppShell.tsx`). The PHI-boundary review happened at spec and plan stage: no §1 zone edited, no boundary moved (`tests/test_visit_chat_phi.py:239` pins clerk text terminating at ai-assistant), `ai.use` still enforced gateway-side only. **The durable lesson outlived the item and is owned elsewhere** — the UI-surface-in-the-ask rule is `.claude/skills/requirement-synthesis/SKILL.md:33`, stage 1, not restated here. Residuals filed rather than fixed: TODO-56, TODO-54 | tags: process, client, phi | src: docs/workflow/w3/, .claude/skills/requirement-synthesis/SKILL.md:33
- [x] TODO-45 | **CLOSED by `e1` (ADR 0018).** Nothing started the frontend container, so a build-clean, boot-broken UI shipped green. All three pieces landed: `/healthz` in the Next.js app (`frontend/app/healthz/route.ts`), a compose `healthcheck` polling it, and a `frontend-boot` CI job that `docker run`s the production image and polls it, wired into the terminal `docker-build` fan-in via `needs` so branch protection sees it. Never introduced by the descope — `frontend` had no healthcheck in any commit | tags: ci, process | src: .github/workflows/ci.yml, docker-compose.yml
- [x] TODO-49 | **CLOSED 2026-08-06 — both halves in effect, reached outside the pipeline plan.** `CLAUDE.md` is tracked in-repo (rewritten 2026-08-06 from a full repo read; prior file at `git show c04806d:CLAUDE.md`) and `.claude/` is tracked with machine-local exclusions recorded in `.gitignore` and `CLAUDE.md` §11. The original tail is void rather than deferred — it concerned worktrees and a parent-directory copy outside this clone | tags: process, hygiene | src: CLAUDE.md §11, .gitignore
- [x] TODO-52 | **CLOSED 2026-08-08 — five files corrected in place, none by deletion.** `tests/README.md` reorganised by area with the two closed gaps recorded as *what closed them* (ADR 0010 breakers; ADR 0017 `test_gateway_authz.py`) and TODO-55's gap added; `docs/runbook.md` now describes ADR 0010's live bounds and names the gitleaks `secret-scan` job; `docs/onboarding-seam-map.md` walls re-stated honestly (`.env` gitignored but credentials still in history and un-rotated; ADR 0017's four roles plus the legacy `staff` row); `docs/debt-log.md` retracts the `SESSION_SECRET` blast radius as fiction (nothing in `services/gateway/` reads it; sessions are opaque UUID4 Redis keys) while keeping the secret on the rotation list; `adr/0011` got a dated citation-repoint header with **no decision text edited**. Turned up and deliberately not fixed there: 73 further stale `CLAUDE.md §n` cites → TODO-57 | tags: process, hygiene
- [x] TODO-53 | **CLOSED 2026-08-08 — live docs reconciled; every remaining dead name sits behind a header that already declares it historical.** Its precondition was met first: a real replacement process existed (`docs/workflow/README.md`, `.claude/skills/`), so each dead name resolved against something real. `adr/_template.md` keeps the rule that a fix which is a decision needs a decision record, and now states that nothing enforces it automatically; `adr/0011`'s stale tooling cite repoints to `.claude/skills/impl-gate/`. The ten archived `wN.md` were **not** edited — the archive README forbids it — so their corrections land once in `docs/specs-deprecated/README.md`. Left as record by design at the time: the dated measurements in `docs/review-loop-metrics.md` §4 and `adr/0013` (superseded). The third — `docs/plans/pipeline-upgrade.md`, then the largest single holder of dead names — was **deleted 2026-08-09** instead of kept; the register's retired-id ledger carries the one property that outlived it | tags: process
