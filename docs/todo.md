# To-do register

> Loose ends that are queued but not scheduled. This file is the **source of truth** for the
> to-do lane of the status dashboard (`make status`) — the generator parses it, so keep the
> line format. Requirements belong in `docs/specs/`, debt belongs in `docs/debt-log.md`;
> neither is restated here.

**Line format** — one item per line, pipe-separated:

```
- [ ] TODO-n | statement | key: value | key: value
```

Checkbox: `[ ]` open · `[~]` in progress · `[x]` done. Keys, all optional:

| Key | Meaning |
|---|---|
| `tags` | comma-separated. `live-defect`, `phi`, `compliance`, `process`, `memory`, `hygiene` |
| `blocked_by` | gate (`G0`), requirement (`FE-R3`), or another `TODO-n`. Comma-separated |
| `human_gate` | `yes` = parked awaiting a human decision, not actionable work |
| `src` | where the real detail lives — read that, not this line |

IDs are allocated once and never renumbered. Completed items stay, checked.

## Items

- [ ] TODO-1 | Intake contract break: portal payload 422s at intake-service, gateway relays 200, UI reports success; no patient row created. Fix shape resolved 2026-07-30 — folded into P2, `ConsentKind` widened by two values; the defect stays live on `main` until G2 | tags: live-defect, phi | blocked_by: G2 | src: docs/specs/frontend-rebuild.md §8 #1 and §8.1, docs/debt-log.md
- [ ] TODO-2 | Run ADR 0011's `make up` end-to-end PHI drill: two-turn visit chat, then confirm via redis-cli the stored transcript is metadata-only | tags: phi | src: adr/0011-eligibility-agent-and-visit-memory.md
- [x] TODO-3 | P0.5 design tokens — palette, type scale, density, component states; last artifact before G0 | src: docs/design/05-design-tokens.md (direction F, chosen 2026-07-30 by polled healthcare worker)
- [ ] TODO-4 | Commit or discard the in-flight W4 work left uncommitted on main (`docs/specs/w4.md`, `w4-multiagent.html`, two PDFs) | tags: hygiene
- [ ] TODO-5 | EARS rewrite of `docs/specs/w4.md` §5 into requirement IDs, so `address-review`'s design gate can reference IDs that exist | tags: process | src: docs/specs/_template.md
- [ ] TODO-6 | W3 portal chat UI: addendum inside `w3.md` (§4 deliverable + new §5b EARS block `W3-UI-R…`), not a new spec file and not a retro-rewrite of shipped prose criteria | src: docs/specs/w3.md
- [~] TODO-7 | Frontend test harness — ADR written (0013: Vitest, two projects, real-browser component tests); the harness itself lands in P2 with the contract fixture | blocked_by: G1 | src: adr/0013-frontend-test-harness.md
- [ ] TODO-8 | `CLAUDE.md` §9 still lists RIV-088/RIV-141 as `[~]` with W3 agent work pending, but PR #14 shipped it | tags: process
- [ ] TODO-9 | Capture a real Bedrock response from a live `complete()` / `complete_structured()` call and pin it as a fixture, so the mocks match a shape AWS actually returned | tags: hygiene
- [ ] TODO-10 | Gated live-LLM integration test; needs a real bearer key | blocked_by: TODO-9
- [ ] TODO-11 | Reply to the post-merge PR #2 no-ship (bot's premise was wrong; `output_config.format` is canonical). Reasoning drafted, user said do not post yet | human_gate: yes
- [ ] TODO-12 | `README.md:1,82` assert PHI is encrypted and the system is fully HIPAA compliant; the schema stores plaintext `TEXT`. Client-facing handoff material, so not a unilateral edit | tags: compliance | human_gate: yes | src: docs/debt-log.md
- [ ] TODO-13 | Delete the merged branch `feat/noref-eligibility-agent-visit-memory` (local + remote); content is fully in squash `074c346` | tags: hygiene
- [x] TODO-14 | Memory page `frontend-rebuild-plan` and the memory index both say `FE-R1`–`FE-R20`; the spec runs to `FE-R26` | tags: memory
- [ ] TODO-15 | Four of the five P0.5 review questions came back unanswered — row density (Dense vs Comfortable), identity-strip field order, whether the allergy banner reads urgent without becoming noise, and dark-mode appetite. Density currently defaulted to Dense against a stronger mis-click argument on the ROI queue | human_gate: yes | src: docs/design/05-design-tokens.md §7
- [ ] TODO-16 | `docs/ux-audit-2026-07.md` is cited by ADR 0008 but was never committed (`git log --all` empty, not gitignored). F1–F8 exist only in project memory + the PR bodies that shipped fixes. Either write the doc or drop the claim from memory | tags: hygiene | src: adr/0008-frontend-date-picker-dependency.md
- [x] TODO-17 | **Pre-P2 decision set CLOSED** — #1 on 2026-07-30 (intake fix folded into P2; `ConsentKind` widened by `financial_responsibility_ack` + `communications_opt_in`, carrying the `FE-R22` re-proof); #11–#15 on 2026-07-31: `adapter-node` multi-stage image · **token held BFF-side behind an `httpOnly` cookie + 10-min idle logoff (ADR 0014, `FE-R27`/`FE-R28`)** · `portal/` on 3071 · P2 = login + minimum intake path · `svelte-check` + eslint. Also added `FE-R29` (no patient data in web storage) and reopened decision #16 (patient surface) | tags: process | src: docs/specs/frontend-rebuild.md §8
- [ ] TODO-20 | **Decision #16 — patient-facing surface, reopened 2026-07-31.** Blocked on D11 session→`patient_id` binding (W4, §6 approval, G4), not on frontend work. `FE-R27`–`R29` already survive it, so it blocks nothing in P2. **Origin half RESOLVED same day by `adr/0015-portal-origin-and-audience-separation.md`** — separate hostnames, host-only cookies (`FE-R30`), `ORIGIN` from runtime env (`FE-R31`), patient surface kept out of the staff route tree. What is still parked: patient **authentication** (D11), the patient principal in `config/roles.yaml` (G4), and the real hostnames (TODO-19). W4 does **not** unblock this — `docs/specs/w4.md:138-139` makes it analysis-plus-prototype with the live IDOR fix landing later | tags: process, phi | human_gate: yes | blocked_by: G4 | src: docs/specs/frontend-rebuild.md §8 #16, adr/0015-portal-origin-and-audience-separation.md
- [ ] TODO-21 | Slot/appointment instants are stored wrong and it is **ours to fix** (user call, 2026-07-31, closing spec §8 #9's ownership question). Scope measured, not estimated: `db/seed/generate_seed.py:159` builds a naive `datetime(2026,6,23,8,0,0)` and `:166` writes a bare `%Y-%m-%d %H:%M:%S` string into `slots.start_at`/`end_at` (`TIMESTAMPTZ`, `db/schema.sql:71-72`) with the session at `Etc/UTC`, so 8:00 ET is stored as `08:00Z`; same for `appointments.scheduled_for` (`:277`). **HL7 ingest is NOT affected** — `services/interop-service/hl7_parser.py:13` maps only `provider` and `location` from PV1, no timestamp. `FE-R26` forbids compensating for this in the formatter | tags: hygiene | blocked_by: G5 | src: docs/specs/frontend-rebuild.md §8 #9
- [x] TODO-22 | **G1 RE-AFFIRMED by the user 2026-07-31**, on the narrowed `FE-R17` and the corrected Alternatives row; `docs/status/fe-verdicts.md:61` now records both the original signature and the re-affirmation. Original entry: **G1 needs an explicit re-affirmation before P2 code starts.** `adr/0012` was amended 2026-07-31 in the criterion G1 was reviewed on: `FE-R17` narrowed from "interactive element" to "button or link" and the Alternatives row scoring it was corrected, because `svelte-check` turned out to be silent on form-control names and defeated by spread attributes. The signature record itself carries the superseded reasoning — `docs/status/fe-verdicts.md:61` reads "Next.js ... lost on `FE-R17` + form ergonomics". The decision looks sound on its remaining grounds (rules on by default vs opt-in, form ergonomics across five forms, continuity as one criterion against several), so this is a re-affirmation and not a re-decision — but it is the user's call, not an inheritance | tags: process | human_gate: yes | src: adr/0012-frontend-framework-sveltekit.md §5 and §Audit-round corrections, docs/status/fe-verdicts.md
- [ ] TODO-23 | `.claude/gates/state.json` — **G0/G1 half FIXED 2026-07-31** (both now `signed`; bar turned `off` at the user's request, so the tracker is dormant until `/gates on`). **Still stale:** it lists 7 requirements at G2 when G2 now carries **14** — counted from §5's table, not its prose (`FE-R27`–`R29` from ADR 0014, `FE-R30`–`R31` from ADR 0015, `FE-R32`, and `FE-R33` added 2026-08-01; the "12" here was already stale before `FE-R33`) — and `gates.sh` has **no subcommand that adds a requirement id** (`req` only flips `todo`/`done`), so closing this needs a `gates.sh` change, not a `/gates` invocation; the skill forbids hand-editing the JSON. Untracked tooling, so it ships in no PR | tags: process, hygiene | src: docs/status/fe-verdicts.md, docs/specs/frontend-rebuild.md §6
- [ ] TODO-24 | `docs/status/fe-verdicts.md` has no verdict rows for `FE-R27`–`FE-R33`, so `make status` emits seven warnings and the dashboard counts them open-by-default rather than by record. Mechanical: add seven rows in the existing format. **Count corrected 2026-08-01** — this read "`FE-R27`–`FE-R31`, five rows" and was already one short (`FE-R32` has no row either); `FE-R33` makes seven. The file tops out at `FE-R26`. Tracked file, so it belongs in a PR — deliberately left out of the audit-corrections PR, which was scoped to `adr/` + the spec + this file | tags: hygiene | src: docs/status/fe-verdicts.md
- [x] TODO-18 | Uncommitted on branch `docs/noref-frontend-test-harness-adr`: ADRs 0013 + 0014, the ADR 0012 §3 amendment, and edits to the spec, `docs/debt-log.md`, `CLAUDE.md` and this file. **Landed 2026-07-31 in squash `aec6ef2` (PR #22); the branch no longer exists locally.** | tags: hygiene
- [ ] TODO-19 | Later-phase decisions, parked deliberately: spec §8 #3 role tier (P5/G4), #5 component gallery + #6 unverified small-viewport behaviour (P3), #7 queue read endpoint (P6/G5), #10 E2E (needs its own artifact-retention ADR). **Added 2026-07-31 by the pre-P2 audit:** `axe-core` for the accessible-name coverage `FE-R17` does not reach — form controls, spread attributes, `role="button"` — decided at P3 against real primitives (ADR 0013 gap #9); ADR 0015 gap #1, the production hostnames and who terminates TLS (client input, blocks nothing while the demo is a `localhost` screen-share). **#9 removed from this line — now owned, see TODO-21** | tags: process | src: docs/specs/frontend-rebuild.md §8, §8.5
- [ ] TODO-25 | **Cutover / rollback story is unwritten and `FE-R15` is the whole of it** — "retain the original portal as a separately runnable service until `FE-R1`–`FE-R3` pass". Missing: cutover criteria beyond those three requirements, a data-path divergence check (both apps write intake through the same gateway, so a disagreement is silent), how and when the Next.js portal is retired, and who notices if the two disagree. The 2026-07-31 pre-P2 audit was framed to cover this and did not — it went at the ADRs' internal contradictions instead. **User call 2026-07-31: an answer comes later.** Worth resolving before G2 is signed, not after | tags: process | human_gate: yes | src: docs/specs/frontend-rebuild.md `FE-R15`, §6
- [x] TODO-26 | **RESOLVED 2026-07-31 (user): drop the free-text field, collect a "Policy holder is the patient" checkbox.** Written into spec §8.1 (with the measurement), spec §8 #14's row, and `docs/debt-log.md` — including the debt it leaves: no policy-holder identity is captured at all, and naming a non-patient policy holder would need an `InsuranceCoverage` column + hand-synced migration. Original entry: **`insurance.policy_holder` keep-or-drop is orphaned.** `docs/specs/frontend-rebuild.md:253` and `docs/debt-log.md:276` both route it to spec §8 decision #14, but #14 closed 2026-07-31 answering a different question (what P2 builds) and never mentions it. Measured 2026-07-31: no schema field, no DB column, and the only consumer is `frontend/app/intake/page.tsx:147` `policy_holder_is_self: !ins.policy_holder` — a **boolean derived from emptiness**; the name string reaches nothing but the Review display at `:366`, and everything downstream (`ai-assistant/schemas.py:66`, `templates.py:92`, `gateway/app.py:446`) sees only the bool. Recommendation: drop the free-text field in P2 and collect the bit as an explicit checkbox, on `FE-R21`'s own principle, recording in the debt log that a real policy-holder-name requirement would need a column. Blocks the P2 contract fixture's shape | tags: process, phi | human_gate: yes | src: docs/specs/frontend-rebuild.md §8.1, docs/debt-log.md
- [ ] TODO-27 | **Path-based CI routing (skip backend jobs on frontend-only diffs) — considered 2026-07-31, deliberately NOT done.** Measured on run `30670931154`: whole workflow is **2m20s**, and the jobs routing would skip (`tests` 25s, the eight `services` shards 9–22s, `secret-scan` 9s) run **in parallel with** `portal` (42s), so skipping them saves **zero wall clock** — the critical path is `portal` + `docker-build` (81s), and `docker-build` is exactly the job a portal change needs. Two blockers beyond the missing payoff: (a) `tests/test_compose_topology.py` reads `docker-compose.yml:21` **and `.github/workflows/ci.yml:253`**, so a "frontend-only" diff editing the portal's compose block or CI's env seeding would route around the very tests guarding it — the coupling is invisible to any path glob; (b) workflow-level `paths:` filters never report a required check, so a filtered-out job leaves the PR blocked forever — only job-level `if:` on a changed-files step is safe. Also weakens the one enforcement point on a HIPAA codebase (CLAUDE.md §10.1 — CI is the only thing that gates a merge). **Revisit when** the critical path passes ~5 min or one genuinely expensive job appears (browser E2E, image vuln scan, or the compose-level smoke with the gateway chain attached that `FE-R32` deferred to P2 PR 2). Then route that *one* job, job-level `if:`, never `tests` | tags: process, ci | src: .github/workflows/ci.yml, docs/specs/frontend-rebuild.md `FE-R32`
- [ ] TODO-28 | **Carry the `/healthz` smoke's body assertion into P2 PR 2** (user call 2026-08-01, deferred from PR #25 r3 rather than dropped). CI's `Portal image serves /healthz` step asserts status only — `curl -fsS` exits 22 on any status >= 400, so codex r3's stated worry ("a 500 that returns bytes should pass") is already false. The residual is **200 with a wrong body**, today unreachable: `portal/src/routes/healthz/+server.ts:15` is a single unbranched `json({status:'ok'})`. P2 PR 2 is when it becomes reachable — ADR 0014's Consequences require non-200 when the cookie key is absent/unusable and ADR 0015 §3 the same for a missing `ORIGIN`, and those guards land with the session module. Both are specified non-200, so `-f` still catches them; the assertion exists to catch the **drift** where a later change reports fail-closed as `200 {"status":"degraded"}` instead. Do three things together: assert the body contains `"status":"ok"` in the CI step (grep, not exact match — key order and whitespace must not make it brittle), extend `test_ci_starts_the_portal_image_and_polls_the_port_compose_publishes` so the body check cannot be silently dropped, and widen `FE-R32` from "answer with 200" to "200 and a body reporting ok" | tags: ci, process | src: .github/workflows/ci.yml, docs/specs/frontend-rebuild.md `FE-R32`, portal/src/routes/healthz/+server.ts
- [ ] TODO-29 | **Close the gap between the chip derivation rule and the chip values, when the stylesheet lands in P2.** `05-design-tokens.md` §2 states the chips are "derived from the palette by one rule ... so they cannot drift when a token moves", but the rule exists only as prose: the P2 mockup at `docs/design/06-p2-mockups.html:201-204` hardcodes eight literals (`#754c07`, `#dde7e3`/`#1a5238`/`#9ab8aa`, `#f1dfde`/`#88241f`/`#d49f9c`, `#3e3c37`). Hardcoded derived output is normal practice *when a generator emits it* (Style Dictionary, a Tailwind config); here nothing does, so moving `--warn`/`--ok`/`--alert` leaves the chips silently behind. Fix when `portal/` first gets CSS — it has none today, so this costs nothing now and is a re-measurement of four chips later. §2 rules 2–3 are directly expressible: `color-mix(in oklab, var(--ok) 15%, var(--surface))` for the tint, 45% for the border. Rule 4 (darken ink until it clears 7:1 against both the plain and zebra row) is **not** expressible in `color-mix` — keep the four ink values explicit and let TODO-30 hold them. **Browser-support caveat, added 2026-08-01:** `color-mix()` needs Chrome 111+ / Safari 16.2+ / Firefox 113+, and an unsupported browser drops the whole declaration as invalid — so a chip renders with **no fill at all**, which is worse than a stale literal. Clinic workstation browsers are unknown (`05-design-tokens.md` §7), so either emit the literals from the palette at build time (the alternative below, no runtime support question) or precede each `color-mix` with a plain-hex fallback declaration. Do not ship runtime `color-mix` as the only source of a chip fill until the browser question is answered. Alternative if a build step is preferred: emit the literals from the palette instead, same outcome | tags: process | blocked_by: G2 | src: docs/design/05-design-tokens.md §2, §6.5, docs/design/06-p2-mockups.html:198-204
- [ ] TODO-30 | **Make the contrast floors a test in `make test-frontend`, not arithmetic in a docs page.** Every value in `05-design-tokens.md` §1/§2/§6 is a measured ratio against the 7:1 / 4.5:1 / 3:1 floors §1 pins, and §6 exists because four pairings failed at proposal time — so the measurement is load-bearing, not decorative. The `lum`/`ratio`/`fixInk` arithmetic that produced those numbers lives as JS inside `docs/design/05-token-directions.html`, which nothing runs; a palette change today means re-measuring ~20 pairings by hand with no gate catching a miss. Port `lum`/`ratio` into a Vitest test that reads the token values and asserts each documented floor, including each chip ink against **both** the plain and the zebra row (§2 rule 4 reports the worse of the two) and the three §6 corrections that are the reason the current values differ from the specimen's. Note this is not the industry default — that is axe/pa11y against rendered pages, which `portal/` cannot have until P3 primitives exist; pair-testing is available now and reaches the values axe would not see until a page uses them. Complements rather than replaces the `axe-core` decision parked in TODO-19 | tags: process | blocked_by: G2 | src: docs/design/05-design-tokens.md §1, §2, §6, docs/design/05-token-directions.html, adr/0013-frontend-test-harness.md
- [ ] TODO-31 | **Two AI features were delivered to the client without a usable surface — scheduled as P7/G6 on 2026-08-01.** `POST /ai/visit-chat` (W3's eligibility assistant, `services/gateway/app.py:938`, ADR 0011 visit memory) has **never had a UI in either portal** — `frontend/app/api/ai/` contains exactly one directory, `intake-instructions`. `POST /ai/intake-instructions` (W1) has one, but only in the legacy portal (`frontend/app/intake/page.tsx:141`), which the rebuild retires. W1's client ask was literally "get something on the screen this week"; W3's was "a little **chat assistant** for the front desk". **Root cause is a spec-shape gap, not an oversight to re-litigate:** neither `w1.md` nor `w3.md` §4 lists a UI deliverable, and "frontend"/"portal"/"UI"/"screen" appear across all three W1–W3 specs exactly once — inside W1's verbatim quote. Before writing `wN.md` for any week whose ask names something the client expects to *see*, put the surface in §4 or state in §3 that it is out. Also: nothing tracked this — `visit-chat` and `intake-instructions` appeared in zero files under `docs/todo.md`, `docs/debt-log.md`, `docs/status/` before this entry, and the client's Jira export has four tickets, none AI, so no client-side artifact would ever have surfaced it. P7 inherits a PHI-boundary review since `/ai/*` is the only vendor-egress path (CLAUDE.md §6, D13/D14 open) | tags: process, client, phi | blocked_by: G5 | src: docs/specs/frontend-rebuild.md §8 #17, docs/specs/w1.md §4, docs/specs/w3.md §4
- [ ] TODO-32 | **No by-provider appointment query exists, and the obvious one is unsafe.** `GET /schedule` shipped without a `provider_id` filter — the filter was written, then cut in codex PR #26 r1. Reason: appointments store only a provider *name* (`appointments.provider`, free-text `TEXT`), so the only route to a provider id is `appointments.slot_id` → `slots`, and that column has **no foreign key** (`db/schema.sql:78`) while `book()` inserts any positive `slot_id` without checking the slot exists (`services/scheduling-service/book.py`). An inner join through it means an appointment with a missing or stale slot appears in the all-day queue and vanishes from the per-provider one, with no error — a clinician missing a visit. Rejected alternative: resolving `provider_id` → name and matching `Appointment.provider` text, which is nullable and misspellable, so it fails the same way less predictably. The real fix is provider identity stored on the appointment, i.e. a column + hand-synced migration (§6 approval-gated) — schedule it with **RIV-175 (W5)**, which already has to touch the slot/appointment relationship for the UNIQUE constraint, so the two migrations should be one decision and one ADR. The clinician's-own-day surface stays unserved until then (`docs/design/01-operators-and-tasks.md` §4) | tags: scheduling | blocked_by: W5 | src: docs/design/01-operators-and-tasks.md §4, docs/specs/frontend-rebuild.md §8 #7, services/scheduling-service/app.py
- [ ] TODO-33 | **`log.exception` on the sibling scheduling reads can render PHI into a traceback.** `list_slots` and `list_appointments` (`services/scheduling-service/app.py:62,89`) both call `log.exception` in their DB-failure branch, which renders the exception and its traceback into the log record. A driver error raised mid-fetch can carry row data in its message, and `list_appointments` returns one patient's `reason` free text. `GET /schedule` was changed to log the exception CLASS only (codex PR #26 r1 pre-push pass) — the same rule the gateway's `_post_checked` already follows — but the siblings were **deliberately not swept**: they are inherited code outside that diff, and widening a review round into untouched handlers trades a known surface for an unreviewed one. This is D1 (PHI in logs). Sweep with the W1/W10 logging work, and add the `tests/test_redaction.py`-style end-to-end scan for each. Same defect class, different blast radius | tags: phi, logging | src: services/scheduling-service/app.py, docs/debt-log.md D1
- [ ] TODO-34 | **The booking write path never persists `scheduled_for`, so the column is NULL for every appointment created after handoff.** `frontend/app/appointments/page.tsx:62` posts only `{patient_id, slot_id, provider, reason}` — it *has* the time (it picked the slot from `/slots`, which returns `start_at`) and discards it; `BookingRequest.scheduled_for` defaults to `None` (`schemas.py:81`), `insert_appointment` writes the NULL, and `db/schema.sql:83` has no default. Only the seed generator populates it, which is why this looks correct in dev. Found by the pre-push adversarial pass on codex PR #26 r1. **Read-path mitigated, not fixed:** `GET /schedule` now filters and sorts on `COALESCE(scheduled_for, slots.start_at)` via an outer join, so the day queue works today — but an appointment with a NULL time *and* a dangling `slot_id` (possible: no FK on `slot_id`, and `book()` never checks the slot exists) has no time source anywhere and is invisible to every day view. The durable fix is to populate the column at booking time, which means editing `book.py` — the deliberately-frozen raw-psycopg2 RIV-175 race path — so it belongs with the W5 booking work and the TODO-32 migration, as one decision and one ADR, not three separate edits to the same table | tags: scheduling, data | blocked_by: W5 | src: frontend/app/appointments/page.tsx:62, services/scheduling-service/book.py, db/schema.sql:83
- [ ] TODO-35 | **Slot availability is derived from two sources that no code keeps in agreement, and one of them is dead data.** `slots.status` is written by **nothing** in the repo — `grep -rn "UPDATE slots\|Slot.status" services db` returns exactly one hit, the read at `services/scheduling-service/app.py:60` — so booking never marks a slot `'booked'` and cancelling never releases one. The values come only from `db/seed/generate_seed.py:165`, which randomly seeds ~25% of slots `'booked'`. Two consequences pointing opposite ways: (a) `GET /slots` filters `Slot.status == "open"`, so the seed's pre-booked slots are permanently unofferable even after their appointment is cancelled — the clinic loses the time with no surface anywhere, and after `FE-R34` the day queue no longer shows the cancelled row that was the only incidental signal; (b) every slot booked at runtime stays `'open'` and keeps being offered, with `book.py`'s `slot_taken` — which reads `appointments`, not `slots` — as the only guard. That guard is itself an **allowlist**, `WHERE slot_id = %s AND status = 'confirmed'` (`services/scheduling-service/book.py:27`), so an appointment in any other state reads as absent: a `'completed'` visit's slot can be booked over, and both rows then render in the same day queue. Note the asymmetry with the day queue's deliberate **exclusion** predicate — the queue shows every status but `'cancelled'`, the booking guard counts only `'confirmed'`, and each status added later widens the disagreement. Found by the pre-push adversarial pass on codex PR #26 r2 (its framing of (a) as "booking marks the slot booked" was checked and is wrong — nothing writes the column at all). Fixing this means deciding whether slot state is derived or stored, then editing `book.py`, the frozen RIV-175 race path — so it belongs with **W5 / RIV-175** alongside TODO-32 and TODO-34, as one decision and one ADR over the slot/appointment relationship, not four separate edits to the same tables | tags: scheduling, data | blocked_by: W5 | src: services/scheduling-service/app.py:60, services/scheduling-service/book.py:27, db/seed/generate_seed.py:165
