# To-do register

> **Scope, as of 2026-08-05: the inherited Next.js portal is the only frontend.** The SvelteKit
> rebuild is descoped and parked on branch `alt/sveltekit-portal` — its spec, design set, scaffold
> and gate track are there, not here. Every item below that existed only to serve it is closed
> with a pointer to that branch; items whose substance is backend were re-homed, not deleted.

> **Registry contract.** `docs/specs/wN.md` owns requirements and acceptance criteria.
> `docs/debt-log.md` owns risk. This file holds only **unscheduled loose ends**, each pointing
> outward via `src:`. When work is scheduled it is promoted into the target week's spec, and the
> line here closes or narrows to a pointer. Nothing is restated across the three.

**Line format** — one item per line, pipe-separated:

```
- [ ] TODO-n | statement | key: value | key: value
```

Checkbox: `[ ]` open · `[~]` in progress · `[x]` done. Keys, all optional:

| Key | Meaning |
|---|---|
| `tags` | comma-separated. `live-defect`, `phi`, `compliance`, `process`, `memory`, `hygiene` |
| `blocked_by` | a week (`W4`), a debt id (`D11`), or another `TODO-n`. Comma-separated. Gate ids (`G0`–`G6`) and `FE-R*` requirements are dead vocabulary — they belonged to the rebuild spec |
| `human_gate` | `yes` = parked awaiting a human decision, not actionable work |
| `src` | where the real detail lives — read that, not this line |

IDs are allocated once and never renumbered. Completed items stay, checked. **Ids 28–39 are
allocated on branch `feat/noref-day-queue-and-p2-doc-corrections`** (the parked day-queue PR #26)
and must not be reused here, even though they are absent from this file.

## Items

- [ ] TODO-1 | **Intake contract break — live on `main`, and now unscheduled.** Portal payload 422s at intake-service, the gateway relays it as 200, the UI reports success, and no patient row is created. The fix shape was resolved 2026-07-30 and folded into the frontend rebuild's P2 (`ConsentKind` widened by `financial_responsibility_ack` + `communications_opt_in`); **the rebuild is descoped, so the plan went with it and the defect did not.** Nothing about this defect was ever frontend-specific — three of its four layers are backend (intake-service's schema, the gateway's error-swallowing `_post`, and the missing shared fixture). Everything needed to fix it is in `docs/debt-log.md` under "Intake contract break": the four layers, the payload table, and the consent-enum decision. **Needs a curriculum week**; it is the highest-value unscheduled item in this file | tags: live-defect, phi | src: docs/debt-log.md "Intake contract break"
- [ ] TODO-2 | Run ADR 0011's `make up` end-to-end PHI drill: two-turn visit chat, then confirm via redis-cli the stored transcript is metadata-only | tags: phi | src: adr/0011-eligibility-agent-and-visit-memory.md
- [x] TODO-3 | **CLOSED — the artifact is parked, not lost.** Was: P0.5 design tokens (palette, type scale, density, component states), delivered as direction F and chosen 2026-07-30 by a polled healthcare worker. `docs/design/05-design-tokens.md` left `main` in the 2026-08-05 frontend descope; it is on branch `alt/sveltekit-portal` with the rest of the design set | tags: hygiene
- [x] TODO-4 | **CLOSED 2026-08-05 — each file resolved, none discarded silently.** Was: commit or discard the in-flight W4 work left uncommitted on main. `docs/specs/w4.md` landed earlier under its own week; `w4-multiagent.html` and its render are now `docs/design/w4-multi-agent-assembly.html`/`.pdf`, pointed at from `w4.md` §4 as sketch-not-deliverable. Of the other two PDFs: the P2 mockup render was frontend-rewrite material and went to `alt/sveltekit-portal` with its HTML source, and the model-comparison deck was personal and left the repo | tags: hygiene | src: docs/specs/w4.md §4
- [ ] TODO-5 | EARS rewrite of `docs/specs/w4.md` §5 into requirement IDs, so `address-review`'s design gate can reference IDs that exist | tags: process | src: docs/specs/_template.md
- [ ] TODO-6 | W3 portal chat UI: addendum inside `w3.md` (§4 deliverable + new §5b EARS block `W3-UI-R…`), not a new spec file and not a retro-rewrite of shipped prose criteria | src: docs/specs/w3.md
- [x] TODO-7 | **CLOSED — parked, see `alt/sveltekit-portal`.** Was: the frontend test harness (ADR 0013 — Vitest, two projects, real-browser component tests), due to land in P2 with the contract fixture. It shipped in PR #25 and was removed from `main` with the rebuild. **The gap it was written to close is open again and now unowned:** there is no JavaScript test harness in this repository, so the intake contract break's whole defect *class* — two sides of a payload that no shared fixture asserts — is unguarded | tags: process | src: adr/0013-frontend-test-harness.md (Status), docs/debt-log.md
- [ ] TODO-8 | `CLAUDE.md` §9 still lists RIV-088/RIV-141 as `[~]` with W3 agent work pending, but PR #14 shipped it | tags: process
- [ ] TODO-9 | Capture a real Bedrock response from a live `complete()` / `complete_structured()` call and pin it as a fixture, so the mocks match a shape AWS actually returned | tags: hygiene
- [ ] TODO-10 | Gated live-LLM integration test; needs a real bearer key | blocked_by: TODO-9
- [ ] TODO-11 | Reply to the post-merge PR #2 no-ship (bot's premise was wrong; `output_config.format` is canonical). Reasoning drafted, user said do not post yet | human_gate: yes
- [ ] TODO-12 | `README.md:1,82` assert PHI is encrypted and the system is fully HIPAA compliant; the schema stores plaintext `TEXT`. Client-facing handoff material, so not a unilateral edit | tags: compliance | human_gate: yes | src: docs/debt-log.md
- [ ] TODO-13 | Delete the merged branch `feat/noref-eligibility-agent-visit-memory` (local + remote); content is fully in squash `074c346` | tags: hygiene
- [x] TODO-14 | Memory page `frontend-rebuild-plan` and the memory index both said `FE-R1`–`FE-R20` while the spec ran to `FE-R26`. Fixed at the time; moot since the 2026-08-05 descope took the spec off `main` | tags: memory
- [x] TODO-15 | **CLOSED — questions about screens no longer being built.** Was: four of the five P0.5 review questions came back unanswered (row density Dense vs Comfortable, identity-strip field order, whether the allergy banner reads urgent without becoming noise, and dark-mode appetite), with density defaulted to Dense against a stronger mis-click argument on the ROI queue. Reopen only if a frontend rebuild is ever rescheduled; the record is `docs/design/05-design-tokens.md` §7 on `alt/sveltekit-portal` | tags: hygiene
- [ ] TODO-16 | `docs/ux-audit-2026-07.md` is cited by ADR 0008 but was never committed (`git log --all` empty, not gitignored). F1–F8 exist only in project memory + the PR bodies that shipped fixes. Either write the doc or drop the claim from memory | tags: hygiene | src: adr/0008-frontend-date-picker-dependency.md
- [x] TODO-17 | **Pre-P2 decision set CLOSED 2026-07-31, and moot since the 2026-08-05 descope** — every decision in it was about the rebuilt portal: the `adapter-node` multi-stage image, the token held BFF-side behind an `httpOnly` cookie with a 10-min idle logoff (ADR 0014), `portal/` on 3071, P2 as login plus the minimum intake path, `svelte-check` + eslint, no patient data in web storage, and the reopened patient-surface question (which survives as TODO-20). The one whose substance outlived the plan — the intake fix, with `ConsentKind` widened by `financial_responsibility_ack` + `communications_opt_in` — is now TODO-1 and `docs/debt-log.md`. Full record on `alt/sveltekit-portal` | tags: process
- [ ] TODO-20 | **Patient-facing surface — open, and now with no frontend plan behind it.** The real blocker is unchanged and was never frontend work: the D11 session→`patient_id` bind (W4, CLAUDE.md §6 approval-gated). The origin/audience half was decided by `adr/0015` — separate hostnames, host-only cookies, `ORIGIN` from runtime env, the patient surface kept out of the staff route tree. That ADR is superseded with the rebuild, but its §4 reasoning is not: a patient surface sharing an origin with the staff app puts an XSS in reach of staff credentials, and any future patient surface inherits that constraint whatever serves it. Still parked: patient **authentication** (D11), the patient principal in `config/roles.yaml`, and the real hostnames. `docs/specs/w4.md` scopes W4 as analysis-plus-prototype with the live IDOR fix landing later, so W4 does not unblock this by itself | tags: process, phi | human_gate: yes | blocked_by: D11 | src: adr/0015-portal-origin-and-audience-separation.md, docs/specs/w4.md
- [ ] TODO-21 | Slot/appointment instants are stored wrong and it is **ours to fix** (user call, 2026-07-31). Scope measured, not estimated: `db/seed/generate_seed.py:159` builds a naive `datetime(2026,6,23,8,0,0)` and `:166` writes a bare `%Y-%m-%d %H:%M:%S` string into `slots.start_at`/`end_at` (`TIMESTAMPTZ`, `db/schema.sql:71-72`) with the session at `Etc/UTC`, so 8:00 ET is stored as `08:00Z`; same for `appointments.scheduled_for` (`:277`). **HL7 ingest is NOT affected** — `services/interop-service/hl7_parser.py:13` maps only `provider` and `location` from PV1, no timestamp. The rebuild spec carried a requirement forbidding a display-layer workaround; that requirement left `main` with the descope, so nothing now stops one — fix the stored data, not the formatter. Belongs with the W5 booking work, which is the other reason to touch these tables | tags: hygiene | blocked_by: W5 | src: db/seed/generate_seed.py:159, db/schema.sql:71-72
- [x] TODO-22 | **G1 was re-affirmed by the user 2026-07-31**, on the narrowed `FE-R17` and the corrected Alternatives row. Moot since 2026-08-05: G1 gated the SvelteKit framework choice, ADR 0012 is superseded, and the verdict record left `main` with `docs/status/fe-verdicts.md`. Kept as the trace of a decision that was re-affirmed rather than inherited — the reasoning is on `alt/sveltekit-portal` | tags: process
- [x] TODO-23 | **CLOSED — the gate tracker is retired with the rebuild.** Was: `.claude/gates/state.json` listed 7 requirements at G2 when G2 carried 14, and `gates.sh` had no subcommand that adds a requirement id, so closing it needed a tooling change rather than a `/gates` invocation. G0–G6 and `FE-R*` no longer exist on `main`, so there is nothing left to keep in sync. The tracker is untracked tooling and shipped in no PR | tags: process, hygiene
- [x] TODO-24 | **CLOSED — the file it was about is gone.** Was: `docs/status/fe-verdicts.md` had no verdict rows for `FE-R27`–`FE-R33`, so `make status` emitted seven warnings and counted them open-by-default rather than by record. Both the verdicts file and `make status` were removed in the 2026-08-05 descope | tags: hygiene
- [x] TODO-18 | Uncommitted on branch `docs/noref-frontend-test-harness-adr`: ADRs 0013 + 0014, the ADR 0012 §3 amendment, and edits to the spec, `docs/debt-log.md`, `CLAUDE.md` and this file. **Landed 2026-07-31 in squash `aec6ef2` (PR #22); the branch no longer exists locally.** | tags: hygiene
- [x] TODO-19 | **CLOSED — parked, see `alt/sveltekit-portal`.** Was: the rebuild's deliberately later-phase decisions — role tier, component gallery, unverified small-viewport behaviour, the queue read endpoint, E2E (which needed its own artifact-retention ADR), `axe-core` for the accessible-name coverage `FE-R17` could not reach, and ADR 0015's production hostnames. Every one of them is a question about the rebuilt portal. The one that outlives it — who terminates TLS and on what hostnames — is client input that blocks nothing today | tags: process
- [x] TODO-25 | **CLOSED by the descope — there is no cutover.** Was: the cutover/rollback story was unwritten and `FE-R15` ("retain the original portal as a separately runnable service") was the whole of it, missing cutover criteria, a data-path divergence check, a retirement trigger, and who notices if the two apps disagree. The rebuild never reached parity, so nothing was ever cut over and there is nothing to roll back to. **What it leaves behind is the inverse question:** the Next.js portal is now permanent, and its two known defects — registration that fails silently, and the bearer token in `localStorage` — have no replacement scheduled | tags: process | src: docs/debt-log.md
- [x] TODO-26 | **RESOLVED 2026-07-31 (user): drop the free-text field, collect a "Policy holder is the patient" checkbox.** Recorded in `docs/debt-log.md`, including the debt it leaves: no policy-holder identity is captured at all, and naming a non-patient policy holder would need an `InsuranceCoverage` column plus a hand-synced migration. Measured 2026-07-31: no schema field, no DB column, and the only consumer is `frontend/app/intake/page.tsx:147` `policy_holder_is_self: !ins.policy_holder` — a **boolean derived from emptiness**; the name string reaches nothing but the Review display at `:366`, and everything downstream (`ai-assistant/schemas.py:66`, `templates.py:92`, `gateway/app.py:446`) sees only the bool. The decision stands under the descope — it is about what intake stores, not about which framework renders the form; the measurement's original write-up is on `alt/sveltekit-portal` | tags: process, phi | src: docs/debt-log.md
- [ ] TODO-27 | **Path-based CI routing (skip jobs on unrelated diffs) — considered 2026-07-31, deliberately NOT done, and cheaper to skip now than it was then.** Measured on run `30670931154`: the whole workflow was **2m20s**, and the jobs routing would skip (`tests` 25s, the eight `services` shards 9–22s, `secret-scan` 9s) ran **in parallel with** the critical path, so skipping them saved zero wall clock. The critical path was `portal` + `docker-build`; the `portal` job is gone with the rebuild, which shortens the workflow and removes the only job a frontend-only diff would have wanted to isolate. Two blockers beyond the missing payoff still stand: (a) `tests/test_compose_topology.py` reads both `docker-compose.yml` and `.github/workflows/ci.yml` (`test_ci_seeds_every_env_file_the_topology_requires`), so a diff that looks confined to one of them routes around the very tests guarding the other — coupling no path glob can see; (b) workflow-level `paths:` filters never report a required check, so a filtered-out job leaves the PR blocked forever, and only job-level `if:` on a changed-files step is safe. It also weakens the one enforcement point on a HIPAA codebase — CI is the only thing that gates a merge. **Revisit when** the critical path passes ~5 min or one genuinely expensive job appears (browser E2E, image vuln scan). Then route that *one* job, job-level `if:`, never `tests` | tags: process, ci | src: .github/workflows/ci.yml
- [x] TODO-40 | **CLOSED in PR #29 (r3+r4). Was: `db/seed/*.csv` and `db/seed/generate_seed.py` were two independent hardcoded copies of the same five fixture patients, with nothing keeping them in agreement.** The generator never read the CSVs — its own `fixture_patients` literal produced `seed.sql` → the database, while the CSVs/goldset fed the RAG eval only; they agreed on every shared column when checked 2026-08-02, but a seed change made only in the generator would leave the CSVs *and* `eval/rag/REPORT.md` stale together with the drift gate green — the §10.1 duplicated-source-of-truth shape in data form, where the copy nothing checks is the one that goes stale. Closed by taking the named cheapest durable fix: one direction of derivation — `generate_seed.py` reads its fixture rows' shared columns from the CSVs (r4, the second copy deleted), and `check_drift.py` byte-checks `seed.sql` against the generator (r3). IDs 28–39 are allocated in other working trees and ride their own PRs; the gap here is deliberate, not a renumber | tags: process, data | src: db/seed/generate_seed.py, eval/rag/check_drift.py, PR #29
- [x] TODO-41 | **CLOSED in PR #29 (r6). Was: the drift gate was blind to §4's inputs** — the committed report is embed-path output whose scores cannot be reproduced without the model, so the gate masks the score cells, and everything reaching the report *only* through them drifted green: goldset's `expected_patient_id`/`expected_answer`/`description` (never rendered — run.py scope), encounters.csv's `summary`/`provider`/`encounter_type`/`occurred_at` (feed only masked cells), and every patients/encounters row outside an SSN candidate cluster (§1/§2 render cluster rows only; reproduced live by the r2 pre-push pass). Closed by the corpus fingerprint: `eval/rag/corpus.sha256` pins the raw bytes of all three input files, checked before the report diff; red instructs regenerating the embed report then `check_drift.py --write-fingerprint`, committed together (r7 amendment: that standalone writer was itself the next finding — it could bless inputs the report never saw; deleted, and `run.py --retriever embed` writing the default REPORT.md location is now the only corpus.sha256 writer, refreshing report + fingerprint in one step). Raw bytes rather than the sketched rendered-document hash, deliberately: document rendering skips columns (a non-clustered patient's ssn/dob/address) and that gap is this blind-spot class again. Kept decoupled from score stability — a model/hardware change cannot turn the gate red. Residual, named in the script header: nothing can verify the §4 *scores* against new inputs without the model; the paired REPORT.md + corpus.sha256 hunks in review are that guard | tags: process, ci | src: eval/rag/check_drift.py, eval/rag/corpus.sha256, PR #29 r6
- [ ] TODO-42 | **Drift-gate test file doubles pre-push hook latency** — `tests/test_drift_check.py` is ~25 subprocess-heavy e2e cases (~17s of the fast-tier wall), and `.claude/hooks/xfail-invariant.sh` runs the full suite on every push (13.7s warm baseline before this file). Accepted knowingly at PR #29 r8 (diff-reviewer low): the layer-isolation design (each red case forges/refreshes earlier layers) is what the coverage buys. If hook latency starts to bite: module-scoped pristine-copy fixture for non-mutating cases, or parametrize the needle-only variants over one copy | tags: process, tests | src: PR #29 r8, tests/test_drift_check.py
- [ ] TODO-43 | **Drift gate blind to eval-code changes** — `eval/rag/corpus.sha256` pins the three data files' raw bytes only; a scoring-definition change in `eval/rag/metrics.py` or a ranking change in `eval/rag/retriever.py` leaves the committed embed-path REPORT.md §4 score cells (masked before diffing) describing old code with the gate green. Named residual since PR #29 r6 (naive fix rejected there: byte-hashing the `.py` files forces a minutes-long 3.12 embed re-run on any comment edit); re-raised at r10, conceded, parked as the PR's recorded open item at merge. Candidate fixes, each new design/CI surface: comment-insensitive code fingerprint (normalized AST / code-object hash) of the score-producing functions; an eval-code version field written only by the embed regen path; or a CI leg re-running scoring stub-side. Pairs with r4's parked cross-interpreter determinism pin (local 3.8 vs CI 3.12, nothing pins `generate_seed.py` output equality) — same shape, take together if either is taken | tags: process, ci | src: PR #29 r10, eval/rag/check_drift.py, docs/review-loop-metrics.md §4
- [ ] TODO-44 | **Two AI features were delivered to the client without a usable surface, and the plan that was to give them one is gone.** `POST /ai/visit-chat` (W3's eligibility assistant, `services/gateway/app.py:938`, ADR 0011 visit memory) has **never had a UI in any portal** — `frontend/app/api/ai/` contains exactly one directory, `intake-instructions`. `POST /ai/intake-instructions` (W1) has one, in the Next.js portal (`frontend/app/intake/page.tsx:141`), which is now the only portal and is no longer being retired — so W1's surface is safe and W3's is missing. W1's client ask was literally "get something on the screen this week"; W3's was "a little **chat assistant** for the front desk". This was scheduled as the rebuild's P7/G6 on 2026-08-01 and is **unscheduled again** since the 2026-08-05 descope; it needs a curriculum week, and building it means writing a screen in the inherited Next.js app. **Root cause is a spec-shape gap, not an oversight to re-litigate:** neither `w1.md` nor `w3.md` §4 lists a UI deliverable, and "frontend"/"portal"/"UI"/"screen" appear across the W1–W3 specs exactly once — inside W1's verbatim quote. Before writing `wN.md` for any week whose ask names something the client expects to *see*, put the surface in §4 or state in §3 that it is out. Whoever builds it inherits a PHI-boundary review: `/ai/*` is the only vendor-egress path (CLAUDE.md §6, D13/D14 open) | tags: process, client, phi | src: docs/specs/w1.md §4, docs/specs/w3.md §4, services/gateway/app.py:938
- [ ] TODO-45 | **Nothing starts the frontend container, so a build-clean, boot-broken UI ships green.** `docker compose build` in CI proves every image compiles and nothing more; the `frontend` service has no compose `healthcheck`, so `make ps` reports it up the moment the container starts, whether or not Next.js is serving. This is the fail-closed-guard-with-no-health-signal shape: a green dashboard over a dead service. **Not introduced by the 2026-08-05 descope** — `frontend` has never had a healthcheck, in any commit (`git log --all -S healthcheck -- docker-compose.yml`), and the CI step removed with the rebuild started `portal`, never `frontend`, so runtime coverage of the Next.js app was zero before and is zero after. Raised by codex on PR #31 as a consequence of the descope; recorded here because the risk is real even though the attribution is not. Fix shape: a health route in the Next.js app, a compose `healthcheck` polling it, and a CI step that starts the image and curls it — the same three pieces `FE-R32` specified for the portal, which are on `alt/sveltekit-portal` and can be read rather than re-derived | tags: ci, process | src: .github/workflows/ci.yml, docker-compose.yml, docs/runbook.md
- [ ] TODO-46 | **interop `log.exception("HL7 parse failed")` can log the raw HL7 message** — `services/interop-service/app.py:54` logs the full traceback on a parse failure, and a parse exception's text can embed the offending segment or the whole raw message: an ADT/ORU carrying name, DOB, MRN, diagnoses — more PHI than any DB-bound row. Found during the PR #34 r1 class sweep; deliberately left out of that PR because it is rule 3's *parse-error* flavor (input-in-exception), not the DB-error class the PR closes, and the safe fix needs a look at what the hand-rolled `hl7_parser.py` actually puts in its exception strings before choosing between class-name-only logging and a metadata projection (message type + control id are probably loggable, nothing else is). Registered OPEN in `docs/phi-logging-policy.md` the same day (PR #33 register-first pattern) | tags: phi | src: services/interop-service/app.py:54, docs/phi-logging-policy.md
- [ ] TODO-47 | **Uvicorn access logs record PHI-bearing query strings at INFO** — observed live 2026-08-05 during the PR #34 r1 dynamic log check: `records-service` access log printed `GET /records/search?q=Quentin+DYNSENT-Q-73` — and `q` is in practice a typed patient name (rule 2 never-loggable). Every service with a query-param identifier surface has this (records `q`, and the gateway access log sees every proxied URL), it is uvicorn's access logger, not any `log.*` call in our code, so the PR #34 class fix does not touch it. Fix shapes to weigh: disable access logs (`--no-access-log`) and rely on our own request logging, or a uvicorn log filter that strips query strings. Pre-existing, not introduced by any PR | tags: phi | src: services/records-service/app.py:144, docs/phi-logging-policy.md
- [~] TODO-48 | **Pipeline upgrade in flight** — staged, gated spec→plan→impl pipeline (work packages PIPE-1…PIPE-6, gates PG-0…PG-3). Plan approved with amendments 2026-08-05, PG-0 passed same day (gate ledger: docs/review-loop-metrics.md §6). This line closes when PIPE-1…6 are checked in the plan | tags: process | src: docs/plans/pipeline-upgrade.md
- [ ] TODO-49 | **CLAUDE.md re-tracked in-repo, reversing PR #32** (decided at PG-0 with OD-1: `.claude/` tracked too, excluding settings.local.json, gates/state.json, scheduled_tasks.lock, `__pycache__`). Outstanding tail after both PIPE-1 PRs merge: rebase-or-retire the `riverbend-demo` worktree (tip 07a0c0b carries no CLAUDE.md), and only then rename `../CLAUDE.md` → `../inactive-claude.md` — rename first would leave that worktree with no rules at all (plan amendment a1) | tags: process, hygiene | blocked_by: TODO-48 | src: docs/plans/pipeline-upgrade.md PIPE-1
- [~] TODO-50 | **phi-secret-guard + xfail-invariant scan the main checkout even when the command targets another repo** — was: both matched `git commit`/`git push` by substring but acted on `${CLAUDE_PROJECT_DIR:-$PWD}`, so `git -C <other> commit` or a `cd <worktree> && git commit` compound was checked against the wrong index (and the substring matcher never even fired on `git -C <dir> commit`). **Claude-side closed in PR #36 r2, fail-closed**: subcommand-position git commit/push matcher + cross-tree deny of any repo-redirection (`-C`, `--git-dir`/`--work-tree`, `GIT_DIR=`/`GIT_WORK_TREE=`/`GIT_INDEX_FILE=` env prefixes, `cd`/`pushd` compound, session cwd elsewhere) unless every target provably resolves onto this checkout's git index (`rev-parse --absolute-git-dir` identity, so nested checkouts like `.claude/worktrees/` regression-proof trees deny too — path containment was the r2 diff-reviewer's reproduced high). False-deny on legit cross-tree commits is deliberate — run those from a session rooted in that tree. Residual (accepted): quoted command text mentioning a redirection form (heredoc/echo/commit-message strings) can false-deny — observed live on first post-fix use; fail-closed by design, `ALLOW_CROSS_TREE_GIT=1` (user-confirmed) skips only the cross-tree check, scan/suite still run. **PR #36 r4 closed the Claude-side scan-timing tail, also fail-closed**: the guards were validating the wrong *state*, not the wrong *repo* — phi scanned the index before a one-shot `commit -a`/pathspec/`git add && git commit` restaged it, and xfail ran the suite against the working tree instead of the pushed ref. Both fixed by allowlist (index-safe commit flags only; clean `git status --porcelain` required) so an unanticipated shape false-denies instead of bypassing; escapes `ALLOW_ONE_SHOT_COMMIT=1` / `ALLOW_UNVERIFIED_PUSH=1`. **Remaining, and now PROMOTED from follow-up to the fix: the git-native gate** — tracked `.githooks/` + `core.hooksPath` wired via Make target (worktree-shared, covers manual commits; must chain the existing `.git/hooks` ignore-delete + shadow guards, snapshots README ownership moves). r4's pre-push pass found 6 defects in the new shell-text classifier, 2 of them [high] bypasses (only the first git invocation was classified; the staging half was a blocklist that missed `checkout`/`reset`/`stash pop --index`/`apply --cached`/`update-index`/`cherry-pick -n`), and three of the six were shell-text parsing defects in code written that round — which ticks the r3 trend flag past its stated threshold (`docs/review-loop-metrics.md`). **The Claude-side guards are therefore interim: they fail closed and the reproduced bypasses are shut, but no further shell-text shape should be chased inside them.** Accepted residuals that only the git-native gate closes: the lexical match denies commands that merely QUOTE a commit shape (kept deliberately — narrowing to command position would let `bash -c "git commit -am x"` through; pinned by harness cases), an escaped one-shot still scans the pre-stage index, and a non-HEAD push still validates HEAD's tree. Own design one-pager + PR; hooks-only enforcement stays advisory (CLAUDE.md §10.1) until then | tags: process, phi | src: .claude/hooks/phi-secret-guard.sh, .claude/hooks/xfail-invariant.sh, PR #36 r2, PR #36 r4
- [ ] TODO-51 | **diff-reviewer pack cost scales with branch size, not round delta** — the r1 pass on PR #36 read the full 46-file/~3k-line branch diff to review a ~60-line round fix (129k tokens). Legit tuning knob: verify-stack §6 pack spec (it owns the doctrine) could scope repeat-round packs to round-delta + prior findings + files they touch. §6 edit, not a spawn-prompt hack; weigh against the front-loading value of whole-branch re-reads | tags: process, tokens | src: .claude/skills/verify-stack/SKILL.md §6
