node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Vitest browser-mode artifacts. `screenshotFailures: false` in vite.config.ts is
# what actually stops these being written (ADR 0013 §2 — images of rendered,
# patient-shaped components are a CI artifact surface this repo declined to
# acquire). These lines are the second net, not the control.
.vitest-attachments/
__screenshots__/
