# Test/dev dependencies (superset of what the unit tests import).
pytest==8.3.4
redis==5.2.1
requests==2.32.3
httpx==0.28.1
pydantic==2.10.4
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
fastapi==0.115.6
PyYAML==6.0.2
boto3==1.40.0
# ai-assistant observability (ADR 0006); requires Python >= 3.10.
langsmith==0.10.5
# eligibility-assistant (ADR 0019): policy retriever tool + agent loop pins, mirrored
# from services/ai-assistant/requirements.txt so the unit tests import them.
langchain==1.3.16
langchain-core==1.6.0
langgraph==1.2.11
