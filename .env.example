ENVIRONMENT=development

DB_HOST=postgres
DB_PORT=5432
DB_NAME=riverbend
DB_USER=riverbend_app
DB_PASSWORD=changeme

REDIS_URL=redis://redis:6379/0

PAYER_API_URL=https://edi.example.com/v1/eligibility
PAYER_API_KEY=changeme

HL7_FEED_HOST=hl7.example.org
HL7_FEED_PORT=2575

SESSION_SECRET=changeme

# Anthropic API (ai-assistant service). Copy this file to .env (gitignored,
# never tracked) and add your real key there only. Note: an older .env was
# previously committed — those values are still in git history and must be
# rotated (see docs/debt-log.md remediation runbook).
ANTHROPIC_API_KEY=changeme
ANTHROPIC_MODEL=claude-opus-4-8
